(self["webpackChunkfidus_writer"] = self["webpackChunkfidus_writer"] || []).push([["editor"], {
"./js/modules/bibliography/tools.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  dateToYear: function() { return dateToYear; },
  litToText: function() { return litToText; },
  nameToText: function() { return nameToText; }
});
// Extract a display year from an EDTF date string.
// Handles simple years, ranges (2023/2024), uncertainty (?, *, u), and open-ended dates.
function dateToYear(dateStr) {
    if (!dateStr) {
        return ""
    }
    // Take the first date component (before "/" for ranges or open-ended dates)
    const firstPart = dateStr.split("/")[0]
    // Strip uncertainty/wildcard characters to get a clean year
    return firstPart.replace(/[?*u~]/g, "")
}

// Takes any richtext text field as used in bibliography and returns the text contents
function litToText(litStringArray) {
    let outText = ""
    litStringArray.forEach(litString => {
        if (litString.type === "text") {
            outText += litString.text
        }
    })
    return outText
}

function nameListItemToString(nameListItem) {
    let nameString = ""
    if (nameListItem["family"]) {
        nameString += litToText(nameListItem["family"])
        if (nameListItem["given"]) {
            nameString += `, ${litToText(nameListItem["given"])}`
        }
    } else if (nameListItem["literal"]) {
        nameString += litToText(nameListItem["literal"])
    }
    return nameString
}

function nameToText(nameList) {
    let nameString
    switch (nameList.length) {
        case 0:
            nameString = ""
            break
        case 1:
            nameString = nameListItemToString(nameList[0])
            break
        case 2:
            nameString = `${nameListItemToString(nameList[0])} ${gettext(
                "and"
            )} ${nameListItemToString(nameList[1])}`
            break
        default:
            nameString = `${nameListItemToString(nameList[0])} ${gettext(
                "and others"
            )}`
            break
    }
    return nameString
}


}),
"./js/modules/citations/citeproc_sys.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  citeprocSys: function() { return citeprocSys; }
});
/* import */ var biblatex_csl_converter__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/biblatex-csl-converter@3.6.0/node_modules/biblatex-csl-converter/lib/index.js");
/* Connects Fidus Writer citation system with citeproc */


class citeprocSys {
    constructor(bibDB) {
        this.bibDB = bibDB
        this.abbreviations = {
            default: {}
        }
        this.abbrevsname = "default"
        // We cache values retrieved once.
        this.items = {}
        this.missingItems = []
    }

    retrieveItem(id) {
        if (!this.items[id]) {
            if (this.bibDB.db[id]) {
                const cslGetter = new biblatex_csl_converter__rspack_import_0.CSLExporter(this.bibDB.db, [id])
                const cslOutput = cslGetter.parse()
                Object.assign(this.items, cslOutput)
            } else {
                this.missingItems.push(id)
                this.items[id] = {author: [{literal: ""}], type: "article", id}
            }
        }
        return this.items[id]
    }

    getAbbreviation(_dummy, obj, _jurisdiction, vartype, key) {
        try {
            if (this.abbreviations[this.abbrevsname][vartype][key]) {
                obj["default"][vartype][key] =
                    this.abbreviations[this.abbrevsname][vartype][key]
            } else {
                obj["default"][vartype][key] = ""
            }
        } catch (_error) {
            // There is breakage here that needs investigating.
        }
    }
}


}),
"./js/modules/citations/format.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FormatCitations: function() { return FormatCitations; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _citeproc_sys__rspack_import_1 = __webpack_require__("./js/modules/citations/citeproc_sys.js");


/*
 * Use CSL and bibDB to format all citations for the given prosemirror json citation nodes
 */

class FormatCitations {
    constructor(
        csl,
        allCitationInfos,
        citationStyle,
        bibliographyHeader,
        bibDB,
        synchronous = false,
        lang = "en-US"
    ) {
        this.csl = csl
        this.allCitationInfos = allCitationInfos
        this.citationStyle = citationStyle
        this.bibliographyHeader = bibliographyHeader
        this.bibDB = bibDB
        this.synchronous = synchronous
        this.lang = lang
    }

    init() {
        this.bibliography = false
        this.citations = []
        this.bibFormats = []
        this.citationTexts = []
        this.citationType = ""
        this.formatAllCitations()
        return this.getFormattedCitations()
    }

    formatAllCitations() {
        this.allCitationInfos.forEach(cInfo => {
            this.bibFormats.push(cInfo.format)
            this.citations.push({
                citationItems: cInfo.references,
                properties: {
                    noteIndex: this.bibFormats.length
                }
            })
        })
    }

    get bibHTML() {
        if (!this.bibliography || !this.bibliography[0].entry_ids.length) {
            return ""
        }
        const bib = this.bibliography,
            bibHTML = bib[0].bibstart + bib[1].join("") + bib[0].bibend
        return `<h1 class="doc-bibliography-header">${(0,_common__rspack_import_0.escapeText)(this.bibliographyHeader)}</h1>${bibHTML}`
    }

    // CSS
    get bibCSS() {
        if (!this.bibliography || !this.bibliography[0].entry_ids.length) {
            return ""
        }
        const bibInfo = this.bibliography[0]
        let css = "\n"
        css += `.csl-entry {padding-bottom: ${bibInfo.entryspacing + 1}em;}\n`
        css += `.csl-bib-body {line-height: ${bibInfo.linespacing};}\n`
        if (bibInfo.hangingindent) {
            css += `
                    .csl-entry {
                        text-indent: -0.5in;
                        padding-left: 0.5in;
                    }\n`
        } else if (bibInfo["second-field-align"] === "margin") {
            css += `
                    .csl-left-margin {
                        text-indent: -${bibInfo.maxoffset}ch;
                        width: ${bibInfo.maxoffset}ch;
                    }
                `
        } else if (bibInfo["second-field-align"] === "flush") {
            css += `
                    .csl-left-margin {
                        width: ${bibInfo.maxoffset}ch;
                    }
                `
        }
        return css
    }

    reloadCitations(missingItems) {
        // Not all citations could be found in the database.
        // Reload the database if possible, but don't cycle if no new matches are found.
        if (!this.bibDB.getDB) {
            return Promise.resolve()
        }

        return this.bibDB.getDB().then(() => {
            if (missingItems.some(item => this.bibDB.db.hasOwnProperty(item))) {
                return this.init()
            } else {
                return Promise.resolve()
            }
        })
    }

    getFormattedCitations() {
        const citeprocConnector = new _citeproc_sys__rspack_import_1.citeprocSys(this.bibDB)
        if (this.synchronous) {
            const citeprocInstance = this.csl.getEngineSync(
                citeprocConnector,
                this.citationStyle,
                this.lang
            )
            if (!citeprocInstance) {
                return false
            }
            this.process(citeprocInstance)
            return true
        } else {
            return this.csl
                .getEngine(citeprocConnector, this.citationStyle, this.lang)
                .then(citeprocInstance => {
                    this.process(citeprocInstance)
                    if (citeprocConnector.missingItems.length > 0) {
                        return this.reloadCitations(
                            citeprocConnector.missingItems
                        )
                    } else {
                        return Promise.resolve()
                    }
                })
        }
    }

    process(citeprocInstance) {
        const allIds = []
        this.citations.forEach(cit =>
            cit.citationItems.forEach(item => allIds.push(String(item.id)))
        )
        citeprocInstance.updateItems(allIds)

        const inText = citeprocInstance.cslXml.dataObj.attrs.class === "in-text"
        const len = this.citations.length
        for (let i = 0; i < len; i++) {
            const citation = this.citations[i],
                citationTexts = citeprocInstance.appendCitationCluster(
                    citation,
                    true
                )
            if (inText && "textcite" == this.bibFormats[i]) {
                const items = citation.citationItems
                let newCiteText = ""

                for (let j = 0; j < items.length; j++) {
                    const onlyNameOption = [
                        {
                            id: items[j].id,
                            "author-only": 1
                        }
                    ]

                    const onlyDateOption = [
                        {
                            id: items[j].id,
                            "suppress-author": 1
                        }
                    ]

                    if (items[j].locator) {
                        onlyDateOption[0].locator = items[j].locator
                    }

                    if (items[j].prefix) {
                        onlyDateOption[0].prefix = items[j].prefix
                    }

                    if (0 < j) {
                        newCiteText +=
                            citeprocInstance.citation.opt.layout_delimiter ||
                            "; "
                    }
                    newCiteText += `${citeprocInstance.makeCitationCluster(onlyNameOption)} ${citeprocInstance.makeCitationCluster(onlyDateOption)}`
                }
                citationTexts.find(citationText => citationText[0] === i)[1] =
                    newCiteText
            }
            citationTexts.forEach(
                ([index, citationText]) =>
                    (this.citationTexts[index] = citationText)
            )
        }
        this.citationType = citeprocInstance.cslXml.dataObj.attrs.class
        this.bibliography = citeprocInstance.makeBibliography()
    }
}


}),
"./js/modules/citations/render.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  RenderCitations: function() { return RenderCitations; }
});
/* import */ var _format__rspack_import_0 = __webpack_require__("./js/modules/citations/format.js");

/**
 * Render citations into the DOM.
 */

class RenderCitations {
    constructor(
        contentElement,
        citationStyle,
        bibliographyHeader,
        bibDB,
        csl,
        synchronous = false,
        lang = "en-US"
    ) {
        this.contentElement = contentElement
        this.citationStyle = citationStyle
        this.bibliographyHeader = bibliographyHeader
        this.bibDB = bibDB
        this.csl = csl
        this.synchronous = synchronous
        this.lang = lang

        this.allCitationNodes = []
        this.allCitationInfos = []
        this.fm = false
    }

    init() {
        this.allCitationNodes =
            this.contentElement.querySelectorAll("span.citation")
        this.allCitationNodes.forEach(cElement => {
            const citeInfo = Object.assign({}, cElement.dataset)
            citeInfo.references = JSON.parse(citeInfo.references)
            this.allCitationInfos.push(citeInfo)
        })
        this.fm = new _format__rspack_import_0.FormatCitations(
            this.csl,
            this.allCitationInfos,
            this.citationStyle,
            this.bibliographyHeader,
            this.bibDB,
            this.synchronous,
            this.lang
        )
        if (this.synchronous) {
            if (!this.fm.init()) {
                return false
            }
            this.renderCitations()
            return true
        } else {
            return this.fm.init().then(() => {
                this.renderCitations()
                return Promise.resolve()
            })
        }
    }

    renderCitations() {
        if ("note" !== this.fm.citationType) {
            this.fm.citationTexts.forEach(
                (citationText, index) =>
                    (this.allCitationNodes[index].innerHTML = citationText)
            )
        }
    }
}


}),
"./js/modules/document_template/change_admin.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateAdmin: function() { return DocumentTemplateAdmin; }
});
/* import */ var citeproc_plus__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/citeproc-plus@0.3.6/node_modules/citeproc-plus/dist/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _designer__rspack_import_2 = __webpack_require__("./js/modules/document_template/designer.js");




class DocumentTemplateAdmin {
    constructor() {
        this.objectTools = false
        this.contentTextarea = false
        this.templateDesigner = false
        this.templateExtras = false
        this.citationStyles = false
        const locationParts = window.location.href.split("/")
        let id = Number.parseInt(locationParts[locationParts.length - 3])
        if (isNaN(id)) {
            id = 0
        }
        this.id = id
    }

    init() {
        if (
            window.location.search.length &&
            window.location.search.includes("debug=true")
        ) {
            return
        }
        (0,_common__rspack_import_1.ensureCSS)([
            staticUrl("css/colors.css"),
            staticUrl("css/document_template_admin.css"),
            staticUrl("css/admin.css"),
            staticUrl("css/ui_dialogs.css"),
            staticUrl("css/buttons.css")
        ])
        const csl = new citeproc_plus__rspack_import_0.CSL()
        const initialTasks = [
            (0,_common__rspack_import_1.whenReady)(),
            csl.getStyles().then(styles => (this.citationStyles = styles))
        ]
        if (this.id) {
            initialTasks.push(
                (0,_common__rspack_import_1.jsonPostJson)("/api/document/admin/get_template/extras/", {
                    id: this.id
                }).then(({json}) => (this.templateExtras = json))
            )
        }

        Promise.all(initialTasks).then(() => {
            this.objectTools = document.querySelector("ul.object-tools")
            if (!this.objectTools) {
                const mainContent = document.querySelector("#content-main")
                mainContent.insertAdjacentHTML(
                    "afterbegin",
                    '<ul class="object-tools"></ul>'
                )
                this.objectTools = document.querySelector("ul.object-tools")
            }
            this.titleInput = document.querySelector("#id_title")
            this.titleBlock = document.querySelector("div.field-title")
            this.contentTextarea = document.querySelector(
                "textarea[name=content]"
            )
            this.contentImportIdInput = document.querySelector("#id_import_id")
            this.contentImportIdBlock = document.querySelector(
                "div.field-import_id"
            )
            this.contentBlock = document.querySelector("div.field-content")
            this.modifyDOM()
            this.initDesigner()
            this.bind()
        })
    }

    initDesigner() {
        this.templateDesigner = new _designer__rspack_import_2.DocumentTemplateDesigner(
            this.id,
            this.titleInput.value,
            JSON.parse(this.contentTextarea.value),
            this.templateExtras.document_styles || [],
            this.citationStyles,
            this.templateExtras.export_templates || [],
            document.getElementById("template-editor")
        )
        this.templateDesigner.init()
    }

    modifyDOM() {
        this.contentBlock.style.display = "none"
        this.contentImportIdBlock.style.display = "none"
        this.titleBlock.style.display = "none"
        this.objectTools.insertAdjacentHTML(
            "beforeend",
            `<li>
                <span class="link" id="toggle-editor">${gettext("Source/Editor")}</span>
            </li>`
        )
        this.titleBlock.insertAdjacentHTML(
            "beforebegin",
            `<div class="form-row template-editor">
                <ul class="errorlist"></ul>
                <div id="template-editor"></div>
            </div>`
        )

        this.templateDesignerBlock = document.querySelector(
            "div.template-editor"
        )
    }

    setCurrentValue() {
        const {valid, value, errors, import_id, title} =
            this.templateDesigner.getCurrentValue()
        this.contentTextarea.value = JSON.stringify(value)
        this.contentImportIdInput.value = import_id
        this.titleInput.value = title
        this.showErrors(errors)
        return valid
    }

    showErrors(errors) {
        this.templateDesignerBlock.querySelector("ul.errorlist").innerHTML =
            Object.values(errors)
                .map(error => `<li>${(0,_common__rspack_import_1.escapeText)(error)}</li>`)
                .join("")
    }

    bind() {
        document.body.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_1.findTarget)(event, "#toggle-editor", el):
                    event.preventDefault()
                    if (this.contentBlock.style.display === "none") {
                        this.contentBlock.style.display = ""
                        this.contentImportIdBlock.style.display = ""
                        this.titleBlock.style.display = ""
                        this.setCurrentValue()
                        this.templateDesigner.close()
                        this.templateDesigner = false
                        this.templateDesignerBlock.style.display = "none"
                    } else {
                        this.contentBlock.style.display = "none"
                        this.contentImportIdBlock.style.display = "none"
                        this.titleBlock.style.display = "none"
                        this.templateDesignerBlock.style.display = ""
                        this.initDesigner()
                    }
                    break
                case (0,_common__rspack_import_1.findTarget)(event, "div.submit-row input[type=submit]", el):
                    if (
                        this.contentBlock.style.display === "none" &&
                        !this.setCurrentValue()
                    ) {
                        event.preventDefault()
                    }
                    break
                default:
                    break
            }
        })
    }
}


}),
"./js/modules/document_template/designer.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateDesigner: function() { return DocumentTemplateDesigner; }
});
/* import */ var prosemirror_commands__rspack_import_15 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_example_setup__rspack_import_13 = __webpack_require__("./node_modules/.pnpm/prosemirror-example-setup@1.2.3/node_modules/prosemirror-example-setup/dist/index.js");
/* import */ var prosemirror_gapcursor__rspack_import_16 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_history__rspack_import_18 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_14 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_menu__rspack_import_17 = __webpack_require__("./node_modules/.pnpm/prosemirror-menu@1.2.4/node_modules/prosemirror-menu/dist/index.js");
/* import */ var prosemirror_state__rspack_import_12 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var prosemirror_view__rspack_import_11 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var sortablejs__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/sortablejs@1.15.0/node_modules/sortablejs/modular/sortable.esm.js");
/* import */ var _common__rspack_import_2 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _editor_state_plugins__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _schema_document__rspack_import_4 = __webpack_require__("./js/modules/schema/document/index.js");
/* import */ var _schema_mini_json__rspack_import_5 = __webpack_require__("./js/modules/schema/mini_json.js");
/* import */ var _document_style_dialog__rspack_import_6 = __webpack_require__("./js/modules/document_template/document_style_dialog.js");
/* import */ var _export_template_dialog__rspack_import_7 = __webpack_require__("./js/modules/document_template/export_template_dialog.js");
/* import */ var _schema__rspack_import_8 = __webpack_require__("./js/modules/document_template/schema.js");
/* import */ var _templates__rspack_import_9 = __webpack_require__("./js/modules/document_template/templates.js");
/* import */ var _tools__rspack_import_10 = __webpack_require__("./js/modules/document_template/tools.js");





















class DocumentTemplateDesigner {
    constructor(
        id,
        title,
        value,
        documentStyles,
        citationStyles,
        exportTemplates,
        dom
    ) {
        this.id = id
        this.title = title
        this.value = (0,_schema_mini_json__rspack_import_5.toFullJSON)(value, _schema_document__rspack_import_4.docSchema)
        this.documentStyles = documentStyles
        this.citationStyles = citationStyles
        this.exportTemplates = exportTemplates
        this.dom = dom

        this.editors = []
        this.listeners = {
            onScroll: (0,_tools__rspack_import_10.debounced)(200, () => this.onScroll())
        }
    }

    init() {
        this.dom.innerHTML = (0,_templates__rspack_import_9.documentDesignerTemplate)({
            id: this.id,
            title: this.title,
            value: this.value,
            documentStyles: this.documentStyles,
            exportTemplates: this.exportTemplates,
            citationStyles: this.citationStyles
        })
        ;(0,_common__rspack_import_2.ensureCSS)([
            staticUrl("css/common.css"),
            staticUrl("css/dialog.css"),
            staticUrl("css/prosemirror.css"),
            staticUrl("css/prosemirror-menu.css"),
            staticUrl("css/prosemirror-example-setup.css"),
            staticUrl("css/document_template_designer.css"),
            staticUrl("css/tags.css"),
            staticUrl("css/contributors.css"),
            staticUrl("css/dialog.css"),
            staticUrl("css/table.css"),
            staticUrl("css/dialog_table.css")
        ])
        this.setupInitialEditors()
        this.bind()
    }

    getCurrentValue() {
        let valid = true
        const ids = []
        const errors = {}
        const titleEl = this.dom.querySelector("input.title")
        if (titleEl.classList.contains("error-element")) {
            titleEl.classList.remove("error-element")
        }
        this.title = titleEl.value
        if (!this.title.length) {
            valid = false
            errors.empty_template_title = gettext("The template needs a title.")
            titleEl.classList.add("error-element")
            titleEl.scrollIntoView({block: "center", behavior: "smooth"})
        }
        const importIdEl = this.dom.querySelector("input.import-id")
        const importId = importIdEl.value
        if (!importId.length) {
            valid = false
            errors.empty_import_id = gettext("The template needs an ID.")
            importIdEl.classList.add("error-element")
            importIdEl.scrollIntoView({block: "center", behavior: "smooth"})
        }
        if (/\s/.test(importId)) {
            valid = false
            errors.no_spaces = gettext("The template ID cannot contain spaces.")
            importIdEl.classList.add("error-element")
            importIdEl.scrollIntoView({block: "center", behavior: "smooth"})
        }

        this.value = {
            type: "doc",
            content: [{type: "title"}].concat(
                Array.from(
                    this.dom.querySelectorAll(
                        ".to-container .doc-part-block:not(.fixed)"
                    )
                ).map(el => {
                    const type = el.dataset.type,
                        id = el.querySelector("input.id").value,
                        title = el.querySelector("input.title")
                            ? el.querySelector("input.title").value
                            : false,
                        help = this.getEditorValue(
                            el.querySelector(".instructions")
                        ),
                        initial = this.getEditorValue(
                            el.querySelector(".initial"),
                            true
                        ),
                        locking = el.querySelector(".locking option:checked")
                            ? el.querySelector(".locking option:checked").value
                            : "false",
                        optional = el.querySelector(".optional option:checked")
                            ? el.querySelector(".optional option:checked").value
                            : false,
                        attrs = {id, title},
                        node = {type, attrs}
                    if (help) {
                        attrs.help = help
                    }
                    if (initial) {
                        attrs.initial = initial
                        node.content = JSON.parse(JSON.stringify(initial))
                    }
                    if (optional !== "false") {
                        attrs.optional = optional
                    }
                    if (optional === "hidden") {
                        attrs.hidden = true
                    }
                    if (locking !== "false") {
                        attrs.locking = locking
                    }
                    switch (type) {
                        case "richtext_part":
                        case "heading_part": {
                            attrs.elements = Array.from(
                                el.querySelectorAll(".elements:checked")
                            ).map(el => el.value)
                            if (!attrs.elements.length) {
                                attrs.elements = ["paragraph"]
                            }
                            attrs.marks = Array.from(
                                el.querySelectorAll(".marks:checked")
                            ).map(el => el.value)
                            const language = el.querySelector(".language").value
                            if (language !== "false") {
                                attrs.language = language
                            }
                            if (!node.content) {
                                node.content = [{type: attrs.elements[0]}]
                            }
                            const metadata =
                                el.querySelector("select.metadata").value
                            if (metadata !== "false") {
                                attrs.metadata = metadata
                            }
                            break
                        }
                        case "table_part": {
                            attrs.elements = Array.from(
                                el.querySelectorAll(".elements:checked")
                            ).map(el => el.value)
                            if (!attrs.elements.includes("paragraph")) {
                                // tables need to allow paragraphs
                                attrs.elements.push("paragraph")
                            }
                            attrs.marks = Array.from(
                                el.querySelectorAll(".marks:checked")
                            ).map(el => el.value)
                            const language = el.querySelector(".language").value
                            if (language !== "false") {
                                attrs.language = language
                            }
                            if (!node.content) {
                                node.content = [
                                    {
                                        type: "table",
                                        content: [
                                            {type: "table_caption"},
                                            {
                                                type: "table_body",
                                                content: [
                                                    {
                                                        type: "table_row",
                                                        content: [
                                                            {
                                                                type: "table_cell",
                                                                content: [
                                                                    {
                                                                        type: "paragraph"
                                                                    }
                                                                ]
                                                            }
                                                        ]
                                                    }
                                                ]
                                            }
                                        ]
                                    }
                                ]
                            }
                            break
                        }
                        case "contributors_part":
                        case "tags_part": {
                            attrs.item_title =
                                el.querySelector("input.item_title").value
                            const metadata =
                                el.querySelector("select.metadata").value
                            if (metadata !== "false") {
                                attrs.metadata = metadata
                            }
                            break
                        }
                        default:
                            break
                    }
                    if (el.classList.contains("error-element")) {
                        el.classList.remove("error-element")
                    }
                    if (!id.length) {
                        valid = false
                        errors.missing_id = gettext(
                            "All document parts need an ID."
                        )
                        el.classList.add("error-element")
                        el.scrollIntoView({block: "center", behavior: "smooth"})
                    }
                    if (/\s/.test(id)) {
                        valid = false
                        errors.no_spaces = gettext("IDs cannot contain spaces.")
                        el.classList.add("error-element")
                        el.scrollIntoView({block: "center", behavior: "smooth"})
                    }
                    if (ids.includes(id)) {
                        valid = false
                        Array.from(
                            this.dom.querySelectorAll(
                                ".to-container .doc-part-block:not(.fixed)"
                            )
                        ).map(el => {
                            const id_duplicate =
                                el.querySelector("input.id").value
                            if (id_duplicate == id) {
                                el.classList.add("error-element")
                            }
                        })
                        el.scrollIntoView({block: "center", behavior: "smooth"})
                        errors.unique_id = gettext("IDs have to be unique.")
                    }
                    ids.push(id)
                    return node
                })
            ),
            attrs: {
                import_id: importId,
                footnote_elements: Array.from(
                    this.dom.querySelectorAll(
                        ".footnote-value .elements:checked"
                    )
                ).map(el => el.value),
                footnote_marks: Array.from(
                    this.dom.querySelectorAll(".footnote-value .marks:checked")
                ).map(el => el.value),
                language: this.dom.querySelector(
                    ".language-value option:checked"
                )
                    ? this.dom.querySelector(".language-value option:checked")
                          .value
                    : false,
                languages: Array.from(
                    this.dom.querySelectorAll(".languages-value option:checked")
                ).map(el => el.value),
                citationstyle: this.dom.querySelector(
                    ".citationstyle-value option:checked"
                )
                    ? this.dom.querySelector(
                          ".citationstyle-value option:checked"
                      ).value
                    : false,
                citationstyles: Array.from(
                    this.dom.querySelectorAll(
                        ".citationstyles-value option:checked"
                    )
                )
                    .map(el => el.value)
                    .slice(0, 30),
                papersizes: Array.from(
                    this.dom.querySelectorAll(
                        ".papersizes-value option:checked"
                    )
                ).map(el => el.value),
                code_languages: Array.from(
                    this.dom.querySelectorAll(
                        ".code-languages-value option:checked"
                    )
                ).map(el => el.value),
                code_categories: Array.from(
                    this.dom.querySelectorAll(
                        ".code-categories-value .code-category:checked"
                    )
                ).reduce((obj, el) => {
                    obj[el.value] = {
                        counter: 0,
                        enabled: true
                    }
                    return obj
                }, {}),
                bibliography_header: Array.from(
                    this.dom.querySelectorAll(".bibliography-header-value tr")
                ).reduce((stringObj, trEl) => {
                    const inputEl = trEl.querySelector("input")
                    if (!inputEl.value.length) {
                        return stringObj
                    }
                    const selectEl = trEl.querySelector("select")
                    stringObj[selectEl.value] = inputEl.value
                    return stringObj
                }, {}),
                template: this.title
            }
        }
        if (!this.value.attrs.papersizes.length) {
            this.value.attrs.papersizes = ["A4"]
        }
        this.value.attrs.papersize = this.value.attrs.papersizes[0]
        if (!this.value.attrs.footnote_elements.length) {
            this.value.attrs.footnote_elements = ["paragraph"]
        }
        if (!this.value.attrs.languages.length) {
            this.value.attrs.languages = ["en-US"]
        }
        if (!this.value.attrs.languages.includes(this.value.attrs.language)) {
            this.value.attrs.language = this.value.attrs.languages[0]
        }
        if (!this.value.attrs.citationstyles.length) {
            this.value.attrs.citationstyles = ["apa"]
        }
        if (
            !this.value.attrs.citationstyles.includes(
                this.value.attrs.citationstyle
            )
        ) {
            this.value.attrs.language = this.value.attrs.citationstyles[0]
        }

        return {
            valid,
            title: this.title,
            value: (0,_schema_mini_json__rspack_import_5.toMiniJSON)(_schema_document__rspack_import_4.docSchema.nodeFromJSON(this.value)),
            errors,
            import_id: importId
        }
    }

    setupInitialEditors() {
        Array.from(
            this.dom.querySelectorAll(
                ".to-container .doc-part-block:not(.fixed)"
            )
        ).forEach((el, index) => {
            const value = this.value.content[index + 1], // offset by title
                help = value.attrs.help,
                initial = value.attrs.initial,
                type = value.type
            this.setupEditors(el, type, help, initial)
        })
    }

    setupEditors(el, type, help = false, initial = false) {
        const helpEl = el.querySelector(".instructions")
        if (!helpEl) {
            return
        }
        const helpDoc = help
                ? _schema__rspack_import_8.helpSchema.nodeFromJSON({type: "doc", content: help})
                : _schema__rspack_import_8.helpSchema.nodes.doc.createAndFill(),
            helpView = new prosemirror_view__rspack_import_11.EditorView(helpEl, {
                state: prosemirror_state__rspack_import_12.EditorState.create({
                    doc: helpDoc,
                    plugins: [
                        (0,prosemirror_example_setup__rspack_import_13.buildInputRules)(_schema__rspack_import_8.helpSchema),
                        (0,prosemirror_keymap__rspack_import_14.keymap)((0,prosemirror_example_setup__rspack_import_13.buildKeymap)(_schema__rspack_import_8.helpSchema)),
                        (0,prosemirror_keymap__rspack_import_14.keymap)(prosemirror_commands__rspack_import_15.baseKeymap),
                        (0,prosemirror_gapcursor__rspack_import_16.gapCursor)(),
                        (0,prosemirror_menu__rspack_import_17.menuBar)({
                            floating: false,
                            content: _schema__rspack_import_8.helpMenuContent
                        }),
                        (0,prosemirror_history__rspack_import_18.history)()
                    ]
                })
            })
        this.editors.push([helpEl, helpView])
        const plugins = [
            new prosemirror_state__rspack_import_12.Plugin({
                // Adding heading IDs to all new headings.
                appendTransaction: (trs, oldState, newState) => {
                    if (trs.every(tr => !tr.steps.length)) {
                        return
                    }
                    return (0,_tools__rspack_import_10.addHeadingIds)(oldState, newState, this.editors)
                }
            })
        ]
        let menuContent = [],
            schema
        switch (type) {
            case "richtext_part":
                schema = _schema__rspack_import_8.richtextPartSchema
                menuContent = _schema__rspack_import_8.richtextMenuContent
                plugins.push((0,prosemirror_tables__rspack_import_0.tableEditing)())
                break
            case "table_part":
                schema = _schema__rspack_import_8.tablePartSchema
                menuContent = _schema__rspack_import_8.tableMenuContent
                plugins.push((0,prosemirror_tables__rspack_import_0.tableEditing)())
                break
            case "heading_part":
                schema = _schema__rspack_import_8.headingPartSchema
                menuContent = _schema__rspack_import_8.headingMenuContent
                break
            case "tags_part":
                schema = _schema__rspack_import_8.tagsPartSchema
                plugins.push(
                    new prosemirror_state__rspack_import_12.Plugin({
                        props: {
                            nodeViews: {
                                tags_part: (node, view, getPos) =>
                                    new _editor_state_plugins__rspack_import_3.TagsPartView(node, view, getPos)
                            }
                        }
                    })
                )
                break
            case "contributors_part":
                schema = _schema__rspack_import_8.contributorsPartSchema
                plugins.push(
                    new prosemirror_state__rspack_import_12.Plugin({
                        props: {
                            nodeViews: {
                                contributors_part: (node, view, getPos) =>
                                    new _editor_state_plugins__rspack_import_3.ContributorsPartView(node, view, getPos)
                            }
                        }
                    })
                )
                break
            default:
                break
        }

        if (!schema) {
            return
        }
        plugins.unshift(
            (0,prosemirror_example_setup__rspack_import_13.buildInputRules)(schema),
            (0,prosemirror_keymap__rspack_import_14.keymap)((0,prosemirror_example_setup__rspack_import_13.buildKeymap)(schema)),
            (0,prosemirror_keymap__rspack_import_14.keymap)(prosemirror_commands__rspack_import_15.baseKeymap),
            (0,prosemirror_gapcursor__rspack_import_16.gapCursor)(),
            (0,prosemirror_menu__rspack_import_17.menuBar)({
                floating: false,
                content: menuContent
            }),
            (0,prosemirror_history__rspack_import_18.history)()
        )
        const initialEl = el.querySelector(".initial"),
            doc = initial
                ? schema.nodeFromJSON({
                      type: "doc",
                      content: [
                          {
                              type: type,
                              content: initial
                          }
                      ]
                  })
                : schema.nodes.doc.createAndFill()
        let state = prosemirror_state__rspack_import_12.EditorState.create({
            doc,
            plugins
        })
        const addedHeadings = (0,_tools__rspack_import_10.addHeadingIds)(state, state, this.editors)
        if (addedHeadings) {
            state = state.apply(addedHeadings)
        }
        const initialView = new prosemirror_view__rspack_import_11.EditorView(initialEl, {state})
        this.editors.push([initialEl, initialView])
    }

    getEditorValue(el, initial = false) {
        const editor = this.editors.find(editor => editor[0] === el)
        if (!editor) {
            return false
        }
        const state = editor[1].state
        // Only return if there is more content that a recently initiated doc
        // would have. The number varies between part types.
        if (
            state.doc.firstChild.type.name === "heading_part" ||
            state.doc.nodeSize > state.schema.nodes.doc.createAndFill().nodeSize
        ) {
            return initial
                ? (0,_tools__rspack_import_10.noTrack)(state.doc.firstChild.toJSON()).content
                : (0,_tools__rspack_import_10.noTrack)(state.doc.toJSON()).content
        }
        return false
    }

    close() {
        this.dom.innerHTML = ""
        document.removeEventListener("scroll", this.listeners.onScroll)
    }

    bind() {
        new sortablejs__rspack_import_1["default"](this.dom.querySelector(".from-container"), {
            group: {
                name: "document",
                pull: "clone",
                put: false
            },
            sort: false,
            handle: ".doc-part-header"
        })
        new sortablejs__rspack_import_1["default"](this.dom.querySelector(".to-container"), {
            group: {
                name: "document",
                pull: true,
                put: true
            },
            handle: ".doc-part-header",
            onAdd: event => {
                this.setupEditors(event.item, event.item.dataset.type)
            }
        })
        new sortablejs__rspack_import_1["default"](this.dom.querySelector(".trash"), {
            group: {
                name: "document",
                put: true
            },
            handle: ".doc-part-header",
            onAdd: event => event.to.removeChild(event.to.firstElementChild) // Remove the item that was just added
        })

        this.dom.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_2.findTarget)(event, ".doc-part-block .configure", el):
                    event.preventDefault()
                    el.target
                        .closest(".doc-part-block")
                        .querySelector(".attrs")
                        .classList.toggle("hidden")
                    break
                case (0,_common__rspack_import_2.findTarget)(
                    event,
                    ".bibliography-header-value .fa-plus-circle",
                    el
                ):
                    event.preventDefault()
                    this.getCurrentValue()
                    this.dom.querySelector(
                        ".bibliography-header-value"
                    ).innerHTML = (0,_templates__rspack_import_9.bibliographyHeaderTemplate)({
                        bibliography_header: Object.assign(
                            {},
                            this.value.attrs.bibliography_header,
                            {zzz: ""}
                        ) // 'zzz' so that the entry is added at the of the list
                    })
                    break
                case (0,_common__rspack_import_2.findTarget)(
                    event,
                    ".bibliography-header-value .fa-minus-circle",
                    el
                ): {
                    event.preventDefault()
                    const trEl = el.target.closest("tr")
                    trEl.parentElement.removeChild(trEl)
                    break
                }
                case (0,_common__rspack_import_2.findTarget)(event, "button.document-style", el): {
                    event.preventDefault()
                    const id = Number.parseInt(el.target.dataset.id)
                    const style = this.documentStyles.find(
                        style => style.pk === id
                    )
                    const dialog = new _document_style_dialog__rspack_import_6.DocumentStyleDialog(
                        id,
                        style,
                        this.id,
                        this.documentStyles,
                        () =>
                            (this.dom.querySelector(
                                ".document-styles"
                            ).innerHTML = (0,_templates__rspack_import_9.documentStylesTemplate)({
                                documentStyles: this.documentStyles
                            }))
                    )
                    dialog.init()
                    break
                }
                case (0,_common__rspack_import_2.findTarget)(event, "button.export-template", el): {
                    event.preventDefault()
                    const id = Number.parseInt(el.target.dataset.id)
                    const template = this.exportTemplates.find(
                        template => template.pk === id
                    )
                    const {value, valid} = this.getCurrentValue()
                    if (valid) {
                        const dialog = new _export_template_dialog__rspack_import_7.ExportTemplateDialog(
                            id,
                            template,
                            this.id,
                            this.exportTemplates,
                            () =>
                                (this.dom.querySelector(
                                    ".export-templates"
                                ).innerHTML = (0,_templates__rspack_import_9.exportTemplatesTemplate)({
                                    exportTemplates: this.exportTemplates
                                })),
                            value
                        )
                        dialog.init()
                    }
                    break
                }
                default:
                    break
            }
        })

        document.addEventListener("scroll", this.listeners.onScroll)

        this.dom
            .querySelector(".languages-value")
            .addEventListener("change", () => {
                this.getCurrentValue()
                this.dom.querySelector(".language-value").innerHTML =
                    (0,_templates__rspack_import_9.languageTemplate)(this.value.attrs)
            })

        this.dom
            .querySelector(".citationstyles-value")
            .addEventListener("change", () => {
                const checkedElements = Array.from(
                    this.dom.querySelectorAll(
                        ".citationstyles-value option:checked"
                    )
                )
                this.getCurrentValue()
                if (
                    checkedElements.length >
                    this.value.attrs.citationstyles.length
                ) {
                    // Selected more than the max limit. We deselect the remaining.
                    checkedElements.forEach(el => {
                        if (
                            !this.value.attrs.citationstyles.includes(el.value)
                        ) {
                            el.selected = false
                        }
                    })
                }
                this.dom.querySelector(".citationstyle-value").innerHTML =
                    (0,_templates__rspack_import_9.citationstyleTemplate)(this.value.attrs, this.citationStyles)
            })
    }

    onScroll() {
        const fromContainer = this.dom.querySelector(".from-container"),
            toContainer = this.dom.querySelector(".to-container"),
            fromRect = fromContainer.getBoundingClientRect(),
            toRect = toContainer.getBoundingClientRect()
        if (toRect.height + 25 + fromRect.top > 0) {
            const contentSize = 6 * 61, // 61px for each content type.
                maxPadding = toRect.height - contentSize - 20 // 20px for padding bottom
            fromContainer.style.paddingTop = `${Math.min(10 - Math.min(fromRect.top, 0), maxPadding)}px`
        }
    }
}


}),
"./js/modules/document_template/document_style_dialog.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentStyleDialog: function() { return DocumentStyleDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");


class DocumentStyleDialog {
    constructor(id, style, documentTemplateId, allStyles, refresh) {
        this.id = id
        this.style = style
        this.documentTemplateId = documentTemplateId
        this.allStyles = allStyles
        this.refresh = refresh

        this.addedFiles = []
        this.deletedFiles = []
    }

    init() {
        const buttons = [
            {
                text: gettext("Save"),
                classes: "fw-dark",
                click: () => {
                    const {title, slug, contents, errors} =
                        this.getCurrentValue()
                    if (errors.length) {
                        this.showErrors(errors)
                        return
                    }
                    this.save({title, slug, contents})
                        .then(({json}) => {
                            const docStyle = json.doc_style[0]
                            const pk = docStyle.pk
                            const oldStyleIndex = this.allStyles.findIndex(
                                style => style.pk === pk
                            )
                            if (oldStyleIndex > -1) {
                                this.allStyles.splice(
                                    oldStyleIndex,
                                    1,
                                    docStyle
                                )
                            } else {
                                this.allStyles.push(docStyle)
                            }
                            this.refresh()
                            this.dialog.close()
                        })
                        .catch(response => {
                            if (response.json) {
                                response.json().then(json => {
                                    if (json.errors) {
                                        const errors = []
                                        Object.keys(json.errors).forEach(
                                            key => {
                                                json.errors[key].forEach(
                                                    error =>
                                                        errors.push(
                                                            `${key}: ${error}`
                                                        )
                                                )
                                            }
                                        )
                                        this.showErrors(errors)
                                    }
                                })
                            } else {
                                throw response
                            }
                        })
                }
            },
            {type: "cancel"}
        ]
        if (this.allStyles.length > 1 && this.id) {
            buttons.unshift({
                text: gettext("Delete"),
                classes: "fw-orange",
                click: () => this.deleteStyleDialog()
            })
        }
        this.dialog = new _common__rspack_import_0.Dialog({
            id: "document-style-dialog",
            title: gettext("Document style"),
            body: `<table class="fw-dialog-table"><tbody>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Title")}</h4></th>
                    <td>
                        <input class="title" type="text" value="${this.style ? (0,_common__rspack_import_0.escapeText)(this.style.fields.title) : ""}">
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Slug")}</h4></th>
                    <td>
                        <input class="slug" type="text" value="${this.style ? (0,_common__rspack_import_0.escapeText)(this.style.fields.slug) : ""}">
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("CSS Style")}</h4></th>
                    <td>
                        <textarea class="contents">${this.style ? (0,_common__rspack_import_0.escapeText)(this.style.fields.contents) : ""}</textarea>
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Files")}</h4></th>
                    <td>
                        <table><tbody class="document-style-files">${
                            this.style
                                ? `${this.style.fields.documentstylefile_set
                                      .map(
                                          ([url, title]) =>
                                              `<tr>
                                            <td><a href="${url}">${(0,_common__rspack_import_0.escapeText)(title)}</a></td>
                                            <td><span class="fw-link-text"><i class="fas fa-trash-alt delete-document-style-file" data-filename="${(0,_common__rspack_import_0.escapeText)(title)}"></i></span></td>
                                        </tr>`
                                      )
                                      .join("<br>")}`
                                : ""
                        }</tbody></table>
                        <button type="button" class="fw-media-select-button fw-button fw-light">
                            ${gettext("Add a file")}
                        </button>
                        <input name="image" type="file" class="fw-media-file-input">
                    </td>
                </tr>
                </tbody></table>
                <ul class="errorlist"></ul>`,
            buttons
        })

        this.dialog.open()
        this.bind()
    }

    showErrors(errors) {
        this.dialog.dialogEl.querySelector("ul.errorlist").innerHTML = errors
            .map(error => `<li>${(0,_common__rspack_import_0.escapeText)(error)}</li>`)
            .join("")
    }

    deleteStyle() {
        (0,_common__rspack_import_0.jsonPostJson)("/api/style/delete_document_style/", {id: this.id})
            .then(() => {
                const oldStyleIndex = this.allStyles.findIndex(
                    style => style.pk === this.id
                )
                if (!(typeof oldStyleIndex === "undefined")) {
                    this.allStyles.splice(oldStyleIndex, 1)
                    this.refresh()
                }
                this.dialog.close()
            })
            .catch(response => {
                if (response.json) {
                    response.json().then(json => {
                        if (json.errors) {
                            const errors = []
                            Object.keys(json.errors).forEach(key => {
                                json.errors[key].forEach(error =>
                                    errors.push(`${key}: ${error}`)
                                )
                            })
                            this.showErrors(errors)
                        }
                    })
                } else {
                    throw response
                }
            })
    }

    deleteStyleDialog() {
        const buttons = [
            {
                text: gettext("Delete"),
                classes: "fw-dark",
                click: () => {
                    dialog.close()
                    this.deleteStyle()
                }
            },
            {
                type: "cancel"
            }
        ]
        const dialog = new _common__rspack_import_0.Dialog({
            id: "confirmdeletion",
            icon: "exclamation-triangle",
            title: gettext("Confirm deletion"),
            body: `<p>${gettext("Do you really want to delete the document style?")}</p>`,
            height: 180,
            buttons
        })
        dialog.open()
    }

    getCurrentValue() {
        const errors = []

        const title = this.dialog.dialogEl.querySelector(".title").value
        const slug = this.dialog.dialogEl.querySelector(".slug").value
        const contents = this.dialog.dialogEl.querySelector(".contents").value

        if (!title.length || !slug.length || !contents.length) {
            errors.push(gettext("All fields need to be filled out."))
        }
        if (!/^[A-Za-z0-9\-_]+$/.test(slug)) {
            errors.push(
                gettext(
                    "A slug can only contain letters, numbers, hyphens and underscores."
                )
            )
        }
        if (
            this.allStyles.find(
                style => style.fields.slug === slug && style.pk !== this.id
            )
        ) {
            errors.push(gettext("The slug has to be unique."))
        }
        return {title, slug, contents, errors}
    }

    save({title, slug, contents}) {
        return (0,_common__rspack_import_0.jsonPostJson)(
            "/api/style/save_document_style/",
            {
                id: this.id,
                title,
                slug,
                contents,
                template_id: this.documentTemplateId,
                deleted_files: this.deletedFiles
            },
            false,
            {
                added_files: this.addedFiles
            }
        )
    }

    bind() {
        const mediaInputSelector = this.dialog.dialogEl.querySelector(
            ".fw-media-file-input"
        )
        this.dialog.dialogEl.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, ".delete-document-style-file", el): {
                    event.preventDefault()
                    this.deletedFiles.push(el.target.dataset.filename)
                    const tr = el.target.closest("tr")
                    tr.parentElement.removeChild(tr)
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, ".delete-new-document-style-file", el): {
                    event.preventDefault()
                    const tr = el.target.closest("tr")
                    let count = 0,
                        findTr = tr
                    while (
                        findTr.previousElementSibling.classList.contains(
                            "new-document-style-file"
                        )
                    ) {
                        findTr = findTr.previousElementSibling
                        count++
                    }
                    this.addedFiles.splice(count, 1)
                    tr.parentElement.removeChild(tr)
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, ".fw-media-select-button", el): {
                    event.preventDefault()
                    mediaInputSelector.click()
                    break
                }
            }
        })

        mediaInputSelector.addEventListener("change", () => {
            this.showErrors([])
            const mediaInput = mediaInputSelector.files[0]
            if (!mediaInput) {
                return
            }
            let currentFiles = this.addedFiles.map(file => file.name)
            if (this.style) {
                currentFiles += this.style.fields.documentstylefile_set
                    .filter(([_url, name]) => !this.deletedFiles.includes(name))
                    .map(([_url, name]) => name)
            }
            if (currentFiles.includes(mediaInput.name)) {
                this.showErrors([
                    gettext("A file with the same name exists already.")
                ])
                return
            }

            this.addedFiles.push(mediaInput)
            this.dialog.dialogEl.querySelector(
                ".document-style-files"
            ).innerHTML += `<tr class="new-document-style-file">
                    <td>${(0,_common__rspack_import_0.escapeText)(mediaInput.name)}</td>
                    <td><span class="fw-link-text"><i class="fas fa-trash-alt delete-new-document-style-file"></i></span></td>
                </tr>`
        })
    }
}


}),
"./js/modules/document_template/export_template_dialog.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ExportTemplateDialog: function() { return ExportTemplateDialog; }
});
/* import */ var jszip__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/jszip@3.10.1/node_modules/jszip/dist/jszip.min.js");
/* import */ var jszip__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(jszip__rspack_import_0);
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");



class ExportTemplateDialog {
    constructor(
        id,
        template,
        documentTemplateId,
        allTemplates,
        refresh,
        documentTemplateValue
    ) {
        this.id = id
        this.template = template
        this.documentTemplateId = documentTemplateId
        this.allTemplates = allTemplates
        this.refresh = refresh
        this.documentTemplateValue = documentTemplateValue
        this.addedFile = false
        this.addedFileType = false
    }

    init() {
        const buttons = [
            {
                text: gettext("Save"),
                classes: "fw-dark",
                click: () => {
                    const {errors} = this.checkCurrent()
                    if (errors.length) {
                        this.showErrors(errors)
                        return
                    }
                    this.save()
                        .then(({json}) => {
                            const exportTemplate = json.export_template[0]
                            const pk = exportTemplate.pk
                            const oldTemplateIndex =
                                this.allTemplates.findIndex(
                                    template => template.pk === pk
                                )
                            if (oldTemplateIndex > -1) {
                                this.allTemplates.splice(
                                    oldTemplateIndex,
                                    1,
                                    exportTemplate
                                )
                            } else {
                                this.allTemplates.push(exportTemplate)
                            }
                            this.refresh()
                            this.dialog.close()
                        })
                        .catch(response => {
                            if (response.json) {
                                response.json().then(json => {
                                    if (json.errors) {
                                        const errors = []
                                        Object.keys(json.errors).forEach(
                                            key => {
                                                json.errors[key].forEach(
                                                    error =>
                                                        errors.push(
                                                            `${key}: ${error}`
                                                        )
                                                )
                                            }
                                        )
                                        this.showErrors(errors)
                                    }
                                })
                            } else {
                                throw response
                            }
                        })
                }
            },
            {type: "cancel"}
        ]
        if (this.id) {
            buttons.unshift({
                text: gettext("Delete"),
                classes: "fw-orange",
                click: () => this.deleteTemplateDialog()
            })
        }
        this.dialog = new _common__rspack_import_1.Dialog({
            id: "export-template-dialog",
            title: gettext("Export template"),
            width: 400,
            body: `<table class="fw-dialog-table fw-data-table"><tbody>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("File")}</h4></th>
                    <td style="width: 250px;">
                        <span class="export-template-file">${
                            this.template
                                ? `<a href="${this.template.fields.template_file}">${(0,_common__rspack_import_1.escapeText)(this.template.fields.title)}</a>`
                                : ""
                        }</span>
                    </td><td style="width: 70px;">
                        <button type="button" class="fw-media-select-button fw-button fw-light">
                            ${gettext("Select")}
                        </button>
                        <input name="image" type="file" class="fw-media-file-input">
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Filetype")}</h4></th>
                    <td colspan="2">
                        <span class="export-template-filetype">${
                            this.template ? this.template.fields.file_type : ""
                        }</span>
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Found tags")}</h4></th>
                    <td colspan="2">
                        <span class="export-template-found-tags"></span>
                    </td>
                </tr>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Missing tags")}</h4></th>
                    <td colspan="2">
                        <span class="export-template-missing-tags"></span>
                    </td>
                </tr>
                </tbody></table>
                <ul class="errorlist"></ul>`,
            buttons
        })
        this.dialog.open()
        this.bind()
        if (this.template) {
            this.checkRemoteFile(this.template.fields.template_file)
        }
    }

    showErrors(errors) {
        this.dialog.dialogEl.querySelector("ul.errorlist").innerHTML = errors
            .map(error => `<li>${(0,_common__rspack_import_1.escapeText)(error)}</li>`)
            .join("")
    }

    deleteTemplate() {
        (0,_common__rspack_import_1.jsonPostJson)("/api/style/delete_export_template/", {id: this.id})
            .then(() => {
                const oldTemplateIndex = this.allTemplates.findIndex(
                    style => style.pk === this.id
                )
                if (!(typeof oldTemplateIndex === "undefined")) {
                    this.allTemplates.splice(oldTemplateIndex, 1)
                    this.refresh()
                }
                this.dialog.close()
            })
            .catch(response => {
                if (response.json) {
                    response.json().then(json => {
                        if (json.errors) {
                            const errors = []
                            Object.keys(json.errors).forEach(key => {
                                json.errors[key].forEach(error =>
                                    errors.push(`${key}: ${error}`)
                                )
                            })
                            this.showErrors(errors)
                        }
                    })
                } else {
                    throw response
                }
            })
    }

    deleteTemplateDialog() {
        const buttons = [
            {
                text: gettext("Delete"),
                classes: "fw-dark",
                click: () => {
                    dialog.close()
                    this.deleteTemplate()
                }
            },
            {
                type: "cancel"
            }
        ]
        const dialog = new _common__rspack_import_1.Dialog({
            id: "confirmdeletion",
            icon: "exclamation-triangle",
            title: gettext("Confirm deletion"),
            body: `<p>${gettext("Do you really want to delete the export template?")}</p>`,
            height: 180,
            buttons
        })
        dialog.open()
    }

    checkCurrent() {
        const errors = []

        if (!this.addedFile) {
            errors.push(
                gettext(
                    "You need to upload a template file in ODT or DOCX format."
                )
            )
        }
        if (
            this.allTemplates.find(
                template =>
                    template.fields.title ===
                        this.addedFile.name.split(".")[0] &&
                    template.pk !== this.id
            )
        ) {
            errors.push(
                gettext(
                    "Another export file with the same filename exists already."
                )
            )
        }
        return {errors}
    }

    save() {
        const jsonData = {
            id: this.id,
            template_id: this.documentTemplateId,
            added_file_type: this.addedFileType
        }
        return (0,_common__rspack_import_1.jsonPostJson)(
            "/api/style/save_export_template/",
            jsonData,
            false,
            {
                added_file: this.addedFile
            }
        )
    }

    checkRemoteFile(url) {
        return (0,_common__rspack_import_1.get)(url)
            .then(response => response.blob())
            .then(blob => this.checkFile(blob))
    }

    checkFile(blob) {
        let fileType
        const zip = new (jszip__rspack_import_0_default())()
        return zip
            .loadAsync(blob)
            .then(zip => {
                if (zip.files["content.xml"]) {
                    fileType = "odt"
                    return zip.files["content.xml"].async("string")
                } else if (zip.files["word/document.xml"]) {
                    fileType = "docx"
                    return zip.files["word/document.xml"].async("string")
                } else {
                    throw new Error(gettext("Unknown filetype"))
                }
            })
            .then(string => {
                const expectedTags = this.documentTemplateValue.content
                    .map(node => {
                        switch (node.type) {
                            case "title":
                                return "title"
                            case "richtext_part":
                            case "table_part":
                                return `@${node.attrs.id}`
                            case "heading_part":
                            case "contributors_part":
                            case "tags_part":
                                return node.attrs.id
                            default:
                                return false
                        }
                    })
                    .concat(["@bibliography", "@copyright", "@licenses"])
                    .filter(tag => tag)
                const parser = new window.DOMParser()
                const xml = parser.parseFromString(string, "text/xml")
                if (fileType === "odt") {
                    this.checkODT(xml, expectedTags)
                } else {
                    this.checkDOCX(xml, expectedTags)
                }
                return fileType
            })
    }

    setStatus(_fileType, foundTags, missingTags) {
        this.dialog.dialogEl.querySelector(
            ".export-template-found-tags"
        ).innerHTML = foundTags.join(", ")
        this.dialog.dialogEl.querySelector(
            ".export-template-missing-tags"
        ).innerHTML = missingTags.join(", ")
    }

    checkODT(xml, expectedTags) {
        const pars = xml.querySelectorAll("p")
        const foundTags = []

        pars.forEach(par => {
            // Assuming there is nothing outside of <w:t>...</w:t>
            const text = par.textContent
            expectedTags.forEach(tag => {
                if (text.includes(`{${tag}}`)) {
                    foundTags.push(tag)
                }
            })
        })

        this.setStatus(
            "odt",
            foundTags,
            expectedTags.filter(tag => !foundTags.includes(tag))
        )
    }

    checkDOCX(xml, expectedTags) {
        const pars = xml.querySelectorAll("p,sectPr")
        const foundTags = []

        pars.forEach(par => {
            // Assuming there is nothing outside of <w:t>...</w:t>
            const text = par.textContent
            expectedTags.forEach(tag => {
                if (text.includes(`{${tag}}`)) {
                    foundTags.push(tag)
                }
            })
        })

        this.setStatus(
            "docx",
            foundTags,
            expectedTags.filter(tag => !foundTags.includes(tag))
        )
    }

    bind() {
        const mediaInputSelector = this.dialog.dialogEl.querySelector(
            ".fw-media-file-input"
        )
        this.dialog.dialogEl.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_1.findTarget)(event, ".fw-media-select-button", el): {
                    event.preventDefault()
                    mediaInputSelector.click()
                    break
                }
            }
        })

        mediaInputSelector.addEventListener("change", () => {
            this.showErrors([])
            const mediaInput = mediaInputSelector.files[0]
            if (!mediaInput) {
                return
            }
            this.checkFile(mediaInput)
                .then(fileType => {
                    this.addedFile = mediaInput
                    this.addedFileType = fileType
                    this.dialog.dialogEl.querySelector(
                        ".export-template-filetype"
                    ).innerHTML = fileType
                    this.dialog.dialogEl.querySelector(
                        ".export-template-file"
                    ).innerHTML = (0,_common__rspack_import_1.escapeText)(mediaInput.name)
                })
                .catch(() => {
                    this.showErrors([gettext("Selected file not supported.")])
                })
        })
    }
}


}),
"./js/modules/document_template/exporter.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateExporter: function() { return DocumentTemplateExporter; }
});
/* import */ var downloadjs__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/downloadjs@1.4.7/node_modules/downloadjs/download.js");
/* import */ var downloadjs__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(downloadjs__rspack_import_0);
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _exporter_tools_file__rspack_import_2 = __webpack_require__("./js/modules/exporter/tools/file.js");
/* import */ var _exporter_tools_zip__rspack_import_3 = __webpack_require__("./js/modules/exporter/tools/zip.js");






class DocumentTemplateExporter {
    constructor(
        id,
        getUrl = "/api/document/admin/get_template/",
        download = true,
        token = false
    ) {
        this.id = id
        this.getUrl = getUrl
        this.download = download
        this.token = token

        this.zipFileName = false
        this.docVersion = false
        this.textFiles = []
        this.httpFiles = []
    }

    init() {
        const params = this.token
            ? {id: this.id, token: this.token}
            : {id: this.id}
        return (0,_common__rspack_import_1.jsonPostJson)(this.getUrl, params).then(({json}) => {
            this.docVersion = json.doc_version
            this.zipFileName = `${(0,_exporter_tools_file__rspack_import_2.createSlug)(json.title)}.fidustemplate`
            this.textFiles.push({
                filename: "template.json",
                contents: JSON.stringify(json.content)
            })
            const exportTemplates = []
            json.export_templates.forEach(template => {
                const filename = `exporttemplates/${template.fields.template_file.split("/").slice(-1)[0]}`
                this.httpFiles.push({
                    filename,
                    url: template.fields.template_file
                })
                exportTemplates.push({
                    file: filename,
                    file_type: template.fields.file_type,
                    title: template.fields.title
                })
            })
            this.textFiles.push({
                filename: "exporttemplates.json",
                contents: JSON.stringify(exportTemplates)
            })
            const documentStyles = []
            json.document_styles.forEach(docStyle => {
                const style = {
                    contents: docStyle.fields.contents,
                    slug: docStyle.fields.slug,
                    title: docStyle.fields.title,
                    files: []
                }
                docStyle.fields.documentstylefile_set.forEach(docstyleFile => {
                    const filename = `documentstyles/${docstyleFile[1]}`
                    this.httpFiles.push({
                        filename,
                        url: docstyleFile[0]
                    })
                    style.files.push(filename)
                })
                documentStyles.push(style)
            })
            this.textFiles.push({
                filename: "documentstyles.json",
                contents: JSON.stringify(documentStyles)
            })
            if (this.download) {
                return this.createZip()
            }
            return Promise.resolve()
        })
    }

    createZip() {
        this.textFiles.push({
            filename: "filetype-version",
            contents: this.docVersion
        })
        const zipper = new _exporter_tools_zip__rspack_import_3.ZipFileCreator(
            this.textFiles,
            this.httpFiles,
            undefined,
            "application/fidustemplate+zip"
        )
        return zipper
            .init()
            .then(blob => downloadjs__rspack_import_0_default()(blob, this.zipFileName, "application/zip"))
    }
}


}),
"./js/modules/document_template/extract_template.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  extractTemplate: function() { return extractTemplate; }
});
/* import */ var _schema_document__rspack_import_0 = __webpack_require__("./js/modules/schema/document/index.js");
/* import */ var _schema_mini_json__rspack_import_1 = __webpack_require__("./js/modules/schema/mini_json.js");



function extractTemplate(doc) {
    const template = (0,_schema_mini_json__rspack_import_1.toFullJSON)(doc, _schema_document__rspack_import_0.docSchema)
    template.attrs.papersize = template.attrs.papersizes[0]
    template.content = template.content.filter(
        part => !part.attrs || !part.attrs.deleted
    )
    template.content.forEach(part => {
        delete part.content
        if (part.type === "title") {
            delete part.attrs
            return
        } else if (part.attrs.initial) {
            part.content = JSON.parse(JSON.stringify(part.attrs.initial))
        } else if (["heading_part", "richtext_part"].includes(part.type)) {
            part.content = [{type: part.attrs.elements[0]}]
        } else if (part.type === "table") {
            part.content = [
                {
                    type: "table",
                    content: [
                        {
                            type: "table_row",
                            content: [
                                {
                                    type: "table_cell",
                                    content: [{type: "paragraph"}]
                                }
                            ]
                        }
                    ]
                }
            ]
        }
        delete part.attrs.deleted
        if (!part.attrs.help) {
            delete part.attrs.help
        }
        if (!part.attrs.language) {
            delete part.attrs.language
        }
        if (!part.attrs.locking) {
            delete part.attrs.locking
        }
        if (!part.attrs.initial) {
            delete part.attrs.initial
        }
        if (!part.attrs.metadata) {
            delete part.attrs.metadata
        }
        delete part.attrs.hidden
        if (part.attrs.optional === "hidden") {
            part.attrs.hidden = true
        }
        if (!part.attrs.optional) {
            delete part.attrs.optional
        }
    })
    const documentStyles = [
        {
            title: template.attrs.documentstyle,
            slug: template.attrs.documentstyle,
            contents: "",
            files: []
        }
    ]
    return {
        content: (0,_schema_mini_json__rspack_import_1.toMiniJSON)(_schema_document__rspack_import_0.docSchema.nodeFromJSON(template)),
        documentStyles,
        exportTemplates: [],
        files: []
    }
}


}),
"./js/modules/document_template/fix_doc.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  adjustDocToTemplate: function() { return adjustDocToTemplate; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);
/* import */ var _schema_mini_json__rspack_import_1 = __webpack_require__("./js/modules/schema/mini_json.js");




function cleanFootnotes(node, elements, marks) {
    if (node.attrs?.footnote) {
        // We remove forbidden block nodes
        node.attrs.footnote = node.attrs.footnote.filter(
            node => !elements.includes(node.type)
        )
        // We remove forbidden marks + inline nodes
        node.attrs.footnote.forEach(subNode =>
            cleanNode(subNode, elements, marks)
        )
    }
    if (node.content) {
        node.content.forEach(subNode =>
            cleanFootnotes(subNode, elements, marks)
        )
    }
}

function cleanNode(node, elements, marks) {
    if (node.marks) {
        // remove forbidden marks
        node.marks = node.marks.filter(mark => !marks.includes(mark.type))
        if (!node.marks.length) {
            delete node.marks
        }
    }
    if (node.content) {
        // remove forbidden elements
        node.content = node.content.filter(
            node => !elements.includes(node.type)
        )
        node.content.forEach(subNode => cleanNode(subNode, elements, marks))
        if (!node.content.length) {
            delete node.content
        }
    }
}

function adjustDocToTemplate(
    miniDoc,
    miniTemplate,
    documentStyleSlugs,
    schema
) {
    const doc = (0,_schema_mini_json__rspack_import_1.toFullJSON)(miniDoc, schema),
        template = (0,_schema_mini_json__rspack_import_1.toFullJSON)(miniTemplate, schema),
        removedFootnoteElements = doc.attrs.footnote_elements.filter(
            element => !template.attrs.footnote_elements.includes(element)
        ),
        removedFootnoteMarks = doc.attrs.footnote_marks.filter(
            mark => !template.attrs.footnote_marks.includes(mark)
        ),
        attrs = [
            "footnote_marks",
            "footnote_elements",
            "languages",
            "citationstyles",
            "papersizes",
            "bibliography_header",
            "template",
            "import_id"
        ]
    attrs.forEach(attr => (doc.attrs[attr] = template.attrs[attr]))

    if (!doc.attrs.citationstyles.includes(doc.attrs.citationstyle)) {
        if (!doc.attrs.citationstyles.length) {
            throw new Error("Document template allows no citation styles.")
        }
        doc.attrs.citationstyle = template.attrs.citationstyle
    }

    if (!doc.attrs.languages.includes(doc.attrs.language)) {
        if (!doc.attrs.languages.length) {
            throw new Error("Document template allows no languages.")
        }
        doc.attrs.language = template.attrs.language
    }

    if (!doc.attrs.papersizes.includes(doc.attrs.papersize)) {
        if (!doc.attrs.papersizes.length) {
            throw new Error("Document template allows no paper sizes.")
        }
        doc.attrs.papersize = doc.attrs.papersizes[0]
    }

    if (!documentStyleSlugs.includes(doc.attrs.documentstyle)) {
        if (!documentStyleSlugs.length) {
            doc.attrs.documentstyle = false
        } else {
            doc.attrs.documentstyle = documentStyleSlugs[0]
        }
    }

    if (!doc.attrs.citationstyles.includes(doc.attrs.citationstyle)) {
        if (!doc.attrs.citationstyles.length) {
            throw new Error(
                "No citation styles have been defined for document template."
            )
        }
        doc.attrs.citationstyle = doc.attrs.citationstyles[0]
    }

    if (removedFootnoteMarks.length || removedFootnoteElements.length) {
        cleanFootnotes(doc, removedFootnoteElements, removedFootnoteMarks)
    }

    const oldContent = doc.content
    doc.content = [
        oldContent.shift() // The title
    ]

    let movedParts = []

    if (template.content) {
        template.content.slice(1).forEach(part => {
            let oldNode = oldContent.find(
                oldContentNode =>
                    oldContentNode.type === part.type &&
                    oldContentNode.attrs.id === part.attrs.id
            )
            if (oldNode) {
                while (oldNode !== oldContent[0]) {
                    const firstOldContent = oldContent.shift(),
                        inTemplate = !!template.content.find(
                            part =>
                                part.type === firstOldContent.type &&
                                part.attrs.id === firstOldContent.attrs.id
                        )
                    if (inTemplate) {
                        movedParts.push(firstOldContent)
                    } else if (
                        firstOldContent.content &&
                        !firstOldContent.attrs.hidden &&
                        firstOldContent.attrs.locking !== "fixed" &&
                        !(
                            // table with just first row, which is fixed.
                            (
                                firstOldContent.attrs.locking === "header" &&
                                firstOldContent.content.length === 1
                            )
                        ) &&
                        !(
                            // heading/richtext with just the default content
                            (
                                firstOldContent.attrs.elements &&
                                firstOldContent.content.length === 1 &&
                                firstOldContent.content[0].type ===
                                    firstOldContent.attrs.elements[0] &&
                                !firstOldContent.content[0].content
                            )
                        )
                    ) {
                        firstOldContent.attrs.deleted = true
                        doc.content.push(firstOldContent)
                    }
                }
                oldContent.shift()
            } else {
                oldNode = movedParts.find(
                    oldContentNode =>
                        oldContentNode.type === part.type &&
                        oldContentNode.attrs.id === part.attrs.id
                )
                if (oldNode) {
                    movedParts = movedParts.filter(
                        oldContentNode => oldContentNode !== oldNode
                    )
                }
            }
            if (oldNode) {
                const newNode = Object.assign({}, oldNode, {
                    attrs: {}
                })
                Object.entries(part.attrs).forEach(([key, value]) => {
                    newNode.attrs[key] = value
                })
                if (newNode.attrs.optional) {
                    newNode.attrs.hidden = oldNode.attrs.hidden
                }
                if (
                    (newNode.attrs.initial || oldNode.attrs.initial) &&
                    (oldNode.attrs.locking === "fixed" ||
                        fast_deep_equal__rspack_import_0_default()(
                            oldNode.attrs.initial || {},
                            oldNode.content || {}
                        ))
                ) {
                    if (newNode.attrs.initial) {
                        newNode.content = newNode.attrs.initial
                    } else {
                        delete newNode.content
                    }
                }

                if (oldNode.attrs.elements) {
                    // parts that define elements also define marks.
                    const removedElements = oldNode.attrs.elements.filter(
                        element => !newNode.attrs.elements.includes(element)
                    )
                    const removedMarks = oldNode.attrs.marks.filter(
                        mark => !newNode.attrs.marks.includes(mark)
                    )
                    if (removedElements.length || removedMarks.length) {
                        cleanNode(newNode, removedElements, removedMarks)
                        if (
                            !newNode.content &&
                            ["richtext_part", "heading_part"].includes(
                                part.type
                            )
                        ) {
                            newNode.content = [{type: part.attrs.elements[0]}]
                        } else if (
                            !newNode.content &&
                            part.type === "table_part"
                        ) {
                            newNode.content = [
                                {
                                    type: "table",
                                    content: [
                                        {type: "table_caption"},
                                        {
                                            type: "table_body",
                                            content: [
                                                {
                                                    type: "table_row",
                                                    content: [
                                                        {
                                                            type: "table_cell",
                                                            content: [
                                                                {
                                                                    type: "paragraph"
                                                                }
                                                            ]
                                                        }
                                                    ]
                                                }
                                            ]
                                        }
                                    ]
                                }
                            ]
                        }
                    }
                }
                doc.content.push(newNode)
            } else {
                // The node is new and didn't exist in the old document.
                doc.content.push(JSON.parse(JSON.stringify(part)))
            }
        })
    }

    // move remaining oldContent items that were not in template.
    while (oldContent.length) {
        const newNode = oldContent.shift()
        const attrs = Object.assign({}, newNode.attrs)
        if (attrs.hasOwnProperty("deleted")) {
            newNode.attrs.deleted = true
            doc.content.push(newNode)
        }
    }
    return doc
}


}),
"./js/modules/document_template/importer.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateImporter: function() { return DocumentTemplateImporter; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _importer_native_file__rspack_import_1 = __webpack_require__("./js/modules/importer/native/file.js");
/* import */ var _schema__rspack_import_2 = __webpack_require__("./js/modules/schema/index.js");
/* import */ var _update__rspack_import_3 = __webpack_require__("./js/modules/document_template/update.js");






const TEXT_FILENAMES = [
    "mimetype",
    "filetype-version",
    "template.json",
    "exporttemplates.json",
    "documentstyles.json"
]

class DocumentTemplateImporter {
    constructor(file, createUrl = "/api/document/admin/create_template/") {
        this.file = file
        this.createUrl = createUrl

        this.textFiles = []
        this.otherFiles = []
        this.ok = false
        this.statusText = ""
        this.docTemplate = false
    }

    init() {
        return new Promise(resolve => {
            // use a BlobReader to read the zip from a Blob object
            const reader = new window.FileReader()
            reader.onloadend = () => {
                if (
                    reader.result.length > 60 &&
                    reader.result.substring(0, 2) === "PK"
                ) {
                    this.initZipFileRead().then(() => resolve(this))
                } else {
                    this.statusText = gettext(
                        "The uploaded file does not appear to be a Fidus Writer Template file."
                    )
                    resolve(this)
                }
            }
            reader.readAsText(this.file)
        })
    }

    initZipFileRead() {
        // Extract all the files that can be found in every fidus-file (not images)
        return Promise.resolve(/* import() */).then(__webpack_require__.t.bind(__webpack_require__, "./node_modules/.pnpm/jszip@3.10.1/node_modules/jszip/dist/jszip.min.js", 23))
            .then(({default: JSZip}) => new JSZip())
            .then(zipfs => zipfs.loadAsync(this.file))
            .then(zipfs => {
                const filenames = [],
                    p = []
                let validFile = true

                zipfs.forEach(filename => filenames.push(filename))

                TEXT_FILENAMES.forEach(filename => {
                    if (filenames.indexOf(filename) === -1) {
                        validFile = false
                    }
                })
                if (!validFile) {
                    this.statusText = gettext(
                        "The uploaded file does not appear to be a Fidus Writer file."
                    )
                    return
                }

                filenames
                    .filter(filename => !filename.endsWith("/"))
                    .forEach(filename => {
                        p.push(
                            new Promise(resolve => {
                                let fileType, fileList
                                if (
                                    ["mimetype", "filetype-version"].includes(
                                        filename
                                    ) ||
                                    filename.endsWith(".json")
                                ) {
                                    fileType = "string"
                                    fileList = this.textFiles
                                } else {
                                    fileType = "blob"
                                    fileList = this.otherFiles
                                }
                                zipfs.files[filename]
                                    .async(fileType)
                                    .then(content => {
                                        fileList.push({filename, content})
                                        resolve()
                                    })
                            })
                        )
                    })
                return Promise.all(p).then(() =>
                    this.processFidusTemplateFile()
                )
            })
    }

    processFidusTemplateFile() {
        const filetypeVersion = Number.parseFloat(
                this.textFiles.find(
                    file => file.filename === "filetype-version"
                ).content
            ),
            mimeType = this.textFiles.find(
                file => file.filename === "mimetype"
            ).content
        if (
            mimeType === "application/fidustemplate+zip" &&
            filetypeVersion >= _importer_native_file__rspack_import_1.MIN_FW_DOCUMENT_VERSION &&
            filetypeVersion <= _importer_native_file__rspack_import_1.MAX_FW_DOCUMENT_VERSION
        ) {
            const template = JSON.parse(
                this.textFiles.find(file => file.filename === "template.json")
                    .content
            )
            const {title, content, exportTemplates, documentStyles} =
                (0,_update__rspack_import_3.updateTemplateFile)(
                    template.attrs.template,
                    template,
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "exporttemplates.json"
                        ).content
                    ),
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "documentstyles.json"
                        ).content
                    ),
                    filetypeVersion
                )
            return (0,_common__rspack_import_0.jsonPostJson)(
                this.createUrl,
                {
                    title,
                    content,
                    import_id: content.attrs.import_id,
                    export_templates: exportTemplates,
                    document_styles: documentStyles
                },
                false,
                {
                    files: this.otherFiles.map(
                        ({filename, content}) => new File([content], filename)
                    )
                }
            ).then(({json}) => {
                this.ok = true
                this.docTemplate = {
                    id: json.id,
                    title: json.title,
                    added: json.added,
                    updated: json.updated
                }
                this.statusText = `${(0,_common__rspack_import_0.escapeText)(title)} ${gettext("successfully imported.")}`
                return this
            })
        } else {
            // The file is not a Fidus Writer file.
            this.statusText =
                gettext(
                    "The uploaded file does not appear to be of the version used on this server: "
                ) + _schema__rspack_import_2.FW_DOCUMENT_VERSION
            return this
        }
    }
}


}),
"./js/modules/document_template/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateAdmin: function() { return /* reexport safe */ _change_admin__rspack_import_6.DocumentTemplateAdmin; },
  DocumentTemplateDesigner: function() { return /* reexport safe */ _designer__rspack_import_0.DocumentTemplateDesigner; },
  DocumentTemplateExporter: function() { return /* reexport safe */ _exporter__rspack_import_4.DocumentTemplateExporter; },
  DocumentTemplateImporter: function() { return /* reexport safe */ _importer__rspack_import_5.DocumentTemplateImporter; },
  DocumentTemplateListAdmin: function() { return /* reexport safe */ _list_admin__rspack_import_7.DocumentTemplateListAdmin; },
  adjustDocToTemplate: function() { return /* reexport safe */ _fix_doc__rspack_import_2.adjustDocToTemplate; },
  extractTemplate: function() { return /* reexport safe */ _extract_template__rspack_import_1.extractTemplate; },
  helpSchema: function() { return /* reexport safe */ _schema__rspack_import_3.helpSchema; },
  serializeHelp: function() { return /* reexport safe */ _schema__rspack_import_3.serializeHelp; },
  updateTemplateFile: function() { return /* reexport safe */ _update__rspack_import_8.updateTemplateFile; }
});
/* import */ var _designer__rspack_import_0 = __webpack_require__("./js/modules/document_template/designer.js");
/* import */ var _extract_template__rspack_import_1 = __webpack_require__("./js/modules/document_template/extract_template.js");
/* import */ var _fix_doc__rspack_import_2 = __webpack_require__("./js/modules/document_template/fix_doc.js");
/* import */ var _schema__rspack_import_3 = __webpack_require__("./js/modules/document_template/schema.js");
/* import */ var _exporter__rspack_import_4 = __webpack_require__("./js/modules/document_template/exporter.js");
/* import */ var _importer__rspack_import_5 = __webpack_require__("./js/modules/document_template/importer.js");
/* import */ var _change_admin__rspack_import_6 = __webpack_require__("./js/modules/document_template/change_admin.js");
/* import */ var _list_admin__rspack_import_7 = __webpack_require__("./js/modules/document_template/list_admin.js");
/* import */ var _update__rspack_import_8 = __webpack_require__("./js/modules/document_template/update.js");











}),
"./js/modules/document_template/list_admin.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentTemplateListAdmin: function() { return DocumentTemplateListAdmin; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _exporter__rspack_import_1 = __webpack_require__("./js/modules/document_template/exporter.js");
/* import */ var _importer__rspack_import_2 = __webpack_require__("./js/modules/document_template/importer.js");




class DocumentTemplateListAdmin {
    constructor(settings) {
        this.settings = settings
        this.objectTools = false
        this.actionDropdown = false
    }

    init() {
        if (
            window.location.search.length &&
            window.location.search.includes("debug=true")
        ) {
            return
        }
        (0,_common__rspack_import_0.ensureCSS)([
            staticUrl("css/document_template_admin.css"),
            staticUrl("css/admin.css")
        ])

        ;(0,_common__rspack_import_0.whenReady)().then(() => {
            this.objectTools = document.querySelector("ul.object-tools")
            this.actionDropdown = document.querySelector(
                'select[name="action"]'
            )
            this.modifyDOM()
            this.bind()
        })
    }

    modifyDOM() {
        this.objectTools.insertAdjacentHTML(
            "beforeend",
            `<li>
                <span class="link" id="upload-template">${gettext("Upload FIDUSTEMPLATE")}</span>
            </li>`
        )
        this.actionDropdown.insertAdjacentHTML(
            "beforeend",
            `<option value="download">${gettext("Download selected as FIDUSTEMPLATE")}</option>`
        )
    }

    showErrors(errors) {
        this.templateDesignerBlock.querySelector("ul.errorlist").innerHTML =
            Object.values(errors)
                .map(error => `<li>${(0,_common__rspack_import_0.escapeText)(error)}</li>`)
                .join("")
    }

    bind() {
        document.body.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, "#upload-template", el): {
                    event.preventDefault()
                    const fileSelector = document.createElement("input")
                    fileSelector.id = "fidus-template-uploader"
                    fileSelector.setAttribute("type", "file")
                    fileSelector.setAttribute("multiple", "")
                    fileSelector.setAttribute("accept", ".fidustemplate")
                    document.body.appendChild(fileSelector)
                    fileSelector.click()
                    fileSelector.addEventListener("change", () => {
                        const files = Array.from(fileSelector.files).filter(
                            file => {
                                //TODO: This is an arbitrary size. What should be done with huge import files?
                                if (
                                    file.length === 0 ||
                                    file.size > 104857600
                                ) {
                                    return false
                                }
                                return true
                            }
                        )
                        Promise.all(
                            files.map(file => {
                                const importer = new _importer__rspack_import_2.DocumentTemplateImporter(
                                    file
                                )
                                return importer.init()
                            })
                        ).then(() => window.location.reload())
                    })
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, "button[type=submit]", el):
                    if (this.actionDropdown.value === "download") {
                        event.preventDefault()
                        const ids = Array.from(
                            document.querySelectorAll(
                                '#result_list tr.selected input[type="checkbox"]'
                            )
                        ).map(el => Number.parseInt(el.value))
                        ids.forEach(id => {
                            const exporter = new _exporter__rspack_import_1.DocumentTemplateExporter(id)
                            exporter.init()
                        })
                    }
                    break
                default:
                    break
            }
        })
    }
}


}),
"./js/modules/document_template/schema.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  contributorsPartSchema: function() { return contributorsPartSchema; },
  headingMenuContent: function() { return headingMenuContent; },
  headingPartSchema: function() { return headingPartSchema; },
  helpMenuContent: function() { return helpMenuContent; },
  helpSchema: function() { return helpSchema; },
  richtextMenuContent: function() { return richtextMenuContent; },
  richtextPartSchema: function() { return richtextPartSchema; },
  serializeHelp: function() { return serializeHelp; },
  tableMenuContent: function() { return tableMenuContent; },
  tablePartSchema: function() { return tablePartSchema; },
  tagsPartSchema: function() { return tagsPartSchema; }
});
/* import */ var prosemirror_example_setup__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-example-setup@1.2.3/node_modules/prosemirror-example-setup/dist/index.js");
/* import */ var prosemirror_menu__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-menu@1.2.4/node_modules/prosemirror-menu/dist/index.js");
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_schema_basic__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-basic@1.2.4/node_modules/prosemirror-schema-basic/dist/index.js");
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var _schema_document__rspack_import_1 = __webpack_require__("./js/modules/schema/document/index.js");







const doc = {
    content: "block+",
    toDOM(_node) {
        return ["div", 0]
    }
}

// from https://github.com/ProseMirror/prosemirror-tables/blob/master/src/util.js
const findTable = state => {
    const $head = state.selection.$head
    for (let d = $head.depth; d > 0; d--) {
        if ($head.node(d).type.spec.tableRole == "table") {
            return $head.node(d)
        }
    }
    return false
}

const helpSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: prosemirror_schema_basic__rspack_import_3.schema.spec.nodes.remove("code_block")
        .remove("image")
        .remove("heading")
        .remove("horizontal_rule")
        .update("doc", doc),
    marks: prosemirror_schema_basic__rspack_import_3.schema.spec.marks.remove("code").update("link", {
        attrs: {
            href: {},
            title: {default: null}
        },
        inclusive: false,
        parseDOM: [
            {
                tag: "a[href]",
                getAttrs(dom) {
                    return {
                        href: dom.getAttribute("href"),
                        title: dom.getAttribute("title")
                    }
                }
            }
        ],
        toDOM(node) {
            return ["a", Object.assign({target: "_blank"}, node.attrs), 0]
        }
    })
})

const helpMenuContent = (0,prosemirror_example_setup__rspack_import_4.buildMenuItems)(helpSchema).fullMenu
helpMenuContent.splice(1, 1) // full menu minus drop downs

const helpSerializer = prosemirror_model__rspack_import_2.DOMSerializer.fromSchema(helpSchema)

const serializeHelp = content => {
    const doc = {type: "doc", content},
        pmNode = helpSchema.nodeFromJSON(doc),
        dom = helpSerializer.serializeNode(pmNode)
    return dom.innerHTML
}

const richtextPartSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: _schema_document__rspack_import_1.docSchema.spec.nodes.update("doc", {content: "richtext_part"}),
    marks: _schema_document__rspack_import_1.docSchema.spec.marks
})

const richtextMenuContent = (0,prosemirror_example_setup__rspack_import_4.buildMenuItems)(richtextPartSchema).fullMenu
for (let i = 1; i <= 6; i++) {
    const type = richtextPartSchema.nodes[`heading${i}`]
    richtextMenuContent[1][1].content.push(
        (0,prosemirror_menu__rspack_import_5.blockTypeItem)(type, {
            title: gettext("Change to heading ") + i,
            label: gettext("Heading level ") + i
        })
    )
}

const type = richtextPartSchema.nodes["table"]
richtextMenuContent[1][0].content.push(
    (0,prosemirror_menu__rspack_import_5.blockTypeItem)(type, {
        title: gettext("Insert Table"),
        label: gettext("Table"),
        enable(state) {
            return !findTable(state)
        },
        run(state, dispatch) {
            const table = {
                type: "table",
                content: [
                    {type: "table_caption"},
                    {
                        type: "table_body",
                        content: [
                            {
                                type: "table_row",
                                content: [
                                    {
                                        type: "table_cell",
                                        content: [{type: "paragraph"}]
                                    }
                                ]
                            }
                        ]
                    }
                ]
            }
            const schema = state.schema
            dispatch(
                state.tr.replaceSelectionWith(schema.nodeFromJSON(table)),
                false
            )
        }
    })
)

const tablePartSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: _schema_document__rspack_import_1.docSchema.spec.nodes.update("doc", {content: "table_part"})
        .update("table_row", {
            content: "(table_cell | table_header)+",
            tableRole: "row",
            parseDOM: [{tag: "tr"}],
            toDOM() {
                return ["tr", 0]
            }
        }),
    marks: _schema_document__rspack_import_1.docSchema.spec.marks
})

const tableMenuContent = (0,prosemirror_example_setup__rspack_import_4.buildMenuItems)(tablePartSchema).fullMenu
for (let i = 1; i <= 6; i++) {
    const type = tablePartSchema.nodes[`heading${i}`]
    tableMenuContent[1][1].content.push(
        (0,prosemirror_menu__rspack_import_5.blockTypeItem)(type, {
            title: gettext("Change to heading ") + i,
            label: gettext("Heading level ") + i
        })
    )
}
function tableMenuItem(label, cmd) {
    return new prosemirror_menu__rspack_import_5.MenuItem({label, select: cmd, run: cmd})
}
const tableMenu = [
    tableMenuItem(gettext("Insert column after"), prosemirror_tables__rspack_import_0.addColumnAfter),
    tableMenuItem(gettext("Insert column before"), prosemirror_tables__rspack_import_0.addColumnBefore),
    tableMenuItem(gettext("Insert column after"), prosemirror_tables__rspack_import_0.addColumnAfter),
    tableMenuItem(gettext("Delete column"), prosemirror_tables__rspack_import_0.deleteColumn),
    tableMenuItem(gettext("Insert row before"), prosemirror_tables__rspack_import_0.addRowBefore),
    tableMenuItem(gettext("Insert row after"), prosemirror_tables__rspack_import_0.addRowAfter),
    tableMenuItem(gettext("Delete row"), prosemirror_tables__rspack_import_0.deleteRow),
    tableMenuItem(gettext("Split cell"), prosemirror_tables__rspack_import_0.splitCell),
    tableMenuItem(gettext("Merge cells"), prosemirror_tables__rspack_import_0.mergeCells),
    tableMenuItem(gettext("Toggle header column"), prosemirror_tables__rspack_import_0.toggleHeaderColumn),
    tableMenuItem(gettext("Toggle header row"), prosemirror_tables__rspack_import_0.toggleHeaderRow),
    tableMenuItem(gettext("Toggle header cells"), prosemirror_tables__rspack_import_0.toggleHeaderCell),
    tableMenuItem(gettext("Delete table"), (state, dispatch) => {
        // Adjusted from https://github.com/ProseMirror/prosemirror-tables/blob/master/src/commands.js
        const $pos = state.selection.$anchor
        for (let d = $pos.depth; d > 0; d--) {
            const node = $pos.node(d)
            if (node.type.name == "table") {
                if (dispatch) {
                    dispatch(
                        state.tr
                            .delete($pos.before(d), $pos.after(d))
                            .scrollIntoView()
                    )
                }
                return true
            }
        }
        return false
    })
]
tableMenuContent.splice(2, 0, [
    new prosemirror_menu__rspack_import_5.Dropdown(tableMenu, {label: gettext("Table")})
])

richtextMenuContent.splice(2, 0, [
    new prosemirror_menu__rspack_import_5.Dropdown(tableMenu, {label: gettext("Table")})
])

const headingPartSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: _schema_document__rspack_import_1.docSchema.spec.nodes.update("doc", {content: "heading_part"})
        .remove("horizontal_rule")
        .remove("paragraph")
        .remove("code_block"),
    marks: _schema_document__rspack_import_1.docSchema.spec.marks
})

const headingMenuContent = (0,prosemirror_example_setup__rspack_import_4.buildMenuItems)(headingPartSchema).fullMenu
for (let i = 1; i <= 6; i++) {
    const type = headingPartSchema.nodes[`heading${i}`]
    headingMenuContent[1][1].content.push(
        (0,prosemirror_menu__rspack_import_5.blockTypeItem)(type, {
            title: gettext("Change to heading ") + i,
            label: gettext("Heading level ") + i
        })
    )
}

const tagsPartSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: {
        doc: {content: "tags_part"},
        tags_part: _schema_document__rspack_import_1.docSchema.spec.nodes.get("tags_part"),
        tag: _schema_document__rspack_import_1.docSchema.spec.nodes.get("tag"),
        text: _schema_document__rspack_import_1.docSchema.spec.nodes.get("text")
    },
    marks: _schema_document__rspack_import_1.docSchema.spec.marks
})

const contributorsPartSchema = new prosemirror_model__rspack_import_2.Schema({
    nodes: {
        doc: {content: "contributors_part"},
        contributors_part: _schema_document__rspack_import_1.docSchema.spec.nodes.get("contributors_part"),
        contributor: _schema_document__rspack_import_1.docSchema.spec.nodes.get("contributor"),
        text: _schema_document__rspack_import_1.docSchema.spec.nodes.get("text")
    },
    marks: _schema_document__rspack_import_1.docSchema.spec.marks
})


}),
"./js/modules/document_template/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  bibliographyHeaderTemplate: function() { return bibliographyHeaderTemplate; },
  citationstyleTemplate: function() { return citationstyleTemplate; },
  documentDesignerTemplate: function() { return documentDesignerTemplate; },
  documentStylesTemplate: function() { return documentStylesTemplate; },
  exportTemplatesTemplate: function() { return exportTemplatesTemplate; },
  languageTemplate: function() { return languageTemplate; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_const__rspack_import_1 = __webpack_require__("./js/modules/schema/const.js");



const allowedElementsTemplate = (
    {elements},
    {isFootnote = false, isTable = false} = {}
) =>
    `<div class="label">
    ${gettext("Allowed elements")}
</div>
<label>
    <input type="checkbox" class="elements" value="paragraph" ${elements.includes("paragraph") ? "checked" : ""}/>
    ${gettext("Paragraph")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading1" ${elements.includes("heading1") ? "checked" : ""}/>
    ${gettext("Heading 1")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading2" ${elements.includes("heading2") ? "checked" : ""}/>
    ${gettext("Heading 2")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading3" ${elements.includes("heading3") ? "checked" : ""}/>
    ${gettext("Heading 3")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading4" ${elements.includes("heading4") ? "checked" : ""}/>
    ${gettext("Heading 4")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading5" ${elements.includes("heading5") ? "checked" : ""}/>
    ${gettext("Heading 5")}
</label>
<label>
    <input type="checkbox" class="elements" value="heading6" ${elements.includes("heading6") ? "checked" : ""}/>
    ${gettext("Heading 6")}
</label>
${
    isFootnote
        ? ""
        : `<label>
        <input type="checkbox" class="elements" value="code_block" ${elements.includes("code_block") ? "checked" : ""}/>
        ${gettext("Code block")}
    </label>`
}
<label>
    <input type="checkbox" class="elements" value="figure" ${elements.includes("figure") ? "checked" : ""}/>
    ${gettext("Figure")}
</label>
<label>
    <input type="checkbox" class="elements" value="ordered_list" ${elements.includes("ordered_list") ? "checked" : ""}/>
    ${gettext("Ordered list")}
</label>
<label>
    <input type="checkbox" class="elements" value="bullet_list" ${elements.includes("bullet_list") ? "checked" : ""}/>
    ${gettext("Bullet list")}
</label>
<label>
    <input type="checkbox" class="elements" value="horizontal_rule" ${elements.includes("horizontal_rule") ? "checked" : ""}/>
    ${gettext("Horizontal rule")}
</label>
<label>
    <input type="checkbox" class="elements" value="equation" ${elements.includes("equation") ? "checked" : ""}/>
    ${gettext("Equation")}
</label>
<label>
    <input type="checkbox" class="elements" value="citation" ${elements.includes("citation") ? "checked" : ""}/>
    ${gettext("Citation")}
</label>
<label>
    <input type="checkbox" class="elements" value="cross_reference" ${elements.includes("cross_reference") ? "checked" : ""}/>
    ${gettext("Cross reference")}
</label>
<label>
    <input type="checkbox" class="elements" value="blockquote" ${elements.includes("blockquote") ? "checked" : ""}/>
    ${gettext("Blockquote")}
</label>
${
    isFootnote
        ? ""
        : `<label>
        <input type="checkbox" class="elements" value="footnote" ${elements.includes("footnote") ? "checked" : ""}/>
        ${gettext("Footnote")}
    </label>`
}
${
    isTable
        ? ""
        : `<label>
        <input type="checkbox" class="elements" value="table" ${elements.includes("table") ? "checked" : ""}/>
        ${gettext("Table")}
    </label>`
}`

const allowedMarksTemplate = ({marks}) =>
    `<div class="label">
    ${gettext("Allowed marks")}
</div>
<label>
    <input type="checkbox" class="marks" value="strong" ${marks.includes("strong") ? "checked" : ""}/>
    ${gettext("Strong")}
</label>
<label>
    <input type="checkbox" class="marks" value="em" ${marks.includes("em") ? "checked" : ""}/>
    ${gettext("Emphasis")}
</label>
<label>
    <input type="checkbox" class="marks" value="underline" ${marks.includes("underline") ? "checked" : ""}/>
    ${gettext("Underline")}
</label>
<label>
    <input type="checkbox" class="marks" value="link" ${marks.includes("link") ? "checked" : ""}/>
    ${gettext("Link")}
</label>
<label>
    <input type="checkbox" class="marks" value="sup" ${marks.includes("sup") ? "checked" : ""}/>
    ${gettext("Superscript")}
</label>
<label>
    <input type="checkbox" class="marks" value="sub" ${marks.includes("sub") ? "checked" : ""}/>
    ${gettext("Subscript")}
</label>
<label>
    <input type="checkbox" class="marks" value="code" ${marks.includes("code") ? "checked" : ""}/>
    ${gettext("Code")}
</label>`

const headingTemplate = ({
    id = "",
    title = "",
    elements = [
        "heading1",
        "heading2",
        "heading3",
        "heading4",
        "heading5",
        "heading6"
    ],
    marks = ["strong", "em", "underline", "link", "sup", "sub", "code"],
    locking = "false",
    optional = "false",
    language = false,
    metadata = false
}) =>
    `<div class="doc-part-block" data-type="heading_part">
    <div class="doc-part-header">
        ${gettext("Heading")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Metadata function")}
            <select class="metadata">
                <option value="false" ${metadata === false ? "selected" : ""}>${gettext("None")}</option>
                <option value="subtitle" ${metadata === "subtitle" ? "selected" : ""}>${gettext("Subtitle")}</option>
                <option value="title" ${metadata === "title" ? "selected" : ""}>${gettext("Title (translated)")}</option>
            </select>
        </div>
        <div class="label">${gettext("Locking")}
            <select class="locking">
                <option value="false" ${locking === "false" ? "selected" : ""}>${gettext("User can change contents")}</option>
                <option value="fixed" ${locking === "fixed" ? "selected" : ""}>${gettext("User can not change contents")}</option>
                <option value="start" ${locking === "start" ? "selected" : ""}>${gettext("User can only add content")}</option>
            </select>
        </div>
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
        <div class="label">
            ${gettext("Allowed headings")}
        </div>
        <label>
            <input type="checkbox" class="elements" value="heading1" ${elements.includes("heading1") ? "checked" : ""}/>
            ${gettext("Heading 1")}
        </label>
        <label>
            <input type="checkbox" class="elements" value="heading2" ${elements.includes("heading2") ? "checked" : ""}/>
            ${gettext("Heading 2")}
        </label>
        <label>
            <input type="checkbox" class="elements" value="heading3" ${elements.includes("heading3") ? "checked" : ""}/>
            ${gettext("Heading 3")}
        </label>
        <label>
            <input type="checkbox" class="elements" value="heading4" ${elements.includes("heading4") ? "checked" : ""}/>
            ${gettext("Heading 4")}
        </label>
        <label>
            <input type="checkbox" class="elements" value="heading5" ${elements.includes("heading5") ? "checked" : ""}/>
            ${gettext("Heading 5")}
        </label>
        <label>
            <input type="checkbox" class="elements" value="heading6" ${elements.includes("heading6") ? "checked" : ""}/>
            ${gettext("Heading 6")}
        </label>
        ${allowedMarksTemplate({marks})}
        <div class="label">${gettext("Language")}
            <select class="language">
                <option value="false" ${language === false ? "selected" : ""}>${gettext("Document language")}</option>
                ${_schema_const__rspack_import_1.LANGUAGES.map(
                    ([code, name]) =>
                        `<option value="${code}" ${language === code ? "selected" : ""}>${name}</option>`
                ).join("")}
            </select>
        </div>
        <div>
            <div class="label">${gettext("Initial content")}</div>
            <div class="initial"></div>
        </div>
        <div>
            <div class="label">${gettext("Instructions")}</div>
            <div class="instructions"></div>
        </div>
    </div>
</div>`

const contributorsTemplate = ({
    id = "",
    title = "",
    item_title = "",
    locking = "false",
    optional = "false",
    metadata = false
}) =>
    `<div class="doc-part-block" data-type="contributors_part">
    <div class="doc-part-header">
        ${gettext("Namelist")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Item title")} <input type="text" class="item_title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(item_title)}"></div>
        <div class="label">${gettext("Metadata function")}
            <select class="metadata">
                <option value="false" ${metadata === false ? "selected" : ""}>${gettext("None")}</option>
                <option value="authors" ${metadata === "authors" ? "selected" : ""}>${gettext("Authors")}</option>
                <option value="editors" ${metadata === "editors" ? "selected" : ""}>${gettext("Editors")}</option>
            </select>
        </div>
        <div class="label">${gettext("Locking")}
            <select class="locking">
                <option value="false" ${locking === "false" ? "selected" : ""}>${gettext("User can change contents")}</option>
                <option value="fixed" ${locking === "fixed" ? "selected" : ""}>${gettext("User can not change contents")}</option>
                <option value="start" ${locking === "start" ? "selected" : ""}>${gettext("User can only add content")}</option>
            </select>
        </div>
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
        <div>
            <div class="label">${gettext("Initial content")}</div>
            <div class="initial"></div>
        </div>
        <div>
            <div class="label">${gettext("Instructions")}</div>
            <div class="instructions"></div>
        </div>
    </div>
</div>`

const richtextTemplate = ({
    id = "",
    title = "",
    elements = [
        "paragraph",
        "heading1",
        "heading2",
        "heading3",
        "heading4",
        "heading5",
        "heading6",
        "figure",
        "ordered_list",
        "bullet_list",
        "horizontal_rule",
        "equation",
        "citation",
        "cross_reference",
        "blockquote",
        "footnote"
    ],
    marks = ["strong", "em", "underline", "link", "sup", "sub", "code"],
    locking = "false",
    optional = "false",
    language = false,
    metadata = false
}) => `<div class="doc-part-block" data-type="richtext_part">
    <div class="doc-part-header">
        ${gettext("Richtext")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Metadata function")}
            <select class="metadata">
                <option value="false" ${metadata === false ? "selected" : ""}>${gettext("None")}</option>
                <option value="abstract" ${metadata === "abstract" ? "selected" : ""}>${gettext("Abstract")}</option>
                <option value="cases" ${metadata === "cases" ? "selected" : ""}>${gettext("Cases/Case Reports")}</option>
                <option value="conclusions" ${metadata === "conclusions" ? "selected" : ""}>${gettext("Conclusions/Comment")}</option>
                <option value="discussion" ${metadata === "discussion" ? "selected" : ""}>${gettext("Discussion/Interpretation")}</option>
                <option value="intro" ${metadata === "intro" ? "selected" : ""}>${gettext("Introduction/Synopsis")}</option>
                <option value="materials" ${metadata === "materials" ? "selected" : ""}>${gettext("Materials")}</option>
                <option value="methods" ${metadata === "methods" ? "selected" : ""}>${gettext("Methods/Methodology/Procedures")}</option>
                <option value="results" ${metadata === "results" ? "selected" : ""}>${gettext("Results/Statement of Findings")}</option>
                <option value="subjects" ${metadata === "subjects" ? "selected" : ""}>${gettext("Subjects/Participants/Patients")}</option>
                <option value="supplementary-material" ${metadata === "supplementary-material" ? "selected" : ""}>${gettext("Supplementary materials")}</option>
            </select>
        </div>
        <div class="label">${gettext("Locking")}
            <select class="locking">
                <option value="false" ${locking === "false" ? "selected" : ""}>${gettext("User can change contents")}</option>
                <option value="fixed" ${locking === "fixed" ? "selected" : ""}>${gettext("User can not change contents")}</option>
                <option value="start" ${locking === "start" ? "selected" : ""}>${gettext("User can only add content")}</option>
            </select>
        </div>
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
        ${allowedElementsTemplate({elements})}
        ${allowedMarksTemplate({marks})}
        <div class="label">${gettext("Language")}
            <select class="language">
                <option value="false" ${language === false ? "selected" : ""}>${gettext("Document language")}</option>
                ${_schema_const__rspack_import_1.LANGUAGES.map(
                    ([code, name]) =>
                        `<option value="${code}" ${language === code ? "selected" : ""}>${name}</option>`
                ).join("")}
            </select>
        </div>
        <div>
            <div class="label">${gettext("Initial content")}</div>
            <div class="initial"></div>
        </div>
        <div>
            <div class="label">${gettext("Instructions")}</div>
            <div class="instructions"></div>
        </div>
    </div>
</div>`

const separatorTemplate = ({id = ""}) =>
    `<div class="doc-part-block" data-type="separator_part">
    <div class="doc-part-header">
        ${gettext("Separator")}
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
        </div>
    </div>
</div>`

const tagsTemplate = ({
    id = "",
    title = "",
    item_title = "",
    locking = "false",
    optional = "false",
    metadata = false
}) =>
    `<div class="doc-part-block" data-type="tags_part">
    <div class="doc-part-header">
        ${gettext("Tags")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Item title")} <input type="text fw-inline" class="item_title" value="${(0,_common__rspack_import_0.escapeText)(item_title)}"></div>
        <div class="label">${gettext("Metadata function")}
            <select class="metadata">
                <option value="false" ${metadata === false ? "selected" : ""}>${gettext("None")}</option>
                <option value="keywords" ${metadata === "keywords" ? "selected" : ""}>${gettext("Keywords")}</option>
            </select>
        </div>
        <div class="label">${gettext("Locking")}
            <select class="locking">
                <option value="false" ${locking === "false" ? "selected" : ""}>${gettext("User can change contents")}</option>
                <option value="fixed" ${locking === "fixed" ? "selected" : ""}>${gettext("User can not change contents")}</option>
                <option value="start" ${locking === "start" ? "selected" : ""}>${gettext("User can only add content")}</option>
            </select>
        </div>
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
        <div>
            <div class="label">${gettext("Initial content")}</div>
            <div class="initial"></div>
        </div>
        <div>
            <div class="label">${gettext("Instructions")}</div>
            <div class="instructions"></div>
        </div>
    </div>
</div>`

const tableTemplate = ({
    id = "",
    title = "",
    elements = [
        "paragraph",
        "heading1",
        "heading2",
        "heading3",
        "heading4",
        "heading5",
        "heading6",
        "code_block",
        "figure",
        "ordered_list",
        "bullet_list",
        "horizontal_rule",
        "equation",
        "citation",
        "cross_reference",
        "blockquote",
        "footnote"
    ],
    marks = ["strong", "em", "underline", "link", "sup", "sub", "code"],
    locking = "false",
    optional = "false",
    language = false
}) =>
    `<div class="doc-part-block" data-type="table_part">
    <div class="doc-part-header">
        ${gettext("Table")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Locking")}
            <select class="locking">
                <option value="false" ${locking === "false" ? "selected" : ""}>${gettext("User can change contents")}</option>
                <option value="fixed" ${locking === "fixed" ? "selected" : ""}>${gettext("User can not change contents")}</option>
                <option value="header" ${locking === "header" ? "selected" : ""}>${gettext("User can not change first row")}</option>
            </select>
        </div>
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
        ${allowedElementsTemplate({elements}, {isTable: true})}
        ${allowedMarksTemplate({marks})}
        <div class="label">${gettext("Language")}
            <select class="language">
                <option value="false" ${language === false ? "selected" : ""}>${gettext("Document language")}</option>
                ${_schema_const__rspack_import_1.LANGUAGES.map(
                    ([code, name]) =>
                        `<option value="${code}" ${language === code ? "selected" : ""}>${name}</option>`
                ).join("")}
            </select>
        </div>
        <div>
            <div class="label">${gettext("Initial content")}</div>
            <div class="initial"></div>
        </div>
        <div>
            <div class="label">${gettext("Instructions")}</div>
            <div class="instructions"></div>
        </div>
    </div>
</div>`

const tocTemplate = ({id = "", title = "", optional = "false"}) =>
    `<div class="doc-part-block" data-type="table_of_contents">
    <div class="doc-part-header">
        ${gettext("Table of Contents")}
        <ul class="object-tools right">
            <li>
                <span class="link configure">${gettext("Configure")}</span>
            </li>
        </ul>
        <div class="label">
            ${gettext("ID")} <input type="text" class="id fw-inline" value="${(0,_common__rspack_import_0.escapeText)(id)}">
            ${gettext("Title")} <input type="text" class="title fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}">
        </div>
    </div>
    <div class="attrs hidden">
        <div class="label">${gettext("Optional")}
            <select class="optional">
                <option value="false" ${optional === "false" ? "selected" : ""}>${gettext("Obligatory field")}</option>
                <option value="shown" ${optional === "shown" ? "selected" : ""}>${gettext("Optional, shown by default")}</option>
                <option value="hidden" ${optional === "hidden" ? "selected" : ""}>${gettext("Optional, not shown by default")}</option>
            </select>
        </div>
    </div>
</div>`

const footnoteTemplate = ({
    footnote_elements = [
        "paragraph",
        "heading1",
        "heading2",
        "heading3",
        "heading4",
        "heading5",
        "heading6",
        "figure",
        "ordered_list",
        "bullet_list",
        "horizontal_rule",
        "equation",
        "citation",
        "cross_reference",
        "blockquote",
        "table"
    ],
    footnote_marks = ["strong", "em", "underline", "link", "sup", "sub", "code"]
}) =>
    `<div class="doc-part-block attrs">${allowedElementsTemplate({elements: footnote_elements}, {isFootnote: true})}${allowedMarksTemplate({marks: footnote_marks})}</div>`

const citationstylesTemplate = (
    {citationstyles = ["apa"]},
    allCitationStyles
) =>
    `<select multiple size=5>
${Object.entries(allCitationStyles)
    .map(
        ([key, value]) =>
            `<option value="${key}"${citationstyles.includes(key) ? " selected" : ""}>${value}</option>`
    )
    .join("")}
</select>`

const citationstyleTemplate = (
    {citationstyle = "apa", citationstyles = ["apa"]},
    allCitationStyles
) => {
    if (!citationstyles.includes(citationstyle)) {
        citationstyle = citationstyles[0]
    }
    return `<select>
        ${citationstyles
            .map(
                key =>
                    `<option value="${key}"${citationstyle === key ? " selected" : ""}>${
                        allCitationStyles[key]
                    }</option>`
            )
            .join("")}
    </select>`
}

const languagesTemplate = ({languages = _schema_const__rspack_import_1.LANGUAGES.map(lang => lang[0])}) =>
    `<select multiple size=5>
${_schema_const__rspack_import_1.LANGUAGES.map(lang => `<option value="${lang[0]}"${languages.includes(lang[0]) ? " selected" : ""}>${lang[1]}</option>`).join("")}
</select>`

const languageTemplate = ({
    language = "en-US",
    languages = _schema_const__rspack_import_1.LANGUAGES.map(lang => lang[0])
}) => {
    if (!languages.includes(language)) {
        language = languages[0]
    }
    return `<select>
        ${_schema_const__rspack_import_1.LANGUAGES.filter(lang => languages.includes(lang[0]))
            .map(
                lang =>
                    `<option value="${lang[0]}"${language === lang[0] ? " selected" : ""}>${lang[1]}</option>`
            )
            .join("")}
    </select>`
}

const papersizesTemplate = ({papersizes = _schema_const__rspack_import_1.PAPER_SIZES.map(size => size[0])}) =>
    `<select multiple size=5>
${_schema_const__rspack_import_1.PAPER_SIZES.map(size => `<option value="${size[0]}"${papersizes.includes(size[0]) ? " selected" : ""}>${size[0]}</option>`).join("")}
</select>`

const languageSelector = language =>
    `<select>
${_schema_const__rspack_import_1.LANGUAGES.map(lang => `<option value="${lang[0]}"${language === lang[0] ? " selected" : ""}>${lang[1]}</option>`).join("")}
</select>`

const bibliographyHeaderTemplate = ({
    bibliography_header = {zzz: ""}
}) => {
    let translations = Object.entries(bibliography_header)
    if (!translations.length) {
        translations = [["zzz", ""]]
    }
    return `<table class="fw-dialog-table fw-small input-list-wrapper">${translations
        .map(
            translation =>
                `<tr>
                    <td>${languageSelector(translation[0])}</td>
                    <td>
                        <input type="text" value="${(0,_common__rspack_import_0.escapeText)(translation[1])}" >
                    </td>
                    <td class="input-field-list-ctrl">
                        <span class="fa fa-minus-circle" tabindex="0"></span>&nbsp;<span class="fa fa-plus-circle" tabindex="0"></span>
                    </td>
                </tr>`
        )
        .join("")}</table>`
}

const codeLanguagesTemplate = ({
    code_languages = [
        "javascript",
        "python",
        "java",
        "cpp",
        "c",
        "csharp",
        "php",
        "ruby",
        "go",
        "rust",
        "swift",
        "kotlin",
        "typescript",
        "html",
        "css",
        "sql",
        "bash",
        "shell",
        "r",
        "matlab",
        "scala",
        "perl",
        "lua",
        "haskell",
        "xml",
        "json",
        "yaml",
        "markdown"
    ]
}) => {
    const allLanguages = [
        "javascript",
        "python",
        "java",
        "cpp",
        "c",
        "csharp",
        "php",
        "ruby",
        "go",
        "rust",
        "swift",
        "kotlin",
        "typescript",
        "html",
        "css",
        "sql",
        "bash",
        "shell",
        "r",
        "matlab",
        "scala",
        "perl",
        "lua",
        "haskell",
        "xml",
        "json",
        "yaml",
        "markdown"
    ]
    return `<select multiple size=10>
${allLanguages.map(lang => `<option value="${lang}"${code_languages.includes(lang) ? " selected" : ""}>${lang}</option>`).join("")}
</select>`
}

const codeCategoriesTemplate = ({code_categories = {}}) => {
    const {CATS} = __webpack_require__("./js/modules/schema/i18n.js")
    const allCategories = Object.keys(CATS)
    const defaultCategories = {
        listing: {counter: 0, enabled: true},
        example: {counter: 0, enabled: true},
        snippet: {counter: 0, enabled: false},
        tutorial: {counter: 0, enabled: false},
        exercise: {counter: 0, enabled: false},
        exercise_solution: {counter: 0, enabled: false}
    }
    const categories = Object.assign({}, defaultCategories, code_categories)

    return `<div class="code-categories-config">
        <p>${gettext("Select which code block categories are available:")}</p>
        ${allCategories
            .map(cat => {
                const isCodeCategory = [
                    "listing",
                    "example",
                    "snippet",
                    "tutorial",
                    "exercise",
                    "exercise_solution"
                ].includes(cat)
                if (!isCodeCategory) {
                    return ""
                }
                const enabled = categories[cat]?.enabled || false
                return `<label>
                    <input type="checkbox" class="code-category" value="${cat}" ${enabled ? "checked" : ""}/>
                    ${CATS[cat]["en-US"] || cat}
                </label>`
            })
            .join("")}
    </div>`
}

const templateEditorValueTemplate = ({content}) =>
    content
        .map(docPart => {
            switch (docPart.type) {
                case "heading_part":
                    return headingTemplate(docPart.attrs)
                case "contributors_part":
                    return contributorsTemplate(docPart.attrs)
                case "richtext_part":
                    return richtextTemplate(docPart.attrs)
                case "tags_part":
                    return tagsTemplate(docPart.attrs)
                case "table_part":
                    return tableTemplate(docPart.attrs)
                case "table_of_contents":
                    return tocTemplate(docPart.attrs)
                case "separator_part":
                    return separatorTemplate(docPart.attrs)
                default:
                    return ""
            }
        })
        .join("")

const documentStylesTemplate = ({documentStyles}) => `${documentStyles
    .map(
        style => `<button class="fw-green fw-small fw-button ui-button document-style" data-id="${style.pk}">
        ${(0,_common__rspack_import_0.escapeText)(style.fields.title)}
    </button>`
    )
    .join("")}
<button class="fw-green fw-small fw-button ui-button document-style" data-id="0">
    <i class="fas fa-plus-circle"></i>
    ${gettext("Add new document style")}
</button>`

const exportTemplatesTemplate = ({exportTemplates}) => `${exportTemplates
    .map(
        template => `<button class="fw-green fw-small fw-button ui-button export-template" data-id="${template.pk}">
        ${(0,_common__rspack_import_0.escapeText)(template.fields.title)}
    </button>`
    )
    .join("")}
<button class="fw-green fw-small fw-button ui-button export-template" data-id="0">
    <i class="fas fa-plus-circle"></i>
    ${gettext("Add new export template")}
</button>`

const documentDesignerTemplate = ({
    id,
    value,
    title,
    documentStyles,
    exportTemplates,
    citationStyles
}) =>
    `<table class="title-id"><tbody>
    <tr><td>${gettext("Title")}</td><td><input type="text" class="title vTextField fw-inline" value="${(0,_common__rspack_import_0.escapeText)(title)}"></td></tr>
    <tr><td>${gettext("ID")}</td><td><input type="text" class="import-id vTextField fw-inline" value="${(0,_common__rspack_import_0.escapeText)(value.attrs.import_id || "")}"></td></tr>
    </tbody></table>
    <table>
        <thead>
            <tr>
                <th>${gettext("Element types")}</th>
                <th>${gettext("Document structure")}</th>
                <th>${gettext("Delete")}</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="from-container">
                    ${headingTemplate({})}
                    ${contributorsTemplate({})}
                    ${richtextTemplate({})}
                    ${tagsTemplate({})}
                    ${tableTemplate({})}
                    ${tocTemplate({})}
                    ${separatorTemplate({})}
                </td>
                <td class="to-column">
                    <div class="doc-part-block fixed" data-type="initial">${gettext("Title")}</div>
                    <div class="to-container">${templateEditorValueTemplate({content: value.content || []})}</div>
                </td>
                <td class="trash">
                </td>
            </tr>
        </tbody>
    </table>
    <table class="fw-data-table">
        <thead>
            <tr>
                <th>${gettext("Setting")}</th>
                <th>${gettext("Value")}</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
                    ${gettext("Footnote specifications")}
                </td>
                <td class="footnote-value">
                    ${footnoteTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Available languages")}
                </td>
                <td class="languages-value">
                    ${languagesTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Default language")}
                </td>
                <td class="language-value">
                    ${languageTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Permitted paper sizes")}
                </td>
                <td class="papersizes-value">
                    ${papersizesTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Code block languages")}
                </td>
                <td class="code-languages-value">
                    ${codeLanguagesTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Code block categories")}
                </td>
                <td class="code-categories-value">
                    ${codeCategoriesTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Custom bibliography header")}
                </td>
                <td class="bibliography-header-value">
                    ${bibliographyHeaderTemplate(value.attrs || {})}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Available citation styles")}
                </td>
                <td class="citationstyles-value">
                    ${citationstylesTemplate(value.attrs || {}, citationStyles)}
                </td>
            </tr>
            <tr>
                <td>
                    ${gettext("Default citation style")}
                </td>
                <td class="citationstyle-value">
                    ${citationstyleTemplate(value.attrs || {}, citationStyles)}
                </td>
            </tr>
            ${
                id
                    ? `<tr>
                    <td>
                        ${gettext("Document styles")}
                    </td>
                    <td>
                        <div class="ui-dialog-buttonset document-styles">
                                ${documentStylesTemplate({documentStyles})}
                        </div>
                    </td>
                </tr>
                <tr>
                    <td>
                        ${gettext("Export templates")}
                    </td>
                    <td>
                        <div class="ui-dialog-buttonset export-templates">
                                ${exportTemplatesTemplate({exportTemplates})}
                        </div>
                    </td>
                </tr>`
                    : ""
            }
        </tbody>
    </table>`


}),
"./js/modules/document_template/tools.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  addHeadingIds: function() { return addHeadingIds; },
  debounced: function() { return debounced; },
  noTrack: function() { return noTrack; }
});
/* import */ var _schema_common__rspack_import_0 = __webpack_require__("./js/modules/schema/common/index.js");


// from https://codeburst.io/throttling-and-debouncing-in-javascript-646d076d0a44
function debounced(delay, fn) {
    let timerId
    return (...args) => {
        if (timerId) {
            clearTimeout(timerId)
        }
        timerId = setTimeout(() => {
            fn(...args)
            timerId = null
        }, delay)
    }
}

function noTrack(node) {
    if (node.attrs?.track) {
        delete node.attrs.track
        if (!Object.keys(node.attrs).length) {
            delete node.attrs
        }
    }
    if (node.content) {
        node.content.forEach(child => noTrack(child))
    }
    return node
}

function addHeadingIds(oldState, newState, editors) {
    const newHeadings = [],
        usedHeadingIds = []

    editors.forEach(([_el, view]) => {
        if (view.state === oldState) {
            return
        }
        view.state.doc.descendants(node => {
            if (node.type.groups.includes("heading")) {
                usedHeadingIds.push(node.attrs.id)
            }
        })
    })
    newState.doc.descendants((node, pos) => {
        if (node.type.groups.includes("heading")) {
            if (
                node.attrs.id === false ||
                usedHeadingIds.includes(node.attrs.id)
            ) {
                newHeadings.push({pos, node})
            } else {
                usedHeadingIds.push(node.attrs.id)
            }
        }
    })
    if (!newHeadings.length) {
        return null
    }
    const newTr = newState.tr
    newHeadings.forEach(newHeading => {
        let id
        while (!id || usedHeadingIds.includes(id)) {
            id = (0,_schema_common__rspack_import_0.randomHeadingId)()
        }
        usedHeadingIds.push(id)
        newTr.setNodeMarkup(
            newHeading.pos,
            null,
            Object.assign({}, newHeading.node.attrs, {id})
        )
    })
    return newTr
}


}),
"./js/modules/document_template/update.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  updateTemplateFile: function() { return updateTemplateFile; }
});
/* import */ var _schema_convert__rspack_import_0 = __webpack_require__("./js/modules/schema/convert.js");


function updateTemplateFile(
    title,
    content,
    exportTemplates,
    documentStyles,
    filetypeVersion
) {
    // This function can be modified and used in case updting the template file is different
    // from updating the content itself some time in the future.

    const oldDoc = {
        content,
        diffs: [],
        bibliography: {},
        comments: {},
        title,
        version: 1,
        id: 1
    }
    const doc = (0,_schema_convert__rspack_import_0.updateDoc)(oldDoc, filetypeVersion)
    return {title, content: doc.content, exportTemplates, documentStyles}
}


}),
"./js/modules/documents/access_rights/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  DocumentAccessRightsDialog: function() { return DocumentAccessRightsDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _contacts_add_dialog__rspack_import_1 = __webpack_require__("./js/modules/contacts/add_dialog.js");
/* import */ var _templates__rspack_import_2 = __webpack_require__("./js/modules/documents/access_rights/templates.js");




/**
 * Functions for the document access rights dialog.
 */

class DocumentAccessRightsDialog {
    constructor(
        documentIds,
        contacts,
        newContactCall,
        e2ee,
        documentPassword,
        onShareSuccess,
        settings
    ) {
        this.documentIds = documentIds
        this.contacts = contacts
        this.newContactCall = newContactCall // a function to be called when a new contact has been added with contact details
        // Share-link tab is only available when a single document is selected
        this.singleDocumentId = documentIds.length === 1 ? documentIds[0] : null
        // Whether the document(s) are E2EE encrypted. For E2EE documents,
        // only a subset of access rights are available.
        this.e2ee = e2ee
        // The document password, if known (e.g. when opened in the editor).
        // Used to pre-fill the share-link password field.
        this.documentPassword = documentPassword
        // Optional callback invoked after access rights are saved successfully.
        // Receives the array of new access rights.
        this.onShareSuccess = onShareSuccess
        this.settings = settings
    }

    init() {
        (0,_common__rspack_import_0.jsonPostJson)("/api/document/get_access_rights/", {
            document_ids: this.documentIds
        })
            .catch(error => {
                (0,_common__rspack_import_0.addAlert)("error", gettext("Cannot load document access data."))
                throw error
            })
            .then(({json}) => {
                this.accessRights = json.access_rights
                this.createAccessRightsDialog()
            })
    }

    getDropdownMenu(currentRight, onChange) {
        // E2EE documents only support a subset of access rights because
        // the server cannot process encrypted content for features like
        // tracked changes, review filtering, etc.
        const E2EE_ALLOWED_RIGHTS = ["write", "read-without-comments", "read"]

        const allItems = [
            {
                type: "header",
                title: gettext("Basic"),
                tooltip: gettext("Basic access rights")
            },
            {
                type: "action",
                title: gettext("Write"),
                icon: "pencil-alt",
                tooltip: gettext("Write"),
                right: "write",
                action: () => {
                    onChange("write")
                },
                selected: currentRight === "write"
            },
            {
                type: "action",
                title: gettext("Write tracked"),
                icon: "pencil-alt",
                tooltip: gettext("Write with changes tracked"),
                right: "write-tracked",
                action: () => {
                    onChange("write-tracked")
                },
                selected: currentRight === "write-tracked"
            },
            {
                type: "action",
                title: gettext("Comment"),
                icon: "comment",
                tooltip: gettext("Comment"),
                right: "comment",
                action: () => {
                    onChange("comment")
                },
                selected: currentRight === "comment"
            },
            {
                type: "action",
                title: gettext("Read"),
                icon: "eye",
                tooltip: gettext("Read"),
                right: "read",
                action: () => {
                    onChange("read")
                },
                selected: currentRight === "read"
            },
            {
                type: "header",
                title: gettext("Review"),
                tooltip: gettext("Access rights used within document review")
            },
            {
                type: "action",
                title: gettext("No comments"),
                icon: "eye",
                tooltip: gettext(
                    "Read document but not see comments and chats of others"
                ),
                right: "read-without-comments",
                action: () => {
                    onChange("read-without-comments")
                },
                selected: currentRight === "read-without-comments"
            },
            {
                type: "action",
                title: gettext("Review"),
                icon: "comment",
                tooltip: gettext(
                    "Comment, but not see comments and chats of others"
                ),
                right: "review",
                action: () => {
                    onChange("review")
                },
                selected: currentRight === "review"
            },
            {
                type: "action",
                title: gettext("Review tracked"),
                icon: "pencil-alt",
                tooltip: gettext(
                    "Write with tracked changes, but not see comments and chats of others"
                ),
                right: "review-tracked",
                action: () => {
                    onChange("review-tracked")
                },
                selected: currentRight === "review-tracked"
            }
        ]

        // Filter items for E2EE documents
        const content = this.e2ee
            ? allItems.filter(item => {
                  // Keep headers
                  if (item.type === "header") {
                      return true
                  }
                  // Only keep allowed rights
                  return E2EE_ALLOWED_RIGHTS.includes(item.right)
              })
            : allItems

        // Remove trailing headers with no items after them
        const filteredContent = content.filter((item, index) => {
            if (item.type === "header") {
                // Check if there's at least one action item after this header
                // before the next header or end of list
                const nextItems = content.slice(index + 1)
                const nextAction = nextItems.find(i => i.type === "action")
                const nextHeader = nextItems.findIndex(i => i.type === "header")
                if (!nextAction) {
                    return false
                }
                if (
                    nextHeader >= 0 &&
                    nextHeader < nextItems.indexOf(nextAction)
                ) {
                    return false
                }
            }
            return true
        })

        return {content: filteredContent}
    }

    createAccessRightsDialog() {
        const docCollabs = {}

        // We are potentially dealing with access rights of several documents, so
        // we first need to find out which users have access on all of the documents.
        // Those are the access rights we will display in the dialog.
        this.accessRights.forEach(ar => {
            if (!this.documentIds.includes(ar.document_id)) {
                return
            }
            const holderIdent = ar.holder.type + ar.holder.id
            if (docCollabs[holderIdent]) {
                if (docCollabs[holderIdent].rights != ar.rights) {
                    // We use read rights if the user has different rights on different docs.
                    docCollabs[holderIdent].rights = "read"
                }
                docCollabs[holderIdent].count += 1
            } else {
                docCollabs[holderIdent] = Object.assign({}, ar)
                docCollabs[holderIdent].count = 1
            }
        })

        const collaborators = Object.values(docCollabs).filter(
            col => col.count === this.documentIds.length
        )

        // E2EE warning banner body (shown only for E2EE documents)
        const e2eeWarningBanner = this.e2ee
            ? `<div class="e2ee-access-rights-warning">
                <strong><i class="fas fa-lock"></i> ${gettext("End-to-end encrypted document")}</strong>
                <p>${gettext("This document is end-to-end encrypted. Collaborators without a personal passphrase will need the document password shared with them through a secure channel outside of Fidus Writer. Do not send the password through the document chat.")}</p>
            </div>`
            : ""

        const buttons = [
            {
                text:
                    this.settings?.REGISTRATION_OPEN ||
                    this.settings?.SOCIALACCOUNT_OPEN
                        ? gettext("Add contact or invite new user")
                        : gettext("Add contact"),
                classes: "fw-light fw-add-button",
                click: () => {
                    const dialog = new _contacts_add_dialog__rspack_import_1.AddContactDialog(this.settings)
                    dialog.init().then(contactsData => {
                        contactsData.forEach(contactData => {
                            if (contactData.id) {
                                document
                                    .querySelector(
                                        "#my-contacts .fw-data-table-body"
                                    )
                                    .insertAdjacentHTML(
                                        "beforeend",
                                        (0,_templates__rspack_import_2.contactsTemplate)({
                                            contacts: [contactData]
                                        })
                                    )
                                document
                                    .querySelector("#share-contact table tbody")
                                    .insertAdjacentHTML(
                                        "beforeend",
                                        (0,_templates__rspack_import_2.collaboratorsTemplate)({
                                            collaborators: [
                                                {
                                                    holder: contactData,
                                                    rights: "read"
                                                }
                                            ]
                                        })
                                    )
                                this.newContactCall(contactData)
                            } else {
                                document
                                    .querySelector("#share-contact table tbody")
                                    .insertAdjacentHTML(
                                        "beforeend",
                                        (0,_templates__rspack_import_2.collaboratorsTemplate)({
                                            collaborators: [
                                                {
                                                    holder: contactData,
                                                    rights: "read"
                                                }
                                            ]
                                        })
                                    )
                            }
                        })
                    })
                }
            },
            {
                text: gettext("Submit"),
                classes: "fw-dark",
                click: () => {
                    //apply the current state to server
                    const accessRights = []
                    document
                        .querySelectorAll("#share-contact .collaborator-tr")
                        .forEach(el => {
                            accessRights.push({
                                holder: {
                                    id: Number.parseInt(el.dataset.id),
                                    type: el.dataset.type
                                },
                                rights: el.dataset.rights
                            })
                        })
                    this.submitAccessRight(accessRights)
                    this.dialog.close()
                }
            },
            {
                type: "cancel"
            }
        ]
        this.dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Share your document with others"),
            id: "access-rights-dialog",
            width: 820,
            height: 440,
            body:
                e2eeWarningBanner +
                (0,_templates__rspack_import_2.accessRightOverviewTemplate)({
                    contacts: this.contacts,
                    collaborators
                }),
            buttons
        })
        this.dialog.open()
        this.bindDialogEvents()
        // Hide the share-link tab when multiple documents are selected
        if (!this.singleDocumentId) {
            const shareTab = this.dialog.dialogEl.querySelector(
                ".ui-tabs-nav .tab-link:last-child"
            )
            if (shareTab) {
                shareTab.style.display = "none"
            }
        }
    }

    loadShareTokens() {
        const listEl = this.dialog.dialogEl.querySelector("#share-token-list")
        listEl.innerHTML = `<p class="fw-ar-loading">${gettext("Loading…")}</p>`
        ;(0,_common__rspack_import_0.jsonPostJson)("/api/document/share_token/list/", {
            document_id: this.singleDocumentId
        })
            .then(({json}) => {
                listEl.innerHTML = (0,_templates__rspack_import_2.shareTokenListTemplate)({tokens: json.tokens})
            })
            .catch(() => {
                listEl.innerHTML = `<p class="fw-ar-error">${gettext("Could not load share links.")}</p>`
            })
    }

    openCreateShareTokenDialog() {
        const createDialog = new _common__rspack_import_0.Dialog({
            title: gettext("Create share link"),
            id: "create-share-token-dialog",
            width: 860,
            body: (0,_templates__rspack_import_2.createShareTokenDialogTemplate)(
                this.e2ee,
                this.documentPassword
            ),
            buttons: [
                {
                    text: gettext("Create"),
                    classes: "fw-dark",
                    click: () => {
                        const rights = createDialog.dialogEl.querySelector(
                            "#share-token-rights"
                        ).value
                        const expiresRaw = createDialog.dialogEl.querySelector(
                            "#share-token-expires"
                        ).value
                        const note = createDialog.dialogEl
                            .querySelector("#share-token-note")
                            .value.trim()
                        ;(0,_common__rspack_import_0.jsonPostJson)("/api/document/share_token/create/", {
                            document_id: this.singleDocumentId,
                            rights,
                            expires_at: expiresRaw || "",
                            note
                        })
                            .then(({json}) => {
                                // For E2EE documents, optionally append the password to the URL fragment
                                let shareUrl = json.share_url
                                if (this.e2ee) {
                                    const passwordInput =
                                        createDialog.dialogEl.querySelector(
                                            "#share-token-password"
                                        )
                                    const password = passwordInput
                                        ? passwordInput.value.trim()
                                        : ""
                                    if (password) {
                                        shareUrl = `${shareUrl}#?password=${encodeURIComponent(password)}`
                                    }
                                }
                                json.share_url = shareUrl
                                const listEl =
                                    this.dialog.dialogEl.querySelector(
                                        "#share-token-list"
                                    )
                                // Remove the "no tokens" placeholder if present
                                const placeholder =
                                    listEl.querySelector(".fw-ar-no-tokens")
                                if (placeholder) {
                                    placeholder.remove()
                                }
                                listEl.insertAdjacentHTML(
                                    "beforeend",
                                    (0,_templates__rspack_import_2.shareTokenRowTemplate)({token: json})
                                )
                                ;(0,_common__rspack_import_0.addAlert)(
                                    "success",
                                    gettext("Share link created.")
                                )
                            })
                            .catch(() =>
                                (0,_common__rspack_import_0.addAlert)(
                                    "error",
                                    gettext("Could not create share link.")
                                )
                            )
                        createDialog.close()
                    }
                },
                {type: "cancel"}
            ]
        })
        createDialog.open()
        const expiresInput = createDialog.dialogEl.querySelector(
            "#share-token-expires"
        )
        ;(0,_common__rspack_import_0.enableDatePicker)(expiresInput, true)
    }

    revokeShareToken(tokenId, rowEl) {
        (0,_common__rspack_import_0.jsonPostJson)("/api/document/share_token/revoke/", {token_id: tokenId})
            .then(({json}) => {
                if (json.success) {
                    rowEl.remove()
                    const listEl =
                        this.dialog.dialogEl.querySelector("#share-token-list")
                    if (!listEl.querySelector(".share-token-row")) {
                        listEl.innerHTML = (0,_templates__rspack_import_2.shareTokenListTemplate)({tokens: []})
                    }
                    (0,_common__rspack_import_0.addAlert)("success", gettext("Share link revoked."))
                } else {
                    (0,_common__rspack_import_0.addAlert)("error", gettext("Could not revoke share link."))
                }
            })
            .catch(() =>
                (0,_common__rspack_import_0.addAlert)("error", gettext("Could not revoke share link."))
            )
    }

    // ------------------------------------------------------------------ //
    //  Event binding
    // ------------------------------------------------------------------ //

    bindDialogEvents() {
        this.dialog.dialogEl
            .querySelector("#add-share-contact")
            .addEventListener("click", () => {
                const selectedData = []
                document
                    .querySelectorAll("#my-contacts .fw-checkable.checked")
                    .forEach(el => {
                        const collaboratorEl = document.getElementById(
                            `collaborator-${el.dataset.type}-${el.dataset.id}`
                        )
                        if (collaboratorEl) {
                            if (collaboratorEl.dataset.rights === "delete") {
                                collaboratorEl.dataset.rights = "read"
                                const accessRightIcon =
                                    collaboratorEl.querySelector(
                                        ".icon-access-right"
                                    )
                                accessRightIcon.classList.remove(
                                    "icon-access-delete"
                                )
                                accessRightIcon.classList.add(
                                    "icon-access-read"
                                )
                            }
                        } else {
                            const collaborator = this.contacts.find(
                                contact =>
                                    contact.type === el.dataset.type &&
                                    contact.id ===
                                        Number.parseInt(el.dataset.id)
                            )
                            if (!collaborator) {
                                console.warn(
                                    `No contact found of type: ${el.dataset.type} id: ${el.dataset.id}.`
                                )
                                return
                            }
                            selectedData.push({
                                holder: {
                                    id: collaborator.id,
                                    type: collaborator.type,
                                    name: collaborator.name,
                                    avatar: collaborator.avatar
                                },
                                rights: "read"
                            })
                        }
                    })

                document
                    .querySelectorAll("#my-contacts .checkable-label.checked")
                    .forEach(el => el.classList.remove("checked"))
                document
                    .querySelector("#share-contact table tbody")
                    .insertAdjacentHTML(
                        "beforeend",
                        (0,_templates__rspack_import_2.collaboratorsTemplate)({
                            collaborators: selectedData
                        })
                    )
            })
        // Tab switching
        this.dialog.dialogEl
            .querySelectorAll(".ui-tabs-nav .tab-link a")
            .forEach(linkEl => {
                linkEl.addEventListener("click", event => {
                    event.preventDefault()
                    const href = linkEl.getAttribute("href")
                    const tabName = href.substring(1)

                    this.dialog.dialogEl
                        .querySelectorAll(".tab-link")
                        .forEach(tabLinkEl =>
                            tabLinkEl.classList.remove("current-tab")
                        )
                    linkEl.parentNode.classList.add("current-tab")

                    this.dialog.dialogEl
                        .querySelectorAll(".tab-content")
                        .forEach(contentEl => {
                            if (contentEl.matches(href)) {
                                contentEl.style.display = ""
                            } else {
                                contentEl.style.display = "none"
                            }
                        })

                    if (tabName === "sharelink" && this.singleDocumentId) {
                        this.loadShareTokens()
                    }
                })
            })

        // Share-link tab: create / copy / revoke
        this.dialog.dialogEl.addEventListener("click", event => {
            const el = {}
            if ((0,_common__rspack_import_0.findTarget)(event, "#create-share-token-btn", el)) {
                this.openCreateShareTokenDialog()
                return
            }
            if ((0,_common__rspack_import_0.findTarget)(event, ".copy-share-token-btn", el)) {
                const url = el.target.closest(".copy-share-token-btn").dataset
                    .url
                navigator.clipboard.writeText(url).then(
                    () =>
                        (0,_common__rspack_import_0.addAlert)(
                            "success",
                            gettext("Link copied to clipboard.")
                        ),
                    () => (0,_common__rspack_import_0.addAlert)("error", gettext("Could not copy link."))
                )
                return
            }
            if ((0,_common__rspack_import_0.findTarget)(event, ".revoke-share-token-btn", el)) {
                const btn = el.target.closest(".revoke-share-token-btn")
                const tokenId = Number.parseInt(btn.dataset.tokenId)
                const rowEl = btn.closest(".share-token-row")
                this.revokeShareToken(tokenId, rowEl)
                return
            }
        })

        this.dialog.dialogEl.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, ".fw-checkable", el):
                    ;(0,_common__rspack_import_0.setCheckableLabel)(el.target)
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".delete-collaborator", el): {
                    const colRow = el.target.closest(".collaborator-tr")
                    colRow.dataset.rights = "delete"
                    colRow
                        .querySelector(".icon-access-right")
                        .setAttribute(
                            "class",
                            "icon-access-right icon-access-delete"
                        )
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, ".edit-right", el): {
                    const colRow = el.target.closest(".collaborator-tr")
                    const currentRight = colRow.dataset.rights
                    const menu = this.getDropdownMenu(
                        currentRight,
                        newRight => {
                            colRow.dataset.rights = newRight
                            colRow
                                .querySelector(".icon-access-right")
                                .setAttribute(
                                    "class",
                                    `icon-access-right icon-access-${newRight}`
                                )
                        }
                    )
                    const contentMenu = new _common__rspack_import_0.ContentMenu({
                        menu,
                        menuPos: {X: event.pageX, Y: event.pageY},
                        width: 200
                    })
                    contentMenu.open()
                    break
                }
                default:
                    break
            }
        })
    }

    submitAccessRight(newAccessRights) {
        (0,_common__rspack_import_0.jsonPost)("/api/document/save_access_rights/", {
            document_ids: this.documentIds,
            access_rights: newAccessRights
        })
            .then(() => {
                (0,_common__rspack_import_0.addAlert)("success", gettext("Access rights have been saved"))
                if (this.onShareSuccess) {
                    this.onShareSuccess(newAccessRights)
                }
            })
            .catch(() =>
                (0,_common__rspack_import_0.addAlert)("error", gettext("Access rights could not be saved"))
            )
    }
}


}),
"./js/modules/documents/access_rights/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  accessRightOverviewTemplate: function() { return accessRightOverviewTemplate; },
  collaboratorsTemplate: function() { return collaboratorsTemplate; },
  contactsTemplate: function() { return contactsTemplate; },
  createShareTokenDialogTemplate: function() { return createShareTokenDialogTemplate; },
  shareTokenListTemplate: function() { return shareTokenListTemplate; },
  shareTokenRowTemplate: function() { return shareTokenRowTemplate; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");


/** Outer wrapper with two tabs: "People" and "Share link" */
const accessRightOverviewTemplate = ({contacts, collaborators}) =>
    `<ul class="ui-tabs-nav">
        <li class="tab-link current-tab"><a href="#people" class="tab-link-inner">${gettext("People")}</a></li>
        <li class="tab-link"><a href="#sharelink" class="tab-link-inner">${gettext("Share link")}</a></li>
    </ul>
    <div id="people" class="tab-content ui-tabs-panel">
        <div id="my-contacts" class="fw-ar-container">
            <h3 class="fw-green-title">${gettext("My contacts")}</h3>
            <table class="fw-data-table">
                <thead class="fw-data-table-header"><tr><th width="337">${gettext("Contacts")}</th></tr></thead>
                <tbody class="fw-data-table-body fw-small">
                    ${contactsTemplate({contacts})}
                </tbody>
            </table>
        </div>
        <span id="add-share-contact" class="fw-button fw-large fw-square fw-light fw-ar-button">
            <i class="fa fa-caret-right"></i>
        </span>
        <div id="share-contact" class="fw-ar-container">
            <h3 class="fw-green-title">${gettext("My collaborators")}</h3>
            <table class="fw-data-table tablesorter">
                <thead class="fw-data-table-header"><tr>
                        <th width="217">${gettext("Collaborators")}</th>
                        <th width="50" align="center">${gettext("Rights")}</th>
                        <th width="50" align="center">${gettext("Delete")}</th>
                </tr></thead>
                <tbody class="fw-data-table-body fw-small">
                    ${collaboratorsTemplate({collaborators})}
                </tbody>
            </table>
        </div>
    </div>
    <div id="sharelink" class="tab-content ui-tabs-panel" style="display: none;">
        <div id="share-token-list">
            <p class="fw-ar-loading">${gettext("Loading…")}</p>
        </div>
        <button class="fw-button fw-light" id="create-share-token-btn">
            <i class="fa fa-plus"></i>&nbsp;${gettext("Create new share link")}
        </button>
    </div>`

/** The list of active share-link tokens for a single document */
const shareTokenListTemplate = ({tokens}) => {
    if (!tokens.length) {
        return `<p class="fw-ar-no-tokens">${gettext("No active share links yet.")}</p>`
    }
    return tokens.map(token => shareTokenRowTemplate({token})).join("")
}

/** A single share-link token row */
const shareTokenRowTemplate = ({token}) => {
    const expiry = token.expires_at
        ? (0,_common__rspack_import_0.escapeText)(token.expires_at.slice(0, 10))
        : gettext("never")
    const note = token.note ? (0,_common__rspack_import_0.escapeText)(token.note) : ""
    return `<div class="share-token-row" data-token-id="${token.id}">
        <div class="share-token-meta">
            <span class="share-token-rights fw-bold">${(0,_common__rspack_import_0.escapeText)(token.rights)}</span>
            <span class="share-token-expiry">&nbsp;·&nbsp;${gettext("expires")}: ${expiry}</span>
            ${note ? `<span class="share-token-note">&nbsp;·&nbsp;${note}</span>` : ""}
        </div>
        <div class="share-token-url-row">
            <input class="share-token-url-input" type="text" readonly value="${(0,_common__rspack_import_0.escapeText)(token.share_url)}" />
            <button class="fw-button fw-light copy-share-token-btn" data-url="${(0,_common__rspack_import_0.escapeText)(token.share_url)}" title="${gettext("Copy link")}">
                <i class="fa fa-copy"></i>
            </button>
            <button class="fw-button fw-light revoke-share-token-btn" data-token-id="${token.id}" title="${gettext("Revoke")}">
                <i class="fas fa-trash-alt"></i>
            </button>
        </div>
    </div>`
}

/** Sub-dialog for creating a new share link */
const createShareTokenDialogTemplate = (
    e2ee = false,
    documentPassword = ""
) =>
    `<table class="fw-dialog-table">
        <tbody>
            <tr>
                <th><label for="share-token-rights">${gettext("Rights")}</label></th>
                <td class="entry-field">
                    <select id="share-token-rights" class="fw-button fw-light fw-large">
                        ${
                            e2ee
                                ? `<option value="read">${gettext("Read")}</option>
                               <option value="read-without-comments">${gettext("Read without comments")}</option>
                               <option value="write">${gettext("Write")}</option>`
                                : `<option value="read">${gettext("Read")}</option>
                               <option value="read-without-comments">${gettext("Read without comments")}</option>
                               <option value="review">${gettext("Review")}</option>
                               <option value="review-tracked">${gettext("Review tracked")}</option>
                               <option value="comment">${gettext("Comment")}</option>
                               <option value="write">${gettext("Write")}</option>
                               <option value="write-tracked">${gettext("Write tracked")}</option>`
                        }
                    </select>
                    <div class="fw-select-arrow fa fa-caret-down"></div>
                </td>
            </tr>
            <tr>
                <th><label for="share-token-expires">${gettext("Expires")}</label></th>
                <td class="entry-field">
                    <input id="share-token-expires" type="text" class="fw-light" placeholder="${gettext("YYYY-MM-DD or empty")}" pattern="\d{4}-\d{2}-\d{2}" />
                </td>
            </tr>
            <tr>
                <th><label for="share-token-note">${gettext("Note")}</label></th>
                <td class="entry-field">
                    <input id="share-token-note" type="text" class="fw-light" placeholder="${gettext('optional label, e.g. "for reviewers"')}"/>
                </td>
            </tr>
            ${
                e2ee
                    ? `<tr>
                    <th><label for="share-token-password">${gettext("Document password")}</label></th>
                    <td class="entry-field">
                        <input id="share-token-password" type="text" class="fw-light" value="${(0,_common__rspack_import_0.escapeText)(documentPassword)}" placeholder="${gettext("Enter the document password to include it in the link")}" autocomplete="off" />
                        <p class="e2ee-share-warning">
                            <i class="fas fa-exclamation-triangle"></i>
                            ${gettext("If you enter the password above, it will be embedded in the link (after #). Anyone with the link can open the document. Only share such links through secure channels.")}
                        </p>
                    </td>
                </tr>`
                    : ""
            }
        </tbody>
    </table>`

/** The template for an individual row in the left hand side list of users (all contacts) of the access rights dialogue. */
const contactsTemplate = ({contacts}) =>
    contacts
        .map(
            contact =>
                `<tr>
            <td width="337" data-id="${contact.id}" data-type="${contact.type}" class="fw-checkable fw-checkable-td">
                <span>${(0,_common__rspack_import_0.avatarTemplate)({user: contact})}</span>
                <span class="fw-inline">
                ${
                    contact.type === "userinvite"
                        ? `${gettext("Invite")}:&nbsp;`
                        : ""
                }
                    ${(0,_common__rspack_import_0.escapeText)(contact.name)}

                </span>
            </td>
        </tr>`
        )
        .join("")

/** The template for the right hand side list of users (the collaborators of the current document) of the access rights dialogue. */
const collaboratorsTemplate = ({collaborators}) =>
    collaborators
        .map(
            collaborator =>
                `<tr id="collaborator-${collaborator.holder.type}-${collaborator.holder.id}"
    data-type="${collaborator.holder.type}" data-id="${collaborator.holder.id}"
    class="collaborator-tr" data-rights="${collaborator.rights}">
        <td width="215">
            <span>${(0,_common__rspack_import_0.avatarTemplate)({user: collaborator.holder})}</span>
            <span class="fw-inline">${
                collaborator.holder.type === "userinvite"
                    ? `${gettext("Invite")}: `
                    : ""
            }${(0,_common__rspack_import_0.escapeText)(collaborator.holder.name)}</span>
        </td>
        <td width="50" align="center">
            <div class="fw-inline edit-right-wrapper">
                <i class="icon-access-right icon-access-${collaborator.rights}"></i>
                <i class="fa fa-caret-down edit-right"></i>
            </div>
        </td>
        <td width="50" align="center">
            <span class="delete-collaborator fw-inline" data-rights="delete">
                <i class="fas fa-trash-alt fw-link-text"></i>
            </span>
        </td>
    </tr>`
        )
        .join("")


}),
"./js/modules/editor/citations/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCitations: function() { return ModCitations; }
});
/* import */ var _citations_render__rspack_import_0 = __webpack_require__("./js/modules/citations/render.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_i18n__rspack_import_2 = __webpack_require__("./js/modules/schema/i18n.js");




class ModCitations {
    constructor(editor) {
        editor.mod.citations = this
        this.editor = editor
        this.citationType = ""
        this.fnOverrideElement = false
    }

    init() {
        /* Add a style to hold dynamic CSS info about footnote numbering overrides.
         * Because we need footnotes in the editor and footnotes added through
         * citations to be numbered but they aren't in the same order in the DOM,
         * we need to organize the numbering manually.
         */
        document.body.insertAdjacentHTML(
            "beforeend",
            '<style type="text/css" id="footnote-numbering-override"></style>'
        )
        this.fnOverrideElement = document.getElementById(
            "footnote-numbering-override"
        )
    }

    resetCitations() {
        const citations = document.querySelectorAll(
            "#paper-editable span.citation"
        )
        citations.forEach(citation => (citation.innerHTML = ""))
        const docBibliography = document.querySelector(".doc-bibliography")
        const citationsContainer = document.getElementById(
            "citation-footnote-box-container"
        )
        if (!docBibliography || !citationsContainer) {
            return
        }
        if (docBibliography.innerHTML !== "") {
            docBibliography.innerHTML = ""
        }
        if (citationsContainer.innerHTML !== "") {
            citationsContainer.innerHTML = ""
        }
        this.layoutCitations()
    }

    layoutCitations() {
        if (!this.editor.mod.db?.bibDB.db) {
            // bibliography hasn't been loaded yet
            return
        }
        const emptyCitations = document.querySelectorAll(
            "#paper-editable span.citation:empty"
        )
        if (emptyCitations.length) {
            const settings = this.editor.view.state.doc.attrs,
                bibliographyHeader =
                    settings.bibliography_header[settings.language] ||
                    _schema_i18n__rspack_import_2.BIBLIOGRAPHY_HEADERS[settings.language]
            this.citRenderer = new _citations_render__rspack_import_0.RenderCitations(
                document.getElementById("paper-editable"),
                settings.citationstyle,
                bibliographyHeader,
                this.editor.mod.db.bibDB,
                this.editor.app.csl,
                false,
                settings.language
            )
            this.citRenderer
                .init()
                .then(() => this.layoutCitationsTwo())
                .catch(() => this.recoverAfterCiteProcCrash())
        }
    }

    recoverAfterCiteProcCrash() {
        console.warn(
            "CitationProcessor crashed. Falling back to simplified citation rendering."
        )

        // Create simplified citation texts for all citations
        const citationNodes = document.querySelectorAll(
            "#paper-editable span.citation"
        )
        citationNodes.forEach(citation => {
            try {
                // Get citation data from dataset
                const referencesData = JSON.parse(
                    citation.dataset.references || "[]"
                )
                if (!referencesData.length) {
                    citation.innerHTML = "[?]"
                    return
                }

                // Create a basic fallback citation text
                const citationText = referencesData
                    .map(ref => {
                        const entryId = ref.id
                        const item = this.editor.mod.db.bibDB.db[entryId]

                        if (!item) {
                            return `[${entryId || "?"}]`
                        }

                        // Extract basic author info
                        let authorText = ""
                        if (item.fields.author && item.fields.author.length) {
                            const author = item.fields.author[0]
                            authorText =
                                (
                                    author.lastName ||
                                    author.firstname ||
                                    author.literal
                                )
                                    ?.map(part => part.text || "")
                                    .join("") || "Unknown Author"
                            if (item.fields.author.length > 1) {
                                authorText += " et al."
                            }
                        } else {
                            authorText = "Unknown Author"
                        }

                        // Extract year
                        const year = item.fields.date
                            ? item.fields.date.substring(0, 4)
                            : "n.d."

                        // Add locator if present
                        const locator = ref.locator ? `, ${ref.locator}` : ""

                        return `(${authorText}, ${year}${locator})`
                    })
                    .join("; ")

                citation.innerHTML = citationText
            } catch (error) {
                console.error("Error creating fallback citation:", error)
                citation.innerHTML = "[Citation]"
            }
        })

        // Create a simplified bibliography
        const docBibliography = document.querySelector(".doc-bibliography")
        if (docBibliography) {
            try {
                const settings = this.editor.view.state.doc.attrs
                const bibliographyHeader =
                    settings.bibliography_header[settings.language] ||
                    _schema_i18n__rspack_import_2.BIBLIOGRAPHY_HEADERS[settings.language]

                let bibHTML = `<h1 class="doc-bibliography-header">${bibliographyHeader}</h1><div class="csl-bib-body">`

                // Collect all citation references
                const allCitations = Array.from(
                    document.querySelectorAll("#paper-editable span.citation")
                )
                const allRefs = new Set()

                allCitations.forEach(citation => {
                    try {
                        const refs = JSON.parse(
                            citation.dataset.references || "[]"
                        )
                        refs.forEach(ref => allRefs.add(ref.id))
                    } catch (_error) {
                        // Skip invalid references
                    }
                })

                // Create simple bibliography entries
                const bibDB = this.editor.mod.db.bibDB.db
                Array.from(allRefs)
                    .sort()
                    .forEach(id => {
                        const item = bibDB[id]
                        if (!item) {
                            bibHTML += `<div class="csl-entry" data-reference="${id}">[Missing reference: ${id}]</div>`
                            return
                        }

                        let authors = ""
                        if (item.fields.author && item.fields.author.length) {
                            authors = item.fields.author
                                .map(author => {
                                    return (
                                        (
                                            author.lastName ||
                                            author.firstname ||
                                            author.literal
                                        )
                                            ?.map(part => part.text || "")
                                            .join("") || "Unknown"
                                    )
                                })
                                .join(", ")
                        } else {
                            authors = "Unknown Author"
                        }

                        const year = item.fields.date
                            ? `(${item.fields.date.substring(0, 4)})`
                            : "(n.d.)"
                        const title =
                            item.fields.title
                                ?.map(part => part.text || "")
                                .join("") || "Untitled"
                        const publisher =
                            item.fields.publisher
                                ?.map(part => part.text || "")
                                .join("") || ""
                        const itemType = item.bib_type || "misc"

                        bibHTML += `<div class="csl-entry" data-reference="${id}">${authors} ${year}. <i>${title}</i>. ${publisher}. [${itemType}]</div>`
                    })

                bibHTML += "</div>"
                docBibliography.innerHTML = bibHTML
                // Add basic bibliography styling
                let styleEl = document.querySelector(".doc-bibliography-style")
                if (!styleEl) {
                    document.body.insertAdjacentHTML(
                        "beforeend",
                        '<style type="text/css" class="doc-bibliography-style"></style>'
                    )
                    styleEl = document.querySelector(".doc-bibliography-style")
                }

                const basicCSS = `
                    .csl-bib-body { line-height: 1.35; }
                    .csl-entry { padding-bottom: 1em; padding-left: 2em; text-indent: -2em; }
                    div.csl-entry { cursor: pointer; }
                    div.csl-entry:hover { background-color: #f0f0f0; }
                `
                styleEl.innerHTML = basicCSS

                // Rebind click handlers for bibliography entries
                if (this.editor.docInfo.access_rights === "write") {
                    this.bindBibliographyClicks()
                }
            } catch (error) {
                console.error("Error creating fallback bibliography:", error)
                docBibliography.innerHTML =
                    "<h1>Bibliography (Error rendering)</h1>"
            }
        }

        // Reset citation type to avoid footnote layout issues
        this.citationType = "in-text"

        return Promise.resolve()
    }

    bindBibliographyClicks() {
        document.querySelectorAll("div.csl-entry").forEach((el, index) => {
            el.addEventListener("click", () => {
                const eID = Number.parseInt(
                    this.citRenderer.fm.bibliography[0]?.entry_ids[index][0] ||
                        el.dataset.reference
                )
                this.checkTrackingDialog()
                    .then(() => Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_tokenfield_1_5_2_node_modules_tokenfield_lib_tokenfield_js"), __webpack_require__.e("js_modules_bibliography_form_index_js"), __webpack_require__.e("js_modules_bibliography_form_strings_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/bibliography/form/index.js")))
                    .then(({BibEntryForm}) => {
                        const form = new BibEntryForm(
                            this.editor.mod.db.bibDB,
                            false,
                            eID
                        )
                        form.init()
                    })
            })
        })
    }

    checkTrackingDialog() {
        if (!this.editor.view.state.doc.attrs.tracked) {
            return Promise.resolve()
        }
        const buttons = [],
            promise = new Promise(resolve => {
                buttons.push({
                    type: "cancel",
                    click: () => {
                        dialog.close()
                        resolve((0,_common__rspack_import_1.cancelPromise)())
                    }
                })
                buttons.push({
                    type: "ok",
                    click: () => {
                        dialog.close()
                        resolve()
                    }
                })
            })

        const dialog = new _common__rspack_import_1.Dialog({
            title: gettext("No tracking"),
            body: gettext("Changes to citation sources are not being tracked!"),
            icon: "exclamation-triangle",
            width: 400,
            height: 100,
            buttons
        })
        dialog.open()
        return promise
    }

    layoutCitationsTwo() {
        const citRenderer = this.citRenderer
        let needFootnoteLayout = false
        if (this.citationType !== citRenderer.fm.citationType) {
            // The citation format has changed, so we need to relayout the footnotes as well
            needFootnoteLayout = true
        }
        this.citationType = citRenderer.fm.citationType
        // Add the rendered html and css of the bibliography to the DOM.
        const docBibliography = document.querySelector(".doc-bibliography")
        if (!docBibliography) {
            return
        }
        docBibliography.innerHTML = citRenderer.fm.bibHTML
        let styleEl = document.querySelector(".doc-bibliography-style")
        if (!styleEl) {
            document.body.insertAdjacentHTML(
                "beforeend",
                '<style type="text/css" class="doc-bibliography-style"></style>'
            )
            styleEl = document.querySelector(".doc-bibliography-style")
        }
        let css = citRenderer.fm.bibCSS
        if (this.editor.docInfo.access_rights === "write") {
            this.bindBibliographyClicks()
            css += `
                div.csl-entry {
                    cursor: pointer;
                }
                div.csl-entry:hover {
                    background-color: grey;
                }`
        }
        if (styleEl.innerHTML !== css) {
            styleEl.innerHTML = css
        }

        const citationsContainer = document.getElementById(
            "citation-footnote-box-container"
        )
        if (this.citationType === "note") {
            // Check if there is an empty citation in the main body text (not footnotes)
            const emptyCitations = document.querySelector("span.citation:empty")

            if (emptyCitations) {
                // Find all the citations in the main body text (not footnotes)
                const citationNodes = document.querySelectorAll(
                        "#document-editable span.citation"
                    ),
                    citationsHTML = citRenderer.fm.citationTexts
                        .slice(0, citationNodes.length)
                        .map(
                            citText =>
                                `<div class="footnote-citation">${citText}</div>`
                        )
                        .join("")
                if (citationsContainer.innerHTML !== citationsHTML) {
                    citationsContainer.innerHTML = citationsHTML
                }
                // The citations have not been filled, so we do so manually.
                citationNodes.forEach(
                    citationNode =>
                        (citationNode.innerHTML =
                            '<span class="citation-footnote-marker"></span>')
                )

                const footnoteCitationNodes = document.querySelectorAll(
                    "#footnote-box-container span.citation"
                )
                const footnoteCitTexts = citRenderer.fm.citationTexts.slice(
                    citationNodes.length
                )

                footnoteCitTexts.forEach((citText, index) => {
                    const citationNode = footnoteCitationNodes[index]
                    if (citationNode) {
                        citationNode.innerHTML = citText
                    }
                })
            }
        } else {
            if (citationsContainer.innerHTML !== "") {
                citationsContainer.innerHTML = ""
            }
        }

        this.footnoteNumberOverride()
        if (needFootnoteLayout) {
            this.editor.mod.footnotes.layout.updateDOM()
        }
    }

    footnoteNumberOverride() {
        /* Find the order of footnotes and citations in the document and
         * write CSS to number all the citation footnotes and other footnotes
         * correspondingly. Update footnote-numbering-override correspondingly.
         */

        let outputCSS = ""

        if (this.citationType === "note") {
            let editorFootnoteCounter = 1,
                citationFootnoteCounter = 1,
                footnoteCounter = 1

            this.editor.view.state.doc.descendants(node => {
                if (
                    node.isInline &&
                    (node.type.name === "footnote" ||
                        node.type.name === "citation")
                ) {
                    if (node.type.name === "footnote") {
                        outputCSS += `#footnote-box-container .footnote-container:nth-of-type(${editorFootnoteCounter}) > *:first-child::before {
                             content: "${footnoteCounter} ";
                         }\n`
                        editorFootnoteCounter++
                    } else {
                        outputCSS += `.footnote-citation:nth-of-type(${citationFootnoteCounter})::before {
                             content: "${footnoteCounter} ";
                         }\n`
                        citationFootnoteCounter++
                    }
                    footnoteCounter++
                }
            })
        }

        if (this.fnOverrideElement.innerHTML !== outputCSS) {
            this.fnOverrideElement.innerHTML = outputCSS
        }
    }
}


}),
"./js/modules/editor/clipboard/copy/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  docClipboardSerializer: function() { return /* reexport safe */ _serializers__rspack_import_0.docClipboardSerializer; },
  fnClipboardSerializer: function() { return /* reexport safe */ _serializers__rspack_import_0.fnClipboardSerializer; }
});
/* import */ var _serializers__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/copy/serializers.js");



}),
"./js/modules/editor/clipboard/copy/schema.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  createDocCopySchema: function() { return createDocCopySchema; },
  fnCopySchema: function() { return fnCopySchema; }
});
/* import */ var prosemirror_model__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var _schema_common__rspack_import_0 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _schema_document_content__rspack_import_1 = __webpack_require__("./js/modules/schema/document/content.js");
/* import */ var _schema_footnotes__rspack_import_2 = __webpack_require__("./js/modules/schema/footnotes.js");
// A slight modification of the document schema for the purpose of copying.






const copyCitation = Object.assign({}, _schema_common__rspack_import_0.citation)

copyCitation.toDOM = node => {
    const bibDB = node.type.schema.cached.bibDB,
        bibs = {}
    node.attrs.references.forEach(ref => (bibs[ref.id] = bibDB.db[ref.id]))
    return [
        "span",
        {
            class: "citation",
            "data-format": node.attrs.format,
            "data-references": JSON.stringify(node.attrs.references),
            "data-bibs": JSON.stringify(bibs)
        }
    ]
}

/*
Citations inside of footnotes copied from the main editor also need to have bibliography
information attached to them.
*/

const fnCopySchema = new prosemirror_model__rspack_import_3.Schema({
    marks: _schema_footnotes__rspack_import_2.fnSchema.spec.marks,
    nodes: _schema_footnotes__rspack_import_2.fnSchema.spec.nodes.update("citation", copyCitation)
})

const copyFootnote = Object.assign({}, _schema_document_content__rspack_import_1.footnote)

copyFootnote.toDOM = node => {
    if (!fnCopySchema.cached.bibDB) {
        fnCopySchema.cached.bibDB = _schema_footnotes__rspack_import_2.fnSchema.cached.bibDB
    }
    const fnCopySerializer = prosemirror_model__rspack_import_3.DOMSerializer.fromSchema(fnCopySchema)
    const dom = document.createElement("span")
    dom.classList.add("footnote-marker")
    const pmNode = prosemirror_model__rspack_import_3.Node.fromJSON(fnCopySchema, {
        type: "footnotecontainer",
        content: node.attrs.footnote
    })
    dom.dataset.footnote = fnCopySerializer.serializeNode(pmNode).innerHTML
    dom.innerHTML = "&nbsp;"
    return dom
}

const createDocCopySchema = docSchema => {
    const newSchema = new prosemirror_model__rspack_import_3.Schema({
        marks: docSchema.spec.marks,
        nodes: docSchema.spec.nodes
            .update("citation", copyCitation)
            .update("footnote", copyFootnote)
    })
    newSchema.cached.bibDB = docSchema.cached.bibDB
    return newSchema
}


}),
"./js/modules/editor/clipboard/copy/serializers.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  docClipboardSerializer: function() { return docClipboardSerializer; },
  fnClipboardSerializer: function() { return fnClipboardSerializer; }
});
/* import */ var prosemirror_model__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var _citations_render__rspack_import_0 = __webpack_require__("./js/modules/citations/render.js");
/* import */ var _schema_i18n__rspack_import_1 = __webpack_require__("./js/modules/schema/i18n.js");
/* import */ var _schema__rspack_import_2 = __webpack_require__("./js/modules/editor/clipboard/copy/schema.js");





// Wrap around DOMSerializer, allowing post processing.
class ClipboardDOMSerializer {
    constructor(nodes, marks, editor) {
        this.domSerializer = new prosemirror_model__rspack_import_3.DOMSerializer(nodes, marks)
        this.editor = editor
    }

    serializeFragment(fragment, options) {
        const domFragment = this.domSerializer.serializeFragment(
            fragment,
            options
        )
        return this.postProcessFragment(domFragment)
    }

    postProcessFragment(domFragment) {
        const citationFormatter = this.renderCitations(domFragment)
        this.renderFootnotes(domFragment, citationFormatter)
        this.removeTrackingData(domFragment)
        this.addBaseUrlToImages(domFragment)
        this.addFigureNumbers(domFragment)
        return domFragment
    }

    renderCitations(domFragment) {
        const settings = this.editor.view.state.doc.attrs,
            bibliographyHeader =
                settings.bibliography_header[settings.language] ||
                _schema_i18n__rspack_import_1.BIBLIOGRAPHY_HEADERS[settings.language]
        const citRenderer = new _citations_render__rspack_import_0.RenderCitations(
            domFragment,
            settings.citationstyle,
            bibliographyHeader,
            this.editor.mod.db.bibDB,
            this.editor.app.csl,
            true // synchronous. Should work as the editor has used the same style previously.
        )
        if (citRenderer.init()) {
            if (citRenderer.fm.bibHTML.length) {
                const bibDiv = document.createElement("div")
                bibDiv.classList.add("fiduswriter-clipboard-bibliography")
                bibDiv.innerHTML = citRenderer.fm.bibHTML
                bibDiv.firstElementChild.innerHTML = gettext("Bibliography")
                domFragment.appendChild(bibDiv)
            }
            return citRenderer.fm
        } else {
            return false
        }
    }

    renderFootnotes(domFragment, citationFormatter) {
        const footnoteSelector =
            citationFormatter && citationFormatter.citationType === "note"
                ? ".footnote-marker, .citation"
                : ".footnote-marker"
        // Inside of footnote markers add anchors and put footnotes with content
        // at the back of the document.
        // Also, link the footnote anchor with the footnote.
        const footnotes = domFragment.querySelectorAll(footnoteSelector)
        const footnotesContainer = document.createElement("section")
        let citationCount = 0
        footnotesContainer.setAttribute("role", "doc-footnotes")
        footnotesContainer.classList.add("fnlist")
        footnotesContainer.classList.add("fiduswriter-clipboard-footnotes")
        footnotes.forEach((footnote, index) => {
            const counter = index + 1,
                id = this.getRandomID()
            const footnoteAnchor = this.getFootnoteAnchor(counter, id)
            footnote.appendChild(footnoteAnchor)
            const newFootnote = document.createElement("h6") // We use H6 as Wordpress Gutenberg only allows IDs on H1-6 elements.
            newFootnote.setAttribute("role", "doc-footnote")
            newFootnote.innerHTML = footnote.matches(".footnote-marker")
                ? footnote.dataset.footnote
                : `<p>${citationFormatter.citationTexts[citationCount++] || " "}</p>`
            if (
                newFootnote.firstElementChild &&
                newFootnote.firstElementChild.matches("p")
            ) {
                newFootnote.firstElementChild.insertAdjacentHTML(
                    "afterbegin",
                    `${counter}. `
                )
            } else {
                newFootnote.insertAdjacentHTML(
                    "afterbegin",
                    `<p>${counter}. </p>`
                )
            }
            newFootnote.id = `fn-${id}`
            footnotesContainer.appendChild(newFootnote)
        })
        if (footnotes.length) {
            domFragment.appendChild(footnotesContainer)
        }
    }

    addFigureNumbers(domFragment) {
        domFragment
            .querySelectorAll(
                "figure[data-category='figure'] figcaption span.label"
            )
            .forEach((el, index) => {
                el.innerHTML += " " + (index + 1) + ": "
            })

        domFragment
            .querySelectorAll(
                "figure[data-category='photo'] figcaption span.label"
            )
            .forEach((el, index) => {
                el.innerHTML += " " + (index + 1) + ": "
            })

        domFragment
            .querySelectorAll(
                "figure[data-category='table'] figcaption span.label"
            )
            .forEach((el, index) => {
                el.innerHTML += " " + (index + 1) + ": "
            })
    }

    addBaseUrlToImages(domFragment) {
        domFragment
            .querySelectorAll("img")
            .forEach(el => el.setAttribute("src", el.src))
    }

    getRandomID() {
        return (0 | (Math.random() * 9e6)).toString(36)
    }

    getFootnoteAnchor(counter, id) {
        const footnoteAnchor = document.createElement("a")
        footnoteAnchor.setAttribute("href", `#fn-${id}`)
        footnoteAnchor.classList.add("fn")
        footnoteAnchor.classList.add("sdfootnoteanc")
        footnoteAnchor.innerHTML = `<sup>${counter}</sup>`
        return footnoteAnchor
    }

    removeTrackingData(domFragment) {
        domFragment
            .querySelectorAll(".approved-insertion, .insertion")
            .forEach(el => {
                const parent = el.parentNode
                const fragment = document.createDocumentFragment()
                while (el.firstChild) {
                    fragment.appendChild(el.firstChild)
                }
                parent.replaceChild(fragment, el)
            })
        domFragment
            .querySelectorAll(".deletion")
            .forEach(el => el.parentElement.removeChild(el))
    }

    static fromSchema(schema, editor) {
        return new ClipboardDOMSerializer(
            prosemirror_model__rspack_import_3.DOMSerializer.nodesFromSchema(schema),
            prosemirror_model__rspack_import_3.DOMSerializer.marksFromSchema(schema),
            editor
        )
    }
}

const docClipboardSerializer = editor =>
    ClipboardDOMSerializer.fromSchema(
        (0,_schema__rspack_import_2.createDocCopySchema)(editor.schema),
        editor
    )
const fnClipboardSerializer = editor =>
    ClipboardDOMSerializer.fromSchema(_schema__rspack_import_2.fnCopySchema, editor)


}),
"./js/modules/editor/clipboard/paste/fidus_writer.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FidusWriterPasteHandler: function() { return FidusWriterPasteHandler; }
});
/* import */ var _general__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/general.js");


// Fidus Writer paste handler
class FidusWriterPasteHandler extends _general__rspack_import_0.GeneralPasteHandler {
    // Convert an existing node to a different node, if needed.
    convertNode(node) {
        node = super.convertNode(node)
        // Replace  nodes with other nodes to not change the number of child nodes
        // <b style="font-weight:normal;">...</b> => <span>...</span>
        if (node.tagName === "SPAN" && node.classList.contains("citation")) {
            node = this.verifyCitationNode(node)
        }
        return node
    }

    verifyCitationNode(node) {
        try {
            const bibs = JSON.parse(node.dataset.bibs),
                references = JSON.parse(node.dataset.references),
                bibDB = this.editor.mod.db.bibDB,
                idTranslations = {}
            Object.keys(bibs).forEach(bibKey => {
                const reference = bibs[bibKey]
                const oldKey = bibDB.findReference(reference)
                if (oldKey) {
                    idTranslations[bibKey] = oldKey
                } else {
                    const newKey = bibDB.addReference(reference, bibKey)
                    idTranslations[bibKey] = newKey
                }
            })

            if (
                Object.entries(idTranslations).every(
                    trans => trans[0] === trans[1]
                )
            ) {
                return node
            }
            references.forEach(ref => (ref.id = idTranslations[ref.id]))
            node.dataset.references = JSON.stringify(references)
            return node
        } catch (_error) {
            return node
        }
    }

    cleanDOM() {
        // Remove the bibliography that was added by the copy serializer
        const clipboardArtifacts = this.dom.querySelectorAll(
            ".fiduswriter-clipboard-bibliography, .fiduswriter-clipboard-footnotes"
        )
        clipboardArtifacts.forEach(el => el.parentElement.removeChild(el))
    }
}


}),
"./js/modules/editor/clipboard/paste/general.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  GeneralPasteHandler: function() { return GeneralPasteHandler; }
});
// General Fallback handler for paste
class GeneralPasteHandler {
    constructor(editor, htmlDoc, pmType) {
        this.editor = editor
        this.htmlDoc = htmlDoc
        this.pmType = pmType
        this.footnoteMarkers = []
        this.footnotes = []
    }

    // Iterate over each node in the body of the pasted content.
    getOutput() {
        this.dom = this.htmlDoc.getElementsByTagName("body")[0]
        this.inHTML = this.dom.innerHTML
        this.iterateNode(this.dom)
        this.cleanDOM()
        this.convertFootnotes()
        this.outHTML = this.dom.innerHTML
        return this.outHTML
    }

    // Remove unused content
    cleanDOM() {}

    // Iterate over pasted nodes and their children
    iterateNode(node) {
        if ((node.tagName === "P") & !node.firstChild) {
            node.parentNode.removeChild(node)
            return
        } else if (node.nodeType === 8) {
            // comment node
            node.parentNode.removeChild(node)
            return
        } else if (node.nodeType === 1) {
            // element node
            let childNode = node.firstChild
            while (childNode) {
                const nextChildNode = childNode.nextSibling
                this.iterateNode(childNode)
                childNode = nextChildNode
            }
            node = this.convertNode(node)
        }
    }

    // Convert an existing node to a different node, if needed.
    convertNode(node) {
        if (
            node.tagName === "TABLE" &&
            node.firstElementChild?.tagName !== "CAPTION"
        ) {
            const caption = document.createElement("caption")
            caption.innerHTML = "<span class='text'></span>"
            node.insertBefore(caption, node.firstChild)
        }
        return node
    }

    // Move footnotes into their markers and turn footnote markers into the
    // required format.
    convertFootnotes() {
        this.footnoteMarkers.forEach((fnM, index) => {
            const footnote = this.footnotes[index]
            const newFnM = document.createElement("span")
            newFnM.classList.add("footnote-marker")
            const footnoteContents = footnote.innerHTML.replace(/\s+/g, " ")
            // Remove linebreaks in string (not <BR>)
            // Turn multiple white spaces into single space
            newFnM.dataset.footnote = footnoteContents
            fnM.parentNode.replaceChild(newFnM, fnM)
        })
        // Remove all footnotes from document. Some footnotes may have several
        // markers, so only remove each footnote once.
        this.footnotes.forEach(fn => {
            if (fn.parentNode) {
                fn.parentNode.removeChild(fn)
            }
        })
    }
}


}),
"./js/modules/editor/clipboard/paste/google_docs.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  GoogleDocsPasteHandler: function() { return GoogleDocsPasteHandler; }
});
/* import */ var _general__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/general.js");


// Google Docs (the writing component) paste handler
class GoogleDocsPasteHandler extends _general__rspack_import_0.GeneralPasteHandler {
    // Convert an existing node to a different node, if needed.
    convertNode(node) {
        node = super.convertNode(node)
        // Replace  nodes with other nodes to not change the number of child nodes
        // <b style="font-weight:normal;">...</b> => <span>...</span>
        if (node.tagName === "B" && node.style.fontWeight === "normal") {
            node = this.neutralizeInlineNode(node)
        }
        return node
    }

    // Replace any type of inline node with a span node.
    neutralizeInlineNode(node) {
        const newNode = document.createElement("span")
        while (node.firstChild) {
            newNode.appendChild(node.firstChild)
        }
        node.parentNode.replaceChild(newNode, node)

        return newNode
    }
}


}),
"./js/modules/editor/clipboard/paste/html.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  HTMLPaste: function() { return HTMLPaste; }
});
/* import */ var _fidus_writer__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/fidus_writer.js");
/* import */ var _general__rspack_import_1 = __webpack_require__("./js/modules/editor/clipboard/paste/general.js");
/* import */ var _google_docs__rspack_import_2 = __webpack_require__("./js/modules/editor/clipboard/paste/google_docs.js");
/* import */ var _libreoffice_writer__rspack_import_3 = __webpack_require__("./js/modules/editor/clipboard/paste/libreoffice_writer.js");
/* import */ var _microsoft_word__rspack_import_4 = __webpack_require__("./js/modules/editor/clipboard/paste/microsoft_word.js");
/* import */ var _state_plugins_clipboard__rspack_import_5 = __webpack_require__("./js/modules/editor/state_plugins/clipboard.js");








// Some pasted HTML will need slight conversions to work correctly.
// We try to sniff whether paste comes from MsWord, LibreOffice or Google Docs
// and use specialized handlers for these and a general handler everything else.

class HTMLPaste {
    constructor(editor, inHTML, pmType, view) {
        this.editor = editor
        this.inHTML = inHTML
        this.pmType = pmType
        this.view = view
    }

    getOutput() {
        this.parseHTML()
        this.selectHandler()
        this.handlerInstance = new this.handler(
            this.editor,
            this.htmlDoc,
            this.pmType
        )
        this.outHTML = this.handlerInstance.getOutput()
        setTimeout(() => {
            this.resetPasteRange()
        }, 0)
        return this.outHTML
    }

    parseHTML() {
        const parser = new window.DOMParser()
        this.htmlDoc = parser
            .parseFromString(this.inHTML, "text/html")
            .getElementsByTagName("html")[0]
    }

    // Find out what the source of the paste is and choose a corresponding
    // handler.
    selectHandler() {
        // For LibreOffice
        const head = this.htmlDoc.getElementsByTagName("head")[0]
        const generatorMetaTag = head.querySelector("meta[name=generator]")
        // For Google Docs
        const body = this.htmlDoc.getElementsByTagName("body")[0]
        const firstB = body.querySelector("b")
        // For Fidus Writer
        const pmSlice = this.htmlDoc.querySelector("[data-pm-slice]")
        if (
            this.htmlDoc.hasAttribute("xmlns:w") &&
            this.htmlDoc.getAttribute("xmlns:w") ===
                "urn:schemas-microsoft-com:office:word"
        ) {
            this.handler = _microsoft_word__rspack_import_4.MicrosoftWordPasteHandler
        } else if (generatorMetaTag?.content?.startsWith("LibreOffice")) {
            this.handler = _libreoffice_writer__rspack_import_3.LibreOfficeWriterPasteHandler
        } else if (firstB?.id.startsWith("docs-internal-guid")) {
            this.handler = _google_docs__rspack_import_2.GoogleDocsPasteHandler
        } else if (pmSlice) {
            this.handler = _fidus_writer__rspack_import_0.FidusWriterPasteHandler
        } else {
            this.handler = _general__rspack_import_1.GeneralPasteHandler
        }
    }

    resetPasteRange() {
        const tr = this.view.state.tr
        ;(0,_state_plugins_clipboard__rspack_import_5.resetPasteRange)(tr)
        this.view.dispatch(tr)
    }
}


}),
"./js/modules/editor/clipboard/paste/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  HTMLPaste: function() { return /* reexport safe */ _html__rspack_import_0.HTMLPaste; },
  TextPaste: function() { return /* reexport safe */ _text__rspack_import_1.TextPaste; }
});
/* import */ var _html__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/html.js");
/* import */ var _text__rspack_import_1 = __webpack_require__("./js/modules/editor/clipboard/paste/text.js");




}),
"./js/modules/editor/clipboard/paste/libreoffice_writer.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  LibreOfficeWriterPasteHandler: function() { return LibreOfficeWriterPasteHandler; }
});
/* import */ var _general__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/general.js");


// LibreOffice Writer paste handler
class LibreOfficeWriterPasteHandler extends _general__rspack_import_0.GeneralPasteHandler {
    // Convert an existing node to a different node, if needed.
    convertNode(node) {
        node = super.convertNode(node)
        // Footnote markers (only in main pm instance):
        if (
            node.tagName === "A" &&
            node.classList.contains("sdfootnoteanc") &&
            this.pmType === "main"
        ) {
            // Remove "sym" at the end of the selector
            const href = node.getAttribute("href")
            const fnSelector =
                href.length > 3 ? href.substring(0, href.length - 3) : href
            const footnote = this.dom.querySelector(fnSelector)
            if (footnote) {
                const footnoteCounter =
                    footnote.querySelector("a.sdfootnotesym")
                if (footnoteCounter) {
                    footnoteCounter.parentNode.removeChild(footnoteCounter)
                }
                this.footnoteMarkers.push(node)
                this.footnotes.push(footnote)
            }
        }

        return node
    }
}


}),
"./js/modules/editor/clipboard/paste/microsoft_word.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  MicrosoftWordPasteHandler: function() { return MicrosoftWordPasteHandler; }
});
/* import */ var _general__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/paste/general.js");


// Microsoft Word 2016 paste handler
class MicrosoftWordPasteHandler extends _general__rspack_import_0.GeneralPasteHandler {
    // Remove unused content
    cleanDOM() {
        // Remove footnote list container with separator line
        const removableElements = this.dom.querySelectorAll(
            'div[style*="mso-element:footnote-list"]'
        )

        removableElements.forEach(el => el.parentNode.removeChild(el))
    }

    // Iterate over pasted nodes and their children
    iterateNode(node) {
        node = super.convertNode(node)
        if ((node.tagName === "P") & !node.firstChild) {
            node.parentNode.removeChild(node)
            return true
        } else if (node.nodeType === 8) {
            if (node.textContent === "EndFragment") {
                // End of paste content. Remove all remaining sibling nodes.
                while (node) {
                    const nextSibling = node.nextSibling
                    node.parentNode.removeChild(node)
                    node = nextSibling
                }
                return false
            } else {
                node.parentNode.removeChild(node)
                return true
            }
        }
        if (node.nodeType === 1) {
            let childNode = node.firstChild
            while (childNode) {
                const nextChildNode = childNode.nextSibling
                if (this.iterateNode(childNode)) {
                    childNode = nextChildNode
                } else {
                    childNode = false
                }
            }
            node = this.convertNode(node)
        }

        return true
    }

    // Convert an existing node to a different node, if needed.
    convertNode(node) {
        // Footnote markers (only in main pm instance):
        if (
            node.tagName === "A" &&
            node.firstChild?.tagName === "SPAN" &&
            node.firstChild?.classList.contains("MsoFootnoteReference") &&
            this.pmType === "main"
        ) {
            // Remove "#_ftn" from the selector (#_ftn1)
            const fnSelector = node.getAttribute("href")
            const fnNumber = node
                .getAttribute("href")
                .substring(5, fnSelector.length)
            const footnote = this.dom.querySelector("#ftn" + fnNumber)
            if (footnote) {
                const footnoteCounter = footnote.querySelector(
                    'a[href="#_ftnref' + fnNumber + '"]'
                )
                if (footnoteCounter) {
                    const followingNode = footnoteCounter.nextSibling
                    footnoteCounter.parentNode.removeChild(footnoteCounter)
                    if (followingNode?.nodeType === 3) {
                        // If there is a text string right after the footnote
                        // marker, remove any leading spaces.
                        followingNode.nodeValue =
                            followingNode.nodeValue.replace(/^\s+/, "")
                    }
                }
                this.footnoteMarkers.push(node)
                this.footnotes.push(footnote)
            }
        }

        return node
    }
}


}),
"./js/modules/editor/clipboard/paste/text.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  TextPaste: function() { return TextPaste; }
});
/* import */ var _state_plugins_clipboard__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/clipboard.js");





class TextPaste {
    constructor(editor, text, view) {
        this.editor = editor
        this.text = text
        this.view = view
        this.foundBibEntries = false
    }

    init() {
        __webpack_require__.e(/* import() */ "js_modules_bibliography_import_index_js").then(__webpack_require__.bind(__webpack_require__, "./js/modules/bibliography/import/index.js")).then(
            ({BibliographyImporter}) => {
                const importer = new BibliographyImporter(
                    this.text,
                    this.editor.mod.db.bibDB,
                    newIds => {
                        this.foundBibEntries = true
                        const format = "autocite",
                            references = newIds.map(id => ({id}))

                        const citationNode = this.view.state.schema.nodes[
                            "citation"
                        ].create({format, references})
                        const pasteRange = (0,_state_plugins_clipboard__rspack_import_0.getPasteRange)(this.view.state)
                        if (pasteRange) {
                            const tr = this.view.state.tr
                            tr.replaceRangeWith(
                                pasteRange[0],
                                pasteRange[1],
                                citationNode
                            )
                            ;(0,_state_plugins_clipboard__rspack_import_0.resetPasteRange)(tr)
                            tr.setMeta("addToHistory", false)
                            this.view.dispatch(tr)
                        }
                    },
                    () => {
                        if (!this.foundBibEntries) {
                            // There were no citations in the pasted text.
                            const pasteRange = (0,_state_plugins_clipboard__rspack_import_0.getPasteRange)(this.view.state)
                            if (pasteRange) {
                                const tr = this.view.state.tr
                                ;(0,_state_plugins_clipboard__rspack_import_0.resetPasteRange)(tr)
                                this.view.dispatch(tr)
                            }
                        }
                    },
                    false // no messages to end user. Would be confusing if user just wants to paste unrelated text.
                )
                importer.init()
            }
        )
    }
}


}),
"./js/modules/editor/collab/chat.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCollabChat: function() { return ModCollabChat; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _e2ee_encryptor__rspack_import_1 = __webpack_require__("./js/modules/editor/e2ee/encryptor.js");
/* import */ var _templates__rspack_import_2 = __webpack_require__("./js/modules/editor/collab/templates.js");




/*
 * Functions for chat between users who access a document simultaneously.
 */

class ModCollabChat {
    constructor(mod) {
        mod.chat = this
        this.mod = mod
        this.currentlyFlashing = false
        this.focus = true
        this.init()
    }

    beep() {
        const notification = document.getElementById("chat-notification")
        notification.play().catch(
            () => {} // Don't worry about it if the browser prohibits playing the sound
        )
    }

    flashtab(messageTitle) {
        if (this.currentlyFlashing) {
            return false
        }
        const origTitle = document.title

        this.currentlyFlashing = true

        const changeDocumentTitle = window.setInterval(() => {
            if (this.focus) {
                window.clearInterval(changeDocumentTitle)
                document.title = origTitle
                this.currentlyFlashing = false
            } else {
                document.title =
                    document.title === origTitle ? messageTitle : origTitle
            }
        }, 500)
    }

    async newMessage(message) {
        if (document.getElementById(`m${message.id}`)) {
            return
        }

        // For E2EE documents, decrypt the chat message body.
        if (message.e2ee) {
            const isE2EE =
                this.mod.editor.e2ee &&
                this.mod.editor.e2ee.encrypted &&
                this.mod.editor.e2ee.key
            if (isE2EE) {
                try {
                    message.body = await _e2ee_encryptor__rspack_import_1.E2EEEncryptor.decrypt(
                        message.body,
                        this.mod.editor.e2ee.key
                    )
                } catch (error) {
                    console.error("E2EE: Failed to decrypt chat message", error)
                    message.body = gettext(
                        "[Encrypted message - decryption failed]"
                    )
                }
            } else {
                message.body = gettext(
                    "[Encrypted message - enter password to read]"
                )
            }
        }

        const theChatter = this.mod.participants.find(
            participant => participant.id === message.from
        )

        const chatContainer = document.getElementById("chat-container")
        chatContainer.insertAdjacentHTML(
            "beforeend",
            (0,_templates__rspack_import_2.messageTemplate)({message, theChatter, localizeDate: _common__rspack_import_0.localizeDate})
        )
        if (!this.focus) {
            this.beep()
            this.flashtab(message.from + ": " + message.body)
        }
        if (chatContainer.style.display === "none") {
            document.getElementById("chat").classList.add("highlighted")
        }
    }

    showChat(participants) {
        // If only one machine is connected and nothing has been chatted, don't show chat
        if (
            participants.length === 1 &&
            !document.querySelector("#chat-container .message")
        ) {
            document.getElementById("chat").style.display = "none"
        } else {
            document.getElementById("chat").style.display = "block"
        }
    }

    async sendMessage(messageText) {
        // For E2EE documents, encrypt the chat message body.
        // The server relays encrypted messages without reading them.
        const isE2EE =
            this.mod.editor.e2ee &&
            this.mod.editor.e2ee.encrypted &&
            this.mod.editor.e2ee.key

        if (isE2EE) {
            try {
                const encryptedBody = await _e2ee_encryptor__rspack_import_1.E2EEEncryptor.encrypt(
                    messageText,
                    this.mod.editor.e2ee.key
                )
                this.mod.editor.ws.send(() => ({
                    type: "chat",
                    body: encryptedBody,
                    e2ee: true
                }))
            } catch (error) {
                console.error("E2EE: Failed to encrypt chat message", error)
                // Fall back to sending unencrypted (should not happen
                // in normal operation)
                this.mod.editor.ws.send(() => ({
                    type: "chat",
                    body: messageText
                }))
            }
        } else {
            this.mod.editor.ws.send(() => ({
                type: "chat",
                body: messageText
            }))
        }
    }

    init() {
        document.body.insertAdjacentHTML(
            "beforeend",
            `<style>\n#messageform.empty:before{content:"${gettext("Send a message...")}"}\n</style>`
        )
        ;(0,_common__rspack_import_0.whenReady)().then(() => {
            document.getElementById("chat-container").style.maxHeight =
                `${window.innerHeight - 200}px`

            const resizeButton = document.querySelector("#chat .resize-button")
            resizeButton.addEventListener("click", () => {
                const chatEl = document.getElementById("chat")
                if (resizeButton.classList.contains("fa-angle-double-down")) {
                    resizeButton.classList.remove("fa-angle-double-down")
                    resizeButton.classList.add("fa-angle-double-up")
                    chatEl.style.top = `${chatEl.getBoundingClientRect().top}px` // Set current height to get the animation working.
                    setTimeout(
                        () =>
                            (chatEl.style.top = `${window.innerHeight - 29}px`),
                        0
                    )
                } else {
                    resizeButton.classList.remove("fa-angle-double-up")
                    resizeButton.classList.add("fa-angle-double-down")
                    // Add height teemporarily to make sliding animation.
                    chatEl.style.top = `${Math.max(window.innerHeight - chatEl.scrollHeight - 11, 0)}px` // 11px for padding
                    setTimeout(() => (chatEl.style.top = ""), 3000)
                }
            })

            const messageForm = document.getElementById("messageform")

            messageForm.addEventListener("focus", () =>
                messageForm.classList.remove("empty")
            )

            messageForm.addEventListener("blur", () => {
                if (messageForm.innerText.trim().length === 0) {
                    messageForm.classList.add("empty")
                }
            })

            messageForm.addEventListener("keypress", event => {
                if (event.keyCode === 13) {
                    this.sendMessage(messageForm.innerText)
                    messageForm.innerText = ""
                    return false
                }
            })
        })

        window.addEventListener("blur", () => (this.focus = false))
        window.addEventListener("focus", () => (this.focus = true))
    }
}


}),
"./js/modules/editor/collab/colors.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCollabColors: function() { return ModCollabColors; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");


const CSS_COLORS = [
    "0,119,190",
    "217,58,50",
    "0,0,160",
    "119,190,0",
    "97,255,105",
    "173,216,230",
    "128,0,128",
    "128,128,128",
    "255,165,0"
]

/* Create a CSS stylesheet for the colors of all users. */
class ModCollabColors {
    constructor(mod) {
        mod.colors = this
        this.mod = mod
        this.userColorStyle = false
        this.colorIds = []
        this.setup()
    }

    setup() {
        const styleContainers = document.createElement("temp")
        styleContainers.innerHTML =
            '<style type="text/css" id="user-colors"></style>'
        while (styleContainers.firstElementChild) {
            document.body.appendChild(styleContainers.firstElementChild)
        }
        this.userColorStyle = document.getElementById("user-colors")
    }

    ensureUserColor(userId) {
        /* We assign a color to each user. This color stays even if the user
         * disconnects or the participant list is being updated.
         */
        if (!this.colorIds.includes(userId)) {
            this.colorIds.push(userId)
            this.provideUserColorStyles()
        }
    }

    // Ensure that there are at least the given number of user color styles.
    provideUserColorStyles() {
        this.userColorStyle.innerHTML = this.colorIds
            .map((id, index) => {
                const color =
                    index < CSS_COLORS.length
                        ? CSS_COLORS[index]
                        : `${Math.round(Math.random() * 255)},${Math.round(Math.random() * 255)},${Math.round(Math.random() * 255)}`
                return (0,_common__rspack_import_0.noSpaceTmp)`
                span.user-${id} {
                    border-color: rgba(${color},1);
                    text-decoration-color: rgba(${color},1);
                }
                span.user-${id}.insertion {
                    color: rgba(${color},1);
                }
                .user-bg-${id} {
                    background-color: rgba(${color},0.2);
                }`
            })
            .join("\n")
    }
}


}),
"./js/modules/editor/collab/doc.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCollabDoc: function() { return ModCollabDoc; }
});
/* import */ var prosemirror_collab__rspack_import_11 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_state__rspack_import_9 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_10 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_export__rspack_import_1 = __webpack_require__("./js/modules/schema/export.js");
/* import */ var _e2ee_encryptor__rspack_import_2 = __webpack_require__("./js/modules/editor/e2ee/encryptor.js");
/* import */ var _e2ee_key_manager__rspack_import_3 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _e2ee_passphrase_dialog__rspack_import_4 = __webpack_require__("./js/modules/editor/e2ee/passphrase-dialog.js");
/* import */ var _e2ee_passphrase_manager__rspack_import_5 = __webpack_require__("./js/modules/editor/e2ee/passphrase-manager.js");
/* import */ var _e2ee_password_dialog__rspack_import_6 = __webpack_require__("./js/modules/editor/e2ee/password-dialog.js");
/* import */ var _state_plugins__rspack_import_7 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _merge__rspack_import_8 = __webpack_require__("./js/modules/editor/collab/merge/index.js");













class ModCollabDoc {
    constructor(mod) {
        mod.doc = this
        this.mod = mod
        this.merge = new _merge__rspack_import_8.Merge(mod)
        this.unconfirmedDiffs = {}
        this.confirmStepsRequestCounter = 0
        this.awaitingDiffResponse = false
        this.receiving = false
        this.currentlyCheckingVersion = false
        this.footnoteRender = false // If the offline user edited a footnote , it needs to be rendered properly to connected users too!
        this.initialVersionConfirmed = false
        this.initialDocLoaded = false
        this.templateAdjustmentPending = false
    }

    cancelCurrentlyCheckingVersion() {
        this.currentlyCheckingVersion = false
        window.clearTimeout(this.enableCheckVersion)
    }

    checkVersion(offline = false) {
        // Guard: WebSocket may not be initialized yet (e.g. during
        // loadDocument before the WS connector is created).
        if (!this.mod.editor.ws) {
            return
        }
        this.mod.editor.ws.send(() => {
            if (
                this.currentlyCheckingVersion ||
                this.mod.editor.docInfo.version === undefined
            ) {
                return
            }
            this.currentlyCheckingVersion = true
            this.enableCheckVersion = window.setTimeout(() => {
                this.currentlyCheckingVersion = false
            }, 1000)
            if (this.mod.editor.ws.connected) {
                this.disableDiffSending()
            }

            const msg = {
                type: "check_version",
                v: this.mod.editor.docInfo.version
            }
            if (offline) {
                msg.offline = true
            }
            return msg
        })
    }

    disableDiffSending() {
        this.awaitingDiffResponse = true
        // If no answer has been received from the server within 2 seconds,
        // check the version
        this.sendNextDiffTimer = window.setTimeout(() => {
            this.awaitingDiffResponse = false
            this.sendToCollaborators()
        }, 8000)
    }

    enableDiffSending() {
        window.clearTimeout(this.sendNextDiffTimer)
        this.awaitingDiffResponse = false
        this.sendToCollaborators()
    }

    receiveDocument(data) {
        this.cancelCurrentlyCheckingVersion()
        if (
            this.mod.editor.docInfo.confirmedDoc &&
            !this.mod.editor.e2ee?.encrypted
        ) {
            this.merge.adjustDocument(data)
        } else {
            this.loadDocument(data)
        }
    }

    finishInitialLoad() {
        if (
            this.initialDocLoaded &&
            this.initialVersionConfirmed &&
            !this.templateAdjustmentPending
        ) {
            (0,_common__rspack_import_0.deactivateWait)()
            this.mod.editor.waitingForDocument = false
        }
    }

    confirmVersion(version) {
        if (!this.mod.editor.docInfo.confirmedDoc) {
            return
        }
        if (!this.initialVersionConfirmed) {
            this.initialVersionConfirmed = true
            if (version === this.mod.editor.docInfo.version) {
                this.finishInitialLoad()
            }
        }
    }

    loadDocument({doc, time, doc_info}) {
        const isInitialLoad = !this.mod.editor.docInfo.confirmedDoc
        // Reset collaboration
        this.unconfirmedDiffs = {}
        if (this.awaitingDiffResponse) {
            this.enableDiffSending()
        }

        this.mod.editor.clientTimeAdjustment = Date.now() - time

        const token = this.mod.editor.docInfo.token
        this.mod.editor.docInfo = doc_info
        this.mod.editor.docInfo.token = token
        // For guests, update user object with the token UUID (stable identity)
        // and session_id (for display). Token UUID persists across reconnections.
        // session_id may not be present when loading from REST (it comes via
        // the session_info WebSocket message). Only update if available.
        if (
            !this.mod.editor.user.is_authenticated &&
            doc_info.session_id !== undefined
        ) {
            const sessionId = doc_info.session_id
            this.mod.editor.user = {
                id: token, // stable — same token UUID even after reconnect
                username: `guest${sessionId}`, // display name per session
                name: `Guest ${sessionId}`,
                is_authenticated: false
            }
        }
        this.mod.editor.docInfo.token = token
        this.mod.editor.docInfo.version = doc.v
        this.mod.editor.docInfo.updated = new Date()

        // Check if this is an E2EE document. If so, we need to decrypt
        // the content before loading it into ProseMirror.
        const isE2EE = doc_info.e2ee === true
        if (isE2EE) {
            this._loadE2EEDocument(doc, doc_info, isInitialLoad)
        } else {
            this.mod.editor.e2ee = {encrypted: false}
            this._loadUnencryptedDocument(doc, isInitialLoad)
        }
    }

    /**
     * Load an E2EE document: try passphrase/DEK first, then fall back to
     * per-document password prompt, decrypt, then load.
     * @private
     */
    async _loadE2EEDocument(doc, doc_info, isInitialLoad) {
        // Extract password from URL fragment if present (share link format:
        // https://example.com/share/TOKEN/#target?password=PASSWORD). The fragment is never
        // sent to the server, so this is safe.
        const locationHash = window.location.hash
        let urlFragmentPassword = ""
        if (locationHash && locationHash.includes("?")) {
            urlFragmentPassword = decodeURIComponent(
                new URLSearchParams(locationHash.split("?")[1]).get(
                    "password"
                ) || ""
            )
        }

        // Get the salt and iterations. These may come from the REST response
        // (doc_info) or from the WebSocket session_info message (already
        // stored in this.mod.editor.e2ee).
        let salt, iterations
        if (this.mod.editor.e2ee) {
            salt = this.mod.editor.e2ee.encryptionSalt
            iterations = this.mod.editor.e2ee.encryptionIterations
        }
        if (doc_info.e2ee_salt) {
            salt = doc_info.e2ee_salt
        }
        if (doc_info.e2ee_iterations) {
            iterations = doc_info.e2ee_iterations
        }

        const docId = this.mod.editor.docInfo.id

        // Helper to save password for passphrase users after successful decryption
        // (migrates per-document-password docs into the passphrase system)
        const maybeSavePasswordForPassphrase = async password => {
            if (!_e2ee_passphrase_manager__rspack_import_5.PassphraseManager.hasKeysInSession()) {
                return
            }
            try {
                const existing =
                    await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.getDocumentPassword(docId)
                if (!existing) {
                    await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.saveDocumentPassword(
                        docId,
                        password,
                        null,
                        "user",
                        true
                    )
                }
            } catch (_e) {
                console.error(
                    "E2EE: Failed to save document password for passphrase:",
                    _e
                )
            }
        }

        // Helper to resolve a password to a key and attempt decryption
        const tryPassword = async password => {
            const key = await _e2ee_key_manager__rspack_import_3.E2EEKeyManager.resolvePasswordToKey(
                password,
                salt
                    ? new Uint8Array(
                          atob(salt)
                              .split("")
                              .map(c => c.charCodeAt(0))
                      )
                    : null,
                iterations || 600000
            )
            await this._decryptAndLoadDoc(
                doc,
                key,
                salt,
                iterations,
                isInitialLoad,
                urlFragmentPassword
            )
            // Cache password and key for this session
            _e2ee_key_manager__rspack_import_3.E2EEKeyManager.storePasswordInSession(docId, password)
            await _e2ee_key_manager__rspack_import_3.E2EEKeyManager.storeKeyInSession(docId, key)
            if (this.mod.editor.e2ee) {
                this.mod.editor.e2ee.key = key
                this.mod.editor.e2ee.password = password
            }
            // Migrate per-document-password docs to passphrase system
            await maybeSavePasswordForPassphrase(password)
        }

        // --- Try passphrase path first ---
        if (_e2ee_passphrase_manager__rspack_import_5.PassphraseManager.hasKeysInSession()) {
            const password = await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.getDocumentPassword(docId)
            if (password) {
                try {
                    await tryPassword(password)
                    if (this.mod.editor.e2ee) {
                        this.mod.editor.e2ee.usesPassphrase = true
                    }
                    return
                } catch (_error) {
                    // Password didn't work — fall through
                }
            }
        }

        // If passphrase keys exist but are not in session, try to unlock
        if (!_e2ee_passphrase_manager__rspack_import_5.PassphraseManager.hasKeysInSession()) {
            const hasKeys = await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.hasEncryptionKeys()
            if (hasKeys) {
                let errorMessage = ""
                let unlocked = false
                while (!unlocked) {
                    const result = await new Promise(resolve => {
                        (0,_e2ee_passphrase_dialog__rspack_import_4.enterPassphraseDialog)(
                            pwd => resolve({action: "unlock", passphrase: pwd}),
                            () => resolve({action: "recover"}),
                            {errorMessage}
                        )
                    })
                    if (result.action === "unlock" && result.passphrase) {
                        try {
                            await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.unlockWithPassphrase(
                                result.passphrase
                            )
                            const password =
                                await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.getDocumentPassword(
                                    docId
                                )
                            if (password) {
                                await tryPassword(password)
                                if (this.mod.editor.e2ee) {
                                    this.mod.editor.e2ee.usesPassphrase = true
                                }
                                unlocked = true
                                return
                            }
                        } catch (_e) {
                            errorMessage = gettext(
                                "Incorrect passphrase. Please try again."
                            )
                        }
                    } else if (result?.action === "recover") {
                        // Recovery flow
                        const {recoverWithKeyDialog} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/passphrase-dialog.js"))
                        const recoverResult = await new Promise(resolve => {
                            recoverWithKeyDialog(resolve)
                        })
                        if (recoverResult) {
                            try {
                                const {newRecoveryKey} =
                                    await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.recoverWithRecoveryKey(
                                        recoverResult.recoveryKey,
                                        recoverResult.newPassphrase
                                    )
                                const {showRecoveryKeyDialog} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/passphrase-dialog.js"))
                                await new Promise(resolve => {
                                    showRecoveryKeyDialog(
                                        newRecoveryKey,
                                        resolve
                                    )
                                })
                                // After recovery, try to get document password again
                                const password =
                                    await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.getDocumentPassword(
                                        docId
                                    )
                                if (password) {
                                    await tryPassword(password)
                                    if (this.mod.editor.e2ee) {
                                        this.mod.editor.e2ee.usesPassphrase = true
                                    }
                                    return
                                }
                            } catch (e) {
                                console.error("E2EE: Recovery failed:", e)
                                const errorDialog = new _common__rspack_import_0.Dialog({
                                    title: gettext("Recovery Failed"),
                                    id: "e2ee-recovery-failed",
                                    body: gettext(
                                        "The recovery key you entered is incorrect, or the recovery process failed. Please try again."
                                    ),
                                    buttons: [
                                        {
                                            text: gettext("Retry"),
                                            classes: "fw-dark",
                                            click: () => {
                                                errorDialog.close()
                                                this._loadE2EEDocument(
                                                    doc,
                                                    doc_info,
                                                    isInitialLoad,
                                                    urlFragmentPassword
                                                )
                                            }
                                        },
                                        {
                                            text: gettext("Cancel"),
                                            classes: "fw-light",
                                            click: () => errorDialog.close()
                                        }
                                    ],
                                    canClose: false
                                })
                                errorDialog.open()
                                return
                            }
                        }
                        // If recovery was cancelled, continue the loop to show
                        // the passphrase dialog again.
                        continue
                    } else {
                        // User cancelled the passphrase dialog — break out of
                        // the loop and fall through to other password sources.
                        break
                    }
                }
            }
        }

        // --- Try sessionStorage password ---
        const sessionPassword = _e2ee_key_manager__rspack_import_3.E2EEKeyManager.getPasswordFromSession(docId)
        if (sessionPassword) {
            try {
                await tryPassword(sessionPassword)
                return
            } catch (_error) {
                _e2ee_key_manager__rspack_import_3.E2EEKeyManager.clearPasswordFromSession(docId)
                _e2ee_key_manager__rspack_import_3.E2EEKeyManager.clearKeyFromSession(docId)
            }
        }

        // --- Try URL fragment password ---
        if (urlFragmentPassword) {
            try {
                await tryPassword(urlFragmentPassword)
                return
            } catch (_error) {
                // URL password didn't work — fall through to prompt
            }
        }

        // --- Try existing key (e.g. from _createE2EEDocument) ---
        const existingKey = this.mod.editor.e2ee?.key
        if (existingKey) {
            try {
                await this._decryptAndLoadDoc(
                    doc,
                    existingKey,
                    salt,
                    iterations,
                    isInitialLoad,
                    urlFragmentPassword
                )
            } catch (error) {
                console.error(
                    "E2EE: Decryption failed with existing key",
                    error
                )
                if (this.mod.editor.e2ee) {
                    this.mod.editor.e2ee.key = null
                }
                await this._promptPasswordAndDecrypt(
                    doc,
                    doc_info,
                    salt,
                    iterations,
                    isInitialLoad,
                    urlFragmentPassword
                )
            }
            return
        }

        // No key available — prompt for the password
        await this._promptPasswordAndDecrypt(
            doc,
            doc_info,
            salt,
            iterations,
            isInitialLoad,
            urlFragmentPassword
        )
    }

    /**
     * Prompt for a password, derive the key, and decrypt/load the document.
     * @private
     */
    async _promptPasswordAndDecrypt(
        doc,
        doc_info,
        salt,
        iterations,
        isInitialLoad,
        urlFragmentPassword
    ) {
        // Decode the salt from Base64 to Uint8Array
        let saltBytes = null
        if (salt) {
            const binary = atob(salt)
            saltBytes = new Uint8Array(binary.length)
            for (let i = 0; i < binary.length; i++) {
                saltBytes[i] = binary.charCodeAt(i)
            }
        }

        const goBack = () => {
            const folderPath = this.mod.editor.docInfo.path.slice(
                0,
                this.mod.editor.docInfo.path.lastIndexOf("/")
            )
            if (this.mod.editor.app && this.mod.editor.app.goTo) {
                if (!folderPath.length) {
                    this.mod.editor.app.goTo("/")
                } else {
                    this.mod.editor.app.goTo(`/documents${folderPath}/`)
                }
            } else {
                window.location.href = "/"
            }
        }

        await (0,_e2ee_password_dialog__rspack_import_6.enterPasswordDialog)(
            async password => {
                try {
                    const key = await _e2ee_key_manager__rspack_import_3.E2EEKeyManager.resolvePasswordToKey(
                        password,
                        saltBytes,
                        iterations || 600000
                    )
                    await this._decryptAndLoadDoc(
                        doc,
                        key,
                        salt,
                        iterations,
                        isInitialLoad,
                        urlFragmentPassword
                    )
                    // Cache password and key for this session
                    _e2ee_key_manager__rspack_import_3.E2EEKeyManager.storePasswordInSession(
                        this.mod.editor.docInfo.id,
                        password
                    )
                    await _e2ee_key_manager__rspack_import_3.E2EEKeyManager.storeKeyInSession(
                        this.mod.editor.docInfo.id,
                        key
                    )
                    if (this.mod.editor.e2ee) {
                        this.mod.editor.e2ee.key = key
                        this.mod.editor.e2ee.password = password
                    }
                    // Migrate per-document-password docs to passphrase system
                    if (_e2ee_passphrase_manager__rspack_import_5.PassphraseManager.hasKeysInSession()) {
                        try {
                            const existing =
                                await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.getDocumentPassword(
                                    this.mod.editor.docInfo.id
                                )
                            if (!existing) {
                                await _e2ee_passphrase_manager__rspack_import_5.PassphraseManager.saveDocumentPassword(
                                    this.mod.editor.docInfo.id,
                                    password,
                                    null,
                                    "user",
                                    true
                                )
                            }
                        } catch (_e) {
                            console.error(
                                "E2EE: Failed to save document password for passphrase:",
                                _e
                            )
                        }
                    }
                } catch (_error) {
                    console.error("E2EE DECRYPT ERROR:", _error)
                    const errorDialog = new _common__rspack_import_0.Dialog({
                        title: gettext("Decryption Failed"),
                        id: "e2ee-decryption-failed",
                        body: gettext(
                            "That password didn't work. Please try again."
                        ),
                        buttons: [
                            {
                                text: gettext("Retry"),
                                classes: "fw-dark",
                                click: () => {
                                    this._promptPasswordAndDecrypt(
                                        doc,
                                        doc_info,
                                        salt,
                                        iterations,
                                        isInitialLoad,
                                        ""
                                    )
                                }
                            },
                            {
                                text: gettext("Cancel"),
                                classes: "fw-light",
                                click: () => {
                                    errorDialog.close()
                                    goBack()
                                }
                            }
                        ],
                        canClose: false
                    })
                    errorDialog.open()
                }
            },
            urlFragmentPassword,
            goBack
        )
    }

    /**
     * Decrypt document content/comments/bibliography with the given key
     * and load into ProseMirror.
     *
     * For a newly created E2EE document, no encrypted snapshot has been
     * saved yet, so the content/comments/bibliography are still plaintext
     * JSON objects (the template content). We detect this by checking
     * whether the value is a string (encrypted = Base64 ciphertext) or
     * an object (plaintext).
     *
     * @private
     */
    async _decryptAndLoadDoc(
        doc,
        key,
        salt,
        iterations,
        isInitialLoad,
        urlFragmentPassword
    ) {
        let decryptedContent = doc.content
        if (typeof doc.content === "string") {
            decryptedContent = await _e2ee_encryptor__rspack_import_2.E2EEEncryptor.decryptObject(
                doc.content,
                key
            )
        }

        let decryptedComments = doc.comments
        if (typeof doc.comments === "string") {
            decryptedComments = await _e2ee_encryptor__rspack_import_2.E2EEEncryptor.decryptObject(
                doc.comments,
                key
            )
        }

        let decryptedBibliography = doc.bibliography
        if (typeof doc.bibliography === "string") {
            decryptedBibliography = await _e2ee_encryptor__rspack_import_2.E2EEEncryptor.decryptObject(
                doc.bibliography,
                key
            )
        }

        // Store the E2EE state on the editor
        this.mod.editor.e2ee = {
            encrypted: true,
            encryptionSalt: salt,
            encryptionIterations: iterations || 600000,
            key: key,
            snapshotManager: this.mod.editor.e2ee?.snapshotManager || null
        }
        // Initialize snapshot manager now that we have the key
        if (!this.mod.editor.e2ee.snapshotManager) {
            const {E2EESnapshotManager} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/snapshot-manager.js"))
            this.mod.editor.e2ee.snapshotManager = new E2EESnapshotManager(
                this.mod.editor
            )
        }
        this.mod.editor.e2ee.snapshotManager.setKey(key)

        // Cache the key in sessionStorage so the user doesn't have to
        // re-enter the password when reopening the document this session.
        try {
            await _e2ee_key_manager__rspack_import_3.E2EEKeyManager.storeKeyInSession(
                this.mod.editor.docInfo.id,
                key
            )
        } catch (_e) {
            // If the key is non-extractable, we can't store it.
        }

        // Remove the password from the URL fragment to avoid
        // leaving it in the address bar or browser history.
        if (urlFragmentPassword && window.history.replaceState) {
            const hash = window.location.hash // "#title?password=fish&targetting=true"
            const [anchor, queryString] = hash.split("?") // ["#title", "password=fish&targetting=true"]

            const params = new URLSearchParams(queryString || "")
            params.delete("password") // Removes "password" if it exists

            const newQueryString = params.toString()
            const newHash = newQueryString
                ? `${anchor}?${newQueryString}`
                : anchor

            window.history.replaceState(null, "", newHash)
        }

        // Decrypt image copyright metadata for E2EE documents.
        // The server stores it as an opaque encrypted string; we decrypt
        // it here so the image DB receives plaintext objects.
        let decryptedImages = doc.images
        if (doc.images) {
            decryptedImages = {}
            const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))
            for (const [id, image] of Object.entries(doc.images)) {
                decryptedImages[id] = image
                if (typeof image.copyright === "string") {
                    try {
                        decryptedImages[id].copyright =
                            await E2EEEncryptor.decryptObject(
                                image.copyright,
                                key
                            )
                    } catch (_e) {
                        // If decryption fails, leave as-is (legacy plaintext)
                    }
                }
            }
        }

        // Now load the decrypted document using the standard path
        const decryptedDoc = {
            ...doc,
            content: decryptedContent,
            comments: decryptedComments,
            bibliography: decryptedBibliography,
            images: decryptedImages
        }
        this._loadUnencryptedDocument(decryptedDoc, isInitialLoad)

        // Cache the decrypted title in sessionStorage so the document overview
        // can display the real title without prompting for the password again.
        let title = ""
        this.mod.editor.view.state.doc.firstChild.forEach(child => {
            if (!child.marks.find(mark => mark.type.name === "deletion")) {
                title += child.textContent
            }
        })
        sessionStorage.setItem(
            `e2ee_title_${this.mod.editor.docInfo.id}`,
            title
        )

        // For newly created E2EE documents, the initial content is still
        // plaintext (it came from the template). We need to send an initial
        // encrypted snapshot so the server stores encrypted content.
        // We detect a new document by checking if the content was plaintext
        // (not a Base64 string) and this is the initial load.
        const isNewE2EEDocument =
            isInitialLoad && typeof doc.content !== "string"
        if (isNewE2EEDocument && this.mod.editor.e2ee.snapshotManager) {
            this.mod.editor.e2ee.snapshotManager.sendInitialSnapshot(
                decryptedContent,
                decryptedComments,
                decryptedBibliography,
                this.mod.editor.docInfo.version
            )
        }

        // Now that the key is available and the document is loaded, open the
        // WebSocket connection. This ensures encrypted catch-up diffs (ep)
        // from the server can be decrypted as soon as they arrive.
        if (this.mod.editor.ws && !this.mod.editor.ws.connected) {
            this.mod.editor.startWebSocket()
        } else if (!this.mod.editor.ws) {
            // Non-collaborative mode: mark version as confirmed so the editor
            // finishes loading and periodic saves can begin.
            this.mod.editor.mod.collab.doc.confirmVersion(
                this.mod.editor.docInfo.version
            )
        }
    }

    /**
     * Load an unencrypted (or already decrypted) document into ProseMirror.
     * This is the original loadDocument logic, extracted for reuse.
     * @private
     */
    _loadUnencryptedDocument(doc, isInitialLoad) {
        // Remember location hash to scroll there subsequently.
        const [locationHash, _queryString] = window.location.hash.split("?")
        this.mod.editor.mod.db.bibDB.setDB(doc.bibliography)
        this.mod.editor.mod.db.imageDB.setDB(doc.images)
        const stateDoc = this.mod.editor.schema.nodeFromJSON(doc.content)
        const plugins = this.mod.editor.statePlugins.map(plugin => {
            if (plugin[1]) {
                return plugin[0](plugin[1](doc))
            } else {
                return plugin[0]()
            }
        })

        const stateConfig = {
            schema: this.mod.editor.schema,
            doc: stateDoc,
            plugins
        }

        // Set document in prosemirror
        this.mod.editor.view.setProps({state: prosemirror_state__rspack_import_9.EditorState.create(stateConfig)})
        this.mod.editor.view.setProps({nodeViews: {}}) // Needed to initialize nodeViews in plugins
        // Set initial confirmed doc
        this.mod.editor.docInfo.confirmedDoc = this.mod.editor.view.state.doc

        // Render footnotes based on main doc
        this.mod.editor.mod.footnotes.fnEditor.renderAllFootnotes()

        //  Setup comment handling
        this.mod.editor.mod.comments.store.reset()
        this.mod.editor.mod.comments.store.loadComments(doc.comments)
        this.mod.editor.mod.marginboxes.view(this.mod.editor.view)
        if (locationHash && locationHash.length) {
            this.mod.editor.scrollIdIntoView(locationHash.slice(1))
        }
        // Update the header bar to reflect the loaded document's title.
        // This is needed because setStyles() (which calls headerView.update())
        // runs before loadDocument(), so the header still shows "Untitled"
        // from the empty initial document.
        if (this.mod.editor.menu.headerView) {
            this.mod.editor.menu.headerView.update()
        }
        if (isInitialLoad) {
            this.initialDocLoaded = true
        } else {
            (0,_common__rspack_import_0.deactivateWait)()
            this.mod.editor.waitingForDocument = false
        }
        if (doc.template) {
            // We received the template. That means we are the first user present with write access.
            // We will adjust the document to the template if necessary.
            // For E2EE documents, the content has already been decrypted
            // by this point, so template adjustment works normally.
            if (isInitialLoad) {
                this.templateAdjustmentPending = true
            }
            (0,_common__rspack_import_0.activateWait)(true, gettext("Updating document. Please wait..."))
            const activateWaitTimer = setTimeout(() => {
                ;(0,_common__rspack_import_0.activateWait)(
                    true,
                    gettext(
                        "It's taking a bit longer than usual, but it should be ready soon. Please wait..."
                    )
                )
            }, 60000)
            const adjustWorker = (0,_common__rspack_import_0.makeWorker)(
                staticUrl("js/adjust_doc_to_template_worker.js")
            )
            adjustWorker.onmessage = message => {
                if (message.data.type === "result") {
                    if (message.data.steps.length) {
                        const tr = this.mod.editor.view.state.tr
                        message.data.steps.forEach(step =>
                            tr.step(prosemirror_transform__rspack_import_10.Step.fromJSON(this.mod.editor.schema, step))
                        )
                        tr.setMeta("remote", true)
                        this.mod.editor.view.dispatch(tr)
                    }
                    // clearing timer for updating message since operation is completed
                    clearTimeout(activateWaitTimer)
                    if (isInitialLoad) {
                        this.templateAdjustmentPending = false
                        this.finishInitialLoad()
                    } else {
                        (0,_common__rspack_import_0.deactivateWait)()
                    }
                    this.setDocSettings()
                }
            }
            adjustWorker.onerror = error => console.error(error)
            const schemaExporter = new _schema_export__rspack_import_1.SchemaExport()
            adjustWorker.postMessage({
                schemaSpec: JSON.parse(schemaExporter.init()),
                doc: doc.content,
                template: doc.template.content,
                documentStyleSlugs:
                    this.mod.editor.mod.documentTemplate.documentStyles.map(
                        style => style.slug
                    )
            })
        } else {
            this.setDocSettings()
            if (isInitialLoad) {
                this.finishInitialLoad()
            }
        }
    }

    setDocSettings() {
        // Set part specific settings
        this.mod.editor.mod.documentTemplate.addDocPartSettings()
        this.mod.editor.mod.documentTemplate.addCitationStylesMenuEntries()
    }

    sendToCollaborators() {
        // Guard: WebSocket may not be initialized yet (e.g. during
        // loadDocument before the WS connector is created).
        if (!this.mod.editor.ws) {
            return
        }

        // For E2EE documents, we need to encrypt diffs before sending.
        // Since encryption is async, we use a separate code path.
        const isE2EE =
            this.mod.editor.e2ee &&
            this.mod.editor.e2ee.encrypted &&
            this.mod.editor.e2ee.key

        if (isE2EE) {
            this._sendE2EEDiff()
            return
        }

        // Handle either doc change and comment updates OR caret update. Priority
        // for doc change/comment update.
        this.mod.editor.ws.send(() => {
            if (
                this.awaitingDiffResponse ||
                this.mod.editor.waitingForDocument ||
                this.receiving
            ) {
                return false
            } else if (
                (0,prosemirror_collab__rspack_import_11.sendableSteps)(this.mod.editor.view.state) ||
                this.mod.editor.mod.comments.store.unsentEvents().length ||
                this.mod.editor.mod.db.bibDB.unsentEvents().length ||
                this.mod.editor.mod.db.imageDB.unsentEvents().length
            ) {
                this.disableDiffSending()
                const stepsToSend = (0,prosemirror_collab__rspack_import_11.sendableSteps)(this.mod.editor.view.state),
                    fnStepsToSend = (0,prosemirror_collab__rspack_import_11.sendableSteps)(
                        this.mod.editor.mod.footnotes.fnEditor.view.state
                    ),
                    commentUpdates =
                        this.mod.editor.mod.comments.store.unsentEvents(),
                    bibliographyUpdates =
                        this.mod.editor.mod.db.bibDB.unsentEvents(),
                    imageUpdates = this.mod.editor.mod.db.imageDB.unsentEvents()

                if (
                    !stepsToSend &&
                    !fnStepsToSend &&
                    !commentUpdates.length &&
                    !bibliographyUpdates.length &&
                    !imageUpdates.length
                ) {
                    // no diff. abandon operation
                    return
                }
                const rid = this.confirmStepsRequestCounter++,
                    unconfirmedDiff = {
                        type: "diff",
                        v: this.mod.editor.docInfo.version,
                        rid
                    }

                unconfirmedDiff["cid"] = this.mod.editor.client_id

                if (stepsToSend) {
                    unconfirmedDiff["ds"] = stepsToSend.steps.map(s =>
                        s.toJSON()
                    )
                    // In case the title changed, we also add a title field to
                    // update the title field instantly - important for the
                    // document overview page.
                    let newTitle = ""
                    this.mod.editor.view.state.doc.firstChild.forEach(child => {
                        if (
                            !child.marks.find(
                                mark => mark.type.name === "deletion"
                            )
                        ) {
                            newTitle += child.textContent
                        }
                    })
                    newTitle = newTitle.slice(0, 255)
                    let oldTitle = ""
                    this.mod.editor.docInfo.confirmedDoc.firstChild.forEach(
                        child => {
                            if (
                                !child.marks.find(
                                    mark => mark.type.name === "deletion"
                                )
                            ) {
                                oldTitle += child.textContent
                            }
                        }
                    )
                    oldTitle = oldTitle.slice(0, 255)
                    if (newTitle !== oldTitle) {
                        unconfirmedDiff["ti"] = newTitle
                    }
                }

                if (fnStepsToSend) {
                    // We add the client ID to every single step
                    unconfirmedDiff["fs"] = fnStepsToSend.steps.map(s =>
                        s.toJSON()
                    )
                }
                if (this.footnoteRender) {
                    unconfirmedDiff["footnoterender"] = true
                    this.footnoteRender = false
                }
                if (commentUpdates.length) {
                    unconfirmedDiff["cu"] = commentUpdates
                }
                if (bibliographyUpdates.length) {
                    unconfirmedDiff["bu"] = bibliographyUpdates
                }
                if (imageUpdates.length) {
                    unconfirmedDiff["iu"] = imageUpdates
                }

                this.unconfirmedDiffs[rid] = Object.assign(
                    {doc: this.mod.editor.view.state.doc},
                    unconfirmedDiff
                )
                return unconfirmedDiff
            } else if (
                this.mod.editor.currentView?.state &&
                (0,_state_plugins__rspack_import_7.getSelectionUpdate)(this.mod.editor.currentView.state)
            ) {
                const currentView = this.mod.editor.currentView

                if (this.lastSelectionUpdateState === currentView.state) {
                    // Selection update has been sent for this state already. Skip
                    return false
                }
                this.lastSelectionUpdateState = currentView.state
                // Create a new caret as the current user
                const selectionUpdate = (0,_state_plugins__rspack_import_7.getSelectionUpdate)(currentView.state)
                return {
                    type: "selection_change",
                    id: this.mod.editor.user.id,
                    v: this.mod.editor.docInfo.version,
                    session_id: this.mod.editor.docInfo.session_id,
                    anchor: selectionUpdate.anchor,
                    head: selectionUpdate.head,
                    // Whether the selection is in the footnote or the main editor
                    editor:
                        currentView === this.mod.editor.view
                            ? "main"
                            : "footnotes"
                }
            } else {
                return false
            }
        })
    }

    /**
     * Send an encrypted diff for an E2EE document.
     *
     * For E2EE documents, the diff payload (steps, comments, bibliography,
     * image updates) is encrypted as a single blob before sending. The
     * server relays the encrypted diff to other clients without being able
     * to read the content.
     *
     * The unencrypted diff is stored locally in `unconfirmedDiffs` for
     * confirmation/rejection handling. Only the wire format is encrypted.
     *
     * @private
     */
    async _sendE2EEDiff() {
        // Check if there's anything to send
        if (
            this.awaitingDiffResponse ||
            this.mod.editor.waitingForDocument ||
            this.receiving
        ) {
            // No diff to send, but check for selection update.
            // Guard: the editor view may not be initialized yet
            // (e.g. during loadDocument before ProseMirror is set up).
            if (this.mod.editor.view) {
                this._sendE2EESelectionChange()
            }
            return
        }

        if (
            !(0,prosemirror_collab__rspack_import_11.sendableSteps)(this.mod.editor.view.state) &&
            !this.mod.editor.mod.comments.store.unsentEvents().length &&
            !this.mod.editor.mod.db.bibDB.unsentEvents().length &&
            !this.mod.editor.mod.db.imageDB.unsentEvents().length
        ) {
            // No diff, check for selection update
            if (this.mod.editor.view) {
                this._sendE2EESelectionChange()
            }
            return
        }

        this.disableDiffSending()

        const stepsToSend = (0,prosemirror_collab__rspack_import_11.sendableSteps)(this.mod.editor.view.state),
            fnStepsToSend = (0,prosemirror_collab__rspack_import_11.sendableSteps)(
                this.mod.editor.mod.footnotes.fnEditor.view.state
            ),
            commentUpdates = this.mod.editor.mod.comments.store.unsentEvents(),
            bibliographyUpdates = this.mod.editor.mod.db.bibDB.unsentEvents(),
            imageUpdates = this.mod.editor.mod.db.imageDB.unsentEvents()

        if (
            !stepsToSend &&
            !fnStepsToSend &&
            !commentUpdates.length &&
            !bibliographyUpdates.length &&
            !imageUpdates.length
        ) {
            // no diff. abandon operation
            this.enableDiffSending()
            return
        }

        const rid = this.confirmStepsRequestCounter++,
            unconfirmedDiff = {
                type: "diff",
                v: this.mod.editor.docInfo.version,
                rid
            }

        unconfirmedDiff["cid"] = this.mod.editor.client_id

        // Collect the payload fields that need encryption
        const encryptedPayload = {}

        if (stepsToSend) {
            encryptedPayload["ds"] = stepsToSend.steps.map(s => s.toJSON())
            // Track title changes for the document overview
            let newTitle = ""
            this.mod.editor.view.state.doc.firstChild.forEach(child => {
                if (!child.marks.find(mark => mark.type.name === "deletion")) {
                    newTitle += child.textContent
                }
            })
            newTitle = newTitle.slice(0, 255)
            let oldTitle = ""
            this.mod.editor.docInfo.confirmedDoc.firstChild.forEach(child => {
                if (!child.marks.find(mark => mark.type.name === "deletion")) {
                    oldTitle += child.textContent
                }
            })
            oldTitle = oldTitle.slice(0, 255)
            if (newTitle !== oldTitle) {
                // For E2EE documents, the title is encrypted too.
                // We don't send "ti" in plaintext; it goes in the
                // encrypted payload.
                encryptedPayload["ti"] = newTitle
            }
        }

        if (fnStepsToSend) {
            encryptedPayload["fs"] = fnStepsToSend.steps.map(s => s.toJSON())
        }
        if (this.footnoteRender) {
            unconfirmedDiff["footnoterender"] = true
            this.footnoteRender = false
        }
        if (commentUpdates.length) {
            encryptedPayload["cu"] = commentUpdates
        }
        if (bibliographyUpdates.length) {
            encryptedPayload["bu"] = bibliographyUpdates
        }
        if (imageUpdates.length) {
            encryptedPayload["iu"] = imageUpdates
        }

        // Store the unencrypted diff locally for confirmation handling.
        // We keep the plaintext steps so confirmDiff can apply them.
        this.unconfirmedDiffs[rid] = Object.assign(
            {doc: this.mod.editor.view.state.doc},
            unconfirmedDiff,
            encryptedPayload
        )

        try {
            // Encrypt the payload fields as a single blob
            const key = this.mod.editor.e2ee.key
            const ep = await _e2ee_encryptor__rspack_import_2.E2EEEncryptor.encryptObject(encryptedPayload, key)

            // Build the wire-format diff: metadata in plaintext,
            // payload encrypted
            const wireDiff = {
                type: "diff",
                v: unconfirmedDiff.v,
                rid: unconfirmedDiff.rid,
                cid: unconfirmedDiff.cid,
                ep, // encrypted payload
                e2ee_salt: this.mod.editor.e2ee.encryptionSalt
            }
            if (unconfirmedDiff.footnoterender) {
                wireDiff.footnoterender = true
            }

            // Send the encrypted diff
            this.mod.editor.ws.send(() => wireDiff)
        } catch (error) {
            console.error("E2EE: Failed to encrypt diff", error)
            // Re-enable diff sending so we can try again
            this.enableDiffSending()
        }
    }

    /**
     * Send a selection change for an E2EE document.
     * Selection changes are not encrypted (they only contain cursor position).
     *
     * @private
     */
    _sendE2EESelectionChange() {
        if (
            !this.mod.editor.currentView ||
            !this.mod.editor.currentView.state
        ) {
            return
        }
        if (!(0,_state_plugins__rspack_import_7.getSelectionUpdate)(this.mod.editor.currentView.state)) {
            return
        }
        const currentView = this.mod.editor.currentView
        if (this.lastSelectionUpdateState === currentView.state) {
            return
        }
        this.lastSelectionUpdateState = currentView.state
        const selectionUpdate = (0,_state_plugins__rspack_import_7.getSelectionUpdate)(currentView.state)
        this.mod.editor.ws.send(() => ({
            type: "selection_change",
            id: this.mod.editor.user.id,
            v: this.mod.editor.docInfo.version,
            session_id: this.mod.editor.docInfo.session_id,
            anchor: selectionUpdate.anchor,
            head: selectionUpdate.head,
            editor: currentView === this.mod.editor.view ? "main" : "footnotes"
        }))
    }

    receiveSelectionChange(data) {
        const participant = this.mod.participants.find(
            par => par.id === data.id
        )
        let tr, fnTr
        if (!participant) {
            // participant is still unknown to us. Ignore
            return
        }
        if (data.editor === "footnotes") {
            fnTr = (0,_state_plugins__rspack_import_7.updateCollaboratorSelection)(
                this.mod.editor.mod.footnotes.fnEditor.view.state,
                participant,
                data
            )
            tr = (0,_state_plugins__rspack_import_7.removeCollaboratorSelection)(this.mod.editor.view.state, data)
        } else {
            tr = (0,_state_plugins__rspack_import_7.updateCollaboratorSelection)(
                this.mod.editor.view.state,
                participant,
                data
            )
            fnTr = (0,_state_plugins__rspack_import_7.removeCollaboratorSelection)(
                this.mod.editor.mod.footnotes.fnEditor.view.state,
                data
            )
        }
        if (tr) {
            this.mod.editor.view.dispatch(tr)
        }
        if (fnTr) {
            this.mod.editor.mod.footnotes.fnEditor.view.dispatch(fnTr)
        }
    }

    receiveDiff(data, serverFix = false) {
        // Check if this is an encrypted diff for an E2EE document.
        // Encrypted diffs have an "ep" (encrypted payload) field instead
        // of plaintext ds/fs/cu/bu/iu fields. We decrypt the payload
        // first, then process the diff normally.
        if (data["ep"] && this.mod.editor.e2ee && this.mod.editor.e2ee.key) {
            this._receiveE2EEDiff(data, serverFix)
            return
        }
        this._processDiff(data, serverFix)
    }

    /**
     * Process a diff (unencrypted or already decrypted).
     * @private
     */
    _processDiff(data, serverFix = false) {
        this.mod.editor.docInfo.version++
        if (data["bu"]) {
            // bibliography updates
            this.mod.editor.mod.db.bibDB.receive(data["bu"])
        }
        if (data["iu"]) {
            // images updates
            this.mod.editor.mod.db.imageDB.receive(data["iu"])
        }
        if (data["cu"]) {
            // comment updates
            this.mod.editor.mod.comments.store.receive(data["cu"])
        }
        if (data["ds"]) {
            // document steps
            this.applyDiffs(data["ds"], data["cid"])
        }
        if (data["fs"]) {
            // footnote steps
            this.mod.editor.mod.footnotes.fnEditor.applyDiffs(
                data["fs"],
                data["cid"]
            )
        }
        if (data["footnoterender"]) {
            // re-render footnotes properly
            this.mod.editor.mod.footnotes.fnEditor.renderAllFootnotes()
        }

        if (serverFix) {
            // Diff is a fix created by server due to missing diffs.
            if ("reject_request_id" in data) {
                delete this.unconfirmedDiffs[data.reject_request_id]
            }
            this.cancelCurrentlyCheckingVersion()

            // There may be unsent local changes. Send them now after .5 seconds,
            // in case collaborators want to send something first.
            this.enableDiffSending()
            window.setTimeout(() => this.sendToCollaborators(), 500)
        }
    }

    /**
     * Receive and decrypt an E2EE diff from another client.
     *
     * For E2EE documents, diffs arrive with an "ep" (encrypted payload)
     * field containing the encrypted steps, comments, bibliography, and
     * image updates. This method decrypts the payload and then processes
     * the diff using the standard _processDiff path.
     *
     * @param {Object} data - The diff message with "ep" field
     * @param {boolean} serverFix - Whether this is a server-generated fix
     * @private
     */
    async _receiveE2EEDiff(data, serverFix = false) {
        try {
            const key = this.mod.editor.e2ee.key
            const decryptedPayload = await _e2ee_encryptor__rspack_import_2.E2EEEncryptor.decryptObject(
                data["ep"],
                key
            )

            // Merge the decrypted payload with the metadata fields
            // that were sent in plaintext (cid, rid, footnoterender)
            const mergedData = {
                cid: data["cid"],
                rid: data["rid"],
                footnoterender: data["footnoterender"],
                reject_request_id: data["reject_request_id"],
                ...decryptedPayload
            }

            // Process the decrypted diff normally
            this._processDiff(mergedData, serverFix)
        } catch (error) {
            console.error("E2EE: Failed to decrypt incoming diff", error)
            // If decryption fails, we still need to increment the version
            // to stay in sync with the server, but we skip applying the
            // diff content.
            this.mod.editor.docInfo.version++
        }
    }

    setConfirmedDoc(tr, stepsLength) {
        // Find the latest version of the doc without any unconfirmed local changes

        const rebased = tr.getMeta("rebased"),
            docNumber = rebased + stepsLength

        this.mod.editor.docInfo.confirmedDoc =
            docNumber === tr.docs.length ? tr.doc : tr.docs[docNumber]
    }

    confirmDiff(request_id) {
        const unconfirmedDiffs = this.unconfirmedDiffs[request_id]
        if (!unconfirmedDiffs) {
            return
        }
        this.mod.editor.docInfo.version++

        const sentSteps = unconfirmedDiffs["ds"] // document steps
        if (sentSteps) {
            const ourIds = sentSteps.map(_step => this.mod.editor.client_id)
            const tr = (0,prosemirror_collab__rspack_import_11.receiveTransaction)(
                this.mod.editor.view.state,
                sentSteps,
                ourIds
            )
            this.mod.editor.view.dispatch(tr)
            this.mod.editor.docInfo.confirmedDoc = unconfirmedDiffs["doc"]
        }

        const sentFnSteps = unconfirmedDiffs["fs"] // footnote steps
        if (sentFnSteps) {
            const fnTr = (0,prosemirror_collab__rspack_import_11.receiveTransaction)(
                this.mod.editor.mod.footnotes.fnEditor.view.state,
                sentFnSteps,
                sentFnSteps.map(_step => this.mod.editor.client_id)
            )
            this.mod.editor.mod.footnotes.fnEditor.view.dispatch(fnTr)
        }

        const sentComments = unconfirmedDiffs["cu"] // comment updates
        if (sentComments) {
            this.mod.editor.mod.comments.store.eventsSent(sentComments)
        }

        const sentBibliographyUpdates = unconfirmedDiffs["bu"] // bibliography updates
        if (sentBibliographyUpdates) {
            this.mod.editor.mod.db.bibDB.eventsSent(sentBibliographyUpdates)
        }

        const sentImageUpdates = unconfirmedDiffs["iu"] // image updates
        if (sentImageUpdates) {
            this.mod.editor.mod.db.imageDB.eventsSent(sentImageUpdates)
        }

        delete this.unconfirmedDiffs[request_id]
        this.enableDiffSending()
    }

    rejectDiff(request_id) {
        delete this.unconfirmedDiffs[request_id]
        this.enableDiffSending()
    }

    applyDiffs(diffs, cid) {
        this.receiving = true
        const steps = diffs.map(j => prosemirror_transform__rspack_import_10.Step.fromJSON(this.mod.editor.schema, j))
        const clientIds = diffs.map(_ => cid)
        const tr = (0,prosemirror_collab__rspack_import_11.receiveTransaction)(
            this.mod.editor.view.state,
            steps,
            clientIds
        )
        tr.setMeta("remote", true)
        this.mod.editor.view.dispatch(tr)
        this.setConfirmedDoc(tr, steps.length)
        this.receiving = false
        this.sendToCollaborators()
    }
}


}),
"./js/modules/editor/collab/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCollab: function() { return ModCollab; }
});
/* import */ var _state_plugins__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _chat__rspack_import_1 = __webpack_require__("./js/modules/editor/collab/chat.js");
/* import */ var _colors__rspack_import_2 = __webpack_require__("./js/modules/editor/collab/colors.js");
/* import */ var _doc__rspack_import_3 = __webpack_require__("./js/modules/editor/collab/doc.js");





class ModCollab {
    constructor(editor) {
        editor.mod.collab = this
        this.editor = editor
        this.participants = []
        this.pastParticipants = [] // Participants who have left comments or tracked changes.
        this.sessionIds = false
        this.collaborativeMode = false

        new _doc__rspack_import_3.ModCollabDoc(this)
        new _chat__rspack_import_1.ModCollabChat(this)
        new _colors__rspack_import_2.ModCollabColors(this)
    }

    updateParticipantList(participantArray) {
        const allSessionIds = [],
            participantObj = {}

        participantArray.forEach(participant => {
            const entry = participantObj[participant.id]
            allSessionIds.push(participant.session_id)
            if (entry) {
                entry.sessionIds.push(participant.session_id)
            } else {
                participant.sessionIds = [participant.session_id]
                delete participant.session_id
                participantObj[participant.id] = participant
            }
        })

        this.participants = Object.values(participantObj)
        if (!this.sessionIds) {
            if (allSessionIds.length === 1) {
                // We just connected to the editor and we are the only connected
                // party. This is a good time to clean up the databases, removing
                // unused images and bibliography items.
                if (this.editor.mod.db) {
                    this.editor.mod.db.clean()
                }
            }
            this.sessionIds = []
        }
        // Check if each of the old session IDs is still present in last update.
        // If not, remove the corresponding carets if any.
        this.sessionIds.forEach(session_id => {
            if (!allSessionIds.includes(session_id)) {
                const tr = (0,_state_plugins__rspack_import_0.removeCollaboratorSelection)(this.editor.view.state, {
                    session_id
                })
                const fnTr = (0,_state_plugins__rspack_import_0.removeCollaboratorSelection)(
                    this.editor.mod.footnotes.fnEditor.view.state,
                    {session_id}
                )
                if (tr) {
                    this.editor.view.dispatch(tr)
                }
                if (fnTr) {
                    this.editor.mod.footnotes.fnEditor.view.dispatch(fnTr)
                }
            }
        })
        this.sessionIds = allSessionIds
        if (participantArray.length > 1) {
            this.collaborativeMode = true
        } else if (participantArray.length === 1) {
            this.collaborativeMode = false
        }
        this.participants.forEach(participant => {
            this.colors.ensureUserColor(participant.id)
        })
        if (this.editor.menu.headerView) {
            this.editor.menu.headerView.update()
        }
        this.chat.showChat(participantArray)
    }
}


}),
"./js/modules/editor/collab/merge/changeset.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  changeSet: function() { return changeSet; }
});
/* import */ var prosemirror_changeset__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-changeset@2.2.1/node_modules/prosemirror-changeset/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");



class changeSet {
    constructor(tr) {
        this.tr = tr
    }

    trDoc(tr, index = 0) {
        return tr.docs.length > index ? tr.docs[index] : tr.doc
    }

    findConflicts(tr1, tr2) {
        const conflicts = []
        const changes1 = this.findContentChanges(tr1)
        const changes2 = this.findContentChanges(tr2)
        changes1.deletedsteps.forEach(deleted => {
            changes2.insertedsteps.forEach(inserted => {
                if (
                    inserted.pos >= deleted.from &&
                    inserted.pos <= deleted.to
                ) {
                    conflicts.push([
                        deleted.data.step,
                        "deletion",
                        inserted.data.step,
                        "insertion"
                    ])
                }
            })
        })

        changes2.deletedsteps.forEach(deleted => {
            changes1.insertedsteps.forEach(inserted => {
                if (
                    inserted.pos >= deleted.from &&
                    inserted.pos <= deleted.to
                ) {
                    conflicts.push([
                        inserted.data.step,
                        "insertion",
                        deleted.data.step,
                        "deletion"
                    ])
                }
            })
        })
        return conflicts
    }

    findContentChanges(tr) {
        const doc = this.trDoc(tr)
        let changes = prosemirror_changeset__rspack_import_0.ChangeSet.create(doc)
        tr.steps.forEach((_step, index) => {
            const doc = this.trDoc(tr, index + 1)
            changes = changes.addSteps(doc, [tr.mapping.maps[index]], {
                step: index
            })
        })
        const invertedMapping = new prosemirror_transform__rspack_import_1.Mapping()
        invertedMapping.appendMappingInverted(tr.mapping)

        const insertedsteps = [],
            deletedsteps = [],
            ins = [],
            del = []
        changes.changes.forEach(change => {
            change.inserted.forEach(inserted => {
                if (!ins.includes(inserted.data.step)) {
                    insertedsteps.push({
                        pos: invertedMapping.map(change.fromB),
                        data: inserted.data
                    })
                    ins.push(inserted.data.step)
                }
            })
            change.deleted.forEach(deleted => {
                if (!del.includes(deleted.data.step)) {
                    del.push(deleted.data.step)
                    deletedsteps.push({
                        from: change.fromA,
                        to: change.toA,
                        data: deleted.data
                    })
                }
            })
        })
        return {insertedsteps, deletedsteps}
    }

    getChangeSet() {
        const tr = this.tr
        const doc = this.trDoc(tr)
        let changes = prosemirror_changeset__rspack_import_0.ChangeSet.create(doc)
        tr.steps.forEach((_step, index) => {
            const doc = this.trDoc(tr, index + 1)
            changes = changes.addSteps(doc, [tr.mapping.maps[index]], {
                step: index
            })
        })
        return changes
    }
}


}),
"./js/modules/editor/collab/merge/editor.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  MergeEditor: function() { return MergeEditor; }
});
/* import */ var prosemirror_collab__rspack_import_15 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_commands__rspack_import_14 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_dropcursor__rspack_import_16 = __webpack_require__("./node_modules/.pnpm/prosemirror-dropcursor@1.8.1/node_modules/prosemirror-dropcursor/dist/index.js");
/* import */ var prosemirror_example_setup__rspack_import_13 = __webpack_require__("./node_modules/.pnpm/prosemirror-example-setup@1.2.3/node_modules/prosemirror-example-setup/dist/index.js");
/* import */ var prosemirror_gapcursor__rspack_import_17 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_12 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_state__rspack_import_20 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_18 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var prosemirror_view__rspack_import_19 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _citations_render__rspack_import_0 = __webpack_require__("./js/modules/citations/render.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_i18n__rspack_import_2 = __webpack_require__("./js/modules/schema/i18n.js");
/* import */ var _keymap__rspack_import_3 = __webpack_require__("./js/modules/editor/keymap.js");
/* import */ var _state_plugins__rspack_import_4 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _track__rspack_import_5 = __webpack_require__("./js/modules/editor/track/index.js");
/* import */ var _changeset__rspack_import_6 = __webpack_require__("./js/modules/editor/collab/merge/changeset.js");
/* import */ var _footnotes__rspack_import_7 = __webpack_require__("./js/modules/editor/collab/merge/footnotes.js");
/* import */ var _recreate_transform__rspack_import_8 = __webpack_require__("./js/modules/editor/collab/merge/recreate_transform.js");
/* import */ var _schema__rspack_import_9 = __webpack_require__("./js/modules/editor/collab/merge/schema.js");
/* import */ var _state_plugins__rspack_import_10 = __webpack_require__("./js/modules/editor/collab/merge/state_plugins/index.js");
/* import */ var _tools__rspack_import_11 = __webpack_require__("./js/modules/editor/collab/merge/tools.js");






















class MergeEditor {
    constructor(editor, cpDoc, offlineDoc, onlineDoc, offlineTr, onlineTr, db) {
        this.editor = editor
        this.schema = (0,_schema__rspack_import_9.createDiffSchema)(editor.schema)
        this.cpDoc = this.schema.nodeFromJSON(cpDoc.toJSON())
        this.offlineDoc = this.schema.nodeFromJSON(offlineDoc.toJSON())
        this.onlineDoc = this.schema.nodeFromJSON(onlineDoc.toJSON())
        this.offlineTr = (0,_tools__rspack_import_11.simplifyTransform)(offlineTr) // The offline transaction
        this.onlineTr = (0,_tools__rspack_import_11.simplifyTransform)(onlineTr) // The online Transaction
        this.mergeDialog = this.createMergeDialog(
            this.offlineTr,
            this.onlineDoc
        )
        this.warningDialog = false
        this.db = db
        this.mergedDocMap = this.onlineTr.mapping // the maps of the middle editor, used for applying steps automatically

        this.mergeView1 = false
        this.mergeView2 = false
        this.mergeView3 = false
        this.imageDataModified = {} // To hold data related to re-uploaded images.

        this.offlineTrackedSteps = []

        this.diffEditorPlugins = [
            [_state_plugins__rspack_import_10.diffPlugin, () => ({merge: this})],
            [prosemirror_keymap__rspack_import_12.keymap, () => (0,_keymap__rspack_import_3.buildEditorKeymap)(this.schema)],
            [prosemirror_keymap__rspack_import_12.keymap, () => (0,prosemirror_example_setup__rspack_import_13.buildKeymap)(this.schema)],
            [prosemirror_keymap__rspack_import_12.keymap, () => prosemirror_commands__rspack_import_14.baseKeymap],
            [prosemirror_collab__rspack_import_15.collab, () => ({clientID: this.editor.client_id})],
            // [history],
            [prosemirror_dropcursor__rspack_import_16.dropCursor],
            [prosemirror_gapcursor__rspack_import_17.gapCursor],
            // [tableEditing],
            [_state_plugins__rspack_import_4.jumpHiddenNodesPlugin],
            [_state_plugins__rspack_import_4.searchPlugin],
            [_state_plugins__rspack_import_10.clipboardPlugin, () => ({editor: this.editor})]
        ]

        // assign image DB & BibDB to be used in document schema.
        this.schema.cached.bibDB = this.editor.mod.db.bibDB
        this.schema.cached.imageDB = this.editor.mod.db.imageDB
    }

    init() {
        /* Create the diff editors */
        (0,_common__rspack_import_1.ensureCSS)([staticUrl("css/merge.css")])
        // Activate wait overlay
        ;(0,_common__rspack_import_1.activateWait)()

        this.mergeDialog.open()

        // Create multiple editor views
        this.mergeView1 = this.bindEditorView("editor-diff-common", this.cpDoc)
        this.mergeView2 = this.bindEditorView(
            "editor-diff-online",
            this.onlineDoc
        )
        this.mergeView3 = this.bindEditorView(
            "editor-diff-offline",
            this.offlineDoc
        )

        const offlineChangeset = new _changeset__rspack_import_6.changeSet(this.offlineTr).getChangeSet()
        const onlineChangeset = new _changeset__rspack_import_6.changeSet(this.onlineTr).getChangeSet()

        // Unhide All Sections in All the 3 views
        this.unHideSectionsinAllDoc()

        const offlineTrackedSteps = this.markChangesinDiffEditor(
            offlineChangeset,
            this.mergeView3,
            "offline-inserted",
            this.offlineTr
        )
        this.markChangesinDiffEditor(
            onlineChangeset,
            this.mergeView2,
            "online-inserted",
            this.onlineTr
        )
        this.offlineTrackedSteps =
            this.offlineTrackedSteps.concat(offlineTrackedSteps)

        if (
            this.mergeView2.state.doc.attrs.tracked ||
            this.mergeView3.state.doc.attrs.tracked
        ) {
            this.mergeView2.dispatch(
                this.mergeView2.state.tr
                    .setDocAttribute("tracked", true)
                    .setMeta("notrack", true)
                    .setMeta("mapAppended", true)
            )
        }

        this.renderCitation(this.mergeView1, "editor-diff-common")
        this.renderCitation(this.mergeView2, "editor-diff-online")
        this.renderCitation(this.mergeView3, "editor-diff-offline")

        this.offStepsNotTracked = this.findNotTrackedSteps(
            this.offlineTr,
            this.offlineTrackedSteps
        )

        ;(0,_common__rspack_import_1.deactivateWait)()

        // Update the Bib and image DB before hand with the data from the offline document and the socket data.
        this.updateDB(this.offlineDoc, this.db) // Updating the editor DB is one-time operation.
    }

    createMergeDialog(offlineTr, onlineDoc) {
        const buttons = [
            {
                text: gettext("Merge Complete"),
                classes: "fw-dark",
                click: () => {
                    if (!this.checkResolution()) {
                        this.startMerge(offlineTr, onlineDoc)
                    } else {
                        this.warningDialog = new _common__rspack_import_1.Dialog({
                            id: "merge-res-warning",
                            title: gettext("Merge Resolution warning"),
                            body: gettext(
                                "Not all changes have been resolved. Please make sure to review all the changes to before proceeding."
                            ),
                            buttons: [
                                {
                                    text: gettext("Proceed to Merge"),
                                    classes: "fw-dark",
                                    click: () => {
                                        this.startMerge(offlineTr, onlineDoc)
                                        this.warningDialog.close()
                                    }
                                }
                            ]
                        })
                        this.warningDialog.open()
                    }
                }
            }
        ]
        const dialog = new _common__rspack_import_1.Dialog({
            id: "editor-merge-view",
            title: gettext("Merging Offline Document"),
            body: `<div style="display:flex"><div class="offline-heading">${gettext("Common Document")}</div><div class="merged-heading">${gettext("Online Document")}</div> <div class="online-heading">${gettext("Offline Document")}</div></div><div class= "user-contents" style="display:flex;"><div id="editor-diff-common" style="float:left;padding:15px;"></div><div id="editor-diff-online" class="merged-view" style="padding:15px;"></div><div id="editor-diff-offline" style="float:right;padding:15px;"></div></div>`,
            fullScreen: true,
            canClose: false,
            help: () => {
                const helpDialog = new _common__rspack_import_1.faqDialog({
                    title: gettext("Merge Dialog Frequent Questions"),
                    questions: [
                        [
                            gettext("Why am I seeing this merge dialog?"),
                            gettext(
                                "You are seeing this merge dialog because you were offline for a long time, and the changes you made to the document while you were offline conflicted with the changes made by other users. So it was not possible to merge your edits in automatically."
                            )
                        ],
                        [
                            gettext("Am I the only one seeing this dialog?"),
                            gettext(
                                "Yes, you are the only one who can see this dialog. Therefore it would be great if you could ask your collaborators to stop editing the document, so that once you are finished with the merge, it will not lead to more conflicts."
                            )
                        ],
                        [
                            gettext(
                                "What if my collaborators continue working on the document while I am merging?"
                            ),
                            gettext(
                                "Do not worry, we can handle such a situation as we will simply show you another merge dialog."
                            )
                        ],
                        [
                            gettext("Why am I seeing three editors?"),
                            gettext(
                                "The editor on the left will show the last common version of the document. It is shown for reference only. The editor in the middle is the merge editor and it initially contains the document resulting from other users' edits. The editor on the right is the offline editor and it contains the document resulting from your changes while you were offline."
                            )
                        ],
                        [
                            gettext(
                                "What are the green and red highlights in the editors?"
                            ),
                            gettext(
                                "The editors in the middle and on the right will show content that is highlighted in green. They also contain text that is highlighted in red. The text marked in green corresponds to the text that was edited (added) by you or other users. The text marked in red corresponds to text that was deleted by either you or other users. The insertions and deletions made by other users will be marked in the merge editor. The insertions and deletions made by you will be marked in the offline editor on the right."
                            )
                        ],
                        [
                            gettext(
                                "How do I accept or reject a particular change?"
                            ),
                            // Add images in dictionary.They'd be interpolated later.
                            gettext(
                                "Accepting or rejecting a change in the merge editor in the middle or the offline editor on the right causes a change in the merge editor. You can accept a change by directly clicking on the highlighted text. A dropdown menu will appear and you can then either accept or reject the change. When you click on the highlighted text, it also highlights the changes that will get accepted. %(mergeImage)s As shown in the above image one can click on a highlighted change, and click on accept change. On accepting a change it will be reflected in the merged document editor in the middle. Rejecting a change works in the same way except on reject a change the highlight of the change will be lost, with it the ability to accept, reject or copy a change."
                            ),
                            {
                                mergeImage: `<img src="${staticUrl("img/accept_change.avif")}" class = "merge-img">`
                            },
                            {hasImage: true}
                        ],
                        [
                            gettext(
                                "I cannot accept a particular change. What do I do?"
                            ),
                            gettext(
                                "If you cannot automatically accept a change into the merge editor, do not worry. You can choose to copy the change either by clicking on the copy button or manually copy the change and then paste it in the merge editor in the middle. It is as simple as that!"
                            )
                        ],
                        [
                            gettext("Can I edit content in all three editors?"),
                            gettext(
                                "You can edit the content only in the merge editor in the middle. You cannot edit content in the other two editors."
                            )
                        ],
                        [
                            gettext(
                                "Does the order in which I work on merging the changes matter?"
                            ),
                            gettext(
                                "It is always better to try to accept the changes in a linear fashion."
                            )
                        ],
                        [
                            gettext("What do I do after completing the merge?"),
                            gettext(
                                "After the merge is completed, you can click on the button 'Merge Complete' which in turn will move your changes from the merge editor in the middle to the main editor. Do note if other users made significant changes to the document while you were merging the document, you might have to merge the documents together again."
                            )
                        ],
                        [
                            gettext(
                                "What happens if I try to complete merge without resolving all the changes?"
                            ),
                            gettext(
                                "You will receive a warning when you try to complete a merge without accepting or rejecting all the changes. You can still proceed to merge the document. If you do that, all the insertions and deletions in the merge editor in the middle will be accepted automatically, and all the insertions and deletions in the offline editor on the right will be ignored."
                            )
                        ]
                    ]
                })
                helpDialog.open()
            },
            buttons
        })
        return dialog
    }

    checkResolution() {
        /* To Check if all the diffs are resolved */
        const offlineVersionDoc = this.mergeView3.state.doc,
            mergedVersionDoc = this.mergeView2.state.doc
        let diffAttrPresent = false
        if (
            offlineVersionDoc.rangeHasMark(
                0,
                offlineVersionDoc.content.size,
                this.schema.marks.diffdata
            ) ||
            mergedVersionDoc.rangeHasMark(
                0,
                mergedVersionDoc.content.size,
                this.schema.marks.diffdata
            )
        ) {
            return true
        }
        offlineVersionDoc.nodesBetween(
            0,
            offlineVersionDoc.content.size,
            (node, _pos) => {
                if (node.attrs.diffdata && node.attrs.diffdata.length > 0) {
                    diffAttrPresent = true
                }
            }
        )
        mergedVersionDoc.nodesBetween(
            0,
            mergedVersionDoc.content.size,
            (node, _pos) => {
                if (node.attrs.diffdata && node.attrs.diffdata.length > 0) {
                    diffAttrPresent = true
                }
            }
        )
        if (
            this.mergeView2.dom.querySelector(".deletion-decoration") ||
            this.mergeView3.dom.querySelector(".deletion-decoration")
        ) {
            diffAttrPresent = true
        }
        if (diffAttrPresent) {
            return true
        }
        return false
    }

    markChangesinDiffEditor(changeset, view, insertionClass, tr) {
        /* This marks all the changes in the diff editor */
        // Mark the insertions in insertion View & deletions in deletionView
        const insertionMarksTr = view.state.tr
        let stepsTrackedByChangeset = []
        // Use the changeset to create the marks
        changeset.changes.forEach(change => {
            if (change.inserted.length > 0) {
                let stepsInvolved = []
                change.inserted.forEach(insertion =>
                    stepsInvolved.push(Number.parseInt(insertion.data.step))
                )
                const stepsSet = new Set(stepsInvolved)
                stepsInvolved = Array.from(stepsSet)

                // Add the footnote related steps because the changeset tracks change but misses some steps related to insertion of footnote node!
                tr.steps.forEach((step, index) => {
                    if (
                        step.from >= change.fromB &&
                        step.to <= change.toB &&
                        step instanceof prosemirror_transform__rspack_import_18.ReplaceStep &&
                        !stepsInvolved.includes(index)
                    ) {
                        const stepOne = step.toJSON()
                        if (
                            stepOne.slice &&
                            stepOne.from !== stepOne.to &&
                            stepOne.slice.content.length == 1 &&
                            (stepOne.slice.content[0].type === "footnote" ||
                                stepOne.slice.content[0].type === "citation")
                        ) {
                            stepsInvolved.push(index)
                        }
                    } else if (
                        step.from >= change.fromB &&
                        step.to <= change.toB &&
                        step instanceof prosemirror_transform__rspack_import_18.AddMarkStep &&
                        !stepsInvolved.includes(index)
                    ) {
                        const stepOne = step.toJSON()
                        if (
                            stepOne.mark &&
                            [
                                "strong",
                                "em",
                                "underline",
                                "link",
                                "deletion",
                                "insertion",
                                "comment"
                            ].includes(stepOne.mark.type)
                        ) {
                            stepsInvolved.push(index)
                        }
                    }
                })

                stepsInvolved.sort((a, b) => a - b)
                const insertionMark = this.schema.marks.diffdata.create({
                    diff: insertionClass,
                    steps: JSON.stringify(stepsInvolved),
                    from: change.fromB,
                    to: change.toB,
                    markOnly: false
                })
                insertionMarksTr.addMark(
                    change.fromB,
                    change.toB,
                    insertionMark
                )
                this.markBlockDiffs(
                    insertionMarksTr,
                    change.fromB,
                    change.toB,
                    insertionClass,
                    stepsInvolved
                )
                if (
                    (0,_tools__rspack_import_11.checkPresenceOfdiffdata)(
                        insertionMarksTr.doc,
                        change.fromB,
                        change.toB
                    )
                ) {
                    stepsTrackedByChangeset =
                        stepsTrackedByChangeset.concat(stepsInvolved)
                }
            }
        })

        // Add all the footnote/mark/citation related steps that are not tracked by changeset.
        tr.steps.forEach((step, index) => {
            const from = tr.mapping.slice(index).map(step.from)
            const to = tr.mapping.slice(index).map(step.to, -1)
            if (
                step instanceof prosemirror_transform__rspack_import_18.ReplaceStep &&
                !stepsTrackedByChangeset.includes(index)
            ) {
                const stepOne = step.toJSON()
                if (
                    stepOne.slice &&
                    stepOne.slice.content.length == 1 &&
                    stepOne.slice.content[0].type === "footnote"
                ) {
                    const insertionMark = this.schema.marks.diffdata.create({
                        diff: insertionClass,
                        steps: JSON.stringify([index]),
                        from: from,
                        to: to,
                        markOnly: false
                    })
                    insertionMarksTr.addMark(from, to, insertionMark)
                    stepsTrackedByChangeset.push(index)
                } else if (
                    stepOne.slice &&
                    stepOne.slice.content.length == 1 &&
                    stepOne.slice.content[0].type === "citation"
                ) {
                    const insertionMark = this.schema.marks.diffdata.create({
                        diff: insertionClass,
                        steps: JSON.stringify([index]),
                        from: from,
                        to: to,
                        markOnly: false
                    })
                    insertionMarksTr.addMark(from, to, insertionMark)
                    stepsTrackedByChangeset.push(index)
                } else if (
                    stepOne.slice &&
                    stepOne.slice.content[0].type === "figure"
                ) {
                    if (stepOne.from == stepOne.to) {
                        this.markBlockDiffs(
                            insertionMarksTr,
                            stepOne.from,
                            stepOne.to + 1,
                            insertionClass,
                            [index]
                        )
                    } else {
                        this.markBlockDiffs(
                            insertionMarksTr,
                            stepOne.from,
                            stepOne.to,
                            insertionClass,
                            [index]
                        )
                    }
                    stepsTrackedByChangeset.push(index)
                }
            } else if (
                (step instanceof prosemirror_transform__rspack_import_18.AddMarkStep ||
                    step instanceof prosemirror_transform__rspack_import_18.RemoveMarkStep) &&
                !stepsTrackedByChangeset.includes(index)
            ) {
                const stepOne = step.toJSON(),
                    stepsInvolved = []
                stepsInvolved.push(index)
                if (
                    stepOne.mark &&
                    ["strong", "em", "underline", "link", "deletion"].includes(
                        stepOne.mark.type
                    )
                ) {
                    tr.steps.forEach((trStep, trIndex) => {
                        // Check for other format changes within this range.
                        if (
                            (trStep instanceof prosemirror_transform__rspack_import_18.AddMarkStep ||
                                trStep instanceof prosemirror_transform__rspack_import_18.RemoveMarkStep) &&
                            !stepsTrackedByChangeset.includes(trIndex)
                        ) {
                            const mapFrom = tr.mapping
                                .slice(trIndex)
                                .map(trStep.from)
                            const mapTo = tr.mapping
                                .slice(trIndex)
                                .map(trStep.to)
                            if (
                                mapFrom >= from &&
                                mapTo <= to &&
                                !stepsInvolved.includes(trIndex)
                            ) {
                                const stepTwo = trStep.toJSON()
                                if (
                                    stepTwo.mark &&
                                    [
                                        "strong",
                                        "em",
                                        "underline",
                                        "link",
                                        "deletion"
                                    ].includes(stepTwo.mark.type)
                                ) {
                                    stepsInvolved.push(trIndex)
                                }
                            }
                        }
                    })
                    const insertionMark = this.schema.marks.diffdata.create({
                        diff: insertionClass,
                        steps: JSON.stringify(stepsInvolved),
                        from: from,
                        to: to,
                        markOnly: true
                    })
                    stepsTrackedByChangeset =
                        stepsTrackedByChangeset.concat(stepsInvolved)
                    insertionMarksTr.addMark(from, to, insertionMark)
                }
            } else if (
                step instanceof prosemirror_transform__rspack_import_18.ReplaceAroundStep &&
                !stepsTrackedByChangeset.includes(index)
            ) {
                const stepOne = step.toJSON()
                if (
                    stepOne.slice &&
                    stepOne.slice.content[0].type === "figure"
                ) {
                    this.markBlockDiffs(
                        insertionMarksTr,
                        stepOne.from,
                        stepOne.to,
                        insertionClass,
                        [index]
                    )
                    stepsTrackedByChangeset.push(index)
                }
            }
        })

        // Dispatch the transactions
        insertionMarksTr
            .setMeta("initialDiffMap", true)
            .setMeta("mapAppended", true)
            .setMeta("notrack", true)
        view.dispatch(insertionMarksTr)

        //Return steps that are tracked in the diff editor
        return stepsTrackedByChangeset
    }

    bindEditorView(elementId, doc) {
        /* Binds the editor view */
        const plugins = this.diffEditorPlugins.map(plugin => {
            if (plugin[1]) {
                return plugin[0](plugin[1](doc))
            } else {
                return plugin[0]()
            }
        })
        let editorView
        const mainEditor = this.editor
        if (elementId == "editor-diff-online") {
            editorView = new prosemirror_view__rspack_import_19.EditorView(document.getElementById(elementId), {
                state: prosemirror_state__rspack_import_20.EditorState.create({
                    schema: this.schema,
                    doc: doc,
                    plugins: plugins
                }),
                dispatchTransaction: tr => {
                    let mapTr = tr
                    if (tr.docChanged) {
                        const mapAppended = tr.getMeta("mapAppended")
                        const noTrack = tr.getMeta("notrack")
                        if (!mapAppended) {
                            this.mergedDocMap.appendMapping(tr.mapping)
                        }
                        if (!noTrack) {
                            // Track only manual insertions
                            mapTr = (0,_track__rspack_import_5.trackedTransaction)(
                                mapTr,
                                this.mergeView2.state,
                                this.editor.user,
                                !this.mergeView2.state.doc.attrs.tracked &&
                                    ![
                                        "write-tracked",
                                        "review-tracked"
                                    ].includes(
                                        this.editor.docInfo.access_rights
                                    ),
                                Date.now() - this.editor.clientTimeAdjustment
                            )
                        }
                    }
                    const newState = editorView.state.apply(mapTr)
                    editorView.updateState(newState)
                    this.renderCitation(editorView, elementId)
                },
                nodeViews: {
                    footnote(node, view, getPos) {
                        return new _footnotes__rspack_import_7.FootnoteView(node, view, getPos, mainEditor)
                    }
                }
            })
        } else {
            editorView = new prosemirror_view__rspack_import_19.EditorView(document.getElementById(elementId), {
                state: prosemirror_state__rspack_import_20.EditorState.create({
                    schema: this.schema,
                    doc: doc,
                    plugins: plugins
                }),
                dispatchTransaction: tr => {
                    const newState = editorView.state.apply(tr)
                    editorView.updateState(newState)
                    this.renderCitation(editorView, elementId)
                },
                editable: () => false,
                nodeViews: {
                    footnote(node, view, getPos) {
                        return new _footnotes__rspack_import_7.FootnoteView(node, view, getPos, mainEditor)
                    }
                }
            })
        }
        return editorView
    }

    renderCitation(view, elementId) {
        const settings = view.state.doc.attrs,
            bibliographyHeader =
                settings.bibliography_header[settings.language] ||
                _schema_i18n__rspack_import_2.BIBLIOGRAPHY_HEADERS[settings.language]
        const citRenderer = new _citations_render__rspack_import_0.RenderCitations(
            document.getElementById(elementId),
            settings.citationstyle,
            bibliographyHeader,
            this.editor.mod.db.bibDB,
            this.editor.app.csl
        )
        citRenderer.init()
    }

    startMerge(offlineTr, onlineDoc) {
        try {
            /* start the merge process of moving changes to the editor */
            // Remove all diff related marks
            (0,_tools__rspack_import_11.dispatchRemoveDiffdata)(
                this.mergeView2,
                0,
                this.mergeView2.state.doc.content.size
            )

            // Apply all the marks that are not handled by recreate steps!
            const markTr = this.mergeView2.state.tr
            const offlineRebaseMapping = new prosemirror_transform__rspack_import_18.Mapping()
            offlineRebaseMapping.appendMappingInverted(offlineTr.mapping)
            offlineRebaseMapping.appendMapping(this.mergedDocMap)
            this.offStepsNotTracked.forEach(markstep => {
                const stepIndex = offlineTr.steps.indexOf(markstep)
                const offlineRebaseMap = offlineRebaseMapping.slice(
                    offlineTr.steps.length - stepIndex
                )
                const mappedMarkStep = markstep.map(offlineRebaseMap)
                if (
                    mappedMarkStep &&
                    !markTr.maybeStep(mappedMarkStep).failed
                ) {
                    this.mergedDocMap.appendMap(mappedMarkStep.getMap())
                    offlineRebaseMapping.appendMap(mappedMarkStep.getMap())
                    offlineRebaseMapping.setMirror(
                        offlineTr.steps.length - stepIndex - 1,
                        offlineTr.steps.length +
                            this.mergedDocMap.maps.length -
                            1
                    )
                }
            })
            this.mergeView2.dispatch(markTr)
            this.mergeDialog.close()

            const mergedDoc = this.mergeView2.state.doc
            //CleanUp
            this.mergeView1.destroy()
            this.mergeView2.destroy()
            this.mergeView3.destroy()

            this.applyChangesToMainEditor(onlineDoc, mergedDoc)
        } catch (error) {
            // Convert the doc to main editor schema
            const onlineDoc = this.editor.schema.nodeFromJSON(
                (0,_tools__rspack_import_11.removeDiffFromJson)(this.onlineDoc.toJSON())
            )
            const offlineDoc = this.editor.schema.nodeFromJSON(
                (0,_tools__rspack_import_11.removeDiffFromJson)(this.offlineDoc.toJSON())
            )

            // Handle merge failure
            this.editor.mod.collab.doc.merge.handleMergeFailure(
                error,
                offlineDoc,
                onlineDoc,
                this
            )
        }
    }

    unHideSections(view) {
        const unHideSectionTr = view.state.tr
        view.state.doc.forEach((child, offset, _index) => {
            if (child.attrs.optional) {
                const attrs = Object.assign({}, child.attrs, {hidden: false})
                unHideSectionTr.setNodeMarkup(offset, false, attrs)
            }
        })
        unHideSectionTr.setMeta("notrack", true).setMeta("mapAppended", true)
        view.dispatch(unHideSectionTr)
    }

    unHideSectionsinAllDoc() {
        /* Unhide all the optional sections */
        this.unHideSections(this.mergeView1)
        this.unHideSections(this.mergeView2)
        this.unHideSections(this.mergeView3)
    }

    applyChangesToMainEditor(onlineDoc, mergedDoc) {
        /* Applies the change from diff editor to main editor */
        onlineDoc = this.editor.schema.nodeFromJSON(
            (0,_tools__rspack_import_11.removeDiffFromJson)(onlineDoc.toJSON())
        )
        mergedDoc = this.editor.schema.nodeFromJSON(
            (0,_tools__rspack_import_11.removeDiffFromJson)(mergedDoc.toJSON())
        )
        const tr = (0,_tools__rspack_import_11.simplifyTransform)((0,_recreate_transform__rspack_import_8.recreateTransform)(onlineDoc, mergedDoc))
        const OnlineStepsLost = (0,_recreate_transform__rspack_import_8.recreateTransform)(
            onlineDoc,
            this.editor.view.state.doc
        )
        const onlineStepsLostChangeset = new _changeset__rspack_import_6.changeSet(OnlineStepsLost)
        const conflicts = onlineStepsLostChangeset.findConflicts(
            tr,
            OnlineStepsLost
        )
        if (conflicts.length > 0) {
            const editor = new MergeEditor(
                this.editor,
                onlineDoc,
                tr.doc,
                OnlineStepsLost.doc,
                tr,
                OnlineStepsLost,
                this.db
            )
            editor.init()
        } else {
            const newTr = this.editor.view.state.tr
            const maps = new prosemirror_transform__rspack_import_18.Mapping(
                []
                    .concat(
                        tr.mapping.maps
                            .slice()
                            .reverse()
                            .map(map => map.invert())
                    )
                    .concat(OnlineStepsLost.mapping.maps)
            )
            tr.steps.forEach((step, index) => {
                const mapped = step.map(maps.slice(tr.steps.length - index))
                if (mapped && !newTr.maybeStep(mapped).failed) {
                    maps.appendMap(mapped.getMap())
                    maps.setMirror(
                        tr.steps.length - index - 1,
                        tr.steps.length +
                            OnlineStepsLost.steps.length +
                            newTr.steps.length -
                            1
                    )
                }
            })
            newTr.setMeta("remote", true)

            // Re-render the fn editor for all the users.
            this.editor.mod.collab.doc.footnoteRender = true

            this.editor.view.dispatch(newTr)
            this.editor.mod.footnotes.fnEditor.renderAllFootnotes()
        }
    }

    findNotTrackedSteps(tr, trackedSteps) {
        /* Find steps not tracked by PM, usually steps that cause change of attrs */
        const nonTrackedSteps = []
        tr.steps.forEach((step, index) => {
            // mark steps other than replace steps as not tracked if not tracked by changeset
            // these steps should effectively only be the node attrs change steps.
            if (
                !trackedSteps.includes(index) &&
                (step instanceof prosemirror_transform__rspack_import_18.ReplaceAroundStep ||
                    step instanceof prosemirror_transform__rspack_import_18.AddMarkStep ||
                    step instanceof prosemirror_transform__rspack_import_18.RemoveMarkStep)
            ) {
                nonTrackedSteps.push(step)
            }
        })
        return nonTrackedSteps
    }

    markBlockDiffs(tr, from, to, difftype, stepsInvolved) {
        /* This Function is used to add diff data to Block Elements. */
        tr.doc.nodesBetween(from, to, (node, pos) => {
            if (
                pos < from ||
                ["bullet_list", "ordered_list"].includes(node.type.name)
            ) {
                return true
            } else if (node.isInline) {
                return false
            }
            if (node.attrs.diffdata) {
                const diffdata = []
                diffdata.push({
                    type: difftype,
                    from: from,
                    to: to,
                    steps: stepsInvolved
                })
                tr.setNodeMarkup(
                    pos,
                    null,
                    Object.assign({}, node.attrs, {diffdata}),
                    node.marks
                )
            }
        })
    }

    updateDB(offlineDoc, db) {
        /* Used to update the image,bib DB and update the doc in case if missing/lost images
        (update the image data with re-uploaded images) */
        let usedImages = []
        const usedBibs = []
        const footnoteFind = (node, usedImages, usedBibs) => {
            if (node.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            } else if (node.content) {
                node.content.forEach(subNode =>
                    footnoteFind(subNode, usedImages, usedBibs)
                )
            }
        }

        // Looking at rebased doc so that it contains the merged document !!!
        offlineDoc.descendants(node => {
            if (node.type.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.type.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            } else if (node.type.name === "footnote" && node.attrs.footnote) {
                node.attrs.footnote.forEach(subNode =>
                    footnoteFind(subNode, usedImages, usedBibs)
                )
            }
        })

        const oldBibDB = this.editor.mod.db.bibDB.db
        this.editor.mod.db.bibDB.setDB(db.bibliography)
        usedBibs.forEach(id => {
            if (!this.editor.mod.db.bibDB.db[id] && oldBibDB[id]) {
                this.editor.mod.db.bibDB.updateReference(id, oldBibDB[id])
            }
        })
        const oldImageDB = this.editor.mod.db.imageDB.db
        let imageUploadFailDialogShown = false
        this.editor.mod.db.imageDB.setDB(db.images)
        usedImages = new Set(usedImages)
        usedImages = Array.from(usedImages)
        usedImages.forEach(id => {
            if (!this.editor.mod.db.imageDB.db[id] && oldImageDB[id]) {
                // If the image was uploaded by the offline user we know that he may not have deleted it so we can resend it normally
                if (
                    Object.keys(this.editor.app.imageDB.db).includes(String(id))
                ) {
                    this.editor.mod.db.imageDB.setImage(id, oldImageDB[id])
                } else {
                    // If the image was uploaded by someone else, to set the image we have to reupload it again as there is backend check to associate who can add an image with the image owner.
                    this.editor.mod.db.imageDB
                        .reUploadImage(
                            id,
                            oldImageDB[id].image,
                            oldImageDB[id].title,
                            oldImageDB[id].copyright
                        )
                        .then(
                            ({id, newId}) => {
                                this.imageDataModified[id] = newId
                                // Update the image node if there are any re uploaded images.
                                this.mergeView1.state.doc.descendants(
                                    (node, pos) => {
                                        if (
                                            node.type.name === "image" &&
                                            node.attrs.image == id
                                        ) {
                                            const attrs = Object.assign(
                                                {},
                                                node.attrs
                                            )
                                            attrs["image"] = newId
                                            const nodeType =
                                                this.mergeView1.state.schema
                                                    .nodes["image"]
                                            const transaction =
                                                this.mergeView1.state.tr.setNodeMarkup(
                                                    pos,
                                                    nodeType,
                                                    attrs
                                                )
                                            transaction.setMeta(
                                                "mapAppended",
                                                true
                                            )
                                            this.mergeView1.dispatch(
                                                transaction
                                            )
                                        }
                                    }
                                )
                                this.mergeView3.state.doc.descendants(
                                    (node, pos) => {
                                        if (
                                            node.type.name === "image" &&
                                            node.attrs.image == id
                                        ) {
                                            const attrs = Object.assign(
                                                {},
                                                node.attrs
                                            )
                                            attrs["image"] = newId
                                            const nodeType =
                                                this.mergeView3.state.schema
                                                    .nodes["image"]
                                            const transaction =
                                                this.mergeView3.state.tr.setNodeMarkup(
                                                    pos,
                                                    nodeType,
                                                    attrs
                                                )
                                            transaction.setMeta(
                                                "mapAppended",
                                                true
                                            )
                                            this.mergeView3.dispatch(
                                                transaction
                                            )
                                        }
                                    }
                                )
                            },
                            id => {
                                // In case of failure make the id as false so the failed upload image is empty for the offline user too!
                                this.mergeView1.state.doc.descendants(
                                    (node, pos) => {
                                        if (
                                            node.type.name === "image" &&
                                            node.attrs.image == id
                                        ) {
                                            const attrs = Object.assign(
                                                {},
                                                node.attrs
                                            )
                                            attrs["image"] = false
                                            const nodeType =
                                                this.mergeView1.state.schema
                                                    .nodes["image"]
                                            const transaction =
                                                this.mergeView1.state.tr.setNodeMarkup(
                                                    pos,
                                                    nodeType,
                                                    attrs
                                                )
                                            transaction.setMeta(
                                                "mapAppended",
                                                true
                                            )
                                            this.mergeView1.dispatch(
                                                transaction
                                            )
                                        }
                                    }
                                )
                                this.mergeView3.state.doc.descendants(
                                    (node, pos) => {
                                        if (
                                            node.type.name === "image" &&
                                            node.attrs.image == id
                                        ) {
                                            const attrs = Object.assign(
                                                {},
                                                node.attrs
                                            )
                                            attrs["image"] = false
                                            const nodeType =
                                                this.mergeView3.state.schema
                                                    .nodes["image"]
                                            const transaction =
                                                this.mergeView3.state.tr.setNodeMarkup(
                                                    pos,
                                                    nodeType,
                                                    attrs
                                                )
                                            transaction.setMeta(
                                                "mapAppended",
                                                true
                                            )
                                            this.mergeView3.dispatch(
                                                transaction
                                            )
                                        }
                                    }
                                )
                                if (!imageUploadFailDialogShown) {
                                    imageUploadFailDialogShown = true
                                    ;(0,_common__rspack_import_1.showSystemMessage)(
                                        gettext(
                                            "One or more of the image(s) you copied could not be found on the server. Consider re-uploading them once the document is merged."
                                        )
                                    )
                                }
                            }
                        )
                }
            }
        })
    }
}


}),
"./js/modules/editor/collab/merge/footnotes.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FootnoteView: function() { return FootnoteView; },
  readOnlyFnEditor: function() { return readOnlyFnEditor; }
});
/* import */ var prosemirror_state__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _schema_footnotes__rspack_import_0 = __webpack_require__("./js/modules/schema/footnotes.js");
/* import */ var _schema_footnotes_convert__rspack_import_1 = __webpack_require__("./js/modules/schema/footnotes_convert.js");
/* import */ var _track__rspack_import_2 = __webpack_require__("./js/modules/editor/track/index.js");






class FootnoteView {
    constructor(node, view, getPos, editor) {
        // We'll need these later
        this.node = node
        this.outerView = view
        this.getPos = getPos
        this.editor = editor

        // The node's representation in the editor (empty, for now)
        this.dom = document.createElement("div")
        this.dom.className = "footnote-view"
        // These are used when the footnote is selected
        this.innerView = null
        // Updated main editor state
        this.updatedMainEditor = false
    }

    selectNode() {
        this.dom.classList.add("ProseMirror-selectednode")
        if (!this.innerView) {
            this.open()
        }
    }

    deselectNode() {
        this.dom.classList.remove("ProseMirror-selectednode")
        if (this.innerView) {
            this.close()
        }
    }

    open() {
        // Append a tooltip to the outer node
        const tooltip = this.dom.appendChild(document.createElement("div"))
        tooltip.className = "footnote-tooltip"
        const diffMark = this.node.marks.find(
            mark => mark.type.name === "diffdata"
        )
        if (diffMark === undefined) {
            tooltip.classList.add("render-arrow")
        } else {
            tooltip.style.top = "-30px"
        }

        const doc = _schema_footnotes__rspack_import_0.fnSchema.nodeFromJSON({
            type: "doc",
            content: [
                {
                    type: "footnotecontainer",
                    content: this.node.attrs.footnote
                }
            ]
        })

        // And put a sub-ProseMirror into that
        this.innerView = new prosemirror_view__rspack_import_3.EditorView(tooltip, {
            state: prosemirror_state__rspack_import_4.EditorState.create({
                doc: doc
            }),
            dispatchTransaction: this.dispatchInner.bind(this),
            handleDOMEvents: {
                mousedown: () => {
                    if (this.outerView.hasFocus()) {
                        this.innerView.focus()
                    }
                }
            }
        })
    }

    close() {
        if (!this.updatedMainEditor && this.outerView) {
            this.updateMainEditor()
        }
        if (this.innerView) {
            this.innerView.destroy()
            this.innerView = null
            this.dom.textContent = ""
            this.updatedMainEditor = false
        }
    }

    updateMainEditor() {
        const outerTr = this.outerView.state.tr
        const footnoteContent = this.innerView.state.doc
            .child(0)
            .toJSON().content
        const pos = this.getPos()
        const node = outerTr.doc.nodeAt(pos)
        if (node) {
            outerTr.setNodeMarkup(pos, node.type, {
                footnote: footnoteContent
            })
        }
        if (outerTr.docChanged) {
            outerTr.setMeta("fromFootnote", true)
            this.updatedMainEditor = true
            this.outerView.dispatch(outerTr)
        }
    }

    dispatchInner(tr) {
        const trackedTr = (0,_track__rspack_import_2.trackedTransaction)(
            tr,
            this.innerView.state,
            this.editor.user,
            !this.outerView.state.doc.attrs.tracked &&
                !["write-tracked", "review-tracked"].includes(
                    this.editor.docInfo.access_rights
                ),
            Date.now() - this.editor.clientTimeAdjustment
        )
        const {state} = this.innerView.state.applyTransaction(trackedTr)
        this.innerView.updateState(state)
    }

    update(node) {
        this.node = node
        return true
    }

    destroy() {
        this.outerView = null
        if (this.innerView) {
            this.close()
        }
    }

    stopEvent(event) {
        return this.innerView && this.innerView.dom.contains(event.target)
    }

    ignoreMutation() {
        return true
    }
}

const readOnlyFnEditor = footnoteElement => {
    // This function creates a read only footnote editor for the purpose of showing the
    // footnote nodes which are drawn as part of deletion decoration.
    const newFnElement = document.createElement("div")
    newFnElement.className = "footnote-view"
    newFnElement.dataset.footnote = footnoteElement.dataset.footnote
    const tooltip = newFnElement.appendChild(document.createElement("div"))
    tooltip.className = "footnote-tooltip"
    tooltip.classList.add("render-arrow")
    tooltip.style.display = "none"

    // Parse Footnote node's data
    const doc = _schema_footnotes__rspack_import_0.fnSchema.nodeFromJSON({
        type: "doc",
        content: [
            {
                type: "footnotecontainer",
                content: (0,_schema_footnotes_convert__rspack_import_1.htmlToFnNode)(footnoteElement.dataset.footnote)
            }
        ]
    })

    // Put a reado only sub-ProseMirror into that
    new prosemirror_view__rspack_import_3.EditorView(tooltip, {
        state: prosemirror_state__rspack_import_4.EditorState.create({
            doc: doc
        }),
        editable: () => false
    })

    return newFnElement
}


}),
"./js/modules/editor/collab/merge/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  Merge: function() { return Merge; }
});
/* import */ var prosemirror_collab__rspack_import_9 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_state__rspack_import_10 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_11 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _exporter_native__rspack_import_2 = __webpack_require__("./js/modules/exporter/native/index.js");
/* import */ var _schema_convert__rspack_import_3 = __webpack_require__("./js/modules/schema/convert.js");
/* import */ var _track__rspack_import_4 = __webpack_require__("./js/modules/editor/track/index.js");
/* import */ var _changeset__rspack_import_5 = __webpack_require__("./js/modules/editor/collab/merge/changeset.js");
/* import */ var _editor__rspack_import_6 = __webpack_require__("./js/modules/editor/collab/merge/editor.js");
/* import */ var _recreate_transform__rspack_import_7 = __webpack_require__("./js/modules/editor/collab/merge/recreate_transform.js");
/* import */ var _tools__rspack_import_8 = __webpack_require__("./js/modules/editor/collab/merge/tools.js");













class Merge {
    constructor(mod) {
        this.mod = mod
        this.trackOfflineLimit = 50 // Limit of local changes while offline for tracking to kick in when multiple users edit
        this.remoteTrackOfflineLimit = 50 // Limit of remote changes while offline for tracking to kick in when multiple users edit
    }

    adjustDocument(data) {
        // Adjust the document when reconnecting after offline and many changes
        // happening on server.
        if (
            this.mod.editor.docInfo.version < data.doc.v &&
            (0,prosemirror_collab__rspack_import_9.sendableSteps)(this.mod.editor.view.state)
        ) {
            this.mod.doc.receiving = true
            const confirmedState = prosemirror_state__rspack_import_10.EditorState.create({
                doc: this.mod.editor.docInfo.confirmedDoc
            })
            const unconfirmedTr = confirmedState.tr
            const sendable = (0,prosemirror_collab__rspack_import_9.sendableSteps)(this.mod.editor.view.state)
            if (sendable) {
                sendable.steps.forEach(step => unconfirmedTr.step(step))
            }
            const rollbackTr = this.mod.editor.view.state.tr
            unconfirmedTr.steps
                .slice()
                .reverse()
                .forEach((step, index) =>
                    rollbackTr.step(
                        step.invert(
                            unconfirmedTr.docs[
                                unconfirmedTr.docs.length - index - 1
                            ]
                        )
                    )
                )
            // We reset to there being no local changes to send.
            this.mod.editor.view.dispatch(
                (0,prosemirror_collab__rspack_import_9.receiveTransaction)(
                    this.mod.editor.view.state,
                    unconfirmedTr.steps,
                    unconfirmedTr.steps.map(_step => this.mod.editor.client_id)
                )
            )
            this.mod.editor.view.dispatch(
                (0,prosemirror_collab__rspack_import_9.receiveTransaction)(
                    this.mod.editor.view.state,
                    rollbackTr.steps,
                    rollbackTr.steps.map(_step => "remote")
                ).setMeta("remote", true)
            )
            const toDoc = this.mod.editor.schema.nodeFromJSON(data.doc.content)
            // Apply the online Transaction
            let lostTr
            if (data.m) {
                lostTr = new prosemirror_transform__rspack_import_11.Transform(this.mod.editor.view.state.doc)
                data.m.forEach(message => {
                    if (
                        message.ds &&
                        message.cid !== this.mod.editor.client_id
                    ) {
                        message.ds.forEach(j =>
                            lostTr.maybeStep(
                                prosemirror_transform__rspack_import_11.Step.fromJSON(this.mod.editor.schema, j)
                            )
                        )
                    }
                })
                if (!lostTr.doc.eq(toDoc)) {
                    // We were not able to recreate the document using the steps in the diffs. So instead we recreate the steps artificially.
                    lostTr = (0,_recreate_transform__rspack_import_7.recreateTransform)(
                        this.mod.editor.view.state.doc,
                        toDoc
                    )
                }
            } else {
                lostTr = (0,_recreate_transform__rspack_import_7.recreateTransform)(
                    this.mod.editor.view.state.doc,
                    toDoc
                )
            }

            this.mod.editor.view.dispatch(
                (0,prosemirror_collab__rspack_import_9.receiveTransaction)(
                    this.mod.editor.view.state,
                    lostTr.steps,
                    lostTr.steps.map(_step => "remote")
                ).setMeta("remote", true)
            )

            // We split the complex steps that delete and insert into simple steps so that finding conflicts is more pronounced.
            const modifiedLostTr = (0,_tools__rspack_import_8.simplifyTransform)(lostTr)
            const lostChangeSet = new _changeset__rspack_import_5.changeSet(modifiedLostTr)
            const conflicts = lostChangeSet.findConflicts(
                unconfirmedTr,
                modifiedLostTr
            )
            // Set the version
            this.mod.editor.docInfo.version = data.doc.v

            // Before starting the merge process update the fn editor to be in sync
            // with main editor
            this.mod.editor.mod.footnotes.fnEditor.renderAllFootnotes()

            // Load all the newly added comments from online users.
            this.mod.editor.mod.comments.store.loadComments(data.doc.comments)

            // If no conflicts arises auto-merge the document
            let editor
            if (conflicts.length > 0) {
                try {
                    editor = new _editor__rspack_import_6.MergeEditor(
                        this.mod.editor,
                        confirmedState.doc,
                        unconfirmedTr.doc,
                        toDoc,
                        unconfirmedTr,
                        lostTr,
                        {
                            bibliography: data.doc.bibliography,
                            images: data.doc.images
                        }
                    )
                    editor.init()
                } catch (error) {
                    this.handleMergeFailure(
                        error,
                        unconfirmedTr.doc,
                        toDoc,
                        editor
                    )
                }
            } else {
                try {
                    this.autoMerge(
                        unconfirmedTr,
                        lostTr,
                        data,
                        this.mod.editor.view.state.selection
                    )
                } catch (error) {
                    this.handleMergeFailure(error, unconfirmedTr.doc, toDoc)
                }
            }

            this.mod.doc.receiving = false
            // this.mod.doc.sendToCollaborators()
        } else if (data.m) {
            // There are no local changes, so we can just receive all the remote messages directly
            data.m.forEach(message => this.mod.doc.receiveDiff(message, true))
        } else {
            // The server seems to have lost some data. We reset.
            this.mod.doc.loadDocument(data)
        }
    }

    autoMerge(unconfirmedTr, lostTr, data, selection) {
        /* This automerges documents incase of no conflicts */
        const toDoc = this.mod.editor.schema.nodeFromJSON(data.doc.content)
        const rebasedTr = prosemirror_state__rspack_import_10.EditorState.create({
            doc: toDoc,
            selection
        }).tr.setMeta("remote", true)
        const maps = new prosemirror_transform__rspack_import_11.Mapping(
            []
                .concat(
                    unconfirmedTr.mapping.maps
                        .slice()
                        .reverse()
                        .map(map => map.invert())
                )
                .concat(lostTr.mapping.maps.slice())
        )

        unconfirmedTr.steps.forEach((step, index) => {
            const mapped = step.map(
                maps.slice(unconfirmedTr.steps.length - index)
            )
            if (mapped && !rebasedTr.maybeStep(mapped).failed) {
                maps.appendMap(mapped.getMap())
                maps.setMirror(
                    unconfirmedTr.steps.length - index - 1,
                    unconfirmedTr.steps.length +
                        lostTr.steps.length +
                        rebasedTr.steps.length -
                        1
                )
            }
        })

        let tracked
        let rebasedTrackedTr // offline steps to be tracked
        if (
            ___rspack_import_0.WRITE_ROLES.includes(this.mod.editor.docInfo.access_rights) &&
            (unconfirmedTr.steps.length > this.trackOfflineLimit ||
                lostTr.steps.length > this.remoteTrackOfflineLimit)
        ) {
            tracked = true
            // Either this user has made 50 changes since going offline,
            // or the document has 20 changes to it. Therefore we add tracking
            // to the changes of this user and ask user to clean up.
            rebasedTrackedTr = (0,_track__rspack_import_4.trackedTransaction)(
                rebasedTr,
                this.mod.editor.view.state,
                this.mod.editor.user,
                false,
                Date.now() - this.mod.editor.clientTimeAdjustment
            )
        } else {
            tracked = false
            rebasedTrackedTr = rebasedTr
        }

        let usedImages = []
        const usedBibs = []
        const footnoteFind = (node, usedImages, usedBibs) => {
            if (node.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            } else if (node.content) {
                node.content.forEach(subNode =>
                    footnoteFind(subNode, usedImages, usedBibs)
                )
            }
        }
        rebasedTr.doc.descendants(node => {
            if (node.type.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.type.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            } else if (node.type.name === "footnote" && node.attrs.footnote) {
                node.attrs.footnote.forEach(subNode =>
                    footnoteFind(subNode, usedImages, usedBibs)
                )
            }
        })
        const oldBibDB = this.mod.editor.mod.db.bibDB.db
        this.mod.editor.mod.db.bibDB.setDB(data.doc.bibliography)
        usedBibs.forEach(id => {
            if (!this.mod.editor.mod.db.bibDB.db[id] && oldBibDB[id]) {
                this.mod.editor.mod.db.bibDB.updateReference(id, oldBibDB[id])
            }
        })
        const oldImageDB = this.mod.editor.mod.db.imageDB.db
        this.mod.editor.mod.db.imageDB.setDB(data.doc.images)
        // Remove the Duplicated image ID's
        usedImages = new Set(usedImages)
        usedImages = Array.from(usedImages)
        usedImages.forEach(id => {
            if (!this.mod.editor.mod.db.imageDB.db[id] && oldImageDB[id]) {
                // If the image was uploaded by the offline user we know that he may not have deleted it so we can resend it normally
                if (Object.keys(this.mod.editor.app.imageDB.db).includes(id)) {
                    this.mod.editor.mod.db.imageDB.setImage(id, oldImageDB[id])
                } else {
                    // If the image was uploaded by someone else , to set the image we have to reupload it again as there is backend check to associate who can add an image with the image owner.
                    this.mod.editor.mod.db.imageDB
                        .reUploadImage(
                            id,
                            oldImageDB[id].image,
                            oldImageDB[id].title,
                            oldImageDB[id].copyright
                        )
                        .then(
                            () => {},
                            id => {
                                const transaction =
                                    this.mod.editor.view.state.tr
                                this.mod.editor.view.state.doc.descendants(
                                    (node, pos) => {
                                        if (
                                            node.type.name === "image" &&
                                            node.attrs.image == id
                                        ) {
                                            const attrs = Object.assign(
                                                {},
                                                node.attrs
                                            )
                                            attrs["image"] = false
                                            const nodeType =
                                                this.mod.editor.currentView
                                                    .state.schema.nodes["image"]
                                            transaction.setNodeMarkup(
                                                pos,
                                                nodeType,
                                                attrs
                                            )
                                        }
                                    }
                                )
                                this.mod.editor.view.dispatch(transaction)
                                ;(0,_common__rspack_import_1.addAlert)(
                                    "error",
                                    gettext(
                                        "One of the image(s) you copied could not be found on the server. Please try uploading it again."
                                    )
                                )
                            }
                        )
                }
            }
        })

        // this.mod.editor.docInfo.version = data.doc.v
        rebasedTrackedTr.setMeta("remote", true)
        this.mod.editor.view.dispatch(rebasedTrackedTr)

        if (tracked) {
            (0,_common__rspack_import_1.showSystemMessage)(
                gettext(
                    "The document was modified substantially by other users while you were offline. We have merged your changes in as tracked changes. You should verify that your edits still make sense."
                )
            )
        }
        this.mod.editor.mod.footnotes.fnEditor.renderAllFootnotes()
    }

    getDocData(offlineDoc) {
        const pmArticle = (0,_track__rspack_import_4.acceptAllNoInsertions)(offlineDoc).firstChild
        let title = ""
        pmArticle.firstChild.forEach(child => {
            if (!child.marks.find(mark => mark.type.name === "deletion")) {
                title += child.textContent
            }
        })

        return {
            content: pmArticle.toJSON(),
            settings: (0,_schema_convert__rspack_import_3.getSettings)(pmArticle),
            title: title,
            version: this.mod.editor.docInfo.version,
            comments: this.mod.editor.mod.comments.store.comments,
            id: this.mod.editor.docInfo.id,
            updated: this.mod.editor.docInfo.updated
        }
    }

    handleMergeFailure(error, offlineDoc, _onlineDoc, mergeEditor = false) {
        // In case the auto-merge or manual merge failed due to JS Errors,
        // make a copy of the offline doc available for download.

        // Close the merge window if open
        if (mergeEditor && document.querySelector("#editor-merge-view")) {
            mergeEditor.mergeDialog.close()
            // Close merge resolution warning if open
            if (document.querySelector("#merge-res-warning")) {
                mergeEditor.warningDialog.close()
            }
        }

        // Prepare Export
        new _exporter_native__rspack_import_2.ExportFidusFile(
            this.getDocData(offlineDoc),
            this.mod.editor.mod.db.bibDB,
            this.mod.editor.mod.db.imageDB
        )

        // Show up proper message
        const mergeFailedDialog = new _common__rspack_import_1.Dialog({
            title: gettext("Merge failed"),
            id: "merge_failed",
            body: gettext(
                "An error occurred during the merge process, so we cannot save your work to the server any longer, and it is downloaded to your computer instead. Please consider importing it into a new document."
            ),
            buttons: [
                {
                    text: gettext("Leave editor"),
                    classes: "fw-dark",
                    click: () => {
                        window.location.href = "/"
                    }
                }
            ],
            canClose: false
        })
        mergeFailedDialog.open()

        // Close the editor operations.
        this.mod.editor.close()

        // Throw the error so it is logged.
        throw error
    }
}


}),
"./js/modules/editor/collab/merge/recreate_transform.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  recreateTransform: function() { return recreateTransform; }
});
/* import */ var diff__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/diff@5.2.0/node_modules/diff/lib/index.mjs");
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var rfc6902__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/rfc6902@5.1.2/node_modules/rfc6902/index.js");
// See https://gitlab.com/mpapp-public/prosemirror-recreate-steps/blob/master/src/recreate.js
// We only need this file from the prosemirror-recreate-steps (apache) project, so it's copied
// in here.





function getReplaceStep(fromDoc, toDoc) {
    let start = toDoc.content.findDiffStart(fromDoc.content)
    if (start === null) {
        return false
    }
    let {a: endA, b: endB} = toDoc.content.findDiffEnd(fromDoc.content)
    const overlap = start - Math.min(endA, endB)
    if (overlap > 0) {
        if (
            // If there is an overlap, there is some freedom of choise in how to calculate the start/end boundary.
            // for an inserted/removed slice. We choose the extreme with the lowest depth value.
            fromDoc.resolve(start - overlap).depth <
            toDoc.resolve(endA + overlap).depth
        ) {
            start -= overlap
        } else {
            endA += overlap
            endB += overlap
        }
    }
    return new prosemirror_transform__rspack_import_2.ReplaceStep(start, endB, toDoc.slice(start, endA))
}

class RecreateTransform {
    constructor(fromDoc, toDoc, complexSteps, wordDiffs) {
        this.fromDoc = fromDoc
        this.toDoc = toDoc
        this.complexSteps = complexSteps // Whether to return steps other than ReplaceSteps
        this.wordDiffs = wordDiffs // Whether to make text diffs cover entire words
        this.schema = fromDoc.type.schema
        this.tr = new prosemirror_transform__rspack_import_2.Transform(fromDoc)
    }

    init() {
        if (this.complexSteps) {
            // For First steps: we create versions of the documents without marks as
            // these will only confuse the diffing mechanism and marks won't cause
            // any mapping changes anyway.
            this.currentJSON = this.marklessDoc(this.fromDoc).toJSON()
            this.finalJSON = this.marklessDoc(this.toDoc).toJSON()
            this.ops = (0,rfc6902__rspack_import_1.createPatch)(this.currentJSON, this.finalJSON)
            this.recreateChangeContentSteps()
            this.recreateChangeMarkSteps()
        } else {
            // We don't differentiate between mark changes and other changes.
            this.currentJSON = this.fromDoc.toJSON()
            this.finalJSON = this.toDoc.toJSON()
            this.ops = (0,rfc6902__rspack_import_1.createPatch)(this.currentJSON, this.finalJSON)
            this.recreateChangeContentSteps()
        }

        this.simplifyTr()

        return this.tr
    }

    recreateChangeContentSteps() {
        // First step: find content changing steps.
        let ops = [],
            afterStepJSON = JSON.parse(JSON.stringify(this.currentJSON))
        while (this.ops.length) {
            let op = this.ops.shift(),
                toDoc = false
            const pathParts = op.path.split("/")
            ops.push(op)
            while (!toDoc) {
                (0,rfc6902__rspack_import_1.applyPatch)(afterStepJSON, [op])
                try {
                    toDoc = this.schema.nodeFromJSON(afterStepJSON)
                    toDoc.check()
                } catch (error) {
                    toDoc = false
                    if (this.ops.length) {
                        op = this.ops.shift()
                        ops.push(op)
                    } else {
                        throw error
                    }
                }
            }

            if (
                this.complexSteps &&
                ops.length === 1 &&
                (pathParts.includes("attrs") || pathParts.includes("type"))
            ) {
                // Node markup is changing
                this.addSetNodeMarkup()
                ops = []
                afterStepJSON = JSON.parse(JSON.stringify(this.currentJSON))
            } else if (
                ops.length === 1 &&
                op.op === "replace" &&
                pathParts[pathParts.length - 1] === "text"
            ) {
                // Text is being replaced, we apply text diffing to find the smallest possible diffs.
                this.addReplaceTextSteps(op, afterStepJSON)
                ops = []
                afterStepJSON = JSON.parse(JSON.stringify(this.currentJSON))
            } else {
                if (this.addReplaceStep(toDoc, afterStepJSON)) {
                    ops = []
                    afterStepJSON = JSON.parse(JSON.stringify(this.currentJSON))
                }
            }
        }
    }

    recreateChangeMarkSteps() {
        // Now the documents should be the same, except their marks, so everything should map 1:1.
        // Second step: Iterate through the toDoc and make sure all marks are the same in tr.doc
        this.toDoc.descendants((tNode, tPos) => {
            if (!tNode.isInline) {
                return true
            }

            this.tr.doc.nodesBetween(
                tPos,
                tPos + tNode.nodeSize,
                (fNode, fPos) => {
                    if (!fNode.isInline) {
                        return true
                    }
                    const from = Math.max(tPos, fPos),
                        to = Math.min(
                            tPos + tNode.nodeSize,
                            fPos + fNode.nodeSize
                        )
                    fNode.marks.forEach(nodeMark => {
                        if (!nodeMark.isInSet(tNode.marks)) {
                            this.tr.removeMark(from, to, nodeMark)
                        }
                    })
                    tNode.marks.forEach(nodeMark => {
                        if (!nodeMark.isInSet(fNode.marks)) {
                            this.tr.addMark(from, to, nodeMark)
                        }
                    })
                }
            )
        })
    }

    marklessDoc(doc) {
        const tr = new prosemirror_transform__rspack_import_2.Transform(doc)
        tr.removeMark(0, doc.nodeSize - 2)
        return tr.doc
    }

    // From http://prosemirror.net/examples/footnote/
    addReplaceStep(toDoc, afterStepJSON) {
        const fromDoc = this.schema.nodeFromJSON(this.currentJSON),
            step = getReplaceStep(fromDoc, toDoc)
        if (!step) {
            return false
        } else if (!this.tr.maybeStep(step).failed) {
            this.currentJSON = afterStepJSON
            return true
        } else {
            throw new Error("No valid step found.")
        }
    }

    addSetNodeMarkup() {
        const fromDoc = this.schema.nodeFromJSON(this.currentJSON),
            toDoc = this.schema.nodeFromJSON(this.finalJSON)
        let updatedSettings = false
        Object.entries(fromDoc.attrs).forEach(([key, value]) => {
            if (toDoc.attrs[key] !== value) {
                this.tr.setDocAttribute(key, toDoc.attrs[key])
                updatedSettings = true
            }
        })
        if (updatedSettings) {
            this.currentJSON = this.marklessDoc(this.tr.doc).toJSON()
            this.ops = (0,rfc6902__rspack_import_1.createPatch)(this.currentJSON, this.finalJSON)
        }
        const start = toDoc.content.findDiffStart(fromDoc.content)
        if (start !== null) {
            const fromNode = fromDoc.nodeAt(start),
                toNode = toDoc.nodeAt(start)
            try {
                this.tr.setNodeMarkup(
                    start,
                    fromNode.type === toNode.type ? null : toNode.type,
                    toNode.attrs,
                    toNode.marks
                )
            } catch (_error) {
                return
            }
            this.currentJSON = this.marklessDoc(this.tr.doc).toJSON()
            // Setting the node markup may have invalidated more ops, so we calculate them again.
            this.ops = (0,rfc6902__rspack_import_1.createPatch)(this.currentJSON, this.finalJSON)
        }
    }

    addReplaceTextSteps(op, afterStepJSON) {
        // We find the position number of the first character in the string
        const op1 = Object.assign({}, op, {value: "xx"}),
            op2 = Object.assign({}, op, {value: "yy"})

        const afterOP1JSON = JSON.parse(JSON.stringify(this.currentJSON)),
            afterOP2JSON = JSON.parse(JSON.stringify(this.currentJSON)),
            pathParts = op.path.split("/")

        let obj = this.currentJSON

        ;(0,rfc6902__rspack_import_1.applyPatch)(afterOP1JSON, [op1])
        ;(0,rfc6902__rspack_import_1.applyPatch)(afterOP2JSON, [op2])

        const op1Doc = this.schema.nodeFromJSON(afterOP1JSON),
            op2Doc = this.schema.nodeFromJSON(afterOP2JSON)

        let offset = op1Doc.content.findDiffStart(op2Doc.content)
        const marks = op1Doc.resolve(offset + 1).marks()

        pathParts.shift()

        while (pathParts.length) {
            const pathPart = pathParts.shift()
            obj = obj[pathPart]
        }

        const finalText = op.value,
            currentText = obj

        const textDiffs = this.wordDiffs
            ? (0,diff__rspack_import_0.diffWordsWithSpace)(currentText, finalText)
            : (0,diff__rspack_import_0.diffChars)(currentText, finalText)

        while (textDiffs.length) {
            const diff = textDiffs.shift()
            if (diff.added) {
                if (textDiffs[0]?.removed) {
                    const nextDiff = textDiffs.shift()
                    this.tr.replaceWith(
                        offset,
                        offset + nextDiff.value.length,
                        this.schema
                            .nodeFromJSON({type: "text", text: diff.value})
                            .mark(marks)
                    )
                } else {
                    this.tr.insert(
                        offset,
                        this.schema
                            .nodeFromJSON({type: "text", text: diff.value})
                            .mark(marks)
                    )
                }
                offset += diff.value.length
            } else if (diff.removed) {
                if (textDiffs[0]?.added) {
                    const nextDiff = textDiffs.shift()
                    this.tr.replaceWith(
                        offset,
                        offset + diff.value.length,
                        this.schema
                            .nodeFromJSON({type: "text", text: nextDiff.value})
                            .mark(marks)
                    )
                    offset += nextDiff.value.length
                } else {
                    this.tr.delete(offset, offset + diff.value.length)
                }
            } else {
                offset += diff.value.length
            }
        }
        this.currentJSON = afterStepJSON
    }

    // join adjacent ReplaceSteps
    simplifyTr() {
        if (!this.tr.steps.length) {
            return
        }

        const newTr = new prosemirror_transform__rspack_import_2.Transform(this.tr.docs[0]),
            oldSteps = this.tr.steps.slice()
        while (oldSteps.length) {
            let step = oldSteps.shift()
            while (oldSteps.length && step.merge(oldSteps[0])) {
                const addedStep = oldSteps.shift()
                if (
                    step instanceof prosemirror_transform__rspack_import_2.ReplaceStep &&
                    addedStep instanceof prosemirror_transform__rspack_import_2.ReplaceStep
                ) {
                    step =
                        getReplaceStep(
                            newTr.doc,
                            addedStep.apply(step.apply(newTr.doc).doc).doc
                        ) || step.merge(addedStep)
                } else {
                    step = step.merge(addedStep)
                }
            }
            newTr.step(step)
        }
        this.tr = newTr
    }
}

function recreateTransform(
    fromDoc,
    toDoc,
    complexSteps = true,
    wordDiffs = true
) {
    const recreator = new RecreateTransform(
        fromDoc,
        toDoc,
        complexSteps,
        wordDiffs
    )
    return recreator.init()
}


}),
"./js/modules/editor/collab/merge/schema.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  createDiffSchema: function() { return createDiffSchema; },
  parseDiff: function() { return parseDiff; }
});
/* import */ var prosemirror_model__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");


function parseDiff(str) {
    if (!str) {
        return []
    }
    let tracks
    try {
        tracks = JSON.parse(str)
    } catch (_error) {
        return []
    }
    if (!Array.isArray(tracks)) {
        return []
    }
}

const createDiffSchema = docSchema => {
    let specNodes = docSchema.spec.nodes

    specNodes.forEach(nodeTypeName => {
        const nodeType = specNodes.get(nodeTypeName)
        if (nodeType.group !== "block") {
            return
        }
        const attrs = nodeType.attrs
        specNodes = specNodes.update(
            nodeTypeName,
            Object.assign({}, nodeType, {
                attrs: Object.assign({diffdata: {default: []}}, attrs),
                toDOM: node => {
                    let dom = nodeType.toDOM(node)
                    if (node.attrs.diffdata && node.attrs.diffdata.length) {
                        if (dom[1].class) {
                            dom[1].class =
                                dom[1].class + " " + node.attrs.diffdata[0].type
                        } else {
                            dom[1]["class"] = node.attrs.diffdata[0].type
                        }
                        dom = [
                            dom[0],
                            Object.assign(
                                {
                                    "data-diffdata": JSON.stringify(
                                        node.attrs.diffdata
                                    )
                                },
                                dom[1]
                            ),
                            dom[2]
                        ]
                    }
                    return dom
                },
                parseDOM: nodeType.parseDOM.map(tag => ({
                    tag: tag.tag,
                    getAttrs: dom => {
                        const attrs = tag.getAttrs(dom)
                        return Object.assign(
                            {
                                diffdata: parseDiff(dom.dataset.diffdata)
                            },
                            attrs
                        )
                    }
                }))
            })
        )
    })

    const diffdata = {
        attrs: {
            diff: {
                default: ""
            },
            steps: {
                default: []
            },
            from: {
                default: ""
            },
            to: {
                default: ""
            },
            markOnly: false
        },
        inclusive: false,
        parseDOM: [
            {
                tag: "span.diff",
                getAttrs(dom) {
                    return {
                        diff: dom.dataset.diff,
                        steps: dom.dataset.steps
                    }
                }
            }
        ],
        toDOM(node) {
            return [
                "span",
                {
                    class: `diff ${node.attrs.diff}`,
                    "data-diff": node.attrs.diff,
                    "data-steps": node.attrs.steps,
                    "data-from": node.attrs.from,
                    "data-to": node.attrs.to,
                    "data-markOnly": node.attrs.markOnly
                }
            ]
        }
    }

    const spec = {
        nodes: specNodes,
        marks: docSchema.spec.marks.addToEnd("diffdata", diffdata)
    }

    // Update link mark toDom to render a span instead of anchor tag
    // Since editable false PM Editor treats anchor tag as a normal a tag
    // and redirects
    const linkMarkSpec = spec.marks.get("link")
    spec.marks = spec.marks.update(
        "link",
        Object.assign({}, linkMarkSpec, {
            toDOM: node => {
                const dom = linkMarkSpec.toDOM(node)
                dom[0] = "span"
                return dom
            }
        })
    )

    return new prosemirror_model__rspack_import_0.Schema(spec)
}


}),
"./js/modules/editor/collab/merge/state_plugins/action.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  acceptChanges: function() { return acceptChanges; },
  addDeletedContentBack: function() { return addDeletedContentBack; },
  copyChange: function() { return copyChange; },
  deleteContent: function() { return deleteContent; },
  handleMarks: function() { return handleMarks; },
  removeDecoration: function() { return removeDecoration; }
});
/* import */ var prosemirror_state__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _tools__rspack_import_1 = __webpack_require__("./js/modules/editor/collab/merge/tools.js");





const copyChange = (view, from, to) => {
    /* when a certain change cannot be applied automatically,
    we give users the ability to copy a change */
    const tr = view.state.tr
    const resolvedFrom = view.state.doc.resolve(from)
    const resolvedTo = view.state.doc.resolve(to)
    const sel = new prosemirror_state__rspack_import_2.TextSelection(resolvedFrom, resolvedTo)
    sel.visible = false
    tr.setSelection(sel)
    view.dispatch(tr)
    view.focus()

    const slice = view.state.selection.content()
    const {dom} = view.serializeForClipboard(slice)

    // Copy data to clipboard!!
    document.body.appendChild(dom)
    const range = document.createRange()
    range.selectNode(dom)
    window.getSelection().addRange(range)
    try {
        document.execCommand("copy") // Security exception may be thrown by some browsers.
        document.body.removeChild(dom)
        ;(0,_common__rspack_import_0.showSystemMessage)(gettext("Change copied to clipboard."))
    } catch (_ex) {
        (0,_common__rspack_import_0.showSystemMessage)(
            gettext("Copy to clipboard failed. Please copy manually.")
        )
    }
    window.getSelection().removeAllRanges()
}

const acceptChanges = (merge, mark, mergeView, originalView, tr) => {
    /* This is used to accept a change either from the offline/online version or
    incase of deletion from the middle editor */
    const mergedDocMap = new prosemirror_transform__rspack_import_3.Mapping()
    mergedDocMap.appendMapping(merge.mergedDocMap)
    const insertionTr = mergeView.state.tr
    const from = mark.attrs.from
    const to = mark.attrs.to
    const steps = JSON.parse(mark.attrs.steps)
    const stepMaps = tr.mapping.maps
        .slice()
        .reverse()
        .map(map => map.invert())
    const rebasedMapping = new prosemirror_transform__rspack_import_3.Mapping(stepMaps)
    rebasedMapping.appendMapping(mergedDocMap)
    for (const stepIndex of steps) {
        const maps = rebasedMapping.slice(tr.steps.length - stepIndex)
        let mappedStep = tr.steps[stepIndex].map(maps)
        if (mappedStep) {
            mappedStep = prosemirror_transform__rspack_import_3.Step.fromJSON(
                // Switch from main editor schema to merge editor schema
                insertionTr.doc.type.schema,
                mappedStep.toJSON()
            )
        }
        if (mappedStep && !insertionTr.maybeStep(mappedStep).failed) {
            mergedDocMap.appendMap(mappedStep.getMap())
            rebasedMapping.appendMap(mappedStep.getMap())
            rebasedMapping.setMirror(
                tr.steps.length - stepIndex - 1,
                tr.steps.length + mergedDocMap.maps.length - 1
            )
        }
    }
    // Make sure that all the content steps are present in the new transaction
    if (insertionTr.steps.length < steps.length) {
        (0,_common__rspack_import_0.showSystemMessage)(
            gettext(
                "The change could not be applied automatically. Please consider using the copy option to copy the changes."
            )
        )
    } else {
        (0,_tools__rspack_import_1.dispatchRemoveDiffdata)(originalView, from, to)
        merge.mergedDocMap = mergedDocMap
        insertionTr.setMeta("mapAppended", true)
        insertionTr.setMeta("notrack", true)
        mergeView.dispatch(insertionTr)
    }
}

const removeDecoration = (view, decorationId) => {
    const tr = view.state.tr
    tr.setMeta("decorationId", decorationId)
    view.dispatch(tr)
}

const deleteContent = (merge, view, diffMark, mappingNeeded = true) => {
    // const originalOnlineMapping = merge.onlineTr.mapping
    const rebasedMapping = new prosemirror_transform__rspack_import_3.Mapping()
    const tr = view.state.tr
    if (mappingNeeded) {
        rebasedMapping.appendMapping(merge.mergedDocMap)
    }
    const rebasedFrom = rebasedMapping.map(diffMark.attrs.from),
        rebasedTo = rebasedMapping.map(diffMark.attrs.to)
    if (rebasedFrom && rebasedTo) {
        tr.delete(rebasedFrom, rebasedTo)
        merge.mergedDocMap.appendMapping(tr.mapping)
        tr.setMeta("mapAppended", true)
        tr.setMeta("notrack", true)
        view.dispatch(tr)
        return true
    }
    (0,_common__rspack_import_0.showSystemMessage)(
        gettext(
            "The change could not be applied automatically. Please consider using the copy option to copy the changes."
        )
    )
    return false
}

const addDeletedContentBack = (merge, view, diffMark) => {
    const commonDoc = merge.cpDoc
    const tr = view.state.tr
    const slice = commonDoc.slice(diffMark.attrs.from, diffMark.attrs.to)
    const rebasedMapping = new prosemirror_transform__rspack_import_3.Mapping()
    rebasedMapping.appendMapping(merge.mergedDocMap)
    const insertionPoint = rebasedMapping.map(diffMark.attrs.from)
    if (insertionPoint) {
        tr.insert(insertionPoint, slice.content)
        tr.setMeta("mapAppended", true)
        tr.setMeta("notrack", true)
        view.dispatch(tr)
        merge.mergedDocMap.appendMapping(tr.mapping)
        return true
    }
    (0,_common__rspack_import_0.showSystemMessage)(
        gettext(
            "The change could not be applied automatically. Please consider using the copy option to copy the changes."
        )
    )
    return false
}

const handleMarks = (view, mark, tr, schema) => {
    // This function is used to remove the marks that have been applied in the online editor
    const newTr = view.state.tr
    const steps = JSON.parse(mark.attrs.steps)
    const marksToBeRemoved = [],
        marksToBeAdded = []
    steps.forEach(index => {
        const JSONStep = tr.steps[index].toJSON()
        if (JSONStep.mark && JSONStep.mark.type) {
            if (tr.steps[index] instanceof prosemirror_transform__rspack_import_3.AddMarkStep) {
                marksToBeRemoved.push(JSONStep.mark.type)
            } else if (tr.steps[index] instanceof prosemirror_transform__rspack_import_3.RemoveMarkStep) {
                marksToBeAdded.push(tr.steps[index].mark)
            }
        }
    })
    marksToBeRemoved.forEach(removalMark =>
        newTr.removeMark(
            mark.attrs.from,
            mark.attrs.to,
            schema.marks[removalMark]
        )
    )
    marksToBeAdded.forEach(AddMark =>
        newTr.addMark(mark.attrs.from, mark.attrs.to, AddMark)
    )
    newTr.setMeta("notrack", true)
    newTr.setMeta("mapAppended", true)
    view.dispatch(newTr)
}


}),
"./js/modules/editor/collab/merge/state_plugins/clipboard.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  clipboardPlugin: function() { return clipboardPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _clipboard_copy__rspack_import_0 = __webpack_require__("./js/modules/editor/clipboard/copy/index.js");



const key = new prosemirror_state__rspack_import_1.PluginKey("clipboard")
const clipboardPlugin = options => {
    return new prosemirror_state__rspack_import_1.Plugin({
        key,
        props: {
            handleDrop: (view, event, slice, moved) => {
                if (moved || (slice && slice.size)) {
                    return false // Something other than en empty plain text string from outside. Handled by PM already.
                }
                const eventPos = view.posAtCoords({
                    left: event.clientX,
                    top: event.clientY
                })
                if (!eventPos) {
                    return false
                }
                const $mouse = view.state.doc.resolve(eventPos.pos)
                if (!$mouse) {
                    return false
                }
                const tr = view.state.tr
                tr.setSelection(new prosemirror_state__rspack_import_1.TextSelection($mouse))
                view.dispatch(tr)
                return true
            },
            clipboardSerializer: (0,_clipboard_copy__rspack_import_0.docClipboardSerializer)(options.editor)
        }
    })
}


}),
"./js/modules/editor/collab/merge/state_plugins/diff.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  diffPlugin: function() { return diffPlugin; },
  key: function() { return key; }
});
/* import */ var prosemirror_model__rspack_import_7 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_8 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var prosemirror_view__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _changeset__rspack_import_1 = __webpack_require__("./js/modules/editor/collab/merge/changeset.js");
/* import */ var _footnotes__rspack_import_2 = __webpack_require__("./js/modules/editor/collab/merge/footnotes.js");
/* import */ var _tools__rspack_import_3 = __webpack_require__("./js/modules/editor/collab/merge/tools.js");
/* import */ var _action__rspack_import_4 = __webpack_require__("./js/modules/editor/collab/merge/state_plugins/action.js");










function createHiglightDecoration(from, to, state) {
    /* Creates a yellow coloured highlight decoration when the user
    tries to look at a change */
    const inlineDeco = prosemirror_view__rspack_import_5.Decoration.inline(from, to, {class: "selected-dec"})
    const deco = []
    deco.push(inlineDeco)
    state.doc.nodesBetween(from, to, (node, pos) => {
        if (
            pos < from ||
            ["bullet_list", "ordered_list"].includes(node.type.name)
        ) {
            return true
        } else if (node.isInline) {
            return false
        }
        if (node && node.attrs.diffdata && node.attrs.diffdata.length > 0) {
            deco.push(
                prosemirror_view__rspack_import_5.Decoration.node(
                    pos,
                    pos + node.nodeSize,
                    {class: "selected-dec"},
                    {}
                )
            )
        }
    })
    return deco
}

function createDeletionHighlight(decos, from, to, state, options) {
    /* Creates a yellow coloured highlight decoration when the user
    tries to look at a deletion change in offline editor */

    decos.find(from + 1, to).forEach(deco => {
        const decoId = deco.spec.id
        if (decoId !== undefined) {
            const specDecoration = options.merge.mergeView2.dom.querySelector(
                '[data-decoid="' + decoId + '"]'
            )
            const parentEl = specDecoration.closest(".deletion-decoration")
            parentEl.querySelectorAll(".online-deleted").forEach(ele => {
                ele.classList.add("selected-dec")
                ele.classList.add("deletion-highlight")
            })
        }
    })
    const inlineDeco = prosemirror_view__rspack_import_5.Decoration.inline(
        from,
        to,
        {class: "selected-dec"},
        {type: "deletion-highlight"}
    )
    const deco = []
    deco.push(inlineDeco)
    state.doc.nodesBetween(from, to, (node, pos) => {
        if (
            pos < from ||
            ["bullet_list", "ordered_list"].includes(node.type.name)
        ) {
            return true
        } else if (node.isInline) {
            return false
        }
        if (node.attrs.diffdata) {
            deco.push(
                prosemirror_view__rspack_import_5.Decoration.node(
                    pos,
                    pos + node.nodeSize,
                    {class: "selected-dec"},
                    {type: "deletion-highlight"}
                )
            )
        }
    })
    decos = decos.add(state.doc, deco)
    return decos
}
function createLinkDropUp(mark) {
    const dom = document.createElement("span")
    dom.classList.add("link-drop-up-outer")
    dom.innerHTML = (0,_common__rspack_import_0.noSpaceTmp)`
        <div class="link-drop-up-inner">
            <span>Link:${mark.attrs.href}</span>
        </div>
        `
    return dom
}

function getDecos(decos, merge, state) {
    /* Creates PM deco for the change popup */
    const $head = state.selection.$head
    const currentMarks = [],
        diffMark = $head.marks().find(mark => mark.type.name === "diffdata")
    const linkMark = $head.marks().find(mark => mark.type.name === "link")

    decos = decos.remove(
        decos.find(null, null, spec => {
            if (spec.type && spec.type == "deletion") {
                return false
            } else {
                return true
            }
        })
    )

    if (diffMark) {
        currentMarks.push(diffMark)
    }
    if (!currentMarks.length) {
        const node =
            state.selection instanceof prosemirror_state__rspack_import_6.NodeSelection
                ? state.selection.node
                : state.selection.$head.parent
        const markFound = {}
        if (node && node.attrs.diffdata && node.attrs.diffdata.length > 0) {
            markFound["attrs"] = {}
            markFound["attrs"]["diff"] = node.attrs.diffdata[0].type
            markFound["attrs"]["from"] = node.attrs.diffdata[0].from
            markFound["attrs"]["to"] = node.attrs.diffdata[0].to
            markFound["attrs"]["steps"] = JSON.stringify(
                node.attrs.diffdata[0].steps
            )

            const startPos = $head.pos // position of block start.
            const dom = createDropUp(merge, markFound, linkMark),
                deco = prosemirror_view__rspack_import_5.Decoration.widget(startPos, dom)
            const highlightDecos = createHiglightDecoration(
                markFound["attrs"]["from"],
                markFound["attrs"]["to"],
                state
            )
            highlightDecos.push(deco)
            return decos.add(state.doc, highlightDecos)
        } else if (node.marks.find(mark => mark.type.name == "diffdata")) {
            const mark = node.marks.find(mark => mark.type.name == "diffdata")
            const startPos = $head.pos // position of block start.
            const dom = createDropUp(merge, mark, linkMark),
                deco = prosemirror_view__rspack_import_5.Decoration.widget(startPos, dom)
            const highlightDecos = createHiglightDecoration(
                mark.attrs.from,
                mark.attrs.to,
                state
            )
            highlightDecos.push(deco)
            return decos.add(state.doc, highlightDecos)
        } else if (linkMark) {
            const startPos = $head.pos
            const dom = createLinkDropUp(linkMark),
                deco = prosemirror_view__rspack_import_5.Decoration.widget(startPos, dom)
            return decos.add(state.doc, [deco])
        }
        return decos
    }
    const startPos = diffMark.attrs.to
    const dom = createDropUp(merge, diffMark, linkMark),
        deco = prosemirror_view__rspack_import_5.Decoration.widget(startPos, dom)
    const highlightDecos = createHiglightDecoration(
        diffMark.attrs.from,
        diffMark.attrs.to,
        state
    )
    highlightDecos.push(deco)
    return decos.add(state.doc, highlightDecos)
}

function deletionDecorations(decos, merge, doc, tr, deletionClass) {
    let index = 0
    let stepsTrackedByChangeset = []
    const changeset = new _changeset__rspack_import_1.changeSet(tr).getChangeSet(),
        schema = merge.schema,
        commonDoc = merge.cpDoc,
        mapping = tr.mapping
    changeset.changes.forEach(change => {
        if (change.deleted.length > 0) {
            let dom = document.createElement("span")
            const slice = commonDoc.slice(change.fromA, change.toA)

            // Apply the marks before trying to serialize!!!!
            let stepsInvolved = []
            change.deleted.forEach(deletion =>
                stepsInvolved.push(Number.parseInt(deletion.data.step))
            )
            const stepsSet = new Set(stepsInvolved)
            stepsInvolved = Array.from(stepsSet)
            stepsInvolved.sort((a, b) => a - b)
            stepsTrackedByChangeset =
                stepsTrackedByChangeset.concat(stepsInvolved)
            const deletionMark = schema.marks.diffdata.create({
                diff: deletionClass,
                steps: JSON.stringify(stepsInvolved),
                from: change.fromA,
                to: change.toA,
                markOnly: false
            })

            // Slice with marked contents
            const content = (0,_tools__rspack_import_3.addDeletionMarks)(slice, deletionMark, schema)
            const deletedContent =
                prosemirror_model__rspack_import_7.DOMSerializer.fromSchema(schema).serializeFragment(content)

            // Parse HTML to accomodate minor changes
            if (deletedContent.querySelector("tr,td")) {
                dom = document.createElement("tbody")
                dom.appendChild(deletedContent)
            } else {
                dom.appendChild(deletedContent)
            }
            dom.querySelectorAll("span.footnote-marker").forEach(
                footnoteElement => {
                    const newFnElement = (0,_footnotes__rspack_import_2.readOnlyFnEditor)(footnoteElement)
                    newFnElement.classList.add("deleted-footnote-element")
                    newFnElement.classList.add(deletionClass)
                    footnoteElement.parentNode.appendChild(newFnElement)
                    footnoteElement.remove()
                }
            )
            dom.classList.add("deletion-decoration")
            dom.dataset.delfrom = change.fromA
            dom.dataset.delto = change.toA

            const dropUp = createDropUp(merge, deletionMark, undefined)
            dropUp.dataset.decoid = index
            dropUp.style.display = "none"
            dom.appendChild(dropUp)

            // Put decoration in proper place. In case of a footnote change, the original content
            // is put first, decoration is shown after the content
            let pos = mapping.map(change.fromA)
            if (change.lenA == change.lenB && stepsInvolved.length == 1) {
                const JSONSlice = slice.toJSON()
                if (
                    JSONSlice.content &&
                    JSONSlice.content.length == 1 &&
                    JSONSlice.content[0].type === "footnote"
                ) {
                    pos += 1
                }
            }
            decos = decos.add(doc, [
                prosemirror_view__rspack_import_5.Decoration.widget(pos, dom, {type: "deletion", id: index})
            ])
            index += 1
        }
    })
    if (deletionClass == "offline-deleted") {
        merge.offlineTrackedSteps = merge.offlineTrackedSteps.concat(
            stepsTrackedByChangeset
        )
    }
    return decos
}

function createDropUp(merge, diffMark, linkMark) {
    /* The actual function that creates a drop up */
    const dropUp = document.createElement("span"),
        requiredPx = 10,
        tr =
            diffMark.attrs.diff.search("offline") != -1
                ? merge.offlineTr
                : merge.onlineTr,
        trType =
            diffMark.attrs.diff.search("offline") != -1 ? "offline" : "online",
        opType =
            diffMark.attrs.diff.search("inserted") != -1
                ? "insertion"
                : "deletion"
    let textToBeDisplayed = ""

    if (diffMark.attrs.markOnly) {
        textToBeDisplayed = gettext("Format Change")
    } else {
        if (trType == "online") {
            if (opType == "insertion") {
                textToBeDisplayed = gettext("Inserted by online users")
            } else {
                textToBeDisplayed = gettext("Deleted by online users")
            }
        } else {
            if (opType == "insertion") {
                textToBeDisplayed = gettext("Inserted by you")
            } else {
                textToBeDisplayed = gettext("Deleted by you")
            }
        }
    }
    linkMark = linkMark === undefined ? false : linkMark
    dropUp.classList.add("drop-up-outer")
    dropUp.innerHTML = (0,_common__rspack_import_0.noSpaceTmp)`
        <div class="link drop-up-inner" style="top: -${requiredPx}px;">
            ${
                diffMark
                    ? `<div class="drop-up-head">
                    ${
                        diffMark.attrs.diff
                            ? `<div class="link-title">${gettext("Change")}:&nbsp; ${textToBeDisplayed}</div>`
                            : ""
                    }
                    ${
                        linkMark
                            ? `<div> Link : ${linkMark.attrs.href}</div>`
                            : ""
                    }
                    ${
                        linkMark
                            ? `<div> Type : ${linkMark.attrs.href[0] == "#" ? "internal" : "external"}</div>`
                            : ""
                    }
                </div>
                <ul class="drop-up-options">
                    <li class="accept-change" title="${gettext("Accept change")}">
                        ${gettext("Accept Change")}
                    </li>
                    <li class="reject-change" title="${gettext("Reject change")}">
                        ${gettext("Reject Change")}
                    </li>
                    <li class="copy-data" title="${gettext("Copy content")}">
                        ${gettext("Copy")}
                    </li>
                </ul>`
                    : ""
            }
        </div>`

    const acceptChange = dropUp.querySelector(".accept-change")
    if (acceptChange) {
        acceptChange.addEventListener("mousedown", event => {
            event.preventDefault()
            event.stopImmediatePropagation()
            try {
                if (trType == "online") {
                    if (opType == "insertion") {
                        (0,_tools__rspack_import_3.dispatchRemoveDiffdata)(
                            merge.mergeView2,
                            diffMark.attrs.from,
                            diffMark.attrs.to
                        )
                    } else {
                        // remove online deletion decoration
                        const decorationId = dropUp.dataset.decoid
                        ;(0,_action__rspack_import_4.removeDecoration)(merge.mergeView2, decorationId)
                    }
                } else {
                    if (opType == "insertion") {
                        (0,_action__rspack_import_4.acceptChanges)(
                            merge,
                            diffMark,
                            merge.mergeView2,
                            merge.mergeView3,
                            tr
                        )
                    } else {
                        // remove offline deletion decoration
                        const decorationId = dropUp.dataset.decoid
                        if ((0,_action__rspack_import_4.deleteContent)(merge, merge.mergeView2, diffMark)) {
                            merge.mergeView2.dispatch(
                                merge.mergeView2.state.tr.setMeta(
                                    "removeHighlight",
                                    true
                                )
                            )
                            ;(0,_action__rspack_import_4.removeDecoration)(merge.mergeView3, decorationId)
                        }
                    }
                }
            } catch (error) {
                const onlineDoc = merge.editor.schema.nodeFromJSON(
                    (0,_tools__rspack_import_3.removeDiffFromJson)(merge.onlineDoc.toJSON())
                )
                const offlineDoc = merge.editor.schema.nodeFromJSON(
                    (0,_tools__rspack_import_3.removeDiffFromJson)(merge.offlineDoc.toJSON())
                )

                // Handle merge failure
                merge.editor.mod.collab.doc.merge.handleMergeFailure(
                    error,
                    offlineDoc,
                    onlineDoc,
                    merge
                )
            }
        })
    }
    const rejectChange = dropUp.querySelector(".reject-change")
    if (rejectChange) {
        rejectChange.addEventListener("mousedown", event => {
            event.preventDefault()
            event.stopImmediatePropagation()
            try {
                if (trType == "online") {
                    if (opType == "insertion") {
                        // Delete inserted content
                        if (diffMark.attrs.markOnly) {
                            (0,_action__rspack_import_4.handleMarks)(
                                merge.mergeView2,
                                diffMark,
                                tr,
                                merge.schema
                            )
                            ;(0,_tools__rspack_import_3.dispatchRemoveDiffdata)(
                                merge.mergeView2,
                                diffMark.attrs.from,
                                diffMark.attrs.to
                            )
                        } else {
                            (0,_action__rspack_import_4.deleteContent)(
                                merge,
                                merge.mergeView2,
                                diffMark,
                                false
                            )
                        }
                    } else {
                        // remove online deletion decoration
                        if (
                            (0,_action__rspack_import_4.addDeletedContentBack)(
                                merge,
                                merge.mergeView2,
                                diffMark
                            )
                        ) {
                            const decorationId = dropUp.dataset.decoid
                            ;(0,_action__rspack_import_4.removeDecoration)(merge.mergeView2, decorationId)
                        }
                    }
                } else {
                    if (opType == "insertion") {
                        (0,_tools__rspack_import_3.dispatchRemoveDiffdata)(
                            merge.mergeView3,
                            diffMark.attrs.from,
                            diffMark.attrs.to
                        )
                    } else {
                        // remove offline deletion decoration
                        dropUp.parentNode.classList.remove("offline-deleted")
                        dropUp.parentNode.classList.remove(
                            "deletion-decoration"
                        )
                        dropUp.parentNode
                            .querySelectorAll(".offline-deleted")
                            .forEach(ele => {
                                ele.classList.remove("offline-deleted")
                                ele.classList.remove("selected-dec")
                            })
                        dropUp.remove()
                        merge.mergeView2.dispatch(
                            merge.mergeView2.state.tr.setMeta(
                                "removeHighlight",
                                true
                            )
                        )
                    }
                }
            } catch (error) {
                const onlineDoc = merge.editor.schema.nodeFromJSON(
                    (0,_tools__rspack_import_3.removeDiffFromJson)(merge.onlineDoc.toJSON())
                )
                const offlineDoc = merge.editor.schema.nodeFromJSON(
                    (0,_tools__rspack_import_3.removeDiffFromJson)(merge.offlineDoc.toJSON())
                )

                // Handle merge failure
                merge.editor.mod.collab.doc.merge.handleMergeFailure(
                    error,
                    offlineDoc,
                    onlineDoc,
                    merge
                )
            }
        })
    }

    const copyData = dropUp.querySelector(".copy-data")
    if (copyData) {
        copyData.addEventListener("mousedown", event => {
            event.preventDefault()
            event.stopImmediatePropagation()
            if (trType == "online") {
                if (opType == "insertion") {
                    (0,_action__rspack_import_4.copyChange)(
                        merge.mergeView2,
                        diffMark.attrs.from,
                        diffMark.attrs.to
                    )
                } else {
                    (0,_action__rspack_import_4.copyChange)(
                        merge.mergeView1,
                        diffMark.attrs.from,
                        diffMark.attrs.to
                    )
                }
            } else {
                if (opType == "insertion") {
                    (0,_action__rspack_import_4.copyChange)(
                        merge.mergeView3,
                        diffMark.attrs.from,
                        diffMark.attrs.to
                    )
                } else {
                    (0,_action__rspack_import_4.copyChange)(
                        merge.mergeView1,
                        diffMark.attrs.from,
                        diffMark.attrs.to
                    )
                }
            }
        })
    }
    return dropUp
}

const key = new prosemirror_state__rspack_import_6.PluginKey("mergeDiff")

const diffPlugin = options =>
    new prosemirror_state__rspack_import_6.Plugin({
        key,
        state: {
            init(state) {
                let baseTr = false
                let deletionClass = false
                let decos = prosemirror_view__rspack_import_5.DecorationSet.empty
                if (state.doc.eq(options.merge.offlineDoc)) {
                    baseTr = options.merge.offlineTr
                    deletionClass = "offline-deleted"
                } else if (state.doc.eq(options.merge.onlineDoc)) {
                    baseTr = options.merge.onlineTr
                    deletionClass = "online-deleted"
                }
                if (baseTr) {
                    decos = deletionDecorations(
                        decos,
                        options.merge,
                        state.doc,
                        baseTr,
                        deletionClass
                    )
                }
                return {
                    baseTr: baseTr,
                    deletionClass: deletionClass,
                    decos: decos
                }
            },
            apply(tr, _prev, oldState, state) {
                let {decos} = this.getState(oldState)
                const {baseTr, deletionClass} = this.getState(oldState)

                decos = getDecos(decos, options.merge, state)

                if (tr.getMeta("removeHighlight")) {
                    decos = decos.remove(
                        decos.find(
                            null,
                            null,
                            spec => spec.type == "deletion-highlight"
                        )
                    )

                    // Remove the class set on deletion decorations
                    options.merge.mergeView2.dom
                        .querySelectorAll(".selected-dec.deletion-highlight")
                        .forEach(ele => {
                            ele.classList.remove("selected-dec")
                            ele.classList.remove("deletion-highlight")
                        })
                }
                if (tr.getMeta("decorationId")) {
                    const decorationId = Number.parseInt(
                        tr.getMeta("decorationId")
                    )
                    decos = decos.remove(
                        decos.find(null, null, spec => spec.id == decorationId)
                    )
                }
                if (tr.getMeta("highlight")) {
                    const data = tr.getMeta("highlight")
                    const from = options.merge.mergedDocMap.map(
                        Number.parseInt(data.from)
                    )
                    const to = options.merge.mergedDocMap.map(
                        Number.parseInt(data.to)
                    )
                    if (from && to) {
                        decos = createDeletionHighlight(
                            decos,
                            from,
                            to,
                            state,
                            options
                        )
                    }
                }

                if (tr.getMeta("initialDiffMap")) {
                    // If it is initial diffMap we update mark data
                    // So no need to update the deco's position.
                    decos = decos.map(new prosemirror_transform__rspack_import_8.Mapping(), tr.doc)
                } else {
                    decos = decos.map(tr.mapping, tr.doc)
                }
                return {
                    baseTr: baseTr,
                    deletionClass: deletionClass,
                    decos: decos
                }
            }
        },
        props: {
            handleClick: (view, _pos, event) => {
                const $pos = view.state.doc.resolve(_pos)
                if (
                    $pos.node &&
                    $pos.parent &&
                    $pos.parent.type.name == "figure"
                ) {
                    // If the click is on a Fig element set up a node selection
                    // so that accept/reject options are shown properly.
                    const tr = view.state.tr
                    const $updatedPos = view.state.doc.resolve(
                        _pos - ($pos.parentOffset + 1)
                    )
                    tr.setSelection(new prosemirror_state__rspack_import_6.NodeSelection($updatedPos))
                    view.dispatch(tr)
                }

                const delDeco = view.dom.querySelectorAll(
                    ".offline-deleted,.online-deleted"
                )
                if (delDeco) {
                    delDeco.forEach(item =>
                        item.classList.remove("selected-dec")
                    )
                }
                const delPopUp = view.dom.querySelectorAll(
                    ".deletion-decoration .drop-up-outer"
                )
                if (delPopUp) {
                    delPopUp.forEach(popUp => (popUp.style.display = "none"))
                }
                const delFnToolTip = view.dom.querySelectorAll(
                    ".deleted-footnote-element"
                )
                if (delFnToolTip) {
                    delFnToolTip.forEach(
                        tooltip =>
                            (tooltip.childNodes[0].style.display = "none")
                    )
                }
                options.merge.mergeView2.dispatch(
                    options.merge.mergeView2.state.tr.setMeta(
                        "removeHighlight",
                        true
                    )
                )
                if (event.target.closest(".offline-deleted")) {
                    const parentEl = event.target.closest(
                        ".deletion-decoration"
                    )
                    const highlightEle =
                        parentEl.querySelectorAll(".offline-deleted")
                    if (highlightEle) {
                        highlightEle.forEach(ele =>
                            ele.classList.add("selected-dec")
                        )
                    }
                    parentEl.querySelector(".drop-up-outer").style.display =
                        "block"

                    // Add a decoration to highlight decoration to the online/merged view
                    options.merge.mergeView2.dispatch(
                        options.merge.mergeView2.state.tr.setMeta("highlight", {
                            from: parentEl.dataset.delfrom,
                            to: parentEl.dataset.delto
                        })
                    )
                } else if (event.target.closest(".online-deleted")) {
                    const parentEl = event.target.closest(
                        ".deletion-decoration"
                    )
                    const highlightEle =
                        parentEl.querySelectorAll(".online-deleted")
                    if (highlightEle) {
                        highlightEle.forEach(ele =>
                            ele.classList.add("selected-dec")
                        )
                    }
                    parentEl.querySelector(".drop-up-outer").style.display =
                        "block"
                }
                if (event.target.matches(".deleted-footnote-element")) {
                    event.target.childNodes[0].style.display = "block"
                }
            },
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        },
        view(_view) {
            return {
                update: view => {
                    // Make sure that pop stays inside the view.
                    const changePopUp = view.dom.querySelector(".drop-up-outer")
                    if (changePopUp) {
                        const bounding = changePopUp.getBoundingClientRect()
                        const dialogBox =
                            document.querySelector("#editor-merge-view")
                        if (dialogBox) {
                            if (
                                bounding.right > dialogBox.offsetWidth ||
                                bounding.right >
                                    (window.innerWidth ||
                                        document.documentElement.clientWidth)
                            ) {
                                changePopUp.style.left = "100px"
                            }
                        }
                    }

                    // Make sure that the deletion decoration footnote stays inside the view
                    const footnote = view.dom.querySelector(
                        ".offline-deleted.deleted-footnote-element .footnote-tooltip"
                    )
                    if (footnote) {
                        const bounding = footnote.getBoundingClientRect()
                        const dialogBox =
                            document.querySelector("#editor-merge-view")
                        if (dialogBox) {
                            if (
                                bounding.right > dialogBox.offsetWidth ||
                                bounding.right >
                                    (window.innerWidth ||
                                        document.documentElement.clientWidth)
                            ) {
                                footnote.style.left = "-100px"
                            }
                        }
                    }
                }
            }
        },
        appendTransaction: (trs, _oldState, newState) => {
            if (trs.every(tr => !tr.steps.length)) {
                return
            }
            const updateMarkTr = newState.tr
            trs.forEach(tr => {
                if (tr.steps.length) {
                    (0,_tools__rspack_import_3.updateMarkData)(
                        tr,
                        options.merge.imageDataModified,
                        updateMarkTr
                    )
                }
            })
            updateMarkTr.setMeta("initialDiffMap", true)
            return updateMarkTr
        }
    })


}),
"./js/modules/editor/collab/merge/state_plugins/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  clipboardPlugin: function() { return /* reexport safe */ _clipboard__rspack_import_0.clipboardPlugin; },
  diffPlugin: function() { return /* reexport safe */ _diff__rspack_import_1.diffPlugin; }
});
/* import */ var _clipboard__rspack_import_0 = __webpack_require__("./js/modules/editor/collab/merge/state_plugins/clipboard.js");
/* import */ var _diff__rspack_import_1 = __webpack_require__("./js/modules/editor/collab/merge/state_plugins/diff.js");




}),
"./js/modules/editor/collab/merge/tools.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  addDeletionMarks: function() { return addDeletionMarks; },
  checkPresenceOfdiffdata: function() { return checkPresenceOfdiffdata; },
  dispatchRemoveDiffdata: function() { return dispatchRemoveDiffdata; },
  removeDiffFromJson: function() { return removeDiffFromJson; },
  removeDiffdata: function() { return removeDiffdata; },
  simplifyTransform: function() { return simplifyTransform; },
  updateMarkData: function() { return updateMarkData; }
});
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");




const checkPresenceOfdiffdata = (doc, from, to) => {
    /* This function checks whether diff mark is present inside the given range */
    let diffAttrPresent = false
    if (doc.rangeHasMark(from, to, doc.type.schema.marks.diffdata)) {
        return true
    }
    doc.nodesBetween(from, to, (node, _pos) => {
        if (node.attrs.diffdata && node.attrs.diffdata.length > 0) {
            diffAttrPresent = true
        }
    })
    return diffAttrPresent
}

const simplifyTransform = tr => {
    /* This splits complex insertion & Deletion steps into simple insertion and deletion
    steps */
    if (tr.docChanged && tr.docs.length > 0) {
        const trState = prosemirror_state__rspack_import_0.EditorState.create({doc: tr.docs[0]})
        const newTr = trState.tr
        for (let index = 0; index < tr.steps.length; index++) {
            const step = tr.steps[index]
            if (step instanceof prosemirror_transform__rspack_import_1.ReplaceStep && step.from !== step.to) {
                const modifiedStep = step.slice.size
                    ? new prosemirror_transform__rspack_import_1.ReplaceStep(
                          step.to, // We insert all the same steps, but with "from"/"to" both set to "to" in order not to delete content. Mapped as needed.
                          step.to,
                          step.slice,
                          step.structure
                      )
                    : false
                if (modifiedStep) {
                    // If while breaking down any step the step fails , we return the original tr (we just split steps containing both insertions and deletions into simple steps which does just insertion/deletion. should not make a big difference.)
                    if (newTr.maybeStep(modifiedStep).failed) {
                        return tr
                    }
                    if (
                        newTr.maybeStep(
                            new prosemirror_transform__rspack_import_1.ReplaceStep(
                                step.from,
                                step.to,
                                prosemirror_model__rspack_import_2.Slice.empty,
                                step.structure
                            )
                        ).failed
                    ) {
                        return tr
                    }
                } else {
                    if (newTr.maybeStep(step).failed) {
                        return tr
                    }
                }
            } else {
                if (newTr.maybeStep(step).failed) {
                    return tr
                }
            }
        }
        return newTr
    } else {
        return tr
    }
}

const removeDiffdata = (tr, from, to) => {
    /* Adds steps to a tr to remove all the diff marks in the given range. */
    tr.doc.nodesBetween(from, to, (node, pos) => {
        if (
            pos < from ||
            ["bullet_list", "ordered_list"].includes(node.type.name)
        ) {
            return true
        } else if (node.isInline) {
            return false
        }
        if (node.attrs.diffdata && node.attrs.diffdata.length > 0) {
            tr.setNodeMarkup(
                pos,
                null,
                Object.assign({}, node.attrs, {diffdata: []}),
                node.marks
            )
        }
    })
    tr.removeMark(from, to, tr.doc.type.schema.marks.diffdata)
    return tr
}

const dispatchRemoveDiffdata = (view, from, to) => {
    const tr = removeDiffdata(view.state.tr, from, to)
    tr.setMeta("initialDiffMap", true).setMeta("mapAppended", true)
    tr.setMeta("notrack", true)
    view.dispatch(tr)
}

const updateMarkData = (tr, imageDataModified, newTr) => {
    /* Update the range inside the marks and also if we have a image that
    was reuploaded , then while accepting it into the middle editor,
    update its attrs */
    const initialdiffMap = tr.getMeta("initialDiffMap")
    if (!initialdiffMap && (tr.steps.length > 0 || tr.docChanged)) {
        tr.doc.nodesBetween(0, tr.doc.content.size, (node, pos) => {
            if (["bullet_list", "ordered_list"].includes(node.type.name)) {
                return true
            } else if (node.isInline) {
                let diffMark = node.marks.find(
                    mark => mark.type.name == "diffdata"
                )
                if (diffMark !== undefined) {
                    diffMark = diffMark.attrs
                    newTr.removeMark(
                        pos,
                        pos + node.nodeSize,
                        tr.doc.type.schema.marks.diffdata
                    )
                    const from = tr.mapping.map(diffMark.from)
                    const to = tr.mapping.map(diffMark.to, -1)
                    const mark = tr.doc.type.schema.marks.diffdata.create({
                        diff: diffMark.diff,
                        steps: diffMark.steps,
                        from: from,
                        to: to,
                        markOnly: diffMark.markOnly
                    })
                    newTr.addMark(pos, pos + node.nodeSize, mark)
                }
            }
            if (
                node.type.name === "image" &&
                Object.keys(imageDataModified).includes(
                    String(node.attrs.image)
                )
            ) {
                const attrs = Object.assign({}, node.attrs)
                attrs["image"] = imageDataModified[String(node.attrs.image)]
                const nodeType = tr.doc.type.schema.nodes["image"]
                newTr.setNodeMarkup(pos, nodeType, attrs)
            }
            if (node.attrs.diffdata && node.attrs.diffdata.length > 0) {
                const diffdata = node.attrs.diffdata
                diffdata[0].from = tr.mapping.map(diffdata[0].from)
                diffdata[0].to = tr.mapping.map(diffdata[0].to)
                newTr.setNodeMarkup(
                    pos,
                    null,
                    Object.assign({}, node.attrs, {diffdata}),
                    node.marks
                )
            }
        })
    }
    return newTr
}

const removeDiffFromJson = object => {
    /* Used to convert a document from the merge editor to a doc that complies with the schema of the main editor */
    if (object.attrs && object.attrs.diffdata) {
        delete object.attrs.diffdata
    }
    if (object.marks) {
        object.marks = object.marks.filter(mark => mark.type !== "diffdata")
    }
    if (object.content) {
        object.content.forEach(child => removeDiffFromJson(child))
    }
    return object
}

function mapFragment(fragment, f, parent, mark) {
    const mapped = []
    for (let i = 0; i < fragment.childCount; i++) {
        let child = fragment.child(i)
        if (child.attrs.diffdata) {
            const diffdata = []
            diffdata.push({
                type: mark.attrs.diff,
                from: mark.attrs.from,
                to: mark.attrs.to,
                steps: mark.attrs.steps
            })
            const attrs = Object.assign({}, child.attrs, {diffdata})
            const dummyNode = child.type.create(attrs, null, child.marks)
            child = dummyNode.copy(child.content)
        }
        if (child.content.size) {
            child = child.copy(mapFragment(child.content, f, child, mark))
        }
        if (child.isInline) {
            child = f(child, parent, i)
        }
        mapped.push(child)
    }
    return prosemirror_model__rspack_import_2.Fragment.fromArray(mapped)
}

const addDeletionMarks = (slice, mark) => {
    const content = mapFragment(
        slice.content,
        (node, parent) => {
            if (parent.type && !parent.type.allowsMarkType(mark.type)) {
                return node
            }
            if (node.isInline) {
                return node.mark(mark.addToSet(node.marks))
            }
            return node
        },
        parent,
        mark
    )
    return content
}


}),
"./js/modules/editor/collab/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  messageTemplate: function() { return messageTemplate; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");

const messageTemplate = ({message, theChatter}) =>
    `<div class="message" id="m${message.id}">
        <div class="comment-user">
            ${(0,_common__rspack_import_0.avatarTemplate)({user: theChatter})}
            <h5 class="comment-user-name">${(0,_common__rspack_import_0.escapeText)(theChatter.name)}</h5>
            <p class="comment-date">${(0,_common__rspack_import_0.localizeDate)(new Date())}</p>
        </div>
        <div class="message-body">${(0,_common__rspack_import_0.escapeText)(message.body)}</div>
    </div>`


}),
"./js/modules/editor/comments/comment.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  Comment: function() { return Comment; }
});
class Comment {
    constructor({
        id,
        user,
        username,
        assignedUser = false,
        assignedUsername = false,
        date,
        comment = [],
        answers = [],
        isMajor = false,
        resolved = false,
        hidden = false
    }) {
        this.id = id
        this.user = user // User ID of user who wrote comment
        this.username = username // Username of user who wrote comment
        this.assignedUser = assignedUser // User ID of user who is currently assigned to comment (if any).
        this.assignedUsername = assignedUsername // Username of user who is currently assigned to comment (if any).
        this.date = date
        this.comment = comment
        this.answers = answers
        this.isMajor = isMajor // boolean - Whether comment is of major importance.
        this.resolved = resolved // Whether comment is marked as resolved.
        this.hidden = hidden
    }
}


}),
"./js/modules/editor/comments/editors/answer.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CommentAnswerEditor: function() { return CommentAnswerEditor; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _comment__rspack_import_0 = __webpack_require__("./js/modules/editor/comments/editors/comment.js");



class CommentAnswerEditor extends _comment__rspack_import_0.CommentEditor {
    constructor(mod, id, dom, text, options = {}) {
        super(mod, id, dom, text, options)

        this.keepOpenAfterSubmit = true
    }

    initViewDOM() {
        this.viewDOM = document.createElement("div")
        this.viewDOM.classList.add("ProseMirror-wrapper")
        this.dom.appendChild(this.viewDOM)
        this.dom.insertAdjacentHTML(
            "beforeend",
            `<div class="comment-btns">
                <button class="submit fw-button fw-dark disabled" type="submit">
                    ${this.options.answerId ? gettext("Edit") : gettext("Submit")}
                </button>
                <button class="cancel fw-button fw-orange" type="submit">
                    ${gettext("Cancel")}
                </button>
            </div>
            <div class="tagger"></div>`
        )
    }

    initView() {
        super.initView()
        if (this.options.answerId) {
            const selection = prosemirror_state__rspack_import_1.TextSelection.atEnd(this.view.state.doc)
            this.view.dispatch(
                this.view.state.tr.setSelection(selection).scrollIntoView()
            )
        }
    }

    submit() {
        const text = this.view.state.doc.toJSON().content
        if (!text) {
            return
        }
        if (this.options.answerId) {
            this.mod.interactions.submitAnswerUpdate(
                this.id,
                this.options.answerId,
                text
            )
        } else {
            this.mod.interactions.createNewAnswer(this.id, text)
        }
        this.sendNotifications()
    }
}


}),
"./js/modules/editor/comments/editors/comment.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CommentEditor: function() { return CommentEditor; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);
/* import */ var prosemirror_commands__rspack_import_7 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_history__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_state__rspack_import_9 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_suggestions__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-suggestions@0.2.0/node_modules/prosemirror-suggestions/src/index.js");
/* import */ var prosemirror_view__rspack_import_8 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _common__rspack_import_2 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _notify__rspack_import_3 = __webpack_require__("./js/modules/editor/comments/editors/notify.js");
/* import */ var _schema__rspack_import_4 = __webpack_require__("./js/modules/editor/comments/editors/schema.js");













class CommentEditor {
    constructor(mod, id, dom, text, options = {}) {
        this.mod = mod
        this.id = id
        this.dom = dom
        this.text = text
        this.options = options

        this.isMajor = this.options.isMajor

        this.keepOpenAfterSubmit = false
        this.selectedTag = 0
        this.userTaggerList = []
        this.plugins = [
            (0,prosemirror_history__rspack_import_5.history)(),
            (0,prosemirror_suggestions__rspack_import_1.suggestionsPlugin)({
                escapeOnSelectionChange: true,
                matcher: (0,prosemirror_suggestions__rspack_import_1.triggerCharacter)("@"),
                onEnter: args => {
                    this.selectedTag = 0
                    this.tagRange = args.range
                    const search = args.text.slice(1)
                    if (search.length) {
                        this.setUserTaggerList(search)
                        this.showUserTagger()
                    }
                },
                onChange: args => {
                    this.selectedTag = 0
                    this.tagRange = args.range
                    const search = args.text.slice(1)
                    if (search.length) {
                        this.setUserTaggerList(search)
                        this.showUserTagger()
                    }
                },
                onExit: _args => {
                    this.selectedTag = 0
                    this.removeTagger()
                },
                onKeyDown: ({event}) => {
                    if (event.key === "ArrowDown") {
                        if (this.userTaggerList.length > this.selectedTag + 1) {
                            this.selectedTag += 1
                            this.showUserTagger()
                        }
                        return true
                    } else if (event.key === "ArrowUp") {
                        if (this.selectedTag > 0) {
                            this.selectedTag -= 1
                            this.showUserTagger()
                        }
                        return true
                    } else if (event.key === "Enter") {
                        return this.selectUserTag()
                    }
                    return false
                },
                escapeKeys: ["Escape", "ArrowRight", "ArrowLeft"]
            }),
            (0,prosemirror_keymap__rspack_import_6.keymap)(prosemirror_commands__rspack_import_7.baseKeymap),
            (0,prosemirror_keymap__rspack_import_6.keymap)({
                "Mod-z": prosemirror_history__rspack_import_5.undo,
                "Mod-shift-z": prosemirror_history__rspack_import_5.undo,
                "Mod-y": prosemirror_history__rspack_import_5.redo,
                "Ctrl-Enter": () => this.submit()
            })
        ]
    }

    init() {
        this.initViewDOM()
        this.initView()
    }

    initViewDOM() {
        this.viewDOM = document.createElement("div")
        this.viewDOM.classList.add("ProseMirror-wrapper")
        this.dom.appendChild(this.viewDOM)
        this.dom.insertAdjacentHTML(
            "beforeend",
            `<input class="comment-is-major" type="checkbox" name="isMajor"
                ${this.options.isMajor ? "checked" : ""}/>
            <label>${gettext("High priority")}</label>
            <div class="comment-btns">
                <button class="submit fw-button fw-dark disabled" type="submit">
                    ${this.id !== "-1" ? gettext("Edit") : gettext("Submit")}
                </button>
                <button class="cancel fw-button fw-orange" type="submit">
                    ${gettext("Cancel")}
                </button>
            </div>
            <div class="tagger"></div>`
        )
    }

    initView() {
        this.view = new prosemirror_view__rspack_import_8.EditorView(this.viewDOM, {
            state: prosemirror_state__rspack_import_9.EditorState.create({
                schema: _schema__rspack_import_4.commentSchema,
                doc: _schema__rspack_import_4.commentSchema.nodeFromJSON({
                    type: "doc",
                    content: this.text
                }),
                plugins: this.plugins
            }),
            dispatchTransaction: tr => {
                const newState = this.view.state.apply(tr)
                this.view.updateState(newState)
                this.updateButtons()
            }
        })
        this.oldUserTags = this.getUserTags()
        this.bind()
    }

    bind() {
        this.dom.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_2.findTarget)(event, "button.submit:not(.disabled)", el):
                    this.submit()
                    if (this.keepOpenAfterSubmit) {
                        this.scrollToBottom()
                    } else {
                        this.mod.interactions.activeCommentId = false
                        this.mod.interactions.deactivateAll()
                        this.mod.interactions.collapseSelectionToEnd()
                    }
                    break
                case (0,_common__rspack_import_2.findTarget)(event, "button.cancel", el):
                    this.mod.interactions.cancelSubmit()
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".ProseMirror-wrapper", el):
                    this.view.focus()
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".tag-user", el):
                    this.selectedTag = Number.parseInt(el.target.dataset.index)
                    this.selectUserTag()
                    this.view.focus()
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".comment-is-major", el):
                    this.isMajor = !this.isMajor
                    this.updateButtons()
                    break
            }
        })
    }

    hasChanged() {
        return (
            !fast_deep_equal__rspack_import_0_default()(
                this.text.length ? this.text : [{type: "paragraph"}],
                this.view.state.doc.toJSON().content || [{type: "paragraph"}]
            ) || this.options.isMajor !== this.isMajor
        )
    }

    updateButtons() {
        if (this.hasChanged()) {
            this.dom.querySelector("button.submit").classList.remove("disabled")
        } else {
            this.dom.querySelector("button.submit").classList.add("disabled")
        }
    }

    submit() {
        const comment = this.view.state.doc.toJSON().content
        if (comment?.length > 0) {
            this.mod.interactions.updateComment({
                id: this.id,
                comment,
                isMajor: this.isMajor
            })
            this.sendNotifications()
        } else {
            this.mod.interactions.deleteComment(this.id)
        }
    }

    sendNotifications() {
        const newUserTags = this.getUserTags().filter(
            id => !this.oldUserTags.includes(id)
        )
        if (newUserTags.length) {
            const comment = this.view.state.doc,
                docId = this.mod.editor.docInfo.id
            newUserTags.forEach(userId =>
                (0,_notify__rspack_import_3.notifyMentionedUser)(docId, userId, comment)
            )
        }
    }

    setUserTaggerList(search) {
        const owner = this.mod.editor.docInfo.owner
        this.userTaggerList = owner.contacts
            .concat(owner)
            .filter(
                user =>
                    user.name.includes(search) || user.username.includes(search)
            )
    }

    showUserTagger() {
        if (!this.userTaggerList.length) {
            return
        }
        this.dom.querySelector("div.tagger").innerHTML = this.userTaggerList
            .map(
                (user, index) =>
                    `<div class="tag-user tag${index === this.selectedTag ? " selected" : ""}" data-index="${index}">
                ${user ? (0,_common__rspack_import_2.avatarTemplate)({user}) : '<span class="fw-string-avatar"></span>'}
                <h5 class="comment-user-name">${(0,_common__rspack_import_2.escapeText)(user.name)}</h5>
            </div>`
            )
            .join("")
    }

    selectUserTag() {
        const user = this.userTaggerList[this.selectedTag]
        if (!user || !this.tagRange) {
            return false
        }
        const tr = this.view.state.tr.replaceRangeWith(
            this.tagRange.from,
            this.tagRange.to,
            this.view.state.schema.nodes.collaborator.create({
                id: user.id,
                name: user.name
            })
        )
        this.view.dispatch(tr)
        return true
    }

    getUserTags() {
        const users = []
        this.view.state.doc.descendants(node => {
            if (node.type.name === "collaborator") {
                users.push(node.attrs.id)
            }
        })
        return [...new Set(users)] // only unique values.
    }

    removeTagger() {
        this.dom.querySelector("div.tagger").innerHTML = ""
        this.tagRange = false
        this.userTaggerList = []
    }

    scrollToBottom() {
        const activeMarginBox = document.querySelector(
            ".margin-box.comment.active .comment-answer-container"
        )
        if (activeMarginBox) {
            activeMarginBox.scrollTop = activeMarginBox.scrollHeight
        }

        // scroll to bottom of the margin-box-container when new comments are added when the screens width is less
        // than 1024px
        const currentScreenWidth =
            window.innerWidth ||
            document.documentElement.clientWidth ||
            document.body.clientWidth
        if (currentScreenWidth < 1024) {
            const activeMarginBoxContainer = document.querySelector(
                "#margin-box-container"
            )
            if (activeMarginBoxContainer) {
                activeMarginBoxContainer.scrollTop =
                    activeMarginBoxContainer.scrollHeight
            }
        }
    }
}


}),
"./js/modules/editor/comments/editors/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CommentAnswerEditor: function() { return /* reexport safe */ _answer__rspack_import_1.CommentAnswerEditor; },
  CommentEditor: function() { return /* reexport safe */ _comment__rspack_import_0.CommentEditor; },
  serializeComment: function() { return /* reexport safe */ _schema__rspack_import_2.serializeComment; }
});
/* import */ var _comment__rspack_import_0 = __webpack_require__("./js/modules/editor/comments/editors/comment.js");
/* import */ var _answer__rspack_import_1 = __webpack_require__("./js/modules/editor/comments/editors/answer.js");
/* import */ var _schema__rspack_import_2 = __webpack_require__("./js/modules/editor/comments/editors/schema.js");






}),
"./js/modules/editor/comments/editors/notify.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  notifyMentionedUser: function() { return notifyMentionedUser; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema__rspack_import_1 = __webpack_require__("./js/modules/editor/comments/editors/schema.js");



const notifyMentionedUser = (docId, userId, comment) => {
    const {html, text} = (0,_schema__rspack_import_1.serializeCommentNode)(comment)
    return (0,_common__rspack_import_0.jsonPost)("/api/document/comment_notify/", {
        doc_id: docId,
        collaborator_id: userId,
        comment_html: html,
        comment_text: text,
        type: "mention"
    })
}


}),
"./js/modules/editor/comments/editors/schema.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  commentSchema: function() { return commentSchema; },
  serializeComment: function() { return serializeComment; },
  serializeCommentNode: function() { return serializeCommentNode; }
});
/* import */ var prosemirror_model__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_schema_basic__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-basic@1.2.4/node_modules/prosemirror-schema-basic/dist/index.js");



const collaborator = {
    inline: true,
    group: "inline",
    attrs: {
        name: {
            default: ""
        },
        id: {
            default: 0
        }
    },
    parseDOM: [
        {
            tag: "span.collaborator",
            getAttrs(dom) {
                return {
                    username: dom.dataset.name,
                    id: Number.parseInt(dom.dataset.id)
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: "collaborator",
                "data-name": node.attrs.name,
                "data-id": node.attrs.id
            },
            node.attrs.name
        ]
    }
}

const doc = {
    content: "block+",
    toDOM(_node) {
        return ["div", 0]
    }
}

const commentSchema = new prosemirror_model__rspack_import_0.Schema({
    nodes: {
        doc,
        paragraph: prosemirror_schema_basic__rspack_import_1.nodes.paragraph,
        text: prosemirror_schema_basic__rspack_import_1.nodes.text,
        collaborator
    },
    marks: {}
})

const serializeCommentNode = pmNode => {
    const serializer = prosemirror_model__rspack_import_0.DOMSerializer.fromSchema(commentSchema),
        dom = serializer.serializeNode(pmNode)
    return {html: dom.innerHTML, text: dom.innerText}
}

const serializeComment = content => {
    const pmNode = commentSchema.nodeFromJSON({type: "doc", content})
    return serializeCommentNode(pmNode)
}


}),
"./js/modules/editor/comments/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModComments: function() { return ModComments; }
});
/* import */ var _interactions__rspack_import_0 = __webpack_require__("./js/modules/editor/comments/interactions.js");
/* import */ var _store__rspack_import_1 = __webpack_require__("./js/modules/editor/comments/store.js");



class ModComments {
    constructor(editor) {
        editor.mod.comments = this
        this.editor = editor
        new _store__rspack_import_1.ModCommentStore(this)
        new _interactions__rspack_import_0.ModCommentInteractions(this)
    }
}


}),
"./js/modules/editor/comments/interactions.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCommentInteractions: function() { return ModCommentInteractions; }
});
/* import */ var prosemirror_gapcursor__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _state_plugins__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _editors__rspack_import_2 = __webpack_require__("./js/modules/editor/comments/editors/index.js");







/* Functions related to user interactions with comments */
class ModCommentInteractions {
    constructor(mod) {
        mod.interactions = this
        this.mod = mod
        this.activeCommentId = false
        this.activeCommentAnswerId = false
        this.editComment = false
        this.editor = false
        this.bindEvents()
    }

    bindEvents() {
        // Bind all the click events related to comments
        document.body.addEventListener("click", event => {
            const el = {}
            let id
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, ".edit-comment", el):
                    this.editComment = true
                    this.activeCommentAnswerId = false
                    id = el.target.dataset.id

                    if (this.activeCommentId !== id) {
                        this.deactivateSelectedChanges()
                        this.activeCommentId = id
                        this.editComment = true
                        this.updateDOM()
                    } else {
                        this.updateDOM()
                    }
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".edit-comment-answer", el):
                    this.editComment = false
                    this.editAnswer(
                        el.target.dataset.id,
                        el.target.dataset.answer
                    )
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".resolve-comment", el):
                    this.resolveComment(el.target.dataset.id)
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".recreate-comment", el):
                    this.recreateComment(el.target.dataset.id)
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".assign-comment", el):
                    this.assignComment(
                        el.target.dataset.id,
                        Number.parseInt(el.target.dataset.user),
                        el.target.dataset.username
                    )
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".unassign-comment", el):
                    this.unassignComment(el.target.dataset.id)
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".delete-comment", el):
                    this.deleteComment(el.target.dataset.id)
                    break
                case (0,_common__rspack_import_0.findTarget)(event, ".delete-comment-answer", el):
                    this.deleteCommentAnswer(
                        el.target.dataset.id,
                        el.target.dataset.answer
                    )
                    break
                default:
                    break
            }
        })
    }

    initEditor() {
        const commentEditorDOM = document.querySelector("#comment-editor"),
            answerEditorDOM = document.querySelector("#answer-editor")

        if (
            commentEditorDOM?.matches(":not(:empty)") ||
            answerEditorDOM?.matches(":not(:empty)")
        ) {
            // Editor has been set up already. Abort.
            return
        }

        if (!(commentEditorDOM || answerEditorDOM)) {
            this.editor = false
            return
        }
        const id = this.activeCommentId
        if (commentEditorDOM) {
            const value =
                id === "-1"
                    ? {text: [], isMajor: false}
                    : {
                          text: this.mod.store.comments[id].comment,
                          isMajor: this.mod.store.comments[id].isMajor
                      }
            this.editor = new _editors__rspack_import_2.CommentEditor(
                this.mod,
                id,
                commentEditorDOM,
                value.text,
                {isMajor: value.isMajor}
            )
        } else {
            const answerId = this.activeCommentAnswerId,
                text = answerId
                    ? this.mod.store.comments[id].answers.find(
                          answer => answer.id === answerId
                      ).answer
                    : []
            this.editor = new _editors__rspack_import_2.CommentAnswerEditor(
                this.mod,
                id,
                answerEditorDOM,
                text,
                {answerId}
            )
        }

        this.editor.init()
    }

    updateDOM() {
        this.mod.editor.mod.marginboxes.updateDOM()
        this.initEditor()
    }

    findCommentIds(node) {
        return node.marks
            .filter(mark => mark.type.name === "comment" && mark.attrs.id)
            .map(mark => mark.attrs.id)
    }

    findCommentsAt(node) {
        return this.findCommentIds(node).map(id =>
            this.mod.store.findComment(id)
        )
    }

    deactivateSelectedChanges() {
        const tr = (0,_state_plugins__rspack_import_1.deactivateAllSelectedChanges)(this.mod.editor.view.state.tr)
        if (tr) {
            this.mod.editor.view.dispatch(tr)
        }
        const fnTr = (0,_state_plugins__rspack_import_1.deactivateAllSelectedChanges)(
            this.mod.editor.mod.footnotes.fnEditor.view.state.tr
        )
        if (fnTr) {
            this.mod.editor.mod.footnotes.fnEditor.view.dispatch(fnTr)
        }
    }

    collapseSelectionToEnd() {
        const $pos = this.mod.editor.currentView.state.selection.$to
        const validTextSelection = $pos.parent.inlineContent
        const selection = validTextSelection
            ? new prosemirror_state__rspack_import_3.TextSelection($pos)
            : new prosemirror_gapcursor__rspack_import_4.GapCursor($pos)
        const tr = this.mod.editor.currentView.state.tr.setSelection(selection)
        if (tr) {
            this.mod.editor.currentView.dispatch(tr)
        }
    }

    activateComment(id) {
        this.deactivateAll()
        this.activeCommentId = id
        this.updateDOM()
    }

    deactivateAll() {
        this.activeCommentId = false
        this.editComment = false
        this.activeCommentAnswerId = false
        // If there is a comment currently under creation, remove it.
        this.mod.store.removeCommentDuringCreation()
    }

    // Activate the comments included in the selection or the comment where the
    // caret is placed, if the editor is in focus.
    activateSelectedComment(view) {
        const selection = view.state.selection
        let comments = []

        if (selection.empty) {
            const node = view.state.doc.nodeAt(selection.from)
            if (node) {
                comments = this.findCommentsAt(node)
            }
        } else {
            view.state.doc.nodesBetween(selection.from, selection.to, node => {
                if (!node.isInline) {
                    return
                }
                comments = comments.concat(this.findCommentsAt(node))
            })
        }

        if (comments.length) {
            if (this.activeCommentId !== comments[0].id) {
                this.activateComment(comments[0].id)
            }
        } else {
            this.deactivateAll()
            this.updateDOM()
        }
    }

    isCurrentlyEditing() {
        // Returns true if
        // A) a comment form is currently open
        // B) the comment answer edit form is currently open
        // C) part of a new answer has been written
        // D) the focus is currently in new answer text area of a comment
        // E) The comment options are open
        // F) a new comment form is about to be displayed, but the updateDOM
        // call has not yet been made.
        if (!this.activeCommentId) {
            return false
        }
        if (document.querySelector(".submit-comment-answer-edit")) {
            // a comment answer edit form is currently open
            return true
        }
        if (this.editor?.view && this.editor?.view.hasFocus()) {
            // There is currently focus in the comment (answer) form
            return true
        }
        if (this.editor?.hasChanged?.()) {
            // Part of a comment (answer) has been entered/changed.
            return true
        }
        if (document.querySelector("div.marginbox-options.fw-open")) {
            // A margin box options menu is open.
            return true
        }
        if (this.mod.store.commentDuringCreation.inDOM === false) {
            // A new comment is about to be created, but it has not
            // yet been added to the DOM.
            return true
        }
        return false
    }

    // Create a temporary empty comment for the current user that is not shared
    // with collaborators.
    createNewComment() {
        // Unhide comments if they had been hidden.
        this.mod.editor.mod.marginboxes.filterOptions.comments = true
        this.deactivateAll()
        this.mod.store.addCommentDuringCreation(this.mod.editor.currentView)
        this.activeCommentId = "-1"
        this.editComment = true
        this.updateDOM()
        this.editor.view.focus()
    }

    deleteComment(id) {
        if (id === "-1") {
            this.deactivateAll()
        } else {
            // Handle the deletion of a comment.
            this.mod.store.deleteComment(id, true)
        }
        this.updateDOM()
    }

    resolveComment(id) {
        this.mod.store.updateComment({id, resolved: true})
    }

    recreateComment(id) {
        if (this.editor) {
            this.editor.dom.childNodes.forEach(node => {
                if (node.classList && node.classList.contains("comment-btns")) {
                    node.childNodes.forEach(buttons => {
                        if (
                            buttons.classList &&
                            buttons.classList.contains("submit") &&
                            buttons.classList.contains("disabled")
                        ) {
                            buttons.classList.remove("disabled")
                        }
                    })
                }
            })
        }
        this.mod.store.updateComment({id, resolved: false})
    }

    assignComment(id, user, username) {
        this.notifyAssignedUser(user, id)
        this.mod.store.updateComment({
            id,
            assignedUser: user,
            assignedUsername: username
        })
    }

    unassignComment(id) {
        this.mod.store.updateComment({
            id,
            assignedUser: false,
            assignedUsername: false
        })
    }

    notifyAssignedUser(user, id) {
        const comment = this.mod.store.findComment(id)
        const {html, text} = (0,_editors__rspack_import_2.serializeComment)(comment.comment)

        ;(0,_common__rspack_import_0.jsonPost)("/api/document/comment_notify/", {
            doc_id: this.mod.editor.docInfo.id,
            collaborator_id: user,
            comment_html: html,
            comment_text: text,
            type: "assign"
        })
    }

    updateComment({id, comment, isMajor}) {
        // Save the change to a comment and mark that the document has been changed
        if (id === "-1") {
            const referrer = (0,_state_plugins__rspack_import_1.getCommentDuringCreationDecoration)(
                this.mod.store.commentDuringCreation.view.state
            )
            // This is a new comment. We need to get an ID for it if it has content.

            let username

            if (
                ["review", "review-tracked"].includes(
                    this.mod.editor.docInfo.access_rights
                )
            ) {
                username = `${gettext("Reviewer")} ${this.mod.editor.user.id}`
            } else {
                username = this.mod.editor.user.username
            }

            this.mod.store.addComment(
                {
                    user: this.mod.editor.user.id,
                    username,
                    date: Date.now() - this.mod.editor.clientTimeAdjustment, // We update the time to the time the comment was stored
                    comment,
                    isMajor
                },
                referrer.from,
                referrer.to,
                this.mod.store.commentDuringCreation.view
            )
        } else {
            this.mod.store.updateComment({id, comment, isMajor})
        }
        this.deactivateAll()
        this.updateDOM()
    }

    cancelSubmit() {
        // Handle a click on the cancel button of the comment submit form.
        const id = this.activeCommentId
        if (id === "-1" || this.mod.store.comments[id]?.comment.length === 0) {
            this.deleteComment(id)
        } else {
            this.deactivateAll()
        }
        this.updateDOM()
    }

    deleteCommentAnswer(id, answerId) {
        // Handle the deletion of a comment answer.
        this.mod.store.deleteAnswer(id, answerId)
        this.deactivateAll()
        this.updateDOM()
    }

    editAnswer(id, answerId) {
        // Mark a specific answer to a comment as active, then layout the
        // comments, which will make that answer editable.
        this.activeCommentId = id
        this.activeCommentAnswerId = answerId
        this.updateDOM()
    }

    createNewAnswer(id, answerText) {
        // Create a new answer to add to the comment store

        let username

        if (
            ["review", "review-tracked"].includes(
                this.mod.editor.docInfo.access_rights
            )
        ) {
            username = `${gettext("Reviewer")} ${this.mod.editor.user.id}`
        } else {
            username = this.mod.editor.user.username
        }

        const answer = {
            answer: answerText,
            user: this.mod.editor.user.id,
            username,
            date: Date.now() - this.mod.editor.clientTimeAdjustment
        }

        this.mod.store.addAnswer(id, answer)

        this.deactivateAll()
        this.updateDOM()
        this.activateComment(id)
    }

    submitAnswerUpdate(id, answerId, commentText) {
        this.mod.store.updateAnswer(id, answerId, commentText)
        this.deactivateAll()
        this.updateDOM()
        this.activateComment(id)
    }
}


}),
"./js/modules/editor/comments/store.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModCommentStore: function() { return ModCommentStore; }
});
/* import */ var _schema_common__rspack_import_0 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _state_plugins__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _comment__rspack_import_2 = __webpack_require__("./js/modules/editor/comments/comment.js");




class ModCommentStore {
    constructor(mod) {
        mod.store = this
        this.mod = mod
        // a comment object for a comment that is still under construction
        this.commentDuringCreation = false
        this.reset()
    }

    reset() {
        this.comments = Object.create(null)
        this.unsent = []
    }

    findComment(id) {
        let found = false
        if (id in this.comments) {
            found = this.comments[id]
        }
        return found
    }

    mustSend() {
        // Set a timeout so that the update can be combines with other updates
        // if they happen more or less simultaneously.
        window.setTimeout(
            () => this.mod.editor.mod.collab.doc.sendToCollaborators(),
            100
        )
    }
    // Create a new temporary comment. This one is not going into the store yet,
    // as it is empty, shouldn't be shared and if canceled, it should go away
    // entirely.
    addCommentDuringCreation(view) {
        const state = view.state,
            tr = (0,_state_plugins__rspack_import_1.addCommentDuringCreationDecoration)(state, state.tr)

        if (!tr) {
            // adding decoration failed
            return
        }

        view.dispatch(tr)

        let username

        if (
            ["review", "review-tracked"].includes(
                this.mod.editor.docInfo.access_rights
            )
        ) {
            username = `${gettext("Reviewer")} ${this.mod.editor.user.id}`
        } else {
            username = this.mod.editor.user.username
        }

        this.commentDuringCreation = {
            comment: new _comment__rspack_import_2.Comment({
                id: "-1",
                user: this.mod.editor.user.id,
                username,
                date: Date.now() - this.mod.editor.clientTimeAdjustment
            }),
            inDOM: false,
            view
        }
    }

    removeCommentDuringCreation() {
        if (this.commentDuringCreation) {
            const view = this.commentDuringCreation.view
            this.commentDuringCreation = false
            const state = view.state
            const tr = (0,_state_plugins__rspack_import_1.removeCommentDuringCreationDecoration)(state, state.tr)
            if (tr) {
                view.dispatch(tr)
            }
        }
    }

    // Add a new comment to the comment database both remotely and locally.
    addComment(commentData, posFrom, posTo, view) {
        const id = (0,_schema_common__rspack_import_0.randomCommentId)(),
            markType = view.state.schema.marks.comment.create({id}),
            tr = this.addMark(view.state.tr, posFrom, posTo, markType)

        if (tr) {
            commentData.id = id
            commentData.answers = []
            this.addLocalComment(commentData, true)
            this.unsent.push({
                type: "create",
                id
            })
            view.dispatch(tr)
            this.mustSend()
        }
    }

    // Add marks to leaf nodes and inline nodes.
    addMark(tr, from, to, mark) {
        // add to inline nodes
        tr.addMark(from, to, mark)
        // add to leaf nodes
        tr.doc.nodesBetween(from, to, (node, pos, parent) => {
            if (!node.isLeaf) {
                return
            }
            const marks = node.marks
            if (!mark.isInSet(marks) && parent.type.allowsMarkType(mark.type)) {
                const newMarks = mark.addToSet(marks)
                tr.setNodeMarkup(pos, null, node.attrs, newMarks)
            }
        })
        if (!tr.steps.length) {
            return
        }
        return tr
    }

    removeMark(tr, from, to, mark) {
        // remove from inline nodes
        tr.removeMark(from, to, mark)
        // remove from leaf nodes
        tr.doc.nodesBetween(from, to, (node, pos) => {
            if (!node.isLeaf) {
                return
            }
            const marks = node.marks
            if (mark.isInSet(marks)) {
                const newMarks = mark.removeFromSet(marks)
                tr.setNodeMarkup(pos, null, node.attrs, newMarks)
            }
        })
    }

    loadComments(commentsData) {
        Object.entries(commentsData).forEach(([id, comment]) =>
            this.addLocalComment(Object.assign({id}, comment))
        )
    }

    addLocalComment(commentData, local) {
        // Don't add to pastParticipants if user is undefined (e.g. new unsaved comments)
        if (
            commentData.user !== undefined &&
            !this.mod.editor.mod.collab.pastParticipants.find(
                participant => participant.id === commentData.user
            )
        ) {
            this.mod.editor.mod.collab.pastParticipants.push({
                id: commentData.user,
                name: commentData.username
            })
        }
        if (
            commentData.assignedUser &&
            !this.mod.editor.mod.collab.pastParticipants.find(
                participant => participant.id === commentData.assignedUser
            )
        ) {
            this.mod.editor.mod.collab.pastParticipants.push({
                id: commentData.assignedUser,
                name: commentData.assignedUsername || ""
            })
        }
        if (!this.comments[commentData.id]) {
            this.comments[commentData.id] = new _comment__rspack_import_2.Comment(commentData)
        }
        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
        }
    }

    updateComment(commentData) {
        this.updateLocalComment(commentData, true)
        this.unsent.push({
            type: "update",
            id: commentData.id
        })
        this.mustSend()
    }

    updateLocalComment(commentData, local) {
        if (
            commentData.assignedUser &&
            !this.mod.editor.mod.collab.pastParticipants.find(
                participant => participant.id === commentData.assignedUser
            )
        ) {
            this.mod.editor.mod.collab.pastParticipants.push({
                id: commentData.assignedUser,
                name: commentData.assignedUsername || ""
            })
        }
        if (this.comments[commentData.id]) {
            Object.assign(this.comments[commentData.id], commentData)
        }
        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
        }
    }

    removeCommentMarks(id) {
        // remove comment marks with the given ID in both views.
        ;[
            this.mod.editor.view,
            this.mod.editor.mod.footnotes.fnEditor.view
        ].forEach(view => {
            const tr = view.state.tr,
                markType = view.state.schema.marks.comment.create({id})
            view.state.doc.descendants((node, pos) => {
                const nodeStart = pos,
                    nodeEnd = pos + node.nodeSize
                node.marks.forEach(mark => {
                    if (mark.type.name === "comment" && mark.attrs.id === id) {
                        this.removeMark(tr, nodeStart, nodeEnd, markType)
                    }
                })
            })
            if (tr.steps.length) {
                view.dispatch(tr)
            }
        })
    }

    deleteLocalComment(id, local) {
        const found = this.comments[id]
        if (found) {
            delete this.comments[id]
            return true
        }
        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
        }
    }

    // Removes the comment from store, optionally also removes marks from document.
    deleteComment(id, removeMarks) {
        if (this.deleteLocalComment(id, true)) {
            this.unsent.push({
                type: "delete",
                id
            })
            if (removeMarks) {
                this.removeCommentMarks(id)
            }
            this.mustSend()
        }
    }

    addLocalAnswer(id, answer, local) {
        if (this.comments[id]) {
            if (!this.comments[id].answers) {
                this.comments[id].answers = []
            }
            const answerFound = this.comments[id].answers.find(
                Answer => Answer.id === answer.id
            )
            if (!answerFound) {
                this.comments[id].answers.push(answer)
            }
        }

        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
            if (!local && this.mod.interactions.activeCommentId === id) {
                this.mod.interactions.activateComment(id)
            }
        }
    }

    addAnswer(id, answer) {
        answer.id = (0,_schema_common__rspack_import_0.randomCommentId)()
        this.addLocalAnswer(id, answer, true)
        this.unsent.push({
            type: "add_answer",
            id,
            answerId: answer.id
        })
        this.mustSend()
    }

    deleteLocalAnswer(id, answerId, local) {
        if (this.comments[id]?.answers) {
            this.comments[id].answers = this.comments[id].answers.filter(
                answer => answer.id !== answerId
            )
        }
        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
            if (!local && this.mod.interactions.activeCommentId === id) {
                this.mod.interactions.activateComment(id)
            }
        }
    }

    deleteAnswer(id, answerId) {
        this.deleteLocalAnswer(id, answerId, true)
        this.unsent.push({
            type: "delete_answer",
            id,
            answerId
        })
        this.mustSend()
    }

    updateLocalAnswer(id, answerId, answerText, local) {
        if (this.comments[id]?.answers) {
            const answer = this.comments[id].answers.find(
                answer => answer.id === answerId
            )
            if (answer) {
                answer.answer = answerText
            }
        }
        if (local || !this.mod.interactions.isCurrentlyEditing()) {
            this.mod.editor.mod.marginboxes.updateDOM()
            if (!local && this.mod.interactions.activeCommentId === id) {
                this.mod.interactions.activateComment(id)
            }
        }
    }

    updateAnswer(id, answerId, answerText) {
        this.updateLocalAnswer(id, answerId, answerText, true)
        this.unsent.push({
            type: "update_answer",
            id,
            answerId
        })
        this.mustSend()
    }

    unsentEvents() {
        const result = []
        for (let i = 0; i < this.unsent.length; i++) {
            const event = this.unsent[i]
            if (event.type == "delete") {
                result.push({
                    type: "delete",
                    id: event.id
                })
            } else if (event.type == "update") {
                const found = this.comments[event.id]
                if (found?.id) {
                    result.push(Object.assign({type: "update"}, found))
                } else {
                    result.push({
                        type: "ignore"
                    })
                }
            } else if (event.type == "create") {
                const found = this.comments[event.id]
                if (found?.id) {
                    result.push(Object.assign({type: "create"}, found))
                } else {
                    result.push({
                        type: "ignore"
                    })
                }
            } else if (event.type == "add_answer") {
                const found = this.comments[event.id]
                let foundAnswer
                if (found?.id && found?.answers) {
                    foundAnswer = found.answers.find(
                        answer => answer.id === event.answerId
                    )
                }
                if (foundAnswer) {
                    result.push(
                        Object.assign({}, foundAnswer, {
                            type: "add_answer",
                            id: event.id,
                            answerId: foundAnswer.id
                        })
                    )
                } else {
                    result.push({
                        type: "ignore"
                    })
                }
            } else if (event.type == "delete_answer") {
                const found = this.comments[event.id]
                if (found?.id && found?.answers) {
                    result.push({
                        type: "delete_answer",
                        id: event.id,
                        answerId: event.answerId
                    })
                } else {
                    result.push({
                        type: "ignore"
                    })
                }
            } else if (event.type == "update_answer") {
                const found = this.comments[event.id]
                let foundAnswer
                if (found?.id && found?.answers) {
                    foundAnswer = found.answers.find(
                        answer => answer.id === event.answerId
                    )
                }
                if (foundAnswer) {
                    result.push(
                        Object.assign({}, foundAnswer, {
                            type: "update_answer",
                            id: event.id,
                            answerId: foundAnswer.id
                        })
                    )
                } else {
                    result.push({
                        type: "ignore"
                    })
                }
            }
        }
        return result
    }

    eventsSent(n) {
        this.unsent = this.unsent.slice(n.length)
    }

    receive(events) {
        events.forEach(event => {
            if (event.type == "delete") {
                this.deleteLocalComment(event.id, false)
            } else if (event.type == "create") {
                this.addLocalComment(event, false)
            } else if (event.type == "update") {
                this.updateLocalComment(event, false)
            } else if (event.type == "add_answer") {
                this.addLocalAnswer(
                    event.id,
                    {
                        answer: event.answer,
                        id: event.answerId,
                        date: event.date,
                        user: event.user,
                        username: event.username
                    },
                    false
                )
            } else if (event.type == "delete_answer") {
                this.deleteLocalAnswer(event.id, event.answerId, false)
            } else if (event.type == "update_answer") {
                this.updateLocalAnswer(
                    event.id,
                    event.answerId,
                    event.answer,
                    false
                )
            }
        })
    }
}


}),
"./js/modules/editor/databases/bibliography.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModBibliographyDB: function() { return ModBibliographyDB; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);


function randomID() {
    return Math.floor(Math.random() * 0xffffffff)
}

class ModBibliographyDB {
    constructor(mod) {
        mod.bibDB = this
        this.mod = mod
        this.db = false
        this.unsent = []
        // cats aren't used, but we keep this for consistency
        this.cats = []
    }

    setDB(db) {
        this.db = db
        this.unsent = []
    }

    mustSend() {
        // Set a timeout so that the update can be combines with other updates
        // if they happen more or less simultaneously.
        window.setTimeout(
            () => this.mod.editor.mod.collab.doc.sendToCollaborators(),
            100
        )
    }

    // function saveBibEntries is the same as in user's individual BibliographyDB.
    // Function added to make document's and user's bibDBs be connectable to the
    // same interface functions.
    saveBibEntries(tmpDB, isNew) {
        const idTranslations = []
        Object.keys(tmpDB).forEach(bibKey => {
            const reference = tmpDB[bibKey],
                bibId = Number.parseInt(bibKey)
            delete reference.cats
            const oldRef = this.findReference(reference)
            if (oldRef) {
                idTranslations.push([bibId, oldRef])
            } else if (isNew) {
                const id = this.addReference(reference, bibId)
                idTranslations.push([bibId, id])
            } else {
                this.updateReference(bibId, reference)
                idTranslations.push([bibId, bibId])
            }
            // We don't use cats in the document internal bibDB, so just
            // to make sure, we remove it.
        })
        return Promise.resolve(idTranslations)
    }

    // Function added here for compatibility with user's bibDB. See comment at
    // saveBibEntries function.
    updateLocalBibEntries(tmpDB, idTranslations) {
        idTranslations.forEach(bibTrans => {
            this.updateLocalReference([bibTrans[1]], tmpDB[bibTrans[0]])
        })
        return idTranslations
    }

    // Function added here for compatibility with user's bibDB. See comment at
    // saveBibEntries function.
    deleteBibEntries(ids) {
        ids.forEach(id => this.deleteReference(id))
        return Promise.resolve(ids)
    }

    // This function only makes real sense in the user's bibDB. It is kept here
    // for compatibility reasons.
    getDB() {
        return new Promise(resolve => {
            window.setTimeout(() => resolve({bibPks: [], bibCats: []}), 100)
        })
    }

    addReference(reference, id) {
        while (!id || this.db[id]) {
            id = randomID()
        }
        this.updateReference(id, reference)
        return id
    }

    // Add or update a reference to the bibliography database both remotely and locally.
    updateReference(id, reference) {
        this.updateLocalReference(id, reference)
        this.unsent.push({
            type: "update",
            id
        })
        this.mustSend()
    }

    // Add or update a reference only locally.
    updateLocalReference(id, reference) {
        const preExisting = this.db[id] ? true : false
        this.db[id] = reference
        if (preExisting) {
            this.mod.editor.mod.citations.resetCitations()
        } else {
            this.mod.editor.mod.citations.layoutCitations()
        }
    }

    deleteReference(id) {
        this.deleteLocalReference(id)
        this.unsent.push({
            type: "delete",
            id
        })
        this.mustSend()
    }

    deleteLocalReference(id) {
        delete this.db[id]
    }

    hasUnsentEvents() {
        return this.unsent.length
    }

    unsentEvents() {
        return this.unsent.map(event => {
            if (event.type === "delete") {
                return event
            } else if (event.type === "update") {
                // Check bib entry still exists. Otherwise ignore.
                const reference = this.db[event.id]
                if (reference) {
                    return {
                        type: "update",
                        id: event.id,
                        reference
                    }
                } else {
                    return {
                        type: "ignore"
                    }
                }
            }
        })
    }

    eventsSent(n) {
        this.unsent = this.unsent.slice(n.length)
    }

    receive(events) {
        events.forEach(event => {
            if (event.type == "delete") {
                this.deleteLocalReference(event.id)
            } else if (event.type == "update") {
                this.updateLocalReference(event.id, event.reference)
            }
        })
    }

    findReference(ref) {
        return Object.keys(this.db).find(id => {
            const bib = this.db[id]
            return (
                bib.bib_type === ref.bib_type &&
                fast_deep_equal__rspack_import_0_default()(bib.fields, ref.fields)
            )
        })
    }

    hasReference(ref) {
        if (this.findReference(ref) !== undefined) {
            return true
        } else {
            return false
        }
    }
}


}),
"./js/modules/editor/databases/images.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModImageDB: function() { return ModImageDB; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/*
 Class to provide similar functionality for the document's imageDB to what the
 user's imageDb provides but using the document's websocket connection.
 Notice: It is not possible to directly upload images via this interface as
 images should not be uploaded via websocket. To add a new image to a document,
 the image needs to be uploaded first to the user's imageDB and can then be
 copied to the doc's imageDB. The IDs used are the same for user and document,
 as they originate from the Image model (not UserImage or DocumentImage).
 For E2EE documents, images are encrypted client-side and uploaded directly
 via a dedicated endpoint to an EncryptedDocumentImage model.
*/

class ModImageDB {
    constructor(mod) {
        mod.imageDB = this
        this.mod = mod
        this.db = false
        this.unsent = []
        // cats always remain empty, as we don't use categories in doc images
        this.cats = []
    }

    setDB(db) {
        this.db = db
        this.unsent = []
    }

    mustSend() {
        // Set a timeout so that the update can be combines with other updates
        // if they happen more or less simultaneously.
        window.setTimeout(
            () => this.mod.editor.mod.collab.doc.sendToCollaborators(),
            100
        )
    }

    // This function only makes real sense in the user's imageDB. It is kept here
    // for compatibility reasons.
    getDB() {
        return new Promise(resolve => {
            window.setTimeout(() => resolve([]), 100)
        })
    }

    // Save an image directly for this document (E2EE only).
    // The image file is expected to be already encrypted client-side.
    saveImage(imageData) {
        const isE2EE = this.mod.editor.e2ee?.encrypted === true
        if (!isE2EE) {
            // Non-E2EE documents must use the user's imageDB
            return this.mod.editor.app.imageDB.saveImage(imageData)
        }

        // E2EE path: upload encrypted image directly to the document
        const jsonData = {
            doc_id: this.mod.editor.docInfo.id,
            title: imageData.title,
            copyright: imageData.copyright,
            checksum: imageData.checksum || 0
        }
        if (imageData.original_file_type) {
            jsonData.original_file_type = imageData.original_file_type
        }

        const files = {}
        if (imageData.image) {
            files.image = {
                file: imageData.image,
                filename: imageData.original_file_type
                    ? `image.${imageData.original_file_type.split("/").pop()}`
                    : "image.bin"
            }
        }

        return (0,_common__rspack_import_0.jsonPostJson)("/api/document/e2ee_image/", jsonData, false, files)
            .then(({json}) => {
                const dbEntry = {
                    id: json.id,
                    title: imageData.title,
                    copyright: imageData.copyright,
                    image: json.image || "",
                    file_type: "application/octet-stream",
                    original_file_type:
                        json.original_file_type ||
                        imageData.original_file_type ||
                        "image/png",
                    checksum: imageData.checksum || 0,
                    cats: []
                }
                this.db[json.id] = dbEntry
                this.setImage(json.id, dbEntry)
                return json.id
            })
            .catch(error => {
                (0,_common__rspack_import_0.addAlert)("error", gettext("Could not save encrypted image"))
                throw error
            })
    }

    // Add or update an in the image database both remotely and locally.
    setImage(id, imageData) {
        this.setLocalImage(id, imageData)
        this.unsent.push({
            type: "update",
            id
        })
        this.mustSend()
    }

    // Add or update an image only locally.
    setLocalImage(id, imageData) {
        this.db[id] = imageData
    }

    deleteImage(id) {
        this.deleteLocalImage(id)
        const wasDeleted = !this.db[id]
        if (wasDeleted && this.mod.editor.e2ee?.encrypted) {
            // For E2EE documents, also delete the encrypted image record
            // from the server. The diff-based delete is only for client
            // sync; the server cannot decrypt it to know what to remove.
            this.deleteE2EEImageFromServer(id)
        }
        this.unsent.push({
            type: "delete",
            id
        })
        this.mustSend()
    }

    deleteE2EEImageFromServer(id) {
        (0,_common__rspack_import_0.jsonPost)("/api/document/delete_e2ee_image/", {
            doc_id: this.mod.editor.docInfo.id,
            image_id: id
        }).catch(() => {
            // Silently ignore — orphaned image records are acceptable
        })
    }

    deleteLocalImage(id) {
        const usedImages = []
        this.mod.editor.view.state.doc.descendants(node => {
            if (node.type.name === "figure" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            }
        })
        if (!usedImages.includes(Number.parseInt(id))) {
            delete this.db[id]
            return
        }
        if (this.mod.editor.e2ee?.encrypted === true) {
            // E2EE images are per-document. If the image is still used in the
            // document, keep the local entry as-is. We cannot recover from the
            // shared user image DB because E2EE images are encrypted with this
            // document's unique key and are not reusable across documents.
            return
        }
        if (Object.keys(this.mod.editor.app.imageDB.db).includes(id)) {
            // Just directly reset the image as we already have the image present in user Image DB
            this.setImage(id, this.mod.editor.app.imageDB.db[id])
        } else {
            // If image is not present in both the userImage DB and docDB we can safely assume that we have to upload again.
            this.reUploadImage(
                id,
                this.db[id].image,
                this.db[id].title,
                this.db[id].copyright
            ).then(
                ({id, _newId}) => delete this.db[id],
                id => {
                    delete this.db[id]
                    const transaction = this.mod.editor.view.state.tr
                    this.mod.editor.view.state.doc.descendants((node, pos) => {
                        if (
                            node.type.name === "figure" &&
                            node.attrs.image == id
                        ) {
                            const attrs = Object.assign({}, node.attrs)
                            attrs["image"] = false
                            const nodeType =
                                this.mod.editor.currentView.state.schema.nodes[
                                    "figure"
                                ]
                            transaction.setNodeMarkup(pos, nodeType, attrs)
                        }
                    })
                    this.mod.editor.view.dispatch(transaction)
                    ;(0,_common__rspack_import_0.addAlert)(
                        "error",
                        gettext(
                            "One of the Image(s) you copied could not be found on the server. Please try uploading it again."
                        )
                    )
                }
            )
        }
    }

    reUploadImage(id, imageUrl, title, copyright) {
        const isE2EE = this.mod.editor.e2ee?.encrypted === true
        if (isE2EE) {
            // E2EE images cannot be re-uploaded from a URL because the server
            // stores encrypted opaque bytes. If the image is missing locally,
            // the user must re-insert it manually.
            return Promise.reject(id)
        }

        const newPromise = new Promise((resolve, reject) => {
            // Depends on the fact that service worker is working and cached the image basically.
            (0,_common__rspack_import_0.get)(imageUrl)
                .then(response => response.blob())
                .then(blob => {
                    const filename = imageUrl.split("/").pop()
                    const file = new File([blob], filename, {type: blob.type})
                    const x = {
                        image: file,
                        title: title,
                        cats: [],
                        copyright: copyright
                    }
                    this.mod.editor.app.imageDB.saveImage(x).then(
                        newId => {
                            const imageData = JSON.parse(
                                JSON.stringify(
                                    this.mod.editor.app.imageDB.db[newId]
                                )
                            )
                            this.setImage(newId, imageData)
                            this.mod.editor.view.state.doc.descendants(
                                (node, pos) => {
                                    if (
                                        node.type.name === "image" &&
                                        node.attrs.image == id
                                    ) {
                                        const attrs = Object.assign(
                                            {},
                                            node.attrs
                                        )
                                        attrs["image"] = newId
                                        const nodeType =
                                            this.mod.editor.currentView.state
                                                .schema.nodes["image"]
                                        const transaction =
                                            this.mod.editor.view.state.tr.setNodeMarkup(
                                                pos,
                                                nodeType,
                                                attrs
                                            )
                                        this.mod.editor.view.dispatch(
                                            transaction
                                        )
                                    }
                                }
                            )
                            resolve({id: id, newId: newId})
                        },
                        _error => reject(id)
                    )
                })
                .catch(_error => {
                    reject(id)
                })
        })
        return newPromise
    }

    hasUnsentEvents() {
        return this.unsent.length
    }

    unsentEvents() {
        return this.unsent.map(event => {
            if (event.type === "delete") {
                return event
            } else if (event.type === "update") {
                // Check image entry still exists. Otherwise ignore.
                const image = this.db[event.id]
                if (image) {
                    return {
                        type: "update",
                        id: event.id,
                        image
                    }
                } else {
                    return {
                        type: "ignore"
                    }
                }
            }
        })
    }

    eventsSent(n) {
        this.unsent = this.unsent.slice(n.length)
    }

    receive(events) {
        events.forEach(event => {
            if (event.type == "delete") {
                this.deleteLocalImage(event.id)
            } else if (event.type == "update") {
                this.setLocalImage(event.id, event.image)
            }
        })
    }

    findImage(imageData) {
        return Object.keys(this.db).find(
            id => this.db[id].checksum === imageData.checksum
        )
    }

    hasImage(imageData) {
        if (this.findImage(imageData) !== undefined) {
            return true
        } else {
            return false
        }
    }
}


}),
"./js/modules/editor/databases/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModDB: function() { return ModDB; }
});
/* import */ var _bibliography__rspack_import_0 = __webpack_require__("./js/modules/editor/databases/bibliography.js");
/* import */ var _images__rspack_import_1 = __webpack_require__("./js/modules/editor/databases/images.js");



class ModDB {
    constructor(editor) {
        editor.mod.db = this
        this.editor = editor
        new _images__rspack_import_1.ModImageDB(this)
        new _bibliography__rspack_import_0.ModBibliographyDB(this)
        // assign bibDB to be used in document schema.
        this.editor.schema.cached.bibDB = this.bibDB
        // assign bibDB to be used in footnote schema.
        this.editor.mod.footnotes.fnEditor.schema.cached.bibDB = this.bibDB
        // assign image DB to be used in document schema.
        this.editor.schema.cached.imageDB = this.imageDB
        // assign image DB to be used in footnote schema.
        this.editor.mod.footnotes.fnEditor.schema.cached.imageDB = this.imageDB
    }

    // remove images/citation items that are no longer part of the document.
    clean() {
        const usedImages = [],
            usedBibs = []
        this.editor.view.state.doc.descendants(node => {
            if (node.type.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.type.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            }
        })

        this.editor.mod.footnotes.fnEditor.view.state.doc.descendants(node => {
            if (node.type.name === "citation") {
                node.attrs.references.forEach(ref =>
                    usedBibs.push(Number.parseInt(ref.id))
                )
            } else if (node.type.name === "image" && node.attrs.image) {
                usedImages.push(node.attrs.image)
            }
        })

        const unusedImages = Object.keys(this.imageDB.db).filter(
                value => !usedImages.includes(Number.parseInt(value))
            ),
            unusedBibs = Object.keys(this.bibDB.db).filter(
                value => !usedBibs.includes(Number.parseInt(value))
            )
        unusedImages.forEach(id => this.imageDB.deleteImage(id))
        unusedBibs.forEach(id => this.bibDB.deleteReference(id))

        const imageDbKeys = Object.keys(this.imageDB.db)
        const missingImages = usedImages.filter(
            id => !imageDbKeys.includes(String(id))
        )
        if (!this.editor.e2ee?.encrypted) {
            missingImages.forEach(id => {
                const userImage = this.editor.app.imageDB.db[id]
                if (!userImage) {
                    // Image is not present. Give up.
                    return
                }
                const imageEntry = JSON.parse(JSON.stringify(userImage))
                this.imageDB.setImage(id, imageEntry)
            })
        }
    }
}


}),
"./js/modules/editor/dialogs/citation.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CitationDialog: function() { return CitationDialog; }
});
/* import */ var simple_datatables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/simple-datatables@10.2.0/node_modules/simple-datatables/dist/module.js");
/* import */ var _plugins_citation_dialog__rspack_import_1 = __webpack_require__("./js/plugins/citation_dialog/index.js");
/* import */ var _plugins_citation_dialog__rspack_import_1_default = /*#__PURE__*/__webpack_require__.n(_plugins_citation_dialog__rspack_import_1);
/* import */ var _bibliography_tools__rspack_import_2 = __webpack_require__("./js/modules/bibliography/tools.js");
/* import */ var _common__rspack_import_3 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_4 = __webpack_require__("./js/modules/editor/dialogs/templates.js");







class CitationDialog {
    constructor(editor) {
        this.editor = editor
        this.initialReferences = []
        this.initialFormat = "autocite"
        this.node = this.editor.currentView.state.selection.node
        this.dialog = false
        this.buttons = []
        this.submitButtonText = gettext("Insert")
    }

    init() {
        if (this.node?.type && this.node?.type.name === "citation") {
            this.initialFormat = this.node.attrs.format
            this.initialReferences = this.node.attrs.references
        }

        this.buttons.push({
            text: gettext("Register new source"),
            click: () => this.registerNewSource(),
            classes: "fw-light fw-add-button register-new-bib-source"
        })

        if (this.node?.type && this.node?.type.name === "citation") {
            this.buttons.push({
                text: gettext("Remove"),
                click: () => {
                    const transaction =
                        this.editor.currentView.state.tr.deleteSelection()
                    this.editor.currentView.dispatch(transaction)
                    this.dialog.close()
                },
                classes: "fw-orange"
            })
            this.submitButtonText = gettext("Update")
        }

        this.buttons.push({
            text: this.submitButtonText,
            click: () => {
                if (this.dialogSubmit()) {
                    this.dialog.close()
                }
            },
            classes: "fw-dark insert-citation"
        })

        this.buttons.push({
            type: "cancel"
        })

        this.activatePlugins()

        this.dialog = new _common__rspack_import_3.Dialog({
            id: "configure-citation",
            title: gettext("Configure Citation"),
            buttons: this.buttons,
            body: this.citationDialogHTML(),
            width: 1002,
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })
        this.dialog.open()
        this.initTable()
        this.bind()
    }

    activatePlugins() {
        // Add plugins
        this.plugins = {}

        Object.keys(_plugins_citation_dialog__rspack_import_1).forEach(plugin => {
            if (typeof _plugins_citation_dialog__rspack_import_1[plugin] === "function") {
                this.plugins[plugin] = new _plugins_citation_dialog__rspack_import_1[plugin](this)
                this.plugins[plugin].init()
            }
        })
    }

    createAllTableRows() {
        const data = []
        // unify bibs from both document and user
        Object.keys(this.editor.mod.db.bibDB.db).forEach(id => {
            data.push(
                this.createTableRow(
                    this.editor.mod.db.bibDB.db[id],
                    id,
                    "document",
                    false
                )
            )
        })
        Object.keys(this.editor.app.bibDB.db).forEach(id => {
            const bib = this.editor.app.bibDB.db[id]
            if (!this.editor.mod.db.bibDB.hasReference(bib)) {
                data.push(this.createTableRow(bib, id, "user", false))
            }
        })
        return data
    }

    createTableRow(bib, id, db, checked) {
        const bibauthors = bib.fields.author || bib.fields.editor
        return [
            `${db}-${id}`,
            `<span class="fw-data-table-title fw-inline">
                <i class="fa fa-book"></i>
                <span class="fw-searchable">${bib.fields.title?.length ? (0,_common__rspack_import_3.escapeText)((0,_bibliography_tools__rspack_import_2.litToText)(bib.fields.title)) : gettext("Untitled")}</span>
            </span>`,
            bibauthors ? (0,_common__rspack_import_3.escapeText)((0,_bibliography_tools__rspack_import_2.nameToText)(bibauthors)) : "",
            bib.fields.date ? (0,_bibliography_tools__rspack_import_2.dateToYear)(bib.fields.date) : "",
            checked ? '<i class="fa fa-check" aria-hidden="true"></i>' : ""
        ]
    }

    citationDialogHTML() {
        // Assemble the HTML of the 'cited' column of the dialog,
        // and return the templated dialog HTML.
        const citedItemsHTML = this.initialReferences
            .map(citEntry => {
                const id = citEntry.id
                if (!this.editor.mod.db.bibDB.db[id]) {
                    return ""
                }
                const bibEntry = this.bibDBToBibEntry(
                    this.editor.mod.db.bibDB.db[id],
                    id,
                    "document"
                )
                bibEntry.prefix = citEntry.prefix ? citEntry.prefix : ""
                bibEntry.locator = citEntry.locator ? citEntry.locator : ""
                return (0,_templates__rspack_import_4.selectedCitationTemplate)(bibEntry)
            })
            .join("")

        return (0,_templates__rspack_import_4.configureCitationTemplate)({
            citedItemsHTML,
            citeFormat: this.initialFormat
        })
    }

    registerNewSource() {
        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_tokenfield_1_5_2_node_modules_tokenfield_lib_tokenfield_js"), __webpack_require__.e("js_modules_bibliography_form_index_js"), __webpack_require__.e("js_modules_bibliography_form_strings_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/bibliography/form/index.js")).then(({BibEntryForm}) => {
            const form = new BibEntryForm(this.editor.mod.db.bibDB)
            form.init().then(idTranslations => {
                const ids = idTranslations.map(idTrans => idTrans[1])
                this.addToCitableItems(ids)
            })
        })
    }

    bibDBToBibEntry(bib, id, db) {
        const bibauthors = bib.fields.author || bib.fields.editor
        return {
            id,
            db,
            bib_type: bib.bib_type,
            title: bib.fields.title?.length
                ? (0,_bibliography_tools__rspack_import_2.litToText)(bib.fields.title)
                : gettext("Untitled"),
            author: bibauthors ? (0,_bibliography_tools__rspack_import_2.nameToText)(bibauthors) : "",
            year: bib.fields.date ? (0,_bibliography_tools__rspack_import_2.dateToYear)(bib.fields.date) : ""
        }
    }

    // Update the citation dialog with new items in 'citable' column.
    // Not when dialog is first opened.
    addToCitableItems(ids) {
        const data = []
        ids.forEach(id => {
            const citeItemData = this.bibDBToBibEntry(
                this.editor.mod.db.bibDB.db[id],
                id,
                "document"
            )
            this.addToCitedItems([citeItemData])
            data.push(
                this.createTableRow(
                    this.editor.mod.db.bibDB.db[id],
                    id,
                    "document",
                    false
                )
            )
        })

        this.table.insert({data})
        this.table.columns.sort(this.lastSort.column, this.lastSort.dir)
    }

    // Update the citation dialog with new items in 'cited' column.
    // Not when dialog is first opened.
    addToCitedItems(items) {
        const len = items.length
        for (let i = 0; i < len; i++) {
            const item = items[i]
            this.dialog.dialogEl
                .querySelector(
                    "#selected-cite-source-table .fw-data-table-body"
                )
                .insertAdjacentHTML(
                    "beforeend",
                    (0,_templates__rspack_import_4.selectedCitationTemplate)({
                        id: item.id,
                        db: item.db,
                        title: item.title,
                        author: item.author,
                        year: item.year,
                        locator: "",
                        prefix: ""
                    })
                )
        }
    }

    initTable() {
        const tableEl = document.createElement("table")
        tableEl.classList.add("fw-data-table")
        tableEl.classList.add("fw-large")
        this.dialog.dialogEl.querySelector("#my-sources").appendChild(tableEl)
        this.table = new simple_datatables__rspack_import_0.DataTable(tableEl, {
            searchable: true,
            paging: false,
            scrollY: "225px",
            labels: {
                noRows: gettext("No sources registered"),
                noResults: gettext("No sources found"), // Message shown when there are no search results
                placeholder: gettext("Search...") // placeholder for search field
            },
            template: (options, dom) => `<div class='${options.classes.top}'>
                <div class='${options.classes.search}'>
                    <input class='${options.classes.input}' placeholder='${options.labels.placeholder}' type='search' title='${options.labels.searchTitle}'${dom.id ? ` aria-controls="${dom.id}"` : ""}>
                </div>
            </div>
            <div class='${options.classes.container}' style='height: ${options.scrollY}; overflow-Y: auto;'></div>`,
            data: {
                headings: [
                    "",
                    gettext("Title"),
                    gettext("Author"),
                    gettext("Year"),
                    ""
                ],
                data: this.createAllTableRows()
            },
            columns: [
                {
                    select: [0, 2, 3],
                    type: "string"
                },
                {
                    select: 0,
                    hidden: true
                },
                {
                    select: 4,
                    sortable: false
                }
            ]
        })
        this.table.on("datatable.sort", (column, dir) => {
            this.lastSort = {column, dir}
        })
        this.table.columns.sort(0, "asc")
    }

    checkRow(dataIndex) {
        const row = this.table.data.data[dataIndex]
        if (!row) {
            return
        }

        if (row.cells[4].data.length) {
            row.cells[4].data = []
        } else {
            row.cells[4].data = [
                {
                    nodeName: "i",
                    attributes: {class: "fa fa-check", "aria-hidden": "true"}
                }
            ]
        }
        this.table.refresh()
    }

    bind() {
        this.table.dom.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_3.findTarget)(event, "tr", el): {
                    this.checkRow(el.target.dataset.index)
                    break
                }
                default:
                    break
            }
        })

        this.dialog.dialogEl
            .querySelector("#add-cite-source")
            .addEventListener("click", () => {
                const selectedItems = []

                this.table.data.data.forEach(row => {
                    if (!row.cells[4].data.length) {
                        return
                    }
                    row.cells[4].data = []
                    const [db, id] = row.cells[0].data.split("-").map(
                        (val, index) => (index ? Number.parseInt(val) : val) // only parseInt id (where index > 0)
                    )
                    if (
                        this.dialog.dialogEl.querySelector(
                            `#selected-source-${db}-${id}`
                        )
                    ) {
                        return
                    }
                    selectedItems.push({
                        id,
                        db,
                        title: row.cells[1].text,
                        author: row.cells[2].data,
                        year: row.cells[3].data
                    })
                })
                this.addToCitedItems(selectedItems)
                this.table.refresh()
            })

        this.dialog.dialogEl.addEventListener("click", event => {
            const el = {}
            let documentEl
            switch (true) {
                case (0,_common__rspack_import_3.findTarget)(event, ".selected-source .delete", el):
                    documentEl = this.dialog.dialogEl.querySelector(
                        `#selected-source-${el.target.dataset.db}-${el.target.dataset.id}`
                    )
                    if (documentEl) {
                        documentEl.parentElement.removeChild(documentEl)
                    }
                    break
                case (0,_common__rspack_import_3.findTarget)(event, ".selected-source .order-up", el):
                    documentEl = this.dialog.dialogEl.querySelector(
                        `#selected-source-${el.target.dataset.db}-${el.target.dataset.id}`
                    )
                    if (documentEl && documentEl.previousElementSibling) {
                        documentEl.parentElement.insertBefore(
                            documentEl,
                            documentEl.previousElementSibling
                        )
                    }
                    break
                case (0,_common__rspack_import_3.findTarget)(event, ".selected-source .order-down", el):
                    documentEl = this.dialog.dialogEl.querySelector(
                        `#selected-source-${el.target.dataset.db}-${el.target.dataset.id}`
                    )
                    if (documentEl && documentEl.nextElementSibling) {
                        documentEl.parentElement.insertBefore(
                            documentEl,
                            documentEl.nextElementSibling.nextElementSibling
                        )
                    }
                    break
                case (0,_common__rspack_import_3.findTarget)(event, ".fw-checkable", el):
                    ;(0,_common__rspack_import_3.setCheckableLabel)(el.target)
                    break
                default:
                    break
            }
        })
    }

    dialogSubmit() {
        const citeItems = Array.from(
                this.dialog.dialogEl.querySelectorAll(
                    "#selected-cite-source-table .fw-cite-parts-table"
                )
            ),
            references = citeItems.map(bibRef => {
                const deleteButton = bibRef.querySelector(".delete"),
                    db = deleteButton.dataset.db
                let id = Number.parseInt(deleteButton.dataset.id)
                if (db === "user") {
                    // entry is from user's bibDB. We need to import it into the
                    // document's bibDB.
                    const bib = this.editor.app.bibDB.db[id]
                    id = this.editor.mod.db.bibDB.addReference(bib, id)
                }
                const returnObj = {
                    id
                }
                const prefix = bibRef.querySelector(".fw-cite-text").value
                if (prefix.length) {
                    returnObj["prefix"] = prefix
                }
                const locator = bibRef.querySelector(".fw-cite-page").value
                if (locator.length) {
                    returnObj["locator"] = locator
                }
                return returnObj
            })

        if (!citeItems.length) {
            (0,_common__rspack_import_3.addAlert)(
                "info",
                gettext("Please select at least one citation source!")
            )
            return false
        }

        const format = this.dialog.dialogEl.querySelector(
            "#citation-style-selector"
        ).value

        if (
            JSON.stringify(references) ===
                JSON.stringify(this.initialReferences) &&
            format == this.initialFormat
        ) {
            // Nothing has been changed, so we just close the dialog again
            return true
        }

        const citationNode = this.editor.currentView.state.schema.nodes[
            "citation"
        ].create({format, references})
        const transaction =
            this.editor.currentView.state.tr.replaceSelectionWith(
                citationNode,
                true
            )
        this.editor.currentView.dispatch(transaction)
        return true
    }
}


}),
"./js/modules/editor/dialogs/code_block.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CodeBlockDialog: function() { return CodeBlockDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_document_content__rspack_import_1 = __webpack_require__("./js/modules/schema/document/content.js");
/* import */ var _schema_i18n__rspack_import_2 = __webpack_require__("./js/modules/schema/i18n.js");




class CodeBlockDialog {
    constructor(editor) {
        this.editor = editor
        this.language = ""
        this.category = ""
        this.title = ""
        this.id = ""
        this.insideCodeBlock = false
        this.node = false
        this.submitMessage = gettext("Insert")
        this.dialog = false
    }

    findCodeBlock(state) {
        if (
            state.selection.node &&
            state.selection.node.type.name === "code_block"
        ) {
            return state.selection.node
        }
        const $head = state.selection.$head
        for (let d = $head.depth; d > 0; d--) {
            if ($head.node(d).type.name === "code_block") {
                return $head.node(d)
            }
        }
        return false
    }

    submitForm() {
        const view = this.editor.currentView
        const {state} = view
        const {schema} = state

        const newAttrs = {
            language: this.language,
            category: this.category,
            title: this.title,
            id: this.insideCodeBlock
                ? this.node.attrs.id ||
                  (this.category ? (0,_schema_document_content__rspack_import_1.randomCodeBlockId)() : "")
                : this.category
                  ? (0,_schema_document_content__rspack_import_1.randomCodeBlockId)()
                  : "",
            track: this.insideCodeBlock ? this.node.attrs.track : []
        }

        let tr

        if (this.insideCodeBlock) {
            // Update existing code block using setNodeMarkup
            const $from = state.selection.$from

            // Find the actual code_block position
            let codeBlockDepth = -1
            for (let d = $from.depth; d > 0; d--) {
                if ($from.node(d).type.name === "code_block") {
                    codeBlockDepth = d
                    break
                }
            }

            const pos =
                codeBlockDepth > 0
                    ? $from.before(codeBlockDepth)
                    : state.selection.from

            tr = state.tr.setNodeMarkup(pos, null, newAttrs)
        } else {
            // Insert new code block
            const codeBlockNode = schema.nodes.code_block.create(newAttrs)
            tr = state.tr.replaceSelectionWith(codeBlockNode, false)
        }

        view.dispatch(tr)
        this.dialog.close()
    }

    getEnabledCategories() {
        const codeCategories =
            this.editor.view.state.doc.attrs.code_categories || {}
        const categories = []

        Object.entries(codeCategories).forEach(([key, value]) => {
            if (value.enabled) {
                categories.push(key)
            }
        })

        return categories
    }

    getAvailableLanguages() {
        return this.editor.view.state.doc.attrs.code_languages || []
    }

    init() {
        this.node = this.findCodeBlock(this.editor.currentView.state)

        if (this.node?.attrs?.track?.find(track => track.type === "deletion")) {
            // The code block is marked as deleted so we don't allow editing it.
            return true
        }

        const buttons = []

        if (this.node?.type && this.node?.type.name === "code_block") {
            this.insideCodeBlock = true
            this.submitMessage = gettext("Update")
            this.language = this.node.attrs.language || ""
            this.category = this.node.attrs.category || ""
            this.title = this.node.attrs.title || ""
            this.id = this.node.attrs.id || ""

            buttons.push({
                text: gettext("Remove"),
                classes: "fw-orange",
                click: () => {
                    const tr =
                        this.editor.currentView.state.tr.deleteSelection()
                    this.editor.currentView.dispatch(tr)
                    this.dialog.close()
                }
            })
        }

        buttons.push({
            text: this.submitMessage,
            classes: "fw-dark",
            click: () => this.submitForm()
        })

        buttons.push({
            type: "cancel"
        })

        const language = this.editor.view.state.doc.attrs.language
        const enabledCategories = this.getEnabledCategories()
        const availableLanguages = this.getAvailableLanguages()

        this.dialog = new _common__rspack_import_0.Dialog({
            id: "code-block-dialog",
            title: gettext("Configure code block"),
            body: this.getDialogTemplate(
                language,
                enabledCategories,
                availableLanguages
            ),
            buttons,
            onClose: () => this.editor.currentView.focus()
        })

        this.dialog.open()

        // Language selector
        const languageSelector = (0,_common__rspack_import_0.dropdownSelect)(
            this.dialog.dialogEl.querySelector(".code-block-language"),
            {
                onChange: newValue => {
                    this.language = newValue
                },
                width: "80%",
                value: this.language
            }
        )
        this.languageSelector = languageSelector

        // Category selector
        if (enabledCategories.length > 0) {
            const categorySelector = (0,_common__rspack_import_0.dropdownSelect)(
                this.dialog.dialogEl.querySelector(".code-block-category"),
                {
                    onChange: newValue => {
                        this.category = newValue
                    },
                    width: "80%",
                    value: this.category
                }
            )
            this.categorySelector = categorySelector
        }

        // Title input
        const titleInput =
            this.dialog.dialogEl.querySelector(".code-block-title")
        if (titleInput) {
            titleInput.value = this.title
            titleInput.addEventListener("input", () => {
                this.title = titleInput.value
            })
        }
    }

    getDialogTemplate(language, enabledCategories, availableLanguages) {
        return `<table class="fw-dialog-table">
            <tbody>
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Language")}</h4></th>
                    <td>
                        <select class="code-block-language">
                            <option value="">${gettext("None")}</option>
                            ${availableLanguages
                                .map(
                                    lang =>
                                        `<option value="${lang}">${lang}</option>`
                                )
                                .join("")}
                        </select>
                    </td>
                </tr>
                ${
                    enabledCategories.length > 0
                        ? `<tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Category")}</h4></th>
                    <td>
                        <select class="code-block-category">
                            <option value="">${gettext("None")}</option>
                            ${enabledCategories
                                .map(
                                    cat =>
                                        `<option value="${cat}">${_schema_i18n__rspack_import_2.CATS[cat]?.[language] || cat}</option>`
                                )
                                .join("")}
                        </select>
                    </td>
                </tr>`
                        : ""
                }
                <tr>
                    <th><h4 class="fw-tablerow-title">${gettext("Title")}</h4></th>
                    <td>
                        <input type="text" class="code-block-title" placeholder="${gettext("Optional title")}" style="width: 80%">
                    </td>
                </tr>
            </tbody>
        </table>`
    }
}


}),
"./js/modules/editor/dialogs/contributor.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ContributorDialog: function() { return ContributorDialog; }
});
/* import */ var prosemirror_state__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/templates.js");



/*
    Source for email regexp:
    https://html.spec.whatwg.org/multipage/input.html#e-mail-state-(type%3Demail)
*/
const emailRegExp =
    /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/

class ContributorDialog {
    /**
     * @param {Object} node - The contributors_part node
     * @param {Object} view - The ProseMirror editor view
     * @param {Object|false} contributor - The contributor data if editing, false if adding
     */
    constructor(node, view, contributor = false) {
        this.node = node
        this.view = view
        this.contributor = contributor
        this.dialog = false
    }

    init() {
        const buttons = []
        // Add Update/Add button
        buttons.push({
            text: this.contributor ? gettext("Update") : gettext("Add"),
            classes: "fw-dark",
            click: () => {
                // Get form values
                let firstname = this.dialog.dialogEl.querySelector(
                        "input[name=firstname]"
                    ).value,
                    lastname = this.dialog.dialogEl.querySelector(
                        "input[name=lastname]"
                    ).value,
                    email =
                        this.dialog.dialogEl.querySelector(
                            "input[name=email]"
                        ).value,
                    institution = this.dialog.dialogEl.querySelector(
                        "input[name=institution]"
                    ).value

                firstname = firstname.length ? firstname : false
                lastname = lastname.length ? lastname : false
                institution = institution.length ? institution : false
                email = email.length ? email : false

                // Validate email format
                if (email && !emailRegExp.test(email)) {
                    (0,_common__rspack_import_0.addAlert)("error", gettext("Email is in incorrect format!"))
                    return
                }

                this.dialog.close()

                // Don't create contributor if all fields are empty
                if (!firstname && !lastname && !institution && !email) {
                    // No data, don't insert.
                    return
                }

                const view = this.view,
                    newNode = view.state.schema.nodes.contributor.create({
                        firstname,
                        lastname,
                        email,
                        institution
                    })
                let tr

                // Check if we're editing an existing contributor based on current selection
                // This works for collaborative editing because we use current selection, not saved positions
                if (
                    this.contributor &&
                    view.state.selection.jsonID === "node" &&
                    view.state.selection.node.type.name === "contributor"
                ) {
                    // Editing: replace the selected contributor
                    tr = view.state.tr.replaceSelectionWith(newNode, false)
                    // Set selection to the updated contributor
                    tr.setSelection(
                        prosemirror_state__rspack_import_2.NodeSelection.create(tr.doc, view.state.selection.from)
                    )
                } else {
                    // Adding: find the insertion point based on current document state
                    // Adding: find the insertion point based on current document state
                    let posFrom, posTo
                    view.state.doc.descendants((node, pos) => {
                        // Find the contributors_part node to determine insertion position
                        if (node.attrs.id === this.node.attrs.id) {
                            posFrom = posTo = pos + node.nodeSize - 1
                            // - 1 to go to end of node contributors container node
                        }
                        if ("id" in node.attrs) {
                            return false
                        }
                    })
                    tr = view.state.tr.replaceRangeWith(posFrom, posTo, newNode)
                    // Set selection to the newly created contributor
                    tr.setSelection(prosemirror_state__rspack_import_2.NodeSelection.create(tr.doc, posFrom))
                }
                // Dispatch the transaction (both replacement and selection)
                view.dispatch(tr)
                return
            }
        })

        buttons.push({
            type: "cancel"
        })

        // Create and open the dialog

        this.dialog = new _common__rspack_import_0.Dialog({
            id: "edit-contributor",
            title: `${this.contributor ? gettext("Update") : gettext("Add")} ${this.node.attrs.item_title.toLowerCase()}`,
            body: (0,_templates__rspack_import_1.contributorTemplate)({
                contributor: this.contributor ? this.contributor : {}
            }),
            width: 836,
            height: 360,
            buttons,
            // Focus the editor view when dialog closes
            onClose: () => this.view.focus(),
            // Don't restore previous active element (dialog handles focus)
            restoreActiveElement: false
        })

        this.dialog.open()
    }
}


}),
"./js/modules/editor/dialogs/figure.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FigureDialog: function() { return FigureDialog; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _images_selection_dialog__rspack_import_2 = __webpack_require__("./js/modules/images/selection_dialog/index.js");
/* import */ var _schema_common__rspack_import_3 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _e2ee_encryptor__rspack_import_4 = __webpack_require__("./js/modules/editor/e2ee/encryptor.js");
/* import */ var _templates__rspack_import_5 = __webpack_require__("./js/modules/editor/dialogs/templates.js");








class FigureDialog {
    constructor(editor) {
        this.editor = editor
        this.imageDB = this.editor.mod.db.imageDB
        this.userImageDB = this.editor.app.imageDB
        this.imgId = false
        this.imgDb = false
        this.copyright = false
        this.insideFigure = false
        this.figureNode = false
        this.contentNode = false
        this.caption = true
        this.category = "none"
        this.aligned = "center"
        this.width = "50"
        this.equation = ""
        this.node = false
        this.submitMessage = gettext("Insert")
        this.dialog = false
    }

    layoutMathEditor() {
        this.dialog.dialogEl.querySelector(".inner-figure-preview").innerHTML =
            `<div><span class="math-field" type="text" name="math" ></span></div>
            <p class="formula-or-figure">${gettext("or")}</p>
            <p><button type="button" id="insert-figure-image" class="fw-button fw-light">
                ${gettext("Insert image")} <i class="fa fa-image"></i>
            </button></p>`

        this.mathliveDOM = this.dialog.dialogEl.querySelector(".math-field")
        this.nonMathElements = [
            this.dialog.dialogEl.querySelector("#insert-figure-image"),
            this.dialog.dialogEl.querySelector(".formula-or-figure")
        ]
        __webpack_require__.e(/* import() */ "vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs").then(__webpack_require__.bind(__webpack_require__, "./node_modules/.pnpm/mathlive@0.104.0/node_modules/mathlive/dist/mathlive.mjs")).then(MathLive => {
            MathLive.MathfieldElement.strings = {
                int: {
                    "keyboard.tooltip.functions": gettext("Functions"),
                    "keyboard.tooltip.greek": gettext("Greek Letters"),
                    "keyboard.tooltip.command": gettext("LaTeX Command Mode"),
                    "keyboard.tooltip.numeric": gettext("Numeric"),
                    "keyboard.tooltip.roman": gettext(
                        "Symbols and Roman Letters"
                    ),
                    "tooltip.copy to clipboard": gettext("Copy to Clipboard"),
                    "tooltip.redo": gettext("Redo"),
                    "tooltip.toggle virtual keyboard": gettext(
                        "Toggle Virtual Keyboard"
                    ),
                    "tooltip.undo": gettext("Undo")
                }
            }
            MathLive.MathfieldElement.locale = "int"
            MathLive.MathfieldElement.plonkSound = null
            MathLive.MathfieldElement.keypressSound = null
            this.mathField = new MathLive.MathfieldElement({
                mathVirtualKeyboardPolicy: "manual"
            })
            this.mathField.value = this.equation
            this.mathliveDOM.appendChild(this.mathField)

            this.mathField.addEventListener("focusout", () =>
                this.showPlaceHolder()
            )
            this.mathField.addEventListener("focus", () =>
                this.hidePlaceHolder()
            )
            this.mathField.addEventListener("input", () => {
                this.equation = this.mathField.getValue()
                this.showHideNonMathElements()
            })
            this.mathField.select()
            this.mathField.value = this.equation

            this.showHideNonMathElements()
            this.dialog.dialogEl
                .querySelector("#insert-figure-image")
                .addEventListener("click", () => this.selectImage())
        })
    }

    showPlaceHolder() {
        if (
            !this.mathField.getValue().length &&
            !this.mathliveDOM.querySelector(".placeholder")
        ) {
            this.mathliveDOM.insertAdjacentHTML(
                "beforeend",
                `<span class="placeholder" >${gettext("Type formula")}</span>`
            )
        }
    }

    hidePlaceHolder() {
        const placeHolder = this.mathliveDOM.querySelector(".placeholder")
        if (placeHolder) {
            this.mathliveDOM.removeChild(placeHolder)
        }
    }

    showHideNonMathElements() {
        if (this.equation.length) {
            this.nonMathElements.forEach(el => (el.style.display = "none"))
        } else {
            this.nonMathElements.forEach(el => (el.style.display = ""))
        }
    }

    selectImage() {
        const imageSelection = new _images_selection_dialog__rspack_import_2.ImageSelectionDialog(
            this.imageDB,
            this.userImageDB,
            this.imgId,
            this.editor
        )
        imageSelection.init().then(({id, db}) => {
            if (id) {
                this.imgId = id
                this.imgDb = db
                // We take a copy of the object in case of the image coming from the user db in order
                // not to overwrite the copyright info from the user's image db.
                this.copyright =
                    db === "document"
                        ? this.imageDB.db[this.imgId].copyright
                        : JSON.parse(
                              JSON.stringify(
                                  this.userImageDB.db[this.imgId].copyright
                              )
                          )
                this.layoutImagePreview()
            } else {
                this.imgId = false
                this.imgDb = false
                this.layoutMathEditor()
            }
        })
    }

    async layoutImagePreview() {
        if (this.imgId) {
            if (this.mathField) {
                this.mathField = false
            }
            const db =
                this.imgDb === "document"
                    ? this.imageDB.db
                    : this.userImageDB.db

            const imageEntry = db[this.imgId]
            let imgSrc = imageEntry?.image || ""

            if (imageEntry?.file_type === "application/octet-stream") {
                const key = this.editor.e2ee?.key
                if (key) {
                    try {
                        imgSrc = await _e2ee_encryptor__rspack_import_4.E2EEEncryptor.decryptImageToUrl(
                            imageEntry.image,
                            key,
                            imageEntry.original_file_type || "image/png"
                        )
                    } catch (_e) {
                        imgSrc = staticUrl("img/error.avif")
                    }
                } else {
                    imgSrc = staticUrl("img/error.avif")
                }
            }

            this.dialog.dialogEl.querySelector(
                ".inner-figure-preview"
            ).innerHTML = `<img src="${imgSrc}" style="max-width: 400px;max-height:220px">
                <span class="dot-menu-icon"><i class="fa fa-ellipsis-v"></i></span>`

            this.dialog.dialogEl
                .querySelector(".dot-menu-icon")
                .addEventListener("click", event => {
                    const contentMenu = new _common__rspack_import_1.ContentMenu({
                        menu: this.editor.menu.imageMenuModel,
                        page: this,
                        menuPos: {X: event.pageX, Y: event.pageY}
                    })
                    contentMenu.open()
                })
        }
    }

    async submitForm() {
        if (new RegExp(/^\s*$/).test(this.equation) && !this.imgId) {
            // The math input is empty. Delete a math node if it exist. Then close the dialog.
            if (this.insideFigure) {
                const tr = this.editor.currentView.state.tr.deleteSelection()
                this.editor.currentView.dispatch(tr)
            }
            this.dialog.close()
            return false
        }

        if (this.imgDb === "user") {
            if (this.editor.e2ee?.encrypted) {
                // For E2EE: encrypt the user image and upload it to the document
                const userImage = this.userImageDB.db[this.imgId]
                try {
                    const response = await fetch(userImage.image)
                    const blob = await response.blob()
                    const file = new File(
                        [blob],
                        userImage.image.split("/").pop(),
                        {type: blob.type}
                    )
                    const encryptedFile = await _e2ee_encryptor__rspack_import_4.E2EEEncryptor.encryptImage(
                        file,
                        this.editor.e2ee.key
                    )
                    const encryptedCopyright =
                        await _e2ee_encryptor__rspack_import_4.E2EEEncryptor.encryptObject(
                            this.copyright,
                            this.editor.e2ee.key
                        )
                    const newId = await this.imageDB.saveImage({
                        image: encryptedFile,
                        title: userImage.title,
                        copyright: encryptedCopyright,
                        checksum: userImage.checksum,
                        original_file_type: blob.type,
                        cats: []
                    })
                    this.imgId = newId
                    this.imgDb = "document"
                } catch (error) {
                    console.error("E2EE: Failed to encrypt user image", error)
                    ;(0,_common__rspack_import_1.addAlert)("error", gettext("Could not encrypt image"))
                    return false
                }
            } else {
                // Add image to document db.
                const imageEntry = JSON.parse(
                    JSON.stringify(this.userImageDB.db[this.imgId])
                )
                imageEntry.copyright = this.copyright
                this.imageDB.setImage(this.imgId, imageEntry)
                this.imgDb = "document"
            }
        } else if (
            this.imgId &&
            this.imageDB.db[this.imgId] &&
            !fast_deep_equal__rspack_import_0_default()(this.copyright, this.imageDB.db[this.imgId].copyright)
        ) {
            const imageEntry = JSON.parse(
                JSON.stringify(this.imageDB.db[this.imgId])
            )
            imageEntry.copyright = this.copyright
            this.imageDB.setImage(this.imgId, imageEntry)
        }

        if (
            this.insideFigure &&
            this.equation ===
                (this.node.content.content.find(
                    node => node.type.name === "figure_equation"
                )?.attrs.equation || "") &&
            this.imgId ===
                (this.node.content.content.find(
                    node => node.type.name === "image"
                )?.attrs.image || false) &&
            this.imgDb === "document" &&
            this.caption === this.node.attrs.caption &&
            this.category === this.node.attrs.category &&
            this.aligned === this.node.attrs.aligned &&
            this.width === this.node.attrs.width
        ) {
            // The figure has not been changed, just close the dialog
            this.dialog.close()
            return false
        }
        const content = []
        if (this.imgId) {
            content.push(
                this.editor.currentView.state.schema.nodes["image"].create({
                    image: this.imgId
                })
            )
        } else {
            content.push(
                this.editor.currentView.state.schema.nodes[
                    "figure_equation"
                ].create({
                    equation: this.equation
                })
            )
        }
        const captionNode =
            this.node.content?.content.find(
                node => node.type.name === "figure_caption"
            ) ||
            this.editor.currentView.state.schema.nodes[
                "figure_caption"
            ].create()
        if (this.category === "table") {
            content.unshift(captionNode)
        } else {
            content.push(captionNode)
        }
        const tr = this.editor.currentView.state.tr.replaceSelectionWith(
            this.editor.currentView.state.schema.nodes["figure"].createAndFill(
                {
                    aligned: this.aligned,
                    width: this.width,
                    category: this.category,
                    caption: this.caption,
                    id: this.insideFigure
                        ? this.node.attrs.id
                        : (0,_schema_common__rspack_import_3.randomFigureId)()
                },
                content
            ),
            false
        )
        this.editor.currentView.dispatch(tr)

        this.dialog.close()
    }

    findFigure(state) {
        if (
            state.selection.node &&
            state.selection.node.type.name == "figure"
        ) {
            return state.selection.node
        }
        const $head = state.selection.$head
        for (let d = $head.depth; d > 0; d--) {
            if ($head.node(d).type.name == "figure") {
                return $head.node(d)
            }
        }
        return false
    }

    init() {
        this.node = this.findFigure(this.editor.currentView.state)

        if (this.node?.attrs?.track?.find(track => track.type === "deletion")) {
            // The figure is marked as deleted so we don't allow editing it.
            return true
        }

        const buttons = []

        if (this.node?.type && this.node?.type.name === "figure") {
            this.insideFigure = true
            this.submitMessage = gettext("Update")
            this.equation =
                this.node.content.content.find(
                    node => node.type.name === "figure_equation"
                )?.attrs.equation || ""
            this.imgId =
                this.node.content.content.find(
                    node => node.type.name === "image"
                )?.attrs.image || false
            this.imgDb = "document"
            this.category = this.node.attrs.category
            this.caption = this.node.attrs.caption
            this.aligned = this.node.attrs.aligned
            this.width = this.node.attrs.width
            buttons.push({
                text: gettext("Remove"),
                classes: "fw-orange",
                click: () => {
                    const tr =
                        this.editor.currentView.state.tr.deleteSelection()
                    this.editor.currentView.dispatch(tr)
                    this.dialog.close()
                }
            })
        }
        // Image positioning both at the time of updating and inserting for the first time
        buttons.push({
            // Update
            text: this.submitMessage,
            classes: "fw-dark",
            click: () => this.submitForm()
        })

        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_1.Dialog({
            id: "figure-dialog",
            title: gettext("Enter latex math or insert an image"),
            body: (0,_templates__rspack_import_5.configureFigureTemplate)({
                language: this.editor.view.state.doc.attrs.language
            }),
            buttons,
            beforeClose: () => {
                if (this.mathField) {
                    this.mathField = false
                }
                if (window.mathVirtualKeyboard) {
                    window.mathVirtualKeyboard.hide()
                }
            },
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })

        this.dialog.open()

        const alignmentSelector = (0,_common__rspack_import_1.dropdownSelect)(
            this.dialog.dialogEl.querySelector(".figure-alignment"),
            {
                onChange: newValue => {
                    this.aligned = newValue
                },
                width: "80%",
                value: this.aligned
            }
        )

        if (this.width === "100") {
            alignmentSelector.setValue("center")
            alignmentSelector.disable()
            this.aligned = "center"
        }

        const figureWidthDOM =
            this.dialog.dialogEl.querySelector(".figure-width")
        figureWidthDOM.style.width = "80%"
        figureWidthDOM.firstElementChild.innerText = `${this.width} %`
        figureWidthDOM.addEventListener("click", event => {
            const contentMenu = new _common__rspack_import_1.ContentMenu({
                menu: this.editor.menu.figureWidthMenuModel,
                page: this,
                menuPos: {X: event.pageX, Y: event.pageY},
                onClose: () => {
                    if (this.width == "100") {
                        alignmentSelector.setValue("center")
                        alignmentSelector.disable()
                        this.aligned = "center"
                    } else {
                        alignmentSelector.enable()
                    }
                }
            })
            contentMenu.open()
        })

        ;(0,_common__rspack_import_1.dropdownSelect)(this.dialog.dialogEl.querySelector(".figure-category"), {
            onChange: newValue => {
                this.category = newValue
            },
            width: "80%",
            value: this.category
        })

        ;(0,_common__rspack_import_1.dropdownSelect)(this.dialog.dialogEl.querySelector(".figure-caption"), {
            onChange: newValue => {
                this.caption = newValue === "true"
            },
            width: "80%",
            value: String(this.caption)
        })

        if (this.imgId && this.imageDB.db[this.imgId]) {
            this.copyright = this.imageDB.db[this.imgId].copyright
            this.layoutImagePreview()
        } else {
            this.layoutMathEditor()
        }
    }
}


}),
"./js/modules/editor/dialogs/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  CitationDialog: function() { return /* reexport safe */ _citation__rspack_import_1.CitationDialog; },
  CodeBlockDialog: function() { return /* reexport safe */ _code_block__rspack_import_9.CodeBlockDialog; },
  ContributorDialog: function() { return /* reexport safe */ _contributor__rspack_import_0.ContributorDialog; },
  FigureDialog: function() { return /* reexport safe */ _figure__rspack_import_2.FigureDialog; },
  LanguageDialog: function() { return /* reexport safe */ _language__rspack_import_4.LanguageDialog; },
  LinkDialog: function() { return /* reexport safe */ _link__rspack_import_5.LinkDialog; },
  MathDialog: function() { return /* reexport safe */ _math__rspack_import_3.MathDialog; },
  OrderedListStartDialog: function() { return /* reexport safe */ _ordered_list__rspack_import_8.OrderedListStartDialog; },
  RevisionDialog: function() { return /* reexport safe */ _revision__rspack_import_7.RevisionDialog; },
  TableConfigurationDialog: function() { return /* reexport safe */ _table__rspack_import_6.TableConfigurationDialog; },
  TableDialog: function() { return /* reexport safe */ _table__rspack_import_6.TableDialog; }
});
/* import */ var _contributor__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/contributor.js");
/* import */ var _citation__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/citation.js");
/* import */ var _figure__rspack_import_2 = __webpack_require__("./js/modules/editor/dialogs/figure.js");
/* import */ var _math__rspack_import_3 = __webpack_require__("./js/modules/editor/dialogs/math.js");
/* import */ var _language__rspack_import_4 = __webpack_require__("./js/modules/editor/dialogs/language.js");
/* import */ var _link__rspack_import_5 = __webpack_require__("./js/modules/editor/dialogs/link.js");
/* import */ var _table__rspack_import_6 = __webpack_require__("./js/modules/editor/dialogs/table.js");
/* import */ var _revision__rspack_import_7 = __webpack_require__("./js/modules/editor/dialogs/revision.js");
/* import */ var _ordered_list__rspack_import_8 = __webpack_require__("./js/modules/editor/dialogs/ordered_list.js");
/* import */ var _code_block__rspack_import_9 = __webpack_require__("./js/modules/editor/dialogs/code_block.js");












}),
"./js/modules/editor/dialogs/language.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  LanguageDialog: function() { return LanguageDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_const__rspack_import_1 = __webpack_require__("./js/modules/schema/const.js");
/* import */ var _templates__rspack_import_2 = __webpack_require__("./js/modules/editor/dialogs/templates.js");





class LanguageDialog {
    constructor(editor, language) {
        this.editor = editor
        this.language = language
        this.dialog = false
    }

    init() {
        const buttons = []
        buttons.push({
            text: gettext("Change"),
            classes: "fw-dark",
            click: () => {
                const language =
                    this.dialog.dialogEl.querySelector("select").value
                this.dialog.close()

                if (language === this.language) {
                    // No change.
                    return
                }
                this.editor.view.dispatch(
                    this.editor.view.state.tr
                        .setDocAttribute("language", language)
                        .setMeta("settings", true)
                )
                return
            }
        })

        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_0.Dialog({
            width: 300,
            height: 180,
            id: "select-document-language",
            title: gettext("Change language of the document"),
            body: (0,_templates__rspack_import_2.languageTemplate)({
                currentLanguage: this.language,
                allowedLanguages: _schema_const__rspack_import_1.LANGUAGES.filter(lang =>
                    this.editor.view.state.doc.attrs.languages.includes(lang[0])
                )
            }),
            buttons,
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })

        this.dialog.open()
    }
}


}),
"./js/modules/editor/dialogs/link.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  LinkDialog: function() { return LinkDialog; }
});
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _templates__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/templates.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _state_plugins__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/index.js");







class LinkDialog {
    constructor(editor) {
        this.editor = editor
        this.target = false // cross reference or internal link
        this.linkType = "external"
        this.defaultLink = "https://"
        this.title = ""
        this.submitButtonText = gettext("Insert")
        this.dialog = false
        this.internalTargets = []
    }

    init() {
        this.checkCrossReference()
        if (!this.target) {
            this.checkLink()
        }
        this.internalTargets = (0,_state_plugins__rspack_import_2.getInternalTargets)(
            this.editor.view.state,
            this.editor.view.state.doc.attrs.language,
            "main"
        ).concat(
            (0,_state_plugins__rspack_import_2.getInternalTargets)(
                this.editor.mod.footnotes.fnEditor.view.state,
                this.editor.view.state.doc.attrs.language,
                "foot"
            )
        )
        this.createDialog()
    }

    // Check if there is an existing link at the selection. If this is the case
    // use its values in dialog.
    checkLink() {
        const state = this.editor.currentView.state,
            from = state.selection.from,
            linkMark = state.selection.$from
                .marks()
                .find(mark => mark.type.name === "link")
        if (linkMark) {
            if (linkMark.attrs.href[0] === "#") {
                this.linkType = "internal"
                this.target = linkMark.attrs.href.slice(1)
            } else {
                this.linkType = "external"
                this.target = linkMark.attrs.href
                this.title = linkMark.attrs.title ? linkMark.attrs.title : ""
            }
            this.extendSelectionToMark(from, linkMark)
            this.submitButtonText = gettext("Update")
        }
    }

    // Check if the selection spans over exactly one cross reference node. If so, use it.
    checkCrossReference() {
        if (
            this.editor.currentView.state.selection.node?.type.name ===
            "cross_reference"
        ) {
            this.submitButtonText = gettext("Update")
            this.target = this.editor.currentView.state.selection.node.attrs.id
            this.linkType = "cross_reference"
        }
    }

    // To check whether links, cross references or both can be added in current position.
    checkAllowedContent() {
        if (this.editor.currentView === this.editor.view) {
            const settings =
                this.editor.view.state.selection.$anchor.node(1).attrs
            return {
                link: settings.marks.includes("link"),
                cross_reference: settings.elements.includes("cross_reference")
            }
        } else {
            const settings = this.editor.view.state.doc.attrs
            return {
                link: settings.footnote_marks.includes("link"),
                cross_reference:
                    settings.footnote_elements.includes("cross_reference")
            }
        }
    }

    // Find the start and end of the link currently selected.
    extendSelectionToMark(pos, mark) {
        const view = this.editor.currentView,
            state = view.state,
            $pos = state.doc.resolve(pos)
        let startIndex = $pos.index(),
            endIndex = $pos.indexAfter()

        while (
            startIndex > 0 &&
            mark.isInSet($pos.parent.child(startIndex - 1).marks)
        ) {
            startIndex--
        }
        while (
            endIndex < $pos.parent.childCount &&
            mark.isInSet($pos.parent.child(endIndex).marks)
        ) {
            endIndex++
        }
        let startPos = $pos.start(),
            endPos = startPos

        for (let i = 0; i < endIndex; i++) {
            const size = $pos.parent.child(i).nodeSize
            if (i < startIndex) {
                startPos += size
            }
            endPos += size
        }
        view.dispatch(
            state.tr.setSelection(
                prosemirror_state__rspack_import_3.TextSelection.create(state.doc, startPos, endPos)
            )
        )
    }

    createDialog() {
        const buttons = []
        buttons.push({
            text: this.submitButtonText,
            classes: "fw-dark",
            click: () => {
                const linkTypeEl = this.dialog.dialogEl.querySelector(
                        "input[name=link-type]:checked"
                    ),
                    linkType = linkTypeEl ? linkTypeEl.value : "external"
                let href, title, target, change

                switch (linkType) {
                    case "internal": {
                        target = this.dialog.dialogEl.querySelector(
                            "select.internal-link-selector"
                        ).value
                        href = `#${target}`
                        title = this.internalTargets.find(
                            iTarget => iTarget.id === target
                        ).text
                        change =
                            target &&
                            (this.linkType !== "internal" ||
                                this.target !== target)
                        break
                    }
                    case "external": {
                        const linkEl =
                            this.dialog.dialogEl.querySelector("input.link")
                        if (
                            !new RegExp(/^\s*$/).test(linkEl.value) &&
                            linkEl.value !== this.defaultLink
                        ) {
                            href = linkEl.value
                        }
                        title =
                            this.dialog.dialogEl.querySelector(
                                "input.link-title"
                            ).value
                        if (new RegExp(/^\s*$/).test(title)) {
                            // The link title is empty. Make it the same as the link itself.
                            title = href
                        }
                        change =
                            href &&
                            (this.linkType !== "external" ||
                                this.target !== href ||
                                this.title !== title)
                        break
                    }
                    case "cross_reference": {
                        target = this.dialog.dialogEl.querySelector(
                            "select.cross-reference-selector"
                        ).value
                        title = this.internalTargets.find(
                            iTarget => iTarget.id === target
                        ).text
                        change =
                            target &&
                            (this.linkType !== "cross_reference" ||
                                this.target !== target)
                        break
                    }
                }
                this.dialog.close()

                if (!change) {
                    // The link input is empty or hasn't been changed from the default value.
                    // Just close the dialog.
                    this.editor.currentView.focus()
                    return
                }

                const view = this.editor.currentView,
                    posFrom = view.state.selection.from,
                    tr = view.state.tr
                let posTo = view.state.selection.to

                if (linkType === "cross_reference") {
                    const node = view.state.schema.nodes.cross_reference.create(
                        {
                            id: target,
                            title
                        }
                    )
                    tr.replaceSelectionWith(node)
                } else {
                    // There is an empty selection. We insert the link title into the editor
                    // and then add the link to that.
                    if (posFrom === posTo) {
                        tr.insertText(title, posFrom, posTo)
                        posTo = tr.mapping.map(posFrom, 1)
                    }
                    const markType = view.state.schema.marks.link.create({
                        href,
                        title
                    })
                    tr.addMark(posFrom, posTo, markType)
                }
                view.dispatch(tr)
                view.focus()
                return
            }
        })

        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_1.Dialog({
            id: "edit-link",
            title: gettext("Link"),
            body: (0,_templates__rspack_import_0.linkDialogTemplate)({
                target: this.target,
                allowedContent: this.checkAllowedContent(),
                title: this.title,
                linkType: this.linkType,
                defaultLink: this.defaultLink,
                internalTargets: this.internalTargets
            }),
            buttons,
            width: 836,
            height: 360,
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })

        this.dialog.open()

        if (this.internalTargets.length) {
            const externalEls = this.dialog.dialogEl.querySelectorAll(
                    "input.link, input.link-title"
                ),
                internalEls = this.dialog.dialogEl.querySelectorAll(
                    "select.internal-link-selector"
                ),
                crossReferenceEls = this.dialog.dialogEl.querySelectorAll(
                    "select.cross-reference-selector"
                ),
                externalSwitchers = this.dialog.dialogEl.querySelectorAll(
                    "input.link, input.link-title, label.link-external-label, input.link-external-check"
                ),
                internalSwitchers = this.dialog.dialogEl.querySelectorAll(
                    "select.internal-link-selector, label.link-internal-label, input.link-internal-check"
                ),
                crossReferenceSwitchers = this.dialog.dialogEl.querySelectorAll(
                    "select.cross-reference-selector, label.cross-reference-label, input.cross-reference-check"
                ),
                radioInternal = this.dialog.dialogEl.querySelector(
                    "input.link-internal-check"
                ),
                radioExternal = this.dialog.dialogEl.querySelector(
                    "input.link-external-check"
                ),
                radioCrossReference = this.dialog.dialogEl.querySelector(
                    "input.cross-reference-check"
                )

            switch (this.linkType) {
                case "internal":
                    externalEls.forEach(el => el.classList.add("disabled"))
                    crossReferenceEls.forEach(el =>
                        el.classList.add("disabled")
                    )
                    radioInternal.checked = true
                    break
                case "external":
                    internalEls.forEach(el => el.classList.add("disabled"))
                    externalEls.forEach(el => el.classList.add("disabled"))
                    radioExternal.checked = true
                    break
                case "cross_reference":
                    internalEls.forEach(el => el.classList.add("disabled"))
                    crossReferenceEls.forEach(el =>
                        el.classList.add("disabled")
                    )
                    radioCrossReference.checked = true
                    break
                default:
                    break
            }

            internalSwitchers.forEach(el =>
                el.addEventListener("click", () => {
                    externalEls.forEach(el => el.classList.add("disabled"))
                    internalEls.forEach(el => el.classList.remove("disabled"))
                    crossReferenceEls.forEach(el =>
                        el.classList.add("disabled")
                    )
                    radioInternal.checked = true
                })
            )

            externalSwitchers.forEach(el =>
                el.addEventListener("click", () => {
                    internalEls.forEach(el => el.classList.add("disabled"))
                    externalEls.forEach(el => el.classList.remove("disabled"))
                    crossReferenceEls.forEach(el =>
                        el.classList.add("disabled")
                    )
                    radioExternal.checked = true
                })
            )

            crossReferenceSwitchers.forEach(el =>
                el.addEventListener("click", () => {
                    internalEls.forEach(el => el.classList.add("disabled"))
                    externalEls.forEach(el => el.classList.add("disabled"))
                    crossReferenceEls.forEach(el =>
                        el.classList.remove("disabled")
                    )
                    radioCrossReference.checked = true
                })
            )
        }
    }
}


}),
"./js/modules/editor/dialogs/math.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  MathDialog: function() { return MathDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _subsup__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/subsup.js");
/* import */ var _templates__rspack_import_2 = __webpack_require__("./js/modules/editor/dialogs/templates.js");




/**
 * Class to work with formula dialog
 */
class MathDialog {
    constructor(editor) {
        this.editor = editor
        this.node = this.editor.currentView.state.selection.node
        this.equationSelected =
            this.node && this.node.attrs && this.node.attrs.equation
                ? true
                : false
        this.equation = this.equationSelected ? this.node.attrs.equation : ""
    }

    init() {
        //get selected node

        //initialize dialog and open it
        this.dialog = new _common__rspack_import_0.Dialog({
            body: (0,_templates__rspack_import_2.mathDialogTemplate)(),
            height: 150,
            buttons: [
                {
                    text: this.equationSelected
                        ? gettext("Update")
                        : gettext("Insert"),
                    classes: "fw-dark insert-math",
                    click: () => {
                        const view = this.editor.currentView,
                            state = view.state

                        this.equation = this.getLatex()

                        if (new RegExp(/^\s*$/).test(this.equation)) {
                            // The math input is empty. Delete a math node if it exist. Then close the dialog.
                            if (this.equationSelected) {
                                view.dispatch(state.tr.deleteSelection())
                            }
                            this.dialog.close()
                            return
                        } else if (
                            new RegExp(
                                `^\\^({[${_subsup__rspack_import_1.supChars}]*}|[${_subsup__rspack_import_1.supChars}]?)$`
                            ).test(this.equation)
                        ) {
                            // The math input is pure superscript and
                            // can be converted to ordinary characters.
                            view.dispatch(
                                state.tr.insertText((0,_subsup__rspack_import_1.sup)(this.equation.slice(1)))
                            )
                            this.dialog.close()
                            return
                        } else if (
                            new RegExp(
                                `^\\_({[${_subsup__rspack_import_1.subChars}]*}|[${_subsup__rspack_import_1.subChars}]?)$`
                            ).test(this.equation)
                        ) {
                            // The math input is pure subscript and
                            // can be converted to ordinary characters.
                            view.dispatch(
                                state.tr.insertText((0,_subsup__rspack_import_1.sub)(this.equation.slice(1)))
                            )
                            this.dialog.close()
                            return
                        } else if (
                            this.equationSelected &&
                            this.equation === this.node.attrs.equation
                        ) {
                            // Equation selected, but has not changed from last time.
                            this.dialog.close()
                            return
                        }
                        const nodeType = state.schema.nodes["equation"]
                        view.dispatch(
                            state.tr.replaceSelectionWith(
                                nodeType.createAndFill({
                                    equation: this.equation
                                })
                            )
                        )
                        this.dialog.close()
                    }
                },
                {
                    type: "cancel"
                }
            ],
            title: gettext("Mathematical formula"),
            beforeClose: () => {
                if (this.mathField) {
                    this.mathField = false
                }
                if (window.mathVirtualKeyboard) {
                    window.mathVirtualKeyboard.hide()
                }
            },
            classes: "math",
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })
        this.dialog.open()

        this.mathliveDOM = this.dialog.dialogEl.querySelector(".math-field")

        __webpack_require__.e(/* import() */ "vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs").then(__webpack_require__.bind(__webpack_require__, "./node_modules/.pnpm/mathlive@0.104.0/node_modules/mathlive/dist/mathlive.mjs")).then(MathLive => {
            MathLive.MathfieldElement.strings = {
                int: {
                    "keyboard.tooltip.functions": gettext("Functions"),
                    "keyboard.tooltip.greek": gettext("Greek Letters"),
                    "keyboard.tooltip.command": gettext("LaTeX Command Mode"),
                    "keyboard.tooltip.numeric": gettext("Numeric"),
                    "keyboard.tooltip.roman": gettext(
                        "Symbols and Roman Letters"
                    ),
                    "tooltip.copy to clipboard": gettext("Copy to Clipboard"),
                    "tooltip.redo": gettext("Redo"),
                    "tooltip.toggle virtual keyboard": gettext(
                        "Toggle Virtual Keyboard"
                    ),
                    "tooltip.undo": gettext("Undo")
                }
            }
            MathLive.MathfieldElement.locale = "int"
            MathLive.MathfieldElement.plonkSound = null
            MathLive.MathfieldElement.keypressSound = null
            this.mathField = new MathLive.MathfieldElement({
                mathVirtualKeyboardPolicy: "manual"
            })
            this.mathField.value = this.equation
            this.mathliveDOM.appendChild(this.mathField)
            //this.mathField.addEventListener("focusin", () => window.mathVirtualKeyboard.show())
            //this.mathField.addEventListener("focusout", () => window.mathVirtualKeyboard.hide())
            this.mathField.select()
        })
    }

    /**
     * Get latex representation as text
     * @returns {string} latex formula
     */
    getLatex() {
        return this.mathField.getValue()
    }
}


}),
"./js/modules/editor/dialogs/ordered_list.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  OrderedListStartDialog: function() { return OrderedListStartDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/templates.js");



class OrderedListStartDialog {
    constructor(editor) {
        this.editor = editor
        this.dialogEl = false
        this.order = 1
    }

    init() {
        const {orderedList} = this.findOrderedList(
            this.editor.currentView.state
        )
        if (orderedList) {
            this.order = orderedList.attrs.order
        }
        this.insertDialog()
    }

    findOrderedList(state) {
        const $head = state.selection.$head
        for (let d = $head.depth; d > 0; d--) {
            if ($head.node(d).type.name == "ordered_list") {
                return {
                    orderedList: $head.node(d),
                    orderedListPos: $head.before(d)
                }
            }
        }
        return {orderedList: false, orderedListPos: false}
    }

    submitForm() {
        const {orderedList, orderedListPos} = this.findOrderedList(
            this.editor.currentView.state
        )
        if (!orderedList) {
            return
        }
        const attrs = Object.assign({}, orderedList.attrs, {
            order: this.order
        })
        this.editor.currentView.dispatch(
            this.editor.currentView.state.tr.setNodeMarkup(
                orderedListPos,
                false,
                attrs
            )
        )
    }

    insertDialog() {
        const buttons = []
        buttons.push({
            text: gettext("Update"),
            classes: "fw-dark",
            click: () => {
                this.submitForm()
                this.dialog.close()
            }
        })
        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Set list start number"),
            body: (0,_templates__rspack_import_1.orderedListStartTemplate)({order: this.order}),
            width: 300,
            height: 100,
            buttons,
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })

        this.dialog.open()

        const listStartInput = document.querySelector("input.list-start")
        listStartInput.addEventListener("change", _event => {
            this.order = Number.parseInt(listStartInput.value) || 1
        })
    }
}


}),
"./js/modules/editor/dialogs/revision.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  RevisionDialog: function() { return RevisionDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/templates.js");



class RevisionDialog {
    constructor(dir) {
        this.dir = dir
        this.dialog = false
    }

    init() {
        const buttons = []
        const dialogDonePromise = new Promise(resolve => {
            buttons.push({
                text: gettext("Save"),
                classes: "fw-dark",
                click: () => {
                    const note =
                        this.dialog.dialogEl.querySelector(
                            ".revision-note"
                        ).value
                    this.dialog.close()
                    return resolve(note)
                }
            })

            buttons.push({
                type: "cancel"
            })
        })

        this.dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Revision description"),
            body: (0,_templates__rspack_import_1.revisionDialogTemplate)({dir: this.dir}),
            height: 100,
            width: 300,
            buttons,
            restoreActiveElement: false
        })
        this.dialog.open()

        return dialogDonePromise
    }
}


}),
"./js/modules/editor/dialogs/subsup.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  sub: function() { return sub; },
  subChars: function() { return subChars; },
  sup: function() { return sup; },
  supChars: function() { return supChars; }
});
// From https://github.com/johanneswilm/subsup/blob/patch-1/index.js

// Author: Michał Jezierski <ambinanitelo@gmail.com>
// License: MIT
const superscripts = {
    0: "⁰",
    1: "¹",
    2: "²",
    3: "³",
    4: "⁴",
    5: "⁵",
    6: "⁶",
    7: "⁷",
    8: "⁸",
    9: "⁹",
    "+": "⁺",
    "-": "⁻",
    "=": "⁼",
    "(": "⁽",
    ")": "⁾",
    a: "ᵃ",
    b: "ᵇ",
    c: "ᶜ",
    d: "ᵈ",
    e: "ᵉ",
    f: "ᶠ",
    g: "ᵍ",
    h: "ʰ",
    i: "ⁱ",
    j: "ʲ",
    k: "ᵏ",
    l: "ˡ",
    m: "ᵐ",
    n: "ⁿ",
    o: "ᵒ",
    p: "ᵖ",
    r: "ʳ",
    s: "ˢ",
    t: "ᵗ",
    u: "ᵘ",
    v: "ᵛ",
    w: "ʷ",
    x: "ˣ",
    y: "ʸ",
    z: "ᶻ",
    A: "ᴬ",
    B: "ᴮ",
    D: "ᴰ",
    E: "ᴱ",
    G: "ᴳ",
    H: "ᴴ",
    I: "ᴵ",
    J: "ᴶ",
    K: "ᴷ",
    L: "ᴸ",
    M: "ᴹ",
    N: "ᴺ",
    O: "ᴼ",
    P: "ᴾ",
    R: "ᴿ",
    T: "ᵀ",
    U: "ᵁ",
    V: "ⱽ",
    W: "ᵂ"
}

const subscripts = {
    0: "₀",
    1: "₁",
    2: "₂",
    3: "₃",
    4: "₄",
    5: "₅",
    6: "₆",
    7: "₇",
    8: "₈",
    9: "₉",
    "+": "₊",
    "-": "₋",
    "=": "₌",
    "(": "₍",
    ")": "₎",
    a: "ₐ",
    e: "ₑ",
    h: "ₕ",
    i: "ᵢ",
    j: "ⱼ",
    k: "ₖ",
    l: "ₗ",
    m: "ₘ",
    n: "ₙ",
    o: "ₒ",
    p: "ₚ",
    r: "ᵣ",
    s: "ₛ",
    t: "ₜ",
    u: "ᵤ",
    v: "ᵥ",
    x: "ₓ"
}

function parse(input) {
    return (input + "").split("")
}

function to(chart) {
    return input => chart[input]
}

const sup = input => parse(input).map(to(superscripts)).join("")

const supChars = Object.keys(superscripts).join("")

const sub = input => parse(input).map(to(subscripts)).join("")

const subChars = Object.keys(subscripts).join("")


}),
"./js/modules/editor/dialogs/table.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  TableConfigurationDialog: function() { return TableConfigurationDialog; },
  TableDialog: function() { return TableDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/templates.js");



class TableDialog {
    constructor(editor) {
        this.editor = editor
        this.dialogEl = false
    }

    init() {
        this.insertTableDialog()
    }

    markInsertTable(cell, className) {
        this.dialog.dialogEl
            .querySelectorAll(`td.${className}`)
            .forEach(el => el.classList.remove(className))
        let colCount = 1
        let countElement = cell
        while (countElement.previousElementSibling) {
            countElement = countElement.previousElementSibling
            colCount += 1
        }
        let rowCount = 1
        countElement = countElement.parentElement
        while (countElement.previousElementSibling) {
            countElement = countElement.previousElementSibling
            rowCount += 1
        }
        // add hover class.
        const rows = this.dialog.dialogEl.querySelectorAll("tr")
        for (let i = 0; i < rowCount; i++) {
            const cols = rows[i].querySelectorAll("td")
            for (let j = 0; j < colCount; j++) {
                cols[j].classList.add(className)
            }
        }
        return {colCount, rowCount}
    }

    insertTableDialog() {
        let rowCount = 1,
            colCount = 1
        const buttons = []
        buttons.push({
            text: gettext("Insert"),
            classes: "fw-dark",
            click: () => {
                const table = {
                    type: "table",
                    content: [
                        {type: "table_caption"},
                        {type: "table_body", content: []}
                    ]
                }
                const table_body = table.content[1]

                for (let i = 0; i < rowCount; i++) {
                    const row = {type: "table_row", content: []}
                    for (let j = 0; j < colCount; j++) {
                        row.content.push({
                            type: "table_cell",
                            content: [{type: "paragraph"}]
                        })
                    }
                    table_body.content.push(row)
                }
                const schema = this.editor.currentView.state.schema
                this.editor.currentView.dispatch(
                    this.editor.currentView.state.tr.replaceSelectionWith(
                        schema.nodeFromJSON(table),
                        false
                    )
                )
                this.dialog.close()
            }
        })
        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Insert table"),
            body: (0,_templates__rspack_import_1.tableInsertTemplate)(),
            width: 360,
            height: 360,
            buttons,
            onClose: () => this.editor.currentView.focus(),
            restoreActiveElement: false
        })

        this.dialog.open()

        // manage hovering over table cells
        this.dialog.dialogEl.querySelectorAll("td").forEach(el =>
            el.addEventListener("mouseenter", () => {
                this.markInsertTable(el, "hover")
            })
        )
        this.dialog.dialogEl.querySelectorAll("td").forEach(el =>
            el.addEventListener("mouseleave", () => {
                this.dialog.dialogEl
                    .querySelectorAll("td.hover")
                    .forEach(mEl => mEl.classList.remove("hover"))
            })
        )

        this.dialog.dialogEl.querySelectorAll("td").forEach(el =>
            el.addEventListener("click", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                const newCounts = this.markInsertTable(el, "selected")
                rowCount = newCounts.rowCount
                colCount = newCounts.colCount
            })
        )
    }
}

class TableConfigurationDialog {
    constructor(editor) {
        this.editor = editor
        this.dialogEl = false
        this.aligned = "center"
        this.width = "100"
        this.layout = "fixed"
        this.category = "none"
        this.caption = false
    }

    init() {
        const {table} = this.findTable(this.editor.currentView.state)
        if (!table) {
            return
        }
        this.width = table.attrs.width
        this.aligned = table.attrs.aligned
        this.layout = table.attrs.layout
        this.category = table.attrs.category
        this.caption = table.attrs.caption
        this.insertDialog()
    }

    findTable(state) {
        const $head = state.selection.$head
        for (let d = $head.depth; d > 0; d--) {
            if ($head.node(d).type.name == "table") {
                return {table: $head.node(d), tablePos: $head.before(d)}
            }
        }
        return {table: false}
    }

    submitForm() {
        const {table, tablePos} = this.findTable(this.editor.currentView.state)
        if (!table) {
            return
        }
        const attrs = Object.assign({}, table.attrs, {
            width: this.width,
            aligned: this.width === "100" ? "center" : this.aligned,
            layout: this.layout,
            category: this.category,
            caption: this.caption
        })
        this.editor.currentView.dispatch(
            this.editor.currentView.state.tr.setNodeMarkup(
                tablePos,
                false,
                attrs
            )
        )
    }

    insertDialog() {
        const buttons = []
        buttons.push({
            text: gettext("Update"),
            classes: "fw-dark",
            click: () => {
                this.submitForm()
                this.dialog.close()
            }
        })
        buttons.push({
            type: "cancel"
        })

        this.dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Configure table"),
            body: (0,_templates__rspack_import_1.tableConfigurationTemplate)({
                language: this.editor.view.state.doc.attrs.language
            }),
            width: 400,
            height: 360,
            buttons,
            onClose: () => this.editor.currentView.focus()
        })

        this.dialog.open()

        const alignmentSelector = (0,_common__rspack_import_0.dropdownSelect)(
            this.dialog.dialogEl.querySelector(".table-alignment"),
            {
                onChange: newValue => {
                    this.aligned = newValue
                },
                width: "80%",
                value: this.aligned
            }
        )

        if (this.width == "100") {
            alignmentSelector.setValue("center")
            alignmentSelector.disable()
            this.aligned = "center"
        }

        (0,_common__rspack_import_0.dropdownSelect)(this.dialog.dialogEl.querySelector(".table-width"), {
            onChange: newValue => {
                this.width = newValue
                if (this.width == "100") {
                    alignmentSelector.setValue("center")
                    alignmentSelector.disable()
                    this.aligned = "center"
                } else {
                    alignmentSelector.enable()
                }
            },
            width: "80%",
            value: this.width
        })

        ;(0,_common__rspack_import_0.dropdownSelect)(this.dialog.dialogEl.querySelector(".table-layout"), {
            onChange: newValue => {
                this.layout = newValue
            },
            width: "80%",
            value: this.layout
        })

        ;(0,_common__rspack_import_0.dropdownSelect)(this.dialog.dialogEl.querySelector(".table-category"), {
            onChange: newValue => {
                this.category = newValue
            },
            width: "80%",
            value: this.category
        })

        ;(0,_common__rspack_import_0.dropdownSelect)(this.dialog.dialogEl.querySelector(".table-caption"), {
            onChange: newValue => {
                this.caption = newValue === "true"
            },
            width: "80%",
            value: String(this.caption)
        })
    }
}


}),
"./js/modules/editor/dialogs/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  configureCitationTemplate: function() { return configureCitationTemplate; },
  configureFigureTemplate: function() { return configureFigureTemplate; },
  contributorTemplate: function() { return contributorTemplate; },
  figureImageItemTemplate: function() { return figureImageItemTemplate; },
  figureImageTemplate: function() { return figureImageTemplate; },
  languageTemplate: function() { return languageTemplate; },
  linkDialogTemplate: function() { return linkDialogTemplate; },
  mathDialogTemplate: function() { return mathDialogTemplate; },
  orderedListStartTemplate: function() { return orderedListStartTemplate; },
  revisionDialogTemplate: function() { return revisionDialogTemplate; },
  selectedCitationTemplate: function() { return selectedCitationTemplate; },
  tableConfigurationTemplate: function() { return tableConfigurationTemplate; },
  tableInsertTemplate: function() { return tableInsertTemplate; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_i18n__rspack_import_1 = __webpack_require__("./js/modules/schema/i18n.js");



const linkDialogTemplate = ({
    defaultLink,
    internalTargets,
    linkType,
    title,
    target,
    allowedContent
}) =>
    `${
        allowedContent.cross_reference && internalTargets.length
            ? `<div class="fw-radio">
            <input type="radio" name="link-type" value="cross_reference" class="cross-reference-check">
            <label class="cross-reference-label">${gettext("Cross reference")}</label>
        </div>
        <div class="fw-select-container">
            <select class="cross-reference-selector fw-button fw-light fw-large" required="">
                <option class="placeholder" selected="" disabled="" value="">
                    ${gettext("Select Target")}
                </option>
                ${internalTargets
                    .map(
                        iTarget =>
                            `<option class="cross-reference-item" type="text" value="${iTarget.id}" ${target === iTarget.id ? "selected" : ""}>
                            ${(0,_common__rspack_import_0.escapeText)(iTarget.text)}
                        </option>`
                    )
                    .join("")}
            </select>
            <div class="fw-select-arrow fa fa-caret-down"></div>
        </div><p></p>`
            : ""
    }${
        allowedContent.link && internalTargets.length
            ? `<div class="fw-radio">
            <input type="radio" name="link-type" value="internal" class="link-internal-check">
            <label class="link-internal-label">${gettext("Internal")}</label>
        </div>
        <div class="fw-select-container">
            <select class="internal-link-selector fw-button fw-light fw-large" required="">
                <option class="placeholder" selected="" disabled="" value="">
                    ${gettext("Select Target")}
                </option>
                ${internalTargets
                    .map(
                        iTarget =>
                            `<option class="link-item" type="text" value="${iTarget.id}" ${target === iTarget.id ? "selected" : ""}>
                            ${(0,_common__rspack_import_0.escapeText)(iTarget.text)}
                        </option>`
                    )
                    .join("")}
            </select>
            <div class="fw-select-arrow fa fa-caret-down"></div>
        </div>
        <p></p>
        <div class="fw-radio">
            <input type="radio" name="link-type" value="external" class="link-external-check">
            <label class="link-external-label">${gettext("External")}</label>
        </div>`
            : ""
    }${
        allowedContent.link
            ? `<input class="link-title" type="text" value="${(0,_common__rspack_import_0.escapeText)(title)}" placeholder="${gettext("Link title")}"/>
        <p></p>
        <input class="link" type="text" value="${target && linkType === "external" ? target : defaultLink}" placeholder="${gettext("URL")}"/>`
            : ""
    }`

/** Dialog to add a note to a revision before saving. */
const revisionDialogTemplate = ({dir}) =>
    `<p>
        <input type="text" class="revision-note" placeholder="${gettext("Description (optional)")}" dir="${dir}">
    </p>`

const tableInsertTemplate = () => `
    <table class="insert-table-selection">
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
        <tr>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
    </table>`

const tableConfigurationTemplate = ({language}) =>
    `<table class="fw-dialog-table">
        <tbody>
            <tr>
                <th><h4 class="fw-tablerow-title">${gettext("Alignment")}</h4></th>
                <td>
                    <select class="table-alignment">
                        <option value="left">${gettext("Left")}</option>
                        <option value="center">${gettext("Center")}</option>
                        <option value="right">${gettext("Right")}</option>
                    </select>
                </td>
            </tr>
            <tr>
                <th><h4 class="fw-tablerow-title">${gettext("Width")}</h4></th>
                <td>
                    <select class="table-width">
                        <option value="100">${gettext("100 %")}</option>
                        <option value="75">${gettext("75 %")}</option>
                        <option value="50">${gettext("50 %")}</option>
                        <option value="25">${gettext("25 %")}</option>
                    </select>
                </td>
            </tr>
            <tr>
                <th><h4 class="fw-tablerow-title">${gettext("Column style")}</h4></th>
                <td>
                    <select class="table-layout">
                        <option value="fixed">${gettext("Fixed width")}</option>
                        <option value="auto">${gettext("Fit content")}</option>
                    </select>
                </td>
            </tr>
            <tr>
                <th><h4 class="fw-tablerow-title">${gettext("Listed as")}</h4></th>
                <td>
                    <select class="table-category">
                        <option value="none">${gettext("None")}</option>
                        <option value="table">${_schema_i18n__rspack_import_1.CATS.table[language]}</option>
                    </select>
                </td>
            </tr>
            <tr>
                <th><h4 class="fw-tablerow-title">${gettext("Caption")}</h4></th>
                <td>
                    <select class="table-caption">
                        <option value="true">${gettext("Enable")}</option>
                        <option value="false">${gettext("Disable")}</option>
                    </select>
                </td>
            </tr>
        </tbody>
    </table>`

const orderedListStartTemplate = ({order}) =>
    `<div title="${gettext("List start")}">
        <p><input class="list-start" type="number" name="list-start" min="1" value="${order}"></p>
    </div>`

const mathDialogTemplate = () =>
    `<div class="math-field-outer">
        <div class="math-field" type="text" name="math" ></div>
    </div>`

const figureImageItemTemplate = ({id, cats, image, thumbnail, title}) =>
    `<tr id="Image_${id}" class="${cats.map(cat => `cat_${(0,_common__rspack_import_0.escapeText)(cat)} `)}" >
         <td class="type" style="width:100px;">
            ${
                thumbnail === undefined
                    ? `<img src="${image}" style="max-heigth:30px;max-width:30px;">`
                    : `<img src="${thumbnail}" style="max-heigth:30px;max-width:30px;">`
            }
        </td>
        <td class="title" style="width:212px;">
            <span class="fw-inline">
                <span class="edit-image fw-link-text fa fa-image" data-id="${id}">
                    ${(0,_common__rspack_import_0.escapeText)(title)}
                </span>
            </span>
        </td>
        <td class="checkable" style="width:30px;">
        </td>
    </tr>`

/** A template to select images inside the figure configuration dialog in the editor. */
const figureImageTemplate = ({imageDB}) =>
    `<div>
        <table id="imagelist" class="tablesorter fw-data-table" style="width:342px;">
            <thead class="fw-data-table-header">
                <tr>
                    <th width="50">${gettext("Image")}</th>
                    <th width="150">${gettext("Title")}</th>
                </tr>
            </thead>
            <tbody class="fw-data-table-body fw-small">
                ${Object.values(imageDB).map(image => figureImageItemTemplate(image))}
            </tbody>
        </table>
        <div class="dialogSubmit">
            <button class="edit-image createNew fw-button fw-light">
                ${gettext("Upload")}
                <span class="fa fa-plus-circle"></span>
            </button>
            <button type="button" id="selectImageFigureButton" class="fw-button fw-dark">
                ${gettext("Insert")}
            </button>
            <button type="button" id="cancelImageFigureButton" class="fw-button fw-orange">
                ${gettext("Cancel")}
            </button>
        </div>
    </div>`

/** A template to configure the display of a figure in the editor. */
const configureFigureTemplate = ({language}) =>
    `<div class="fw-media-uploader">
            <input type="hidden" id="figure-category">
            <div class="figure-preview">
                <div class="inner-figure-preview"></div>
            </div>
            <table class="fw-dialog-table">
                <tbody>
                    <tr>
                        <th><h4 class="fw-tablerow-title">${gettext("Alignment")}</h4></th>
                        <td>
                            <select class="figure-alignment">
                                <option value="left">${gettext("Left")}</option>
                                <option value="center">${gettext("Center")}</option>
                                <option value="right">${gettext("Right")}</option>
                            </select>
                        </td>
                    </tr>
                    <tr>
                        <th><h4 class="fw-tablerow-title">${gettext("Width")}</h4></th>
                        <td>
                            <div class="figure-width fw-dropdown fw-large fw-light fw-button"><label></label>&nbsp;<span class="fa fa-caret-down"></span></div>
                        </td>
                    </tr>
                    <tr>
                        <th><h4 class="fw-tablerow-title">${gettext("Listed as")}</h4></th>
                        <td>
                            <select class="figure-category">
                                <option value="none">${gettext("None")}</option>
                                ${Object.entries(_schema_i18n__rspack_import_1.CATS)
                                    .map(
                                        ([id, titleObject]) =>
                                            `<option value="${id}">${titleObject[language]}</option>`
                                    )
                                    .join("")}
                            </select>
                        </td>
                    </tr>
                    <tr>
                        <th><h4 class="fw-tablerow-title">${gettext("Caption")}</h4></th>
                        <td>
                            <select class="figure-caption">
                                <option value="true">${gettext("Enable")}</option>
                                <option value="false">${gettext("Disable")}</option>
                            </select>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>`

/** A template to configure citations in the editor */
const configureCitationTemplate = ({citedItemsHTML, citeFormat}) =>
    `<div id="my-sources" class="fw-ar-container">
            <h3 class="fw-green-title">${gettext("My sources")}</h3>
        </div>
        <span id="add-cite-source" class="fw-button fw-large fw-square fw-light fw-ar-button"><i class="fa fa-caret-right"></i></span>
        <div id="cited-items" class="fw-ar-container">
            <h3 class="fw-green-title">${gettext("Citation format")}</h3>
            <div class="fw-select-container">
                <select id="citation-style-selector" class="fw-button fw-light fw-large" required="">
                    <option value="autocite" ${citeFormat === "autocite" ? "selected" : ""}>${gettext("(Author, 1998)")}</option>
                    <option value="textcite" ${citeFormat === "textcite" ? "selected" : ""}>${gettext("Author (1998)")}</option>
                </select>
                <div class="fw-select-arrow fa fa-caret-down"></div>
            </div>
            <table id="selected-cite-source-table" class="fw-data-table tablesorter">
                <thead class="fw-data-table-header"><tr>
                    <th width="110">${gettext("Title")}</th>
                    <th width="110">${gettext("Author")}</th>
                    <th width="50">${gettext("Year")}</th>
                    <th width="50" align="center">${gettext("Order")}</th>
                    <th width="50" align="center">${gettext("Remove")}</th>
                </tr></thead>
                <tbody class="fw-data-table-body fw-min">
                  ${citedItemsHTML}
                </tbody>
            </table>
        </div>`

/** A template for each selected citation item inside the citation configuration
    dialog of the editor. */
const selectedCitationTemplate = ({
    title,
    author,
    year,
    id,
    db,
    prefix,
    locator
}) =>
    `<tr id="selected-source-${db}-${id}" class="selected-source">
        <td colspan="5" width="470">
          <table class="fw-cite-parts-table">
              <tr>
                  <td width="110">
                      <span class="fw-data-table-title fw-inline">
                          <i class="fa fa-book"></i>
                          <span data-id="${id}">
                              ${(0,_common__rspack_import_0.escapeText)(title)}
                          </span>
                      </span>
                  </td>
                  <td width="110">
                      <span class="fw-inline">
                          ${(0,_common__rspack_import_0.escapeText)(author)}
                      </span>
                  </td>
                  <td width="50">
                      <span class="fw-inline">
                          ${(0,_common__rspack_import_0.escapeText)(year)}
                      </span>
                  </td>
                  <td width="50" align="center">
                      <span class="order-down fw-inline fw-link-text" data-id="${id}" data-db="${db}">
                          <i class="fa fa-sort-down"></i>
                      </span>
                      <span class="order-up fw-inline fw-link-text" data-id="${id}" data-db="${db}">
                          <i class="fa fa-sort-up"></i>
                      </span>
                  </td>
                  <td width="50" align="center">
                      <span class="delete fw-inline fw-link-text" data-id="${id}" data-db="${db}">
                          <i class="fa fa-trash-alt"></i>
                      </span>
                  </td>
              </tr>
              <tr>
                  <td class="cite-extra-fields" colspan="3" width="270">
                      <div>
                          <label>${gettext("Page")}</label>
                          <input class="fw-cite-page" type="text" value="${(0,_common__rspack_import_0.escapeText)(locator)}" />
                      </div>
                      <div>
                          <label>${gettext("Text before")}</label>
                          <input class="fw-cite-text" type="text" value="${(0,_common__rspack_import_0.escapeText)(prefix)}" />
                      </div>
                  </td>
              </tr>
          </table>
      </td>
    </tr>`

const contributorTemplate = ({contributor}) =>
    `<input type="text" name="firstname" value="${contributor.firstname ? contributor.firstname : ""}" placeholder="${gettext("Firstname")}"/>
    <input type="text" name="lastname" value="${contributor.lastname ? contributor.lastname : ""}" placeholder="${gettext("Lastname")}"/>
    <input type="text" name="email" value="${contributor.email ? contributor.email : ""}" placeholder="${gettext("Email")}"/>
    <input type="text" name="institution" value="${contributor.institution ? contributor.institution : ""}" placeholder="${gettext("Institution")}"/>
    `

const languageTemplate = ({currentLanguage, allowedLanguages}) =>
    `<select class="fw-button fw-light fw-large">
        ${allowedLanguages
            .map(
                language =>
                    `<option value="${language[0]}" ${language[0] === currentLanguage ? "selected" : ""}>
                    ${language[1]}
                </option>`
            )
            .join("")}
    </select>`


}),
"./js/modules/editor/document_template/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModDocumentTemplate: function() { return ModDocumentTemplate; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _exporter_native__rspack_import_1 = __webpack_require__("./js/modules/exporter/native/index.js");
/* import */ var _e2ee_key_manager__rspack_import_2 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _e2ee_password_dialog__rspack_import_3 = __webpack_require__("./js/modules/editor/e2ee/password-dialog.js");





class ModDocumentTemplate {
    constructor(editor) {
        editor.mod.documentTemplate = this
        this.editor = editor
        this.exportTemplates = []
        this.documentStyles = []
    }

    setStyles(styles) {
        this.exportTemplates = styles.export_templates
        this.documentStyles = styles.document_styles
        this.documentTemplates = styles.document_templates
        this.addExportTemplateMenuEntries()
        this.addDocumentStylesMenuEntries()
        if (Object.keys(this.documentTemplates).length) {
            this.addCopyAsMenuEntry()
        }
        if (this.editor.menu.headerView) {
            this.editor.menu.headerView.update()
        }
        //Cache the template files using Service Worker
        for (const key in styles.export_templates) {
            const template = styles.export_templates[key]
            ;(0,_common__rspack_import_0.get)(template.template_file)
        }
        //Cache the required font related files too!
        this.documentStyles.forEach(docStyle => {
            docStyle.documentstylefile_set.forEach(([url, _filename]) =>
                (0,_common__rspack_import_0.get)(url)
            )
        })
    }

    addDocPartSettings() {
        const hideableDocParts = []
        this.editor.view.state.doc.forEach((child, _offset, index) => {
            if (child.attrs.optional) {
                hideableDocParts.push({
                    title: child.attrs.title,
                    id: child.attrs.id,
                    index
                })
            }
        })
        if (!hideableDocParts.length) {
            return
        }
        const metadataMenu = {
            id: "metadata",
            title: gettext("Optional sections"),
            type: "menu",
            tooltip: gettext("Choose which optional sections to enable."),
            order: 0,
            disabled: editor => editor.docInfo.access_rights !== "write",
            content: hideableDocParts.map(docPart => ({
                title: docPart.title,
                type: "setting",
                tooltip: `${gettext("Show/hide")} ${docPart.title}`,
                order: docPart.index,
                action: editor => {
                    let offset = 0
                    for (let i = 0; i < docPart.index; i++) {
                        offset += editor.view.state.doc.child(i).nodeSize
                    }
                    const node = editor.view.state.doc.child(docPart.index)
                    editor.view.dispatch(
                        editor.view.state.tr
                            .setNodeMarkup(
                                offset,
                                false,
                                Object.assign({}, node.attrs, {
                                    hidden: !node.attrs.hidden
                                })
                            )
                            .setMeta("settings", true)
                    )
                },
                selected: editor =>
                    !editor.view.state.doc.child(docPart.index).attrs.hidden
            }))
        }
        const settingsMenu = this.editor.menu.headerbarModel.content.find(
            menu => menu.id === "settings"
        )
        settingsMenu.content = settingsMenu.content.filter(
            item => item.id !== "metadata"
        )
        settingsMenu.content.unshift(metadataMenu)
    }

    addCopyAsMenuEntry() {
        const fileMenu = this.editor.menu.headerbarModel.content.find(
            menu => menu.id === "file"
        )
        // Cancel if run already
        if (fileMenu.content.find(menuItem => menuItem.id === "copy_as")) {
            return
        }
        fileMenu.content.push({
            id: "copy_as",
            title: gettext("Create copy as ..."),
            type: "action",
            tooltip: gettext(
                "Create copy of the current document with a specific template."
            ),
            order: 3.5,
            action: editor => {
                const isE2EE = editor.docInfo.e2ee
                const e2eeMode = editor.app.settings.E2EE_MODE
                const canToggleE2EE =
                    e2eeMode === "enabled" ||
                    (e2eeMode === "required" && !isE2EE) ||
                    (e2eeMode === "disabled" && isE2EE)

                let e2eeHtml = ""
                if (canToggleE2EE) {
                    e2eeHtml = `
                        <div class="e2ee-copy-toggle" style="margin-top: 15px;">
                            <label>
                                <input type="checkbox" id="e2ee-copy-toggle" ${e2eeMode === "required" || isE2EE ? "checked" : ""}>
                                ${gettext("Encrypt the copy")}
                            </label>
                        </div>
                    `
                }

                const selectTemplateDialog = new _common__rspack_import_0.Dialog({
                    title: gettext("Choose document template"),
                    body: `<p>
                        ${gettext("Select document template for copy.")}
                        </p>
                        <select class="fw-button fw-large fw-light">${Object.entries(
                            editor.mod.documentTemplate.documentTemplates
                        )
                            .map(
                                ([importId, dt]) =>
                                    `<option value="${(0,_common__rspack_import_0.escapeText)(importId)}">${(0,_common__rspack_import_0.escapeText)(dt.title)}</option>`
                            )
                            .join("")}</select>
                        ${e2eeHtml}`,
                    buttons: [
                        {
                            text: gettext("Copy"),
                            classes: "fw-dark",
                            click: () => {
                                if (editor.app.isOffline()) {
                                    (0,_common__rspack_import_0.addAlert)(
                                        "error",
                                        "You are offline. Please try again after you are online."
                                    )
                                    selectTemplateDialog.close()
                                    return
                                }

                                const targetE2EE =
                                    canToggleE2EE &&
                                    selectTemplateDialog.dialogEl.querySelector(
                                        "#e2ee-copy-toggle"
                                    )?.checked

                                const doCopy = e2eeOptions => {
                                    const copier = new _exporter_native__rspack_import_1.SaveCopy(
                                        editor.getDoc(),
                                        editor.mod.db.bibDB,
                                        editor.mod.db.imageDB,
                                        editor.user,
                                        selectTemplateDialog.dialogEl.querySelector(
                                            "select"
                                        ).value,
                                        e2eeOptions
                                    )
                                    copier
                                        .init()
                                        .then(({docInfo}) => {
                                            const url = targetE2EE
                                                ? `/document/${docInfo.id}/?e2ee=1`
                                                : `/document/${docInfo.id}/`
                                            editor.app.goTo(url)
                                        })
                                        .catch(() => false)
                                    selectTemplateDialog.close()
                                }

                                if (isE2EE && !targetE2EE) {
                                    // Decrypting: need source key
                                    if (editor.e2ee && editor.e2ee.key) {
                                        doCopy({
                                            sourceKey: editor.e2ee.key,
                                            targetE2EE: false
                                        })
                                    } else {
                                        (0,_e2ee_password_dialog__rspack_import_3.enterPasswordDialog)(async password => {
                                            try {
                                                const key =
                                                    await _e2ee_key_manager__rspack_import_2.E2EEKeyManager.deriveKey(
                                                        password,
                                                        new Uint8Array(
                                                            atob(
                                                                editor.docInfo
                                                                    .e2ee_salt
                                                            )
                                                                .split("")
                                                                .map(c =>
                                                                    c.charCodeAt(
                                                                        0
                                                                    )
                                                                )
                                                        ),
                                                        editor.docInfo
                                                            .e2ee_iterations ||
                                                            600000
                                                    )
                                                doCopy({
                                                    sourceKey: key,
                                                    targetE2EE: false
                                                })
                                            } catch (_err) {
                                                (0,_common__rspack_import_0.addAlert)(
                                                    "error",
                                                    gettext(
                                                        "Incorrect password."
                                                    )
                                                )
                                            }
                                        })
                                    }
                                } else if (!isE2EE && targetE2EE) {
                                    // Encrypting: need new password
                                    (0,_e2ee_password_dialog__rspack_import_3.createPasswordDialog)(password => {
                                        doCopy({
                                            targetE2EE: true,
                                            targetPassword: password
                                        })
                                    })
                                } else if (isE2EE && targetE2EE) {
                                    // E2EE -> E2EE: need source key, then new password
                                    const handlePasswords = sourceKey => {
                                        (0,_e2ee_password_dialog__rspack_import_3.createPasswordDialog)(password => {
                                            doCopy({
                                                sourceKey: sourceKey,
                                                targetE2EE: true,
                                                targetPassword: password
                                            })
                                        })
                                    }
                                    if (editor.e2ee && editor.e2ee.key) {
                                        handlePasswords(editor.e2ee.key)
                                    } else {
                                        (0,_e2ee_password_dialog__rspack_import_3.enterPasswordDialog)(async password => {
                                            try {
                                                const key =
                                                    await _e2ee_key_manager__rspack_import_2.E2EEKeyManager.deriveKey(
                                                        password,
                                                        new Uint8Array(
                                                            atob(
                                                                editor.docInfo
                                                                    .e2ee_salt
                                                            )
                                                                .split("")
                                                                .map(c =>
                                                                    c.charCodeAt(
                                                                        0
                                                                    )
                                                                )
                                                        ),
                                                        editor.docInfo
                                                            .e2ee_iterations ||
                                                            600000
                                                    )
                                                handlePasswords(key)
                                            } catch (_err) {
                                                (0,_common__rspack_import_0.addAlert)(
                                                    "error",
                                                    gettext(
                                                        "Incorrect password."
                                                    )
                                                )
                                            }
                                        })
                                    }
                                } else {
                                    // Plain -> plain
                                    doCopy(null)
                                }
                            }
                        },
                        {
                            type: "cancel"
                        }
                    ]
                })
                selectTemplateDialog.open()
            },
            disabled: editor => editor.app.isOffline()
        })

        fileMenu.content = fileMenu.content.sort((a, b) => a.order - b.order)
    }

    addExportTemplateMenuEntries() {
        const exportMenu = this.editor.menu.headerbarModel.content.find(
            menu => menu.id === "export"
        )
        // Remove any previous entries in case we run this a second time
        exportMenu.content = exportMenu.content.filter(
            menuItem => menuItem.class !== "export_template"
        )
        // Find highest menu item under 100 to put templates at end of native exporter options.
        let order = 1
        exportMenu.content.forEach(menuItem => {
            if (menuItem.order < 100 && menuItem.order > order) {
                order = menuItem.order
            }
        })
        const exportMenuEntries = this.exportTemplates.map(template => {
            if (template.file_type === "docx") {
                return {
                    class: "export_template",
                    title: `${template.title} (DOCX)`,
                    type: "action",
                    order: ++order,
                    tooltip: gettext(
                        "Export the document to a DOCX file with the given template."
                    ),
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_fast-xml-parser_4_5_0_node_modules_fast-xml-parser_src_fxp_js"), __webpack_require__.e("vendors-node_modules_pnpm_mathml2omml_0_5_0_node_modules_mathml2omml_dist_index_js"), __webpack_require__.e("js_modules_bibliography_schema_csl_bib_js-js_modules_exporter_tools_doc_content_js-js_modules-d5afa0"), __webpack_require__.e("js_modules_exporter_tools_xml_js"), __webpack_require__.e("js_modules_exporter_docx_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/docx/index.js")).then(({DOCXExporter}) => {
                            const exporter = new DOCXExporter(
                                editor.getDoc(),
                                template.template_file,
                                editor.mod.db.bibDB,
                                editor.mod.db.imageDB,
                                editor.app.csl
                            )
                            exporter.init()
                        })
                    },
                    disabled: editor => editor.app.isOffline()
                }
            } else {
                return {
                    class: "export_template",
                    title: `${template.title} (ODT)`,
                    type: "action",
                    order: ++order,
                    tooltip: gettext(
                        "Export the document to an ODT file with the given template."
                    ),
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_fast-xml-parser_4_5_0_node_modules_fast-xml-parser_src_fxp_js"), __webpack_require__.e("js_modules_bibliography_schema_csl_bib_js-js_modules_exporter_tools_doc_content_js-js_modules-d5afa0"), __webpack_require__.e("js_modules_exporter_tools_xml_js"), __webpack_require__.e("js_modules_exporter_odt_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/odt/index.js")).then(({ODTExporter}) => {
                            const exporter = new ODTExporter(
                                editor.getDoc(),
                                template.template_file,
                                editor.mod.db.bibDB,
                                editor.mod.db.imageDB,
                                editor.app.csl
                            )
                            exporter.init()
                        })
                    },
                    disabled: editor => editor.app.isOffline()
                }
            }
        })
        exportMenu.content = exportMenu.content.concat(exportMenuEntries)
        exportMenu.content = exportMenu.content.sort(
            (a, b) => a.order - b.order
        )
    }

    addDocumentStylesMenuEntries() {
        const settingsMenu = this.editor.menu.headerbarModel.content.find(
                menu => menu.id === "settings"
            ),
            documentStyleMenu = settingsMenu.content.find(
                menu => menu.id === "document_style"
            )

        documentStyleMenu.content = this.documentStyles.map(docStyle => {
            return {
                title: docStyle.title,
                type: "setting",
                action: editor => {
                    editor.view.dispatch(
                        editor.view.state.tr
                            .setDocAttribute("documentstyle", docStyle.slug)
                            .setMeta("settings", true)
                    )
                },
                selected: editor =>
                    editor.view.state.doc.attrs.documentstyle === docStyle.slug,
                disabled: editor => editor.app.isOffline()
            }
        })
    }

    getCitationStyles() {
        return this.editor.app.csl.getStyles().then(styles => {
            this.citationStyles = styles
        })
    }

    addCitationStylesMenuEntries() {
        const settingsMenu = this.editor.menu.headerbarModel.content.find(
                menu => menu.id === "settings"
            ),
            citationStyleMenu = settingsMenu.content.find(
                menu => menu.id === "citation_style"
            )
        if (citationStyleMenu) {
            citationStyleMenu.content =
                this.editor.view.state.doc.attrs.citationstyles.map(
                    citationstyle => {
                        return {
                            title: this.citationStyles[citationstyle],
                            type: "setting",
                            action: editor => {
                                editor.view.dispatch(
                                    editor.view.state.tr
                                        .setDocAttribute(
                                            "citationstyle",
                                            citationstyle
                                        )
                                        .setMeta("settings", true)
                                )
                            },
                            selected: editor => {
                                return (
                                    editor.view.state.doc.attrs
                                        .citationstyle === citationstyle
                                )
                            }
                        }
                    }
                )
        }
    }
}


}),
"./js/modules/editor/e2ee/snapshot-manager.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  E2EESnapshotManager: function() { return E2EESnapshotManager; }
});
/**
 * E2EE Snapshot Manager - Handles encrypted snapshot requests and saves
 * for end-to-end encrypted documents.
 *
 * For E2EE documents, the server cannot process encrypted content, so clients
 * are responsible for saving periodic snapshots. When the server's
 * DOC_SAVE_INTERVAL is reached, it sends a "request_snapshot" message to
 * one of the write-capable clients. This module handles that request by:
 *
 * 1. Collecting the current document state (content, comments, bibliography)
 * 2. Encrypting each field with the document's AES-GCM key
 * 3. Sending an "e2ee_snapshot" message to the server for persistence
 *
 * The server stores the encrypted snapshot as opaque data and notifies
 * other clients via an "e2ee_snapshot_received" message.
 */
class E2EESnapshotManager {
    /**
     * @param {Editor} editor - The Fidus Writer editor instance
     */
    constructor(editor) {
        this.editor = editor
        this.pendingSave = false
        this.key = null
    }

    /**
     * Set the encryption key for this document.
     * Called after the user enters the password and the key is derived.
     *
     * @param {CryptoKey} key - An AES-GCM key (from E2EEKeyManager.deriveKey)
     */
    setKey(key) {
        this.key = key
    }

    /**
     * Clear the encryption key (e.g., when the document is closed).
     */
    clearKey() {
        this.key = null
    }

    /**
     * Check whether the snapshot manager has a key set.
     *
     * @returns {boolean}
     */
    get hasKey() {
        return this.key !== null
    }

    /**
     * Build an encrypted snapshot payload for the current document state.
     *
     * Returns the encrypted data without sending it. Used in
     * non-collaborative mode where snapshots are sent via REST instead
     * of WebSocket.
     *
     * @returns {Object|null} The encrypted snapshot payload or null if no key.
     */
    async getEncryptedSnapshot() {
        if (!this.key) {
            return null
        }
        const content = this.editor.view.docView.node.toJSON()
        const comments = this.editor.docInfo.comments || {}
        const bibliography = this.editor.docInfo.bibliography || {}
        const {title} = this.editor.getDoc()

        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))

        const encryptedContent = await E2EEEncryptor.encryptObject(
            content,
            this.key
        )
        const encryptedComments = await E2EEEncryptor.encryptObject(
            comments,
            this.key
        )
        const encryptedBibliography = await E2EEEncryptor.encryptObject(
            bibliography,
            this.key
        )
        const encryptedTitle = await E2EEEncryptor.encrypt(title, this.key)

        return {
            content: encryptedContent,
            comments: encryptedComments,
            bibliography: encryptedBibliography,
            title: encryptedTitle,
            e2ee_salt: this.editor.e2ee.encryptionSalt,
            e2ee_iterations: this.editor.e2ee.encryptionIterations,
            v: this.editor.docInfo.version
        }
    }

    /**
     * Handle a "request_snapshot" message from the server.
     *
     * The server sends this message when it needs a client to save
     * an encrypted snapshot (typically after DOC_SAVE_INTERVAL diffs).
     * Only write-capable clients should respond.
     *
     * @param {Object} request - The request_snapshot message
     * @param {number} request.v - The server's current document version
     */
    async handleRequestSnapshot(_request) {
        if (this.pendingSave) {
            return
        }
        if (!this.key) {
            return
        }
        if (this.editor.docInfo.access_rights !== "write") {
            return
        }

        this.pendingSave = true

        const docInfo = this.editor.docInfo

        // Get the current document content as JSON
        const content = this.editor.view.docView.node.toJSON()
        const comments = docInfo.comments || {}
        const bibliography = docInfo.bibliography || {}

        // Extract the current title from the editor
        const {title} = this.editor.getDoc()

        // Dynamically import to avoid circular dependencies
        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))

        // Encrypt all document data
        const encryptedContent = await E2EEEncryptor.encryptObject(
            content,
            this.key
        )
        const encryptedComments = await E2EEEncryptor.encryptObject(
            comments,
            this.key
        )
        const encryptedBibliography = await E2EEEncryptor.encryptObject(
            bibliography,
            this.key
        )
        const encryptedTitle = await E2EEEncryptor.encrypt(title, this.key)

        // Send snapshot to server
        this.editor.ws.send(() => ({
            type: "e2ee_snapshot",
            v: docInfo.version,
            content: encryptedContent,
            comments: encryptedComments,
            bibliography: encryptedBibliography,
            e2ee_salt: this.editor.e2ee.encryptionSalt,
            title: encryptedTitle
        }))
        this.pendingSave = false
    }

    /**
     * Handle an "e2ee_snapshot_received" message from the server.
     *
     * The server sends this to all clients (except the one that sent
     * the snapshot) to notify them that a new snapshot has been saved.
     * This is informational — clients don't need to take any action
     * since they already have the latest state from the diffs they've
     * been receiving.
     *
     * @param {Object} message - The e2ee_snapshot_received message
     * @param {number} message.v - The version of the saved snapshot
     */
    async handleSnapshotReceived(message) {
        // Informational — no action needed for regular snapshots.
        // If the server included new KDF params, the password was changed
        // by another client. Update local state so that if we re-fetch
        // we use the new salt.
        if (message.e2ee_salt) {
            this.editor.e2ee.encryptionSalt = message.e2ee_salt
            this.editor.e2ee.encryptionIterations = message.e2ee_iterations
            this.editor.e2ee.key = null
            this.clearKey()
            // Clear cached password and key from sessionStorage so the user
            // is prompted for the new password on next access.
            const {E2EEKeyManager} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/key-manager.js"))
            E2EEKeyManager.clearKeyFromSession(this.editor.docInfo.id)
            E2EEKeyManager.clearPasswordFromSession(this.editor.docInfo.id)
            // Notify the user that the password has changed.
            const {showSystemMessage} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/common/index.js"))
            showSystemMessage(
                gettext(
                    "The document password was changed by another user. You will need to enter the new password to continue editing."
                )
            )
        }
    }

    /**
     * Send an initial snapshot for a newly created E2EE document.
     *
     * When a new E2EE document is created, the initial content (from
     * the template) needs to be encrypted and saved as the first snapshot.
     * This method handles that initial save.
     *
     * @param {Object} content - The document content (ProseMirror JSON)
     * @param {Object} comments - The comments object (usually empty {})
     * @param {Object} bibliography - The bibliography object (usually {})
     * @param {number} version - The document version (usually 0)
     */
    async sendInitialSnapshot(content, comments, bibliography, version) {
        if (!this.key) {
            return
        }

        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))

        const encryptedContent = await E2EEEncryptor.encryptObject(
            content,
            this.key
        )
        const encryptedComments = await E2EEEncryptor.encryptObject(
            comments,
            this.key
        )
        const encryptedBibliography = await E2EEEncryptor.encryptObject(
            bibliography,
            this.key
        )

        // Extract the current title from the editor
        const {title} = this.editor.getDoc()
        const encryptedTitle = await E2EEEncryptor.encrypt(title, this.key)

        this.editor.ws.send(() => ({
            type: "e2ee_snapshot",
            v: version,
            content: encryptedContent,
            comments: encryptedComments,
            bibliography: encryptedBibliography,
            e2ee_salt: this.editor.e2ee.encryptionSalt,
            title: encryptedTitle
        }))
    }

    /**
     * Re-encrypt the document with a new key (for password changes).
     *
     * When the user changes the document password, a new key is derived
     * and the entire document must be re-encrypted. This method handles
     * that by collecting the current document state, encrypting it with
     * the new key, and sending a snapshot.
     *
     * @param {CryptoKey} newKey - The new AES-GCM key derived from the new password
     * @param {string} newSaltBase64 - The new salt (Base64-encoded) to send to the server
     * @param {number} newIterations - The new iteration count
     */
    async reEncryptWithNewKey(newKey, newSaltBase64, newIterations) {
        if (this.pendingSave) {
            return
        }
        this.pendingSave = true

        const docInfo = this.editor.docInfo
        const content = this.editor.view.docView.node.toJSON()
        const comments = docInfo.comments || {}
        const bibliography = docInfo.bibliography || {}

        // Extract the current title from the editor
        const {title} = this.editor.getDoc()

        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))

        // Encrypt with the new key
        const encryptedContent = await E2EEEncryptor.encryptObject(
            content,
            newKey
        )
        const encryptedComments = await E2EEEncryptor.encryptObject(
            comments,
            newKey
        )
        const encryptedBibliography = await E2EEEncryptor.encryptObject(
            bibliography,
            newKey
        )
        const encryptedTitle = await E2EEEncryptor.encrypt(title, newKey)

        // Update the key
        this.key = newKey

        // Send the re-encrypted snapshot along with the new KDF parameters
        this.editor.ws.send(() => ({
            type: "e2ee_snapshot",
            v: docInfo.version,
            content: encryptedContent,
            comments: encryptedComments,
            bibliography: encryptedBibliography,
            e2ee_salt: newSaltBase64,
            e2ee_iterations: newIterations,
            title: encryptedTitle
        }))
        this.pendingSave = false
    }
}


}),
"./js/modules/editor/footnotes/editor.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModFootnoteEditor: function() { return ModFootnoteEditor; }
});
/* import */ var prosemirror_collab__rspack_import_9 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_commands__rspack_import_7 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_dropcursor__rspack_import_10 = __webpack_require__("./node_modules/.pnpm/prosemirror-dropcursor@1.8.1/node_modules/prosemirror-dropcursor/dist/index.js");
/* import */ var prosemirror_example_setup__rspack_import_8 = __webpack_require__("./node_modules/.pnpm/prosemirror-example-setup@1.2.3/node_modules/prosemirror-example-setup/dist/index.js");
/* import */ var prosemirror_gapcursor__rspack_import_11 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_history__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_state__rspack_import_13 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_14 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var prosemirror_view__rspack_import_12 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _schema_footnotes__rspack_import_0 = __webpack_require__("./js/modules/schema/footnotes.js");
/* import */ var _schema_footnotes_convert__rspack_import_1 = __webpack_require__("./js/modules/schema/footnotes_convert.js");
/* import */ var _state_plugins__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _track__rspack_import_3 = __webpack_require__("./js/modules/editor/track/index.js");
/* import */ var _state_plugins__rspack_import_4 = __webpack_require__("./js/modules/editor/footnotes/state_plugins/index.js");

















/* Functions related to the footnote editor instance */
class ModFootnoteEditor {
    constructor(mod) {
        mod.fnEditor = this
        this.mod = mod
        this.schema = _schema_footnotes__rspack_import_0.fnSchema
        this.fnStatePlugins = [
            [_state_plugins__rspack_import_2.linksPlugin, () => ({editor: this.mod.editor})],
            [prosemirror_history__rspack_import_5.history],
            [prosemirror_keymap__rspack_import_6.keymap, () => prosemirror_commands__rspack_import_7.baseKeymap],
            [prosemirror_keymap__rspack_import_6.keymap, () => (0,prosemirror_example_setup__rspack_import_8.buildKeymap)(this.schema)],
            [prosemirror_collab__rspack_import_9.collab, () => ({clientID: this.mod.editor.client_id})],
            [prosemirror_dropcursor__rspack_import_10.dropCursor],
            [prosemirror_gapcursor__rspack_import_11.gapCursor],
            [_state_plugins__rspack_import_2.selectionMenuPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.toolbarPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.citationRenderPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.collabCaretsPlugin, () => ({editor: this.mod.editor})],
            [
                _state_plugins__rspack_import_2.clipboardPlugin,
                () => ({editor: this.mod.editor, viewType: "footnotes"})
            ],
            [_state_plugins__rspack_import_4.accessRightsPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.commentsPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.trackPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.marginboxesPlugin, () => ({editor: this.mod.editor})],
            [_state_plugins__rspack_import_2.searchPlugin],
            [_state_plugins__rspack_import_2.figurePlugin, () => ({editor: this.mod.editor})]
        ]
    }

    init() {
        const doc = this.schema.nodeFromJSON({type: "doc", content: []}),
            plugins = this.fnStatePlugins.map(plugin => {
                if (plugin[1]) {
                    return plugin[0](plugin[1](doc))
                } else {
                    return plugin[0](doc)
                }
            })

        this.view = new prosemirror_view__rspack_import_12.EditorView(
            document.getElementById("footnote-box-container"),
            {
                state: prosemirror_state__rspack_import_13.EditorState.create({
                    schema: this.schema,
                    doc,
                    plugins
                }),
                handleDOMEvents: {
                    focus: (_view, _event) => {
                        this.mod.editor.currentView = this.view
                    }
                },
                dispatchTransaction: tr => {
                    const remote = tr.getMeta("remote"),
                        fromMain = tr.getMeta("fromMain")
                    // Skip if creating new footnote by typing directly into empty footnote editor.
                    if (
                        tr.docChanged &&
                        tr.steps[0].jsonID === "replace" &&
                        tr.steps[0].from === 0 &&
                        tr.steps[0].to === 0 &&
                        !remote &&
                        !fromMain
                    ) {
                        return
                    }
                    const trackedTr = (0,_track__rspack_import_3.amendTransaction)(
                        tr,
                        this.view.state,
                        this.mod.editor
                    )
                    const {state: newState, transactions} =
                        this.view.state.applyTransaction(trackedTr)
                    this.view.updateState(newState)
                    transactions.forEach(subTr => this.onTransaction(subTr))
                    this.mod.layout.updateDOM()
                }
            }
        )
    }

    // Find out if we need to recalculate the bibliography
    onTransaction(tr) {
        const mainMeta = tr.getMeta("toMain")
        if (tr.getMeta("remote") || (!mainMeta && !tr.docChanged)) {
            return
        }
        const mainTr = this.mod.editor.view.state.tr,
            mainState = this.mod.editor.view.state

        if (tr.docChanged) {
            const fns = [
                ...new Set(
                    tr.steps
                        .map((step, index) => {
                            if (!step.hasOwnProperty("from")) {
                                return -1
                            }
                            // Full insertion or deletion of footnotes mean that the from will be in-between footnotes
                            // and have a depth of zero. We ignore these changes as they will have originated from
                            // the main editor.
                            const $pos = tr.docs[index].resolve(step.from)
                            if ($pos.depth === 0) {
                                return -1
                            }
                            return $pos.index(0)
                        })
                        .filter(index => index > -1)
                )
            ]

            fns.forEach(fnIndex => {
                const fnContent = tr.doc.child(fnIndex).toJSON().content
                ;(0,_state_plugins__rspack_import_2.updateFootnoteMarker)(mainState, mainTr, fnIndex, fnContent)
            })
        }

        if (mainMeta) {
            Object.entries(mainMeta).forEach(([key, value]) => {
                mainTr.setMeta(key, value)
            })
        }
        if (mainMeta || mainTr.docChanged) {
            this.mod.editor.view.dispatch(mainTr)
        }
    }

    applyDiffs(diffs, cid) {
        const steps = diffs.map(j => prosemirror_transform__rspack_import_14.Step.fromJSON(this.view.state.schema, j))
        const clientIds = diffs.map(_ => cid)
        const tr = (0,prosemirror_collab__rspack_import_9.receiveTransaction)(this.view.state, steps, clientIds)
        tr.setMeta("remote", true)
        this.view.dispatch(tr)
    }

    renderAllFootnotes() {
        const content = (0,_state_plugins__rspack_import_2.getFootnoteMarkerContents)(
            this.mod.editor.view.state
        ).map(fnContent => ({
            type: "footnotecontainer",
            content: fnContent
        }))
        const doc = this.schema.nodeFromJSON({type: "doc", content}),
            plugins = this.fnStatePlugins.map(plugin => {
                if (plugin[1]) {
                    return plugin[0](plugin[1](doc))
                } else {
                    return plugin[0]()
                }
            }),
            newState = prosemirror_state__rspack_import_13.EditorState.create({
                schema: this.schema,
                doc,
                plugins
            })
        this.view.updateState(newState)
    }

    renderFootnote(content, index = 0, tr) {
        const node = (0,_schema_footnotes_convert__rspack_import_1.fnNodeToPmNode)(content)
        let pos = 0
        for (let i = 0; i < index; i++) {
            pos += tr.doc.child(i).nodeSize
        }
        tr.insert(pos, node)
    }

    removeFootnote(index, tr) {
        if (this.mod.editor.mod.collab.doc.receiving) {
            return
        }

        let startPos = 0
        for (let i = 0; i < index; i++) {
            startPos += this.view.state.doc.child(i).nodeSize
        }
        const endPos = startPos + this.view.state.doc.child(index).nodeSize
        tr.delete(startPos, endPos)
    }
}


}),
"./js/modules/editor/footnotes/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModFootnotes: function() { return ModFootnotes; }
});
/* import */ var _editor__rspack_import_0 = __webpack_require__("./js/modules/editor/footnotes/editor.js");
/* import */ var _layout__rspack_import_1 = __webpack_require__("./js/modules/editor/footnotes/layout.js");



class ModFootnotes {
    constructor(editor) {
        editor.mod.footnotes = this
        this.editor = editor
        new _editor__rspack_import_0.ModFootnoteEditor(this)
        new _layout__rspack_import_1.ModFootnoteLayout(this)
    }

    init() {
        this.fnEditor.init()
        this.layout.init()
    }
}


}),
"./js/modules/editor/footnotes/layout.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModFootnoteLayout: function() { return ModFootnoteLayout; }
});
/* import */ var fastdom__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fastdom@1.0.11/node_modules/fastdom/fastdom.js");
/* import */ var fastdom__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fastdom__rspack_import_0);
/* import */ var _state_plugins__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/index.js");



/* A class to make footnotes appear correctly off the side of their referrer. */
class ModFootnoteLayout {
    constructor(mod) {
        mod.layout = this
        this.mod = mod
    }

    init() {
        // Add two elements to hold dynamic CSS info about comments.
        const styleContainers = document.createElement("temp")
        styleContainers.innerHTML =
            '<style type="text/css" id="footnote-placement-style"></style>'
        while (styleContainers.firstElementChild) {
            document.body.appendChild(styleContainers.firstElementChild)
        }
    }

    updateDOM() {
        // Handle the CSS layout of the footnotes on the screen.
        // DOM write phase - nothing to do.
        fastdom__rspack_import_0_default().measure(() => {
            // DOM read phase
            const footnoteBoxes = document.querySelectorAll(
                    "#footnote-box-container .footnote-container"
                ),
                referrers = (0,_state_plugins__rspack_import_1.getFootnoteMarkers)(this.mod.editor.view.state)
            if (
                (!referrers.length &&
                    this.mod.editor.mod.citations.citationType !== "note") ||
                referrers.length !== footnoteBoxes.length
            ) {
                // Apparently not all footnote boxes have been drawn or there are none. Abort for now.
                return
            }
            const footnoteBoxContainer = document.getElementById(
                "footnote-box-container"
            )
            let footnotePlacementStyle = ""

            if (getComputedStyle(footnoteBoxContainer).display === "block") {
                // We are in mobile/tablet mode. We don't need to place footnotes.
            } else if (this.mod.editor.mod.citations.citationType === "note") {
                const totalOffset =
                    footnoteBoxContainer.getBoundingClientRect().top
                /* Citations are also in footnotes, so both citation footnotes
                 * and editor footnotes have to be placed. They should be placed
                 * in the order the markers appear in the content, even though
                 * editor footnotes and citations footnotes are separated in the DOM.
                 */
                const citationFootnotes = document.querySelectorAll(
                    "#citation-footnote-box-container .footnote-citation"
                )
                let editorFootnoteIndex = 0,
                    citationFootnoteIndex = 0,
                    totalEditorOffset = totalOffset,
                    totalCitationOffset = totalOffset
                this.mod.editor.view.state.doc.descendants((node, pos) => {
                    if (
                        node.isInline &&
                        (node.type.name === "footnote" ||
                            node.type.name === "citation")
                    ) {
                        let topMargin = 10
                        if (node.type.name === "footnote") {
                            const footnoteBox =
                                    footnoteBoxes[editorFootnoteIndex],
                                selector = `.footnote-container:nth-of-type(${editorFootnoteIndex + 1})`,
                                footnoteBoxCoords =
                                    footnoteBox.getBoundingClientRect(),
                                footnoteBoxHeight = footnoteBoxCoords.height,
                                referrerTop =
                                    this.mod.editor.view.coordsAtPos(pos).top
                            editorFootnoteIndex++
                            if (!referrerTop) {
                                // footnote is not shown. Also hide the footnote from the editor.
                                footnotePlacementStyle += `${selector} {display: none;}\n`
                                return
                            }
                            if (
                                referrerTop > totalEditorOffset ||
                                totalEditorOffset <
                                    totalCitationOffset + topMargin
                            ) {
                                topMargin = Number.parseInt(
                                    Math.max(
                                        referrerTop - totalEditorOffset,
                                        totalCitationOffset -
                                            totalEditorOffset +
                                            topMargin
                                    )
                                )
                                footnotePlacementStyle += `${selector} {margin-top: ${topMargin}px;}\n`
                            }
                            totalEditorOffset += footnoteBoxHeight + topMargin
                        } else {
                            if (
                                citationFootnotes.length > citationFootnoteIndex
                            ) {
                                const footnoteBox =
                                        citationFootnotes[
                                            citationFootnoteIndex
                                        ],
                                    selector =
                                        ".footnote-citation:nth-of-type(" +
                                        (citationFootnoteIndex + 1) +
                                        ")",
                                    footnoteBoxCoords =
                                        footnoteBox.getBoundingClientRect(),
                                    footnoteBoxHeight =
                                        footnoteBoxCoords.height,
                                    referrerTop =
                                        this.mod.editor.view.coordsAtPos(
                                            pos
                                        ).top
                                citationFootnoteIndex++
                                if (!referrerTop) {
                                    // footnote is not shown. Also hide the footnote from the editor.
                                    footnotePlacementStyle += `${selector} {display: none;}\n`
                                    return
                                }
                                if (
                                    referrerTop > totalCitationOffset ||
                                    totalCitationOffset <
                                        totalEditorOffset + topMargin
                                ) {
                                    topMargin = Number.parseInt(
                                        Math.max(
                                            referrerTop - totalCitationOffset,
                                            topMargin +
                                                totalEditorOffset -
                                                totalCitationOffset
                                        )
                                    )
                                    footnotePlacementStyle += `${selector} {margin-top: ${topMargin}px;}\n`
                                }
                                totalCitationOffset +=
                                    footnoteBoxHeight + topMargin
                            }
                        }
                    }
                })
            } else {
                let totalOffset =
                    footnoteBoxContainer.getBoundingClientRect().top
                /* Only editor footnotes (no citation footnotes) need to be layouted.
                 * We use the existing footnote markers referrers to find the
                 * placement.
                 */
                referrers.forEach((referrer, index) => {
                    const footnoteBox = footnoteBoxes[index]

                    const footnoteBoxCoords =
                            footnoteBox.getBoundingClientRect(),
                        footnoteBoxHeight = footnoteBoxCoords.height,
                        referrerTop = this.mod.editor.view.coordsAtPos(
                            referrer.from
                        ).top,
                        selector = `.footnote-container:nth-of-type(${index + 1})`
                    if (!referrerTop) {
                        // footnote is not shown. Also hide the footnote from the editor.
                        footnotePlacementStyle += `${selector} {display: none;}\n`
                        return
                    }
                    let topMargin = 10

                    if (referrerTop > totalOffset) {
                        topMargin = Number.parseInt(referrerTop - totalOffset)
                        footnotePlacementStyle += `${selector} {margin-top: ${topMargin}px;}\n`
                    }
                    totalOffset += footnoteBoxHeight + topMargin
                })
            }

            fastdom__rspack_import_0_default().mutate(() => {
                //DOM write phase
                if (
                    document.getElementById("footnote-placement-style")
                        .innerHTML != footnotePlacementStyle
                ) {
                    document.getElementById(
                        "footnote-placement-style"
                    ).innerHTML = footnotePlacementStyle
                }
            })
        })
    }
}


}),
"./js/modules/editor/footnotes/state_plugins/access_rights.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  accessRightsPlugin: function() { return accessRightsPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("accessRights")

const accessRightsPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        filterTransaction: (tr, _state) => {
            let allowed = true
            const remote = tr.getMeta("remote")
            const fromMain = tr.getMeta("fromMain")
            if (remote || fromMain) {
                return allowed
            }

            if (
                (___rspack_import_0.COMMENT_ONLY_ROLES.includes(
                    options.editor.docInfo.access_rights
                ) ||
                    ___rspack_import_0.READ_ONLY_ROLES.includes(
                        options.editor.docInfo.access_rights
                    )) &&
                tr.docChanged
            ) {
                allowed = false
            }

            if (
                tr.docs.length &&
                tr.docs[0]?.childCount !== tr.doc.childCount
            ) {
                allowed = false
            }

            return allowed
        }
    })


}),
"./js/modules/editor/footnotes/state_plugins/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  accessRightsPlugin: function() { return /* reexport safe */ _access_rights__rspack_import_0.accessRightsPlugin; }
});
/* import */ var _access_rights__rspack_import_0 = __webpack_require__("./js/modules/editor/footnotes/state_plugins/access_rights.js");



}),
"./js/modules/editor/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  COMMENT_ONLY_ROLES: function() { return COMMENT_ONLY_ROLES; },
  Editor: function() { return Editor; },
  READ_ONLY_ROLES: function() { return READ_ONLY_ROLES; },
  REVIEW_ROLES: function() { return REVIEW_ROLES; },
  WRITE_ROLES: function() { return WRITE_ROLES; }
});
/* import */ var prosemirror_collab__rspack_import_30 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_commands__rspack_import_29 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_dropcursor__rspack_import_32 = __webpack_require__("./node_modules/.pnpm/prosemirror-dropcursor@1.8.1/node_modules/prosemirror-dropcursor/dist/index.js");
/* import */ var prosemirror_example_setup__rspack_import_28 = __webpack_require__("./node_modules/.pnpm/prosemirror-example-setup@1.2.3/node_modules/prosemirror-example-setup/dist/index.js");
/* import */ var prosemirror_gapcursor__rspack_import_33 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_history__rspack_import_31 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_27 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_state__rspack_import_35 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var prosemirror_view__rspack_import_34 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _feedback__rspack_import_2 = __webpack_require__("./js/modules/feedback/index.js");
/* import */ var _e2ee_encryptor__rspack_import_3 = __webpack_require__("./js/modules/editor/e2ee/encryptor.js");
/* import */ var _e2ee_key_manager__rspack_import_4 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _e2ee_passphrase_dialog__rspack_import_5 = __webpack_require__("./js/modules/editor/e2ee/passphrase-dialog.js");
/* import */ var _e2ee_passphrase_manager__rspack_import_6 = __webpack_require__("./js/modules/editor/e2ee/passphrase-manager.js");
/* import */ var _e2ee_password_dialog__rspack_import_7 = __webpack_require__("./js/modules/editor/e2ee/password-dialog.js");
/* import */ var _e2ee_snapshot_manager__rspack_import_8 = __webpack_require__("./js/modules/editor/e2ee/snapshot-manager.js");
/* import */ var _plugins_editor__rspack_import_9 = __webpack_require__("./js/plugins/editor/index.js");
/* import */ var _plugins_editor__rspack_import_9_default = /*#__PURE__*/__webpack_require__.n(_plugins_editor__rspack_import_9);
/* import */ var _schema_convert__rspack_import_10 = __webpack_require__("./js/modules/schema/convert.js");
/* import */ var _schema_document__rspack_import_11 = __webpack_require__("./js/modules/schema/document/index.js");
/* import */ var _citations__rspack_import_12 = __webpack_require__("./js/modules/editor/citations/index.js");
/* import */ var _collab__rspack_import_13 = __webpack_require__("./js/modules/editor/collab/index.js");
/* import */ var _comments__rspack_import_14 = __webpack_require__("./js/modules/editor/comments/index.js");
/* import */ var _databases__rspack_import_15 = __webpack_require__("./js/modules/editor/databases/index.js");
/* import */ var _document_template__rspack_import_16 = __webpack_require__("./js/modules/editor/document_template/index.js");
/* import */ var _footnotes__rspack_import_17 = __webpack_require__("./js/modules/editor/footnotes/index.js");
/* import */ var _marginboxes__rspack_import_18 = __webpack_require__("./js/modules/editor/marginboxes/index.js");
/* import */ var _menus__rspack_import_19 = __webpack_require__("./js/modules/editor/menus/index.js");
/* import */ var _navigator__rspack_import_20 = __webpack_require__("./js/modules/editor/navigator/index.js");
/* import */ var _no_collab_save__rspack_import_21 = __webpack_require__("./js/modules/editor/no_collab_save/index.js");
/* import */ var _track__rspack_import_22 = __webpack_require__("./js/modules/editor/track/index.js");
/* import */ var _exporter_native_file__rspack_import_23 = __webpack_require__("./js/modules/exporter/native/file.js");
/* import */ var _images_edit_dialog_model__rspack_import_24 = __webpack_require__("./js/modules/images/edit_dialog/model.js");
/* import */ var _keymap__rspack_import_25 = __webpack_require__("./js/modules/editor/keymap.js");
/* import */ var _state_plugins__rspack_import_26 = __webpack_require__("./js/modules/editor/state_plugins/index.js");







































// UUID v4 pattern for share tokens
const uuid4Pattern =
    /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i

const COMMENT_ONLY_ROLES = ["review", "comment"]
const READ_ONLY_ROLES = ["read", "read-without-comments"]
const REVIEW_ROLES = ["review", "review-tracked"]
const WRITE_ROLES = ["write", "write-tracked", "review-tracked"]

class Editor {
    // A class that contains everything that happens on the editor page.
    // It is currently not possible to initialize more than one editor class, as it
    // contains bindings to menu items, etc. that are uniquely defined.
    constructor({app, user}, path, idString) {
        this.app = app
        // For unauthenticated guests, replace the bare config object with a
        // proper placeholder that has all the fields templates expect.
        this.user = user.is_authenticated
            ? user
            : {id: undefined, username: "", name: "", is_authenticated: false}
        this.mod = {}
        // Whether the editor is currently waiting for a document update. Set to true
        // initially so that diffs that arrive before document has been loaded are not
        // dealt with.
        this.waitingForDocument = true

        this.docInfo = {
            rights: "",
            owner: undefined,
            is_owner: false,
            confirmedDoc: false, // The latest doc as confirmed by the server.
            updated: false, // Latest update time stamp
            dir: "ltr", // standard direction, used in input fields, etc.
            path // Default doc path.
        }
        if (uuid4Pattern.test(idString)) {
            // Share token — store the raw UUID; init() will resolve it to a real doc id
            this.docInfo.id = idString
            this.docInfo.token = idString
        } else {
            let id = Number.parseInt(idString)
            if (isNaN(id)) {
                id = 0
                let templateId = Number.parseInt(idString.slice(1))
                if (isNaN(templateId)) {
                    templateId = 1
                }
                this.docInfo.templateId = templateId
            }
            this.docInfo.id = id
        }
        this.schema = _schema_document__rspack_import_11.docSchema

        this.menu = {
            headerbarModel: (0,_menus__rspack_import_19.headerbarModel)(),
            imageMenuModel: (0,_menus__rspack_import_19.imageMenuModel)(),
            navigatorFilterModel: (0,_menus__rspack_import_19.navigatorFilterModel)(),
            orderedListMenuModel: (0,_menus__rspack_import_19.orderedListMenuModel)(),
            selectionMenuModel: (0,_menus__rspack_import_19.selectionMenuModel)(),
            imageEditModel: (0,_images_edit_dialog_model__rspack_import_24.imageEditModel)(),
            tableMenuModel: (0,_menus__rspack_import_19.tableMenuModel)(),
            figureMenuModel: (0,_menus__rspack_import_19.figureMenuModel)(),
            toolbarModel: (0,_menus__rspack_import_19.toolbarModel)(),
            figureWidthMenuModel: (0,_menus__rspack_import_19.figureWidthMenuModel)(),
            codeBlockMenuModel: (0,_menus__rspack_import_19.codeBlockMenuModel)()
        }
        this.client_id = Math.floor(Math.random() * 0xffffffff)
        this.clientTimeAdjustment = 0

        // E2EE state: holds the encryption key and snapshot manager
        // for end-to-end encrypted documents. Set to an object with
        // { key, snapshotManager, encrypted, encryptionSalt, encryptionIterations }
        // when the document is encrypted and the user has entered the password.
        this.e2ee = null

        this.pathEditable = true // Set to false through plugin to disable path editing.

        this.statePlugins = [
            [prosemirror_keymap__rspack_import_27.keymap, () => (0,_keymap__rspack_import_25.buildEditorKeymap)(this.schema)],
            [prosemirror_keymap__rspack_import_27.keymap, () => (0,prosemirror_example_setup__rspack_import_28.buildKeymap)(this.schema)],
            [prosemirror_keymap__rspack_import_27.keymap, () => prosemirror_commands__rspack_import_29.baseKeymap],
            [prosemirror_collab__rspack_import_30.collab, () => ({clientID: this.client_id})],
            [_state_plugins__rspack_import_26.linksPlugin, () => ({editor: this})],
            [prosemirror_history__rspack_import_31.history],
            [prosemirror_dropcursor__rspack_import_32.dropCursor],
            [prosemirror_gapcursor__rspack_import_33.gapCursor],
            [prosemirror_tables__rspack_import_0.tableEditing],
            [_state_plugins__rspack_import_26.jumpHiddenNodesPlugin],
            [_state_plugins__rspack_import_26.placeholdersPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.citationRenderPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.headerbarPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.toolbarPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.selectionMenuPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.collabCaretsPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.footnoteMarkersPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.commentsPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.marginboxesPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.tagInputPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.contributorInputPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.clipboardPlugin, () => ({editor: this, viewType: "main"})],
            [_state_plugins__rspack_import_26.accessRightsPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.settingsPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.documentTemplatePlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.trackPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.tablePlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.orderedListMenuPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.figurePlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.codeBlockPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.tocRenderPlugin, () => ({editor: this})],
            [_state_plugins__rspack_import_26.searchPlugin]
        ]
    }

    init() {
        (0,_common__rspack_import_1.ensureCSS)([
            staticUrl("css/mathlive.css"),
            staticUrl("css/editor.css"),
            staticUrl("css/tags.css"),
            staticUrl("css/contributors.css"),
            staticUrl("css/document.css"),
            staticUrl("css/carets.css"),
            staticUrl("css/tracking.css"),
            staticUrl("css/margin_boxes.css"),
            staticUrl("css/prosemirror.css"),
            staticUrl("css/footnotes.css"),
            staticUrl("css/chat.css"),
            staticUrl("css/access_rights_dialog.css"),
            staticUrl("css/citation_dialog.css"),
            staticUrl("css/review.css"),
            staticUrl("css/add_remove_dialog.css"),
            staticUrl("css/bibliography.css"),
            staticUrl("css/dot_menu.css"),
            staticUrl("css/cropper.min.css"),
            staticUrl("css/inline_tools.css"),
            staticUrl("css/e2ee.css")
        ])
        new _document_template__rspack_import_16.ModDocumentTemplate(this)
        const initPromises = [
            (0,_common__rspack_import_1.whenReady)(),
            this.mod.documentTemplate.getCitationStyles()
        ]
        if (this.docInfo.hasOwnProperty("templateId")) {
            // Check if this should be an E2EE document.
            // The E2EE mode can be indicated via:
            // 1. URL query parameter ?e2ee=true (from overview page)
            // 2. Global E2EE_MODE variable set by the settings
            const e2eeParam = new URLSearchParams(window.location.search).get(
                "e2ee"
            )
            const isE2EERequired = this.app.settings.E2EE_MODE === "required"
            const isE2EERequested = e2eeParam === "true" || e2eeParam === "1"
            const shouldEncrypt = isE2EERequired || isE2EERequested

            if (shouldEncrypt) {
                initPromises.push(this._createE2EEDocument())
            } else {
                initPromises.push(
                    (0,_common__rspack_import_1.jsonPostJson)("/api/document/create_doc/", {
                        template_id: this.docInfo.templateId,
                        path: this.docInfo.path
                    }).then(({json}) => {
                        this.docInfo.id = json.id
                        window.history.replaceState(
                            "",
                            "",
                            `/document/${this.docInfo.id}/`
                        )
                        delete this.docInfo.templateId
                        return Promise.resolve()
                    })
                )
            }
        }
        // Handle token-based access (share links)
        if (uuid4Pattern.test(this.docInfo.id)) {
            this.docInfo.token = this.docInfo.id
            initPromises.push(
                (0,_common__rspack_import_1.jsonPostJson)(
                    `/api/document/share_token/validate/${this.docInfo.token}/`
                ).then(({json, status}) => {
                    if (status === 200 && json.document_id) {
                        this.docInfo.id = json.document_id
                        this.docInfo.access_rights = json.rights
                        this.docInfo.wsBase = json.ws_base
                        return Promise.resolve()
                    } else {
                        // Token is invalid or expired
                        return Promise.reject(
                            new Error("Invalid or expired share link")
                        )
                    }
                })
            )
        }
        return Promise.all(initPromises)
            .then(() => {
                new _citations__rspack_import_12.ModCitations(this)
                new _footnotes__rspack_import_17.ModFootnotes(this)
                return this.activateFidusPlugins()
            })
            .then(() => {
                (0,_common__rspack_import_1.activateWait)(true)
                const stylesPayload = {id: this.docInfo.id}
                if (this.docInfo.token) {
                    stylesPayload.token = this.docInfo.token
                }
                const collaborativeEditing =
                    this.app.settings.COLLABORATIVE_EDITING !== false
                const wsBasePromise = collaborativeEditing
                    ? this.docInfo.wsBase
                        ? Promise.resolve({
                              json: {ws_base: this.docInfo.wsBase}
                          })
                        : (0,_common__rspack_import_1.jsonPostJson)("/api/document/get_ws_base/", {
                              id: this.docInfo.id
                          })
                    : Promise.resolve({json: {ws_base: ""}})
                const stylesPromise = (0,_common__rspack_import_1.jsonPostJson)(
                    "/api/document/get_doc_styles/",
                    stylesPayload
                )
                const docDataPromise = (0,_common__rspack_import_1.jsonPostJson)(
                    "/api/document/get_doc_data/",
                    stylesPayload
                )
                return Promise.all([
                    wsBasePromise,
                    stylesPromise,
                    docDataPromise
                ])
            })
            .catch(error => {
                // Only show "Invalid Share Link" for token validation errors.
                // Other errors (REST failures, etc.) should not show this dialog.
                if (error.message === "Invalid or expired share link") {
                    (0,_common__rspack_import_1.deactivateWait)()
                    const errorDialog = new _common__rspack_import_1.Dialog({
                        title: gettext("Invalid Share Link"),
                        id: "invalid_share_link_dialog",
                        body: gettext(
                            "This share link has expired or is invalid. Please ask the document owner for a new link."
                        ),
                        buttons: [
                            {
                                text: gettext("OK"),
                                classes: "fw-dark",
                                click: () => {
                                    window.location.href = "/"
                                }
                            }
                        ],
                        canClose: false
                    })
                    errorDialog.open()
                } else {
                    (0,_common__rspack_import_1.deactivateWait)()
                    console.error("Editor initialization failed:", error)
                }
                return Promise.reject(error)
            })
            .then(([wsResult, stylesResult, docResult]) => {
                let resubScribed = false
                this.render()
                this.initEditor()
                const collaborativeEditing =
                    this.app.settings.COLLABORATIVE_EDITING !== false
                if (collaborativeEditing) {
                    // Include token in WebSocket path if present
                    let wsPath = `/document/${this.docInfo.id}/`
                    if (this.docInfo.token) {
                        wsPath += `?token=${this.docInfo.token}`
                    }
                    this.ws = new _common__rspack_import_1.WebSocketConnector({
                        base: wsResult.json.ws_base,
                        path: wsPath,
                        appLoaded: () => this.view.state.plugins.length,
                        anythingToSend: () => (0,prosemirror_collab__rspack_import_30.sendableSteps)(this.view.state),
                        initialMessage: () => {
                            const message = {
                                type: "subscribe",
                                v: this.docInfo.version
                            }

                            if (this.ws.connectionCount) {
                                message.connection = this.ws.connectionCount
                            }
                            return message
                        },
                        resubScribed: () => {
                            if (
                                (0,prosemirror_collab__rspack_import_30.sendableSteps)(
                                    this.mod.footnotes.fnEditor.view.state
                                )
                            ) {
                                this.mod.collab.doc.footnoteRender = true
                            }
                            resubScribed = true
                            this.mod.footnotes.fnEditor.renderAllFootnotes()
                            this.mod.collab.doc.awaitingDiffResponse = true // wait sending diffs till the version is confirmed
                        },
                        restartMessage: () => ({
                            type: "check_version",
                            v: this.docInfo.version
                        }),
                        messagesElement: () =>
                            this.dom.querySelector("#unobtrusive-messages"),
                        warningNotAllSent: gettext(
                            "Warning! Not all your changes have been saved! You could suffer data loss. Attempting to reconnect..."
                        ),
                        infoDisconnected: gettext(
                            "Disconnected. Attempting to reconnect..."
                        ),
                        receiveData: data => {
                            if (document.body !== this.dom) {
                                return // user navigated away.
                            }
                            switch (data.type) {
                                case "chat":
                                    this.mod.collab.chat.newMessage(data)
                                    break
                                case "connections":
                                    this.mod.collab.updateParticipantList(
                                        data.participant_list
                                    )
                                    if (resubScribed) {
                                        // Reconnecting after offline with
                                        // local edits: ask the server to
                                        // save first so that the REST
                                        // refetch reflects the definitive
                                        // server state and adjustDocument
                                        // can run the tracked-changes merge.
                                        const missingSteps = (0,prosemirror_collab__rspack_import_30.sendableSteps)(
                                            this.view.state
                                        )
                                        this.mod.collab.doc.checkVersion(
                                            missingSteps
                                        )
                                        resubScribed = false
                                    }
                                    break
                                case "session_info":
                                    this.docInfo.session_id = data.session_id
                                    // Update access rights from server
                                    if (data.access_right) {
                                        this.docInfo.access_rights =
                                            data.access_right
                                    }
                                    // Update guest user identity with session_id
                                    if (!this.user.is_authenticated) {
                                        this.user = {
                                            id: this.docInfo.token,
                                            username: `guest${data.session_id}`,
                                            name: `Guest ${data.session_id}`,
                                            is_authenticated: false
                                        }
                                    }
                                    // Handle E2EE encryption parameters from server
                                    if (data.e2ee) {
                                        if (!this.e2ee) {
                                            this.e2ee = {
                                                encrypted: true,
                                                encryptionSalt: data.e2ee_salt,
                                                encryptionIterations:
                                                    data.e2ee_iterations,
                                                key: null,
                                                snapshotManager:
                                                    new _e2ee_snapshot_manager__rspack_import_8.E2EESnapshotManager(
                                                        this
                                                    )
                                            }
                                        } else {
                                            this.e2ee.encrypted = true
                                            this.e2ee.encryptionSalt =
                                                data.e2ee_salt
                                            this.e2ee.encryptionIterations =
                                                data.e2ee_iterations
                                        }
                                    }
                                    break
                                case "refetch_doc":
                                    // The server has forced a DB save before
                                    // sending this message, so the REST response
                                    // will be at the current server version. Passing
                                    // our version (v) lets the endpoint include the
                                    // covering diffs as `m` so adjustDocument can
                                    // do a precise merge.
                                    ;(0,_common__rspack_import_1.jsonPostJson)(
                                        "/api/document/get_doc_data/",
                                        {
                                            id: this.docInfo.id,
                                            token: this.docInfo.token,
                                            v: this.docInfo.version
                                        }
                                    ).then(({json}) => {
                                        this.mod.collab.doc.receiveDocument(
                                            json
                                        )
                                    })
                                    break
                                case "confirm_version":
                                    this.mod.collab.doc.cancelCurrentlyCheckingVersion()
                                    if (data["v"] !== this.docInfo.version) {
                                        this.mod.collab.doc.checkVersion()
                                        return
                                    }
                                    this.mod.collab.doc.confirmVersion(
                                        data["v"]
                                    )
                                    this.mod.collab.doc.enableDiffSending()
                                    break
                                case "selection_change":
                                    this.mod.collab.doc.cancelCurrentlyCheckingVersion()
                                    if (data["v"] !== this.docInfo.version) {
                                        this.mod.collab.doc.checkVersion()
                                        return
                                    }
                                    this.mod.collab.doc.receiveSelectionChange(
                                        data
                                    )
                                    break
                                case "path_change":
                                    this.docInfo.path = data["path"]
                                    this.menu.headerView.update()
                                    break
                                case "diff":
                                    if (data["cid"] === this.client_id) {
                                        // The diff origins from the local user.
                                        this.mod.collab.doc.confirmDiff(
                                            data["rid"]
                                        )
                                        return
                                    }
                                    if (data["v"] !== this.docInfo.version) {
                                        this.mod.collab.doc.checkVersion()
                                        return
                                    }
                                    this.mod.collab.doc.receiveDiff(data)
                                    break
                                case "confirm_diff":
                                    this.mod.collab.doc.confirmDiff(data["rid"])
                                    break
                                case "reject_diff":
                                    this.mod.collab.doc.rejectDiff(data["rid"])
                                    break
                                case "patch_error":
                                    ;(0,_common__rspack_import_1.showSystemMessage)(
                                        gettext(
                                            "Your document was out of sync and has been reset."
                                        )
                                    )
                                    break
                                case "access_right":
                                    if (
                                        data.access_right !==
                                        this.docInfo.access_rights
                                    ) {
                                        if (
                                            (0,prosemirror_collab__rspack_import_30.sendableSteps)(this.view.state) &&
                                            !WRITE_ROLES.includes(
                                                data.access_right
                                            )
                                        ) {
                                            // If the user's new rights does not allow him to update document , then download a copy of the
                                            // same and ask him to re-open the document.
                                            this.handleAccessRightModification()
                                        } else {
                                            (0,_common__rspack_import_1.addAlert)(
                                                "info",
                                                interpolate(
                                                    gettext(
                                                        "Your Access rights have been modified. You now have %(accessRight)s access to this document."
                                                    ),
                                                    {
                                                        accessRight:
                                                            data.access_right
                                                    },
                                                    true
                                                )
                                            )
                                            this.docInfo.access_rights =
                                                data.access_right
                                        }
                                    }
                                    break
                                case "request_snapshot":
                                    // Server is asking a write-capable client to
                                    // send an encrypted snapshot for persistence.
                                    if (
                                        this.e2ee &&
                                        this.e2ee.snapshotManager
                                    ) {
                                        this.e2ee.snapshotManager.handleRequestSnapshot(
                                            data
                                        )
                                    }
                                    break
                                case "e2ee_snapshot_received":
                                    // Server confirmed that an encrypted snapshot
                                    // was saved. Informational — no action needed.
                                    if (
                                        this.e2ee &&
                                        this.e2ee.snapshotManager
                                    ) {
                                        this.e2ee.snapshotManager.handleSnapshotReceived(
                                            data
                                        )
                                    }
                                    break
                                default:
                                    break
                            }
                        },
                        failedAuth: () => {
                            // Clear all E2EE keys on session expiration
                            _e2ee_key_manager__rspack_import_4.E2EEKeyManager.clearAllKeysFromSession()
                            _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.clearKeysFromSession()
                            if (this.docInfo.token) {
                                // Token-based access failed
                                const tokenDialog = new _common__rspack_import_1.Dialog({
                                    title: gettext("Invalid Share Link"),
                                    id: "invalid_token_dialog",
                                    body: gettext(
                                        "This share link has expired or is invalid. Please ask the document owner for a new link."
                                    ),
                                    buttons: [
                                        {
                                            text: gettext("OK"),
                                            classes: "fw-dark",
                                            click: () => {
                                                window.location.href = "/"
                                            }
                                        }
                                    ],
                                    canClose: false
                                })
                                tokenDialog.open()
                                return
                            }
                            if (
                                this.view.state.plugins.length &&
                                (0,prosemirror_collab__rspack_import_30.sendableSteps)(this.view.state) &&
                                this.ws.connectionCount > 0
                            ) {
                                this.ws.online = false // To avoid Websocket trying to reconnect.
                                const sessionDialog = new _common__rspack_import_1.Dialog({
                                    title: gettext("Session Expired"),
                                    id: "session_expiration_dialog",
                                    body: gettext(
                                        "Your session expired while you were offline, so we cannot save your work to the server any longer, but you can download it to your computer instead. Please consider importing it as a new document after logging in."
                                    ),
                                    buttons: [
                                        {
                                            text: gettext("Download Document"),
                                            click: () => {
                                                const doc = this.getDoc({
                                                    use_current_view: true
                                                })
                                                new _exporter_native_file__rspack_import_23.ExportFidusFile(
                                                    doc,
                                                    this.mod.db.bibDB,
                                                    this.mod.db.imageDB,
                                                    false
                                                )
                                            }
                                        },
                                        {
                                            text: gettext(
                                                "Proceed to Login page"
                                            ),
                                            classes: "fw-dark",
                                            click: () => {
                                                window.location.href = "/"
                                            }
                                        }
                                    ],
                                    canClose: false
                                })
                                sessionDialog.open()
                            } else {
                                window.location.href = "/"
                            }
                        }
                    })
                }
                this.mod.documentTemplate.setStyles(stylesResult.json)
                const docIsE2EE = docResult.json.doc_info?.e2ee === true
                this.mod.collab.doc.receiveDocument(docResult.json)
                // For E2EE documents, delay the WebSocket connection until
                // after the password dialog and decryption are complete (see
                // _decryptAndLoadDoc). This ensures the key is available when
                // catch-up diffs arrive from the server.
                if (collaborativeEditing && !docIsE2EE) {
                    this.startWebSocket()
                } else if (!collaborativeEditing) {
                    if (!this.noCollabSave) {
                        this.noCollabSave = new _no_collab_save__rspack_import_21.NoCollabSave(this)
                    }
                    this.noCollabSave.start()
                    if (!docIsE2EE) {
                        // In non-collaborative mode there is no server
                        // confirm_version message, so mark it locally.
                        this.mod.collab.doc.confirmVersion(this.docInfo.version)
                    }
                }
            })
    }

    /**
     * Open the WebSocket connection and attach the close listener.
     * Called immediately for non-E2EE documents and deferred until after
     * password entry + decryption for E2EE documents.
     */
    startWebSocket() {
        this.ws.init()
        this.ws.ws.addEventListener("close", () => {
            if (this.menu.toolbarViews) {
                this.menu.toolbarViews.forEach(view => view.update())
            }
            if (this.menu.headerView) {
                this.menu.headerView.update()
            }
        })
    }

    handleAccessRightModification() {
        // This function when invoked creates a copy of document in FW format and closes editor operation.
        new _exporter_native_file__rspack_import_23.ExportFidusFile(
            this.getDoc({use_current_view: true}),
            this.mod.db.bibDB,
            this.mod.db.imageDB
        )
        const accessRightModifiedDialog = new _common__rspack_import_1.Dialog({
            title: gettext("Access rights modified"),
            id: "access_rights_modified",
            body: gettext(
                "Your access rights were modified while you were offline, so we cannot save your work to the server any longer, and it is downloaded to your computer instead. Please consider importing it into a new document."
            ),
            buttons: [
                {
                    text: gettext("Leave editor"),
                    classes: "fw-dark",
                    click: () => {
                        window.location.href = "/"
                    }
                }
            ],
            canClose: false
        })
        accessRightModifiedDialog.open()
        this.close() // Close the editor operations.
    }

    /**
     * Create a new E2EE document.
     *
     * If the user has set up a personal passphrase, generates a random
     * document encryption key (DEK) and encrypts it with the master key.
     * Otherwise, prompts for a per-document password.
     *
     * @returns {Promise<void>}
     * @private
     */
    async _createE2EEDocument() {
        let password, key, saltBase64, iterations

        // Check if the user has a personal passphrase set up
        const hasPassphraseKeys = await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.hasEncryptionKeys()

        if (hasPassphraseKeys) {
            // Check if keys are unlocked in sessionStorage
            if (!_e2ee_passphrase_manager__rspack_import_6.PassphraseManager.hasKeysInSession()) {
                // Prompt for passphrase to unlock
                const result = await new Promise(resolve => {
                    (0,_e2ee_passphrase_dialog__rspack_import_5.enterPassphraseDialog)(
                        pwd => resolve({action: "unlock", passphrase: pwd}),
                        () => resolve({action: "recover"})
                    )
                })
                if (result.action === "unlock" && result.passphrase) {
                    try {
                        await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.unlockWithPassphrase(
                            result.passphrase
                        )
                    } catch (_e) {
                        (0,_common__rspack_import_1.addAlert)("error", gettext("Incorrect passphrase."))
                    }
                } else if (result.action === "recover") {
                    // Recovery flow
                    const {recoverWithKeyDialog} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/passphrase-dialog.js"))
                    const recoverResult = await new Promise(resolve => {
                        recoverWithKeyDialog(resolve)
                    })
                    if (recoverResult) {
                        try {
                            const {newRecoveryKey} =
                                await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.recoverWithRecoveryKey(
                                    recoverResult.recoveryKey,
                                    recoverResult.newPassphrase
                                )
                            const {showRecoveryKeyDialog} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/passphrase-dialog.js"))
                            await new Promise(resolve => {
                                showRecoveryKeyDialog(newRecoveryKey, resolve)
                            })
                        } catch (e) {
                            (0,_common__rspack_import_1.addAlert)(
                                "error",
                                gettext("Recovery failed: ") + e.message
                            )
                        }
                    }
                }
            }

            if (_e2ee_passphrase_manager__rspack_import_6.PassphraseManager.hasKeysInSession()) {
                // Generate a random document password that is itself a raw DEK
                password = await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.generateDocumentPassword()
            }
            // Fall through to per-document password if passphrase unlock failed
        }

        if (!password) {
            // Prompt for per-document password
            password = await new Promise(resolve => {
                (0,_e2ee_password_dialog__rspack_import_7.createPasswordDialog)(pwd => resolve(pwd))
            })
        }

        // Generate a random 16-byte salt
        const salt = _e2ee_key_manager__rspack_import_4.E2EEKeyManager.generateSalt()
        saltBase64 = btoa(String.fromCharCode(...salt))
        iterations = 600000

        // Resolve password to encryption key
        key = await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.resolvePasswordToKey(
            password,
            salt,
            iterations
        )

        // Create the document on the server with E2EE parameters
        const {json, status} = await (0,_common__rspack_import_1.jsonPostJson)("/api/document/create_doc/", {
            template_id: this.docInfo.templateId,
            path: this.docInfo.path,
            e2ee: true,
            e2ee_salt: saltBase64,
            e2ee_iterations: iterations
        })

        if (status === 403) {
            (0,_common__rspack_import_1.addAlert)(
                "error",
                json.error || gettext("E2EE is not enabled on this server.")
            )
            window.location.href = "/"
            return
        }

        this.docInfo.id = json.id
        window.history.replaceState("", "", `/document/${this.docInfo.id}/`)
        delete this.docInfo.templateId

        // Store password in sessionStorage
        _e2ee_key_manager__rspack_import_4.E2EEKeyManager.storePasswordInSession(this.docInfo.id, password)

        // If passphrase user, also save encrypted password on server
        if (hasPassphraseKeys && _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.hasKeysInSession()) {
            try {
                await _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.saveDocumentPassword(
                    this.docInfo.id,
                    password,
                    null,
                    "user",
                    true
                )
            } catch (_e) {
                console.error("Failed to save document password:", _e)
            }
        } else {
            // Non-passphrase user: cache the key in sessionStorage
            await _e2ee_key_manager__rspack_import_4.E2EEKeyManager.storeKeyInSession(this.docInfo.id, key)
        }

        // Set up E2EE state on the editor
        this.e2ee = {
            encrypted: true,
            encryptionSalt: saltBase64,
            encryptionIterations: iterations,
            key: key,
            password: password,
            snapshotManager: new _e2ee_snapshot_manager__rspack_import_8.E2EESnapshotManager(this),
            usesPassphrase:
                hasPassphraseKeys && _e2ee_passphrase_manager__rspack_import_6.PassphraseManager.hasKeysInSession()
        }
        this.e2ee.snapshotManager.setKey(key)
    }

    close() {
        if (this.menu.toolbarViews) {
            this.menu.toolbarViews.forEach(view => view.destroy())
        }
        if (this.menu.selectionMenuViews) {
            this.menu.selectionMenuViews.forEach(view => view.destroy())
        }
        if (this.menu.headerView) {
            this.menu.headerView.destroy()
        }
        // For E2EE documents, update the sessionStorage title cache with the
        // current ProseMirror title so the overview shows the correct title
        // immediately, even before the encrypted snapshot has been saved to
        // the DB.
        if (this.e2ee && this.view) {
            const titleNode = this.view.state.doc.firstChild
            if (titleNode) {
                let title = ""
                titleNode.forEach(child => {
                    if (
                        !child.marks.find(mark => mark.type.name === "deletion")
                    ) {
                        title += child.textContent
                    }
                })
                sessionStorage.setItem(`e2ee_title_${this.docInfo.id}`, title)
            }
        }
        if (this.noCollabSave) {
            this.noCollabSave.stop()
            if (this.docInfo.access_rights === "write") {
                // Fire a final save before closing. For SPA navigation the
                // promise usually has enough time to complete; for actual page
                // unload the beforeunload/pagehide handler covers us.
                this.noCollabSave.save()
            }
        }
        if (this.ws) {
            // For E2EE documents, try to send one last encrypted snapshot before
            // closing so the title (and any last-minute edits) are persisted.
            // The WebSocket connector has an 80ms throttle between messages, so
            // we delay ws.close() to give the snapshot time to be queued and
            // sent. For SPA navigation 300ms is usually enough; for tab-close
            // the browser may still kill us early.
            // For non-E2EE documents we close immediately so the server's
            // disconnect handler saves the document to the DB before the
            // overview page loads its data (avoiding a race where the overview
            // would still see the old/empty title).
            if (
                this.e2ee &&
                this.e2ee.key &&
                this.e2ee.snapshotManager &&
                this.docInfo.access_rights === "write"
            ) {
                this.e2ee.snapshotManager.handleRequestSnapshot({})
                window.setTimeout(() => this.ws.close(), 300)
            } else {
                this.ws.close()
            }
        }
    }

    render() {
        this.dom = document.createElement("body")
        document.body = this.dom
        this.dom.classList.add("editor")
        this.dom.classList.add("scrollable")
        this.dom.innerHTML = `<div id="editor">
            <div id="wait"><i class="fa fa-spinner fa-pulse"></i></div>
            <header>
                <nav id="headerbar"><div></div></nav>
                <nav id="toolbar"><div></div></nav>
            </header>
            <div id="navigator"></div>
            <div id="editor-content">
                <div id="flow">
                    <div id="paper-editable">
                        <div id="document-editable" class="user-contents"></div>
                        <div id="footnote-box-container" class="user-contents">
                            <div id="citation-footnote-box-container"></div>
                        </div>
                    </div>
                    <div id="bibliography" class="doc-bibliography user-contents"></div>
                </div>
                <nav id="selection-menu"><div></div></nav>
                <div id="margin-box-column">
                    <div id="margin-box-filter"></div>
                    <div id="margin-box-container"><div></div></div>
                </div>
            </div>
            <div id="chat">
                <i class="resize-button fa fa-angle-double-down"></i>
                <div id="chat-container"></div>
                <div id="messageform" contentEditable="true" class="empty"></div>
                <audio id="chat-notification">
                    <source src="${staticUrl("ogg/chat_notification.ogg")}" type="audio/ogg">
                </audio>
            </div>
        </div>
        <div id="unobtrusive-messages"></div>`
        const feedbackTab = new _feedback__rspack_import_2.FeedbackTab()
        feedbackTab.init()
    }

    onResize() {
        if (!this.view || !this.mod.marginboxes) {
            // Editor not yet set up
            return
        }
        this.mod.marginboxes.updateDOM()
        this.mod.footnotes.layout.updateDOM()
        this.menu.toolbarViews.forEach(view => view.onResize())
    }

    onBeforeUnload() {
        if (this.app.isOffline()) {
            (0,_common__rspack_import_1.showSystemMessage)(
                gettext(
                    "Changes you made to the document since going offline will be lost, if you choose to close/refresh the tab or close the browser."
                )
            )
            return true
        }
        this.close()
    }

    initEditor() {
        let setFocus = false
        this.view = new prosemirror_view__rspack_import_34.EditorView(
            this.dom.querySelector("#document-editable"),
            {
                state: prosemirror_state__rspack_import_35.EditorState.create({
                    schema: this.schema
                }),
                handleDOMEvents: {
                    mousedown: (view, _event) => {
                        this.currentView = this.view
                        view.focus()
                        return false
                    },
                    focus: (view, _event) => {
                        if (!setFocus) {
                            this.currentView = this.view
                            // We focus once more, as focus may have disappeared due to
                            // disappearing placeholders.
                            setFocus = true
                            view.focus()
                            setFocus = false
                        }
                    }
                },
                dispatchTransaction: tr => {
                    const trackedTr = (0,_track__rspack_import_22.amendTransaction)(
                        tr,
                        this.view.state,
                        this
                    )
                    const {state: newState, transactions} =
                        this.view.state.applyTransaction(trackedTr)
                    this.view.updateState(newState)
                    transactions.forEach(subTr => {
                        const footTr = subTr.getMeta("footTr")
                        if (footTr && footTr.steps.length) {
                            this.mod.footnotes.fnEditor.view.dispatch(footTr)
                        }
                    })
                    if (tr.steps.length) {
                        this.docInfo.updated = new Date()
                    }
                    // Update the header bar to reflect any title changes
                    if (this.menu.headerView) {
                        this.menu.headerView.update()
                    }

                    this.mod.collab.doc.sendToCollaborators()
                }
            }
        )
        // The editor that is currently being edited in -- main or footnote editor
        this.currentView = this.view
        this.mod.citations.init()
        this.mod.footnotes.init()
        new _databases__rspack_import_15.ModDB(this)
        new _collab__rspack_import_13.ModCollab(this)
        new _track__rspack_import_22.ModTrack(this)
        new _marginboxes__rspack_import_18.ModMarginboxes(this)
        this.mod.marginboxes.init()
        new _comments__rspack_import_14.ModComments(this)
        new _navigator__rspack_import_20.ModNavigator(this)
        this.mod.navigator.init()
    }

    activateFidusPlugins() {
        // Add plugins.
        this.plugins = {}

        return Promise.all(
            Object.keys(_plugins_editor__rspack_import_9).map(plugin => {
                if (typeof _plugins_editor__rspack_import_9[plugin] === "function") {
                    this.plugins[plugin] = new _plugins_editor__rspack_import_9[plugin](this)
                    return this.plugins[plugin].init() || Promise.resolve()
                }
                return Promise.resolve()
            })
        )
    }

    // Collect all components of the current doc. Needed for saving and export
    // filters
    getDoc(options = {}) {
        const doc =
            this.app.isOffline() || Boolean(options.use_current_view)
                ? this.view.docView.node
                : this.docInfo.confirmedDoc
        const pmDoc =
            options.changes === "acceptAllNoInsertions"
                ? (0,_track__rspack_import_22.acceptAllNoInsertions)(doc)
                : doc

        let title = ""
        pmDoc.firstChild.forEach(child => {
            if (!child.marks.find(mark => mark.type.name === "deletion")) {
                title += child.textContent
            }
        })
        return {
            content: pmDoc.toJSON(),
            settings: (0,_schema_convert__rspack_import_10.getSettings)(pmDoc),
            title: title.substring(0, 255),
            path: this.docInfo.path,
            version: this.docInfo.version,
            comments: this.mod.comments.store.comments,
            id: this.docInfo.id,
            updated: this.docInfo.updated
        }
    }

    // Use PMs scrollIntoView function and adjust for top menu
    scrollIdIntoView(id) {
        let foundPos = false,
            view

        this.view.state.doc.descendants((node, pos) => {
            if (foundPos) {
                return
            } else if (
                (node.type.groups.includes("heading") ||
                    node.type.name === "figure") &&
                node.attrs.id === id
            ) {
                foundPos = node.type.name === "figure" ? pos : pos + 1
                view = this.view
            } else {
                const anchorMark = node.marks.find(
                    mark => mark.type.name === "anchor"
                )
                if (anchorMark?.attrs.id === id) {
                    foundPos = pos + 1
                    view = this.view
                }
            }
        })

        if (!foundPos) {
            this.mod.footnotes.fnEditor.view.state.doc.descendants(
                (node, pos) => {
                    if (foundPos) {
                        return
                    } else if (
                        (node.type.groups.includes("heading") ||
                            node.type.name === "figure") &&
                        node.attrs.id === id
                    ) {
                        foundPos = node.type.name === "figure" ? pos : pos + 1
                        view = this.mod.footnotes.fnEditor.view
                    } else {
                        const anchorMark = node.marks.find(
                            mark => mark.type.name === "anchor"
                        )
                        if (anchorMark?.attrs.id === id) {
                            foundPos = pos + 1
                            view = this.mod.footnotes.fnEditor.view
                        }
                    }
                }
            )
        }
        if (foundPos) {
            this.scrollPosIntoView(foundPos, view)
        }
    }

    scrollPosIntoView(pos, view) {
        const topMenuHeight = this.dom.querySelector("header").offsetHeight + 10
        const $pos = view.state.doc.resolve(pos)
        view.dispatch(view.state.tr.setSelection(new prosemirror_state__rspack_import_35.TextSelection($pos, $pos)))
        view.focus()
        const distanceFromTop = view.coordsAtPos(pos).top - topMenuHeight
        window.scrollBy({
            left: 0,
            top: distanceFromTop,
            behavior: "smooth",
            block: "center"
        })
        return
    }

    scrollBibliographyIntoView() {
        const topMenuHeight = this.dom.querySelector("header").offsetHeight + 10
        const bibliographyHeaderEl = document.querySelector(
            "h1.doc-bibliography-header"
        )
        const distanceFromTop =
            bibliographyHeaderEl.getBoundingClientRect().top - topMenuHeight
        window.scrollBy({
            left: 0,
            top: distanceFromTop,
            behavior: "smooth",
            block: "center"
        })
        return
    }
}


}),
"./js/modules/editor/keymap.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  buildEditorKeymap: function() { return buildEditorKeymap; },
  setBlockType: function() { return setBlockType; }
});
/* import */ var prosemirror_commands__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_history__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_schema_list__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-list@1.5.1/node_modules/prosemirror-schema-list/dist/index.js");



const mac =
    typeof navigator != "undefined" ? /Mac/.test(navigator.platform) : false

const backspace = (0,prosemirror_commands__rspack_import_0.chainCommands)(
    prosemirror_commands__rspack_import_0.deleteSelection,
    prosemirror_commands__rspack_import_0.joinBackward,
    prosemirror_commands__rspack_import_0.selectNodeBackward
)
const addInputType = (tr, inputType) => tr.setMeta("inputType", inputType)

/* Adjusted version of setBlockType that preserves attributes */
/* source https://github.com/ProseMirror/prosemirror-commands/blob/b9ceb06e340ffcb3e12cd214f58939e81c0b61af/src/commands.js#L414-L432 */
function setBlockType(nodeType, attrs = {}) {
    return (state, dispatch) => {
        const {from, to} = state.selection
        const tr = state.tr
        state.doc.nodesBetween(from, to, (node, pos) => {
            if (!node.isTextblock || node.hasMarkup(nodeType, attrs)) {
                return
            }
            let applicable = false
            if (node.type == nodeType) {
                applicable = true
            } else {
                const $pos = state.doc.resolve(pos),
                    index = $pos.index()
                applicable = $pos.parent.canReplaceWith(
                    index,
                    index + 1,
                    nodeType
                )
            }
            if (applicable) {
                tr.setBlockType(
                    pos,
                    pos + node.nodeSize,
                    nodeType,
                    Object.assign({}, node.attrs, attrs) // preserve existing attributes
                )
            }
        })
        if (!tr.steps.length) {
            return false
        }
        if (dispatch) {
            dispatch(tr.scrollIntoView())
        }
        return true
    }
}

const buildEditorKeymap = schema => {
    const editorKeymap = {
        Backspace: (state, dispatch, view) =>
            backspace(
                state,
                tr => dispatch(addInputType(tr, "deleteContentBackward")),
                view
            ),
        "Mod-z": (state, dispatch, view) =>
            (0,prosemirror_history__rspack_import_1.undo)(state, tr => dispatch(addInputType(tr, "historyUndo")), view),
        "Shift-Mod-z": (state, dispatch, view) =>
            (0,prosemirror_history__rspack_import_1.redo)(state, tr => dispatch(addInputType(tr, "historyRedo")), view),
        "Shift-Ctrl-0": setBlockType(schema.nodes.paragraph),
        "Shift-Ctrl-\\": setBlockType(schema.nodes.code_block),
        "Ctrl-<": (0,prosemirror_schema_list__rspack_import_2.liftListItem)(schema.nodes.list_item),
        "Mod-.": (state, dispatch) => {
            const mark = schema.marks.sup
            const command = (0,prosemirror_commands__rspack_import_0.toggleMark)(mark)
            return command(state, dispatch)
        },
        "Mod-,": (state, dispatch) => {
            const mark = schema.marks.sub
            const command = (0,prosemirror_commands__rspack_import_0.toggleMark)(mark)
            return command(state, dispatch)
        },
        "Mod-'": (state, dispatch) => {
            const mark = schema.marks.code
            const command = (0,prosemirror_commands__rspack_import_0.toggleMark)(mark)
            return command(state, dispatch)
        }
    }
    for (let i = 1; i <= 6; i++) {
        editorKeymap["Shift-Ctrl-" + i] = setBlockType(schema.nodes.heading, {
            level: i
        })
    }
    if (!mac) {
        editorKeymap["Mod-y"] = (state, dispatch, view) =>
            (0,prosemirror_history__rspack_import_1.redo)(state, tr => dispatch(addInputType(tr, "historyRedo")), view)
    }
    return editorKeymap
}


}),
"./js/modules/editor/marginboxes/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModMarginboxes: function() { return ModMarginboxes; }
});
/* import */ var diff_dom__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/diff-dom@5.2.1/node_modules/diff-dom/dist/module.js");
/* import */ var fastdom__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/fastdom@1.0.11/node_modules/fastdom/fastdom.js");
/* import */ var fastdom__rspack_import_1_default = /*#__PURE__*/__webpack_require__.n(fastdom__rspack_import_1);
/* import */ var _common__rspack_import_2 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _state_plugins__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _templates__rspack_import_4 = __webpack_require__("./js/modules/editor/marginboxes/templates.js");







/* Functions related to layouting of comments */
class ModMarginboxes {
    constructor(editor) {
        editor.mod.marginboxes = this
        this.editor = editor
        this.activeCommentStyle = ""
        this.filterOptions = {
            track: true,
            comments: true,
            info: true,
            help: true,
            warning: true,
            commentsResolved: false,
            author: 0,
            assigned: 0
        }
        this.commentColors = {
            isMajor: "#f4c9d9",
            marker: "#f9f9f9",
            active: "#fffacf"
        }
        this.dd = new diff_dom__rspack_import_0.DiffDOM({
            valueDiffing: false
        })
        this.marginBoxesContainerString =
            '<div id="margin-box-container"><div></div></div>'
        this.marginBoxesContainerObj = (0,diff_dom__rspack_import_0.stringToObj)(
            this.marginBoxesContainerString
        )
        this.marginBoxesPlacementStyle = ""
        this.marginBoxes = []
    }

    init() {
        // Add two elements to hold dynamic CSS info about comments.
        document.body.insertAdjacentHTML(
            "beforeend",
            '<style type="text/css" id="active-comment-style"></style><style type="text/css" id="track-options-style"></style><style type="text/css" id="margin-box-placement-style"></style>'
        )
        this.marginBoxesContainer = document.getElementById(
            "margin-box-container"
        )
        this.activeCommentStyleElement = document.getElementById(
            "active-comment-style"
        )
        this.trackOptionsStyleElement = document.getElementById(
            "track-options-style"
        )
        this.bindEvents()
    }

    bindEvents() {
        // Bind all the click events related to the margin box filter
        document.body.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_2.findTarget)(event, ".margin-box-filter-check", el):
                    // do not react to clicks on checkboxes within sub menus
                    break
                case (0,_common__rspack_import_2.findTarget)(
                    event,
                    ".margin-box-filter-comments-author",
                    el
                ):
                    this.filterOptions.commentsAuthor = Number.parseInt(
                        el.target.dataset.id
                    )
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(
                    event,
                    ".margin-box-filter-comments-assigned",
                    el
                ):
                    this.filterOptions.assigned = Number.parseInt(
                        el.target.dataset.id
                    )
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".show-marginbox-options-submenu", el):
                    this.closeAllMenus(".marginbox-options-submenu.fw-open")
                    Array.from(el.target.parentElement.children)
                        .find(node =>
                            node.matches(".marginbox-options-submenu")
                        )
                        .classList.add("fw-open")
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".show-marginbox-options", el):
                    this.closeAllMenus()
                    if (
                        el.target.parentElement.classList.contains(
                            "margin-box-filter-button"
                        )
                    ) {
                        Array.from(el.target.parentElement.children)
                            .find(node => node.matches(".marginbox-options"))
                            .classList.add("fw-open")
                    } else {
                        let resolved = false
                        if (el.target.parentElement?.parentElement) {
                            resolved =
                                el.target.parentElement.parentElement.classList.contains(
                                    "resolved"
                                )
                        }
                        const user = this.editor.user
                        const docInfo = this.editor.docInfo
                        const elData = el.target.dataset
                        const comment = {
                            answer: elData.hasOwnProperty("answer"),
                            id: elData.id,
                            commentId: elData.commentid,
                            user: Number(elData.commentuser),
                            resolved: resolved
                        }
                        document.body.insertAdjacentHTML(
                            "beforeend",
                            (0,_templates__rspack_import_4.marginBoxOptions)(comment, user, docInfo)
                        )
                        const marginboxOptions = document.body.querySelector(
                            ".comment-answer-options.marginbox-options"
                        )
                        marginboxOptions.classList.add("fw-open")
                        this.positionMarginBoxOptions(
                            marginboxOptions,
                            el.target
                        )
                    }
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".margin-box-filter-track-author", el):
                    this.filterOptions.trackAuthor = Number.parseInt(
                        el.target.dataset.id
                    )
                    this.setTrackStyle()
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(event, "#margin-box-filter-track", el):
                    this.filterOptions.track = !this.filterOptions.track
                    this.setTrackStyle()
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(event, "#margin-box-filter-comments", el):
                    this.filterOptions.comments = !this.filterOptions.comments
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(event, "#margin-box-filter-info", el):
                    this.filterOptions.info = !this.filterOptions.info
                    this.view(this.editor.currentView)
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".margin-box.comment.inactive", el):
                    this.editor.mod.comments.interactions.deactivateSelectedChanges()
                    this.editor.mod.comments.interactions.activateComment(
                        el.target.dataset.id
                    )
                    break
                case (0,_common__rspack_import_2.findTarget)(event, ".margin-box.track.inactive", el): {
                    let boxNumber = 0
                    let seekItem = el.target
                    while (seekItem.previousElementSibling) {
                        boxNumber += 1
                        seekItem = seekItem.previousElementSibling
                    }
                    const box = this.marginBoxes[boxNumber]
                    this.editor.mod.track.activateTrack(
                        box.view,
                        box.type,
                        box.pos
                    )
                    break
                }
                case (0,_common__rspack_import_2.findTarget)(
                    event,
                    ".margin-box.comment.active .show-more-less",
                    el
                ):
                    this.toggleShowMore(el)
                    break
                default:
                    this.closeAllMenus()
                    this.closeAllLongComments()
                    break
            }
        })

        document.body.addEventListener(
            "change",
            evt => {
                switch (evt.target.id) {
                    case "margin-box-filter-comments-resolved":
                        this.filterOptions.commentsResolved = evt.target.checked
                        this.view(this.editor.currentView)
                        break

                    case "margin-box-filter-comments-only-major":
                        this.filterOptions.commentsOnlyMajor =
                            evt.target.checked
                        this.view(this.editor.currentView)
                        break

                    case "margin-box-filter-info-help":
                        this.filterOptions.help = evt.target.checked
                        this.view(this.editor.currentView)
                        break

                    case "margin-box-filter-info-warning":
                        this.filterOptions.warning = evt.target.checked
                        this.view(this.editor.currentView)
                        break
                }
            },
            false
        )

        setTimeout(this.commentOptionsOnScroll, 100)
    }

    closeAllMenus(
        selector = ".marginbox-options-submenu.fw-open, .marginbox-options.fw-open"
    ) {
        document.querySelectorAll(selector).forEach(el => {
            if (el.classList.contains("comment-answer-options")) {
                el.parentElement.removeChild(el)
            } else {
                el.classList.remove("fw-open")
            }
        })
    }

    view(view) {
        // Give up if the user is currently editing a comment.
        if (this.editor.mod.comments.interactions.isCurrentlyEditing()) {
            return false
        }
        this.editor.mod.comments.interactions.activateSelectedComment(view)
    }

    updateDOM() {
        // Handle the layout of the comments on the screen.
        // DOM write phase

        const marginBoxes = [],
            referrers = [],
            selectedChanges = (0,_state_plugins__rspack_import_3.getSelectedChanges)(this.editor.currentView.state)
        let fnIndex = 0,
            fnPosCount = 0,
            lastNodeTracks = [],
            lastNode = this.editor.view.state.doc

        this.activeCommentStyle = ""
        this.editor.view.state.doc.descendants((node, pos) => {
            if (node.attrs.hidden) {
                return false
            }
            lastNodeTracks = this.getMarginBoxes(
                node,
                pos,
                pos,
                lastNode,
                lastNodeTracks,
                "main",
                marginBoxes,
                referrers,
                selectedChanges,
                this.editor.view.state.selection
            )
            lastNode = node

            if (node.type.name === "footnote") {
                let lastFnNode =
                        this.editor.mod.footnotes.fnEditor.view.state.doc,
                    lastFnNodeTracks = []
                const footnote =
                    lastFnNode.childCount > fnIndex
                        ? lastFnNode.child(fnIndex)
                        : false
                if (!footnote) {
                    return
                }
                this.editor.mod.footnotes.fnEditor.view.state.doc.nodesBetween(
                    fnPosCount,
                    fnPosCount + footnote.nodeSize,
                    (fnNode, fnPos) => {
                        if (fnPos < fnPosCount) {
                            return false
                        }
                        lastFnNodeTracks = this.getMarginBoxes(
                            fnNode,
                            fnPos,
                            pos,
                            lastFnNode,
                            lastFnNodeTracks,
                            "footnote",
                            marginBoxes,
                            referrers,
                            selectedChanges,
                            this.editor.view.state.selection
                        )
                        lastFnNode = fnNode
                    }
                )
                fnIndex++
                fnPosCount += footnote.nodeSize
            }
        })

        // Add a comment that is currently under construction to the list.
        if (this.editor.mod.comments.store.commentDuringCreation) {
            const deco = (0,_state_plugins__rspack_import_3.getCommentDuringCreationDecoration)(
                this.editor.view.state
            )
            let pos, view
            if (deco) {
                pos = deco.from
                view = "main"
            } else {
                const fnDeco = (0,_state_plugins__rspack_import_3.getCommentDuringCreationDecoration)(
                    this.editor.mod.footnotes.fnEditor.view.state
                )
                if (fnDeco) {
                    pos = this.fnPosToPos(fnDeco.from)
                    view = "footnote"
                }
            }
            if (pos) {
                const comment =
                    this.editor.mod.comments.store.commentDuringCreation.comment
                let index = 0
                // // We need the position of the new comment in relation to the other
                // // comments in order to insert it in the right place
                while (referrers.length > index && referrers[index] < pos) {
                    index++
                }
                marginBoxes.splice(index, 0, {
                    type: "comment",
                    data: comment,
                    view,
                    pos,
                    active: true
                })
                referrers.splice(index, 0, pos)
                this.activeCommentStyle +=
                    ".active-comment, .active-comment .comment {background-color: #fffacf !important;}"
            }
        }

        const marginBoxesHTML = (0,_templates__rspack_import_4.marginBoxesTemplate)({
            marginBoxes,
            user: this.editor.user,
            docInfo: this.editor.docInfo,
            editComment: this.editor.mod.comments.interactions.editComment,
            activeCommentAnswerId:
                this.editor.mod.comments.interactions.activeCommentAnswerId,
            filterOptions: this.filterOptions
        })

        this.marginBoxes = marginBoxes

        if (this.marginBoxesContainerString !== marginBoxesHTML) {
            const newMarginBoxesContainerObj = (0,diff_dom__rspack_import_0.stringToObj)(marginBoxesHTML)
            const diff = this.dd.diff(
                this.marginBoxesContainerObj,
                newMarginBoxesContainerObj
            )
            this.dd.apply(this.marginBoxesContainer, diff)
            this.marginBoxesContainerString = marginBoxesHTML
            this.marginBoxesContainerObj = newMarginBoxesContainerObj
        }

        if (
            this.activeCommentStyleElement.innerHTML != this.activeCommentStyle
        ) {
            this.activeCommentStyleElement.innerHTML = this.activeCommentStyle
        }

        const marginBoxFilterHTML = (0,_templates__rspack_import_4.marginboxFilterTemplate)({
            marginBoxes,
            filterOptions: this.filterOptions,
            pastParticipants: this.editor.mod.collab.pastParticipants
        })

        const marginBoxFilterElement =
            document.getElementById("margin-box-filter")

        if (!marginBoxFilterElement) {
            // User has navigated away already.
            return (0,_common__rspack_import_2.cancelPromise)()
        }

        if (marginBoxFilterElement.innerHTML != marginBoxFilterHTML) {
            marginBoxFilterElement.innerHTML = marginBoxFilterHTML
        }

        return new Promise(resolve => {
            fastdom__rspack_import_1_default().measure(() => {
                // DOM read phase
                let marginBoxesPlacementStyle = ""
                if (
                    getComputedStyle(marginBoxFilterElement).position ===
                    "fixed"
                ) {
                    // We are in mobile/tablet mode. We don't need to place margin boxes.
                } else {
                    const marginBoxesDOM = document.querySelectorAll(
                        "#margin-box-container .margin-box"
                    )
                    if (
                        marginBoxesDOM.length !== referrers.length ||
                        !marginBoxesDOM.length
                    ) {
                        // Number of comment boxes and referrers differ.
                        // This isn't right. Abort.
                        resolve()
                        return
                    }
                    const bodyTop = document.body.getBoundingClientRect().top,
                        marginBoxPlacements = Array.from(marginBoxesDOM).map(
                            (mboxDOM, index) => {
                                const mboxDOMRect =
                                    mboxDOM.getBoundingClientRect()
                                return {
                                    height: mboxDOMRect.height,
                                    refPos:
                                        this.editor.view.coordsAtPos(
                                            referrers[index]
                                        ).top - bodyTop
                                }
                            }
                        ),
                        firstActiveIndex = marginBoxes.findIndex(
                            mBox => mBox.active
                        ),
                        firstActiveMboxPlacement =
                            marginBoxPlacements[firstActiveIndex]

                    let activeIndex = firstActiveIndex,
                        currentPos = 0
                    while (activeIndex > -1) {
                        const mboxPlacement = marginBoxPlacements[activeIndex]
                        if (mboxPlacement.height === 0) {
                            mboxPlacement.pos = currentPos
                        } else if (mboxPlacement === firstActiveMboxPlacement) {
                            mboxPlacement.pos = mboxPlacement.refPos
                        } else {
                            mboxPlacement.pos = Math.min(
                                currentPos - 2 - mboxPlacement.height,
                                mboxPlacement.refPos
                            )
                        }
                        currentPos = mboxPlacement.pos
                        activeIndex--
                    }
                    if (firstActiveIndex > -1) {
                        currentPos =
                            firstActiveMboxPlacement.pos +
                            firstActiveMboxPlacement.height
                        activeIndex = firstActiveIndex + 1
                    } else {
                        activeIndex = 0
                    }

                    while (activeIndex < marginBoxPlacements.length) {
                        const mboxPlacement = marginBoxPlacements[activeIndex]
                        mboxPlacement.pos = Math.max(
                            currentPos + 2,
                            mboxPlacement.refPos
                        )
                        currentPos = mboxPlacement.pos + mboxPlacement.height
                        activeIndex++
                    }

                    const initialOffset = document.body.classList.contains(
                        "header-closed"
                    )
                        ? 72 + 90
                        : 225 + 90
                    let totalOffset = 0

                    marginBoxesPlacementStyle = marginBoxPlacements
                        .map((mboxPlacement, index) => {
                            if (mboxPlacement.height === 0) {
                                return ""
                            }
                            const pos = mboxPlacement.pos - initialOffset
                            let css = ""
                            if (pos !== totalOffset) {
                                const topMargin = Math.max(
                                    0,
                                    Number.parseInt(pos - totalOffset)
                                )
                                css += `#margin-box-container div.margin-box:nth-of-type(${index + 1}) {margin-top: ${topMargin}px;}\n`
                                totalOffset += topMargin
                            }
                            totalOffset += mboxPlacement.height
                            return css
                        })
                        .join("")
                    if (firstActiveIndex > -1) {
                        const topMenuHeight =
                            this.editor.dom.querySelector("header").offsetHeight
                        const refDistanceFromTop = this.editor.view.coordsAtPos(
                            referrers[firstActiveIndex]
                        ).top

                        if (
                            refDistanceFromTop - topMenuHeight < 0 ||
                            refDistanceFromTop > window.innerHeight - 30
                        ) {
                            const scrollTop =
                                refDistanceFromTop - (topMenuHeight + 90)
                            window.scrollBy({
                                left: 0,
                                top: scrollTop,
                                behavior: "smooth",
                                block: "center"
                            })
                        }
                    }
                }

                fastdom__rspack_import_1_default().mutate(() => {
                    //DOM write phase
                    if (
                        this.marginBoxesPlacementStyle !==
                        marginBoxesPlacementStyle
                    ) {
                        document.getElementById(
                            "margin-box-placement-style"
                        ).innerHTML = marginBoxesPlacementStyle
                        this.marginBoxesPlacementStyle =
                            marginBoxesPlacementStyle
                    }
                    if (this.editor.mod.comments.store.commentDuringCreation) {
                        this.editor.mod.comments.store.commentDuringCreation.inDOM = true
                    }
                    resolve()
                })
            })
        })
    }

    fnPosToPos(fnPos) {
        const fnIndex = this.editor.mod.footnotes.fnEditor.view.state.doc
                .resolve(fnPos)
                .index(0),
            fnMarker = (0,_state_plugins__rspack_import_3.getFootnoteMarkers)(this.editor.view.state)[fnIndex]

        return fnMarker.from
    }

    getMarginBoxes(
        node,
        pos,
        refPos,
        lastNode,
        lastNodeTracks,
        view,
        marginBoxes,
        referrers,
        selectedChanges,
        selection // Selection in main editor
    ) {
        if (node.attrs.help) {
            // Help/instruction margin boxes
            const helpBox = {
                type: "help",
                data: {
                    active: selection.$anchor.node(1) === node ? true : false,
                    help: node.attrs.help
                }
            }
            marginBoxes.push(helpBox)
            referrers.push(refPos)
        }

        if (node.type.name === "cross_reference" && !node.attrs.title) {
            const warningBox = {
                type: "warning",
                data: {
                    active: selection.node && selection.node === node,
                    warning: gettext("A cross reference has lost its target.")
                }
            }
            marginBoxes.push(warningBox)
            referrers.push(refPos)
        }

        if (
            node.marks.find(
                mark =>
                    mark.type.name === "link" &&
                    mark.attrs.href.charAt(0) === "#" &&
                    !mark.attrs.title
            )
        ) {
            const linkMark = node.marks.find(mark => mark.type.name === "link")
            const warningBox = {
                type: "warning",
                data: {
                    active: linkMark.isInSet(selection.$anchor.marks()),
                    warning: gettext("An internal link has lost its target.")
                }
            }
            marginBoxes.push(warningBox)
            referrers.push(refPos)
        }

        const commentIds =
            node.isInline || node.isLeaf
                ? this.editor.mod.comments.interactions.findCommentIds(node)
                : []

        const nodeTracks = node.attrs.track
            ? node.attrs.track.map(track => {
                  const nodeTrack = {
                      type: track.type,
                      data: {
                          user: track.user,
                          username: track.username,
                          date: track.date
                      }
                  }
                  if (track.type === "block_change") {
                      nodeTrack.data.before = track.before
                  }
                  return nodeTrack
              })
            : node.marks
                  .filter(
                      mark =>
                          ["deletion", "format_change"].includes(
                              mark.type.name
                          ) ||
                          (mark.type.name === "insertion" &&
                              !mark.attrs.approved)
                  )
                  .map(mark => ({type: mark.type.name, data: mark.attrs}))

        // Filter out trackmarks already present in the last node (if it's an inline node).
        const tracks =
            node.isInline === lastNode.isInline
                ? nodeTracks.filter(
                      track =>
                          !lastNodeTracks.find(
                              lastTrack =>
                                  track.type === lastTrack.type &&
                                  track.data.user === lastTrack.data.user &&
                                  track.data.date === lastTrack.data.date &&
                                  (node.isInline || // block level changes almost always need new boxes
                                      (node.type.name === "paragraph" &&
                                          lastNode.type.name === "list_item" &&
                                          lastTrack.type === "insertion")) && // Don't show first paragraphs in list items.
                                  (["insertion", "deletion"].includes(
                                      track.type
                                  ) ||
                                      (track.type === "format_change" &&
                                          track.data.before.length ===
                                              lastTrack.data.before.length &&
                                          track.data.after.length ===
                                              lastTrack.data.after.length &&
                                          track.data.before.every(markName =>
                                              lastTrack.data.before.includes(
                                                  markName
                                              )
                                          ) &&
                                          track.data.after.every(markName =>
                                              lastTrack.data.after.includes(
                                                  markName
                                              )
                                          )) ||
                                      (track.type === "block_change" &&
                                          track.data.before.type ===
                                              lastTrack.data.before.type &&
                                          track.data.before.attrs.level ===
                                              lastTrack.data.before.attrs
                                                  .level))
                          )
                  )
                : nodeTracks
        tracks.forEach(track => {
            marginBoxes.push(
                Object.assign(
                    {
                        node,
                        pos,
                        view,
                        active:
                            selectedChanges[track.type] &&
                            selectedChanges[track.type].from === pos
                    },
                    track
                )
            )
            referrers.push(refPos)
        })

        if (!commentIds.length && !tracks.length) {
            return nodeTracks
        }
        commentIds.forEach(commentId => {
            const comment =
                this.editor.mod.comments.store.findComment(commentId)
            if (
                !comment ||
                (this.filterOptions.commentsOnlyMajor && !comment.isMajor) ||
                (!this.filterOptions.commentsResolved && comment.resolved)
            ) {
                // We have no comment with this ID. Ignore the referrer.
                return
            }
            if (marginBoxes.find(marginBox => marginBox.data === comment)) {
                // comment already placed
                return
            }
            const active =
                comment.id ===
                this.editor.mod.comments.interactions.activeCommentId

            if (this.filterOptions.comments) {
                if (active) {
                    this.activeCommentStyle += `.comment[data-id="${comment.id}"], .comment[data-id="${comment.id}"] .comment {background-color: ${this.commentColors.active} !important;}`
                } else if (comment.isMajor) {
                    this.activeCommentStyle += `#paper-editable .comment[data-id="${comment.id}"] {background-color: ${this.commentColors.isMajor};}`
                } else {
                    this.activeCommentStyle += `#paper-editable .comment[data-id="${comment.id}"] {background-color: ${this.commentColors.marker};}`
                }
            }
            marginBoxes.push({
                type: "comment",
                data: comment,
                pos,
                view,
                active
            })
            referrers.push(refPos)
        })

        return nodeTracks
    }

    closeAllLongComments(selector = ".comment-p.show-more") {
        document.body.querySelectorAll(selector).forEach(el => {
            el.classList.remove("show-more")
            const showMoreButton =
                el.parentElement.parentElement.querySelector(".show-more-less")
            if (showMoreButton) {
                showMoreButton.innerText = `${gettext("show more")}`
            }
        })
    }

    positionMarginBoxOptions(marginBoxDialog, showMarginboxOptionsBtn) {
        const btnTop = showMarginboxOptionsBtn.getBoundingClientRect().top,
            scrollTopOffset = window.pageYOffset,
            mBoxRight = showMarginboxOptionsBtn
                .closest(".comment-answer-container")
                .getBoundingClientRect().right

        marginBoxDialog.style.top = `${btnTop + scrollTopOffset + 30}px`
        marginBoxDialog.style.left = `${mBoxRight - marginBoxDialog.getBoundingClientRect().width - 10}px`
    }

    commentOptionsOnScroll() {
        document
            .querySelectorAll(".comment-answer-container")
            .forEach(element => {
                element.addEventListener("scroll", () => {
                    const scrollTop = element.scrollTop
                    const marginBoxOption = Array.from(element.children).find(
                        node => node.matches(".show-marginbox-options")
                    )
                    if (scrollTop > 50) {
                        marginBoxOption?.classList.add("hide")
                    } else {
                        marginBoxOption?.classList.remove("hide")
                    }
                })
            })
    }

    toggleShowMore(element) {
        const commentText =
            element.target.parentElement.parentElement.querySelector(
                ".comment-p"
            )
        commentText.classList.toggle("show-more")
        if (commentText.classList.contains("show-more")) {
            element.target.innerText = `${gettext("show less")}`
        } else {
            element.target.innerText = `${gettext("show more")}`
        }
    }

    setTrackStyle() {
        if (!this.filterOptions.track) {
            this.trackOptionsStyleElement.innerHTML = `span.insertion, .selected-insertion, .selected-format_change, .selected-block_change {
                color: inherit !important;
                background-color: inherit !important;
            }
            span.deletion {
                display: none;
            }`
        } else if (this.filterOptions.trackAuthor) {
            this.trackOptionsStyleElement.innerHTML = `span.insertion:not([data-user="${this.filterOptions.trackAuthor}"]),
            span.insertion:not([data-user="${this.filterOptions.trackAuthor}"]) .selected-insertion,
            .selected-format_change:not([data-user="${this.filterOptions.trackAuthor}"]),
            .selected-block_change:not([data-user="${this.filterOptions.trackAuthor}"]) {
                color: inherit !important;
                background-color: inherit !important;
            }
            span.deletion:not([data-user="${this.filterOptions.trackAuthor}"]) {
                display: none;
            }`
        } else {
            this.trackOptionsStyleElement.innerHTML = ""
        }
    }
}


}),
"./js/modules/editor/marginboxes/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  marginBoxOptions: function() { return marginBoxOptions; },
  marginBoxesTemplate: function() { return marginBoxesTemplate; },
  marginboxFilterTemplate: function() { return marginboxFilterTemplate; }
});
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _document_template__rspack_import_2 = __webpack_require__("./js/modules/document_template/index.js");
/* import */ var _comments_editors__rspack_import_3 = __webpack_require__("./js/modules/editor/comments/editors/index.js");





/** A template for an answer to a comment */
const answerCommentTemplate = ({
    answer,
    author,
    commentId,
    activeCommentAnswerId,
    active,
    user
}) =>
    `<div class="comment-item comment-answer collapse ${active ? "show" : ""}" id="comment-answer-${answer.id}">
        <div class="comment-user">
            ${author ? (0,_common__rspack_import_1.avatarTemplate)({user: author}) : '<span class="fw-string-avatar"></span>'}
            <h5 class="comment-user-name">${(0,_common__rspack_import_1.escapeText)(author ? author.name : answer.username)}</h5>
            <p class="comment-date">${(0,_common__rspack_import_1.localizeDate)(answer.date)}</p>
        </div>
        ${
            active && answer.id === activeCommentAnswerId
                ? `<div class="comment-text-wrapper">
                <div class="comment-answer-form">
                    <div id="answer-editor"></div>
                </div>
           </div>`
                : `<div class="comment-text-wrapper">
               <div class="comment-p">${(0,_comments_editors__rspack_import_3.serializeComment)(answer.answer).html}</div>
           </div>
        <div class="comment-collapsible-buttons">
                ${
                    (0,_comments_editors__rspack_import_3.serializeComment)(answer.answer).text.length > 68
                        ? `<a type="button" class="comment-expand-compress show-more-less">${gettext("show more")}</a>`
                        : ""
                }
                </div>
           ${
               answer.user === user.id
                   ? `<span class="show-marginbox-options fa fa-ellipsis-v" data-id="${answer.id}" data-commentId="${commentId}" data-answer=${true}></span>`
                   : ""
           }`
        }
    </div>`

/** A template to show one individual comment */
const singleCommentTemplate = ({comment, author, active, editComment}) =>
    `<div class="comment-item">
        <div class="comment-user">
            ${author ? (0,_common__rspack_import_1.avatarTemplate)({user: author}) : '<span class="fw-string-avatar"></span>'}
            <h5 class="comment-user-name">${(0,_common__rspack_import_1.escapeText)(author ? author.name : comment.username)}</h5>
            <p class="comment-date">${(0,_common__rspack_import_1.localizeDate)(comment.date)}</p>
        </div>
        <div class="comment-text-wrapper">
            ${
                active && editComment
                    ? '<div id="comment-editor"></div>'
                    : `<p class="comment-p">${(0,_comments_editors__rspack_import_3.serializeComment)(comment.comment).html}</p>`
            }
        </div>
        <div class="comment-collapsible-buttons">
                ${
                    !editComment &&
                    (0,_comments_editors__rspack_import_3.serializeComment)(comment.comment).text.length > 68
                        ? `<a type="button" class="comment-expand-compress show-more-less">${gettext("show more")}</a>`
                        : ""
                }
                ${
                    !active && comment.answers.length > 0
                        ? `<a type="button" class="comment-expand-compress replies">+${comment.answers.length} ${gettext("replies")}</a>`
                        : ""
                }
            </div>
    </div>`

/** A template for the editor of a first comment before it has been saved (not an answer to a comment). */
const firstCommentTemplate = ({comment, author}) =>
    `<div class="comment-item">
        <div class="comment-user">
            ${author ? (0,_common__rspack_import_1.avatarTemplate)({user: author}) : '<span class="fw-string-avatar"></span>'}
            <h5 class="comment-user-name">${(0,_common__rspack_import_1.escapeText)(author ? author.name : comment.username)}</h5>
            <p class="comment-date">${(0,_common__rspack_import_1.localizeDate)(comment.date)}</p>
        </div>
        <div class="comment-text-wrapper">
            <div id="comment-editor"></div>
        </div>
    </div>`

const helpTemplate = ({help, filterOptions}) => {
    if (!filterOptions.help || !filterOptions.info) {
        return '<div class="margin-box help hidden"></div>'
    } else {
        return `<div class="margin-box help ${help.active ? "active" : ""}"><div class="help-text-wrapper">${(0,_document_template__rspack_import_2.serializeHelp)(help.help)}</div></div>`
    }
}

const warningTemplate = ({warning, filterOptions}) => {
    if (!filterOptions.warning || !filterOptions.info) {
        return '<div class="margin-box warning hidden"></div>'
    } else {
        return `<div class="margin-box warning ${warning.active ? "active" : ""}"><div class="help-text-wrapper">${warning.warning}</div></div>`
    }
}

const commentTemplate = ({
    comment,
    view,
    active,
    editComment,
    activeCommentAnswerId,
    user,
    docInfo,
    filterOptions
}) => {
    if (
        !filterOptions.comments ||
        (filterOptions.commentsOnlyMajor && !comment.isMajor) ||
        (!filterOptions.commentsResolved && comment.resolved) ||
        (filterOptions.commentsAuthor &&
            comment.user !== filterOptions.commentsAuthor) ||
        (filterOptions.assigned &&
            comment.assignedUser !== filterOptions.assigned) ||
        comment.hidden
    ) {
        return '<div class="margin-box comment hidden"></div>'
    }
    const author =
            comment.user === docInfo.owner.id
                ? docInfo.owner
                : docInfo.owner.contacts.find(
                      contact =>
                          contact.id === comment.user && contact.type === "user"
                  ),
        assignedUser = comment.assignedUser
            ? comment.assignedUser === docInfo.owner.id
                ? docInfo.owner
                : docInfo.owner.contacts.find(
                      contact =>
                          contact.id === comment.assignedUser &&
                          contact.type === "user"
                  ) || {
                      name: comment.assignedUsername || ""
                  }
            : false,
        assignedUsername = assignedUser ? assignedUser.name : false
    return `
        <div id="margin-box-${comment.id}" data-view="${view}" data-id="${comment.id}" data-user-id="${comment.user}"
            class="margin-box comment ${active ? "active" : "inactive"} ${comment.resolved ? "resolved" : ""} ${comment.isMajor === true ? "comment-is-major-bgc" : ""}">
<div class="comment-answer-container">
${
    comment.comment.length === 0
        ? firstCommentTemplate({comment, author})
        : singleCommentTemplate({comment, user, author, active, editComment})
}
    ${
        assignedUsername
            ? `<div class="assigned-user">${gettext("Assigned to")} <em>${(0,_common__rspack_import_1.escapeText)(assignedUsername)}</em></div>`
            : ""
    }
    ${
        comment.answers
            ? comment.answers
                  .map(answer =>
                      answerCommentTemplate({
                          answer,
                          author:
                              answer.user === docInfo.owner.id
                                  ? docInfo.owner
                                  : docInfo.owner.contacts.find(
                                        contact =>
                                            contact.id === answer.user &&
                                            contact.type === "user"
                                    ),
                          commentId: comment.id,
                          active,
                          activeCommentAnswerId,
                          user,
                          docInfo
                      })
                  )
                  .join("")
            : ""
    }
    ${
        comment.id > 0 &&
        ((comment.user === user.id &&
            !___rspack_import_0.READ_ONLY_ROLES.includes(docInfo.access_rights)) ||
            docInfo.access_rights === "write") &&
        !editComment
            ? `<span class="show-marginbox-options comment-option fas fa-ellipsis-v" data-id="${comment.id}" data-commentUser="${comment.user}"
></span>`
            : ""
    }
    </div>
    ${
        active &&
        !activeCommentAnswerId &&
        !editComment &&
        0 < comment.comment.length &&
        !___rspack_import_0.READ_ONLY_ROLES.includes(docInfo.access_rights)
            ? `<div class="comment-item comment-answer comment-answer-editor">
                <div id="answer-editor"></div>
            </div>`
            : ""
    }
    </div>
`
}

const ACTIONS = {
    insertion: gettext("Insertion"),
    deletion: gettext("Deletion"),
    format_change: gettext("Format change"),
    block_change: gettext("Block change"),
    insertion_paragraph: gettext("New paragraph"),
    insertion_heading: gettext("New heading"),
    insertion_citation: gettext("Inserted citation"),
    insertion_blockquote: gettext("Wrapped into blockquote"),
    insertion_code_block: gettext("Added code block"),
    insertion_figure: gettext("Inserted figure"),
    insertion_list_item: gettext("New list item"),
    insertion_table: gettext("Inserted table"),
    insertion_keyword: gettext("New keyword: %(keyword)s"),
    deletion_paragraph: gettext("Merged paragraph"),
    deletion_heading: gettext("Merged heading"),
    deletion_citation: gettext("Deleted citation"),
    deletion_blockquote: gettext("Unwrapped blockquote"),
    deletion_code_block: gettext("Removed code block"),
    deletion_figure: gettext("Deleted figure"),
    deletion_list_item: gettext("Lifted list item"),
    deletion_table: gettext("Delete table"),
    deletion_keyword: gettext("Deleted keyword: %(keyword)s"),
    block_change_paragraph: gettext("Changed into paragraph"),
    block_change_heading: gettext("Changed into heading %(level)s"),
    block_change_code_block: gettext("Changed into code block")
}

const FORMAT_MARK_NAMES = {
    em: gettext("Emphasis"),
    strong: gettext("Strong"),
    underline: gettext("Underline")
}

const formatChangeTemplate = ({before, after}) => {
    let returnText = ""
    if (before.length) {
        returnText += `<div class="format-change-info"><b>${gettext("Removed")}:</b> ${before.map(markName => FORMAT_MARK_NAMES[markName]).join(", ")}</div>`
    }
    if (after.length) {
        returnText += `<div class="format-change-info"><b>${gettext("Added")}:</b> ${after.map(markName => FORMAT_MARK_NAMES[markName]).join(", ")}</div>`
    }
    return returnText
}

const BLOCK_NAMES = {
    paragraph: gettext("Paragraph"),
    heading1: gettext("Heading 1"),
    heading2: gettext("Heading 2"),
    heading3: gettext("Heading 3"),
    heading4: gettext("Heading 4"),
    heading5: gettext("Heading 5"),
    heading6: gettext("Heading 6"),
    code_block: gettext("Code block"),
    ordered_list: gettext("Ordered list"),
    bullet_list: gettext("Bullet list")
}

const blockChangeTemplate = ({before}, node) =>
    `<div class="format-change-info"><b>${gettext("Was")}:</b> ${BLOCK_NAMES[before.type]}${before.type === "ordered_list" && node.type.name === "ordered_list" ? `, ${gettext("start")}: ${before.attrs.order}` : ""}</div>`

const trackTemplate = ({type, data, node, active, docInfo, filterOptions}) => {
    if (
        !filterOptions.track ||
        (filterOptions.trackAuthor && data.user !== filterOptions.trackAuthor)
    ) {
        return '<div class="margin-box track hidden"></div>'
    }

    const author =
            data.user === docInfo.owner.id
                ? docInfo.owner
                : docInfo.owner.contacts.find(
                      contact =>
                          contact.id === data.user && contact.type === "user"
                  ),
        nodeActionType = `${type}_${node.type.name}`

    return `
        <div class="margin-box track ${active ? "active" : "inactive"}" data-type="${type}">
            <div class="track-${type}">
                <div class="comment-user">
                    ${author ? (0,_common__rspack_import_1.avatarTemplate)({user: author}) : '<span class="fw-string-avatar"></span>'}
                    <h5 class="comment-user-name">${(0,_common__rspack_import_1.escapeText)(author ? author.name : data.username)}</h5>
                    <p class="comment-date">${node.type.name === "text" ? `${gettext("ca.")} ` : ""}${(0,_common__rspack_import_1.localizeDate)(data.date * 60000, "minutes")}</p>
                </div>
                <div class="track-title">
                    ${interpolate(ACTIONS[nodeActionType] ? ACTIONS[nodeActionType] : ACTIONS[type], node.attrs, true)}
                </div>
                ${type === "format_change" ? formatChangeTemplate(data) : type === "block_change" ? blockChangeTemplate(data, node) : ""}
            </div>
            ${
                docInfo.access_rights === "write"
                    ? `<div class="track-ctas">
                        <button class="track-accept fw-button fw-dark" type="submit" data-type="${type}">${gettext("Accept")}</button>
                        <button class="track-reject fw-button fw-orange" type="submit" data-type="${type}">${gettext("Reject")}</button>
                    </div>`
                    : ""
            }

        </div>`
}

const marginboxFilterTemplate = ({
    marginBoxes,
    filterOptions,
    pastParticipants
}) => {
    const comments = marginBoxes.find(box => box.type === "comment")
    const tracks = marginBoxes.find(box =>
        ["insertion", "deletion", "format_change", "block_change"].includes(
            box.type
        )
    )
    const help = marginBoxes.find(box => box.type === "help")
    const warning = marginBoxes.find(box => box.type === "warning")
    let filterHTML = ""
    if (comments || filterOptions.commentsOnlyMajor) {
        filterHTML += `<div id="margin-box-filter-comments" class="margin-box-filter-button${filterOptions.comments ? "" : " disabled"}">
            <span class="label">${gettext("Comments")}</span>
            <span class="show-marginbox-options fa fa-ellipsis-v"></span>
            <div class="marginbox-options fw-pulldown fw-right"><ul>
                <li>
                    <span class="fw-pulldown-item show-marginbox-options-submenu" title="${gettext("Author")}">
                        ${gettext("Author")}
                        <span class="fw-icon-right"><i class="fa fa-caret-right"></i></span>
                    </span>
                    <div class="fw-pulldown marginbox-options-submenu">
                        <ul>
                            <li><span class="fw-pulldown-item margin-box-filter-comments-author${filterOptions.commentsAuthor === 0 ? " selected" : ""}" data-id="0" title="${gettext("Show comments from all authors.")}">
                                ${gettext("Any")}
                            </span></li>
                        ${pastParticipants
                            .map(
                                user => `<li><span class="fw-pulldown-item margin-box-filter-comments-author${filterOptions.commentsAuthor === user.id ? " selected" : ""}" data-id="${user.id}" title="${gettext("Show comments of ")} ${(0,_common__rspack_import_1.escapeText)(user.name)}">
                                    ${(0,_common__rspack_import_1.escapeText)(user.name)}
                                </span></li>`
                            )
                            .join("")}
                        </ul>
                    </div>
                </li>
                <li>
                    <span class="fw-pulldown-item show-marginbox-options-submenu" title="${gettext("Assignee")}">
                        ${gettext("Assignee")}
                        <span class="fw-icon-right"><i class="fa fa-caret-right"></i></span>
                    </span>
                    <div class="fw-pulldown marginbox-options-submenu">
                        <ul>
                            <li><span class="fw-pulldown-item margin-box-filter-comments-assigned${filterOptions.assigned === 0 ? " selected" : ""}" data-id="0" title="${gettext("Show comments from all authors.")}">
                                ${gettext("Any/None")}
                            </span></li>
                        ${pastParticipants
                            .map(
                                user => `<li><span class="fw-pulldown-item margin-box-filter-comments-assigned${filterOptions.assigned === user.id ? " selected" : ""}" data-id="${user.id}" title="${gettext("Show comments of ")} ${(0,_common__rspack_import_1.escapeText)(user.name)}">
                                    ${(0,_common__rspack_import_1.escapeText)(user.name)}
                                </span></li>`
                            )
                            .join("")}
                        </ul>
                    </div>
                </li>
                <li>
                    <span class="fw-pulldown-item margin-box-filter-check">
                        <input type="checkbox" class="fw-check fw-label-check"${filterOptions.commentsOnlyMajor ? " checked" : ""} id="margin-box-filter-comments-only-major">
                        <label for="margin-box-filter-comments-only-major">${gettext("Only major comments")}</label>
                    </span>
                </li>
                <li>
                    <span class="fw-pulldown-item margin-box-filter-check">
                        <input type="checkbox" class="fw-check fw-label-check"${filterOptions.commentsResolved ? " checked" : ""} id="margin-box-filter-comments-resolved">
                        <label for="margin-box-filter-comments-resolved">${gettext("Resolved comments")}</label>
                    </span>
                </li>
            </ul></div>
        </div>`
    }
    if (tracks) {
        filterHTML += `<div id="margin-box-filter-track" class="margin-box-filter-button${filterOptions.track ? "" : " disabled"}">
            <span class="label">${gettext("Tracking")}</span><span class="show-marginbox-options fa fa-ellipsis-v"></span>
            <div class="marginbox-options fw-pulldown fw-right"><ul>
                <li>
                    <span class="fw-pulldown-item show-marginbox-options-submenu" title="${gettext("Author")}">
                        ${gettext("Author")}
                        <span class="fw-icon-right"><i class="fa fa-caret-right"></i></span>
                    </span>
                    <div class="fw-pulldown marginbox-options-submenu">
                        <ul>
                            <li><span class="fw-pulldown-item margin-box-filter-track-author${filterOptions.trackAuthor === 0 ? " selected" : ""}" data-id="0" title="${gettext("Show track changes from all authors.")}">
                                ${gettext("Any")}
                            </span></li>
                        ${pastParticipants
                            .map(
                                user => `<li><span class="fw-pulldown-item margin-box-filter-track-author${filterOptions.trackAuthor === user.id ? " selected" : ""}" data-id="${user.id}" title="${gettext("Show track changes of ")} ${(0,_common__rspack_import_1.escapeText)(user.name)}">
                                    ${(0,_common__rspack_import_1.escapeText)(user.name)}
                                </span></li>`
                            )
                            .join("")}
                        </ul>
                    </div>
                </li>
            </ul></div>
        </div>`
    }
    if (help || warning) {
        filterHTML += `<div id="margin-box-filter-info" class="margin-box-filter-button${filterOptions.info ? "" : " disabled"}">
            <span class="label">${gettext("Informational")}</span>
            <span class="show-marginbox-options fa fa-ellipsis-v"></span>
            <div class="marginbox-options fw-pulldown fw-right"><ul>
                <li>
                <span class="fw-pulldown-item margin-box-filter-check">
                    <input type="checkbox" class="fw-check fw-label-check"${filterOptions.help ? " checked" : ""} id="margin-box-filter-info-help">
                    <label for="margin-box-filter-info-help">${gettext("Instructions")}</label>
                </span>
                </li>
                <li>
                <span class="fw-pulldown-item margin-box-filter-check">
                    <input type="checkbox" class="fw-check fw-label-check"${filterOptions.warning ? " checked" : ""} id="margin-box-filter-info-warning">
                    <label for="margin-box-filter-info-warning">${gettext("Warnings")}</label>
                </span>
                </li>
            </ul></div>
        </div>`
    }
    return filterHTML
}

/** A template to display all the margin boxes (comments, deletion/insertion notifications) */
const marginBoxesTemplate = ({
    marginBoxes,
    editComment,
    activeCommentAnswerId,
    user,
    docInfo,
    filterOptions
}) =>
    `<div id="margin-box-container"><div>${marginBoxes
        .map(mBox => {
            let returnValue = ""
            switch (mBox.type) {
                case "comment":
                    returnValue = commentTemplate({
                        comment: mBox.data,
                        view: mBox.view,
                        active: mBox.active,
                        activeCommentAnswerId,
                        editComment,
                        user,
                        docInfo,
                        filterOptions
                    })
                    break
                case "insertion":
                case "deletion":
                case "format_change":
                case "block_change":
                    returnValue = trackTemplate({
                        type: mBox.type,
                        node: mBox.node,
                        data: mBox.data,
                        active: mBox.active,
                        docInfo,
                        filterOptions
                    })
                    break
                case "help":
                    returnValue = helpTemplate({help: mBox.data, filterOptions})
                    break
                case "warning":
                    returnValue = warningTemplate({
                        warning: mBox.data,
                        filterOptions
                    })
                    break
                default:
                    break
            }
            return returnValue
        })
        .join("")}</div></div>`

const marginBoxOptions = (comment, user, docInfo) => {
    return `<div class="comment-answer-options marginbox-options fw-pulldown">
                ${
                    !comment.answer
                        ? `<ul>
                    ${
                        comment.user === user.id
                            ? `<li><span class="fw-pulldown-item edit-comment" data-id="${comment.id}" title="${gettext("Edit")}">
                                    ${gettext("Edit")}
                                </span></li>`
                            : ""
                    }
                <li>
                    <span class="fw-pulldown-item show-marginbox-options-submenu" title="${gettext("Assign comment to user")}">
                        ${gettext("Assign to")}
                        <span class="fw-icon-right"><i class="fa fa-caret-right"></i></span>
                    </span>
                    <div class="fw-pulldown marginbox-options-submenu">
                        <ul>
                            <li><span class="fw-pulldown-item unassign-comment" data-id="${comment.id}" title="${gettext("Remove user assignment from comment")}">${gettext("No-one")}</span></li>
                        ${docInfo.owner.contacts
                            .concat(docInfo.owner)
                            .filter(contact => contact.type !== "userinvite")
                            .map(
                                user =>
                                    `<li><span class="fw-pulldown-item assign-comment" data-id="${comment.id}" data-user="${user.id}" data-username="${(0,_common__rspack_import_1.escapeText)(user.name)}" title="${gettext("Assign comment to")} ${(0,_common__rspack_import_1.escapeText)(user.name)}">${(0,_common__rspack_import_1.escapeText)(user.name)}</span></li>`
                            )
                            .join("")}
                        </ul>
                    </div>
                </li>
                <li>
                    ${
                        comment.resolved
                            ? `<span class="fw-pulldown-item recreate-comment" data-id="${comment.id}" title="${gettext("Recreate comment")}">${gettext("Recreate")}</span>`
                            : `<span class="fw-pulldown-item resolve-comment" data-id="${comment.id}" title="${gettext("Resolve comment")}">${gettext("Resolve")}</span>`
                    }

                </li>
                <li>
                    <span class="fw-pulldown-item delete-comment" data-id="${comment.id}" title="${gettext("Delete comment")}">${gettext("Delete")}</span>
                </li>
            </ul>`
                        : `<ul>
                <li><span class="fw-pulldown-item edit-comment-answer" data-id="${comment.commentId}" data-answer="${comment.id}" title="${gettext("Edit")}">
                ${gettext("Edit")}
                </span></li>
                <li><span class="fw-pulldown-item delete-comment-answer" data-id="${comment.commentId}" data-answer="${comment.id}" title="${gettext("Delete")}">
                ${gettext("Delete")}
                </span></li>
           </ul>`
                }
            </div>`
}


}),
"./js/modules/editor/menus/code_block/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  codeBlockMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.codeBlockMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/code_block/model.js");



}),
"./js/modules/editor/menus/code_block/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  codeBlockMenuModel: function() { return codeBlockMenuModel; }
});
/* import */ var _dialogs__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/index.js");


const codeBlockMenuModel = () => ({
    content: [
        {
            title: `${gettext("Configure")} ...`,
            type: "action",
            tooltip: gettext("Configure the code block."),
            order: 1,
            action: editor => {
                const dialog = new _dialogs__rspack_import_0.CodeBlockDialog(editor)
                dialog.init()
                return false
            },
            disabled: editor => {
                const view = editor.currentView
                const {selection} = view.state

                // Check if we're in a code block
                let inCodeBlock = false
                if (
                    selection.node &&
                    selection.node.type.name === "code_block"
                ) {
                    inCodeBlock = true
                } else {
                    const $head = selection.$head
                    for (let d = $head.depth; d > 0; d--) {
                        if ($head.node(d).type.name === "code_block") {
                            inCodeBlock = true
                            break
                        }
                    }
                }

                return !inCodeBlock
            }
        },
        {
            title: gettext("Remove"),
            type: "action",
            tooltip: gettext("Remove the code block."),
            order: 2,
            action: editor => {
                const view = editor.currentView
                const tr = view.state.tr.deleteSelection()
                view.dispatch(tr)
                return false
            }
        }
    ]
})


}),
"./js/modules/editor/menus/figure/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  figureMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.figureMenuModel; },
  figureWidthMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.figureWidthMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/figure/model.js");




}),
"./js/modules/editor/menus/figure/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  figureMenuModel: function() { return figureMenuModel; },
  figureWidthMenuModel: function() { return figureWidthMenuModel; }
});
/* import */ var _dialogs__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/index.js");
/* import */ var _utils__rspack_import_1 = __webpack_require__("./js/modules/editor/menus/figure/utils.js");



const figureMenuModel = () => ({
    content: [
        {
            title: `${gettext("Configure")} ...`,
            type: "action",
            tooltip: gettext("Configure the figure."),
            order: 1,
            action: editor => {
                const dialog = new _dialogs__rspack_import_0.FigureDialog(editor)
                dialog.init()
                return false
            },
            disabled: editor =>
                !(
                    editor.currentView.state.selection.node?.type.name ===
                    "figure"
                ) ||
                editor.currentView.state.selection.node?.attrs.track?.find(
                    track => track.type === "deletion"
                )
        },
        {
            title: gettext("Delete figure"),
            type: "action",
            icon: "trash-alt",
            tooltip: gettext("Delete the figure"),
            order: 2,
            action: editor => {
                const tr = editor.currentView.state.tr
                tr.deleteSelection()
                editor.currentView.dispatch(tr)
            },
            disabled: editor =>
                !(
                    editor.currentView.state.selection.node?.type.name ===
                    "figure"
                ) ||
                editor.currentView.state.selection.node?.attrs.track?.find(
                    track => track.type === "deletion"
                )
        }
    ]
})

const figureWidthMenuModel = () => ({
    content: [
        {
            title: "100 %",
            type: "action",
            order: 0,
            value: "100",
            action: figureDialog => {
                ;(0,_utils__rspack_import_1.figureMenuAction)("100", figureDialog)
            },
            selected: false
        },
        {
            title: "75 %",
            type: "action",
            order: 1,
            value: "75",
            action: figureDialog => {
                (0,_utils__rspack_import_1.figureMenuAction)("75", figureDialog)
            },
            selected: false
        },
        {
            title: "50 %",
            type: "action",
            order: 2,
            value: "50",
            action: figureDialog => {
                (0,_utils__rspack_import_1.figureMenuAction)("50", figureDialog)
            },
            selected: false
        },
        {
            title: "25 %",
            type: "action",
            order: 3,
            value: "25",
            action: figureDialog => {
                (0,_utils__rspack_import_1.figureMenuAction)("25", figureDialog)
            },
            selected: false
        }
    ]
})


}),
"./js/modules/editor/menus/figure/utils.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  figureMenuAction: function() { return figureMenuAction; }
});
const figureMenuAction = (value, figureDialog) => {
    const buttonDOM = document.querySelector(".figure-width")
    buttonDOM.firstElementChild.innerText = `${value} %`
    figureDialog.width = value
}


}),
"./js/modules/editor/menus/headerbar/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  HeaderbarView: function() { return /* reexport safe */ _view__rspack_import_1.HeaderbarView; },
  headerbarModel: function() { return /* reexport safe */ _model__rspack_import_0.headerbarModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/headerbar/model.js");
/* import */ var _view__rspack_import_1 = __webpack_require__("./js/modules/editor/menus/headerbar/view.js");




}),
"./js/modules/editor/menus/headerbar/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  headerbarModel: function() { return headerbarModel; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _copyright_dialog__rspack_import_1 = __webpack_require__("./js/modules/copyright_dialog/index.js");
/* import */ var _documents_access_rights__rspack_import_2 = __webpack_require__("./js/modules/documents/access_rights/index.js");
/* import */ var _exporter_native__rspack_import_3 = __webpack_require__("./js/modules/exporter/native/index.js");
/* import */ var _exporter_native_file__rspack_import_4 = __webpack_require__("./js/modules/exporter/native/file.js");
/* import */ var _dialogs__rspack_import_5 = __webpack_require__("./js/modules/editor/dialogs/index.js");
/* import */ var _e2ee_key_manager__rspack_import_6 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _e2ee_passphrase_manager__rspack_import_7 = __webpack_require__("./js/modules/editor/e2ee/passphrase-manager.js");
/* import */ var _e2ee_password_dialog__rspack_import_8 = __webpack_require__("./js/modules/editor/e2ee/password-dialog.js");
/* import */ var _tools__rspack_import_9 = __webpack_require__("./js/modules/editor/tools/index.js");











const languageItem = (language, name, order) => ({
    title: name,
    type: "setting",
    order,
    action: editor => {
        editor.view.dispatch(
            editor.view.state.tr
                .setDocAttribute("language", language)
                .setMeta("settings", true)
        )
    },
    selected: editor => {
        return editor.view.state.doc.attrs.language === language
    },
    available: editor => {
        return editor.view.state.doc.attrs.languages.includes(language)
    }
})

const headerbarModel = () => ({
    open: window.innerWidth > 500, // Whether the menu is shown at all.
    content: [
        {
            id: "file",
            title: gettext("File"),
            tooltip: gettext("File handling"),
            type: "menu",
            keys: "Alt-f",
            order: 0,
            content: [
                {
                    title: editor =>
                        editor.user.is_authenticated &&
                        editor.docInfo.token &&
                        !editor.docInfo.is_owner
                            ? gettext("Request Access")
                            : gettext("Share"),
                    type: "action",
                    //icon: 'share',
                    tooltip: editor =>
                        editor.user.is_authenticated &&
                        editor.docInfo.token &&
                        !editor.docInfo.is_owner
                            ? gettext("Request to be added as a collaborator.")
                            : gettext("Share the document with other users."),
                    order: 0,
                    action: editor => {
                        if (
                            editor.user.is_authenticated &&
                            editor.docInfo.token &&
                            !editor.docInfo.is_owner
                        ) {
                            // TokenUser requesting access
                            (0,_common__rspack_import_0.jsonPostJson)("/api/document/request_access/", {
                                document_id: editor.docInfo.id,
                                rights: "write"
                            })
                                .then(({json}) => {
                                    if (json.success) {
                                        (0,_common__rspack_import_0.addAlert)(
                                            "success",
                                            gettext(
                                                "Your access request has been sent to the document owner."
                                            )
                                        )
                                    } else {
                                        (0,_common__rspack_import_0.addAlert)(
                                            "error",
                                            json.error ||
                                                gettext(
                                                    "Could not send access request."
                                                )
                                        )
                                    }
                                })
                                .catch(() => {
                                    (0,_common__rspack_import_0.addAlert)(
                                        "error",
                                        gettext(
                                            "Could not send access request."
                                        )
                                    )
                                })
                            return
                        }
                        const onShareSuccess = async newAccessRights => {
                            // Share the document password with newly-added user recipients.
                            if (
                                editor.e2ee?.password &&
                                _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.hasKeysInSession()
                            ) {
                                const userRecipients = newAccessRights.filter(
                                    ar =>
                                        ar.holder?.type === "user" &&
                                        ar.rights !== "delete"
                                )
                                const noPassphraseUsers = []
                                for (const ar of userRecipients) {
                                    try {
                                        const hasKeys =
                                            await _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.userHasEncryptionKeys(
                                                ar.holder.id
                                            )
                                        if (hasKeys) {
                                            // Recipient has passphrase keys —
                                            // encrypt document password with their public key.
                                            await _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.saveDocumentPassword(
                                                editor.docInfo.id,
                                                editor.e2ee.password,
                                                ar.holder.id,
                                                "user",
                                                false
                                            )
                                        } else {
                                            noPassphraseUsers.push(
                                                ar.holder?.name || "user"
                                            )
                                        }
                                    } catch (_e) {
                                        noPassphraseUsers.push(
                                            ar.holder?.name || "user"
                                        )
                                    }
                                }

                                if (noPassphraseUsers.length > 0) {
                                    const dialog = new _common__rspack_import_0.Dialog({
                                        title: gettext(
                                            "Share Document Password"
                                        ),
                                        id: "share-password-dialog",
                                        width: 500,
                                        body: `<p>${gettext(
                                            "The following users don't have passphrase encryption. Please share the document password with them directly."
                                        )}</p><ul>${noPassphraseUsers
                                            .map(
                                                name =>
                                                    `<li><strong>${name}</strong>: <code>${editor.e2ee.password}</code></li>`
                                            )
                                            .join("")}</ul>`,
                                        buttons: [
                                            {
                                                text: gettext("Close"),
                                                classes: "fw-dark",
                                                click: () => dialog.close()
                                            }
                                        ]
                                    })
                                    dialog.open()
                                }
                            }
                        }
                        const dialog = new _documents_access_rights__rspack_import_2.DocumentAccessRightsDialog(
                            [editor.docInfo.id],
                            editor.docInfo.owner.contacts,
                            contactData => {
                                editor.docInfo.owner.contacts.push(contactData)
                            },
                            editor.e2ee?.encrypted,
                            editor.e2ee?.password || "",
                            onShareSuccess,
                            editor.app.settings
                        )
                        dialog.init()
                    },
                    disabled: editor => {
                        return (
                            editor.app.isOffline() ||
                            !editor.user.is_authenticated
                        )
                    }
                },
                {
                    title: editor =>
                        editor.user.is_authenticated
                            ? gettext("Close")
                            : gettext("Sign up / Log in"),
                    type: "action",
                    //icon: 'times-circle',
                    tooltip: editor =>
                        editor.user.is_authenticated
                            ? gettext(
                                  "Close the document and return to the document overview menu."
                              )
                            : gettext("Sign up for an account or log in."),
                    order: 1,
                    action: editor => {
                        if (editor.user.is_authenticated) {
                            const folderPath = editor.docInfo.path.slice(
                                0,
                                editor.docInfo.path.lastIndexOf("/")
                            )
                            if (
                                !folderPath.length &&
                                editor.app.routes[""].app === "document"
                            ) {
                                editor.app.goTo("/")
                            } else {
                                editor.app.goTo(`/documents${folderPath}/`)
                            }
                        } else {
                            if (
                                editor.app.settings?.REGISTRATION_OPEN ||
                                editor.app.settings?.SOCIALACCOUNT_OPEN
                            ) {
                                window.location.href = "/account/sign-up/"
                            } else {
                                window.location.href = "/"
                            }
                        }
                        return
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("Save revision"),
                    type: "action",
                    //icon: 'save',
                    tooltip: gettext("Save a revision of the document."),
                    order: 2,
                    keys: "Ctrl-s",
                    action: editor => {
                        const dialog = new _dialogs__rspack_import_5.RevisionDialog(editor.docInfo.dir)
                        dialog.init().then(note => {
                            const saver = new _exporter_native__rspack_import_3.SaveRevision(
                                editor.getDoc(),
                                editor.mod.db.imageDB,
                                editor.mod.db.bibDB,
                                note,
                                editor.app
                            )
                            return saver.init()
                        })
                    },
                    disabled: editor =>
                        editor.docInfo.access_rights !== "write" ||
                        editor.app.isOffline() ||
                        !!editor.docInfo.token
                },
                {
                    title: gettext("Create copy"),
                    type: "action",
                    //icon: 'copy',
                    tooltip: gettext("Create a copy of the document."),
                    order: 3,
                    action: editor => {
                        const copier = new _exporter_native__rspack_import_3.SaveCopy(
                            editor.getDoc(),
                            editor.mod.db.bibDB,
                            editor.mod.db.imageDB,
                            editor.user
                        )
                        copier
                            .init()
                            .then(({docInfo}) =>
                                editor.app.goTo(`/document/${docInfo.id}/`)
                            )
                            .catch(() => false)
                    },
                    disabled: editor =>
                        editor.app.isOffline() ||
                        (!!editor.docInfo.token &&
                            !editor.user.is_authenticated)
                },
                {
                    title: gettext("Download"),
                    type: "action",
                    //icon: 'download',
                    tooltip: gettext(
                        "Export the document as a FIDUS file including its template."
                    ),
                    order: 4,
                    action: editor => {
                        new _exporter_native_file__rspack_import_4.ExportFidusFile(
                            editor.getDoc(),
                            editor.mod.db.bibDB,
                            editor.mod.db.imageDB,
                            true,
                            editor.docInfo.token
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("Print/PDF"),
                    type: "action",
                    //icon: 'print',
                    tooltip: gettext(
                        "Either print or create a PDF using your browser print dialog."
                    ),
                    order: 5,
                    keys: "Ctrl-p",
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs"), __webpack_require__.e("vendors-node_modules_pnpm_pretty_2_0_0_node_modules_pretty_index_js"), __webpack_require__.e("vendors-node_modules_pnpm_vivliostyle_print_2_39_1_node_modules_vivliostyle_print_dist_index_es6_js"), __webpack_require__.e("js_modules_exporter_html_index_js"), __webpack_require__.e("js_modules_exporter_print_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/print/index.js")).then(
                            ({PrintExporter}) => {
                                const exporter = new PrintExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated,
                                    editor.mod.documentTemplate.documentStyles
                                )
                                exporter.init()
                            }
                        )
                    }
                },
                {
                    title: gettext("Change password"),
                    type: "action",
                    tooltip: gettext(
                        "Change the password of this encrypted document."
                    ),
                    order: 6,
                    action: async editor => {
                        if (!editor.e2ee?.key) {
                            (0,_common__rspack_import_0.addAlert)(
                                "error",
                                gettext(
                                    "Document key is not available. Please reload the document."
                                )
                            )
                            return
                        }
                        const isPassphraseUser =
                            editor.e2ee?.usesPassphrase &&
                            _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.hasKeysInSession()
                        const suggestedNewPassword = isPassphraseUser
                            ? await _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.generateDocumentPassword()
                            : ""

                        let changeOptions
                        if (isPassphraseUser) {
                            // Passphrase users: current password is known, new password
                            // is auto-generated and shown in plaintext for sharing.
                            changeOptions = {
                                currentPassword:
                                    editor.e2ee.password ||
                                    _e2ee_key_manager__rspack_import_6.E2EEKeyManager.getPasswordFromSession(
                                        editor.docInfo.id
                                    ) ||
                                    "",
                                suggestedNewPassword,
                                hideCurrentPassword: true,
                                showNewPasswordPlaintext: true,
                                infoText: gettext(
                                    "Your personal passphrase will still unlock this document. A new random password is generated automatically. Other passphrase users will receive it automatically; non-passphrase collaborators need it shared with them manually."
                                )
                            }
                        } else {
                            // Non-passphrase users: prefill current password from
                            // sessionStorage if available, but keep it visible for
                            // verification.
                            const sessionPassword =
                                _e2ee_key_manager__rspack_import_6.E2EEKeyManager.getPasswordFromSession(
                                    editor.docInfo.id
                                )
                            changeOptions = {
                                currentPassword: sessionPassword || "",
                                suggestedNewPassword: ""
                            }
                        }

                        (0,_e2ee_password_dialog__rspack_import_8.changePasswordDialog)(
                            async ({currentPassword, newPassword}) => {
                                try {
                                    // Verify current password by resolving to key
                                    const currentSaltBytes = new Uint8Array(
                                        atob(editor.e2ee.encryptionSalt)
                                            .split("")
                                            .map(c => c.charCodeAt(0))
                                    )
                                    const currentKey =
                                        await _e2ee_key_manager__rspack_import_6.E2EEKeyManager.resolvePasswordToKey(
                                            currentPassword,
                                            currentSaltBytes,
                                            editor.e2ee.encryptionIterations
                                        )
                                    // Test the key by encrypting and decrypting a test value
                                    const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))
                                    const testValue = "test"
                                    const encryptedTest =
                                        await E2EEEncryptor.encrypt(
                                            testValue,
                                            currentKey
                                        )
                                    await E2EEEncryptor.decrypt(
                                        encryptedTest,
                                        editor.e2ee.key
                                    )

                                    // Current password verified — generate new salt and key
                                    const newSalt =
                                        _e2ee_key_manager__rspack_import_6.E2EEKeyManager.generateSalt()
                                    const newSaltBase64 = btoa(
                                        String.fromCharCode(...newSalt)
                                    )
                                    const newIterations = 600000
                                    const newKey =
                                        await _e2ee_key_manager__rspack_import_6.E2EEKeyManager.resolvePasswordToKey(
                                            newPassword,
                                            newSalt,
                                            newIterations
                                        )

                                    // Re-encrypt the document with the new key
                                    await editor.e2ee.snapshotManager.reEncryptWithNewKey(
                                        newKey,
                                        newSaltBase64,
                                        newIterations
                                    )

                                    // Update local E2EE state
                                    editor.e2ee.encryptionSalt = newSaltBase64
                                    editor.e2ee.encryptionIterations =
                                        newIterations
                                    editor.e2ee.key = newKey
                                    editor.e2ee.password = newPassword

                                    // Cache password and key in sessionStorage
                                    _e2ee_key_manager__rspack_import_6.E2EEKeyManager.storePasswordInSession(
                                        editor.docInfo.id,
                                        newPassword
                                    )
                                    await _e2ee_key_manager__rspack_import_6.E2EEKeyManager.storeKeyInSession(
                                        editor.docInfo.id,
                                        newKey
                                    )

                                    // If passphrase user, update encrypted password on server
                                    if (isPassphraseUser) {
                                        try {
                                            await _e2ee_passphrase_manager__rspack_import_7.PassphraseManager.saveDocumentPassword(
                                                editor.docInfo.id,
                                                newPassword,
                                                null,
                                                "user",
                                                true
                                            )
                                        } catch (_e) {
                                            console.error(
                                                "Failed to update document password on server:",
                                                _e
                                            )
                                        }
                                    }

                                    if (isPassphraseUser) {
                                        (0,_common__rspack_import_0.addAlert)(
                                            "success",
                                            gettext(
                                                "Document encryption key rotated. Other passphrase users will receive the new key automatically."
                                            )
                                        )
                                    } else {
                                        (0,_common__rspack_import_0.addAlert)(
                                            "success",
                                            gettext(
                                                "Document password changed. Remember to share the new password with your collaborators."
                                            )
                                        )
                                    }
                                } catch (_error) {
                                    (0,_common__rspack_import_0.addAlert)(
                                        "error",
                                        gettext(
                                            "The current password is incorrect."
                                        )
                                    )
                                }
                            },
                            changeOptions
                        )
                    },
                    disabled: editor =>
                        !editor.e2ee?.encrypted ||
                        editor.docInfo.access_rights !== "write"
                }
            ]
        },
        {
            id: "export",
            title: gettext("Export"),
            tooltip: gettext("Export of the document contents"),
            type: "menu",
            order: 1,
            keys: "Alt-e",
            content: [
                {
                    title: gettext("HTML"),
                    type: "action",
                    tooltip: gettext("Export the document to an HTML file."),
                    order: 0,
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs"), __webpack_require__.e("vendors-node_modules_pnpm_pretty_2_0_0_node_modules_pretty_index_js"), __webpack_require__.e("js_modules_exporter_html_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/html/index.js")).then(
                            ({HTMLExporter}) => {
                                const exporter = new HTMLExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated,
                                    editor.mod.documentTemplate.documentStyles
                                )
                                exporter.init()
                            }
                        )
                    }
                },
                {
                    title: gettext("Epub"),
                    type: "action",
                    tooltip: gettext(
                        "Export the document to an Epub electronic reader file."
                    ),
                    order: 1,
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs"), __webpack_require__.e("vendors-node_modules_pnpm_pretty_2_0_0_node_modules_pretty_index_js"), __webpack_require__.e("js_modules_exporter_html_index_js"), __webpack_require__.e("js_modules_exporter_epub_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/epub/index.js")).then(
                            ({EpubExporter}) => {
                                const exporter = new EpubExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated,
                                    editor.mod.documentTemplate.documentStyles
                                )
                                exporter.init()
                            }
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("LaTeX"),
                    type: "action",
                    tooltip: gettext("Export the document to an LaTeX file."),
                    order: 2,
                    action: editor => {
                        __webpack_require__.e(/* import() */ "js_modules_exporter_latex_index_js").then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/latex/index.js")).then(
                            ({LatexExporter}) => {
                                const exporter = new LatexExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.docInfo.updated
                                )
                                exporter.init()
                            }
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("JATS"),
                    type: "action",
                    tooltip: gettext(
                        "Export the document to a Journal Archiving and Interchange Tag Library NISO JATS Version 1.2 file."
                    ),
                    order: 2,
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs"), __webpack_require__.e("vendors-node_modules_pnpm_pretty_2_0_0_node_modules_pretty_index_js"), __webpack_require__.e("js_modules_exporter_jats_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/jats/index.js")).then(
                            ({JATSExporter}) => {
                                const exporter = new JATSExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated,
                                    "article"
                                )
                                exporter.init()
                            }
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("BITS"),
                    type: "action",
                    tooltip: gettext(
                        "Export the document to a Book Interchange Tag Set BITS Version 2.1 file."
                    ),
                    order: 2,
                    action: editor => {
                        Promise.all(/* import() */ [__webpack_require__.e("vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs"), __webpack_require__.e("vendors-node_modules_pnpm_pretty_2_0_0_node_modules_pretty_index_js"), __webpack_require__.e("js_modules_exporter_jats_index_js")]).then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/jats/index.js")).then(
                            ({JATSExporter}) => {
                                const exporter = new JATSExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated,
                                    "book-part-wrapper"
                                )
                                exporter.init()
                            }
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("Pandoc JSON"),
                    type: "action",
                    tooltip: gettext(
                        "Export the document to a Pandoc JSON file."
                    ),
                    order: 3,
                    action: editor => {
                        __webpack_require__.e(/* import() */ "js_modules_exporter_pandoc_index_js").then(__webpack_require__.bind(__webpack_require__, "./js/modules/exporter/pandoc/index.js")).then(
                            ({PandocExporter}) => {
                                const exporter = new PandocExporter(
                                    editor.getDoc({
                                        changes: "acceptAllNoInsertions"
                                    }),
                                    editor.mod.db.bibDB,
                                    editor.mod.db.imageDB,
                                    editor.app.csl,
                                    editor.docInfo.updated
                                )
                                exporter.init()
                            }
                        )
                    },
                    disabled: editor => editor.app.isOffline()
                },
                {
                    title: gettext("Slim FIDUS"),
                    type: "action",
                    tooltip: gettext(
                        "Export the document to a FIDUS file without its template."
                    ),
                    order: 4,
                    action: editor => {
                        new _exporter_native_file__rspack_import_4.ExportFidusFile(
                            editor.getDoc(),
                            editor.mod.db.bibDB,
                            editor.mod.db.imageDB,
                            false
                        )
                    }
                }
            ]
        },
        {
            id: "settings",
            title: gettext("Settings"),
            tooltip: gettext("Configure settings of this document."),
            type: "menu",
            order: 2,
            keys: "Alt-s",
            content: [
                {
                    id: "citation_style",
                    title: gettext("Citation Style"),
                    type: "menu",
                    tooltip: gettext("Choose your preferred citation style."),
                    order: 1,
                    disabled: editor => {
                        return editor.docInfo.access_rights !== "write"
                    },
                    content: []
                },
                {
                    id: "document_style",
                    title: gettext("Document Style"),
                    type: "menu",
                    tooltip: gettext("Choose your preferred document style."),
                    order: 2,
                    disabled: editor => {
                        return (
                            editor.docInfo.access_rights !== "write" ||
                            editor.app.isOffline()
                        )
                    },
                    content: []
                },
                {
                    id: "language",
                    title: gettext("Text Language"),
                    type: "menu",
                    tooltip: gettext("Choose the language of the document."),
                    order: 3,
                    disabled: editor => {
                        return editor.docInfo.access_rights !== "write"
                    },
                    content: [
                        languageItem(
                            "en-US",
                            gettext("English (United States)"),
                            0
                        ),
                        languageItem(
                            "en-GB",
                            gettext("English (United Kingdom)"),
                            1
                        ),
                        languageItem("de-DE", gettext("German (Germany)"), 2),
                        languageItem(
                            "zh-CN",
                            gettext("Chinese (Simplified)"),
                            3
                        ),
                        languageItem("es", gettext("Spanish"), 4),
                        languageItem("fr", gettext("French"), 5),
                        languageItem("ja", gettext("Japanese"), 6),
                        languageItem("it", gettext("Italian"), 7),
                        //languageItem('pl', gettext('Polish'), 8),
                        languageItem(
                            "pt-BR",
                            gettext("Portuguese (Brazil)"),
                            9
                        ),
                        //languageItem('nl', gettext('Dutch'), 10),
                        //languageItem('ru', gettext('Russian'), 11),
                        {
                            type: "separator",
                            order: 12,
                            available: editor => {
                                // There has to be at least one language of the default languages
                                // among the default ones and one that is not among the default ones.
                                return (
                                    !!editor.view.state.doc.attrs.languages.find(
                                        lang =>
                                            [
                                                "en-US",
                                                "en-GB",
                                                "de-DE",
                                                "zh-CN",
                                                "es",
                                                "fr",
                                                "ja",
                                                "it",
                                                "pl",
                                                "pt-BR",
                                                "nl",
                                                "ru"
                                            ].includes(lang)
                                    ) &&
                                    !!editor.view.state.doc.attrs.languages.find(
                                        lang =>
                                            ![
                                                "en-US",
                                                "en-GB",
                                                "de-DE",
                                                "zh-CN",
                                                "es",
                                                "fr",
                                                "ja",
                                                "it",
                                                "pl",
                                                "pt-BR",
                                                "nl",
                                                "ru"
                                            ].includes(lang)
                                    )
                                )
                            }
                        },
                        {
                            title: gettext("Other"),
                            type: "setting",
                            order: 13,
                            action: editor => {
                                const language =
                                        editor.view.state.doc.attrs.language,
                                    dialog = new _dialogs__rspack_import_5.LanguageDialog(
                                        editor,
                                        language
                                    )
                                dialog.init()
                            },
                            selected: editor => {
                                return ![
                                    "en-US",
                                    "en-GB",
                                    "de-DE",
                                    "zh-CN",
                                    "es",
                                    "fr",
                                    "ja",
                                    "it",
                                    "pl",
                                    "pt-BR",
                                    "nl",
                                    "ru"
                                ].includes(editor.view.state.doc.attrs.language)
                            },
                            available: editor =>
                                !!editor.view.state.doc.attrs.languages.find(
                                    lang =>
                                        ![
                                            "en-US",
                                            "en-GB",
                                            "de-DE",
                                            "zh-CN",
                                            "es",
                                            "fr",
                                            "ja",
                                            "it",
                                            "pl",
                                            "pt-BR",
                                            "nl",
                                            "ru"
                                        ].includes(lang)
                                )
                        }
                    ]
                },
                {
                    id: "paper_size",
                    title: gettext("Paper Size"),
                    type: "menu",
                    tooltip: gettext(
                        "Choose a papersize for print and PDF generation."
                    ),
                    order: 4,
                    disabled: editor => {
                        return editor.docInfo.access_rights !== "write"
                    },
                    content: [
                        {
                            title: gettext("DIN A4"),
                            type: "setting",
                            tooltip: gettext(
                                "A4 (DIN A4/ISO 216) which is used in most of the world."
                            ),
                            order: 0,
                            action: editor => {
                                editor.view.dispatch(
                                    editor.view.state.tr
                                        .setDocAttribute("papersize", "A4")
                                        .setMeta("settings", true)
                                )
                            },
                            selected: editor => {
                                return (
                                    editor.view.state.doc.attrs.papersize ===
                                    "A4"
                                )
                            },
                            available: editor => {
                                return editor.view.state.doc.attrs.papersizes.includes(
                                    "A4"
                                )
                            }
                        },
                        {
                            title: gettext("US Letter"),
                            type: "setting",
                            tooltip: gettext(
                                "The format used by the USA and some other American countries."
                            ),
                            order: 1,
                            action: editor => {
                                editor.view.dispatch(
                                    editor.view.state.tr
                                        .setDocAttribute(
                                            "papersize",
                                            "US Letter"
                                        )
                                        .setMeta("settings", true)
                                )
                            },
                            selected: editor => {
                                return (
                                    editor.view.state.doc.attrs.papersize ===
                                    "US Letter"
                                )
                            },
                            available: editor => {
                                return editor.view.state.doc.attrs.papersizes.includes(
                                    "US Letter"
                                )
                            }
                        }
                    ]
                },
                {
                    title: gettext("Copyright Information"),
                    type: "setting",
                    order: 5,
                    action: editor => {
                        const dialog = new _copyright_dialog__rspack_import_1.CopyrightDialog(
                            editor.view.state.doc.attrs.copyright
                        )
                        dialog.init().then(copyright => {
                            if (copyright) {
                                editor.view.dispatch(
                                    editor.view.state.tr
                                        .setDocAttribute("copyright", copyright)
                                        .setMeta("settings", true)
                                )
                            }
                            editor.currentView.focus()
                        })
                    },
                    disabled: editor => editor.docInfo.access_rights !== "write"
                }
            ]
        },
        {
            id: "tools",
            title: gettext("Tools"),
            tooltip: gettext("Select document editing tool."),
            type: "menu",
            order: 3,
            keys: "Alt-t",
            content: [
                {
                    title: gettext("Word counter"),
                    type: "action",
                    tooltip: gettext("See document statistics."),
                    order: 0,
                    action: editor => {
                        const dialog = new _tools__rspack_import_9.WordCountDialog(editor)
                        dialog.init()
                    }
                },
                {
                    title: gettext("Search and replace"),
                    type: "action",
                    tooltip: gettext("Show a search and replace dialog."),
                    order: 1,
                    keys: "Ctrl-h",
                    action: editor => {
                        const dialog = new _tools__rspack_import_9.SearchReplaceDialog(editor)
                        dialog.init()
                    }
                },
                {
                    title: gettext("Keyboard shortcuts"),
                    type: "action",
                    tooltip: gettext(
                        "Show an overview of available keyboard shortcuts."
                    ),
                    order: 2,
                    keys: "Shift-Ctrl-/",
                    action: editor => {
                        const dialog = new _tools__rspack_import_9.KeyBindingsDialog(editor)
                        dialog.init()
                    }
                }
            ]
        },
        {
            title: gettext("Track changes"),
            type: "menu",
            tooltip: gettext("Tracking changes to the document"),
            order: 4,
            keys: "Alt-c",
            disabled: editor => editor.docInfo.access_rights !== "write",
            content: [
                {
                    title: gettext("Record"),
                    type: "setting",
                    tooltip: gettext("Record document changes"),
                    order: 0,
                    disabled: editor => {
                        return editor.docInfo.access_rights !== "write"
                    },
                    action: editor => {
                        const tracked = !editor.view.state.doc.attrs.tracked
                        editor.view.dispatch(
                            editor.view.state.tr
                                .setDocAttribute("tracked", tracked)
                                .setMeta("settings", true)
                        )
                    },
                    selected: editor => {
                        return editor.view.state.doc.attrs.tracked === true
                    }
                },
                {
                    title: gettext("Accept all"),
                    type: "action",
                    tooltip: gettext("Accept all tracked changes."),
                    order: 1,
                    action: editor => {
                        editor.mod.track.acceptAll()
                    }
                },
                {
                    title: gettext("Reject all"),
                    type: "action",
                    tooltip: gettext("Reject all tracked changes."),
                    order: 2,
                    action: editor => {
                        editor.mod.track.rejectAll()
                    }
                }
            ]
        }
    ]
})


}),
"./js/modules/editor/menus/headerbar/view.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  HeaderbarView: function() { return HeaderbarView; }
});
/* import */ var diff_dom__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/diff-dom@5.2.1/node_modules/diff-dom/dist/module.js");
/* import */ var w3c_keyname__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/w3c-keyname@2.2.8/node_modules/w3c-keyname/index.js");
/* import */ var _common__rspack_import_2 = __webpack_require__("./js/modules/common/index.js");




class HeaderbarView {
    constructor(editorView, options) {
        this.editorView = editorView
        this.options = options

        this.editor = this.options.editor
        this.editor.menu.headerView = this

        this.dd = new diff_dom__rspack_import_0.DiffDOM({
            valueDiffing: false
        })
        this.headerEl = document.querySelector("#headerbar").firstElementChild
        this.listeners = {}

        this.removeUnavailable(this.options.editor.menu.headerbarModel)
        this.addMissingIds(this.options.editor.menu.headerbarModel)
        this.bindEvents()
        this.update()
        this.parentChain = []

        this.openMenu = null
        this.cursorMenuItem = null
    }

    removeUnavailable(menu) {
        // Remove those menu items from the menu model that are not available for this document.
        // Used for example for language or page size options that aren't permitted according to the
        // document template.
        menu.content = menu.content.filter(item => {
            if (item.available && !item.available(this.editor)) {
                return false
            } else if (item.type === "menu") {
                this.removeUnavailable(item)
            }
            return true
        })
    }

    addMissingIds(menu) {
        // Add missing ids to menu items that don't have an ID.
        menu.content.forEach(item => {
            if (!item.id) {
                item.id = Math.random().toString(36).substring(2)
            }
            if (item.type === "menu") {
                this.addMissingIds(item)
            }
        })
    }

    bindEvents() {
        this.listeners.onclick = event => this.onclick(event)
        document.body.addEventListener("click", this.listeners.onclick)
        this.listeners.onKeydown = event => this.onKeydown(event)
        document.body.addEventListener("keydown", this.listeners.onKeydown)
        this.listeners.onKeyup = event => this.onKeyup(event)
        document.body.addEventListener("keyup", this.listeners.onKeyup)
        this.listeners.onFocusout = event => this.onFocusout(event)
        document.body.addEventListener("focusout", this.listeners.onFocusout)
    }

    destroy() {
        if (document.activeElement.id === "document-title") {
            this.saveFileName()
        }
        document.body.removeEventListener("click", this.listeners.onclick)
        document.body.removeEventListener("keydown", this.listeners.onKeydown)
        document.body.removeEventListener("keyup", this.listeners.onKeyup)
        document.body.removeEventListener("focusout", this.listeners.onFocusout)
    }

    onclick(event) {
        const target = event.target
        if (target.matches("div#close-document-top a i.fa-times")) {
            // If the user is offline prevent the closing of the document.
            if (this.editor.app.isOffline()) {
                event.preventDefault()
                event.stopPropagation()
                ;(0,_common__rspack_import_2.addAlert)(
                    "info",
                    gettext("You cannot close a document when you're offline.")
                )
            }
        } else if (
            target.matches("#headerbar #header-navigation .fw-pulldown-item")
        ) {
            // A header nav menu item was clicked. Now we just need to find
            // which one and execute the corresponding action.
            const searchPath = []
            let seekItem = target
            while (seekItem.closest("li")) {
                let itemNumber = 0
                seekItem = seekItem.closest("li")
                while (seekItem.previousElementSibling) {
                    itemNumber++
                    seekItem = seekItem.previousElementSibling
                }
                searchPath.push(itemNumber)
                seekItem = seekItem.parentElement
            }

            seekItem = seekItem.closest("div.header-menu")
            let menuNumber = 0
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            const menu = this.editor.menu.headerbarModel.content[menuNumber]

            let menuItem = menu

            while (searchPath.length) {
                menuItem = menuItem.content[searchPath.pop()]
            }
            this.executeMenuItem(menuItem, menu)
        } else if (
            target.matches(
                "#headerbar #header-navigation .header-nav-item:not(.disabled)"
            )
        ) {
            // A menu has been clicked, lets find out which one.
            let menuNumber = 0
            let seekItem = target.parentElement
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            this.editor.menu.headerbarModel.content.forEach(menu => {
                if (menu.open) {
                    menu.open = false
                    this.openMenu = null
                    this.closeAllMenu(menu)
                    this.parentChain = []
                }
            })
            this.editor.menu.headerbarModel.content[menuNumber].open = true
            this.openMenu = this.editor.menu.headerbarModel.content[menuNumber]
            this.parentChain = [this.openMenu]
            this.cursorMenuItem = null
            this.update()
        } else {
            let needUpdate = false
            this.cursorMenuItem = null
            this.editor.menu.headerbarModel.content.forEach(menu => {
                if (menu.open) {
                    needUpdate = true
                    menu.open = false
                    this.closeAllMenu(menu)
                    this.parentChain = []
                }
            })
            if (needUpdate) {
                this.openMenu = null
                this.update()
            }
        }
    }

    executeMenuItem(menuItem, menu) {
        switch (menuItem.type) {
            case "action":
                if (menuItem.disabled?.(this.editor)) {
                    return
                }
                menuItem.action(this.editor)
                menu.open = false
                this.openMenu = null
                this.closeAllMenu(menu)
                this.parentChain = []
                this.cursorMenuItem = null
                this.update()
                break
            case "setting":
                // Similar to 'action' but not closing menu.
                if (menuItem.disabled?.(this.editor)) {
                    return
                }
                menuItem.action(this.editor)
                this.update()
                break
            case "menu": {
                let flagCloseAllMenu = true
                if (!this.parentChain.length) {
                    this.parentChain = [menuItem]
                    if (this.openMenu) {
                        this.openMenu.open = false
                    }
                    this.openMenu = menuItem
                    this.openMenu.open = true
                } else {
                    const isMenuItemInParentChain = this.parentChain[
                        this.parentChain.length - 1
                    ].content.find(menu => menu.id === menuItem.id)
                    if (isMenuItemInParentChain) {
                        //Do not close all open menus
                        this.parentChain.push(menuItem)
                    } else if (!isMenuItemInParentChain) {
                        for (
                            let index = this.parentChain.length - 2;
                            index >= 0;
                            index--
                        ) {
                            if (
                                this.parentChain[index].content.find(
                                    menu => menu.id === menuItem.id
                                )
                            ) {
                                const noOfRemovals =
                                    this.parentChain.length - (index + 1)
                                if (noOfRemovals > 0) {
                                    //not last element
                                    this.parentChain.splice(
                                        index + 1,
                                        noOfRemovals
                                    )
                                }
                                this.closeOtherMenu(
                                    this.parentChain[
                                        this.parentChain.length - 1
                                    ],
                                    menuItem
                                )
                                this.parentChain.push(menuItem)
                                flagCloseAllMenu = false
                                break
                            }
                        }
                    }
                    if (flagCloseAllMenu && !isMenuItemInParentChain) {
                        this.closeAllMenu(menu)
                        this.parentChain = [menuItem]
                    }
                }
                menuItem.open = true
                this.update()
                break
            }
            default:
                break
        }
    }

    closeAllMenu(menu) {
        menu.content.forEach(menuItem => {
            if (menuItem.type === "menu" && menuItem.open) {
                menuItem.open = false
                this.closeAllMenu(menuItem)
            }
        })
    }

    closeOtherMenu(menu, currentMenuItem) {
        menu.content.forEach(menuItem => {
            if (menuItem.type === "menu" && menuItem.open) {
                if (
                    !this.parentChain.includes(menuItem) &&
                    currentMenuItem != menuItem
                ) {
                    menuItem.open = false
                }
                this.closeOtherMenu(menuItem, currentMenuItem)
            }
        })
    }

    onKeydown(event) {
        if ((0,_common__rspack_import_2.findTarget)(event, "h1#document-title")) {
            return
        }
        let name = (0,w3c_keyname__rspack_import_1.keyName)(event)
        if (event.altKey) {
            name = "Alt-" + name
        }
        if (event.ctrlKey) {
            name = "Ctrl-" + name
        }
        if (event.metaKey) {
            name = "Meta-" + name
        }
        if (event.shiftKey) {
            name = "Shift-" + name
        }

        if (this.openMenu) {
            if (
                ["ArrowDown", "ArrowUp", "ArrowLeft", "ArrowRight"].includes(
                    name
                )
            ) {
                event.preventDefault()
                event.stopPropagation()
                this.changeCursorMenuItem(name)
                return
            } else if (["Enter", " "].includes(name) && this.cursorMenuItem) {
                event.preventDefault()
                event.stopPropagation()
                const menuItem = this.cursorMenuItem
                if (menuItem.type === "menu") {
                    this.cursorMenuItem = menuItem.content[0]
                }
                this.executeMenuItem(
                    menuItem,
                    this.parentChain[this.parentChain.length - 1]
                )
                return
            } else if (name === "Escape") {
                event.preventDefault()
                event.stopPropagation()
                this.openMenu.open = false
                this.cursorMenuItem = null
                this.openMenu = null
                this.parentChain = []
                this.update()
                return
            }
        }
        this.checkKeys(event, this.editor.menu.headerbarModel, name)
    }

    onKeyup(event) {
        if (!(0,_common__rspack_import_2.findTarget)(event, "h1#document-title")) {
            return
        }
        const docTitleEl = document.body.querySelector("h1#document-title")
        if (
            !docTitleEl.childNodes.length ||
            (docTitleEl.childNodes.length === 1 &&
                docTitleEl.firstChild.nodeType === 3)
        ) {
            return
        }
        // Special key was pressed, we reset to text only and blur
        docTitleEl.innerHTML = docTitleEl.innerText
            .trim()
            .replace(/\r?\n|\r/g, "")
        docTitleEl.blur()
    }

    changeCursorMenuItem(name) {
        if (!this.cursorMenuItem) {
            this.cursorMenuItem = this.parentChain[0].content[0]
        } else {
            if (["ArrowDown", "ArrowUp"].includes(name)) {
                let index = this.parentChain[
                    this.parentChain.length - 1
                ].content.indexOf(this.cursorMenuItem)
                if (name === "ArrowDown") {
                    if (
                        index <
                        this.parentChain[this.parentChain.length - 1].content
                            .length -
                            1
                    ) {
                        index++
                    } else {
                        index = 0
                    }
                } else {
                    if (index > 0) {
                        index--
                    } else {
                        index =
                            this.parentChain[this.parentChain.length - 1]
                                .content.length - 1
                    }
                }
                this.cursorMenuItem =
                    this.parentChain[this.parentChain.length - 1].content[index]
            } else if (name === "ArrowLeft") {
                if (this.parentChain.length > 1) {
                    this.cursorMenuItem =
                        this.parentChain[this.parentChain.length - 1]
                    this.parentChain.pop()
                    this.closeAllMenu(
                        this.parentChain[this.parentChain.length - 1]
                    )
                } else {
                    const currentMenuIndex =
                        this.editor.menu.headerbarModel.content.findIndex(
                            menu => menu.open
                        )
                    const newMenuIndex = currentMenuIndex
                        ? currentMenuIndex - 1
                        : this.editor.menu.headerbarModel.content.length - 1
                    this.openMenu.open = false
                    this.openMenu =
                        this.editor.menu.headerbarModel.content[newMenuIndex]
                    this.openMenu.open = true
                    this.cursorMenuItem = this.openMenu.content[0]
                    this.parentChain = [this.openMenu]
                }
            } else if (name === "ArrowRight") {
                if (this.cursorMenuItem.type === "menu") {
                    const menuItem = this.cursorMenuItem
                    this.cursorMenuItem = menuItem.content[0]
                    this.executeMenuItem(
                        menuItem,
                        this.parentChain[this.parentChain.length - 1]
                    )
                } else {
                    const currentMenuIndex =
                        this.editor.menu.headerbarModel.content.findIndex(
                            menu => menu.open
                        )
                    const newMenuIndex =
                        currentMenuIndex ===
                        this.editor.menu.headerbarModel.content.length - 1
                            ? 0
                            : currentMenuIndex + 1
                    this.openMenu.open = false
                    this.openMenu =
                        this.editor.menu.headerbarModel.content[newMenuIndex]
                    this.openMenu.open = true
                    this.cursorMenuItem = this.openMenu.content[0]
                    this.parentChain = [this.openMenu]
                }
            }
        }
        this.update()
    }

    getAccessKeyHTML(title, accessKey) {
        if (!accessKey) {
            return (0,_common__rspack_import_2.escapeText)(title)
        }
        const index = title.toLowerCase().indexOf(accessKey.toLowerCase())
        if (index === -1) {
            return (0,_common__rspack_import_2.escapeText)(title)
        }
        return `${(0,_common__rspack_import_2.escapeText)(title.substring(0, index))}<span class="access-key">${(0,_common__rspack_import_2.escapeText)(title.charAt(index))}</span>${(0,_common__rspack_import_2.escapeText)(title.substring(index + 1))}`
    }

    saveFileName() {
        if (this.editor.app.isOffline()) {
            // We are offline. Just reset.
            return this.update()
        }
        const docTitleEl = document.body.querySelector("h1#document-title")
        const path = (0,_common__rspack_import_2.cleanPath)(this.getTitle(), docTitleEl.innerText.trim())
        this.editor.docInfo.path = path
        if (this.editor.ws) {
            this.editor.ws.send(() => {
                return {
                    type: "path_change",
                    path
                }
            })
        }
        this.update()
    }

    onFocusout(event) {
        if (!(0,_common__rspack_import_2.findTarget)(event, "h1#document-title")) {
            return
        }
        this.saveFileName()
    }

    checkKeys(event, menu, nameKey) {
        menu.content.forEach(menuItem => {
            if (menuItem.keys === nameKey) {
                event.preventDefault()

                // Now execute the menu item
                this.executeMenuItem(menuItem, menu)
            } else if (menuItem.content) {
                this.checkKeys(event, menuItem, nameKey)
            }
        })
    }

    update() {
        if (
            document.activeElement &&
            document.activeElement.matches("h1#document-title")
        ) {
            return
        }
        const diff = this.dd.diff(this.headerEl, this.getHeaderHTML())
        this.dd.apply(this.headerEl, diff)
        if (this.editor.menu.headerbarModel.open) {
            document.body.classList.remove("header-closed")
        } else {
            document.body.classList.add("header-closed")
        }
    }

    getPathText() {
        let text = this.editor.docInfo.path
        if (text.length && !text.endsWith("/")) {
            return text
        } else if (text === "/") {
            text = ""
        }
        text += this.getTitle().replace(/\//g, "") || gettext("Untitled")
        return text
    }

    getTitle() {
        const doc = this.editor.view.state.doc
        let title = ""
        doc.firstChild.forEach(child => {
            if (!child.marks.find(mark => mark.type.name === "deletion")) {
                title += (0,_common__rspack_import_2.escapeText)(child.textContent)
            }
        })
        return title.trim()
    }

    getHeaderHTML() {
        if (!this.editor.menu.headerbarModel.open) {
            // header is closed
            return "<div></div>"
        }
        let exitUrl
        if (this.editor.docInfo.token) {
            // Guest user — send to sign-up if open, otherwise to login
            exitUrl =
                this.editor.app.settings?.REGISTRATION_OPEN ||
                this.editor.app.settings?.SOCIALACCOUNT_OPEN
                    ? "/account/sign-up/"
                    : "/"
        } else {
            const folderPath = this.editor.docInfo.path.slice(
                0,
                this.editor.docInfo.path.lastIndexOf("/")
            )
            exitUrl =
                !folderPath.length &&
                this.editor.app.routes[""].app === "document"
                    ? "/"
                    : `/documents${encodeURI(folderPath)}/`
        }
        return `<div>
            <div id="close-document-top" title="${this.editor.docInfo.token ? gettext("Sign up or log in") : gettext("Close the document and return to the document overview menu.")}">
                <a href="${exitUrl}" aria-label="${this.editor.docInfo.token ? gettext("Sign up or log in") : gettext("Close document")}" title="${this.editor.docInfo.token ? gettext("Sign up or log in") : gettext("Close the document and return to the document overview menu.")}">
                    <i class="fa fa-times"></i>
                </a>
            </div>
            <div id="document-top">
                <h1 id="document-title"${this.editor.app.isOffline() || !this.editor.pathEditable ? "" : ' contenteditable="true"'}>${(0,_common__rspack_import_2.escapeText)(this.getPathText())}</h1>
                <nav id="header-navigation">
                    ${this.getHeaderNavHTML()}
                </nav>
                ${this.getParticipantListHTML()}
            </div>
        </div>`
    }

    getParticipantListHTML() {
        const participants = this.editor.mod.collab.participants
        if (participants.length > 1) {
            return `
                <div id="connected-collaborators">
                    ${participants
                        .map(participant => (0,_common__rspack_import_2.avatarTemplate)({user: participant}))
                        .join("")}
                </div>
            `
        } else {
            return ""
        }
    }

    getHeaderNavHTML() {
        return this.editor.menu.headerbarModel.content
            .map(
                menu => `
                <div class="header-menu">
                    <span class="header-nav-item${menu.disabled && menu.disabled(this.editor) ? " disabled" : ""}"
                          title="${typeof menu.tooltip === "function" ? menu.tooltip(this.editor) : menu.tooltip}"
                          aria-label="${typeof menu.tooltip === "function" ? menu.tooltip(this.editor) : menu.tooltip}"
                          role="menuitem"
                          aria-haspopup="true">
                        ${this.getAccessKeyHTML(menu.title, menu.keys?.slice(-1))}
                    </span>
                    ${menu.open ? this.getMenuHTML(menu) : ""}
                </div>
            `
            )
            .join("")
    }

    getMenuHTML(menu) {
        const title =
            typeof menu.title === "function"
                ? menu.title(this.editor)
                : menu.title
        return `<div class="fw-pulldown fw-left fw-open"
                     role="menu"
                     aria-label="${title}"
                     title="${title}">
            <ul>
                ${menu.content
                    .map(
                        menuItem => `
                        <li role="none">
                            ${this.getMenuItemHTML(menuItem)}
                        </li>`
                    )
                    .join("")}
            </ul>
        </div>`
    }

    getMenuItemHTML(menuItem) {
        let returnValue
        switch (menuItem.type) {
            case "action":
            case "setting":
                returnValue = this.getActionMenuItemHTML(menuItem)
                break
            case "menu":
                returnValue = this.getMenuMenuItemHTML(menuItem)
                break
            case "separator":
                returnValue = "<hr>"
                break
            default:
                break
        }
        return returnValue
    }

    getActionMenuItemHTML(menuItem) {
        return `<span class="fw-pulldown-item${
            menuItem.selected && menuItem.selected(this.editor)
                ? " selected"
                : ""
        }${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}${
            menuItem === this.cursorMenuItem ? " cursor" : ""
        }"
        role="menuitem"
        ${menuItem.disabled && menuItem.disabled(this.editor) ? 'aria-disabled="true"' : ""}
        ${menuItem.selected && menuItem.selected(this.editor) ? 'aria-checked="true"' : ""}
        ${menuItem.tooltip ? (typeof menuItem.tooltip === "function" ? `title="${menuItem.tooltip(this.editor)}" aria-label="${menuItem.tooltip(this.editor)}"` : `title="${menuItem.tooltip}" aria-label="${menuItem.tooltip}"`) : ""}>
            ${menuItem.icon ? `<i class="fa fa-${menuItem.icon}" aria-hidden="true"></i>` : ""}
            ${typeof menuItem.title === "function" ? menuItem.title(this.editor) : menuItem.title}
        </span>`
    }

    getMenuMenuItemHTML(menuItem) {
        return `<span class="fw-pulldown-item${
            menuItem.selected && menuItem.selected(this.editor)
                ? " selected"
                : ""
        }${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}${
            menuItem === this.cursorMenuItem ? " cursor" : ""
        }" ${menuItem.tooltip ? `title="${menuItem.tooltip}" aria-label="${menuItem.tooltip}"` : ""}>
            ${menuItem.icon ? `<i class="fa fa-${menuItem.icon}"></i>` : ""}
            ${typeof menuItem.title === "function" ? menuItem.title(this.editor) : menuItem.title}
            <span class="fw-icon-right"><i class="fa fa-caret-right"></i></span>
        </span>
        ${menuItem.open ? this.getMenuHTML(menuItem) : ""}`
    }
}


}),
"./js/modules/editor/menus/image/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  imageMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.imageMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/image/model.js");



}),
"./js/modules/editor/menus/image/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  imageMenuModel: function() { return imageMenuModel; }
});
/* import */ var _copyright_dialog__rspack_import_0 = __webpack_require__("./js/modules/copyright_dialog/index.js");


const imageMenuModel = () => ({
    content: [
        {
            title: gettext("Set Copyright"),
            type: "action",
            tooltip: gettext("Specify copyright information"),
            order: 0,
            action: figureDialog => {
                const dialog = new _copyright_dialog__rspack_import_0.CopyrightDialog(figureDialog.copyright)
                dialog.init().then(copyright => {
                    if (copyright) {
                        figureDialog.copyright = copyright
                    }
                })
            }
        },
        {
            title: gettext("Remove image"),
            type: "action",
            tooltip: gettext("Remove the image that is previewed"),
            order: 1,
            action: figureDialog => {
                figureDialog.imgId = false
                figureDialog.imgDb = false
                figureDialog.copyright = false
                figureDialog.layoutMathEditor()
            }
        }
    ]
})


}),
"./js/modules/editor/menus/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  HeaderbarView: function() { return /* reexport safe */ _headerbar__rspack_import_0.HeaderbarView; },
  SelectionMenuView: function() { return /* reexport safe */ _selection__rspack_import_2.SelectionMenuView; },
  ToolbarView: function() { return /* reexport safe */ _toolbar__rspack_import_1.ToolbarView; },
  codeBlockMenuModel: function() { return /* reexport safe */ _code_block__rspack_import_8.codeBlockMenuModel; },
  figureMenuModel: function() { return /* reexport safe */ _figure__rspack_import_7.figureMenuModel; },
  figureWidthMenuModel: function() { return /* reexport safe */ _figure__rspack_import_7.figureWidthMenuModel; },
  headerbarModel: function() { return /* reexport safe */ _headerbar__rspack_import_0.headerbarModel; },
  imageMenuModel: function() { return /* reexport safe */ _image__rspack_import_5.imageMenuModel; },
  navigatorFilterModel: function() { return /* reexport safe */ _navigator__rspack_import_6.navigatorFilterModel; },
  orderedListMenuModel: function() { return /* reexport safe */ _ordered_list__rspack_import_4.orderedListMenuModel; },
  selectionMenuModel: function() { return /* reexport safe */ _selection__rspack_import_2.selectionMenuModel; },
  tableMenuModel: function() { return /* reexport safe */ _table__rspack_import_3.tableMenuModel; },
  toolbarModel: function() { return /* reexport safe */ _toolbar__rspack_import_1.toolbarModel; }
});
/* import */ var _headerbar__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/headerbar/index.js");
/* import */ var _toolbar__rspack_import_1 = __webpack_require__("./js/modules/editor/menus/toolbar/index.js");
/* import */ var _selection__rspack_import_2 = __webpack_require__("./js/modules/editor/menus/selection/index.js");
/* import */ var _table__rspack_import_3 = __webpack_require__("./js/modules/editor/menus/table/index.js");
/* import */ var _ordered_list__rspack_import_4 = __webpack_require__("./js/modules/editor/menus/ordered_list/index.js");
/* import */ var _image__rspack_import_5 = __webpack_require__("./js/modules/editor/menus/image/index.js");
/* import */ var _navigator__rspack_import_6 = __webpack_require__("./js/modules/editor/menus/navigator/index.js");
/* import */ var _figure__rspack_import_7 = __webpack_require__("./js/modules/editor/menus/figure/index.js");
/* import */ var _code_block__rspack_import_8 = __webpack_require__("./js/modules/editor/menus/code_block/index.js");











}),
"./js/modules/editor/menus/navigator/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  navigatorFilterModel: function() { return /* reexport safe */ _model__rspack_import_0.navigatorFilterModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/navigator/model.js");



}),
"./js/modules/editor/menus/navigator/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  navigatorFilterModel: function() { return navigatorFilterModel; }
});
const navigatorFilterModel = () => ({
    content: [
        {
            title: gettext("Heading 1"),
            level: 1
        },
        {
            title: gettext("Heading 2"),
            level: 2
        },
        {
            title: gettext("Heading 3"),
            level: 3
        },
        {
            title: gettext("Heading 4"),
            level: 4
        },
        {
            title: gettext("Heading 5"),
            level: 5
        },
        {
            title: gettext("Heading 6"),
            level: 6
        }
    ]
})


}),
"./js/modules/editor/menus/ordered_list/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  orderedListMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.orderedListMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/ordered_list/model.js");



}),
"./js/modules/editor/menus/ordered_list/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  orderedListMenuModel: function() { return orderedListMenuModel; }
});
/* import */ var _dialogs__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/index.js");


const orderedListMenuModel = () => ({
    content: [
        {
            title: gettext("Set list start number"),
            type: "action",
            tooltip: gettext(
                "Specify the number from which this list is to start counting"
            ),
            order: 0,
            action: editor => {
                const dialog = new _dialogs__rspack_import_0.OrderedListStartDialog(editor)
                dialog.init()
                return false
            }
        }
    ]
})


}),
"./js/modules/editor/menus/selection/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SelectionMenuView: function() { return /* reexport safe */ _view__rspack_import_1.SelectionMenuView; },
  selectionMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.selectionMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/selection/model.js");
/* import */ var _view__rspack_import_1 = __webpack_require__("./js/modules/editor/menus/selection/view.js");




}),
"./js/modules/editor/menus/selection/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  selectionMenuModel: function() { return selectionMenuModel; }
});
/* import */ var prosemirror_commands__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _schema_common__rspack_import_1 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _state_plugins__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _track__rspack_import_3 = __webpack_require__("./js/modules/editor/track/index.js");







const tracksInSelection = view => {
    // Check whether track marks are present within the range of selection
    let tracks = false
    const from = view.state.selection.from,
        to = view.state.selection.to

    view.state.doc.nodesBetween(from, to, (node, pos) => {
        if (pos < from && !node.isInline) {
            return true
        } else if (tracks) {
            return false
        } else if (node.attrs.track?.length) {
            tracks = true
        } else if (
            node.marks?.find(mark => {
                if (
                    ["deletion", "format_change"].includes(mark.type.name) ||
                    (mark.type.name === "insertion" && !mark.attrs.approved)
                ) {
                    return true
                } else {
                    return false
                }
            })
        ) {
            tracks = true
        }
    })
    return tracks
}

const selectionMenuModel = () => ({
    content: [
        {
            type: "button",
            title: gettext("Comment"),
            icon: "comment",
            action: editor => {
                editor.mod.comments.interactions.createNewComment()
                return false
            },
            hidden: editor =>
                editor.currentView.state.selection.$anchor.depth < 1,
            selected: editor =>
                !!editor.currentView.state.selection.$head
                    .marks()
                    .some(mark => mark.type.name === "comment"),
            disabled: editor => {
                if (editor.currentView === editor.view) {
                    //  main editor
                    return (0,_state_plugins__rspack_import_2.checkProtectedInSelection)(editor.view.state)
                } else {
                    // footnote editor
                    return false
                }
            },
            order: 1
        },
        {
            type: "button",
            title: gettext("Anchor"),
            icon: "anchor",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["anchor"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark, {id: (0,_schema_common__rspack_import_1.randomAnchorId)()})
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            disabled: editor => {
                if (editor.currentView === editor.view) {
                    //  main editor
                    return (
                        (0,_state_plugins__rspack_import_2.checkProtectedInSelection)(editor.view.state) ||
                        ___rspack_import_0.COMMENT_ONLY_ROLES.includes(
                            editor.docInfo.access_rights
                        )
                    )
                } else {
                    // footnote editor
                    return ___rspack_import_0.COMMENT_ONLY_ROLES.includes(
                        editor.docInfo.access_rights
                    )
                }
            },
            hidden: editor =>
                editor.currentView.state.selection.$anchor.depth < 1,
            selected: editor =>
                !!editor.currentView.state.selection.$head
                    .marks()
                    .some(mark => mark.type.name === "anchor"),
            order: 2
        },
        {
            type: "button",
            title: gettext("Accept all in selection"),
            icon: "check-double",
            action: editor =>
                (0,_track__rspack_import_3.acceptAll)(
                    editor.currentView,
                    editor.currentView.state.selection.from,
                    editor.currentView.state.selection.to
                ),
            disabled: editor => editor.docInfo.access_rights !== "write",
            hidden: editor =>
                editor.currentView.state.selection.$anchor.depth < 1 ||
                !tracksInSelection(editor.currentView),
            order: 3
        },
        {
            type: "button",
            title: gettext("Reject all in selection"),
            icon: "trash",
            action: editor =>
                (0,_track__rspack_import_3.rejectAll)(
                    editor.currentView,
                    editor.currentView.state.selection.from,
                    editor.currentView.state.selection.to
                ),
            disabled: editor => editor.docInfo.access_rights !== "write",
            hidden: editor =>
                editor.currentView.state.selection.$anchor.depth < 1 ||
                !tracksInSelection(editor.currentView),
            order: 4
        }
    ]
})


}),
"./js/modules/editor/menus/selection/view.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SelectionMenuView: function() { return SelectionMenuView; }
});
/* import */ var diff_dom__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/diff-dom@5.2.1/node_modules/diff-dom/dist/module.js");
/* import */ var ___rspack_import_1 = __webpack_require__("./js/modules/editor/index.js");




class SelectionMenuView {
    constructor(editorView, options) {
        this.editorView = editorView
        this.options = options

        this.editor = this.options.editor
        if (!this.editor.menu.selectionMenuViews) {
            this.editor.menu.selectionMenuViews = []
        }
        this.editor.menu.selectionMenuViews.push(this)

        this.dd = new diff_dom__rspack_import_0.DiffDOM({
            valueDiffing: false
        })
        this.openedMenu = false
        this.listeners = {}

        this.removeUnavailable(this.editor.menu.selectionMenuModel)

        this.bindEvents()
        this.update()
    }

    removeUnavailable(menu) {
        // Remove those menu items from the menu model that are not available for this document.
        // Used for example for mark or element buttons that aren't permitted according to the
        // document template.
        menu.content = menu.content.filter(item => {
            if (item.available && !item.available(this.editor)) {
                return false
            } else if (item.type === "menu") {
                this.removeUnavailable(item)
            }
            return true
        })
    }

    bindEvents() {
        this.listeners.onclick = event => this.onclick(event)
        document.body.addEventListener("click", this.listeners.onclick)
    }

    destroy() {
        document.body.removeEventListener("click", this.listeners.onclick)
        this.editor.menu.selectionMenuViews =
            this.editor.menu.selectionMenuViews.filter(view => view !== this)
    }

    onclick(event) {
        if (this.editorView !== this.editor.currentView) {
            // the other editor must be active
            return
        }
        const target = event.target
        if (
            target.matches(
                ".editor-selection-menu > div:not(.disabled), .editor-selection-menu > div:not(.disabled) *"
            )
        ) {
            // A menu item has been clicked, lets find out which one.
            let menuNumber = 0
            let seekItem = target.closest("div.ui-buttonset")
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            const menuItem =
                this.editor.menu.selectionMenuModel.content[menuNumber]
            // execute an associated action.
            if (menuItem.action) {
                event.preventDefault()
                const focus = menuItem.action(this.editor)
                this.update()
                if (focus !== false) {
                    this.editor.currentView.focus()
                }
            }
        } else if (
            this.openedMenu !== false ||
            this.editor.menu.selectionMenuModel.openMore
        ) {
            if (this.openedMenu !== false) {
                this.editor.menu.selectionMenuModel.content[
                    this.openedMenu
                ].open = false
            }
            this.editor.menu.selectionMenuModel.openMore = false
            this.openedMenu = false
            this.update()
        }
    }

    update() {
        if (this.editorView !== this.editor.currentView) {
            // the other editor must be active
            return
        }
        const selectionMenuEl =
            document.querySelector("#selection-menu").firstElementChild
        const diff = this.dd.diff(selectionMenuEl, this.getSelectionMenuHTML())
        this.dd.apply(selectionMenuEl, diff)
    }

    getSelectionMenuHTML() {
        if (
            ___rspack_import_1.READ_ONLY_ROLES.includes(this.editor.docInfo.access_rights) ||
            this.editorView.state.selection.empty ||
            this.editor.mod.comments.store.commentDuringCreation ||
            this.editor.mod.comments.interactions.isCurrentlyEditing()
        ) {
            return "<div></div>"
        }
        const selectionMenuTop = document
                .querySelector("#selection-menu")
                .getBoundingClientRect().top,
            offset =
                this.editorView.coordsAtPos(
                    this.editorView.state.selection.from
                ).top - selectionMenuTop
        return `<div style="margin-top: ${offset}px;">
            <div class="editor-selection-menu">
                ${this.editor.menu.selectionMenuModel.content
                    .map(
                        (menuItem, index) =>
                            `<div class="ui-buttonset${(menuItem.hidden && menuItem.hidden(this.editor)) || (menuItem.disabled && menuItem.disabled(this.editor)) ? " disabled" : ""}">
                        ${this.getSelectionMenuItemHTML(menuItem, index)}
                    </div>`
                    )
                    .join("")}
            </div>
        </div>`
    }

    getSelectionMenuItemHTML(menuItem, _index) {
        if (menuItem.hidden?.(this.editor)) {
            return ""
        } else {
            return this.getButtonHTML(menuItem)
        }
    }

    getButtonHTML(menuItem) {
        return `
        <button aria-label="${menuItem.title}" class="fw-button fw-light fw-large fw-square edit-button${menuItem.selected && menuItem.selected(this.editor) ? " ui-state-active" : ""}${menuItem.class ? ` ${menuItem.class(this.editor)}` : ""}${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}" title="${menuItem.title}" >
            <span class="ui-button-text">
                <i class="fa fa-${typeof menuItem.icon === "function" ? menuItem.icon(this.editor) : menuItem.icon}"></i>
            </span>
        </button>`
    }
}


}),
"./js/modules/editor/menus/table/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  tableMenuModel: function() { return /* reexport safe */ _model__rspack_import_0.tableMenuModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/table/model.js");



}),
"./js/modules/editor/menus/table/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  deleteTable: function() { return deleteTable; },
  tableMenuModel: function() { return tableMenuModel; }
});
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var _dialogs__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/index.js");



// from https://github.com/ProseMirror/prosemirror-tables/blob/master/src/util.js
const findTable = state => {
    const $head = state.selection.$head
    for (let d = $head.depth; d > 0; d--) {
        if ($head.node(d).type.name == "table") {
            return $head.node(d)
        }
    }
    return false
}

// Adjusted from https://github.com/ProseMirror/prosemirror-tables/blob/master/src/commands.js
function deleteTable(state, dispatch) {
    const $pos = state.selection.$anchor
    for (let d = $pos.depth; d > 0; d--) {
        const node = $pos.node(d)
        if (node.type.name == "table") {
            if (dispatch) {
                dispatch(
                    state.tr
                        .delete($pos.before(d), $pos.after(d))
                        .scrollIntoView()
                )
            }
            return true
        }
    }
    return false
}

const tableAddedFromTemplate = state => {
    const $head = state.selection.$head
    for (let d = $head.depth; d > 0; d--) {
        if ($head.node(d).type.name == "table") {
            if ($head.node(d - 1).type.name === "table_part") {
                return true
            } else {
                return false
            }
        }
    }
    return true
}

const tableAddedByUser = (table, userId) =>
    table.attrs.track.find(
        track => track.type === "insertion" && track.user === userId
    )
        ? true
        : false
const tableMenuModel = () => ({
    content: [
        {
            title: editor =>
                `${gettext("Add row above")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Add a row above the current row"),
            order: 0,
            action: editor => {
                ;(0,prosemirror_tables__rspack_import_0.addRowBefore)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Add row below")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Add a row below the current row"),
            order: 1,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.addRowAfter)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Add column left")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Add a column to the left of the current column"),
            order: 2,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.addColumnBefore)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Add column right")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Add a column to the right of the current column"),
            order: 3,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.addColumnAfter)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            type: "separator",
            order: 4
        },
        {
            title: editor =>
                `${gettext("Delete row")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Delete current row"),
            order: 5,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.deleteRow)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Delete column")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Delete current column"),
            order: 6,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.deleteColumn)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            type: "separator",
            order: 7
        },
        {
            title: editor =>
                `${gettext("Merge cells")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Merge selected cells"),
            order: 8,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.mergeCells)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    editor.currentView.state.selection.jsonID !== "cell" ||
                    editor.currentView.state.selection.$headCell.pos ===
                        editor.currentView.state.selection.$anchorCell.pos ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Split cells")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Split selected cell"),
            order: 9,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.splitCell)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr.setMeta("untracked", true))
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    editor.currentView.state.selection.jsonID !== "cell" ||
                    editor.currentView.state.selection.$headCell.pos !==
                        editor.currentView.state.selection.$anchorCell.pos ||
                    (editor.currentView.state.selection.$anchorCell.nodeAfter
                        .attrs.rowspan === 1 &&
                        editor.currentView.state.selection.$anchorCell.nodeAfter
                            .attrs.colspan === 1) ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            type: "separator",
            order: 10
        },
        {
            title: editor =>
                `${gettext("Toggle header row")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext("Toggle header-status of currently selected row"),
            order: 11,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.toggleHeaderRow)(
                    editor.currentView.state,
                    editor.currentView.dispatch
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Toggle header column")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext(
                "Toggle header-status of currently selected column"
            ),
            order: 12,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.toggleHeaderColumn)(
                    editor.currentView.state,
                    editor.currentView.dispatch
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            title: editor =>
                `${gettext("Toggle header cell")}${editor.view.state.doc.attrs.tracked ? ` (${gettext("Not tracked")})` : ""}`,
            type: "action",
            tooltip: gettext(
                "Toggle header-status of currently selected cells"
            ),
            order: 13,
            action: editor => {
                (0,prosemirror_tables__rspack_import_0.toggleHeaderCell)(
                    editor.currentView.state,
                    editor.currentView.dispatch
                )
            },
            disabled: editor => {
                const table = findTable(editor.currentView.state)
                if (
                    !table ||
                    (["write-tracked", "review-tracked"].includes(
                        editor.docInfo.access_rights
                    ) &&
                        !tableAddedByUser(table, editor.user.id))
                ) {
                    return true
                } else {
                    return false
                }
            }
        },
        {
            type: "separator",
            order: 14
        },
        {
            title: `${gettext("Configure")} ...`,
            type: "action",
            tooltip: gettext("Configure the table."),
            order: 15,
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.TableConfigurationDialog(editor)
                dialog.init()
                return false
            },
            disabled: editor => !findTable(editor.currentView.state)
        },
        {
            title: gettext("Delete table"),
            type: "action",
            icon: "trash-alt",
            tooltip: gettext("Delete currently selected table"),
            order: 16,
            action: editor => {
                deleteTable(
                    editor.currentView.state,
                    editor.currentView.dispatch
                )
            },
            disabled: editor => tableAddedFromTemplate(editor.currentView.state)
        }
    ]
})


}),
"./js/modules/editor/menus/toolbar/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ToolbarView: function() { return /* reexport safe */ _view__rspack_import_1.ToolbarView; },
  toolbarModel: function() { return /* reexport safe */ _model__rspack_import_0.toolbarModel; }
});
/* import */ var _model__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/toolbar/model.js");
/* import */ var _view__rspack_import_1 = __webpack_require__("./js/modules/editor/menus/toolbar/view.js");




}),
"./js/modules/editor/menus/toolbar/model.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  elementDisabled: function() { return elementDisabled; },
  toolbarModel: function() { return toolbarModel; }
});
/* import */ var prosemirror_commands__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-commands@1.7.0/node_modules/prosemirror-commands/dist/index.js");
/* import */ var prosemirror_history__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_schema_list__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-list@1.5.1/node_modules/prosemirror-schema-list/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _dialogs__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/index.js");
/* import */ var _keymap__rspack_import_2 = __webpack_require__("./js/modules/editor/keymap.js");
/* import */ var _state_plugins__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/index.js");









const BLOCK_LABELS = {
    paragraph: gettext("Normal Text"),
    heading1: gettext("1st Heading"),
    heading2: gettext("2nd Heading"),
    heading3: gettext("3rd Heading"),
    heading4: gettext("4th Heading"),
    heading5: gettext("5th Heading"),
    heading6: gettext("6th Heading"),
    code_block: gettext("Code"),
    figure: gettext("Figure")
}

// from https://github.com/ProseMirror/prosemirror-tables/blob/master/src/util.js
const findTable = state => {
    const $head = state.selection.$head
    for (let d = $head.depth; d > 0; d--) {
        if ($head.node(d).type.spec.tableRole == "table") {
            return $head.node(d)
        }
    }
    return false
}

function elementAvailable(editor, elementName) {
    let elementInDocParts = false
    editor.view.state.doc.forEach(docPart => {
        if (
            docPart.attrs.elements &&
            docPart.attrs.elements.includes(elementName)
        ) {
            elementInDocParts = true
        }
    })
    return (
        editor.view.state.doc.attrs.footnote_elements.includes(elementName) ||
        elementInDocParts
    )
}

function elementDisabled(editor, elementName) {
    if (editor.currentView === editor.view) {
        // main editor
        const anchorDocPart =
                editor.currentView.state.selection.$anchor.node(1),
            headDocPart = editor.currentView.state.selection.$head.node(1)

        return (
            !anchorDocPart ||
            headDocPart !== anchorDocPart ||
            !anchorDocPart.attrs.elements ||
            !anchorDocPart.attrs.elements.includes(elementName) ||
            (0,_state_plugins__rspack_import_3.checkProtectedInSelection)(editor.view.state)
        )
    } else {
        // footnote editor
        const anchorFootnote =
                editor.currentView.state.selection.$anchor.node(1),
            headFootnote = editor.currentView.state.selection.$head.node(1)

        return (
            !anchorFootnote ||
            headFootnote !== anchorFootnote ||
            !editor.view.state.doc.attrs.footnote_elements.includes(elementName)
        )
    }
}

function markAvailable(editor, markName) {
    let markInDocParts = false
    editor.view.state.doc.forEach(docPart => {
        if (docPart.attrs.elements && docPart.attrs.marks.includes(markName)) {
            markInDocParts = true
        }
    })
    return (
        editor.view.state.doc.attrs.footnote_marks.includes(markName) ||
        markInDocParts
    )
}

function markDisabled(editor, markName) {
    if (editor.currentView === editor.view) {
        // main editor
        const anchorDocPart =
                editor.currentView.state.selection.$anchor.node(1),
            headDocPart = editor.currentView.state.selection.$head.node(1)

        return (
            !anchorDocPart ||
            headDocPart !== anchorDocPart ||
            !anchorDocPart.attrs.marks ||
            !anchorDocPart.attrs.marks.includes(markName) ||
            (0,_state_plugins__rspack_import_3.checkProtectedInSelection)(editor.view.state)
        )
    } else {
        // footnote editor
        const anchorFootnote =
                editor.currentView.state.selection.$anchor.node(1),
            headFootnote = editor.currentView.state.selection.$head.node(1)

        return (
            !anchorFootnote ||
            headFootnote !== anchorFootnote ||
            !editor.view.state.doc.attrs.footnote_marks.includes(markName)
        )
    }
}

const toolbarModel = () => ({
    openMore: false, // whether 'more' menu is opened.
    content: [
        {
            type: "button",
            title: gettext("Open/close header menu"),
            icon: editor => {
                if (editor.menu.headerbarModel.open) {
                    return "angle-double-up"
                } else {
                    return "angle-double-down"
                }
            },
            action: editor => {
                editor.menu.headerbarModel.open =
                    !editor.menu.headerbarModel.open
                if (editor.menu.headerView) {
                    editor.menu.headerView.update()
                }
            },
            class: editor => {
                if (editor.menu.headerbarModel.open) {
                    return "no-border"
                } else {
                    return "no-border header-closed"
                }
            },
            order: 0
        },
        {
            type: "info",
            show: editor => {
                let title = ""
                if (editor.currentView !== editor.view) {
                    return gettext("Footnote")
                } else if (
                    editor.currentView.state.selection.$anchor.node(1) &&
                    editor.currentView.state.selection.$anchor.node(1) ===
                        editor.currentView.state.selection.$head.node(1)
                ) {
                    title =
                        editor.currentView.state.selection.$anchor.node(1).attrs
                            .title || gettext("Title")
                    return title.length > 20
                        ? title.slice(0, 20) + "..."
                        : title
                } else if (
                    editor.currentView.state.selection.$anchor.depth === 0 &&
                    editor.currentView.state.selection.from ===
                        editor.currentView.state.selection.to
                ) {
                    title =
                        editor.currentView.state.selection.$anchor.nodeAfter
                            .attrs.title
                    return title.length > 20
                        ? title.slice(0, 20) + "..."
                        : title
                } else if (
                    editor.currentView.state.selection.jsonID === "node" &&
                    editor.currentView.state.selection.node.isBlock &&
                    editor.currentView.state.selection.node.attrs.title
                ) {
                    title = editor.currentView.state.selection.node.attrs.title
                    return title.length > 20
                        ? title.slice(0, 20) + "..."
                        : title
                } else {
                    return ""
                }
            },
            order: 1
        },
        {
            type: "menu",
            show: editor => {
                if (
                    editor.currentView.state.selection.$anchor.node(1) &&
                    !editor.view.state.selection.$anchor.node(1).attrs.elements
                ) {
                    return ""
                }
                if (
                    editor.currentView.state.selection.jsonID === "node" &&
                    editor.currentView.state.selection.node.isBlock
                ) {
                    const selectedNode = editor.currentView.state.selection.node
                    return BLOCK_LABELS[selectedNode.type.name]
                        ? BLOCK_LABELS[selectedNode.type.name]
                        : ""
                }
                const startElement =
                        editor.currentView.state.selection.$anchor.parent,
                    endElement = editor.currentView.state.selection.$head.parent
                if (!startElement || !endElement) {
                    return ""
                } else if (startElement === endElement) {
                    const blockNodeType = startElement.type.name
                    return BLOCK_LABELS[blockNodeType]
                        ? BLOCK_LABELS[blockNodeType]
                        : ""
                } else {
                    let blockNodeType = true
                    editor.currentView.state.doc.nodesBetween(
                        editor.currentView.state.selection.from,
                        editor.currentView.state.selection.to,
                        node => {
                            if (node.isTextblock) {
                                const nextBlockNodeType = node.type.name
                                if (blockNodeType === true) {
                                    blockNodeType = nextBlockNodeType
                                }
                                if (blockNodeType !== nextBlockNodeType) {
                                    blockNodeType = false
                                }
                            }
                        }
                    )

                    if (blockNodeType) {
                        return BLOCK_LABELS[blockNodeType]
                            ? BLOCK_LABELS[blockNodeType]
                            : ""
                    } else {
                        return ""
                    }
                }
            },
            disabled: editor =>
                ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                !editor.currentView.state.selection.$anchor.node(1) ||
                !editor.currentView.state.selection.$anchor.node(1).attrs
                    .elements ||
                (editor.currentView.state.selection.jsonID === "node" &&
                    editor.currentView.state.selection.node.isBlock &&
                    !editor.currentView.state.selection.node.isTextblock) ||
                editor.currentView.state.selection.jsonID === "gapcursor",
            content: [
                {
                    title: BLOCK_LABELS["paragraph"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.paragraph)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "paragraph"),
                    disabled: editor => elementDisabled(editor, "paragraph"),
                    order: 0
                },
                {
                    title: BLOCK_LABELS["heading1"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading1)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading1"),
                    disabled: editor => elementDisabled(editor, "heading1"),
                    order: 1
                },
                {
                    title: BLOCK_LABELS["heading2"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading2)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading2"),
                    disabled: editor => elementDisabled(editor, "heading2"),
                    order: 2
                },
                {
                    title: BLOCK_LABELS["heading3"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading3)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading3"),
                    disabled: editor => elementDisabled(editor, "heading3"),
                    order: 3
                },
                {
                    title: BLOCK_LABELS["heading4"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading4)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading4"),
                    disabled: editor => elementDisabled(editor, "heading4"),
                    order: 4
                },
                {
                    title: BLOCK_LABELS["heading5"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading5)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading5"),
                    disabled: editor => elementDisabled(editor, "heading5"),
                    order: 5
                },
                {
                    title: BLOCK_LABELS["heading6"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.heading6)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "heading6"),
                    disabled: editor => elementDisabled(editor, "heading6"),
                    order: 6
                },
                {
                    title: BLOCK_LABELS["code_block"],
                    action: editor => {
                        const view = editor.currentView
                        ;(0,_keymap__rspack_import_2.setBlockType)(view.state.schema.nodes.code_block)(
                            view.state,
                            view.dispatch
                        )
                    },
                    available: editor => elementAvailable(editor, "code_block"),
                    disabled: editor => elementDisabled(editor, "code_block"),
                    order: 7
                }
            ],
            order: 2
        },
        {
            type: "button",
            title: gettext("Strong"),
            icon: "bold",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["strong"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "strong"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.currentView.state.selection.jsonID === "gapcursor" ||
                    markDisabled(editor, "strong")
                ) {
                    return true
                }
            },
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "strong") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "strong")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 3
        },
        {
            type: "button",
            title: gettext("Emphasis"),
            icon: "italic",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["em"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "em"),
            disabled: editor =>
                ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                editor.currentView.state.selection.jsonID === "gapcursor" ||
                markDisabled(editor, "em"),
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "em") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "em")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 4
        },
        {
            type: "button",
            title: gettext("Underline"),
            icon: "underline",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["underline"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "underline"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.currentView.state.selection.jsonID === "gapcursor" ||
                    markDisabled(editor, "underline")
                ) {
                    return true
                }
            },
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "underline") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "underline")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 5
        },
        {
            type: "button",
            title: gettext("Superscript"),
            icon: "superscript",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["sup"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "sup"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.currentView.state.selection.jsonID === "gapcursor" ||
                    markDisabled(editor, "sup")
                ) {
                    return true
                }
            },
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "sup") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "sup")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 6
        },
        {
            type: "button",
            title: gettext("Subscript"),
            icon: "subscript",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["sub"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "sub"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.currentView.state.selection.jsonID === "gapcursor" ||
                    markDisabled(editor, "sub")
                ) {
                    return true
                }
            },
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "sub") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "sub")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 7
        },
        {
            type: "button",
            title: gettext("Code"),
            icon: "code",
            action: editor => {
                const mark = editor.currentView.state.schema.marks["code"]
                const command = (0,prosemirror_commands__rspack_import_4.toggleMark)(mark)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => markAvailable(editor, "code"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.currentView.state.selection.jsonID === "gapcursor" ||
                    markDisabled(editor, "code")
                ) {
                    return true
                }
            },
            selected: editor => {
                const storedMarks = editor.currentView.state.storedMarks
                if (
                    storedMarks?.some(mark => mark.type.name === "code") ||
                    editor.currentView.state.selection.$head
                        .marks()
                        .some(mark => mark.type.name === "code")
                ) {
                    return true
                } else {
                    return false
                }
            },
            order: 8
        },
        {
            type: "button",
            title: gettext("Numbered list"),
            icon: "list-ol",
            action: editor => {
                const node =
                    editor.currentView.state.schema.nodes["ordered_list"]
                const command = (0,prosemirror_schema_list__rspack_import_5.wrapInList)(node)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => elementAvailable(editor, "ordered_list"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "ordered_list")
                ) {
                    return true
                }
            },
            selected: editor => {
                const depth =
                    editor.currentView.state.selection.$head.sharedDepth(
                        editor.currentView.state.selection.anchor
                    )
                const nodeType =
                    editor.currentView.state.schema.nodes["ordered_list"]
                for (let i = 0; i < depth; i++) {
                    const node =
                        editor.currentView.state.selection.$head.node(i)
                    if (node.type === nodeType) {
                        return true
                    }
                }
                return false
            },
            order: 9
        },
        {
            type: "button",
            title: gettext("Bullet list"),
            icon: "list-ul",
            action: editor => {
                const node =
                    editor.currentView.state.schema.nodes["bullet_list"]
                const command = (0,prosemirror_schema_list__rspack_import_5.wrapInList)(node)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => elementAvailable(editor, "bullet_list"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "bullet_list")
                ) {
                    return true
                }
            },
            selected: editor => {
                const depth =
                    editor.currentView.state.selection.$head.sharedDepth(
                        editor.currentView.state.selection.anchor
                    )
                const nodeType =
                    editor.currentView.state.schema.nodes["bullet_list"]
                for (let i = 0; i < depth; i++) {
                    const node =
                        editor.currentView.state.selection.$head.node(i)
                    if (node.type === nodeType) {
                        return true
                    }
                }
                return false
            },
            order: 10
        },
        {
            type: "button",
            title: gettext("Blockquote"),
            icon: "quote-right",
            action: editor => {
                const node = editor.currentView.state.schema.nodes["blockquote"]
                const command = (0,prosemirror_commands__rspack_import_4.wrapIn)(node)
                command(editor.currentView.state, tr =>
                    editor.currentView.dispatch(tr)
                )
            },
            available: editor => elementAvailable(editor, "blockquote"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "blockquote")
                ) {
                    return true
                }
            },
            selected: editor => {
                const depth =
                    editor.currentView.state.selection.$head.sharedDepth(
                        editor.currentView.state.selection.anchor
                    )
                const nodeType =
                    editor.currentView.state.schema.nodes["blockquote"]
                for (let i = 0; i < depth; i++) {
                    const node =
                        editor.currentView.state.selection.$head.node(i)
                    if (node.type === nodeType) {
                        return true
                    }
                }
                return false
            },
            order: 11
        },
        {
            id: "link",
            type: "button",
            title: gettext("Link"),
            icon: "link",
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.LinkDialog(editor)
                dialog.init()
            },
            available: editor => markAvailable(editor, "link"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    (markDisabled(editor, "link") &&
                        elementDisabled(editor, "cross_reference"))
                ) {
                    return true
                }
            },
            selected: editor =>
                editor.currentView.state.selection.$head
                    .marks()
                    .some(mark => mark.type.name === "link"),
            order: 12
        },
        {
            type: "button",
            title: gettext("Footnote"),
            icon: "asterisk",
            action: editor => {
                const node = editor.view.state.schema.nodes["footnote"]
                const tr = editor.view.state.tr.replaceSelectionWith(
                    node.createAndFill(),
                    false
                )
                editor.view.dispatch(tr)
                return false
            },
            available: editor => elementAvailable(editor, "footnote"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    editor.view !== editor.currentView || // we don't allow footnotes in footnotes
                    elementDisabled(editor, "footnote")
                ) {
                    return true
                }
            },
            order: 13
        },
        {
            type: "button",
            title: gettext("Cite"),
            icon: "book",
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.CitationDialog(editor)
                dialog.init()
                return false
            },
            available: editor => elementAvailable(editor, "citation"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "citation") ||
                    !["text", "node"].includes(
                        editor.currentView.state.selection.jsonID
                    ) ||
                    (editor.currentView.state.selection.jsonID === "node" &&
                        editor.currentView.state.selection.node.type.name !==
                            "citation")
                ) {
                    return true
                }
            },
            order: 14
        },
        {
            type: "button",
            title: gettext("Horizontal line"),
            icon: "minus",
            action: editor => {
                const view = editor.currentView,
                    state = view.state
                view.dispatch(
                    state.tr.replaceSelectionWith(
                        state.schema.node("horizontal_rule")
                    )
                )
            },
            available: editor => elementAvailable(editor, "horizontal_rule"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "horizontal_rule")
                ) {
                    return true
                }
            },
            order: 15
        },
        {
            type: "button",
            title: gettext("Math"),
            icon: "percent",
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.MathDialog(editor)
                dialog.init()
            },
            available: editor => elementAvailable(editor, "equation"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "equation") ||
                    !["text", "node"].includes(
                        editor.currentView.state.selection.jsonID
                    ) ||
                    (editor.currentView.state.selection.jsonID === "node" &&
                        editor.currentView.state.selection.node.type.name !==
                            "equation")
                ) {
                    return true
                }
            },
            order: 16
        },
        {
            type: "button",
            title: gettext("Figure"),
            icon: "image",
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.FigureDialog(editor)
                dialog.init()
                return false
            },
            available: editor => elementAvailable(editor, "figure"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "figure")
                ) {
                    return true
                }
            },
            order: 17
        },
        {
            type: "button",
            title: gettext("Table"),
            tooltip: gettext("Insert a table into the document."),
            icon: "table",
            action: editor => {
                const dialog = new _dialogs__rspack_import_1.TableDialog(editor)
                dialog.init()
                return false
            },
            available: editor => elementAvailable(editor, "table"),
            disabled: editor => {
                if (
                    ___rspack_import_0.READ_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    ___rspack_import_0.COMMENT_ONLY_ROLES.includes(editor.docInfo.access_rights) ||
                    elementDisabled(editor, "table") ||
                    findTable(editor.currentView.state)
                ) {
                    return true
                }
            },
            order: 15
        },
        {
            type: "button",
            title: gettext("Undo"),
            icon: "undo",
            action: editor =>
                (0,prosemirror_history__rspack_import_6.undo)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(
                        tr.setMeta("inputType", "historyUndo")
                    )
                ),
            disabled: editor => (0,prosemirror_history__rspack_import_6.undoDepth)(editor.currentView.state) === 0,
            order: 16
        },
        {
            type: "button",
            title: gettext("Redo"),
            icon: "redo",
            action: editor =>
                (0,prosemirror_history__rspack_import_6.redo)(editor.currentView.state, tr =>
                    editor.currentView.dispatch(
                        tr.setMeta("inputType", "historyRedo")
                    )
                ),
            disabled: editor => (0,prosemirror_history__rspack_import_6.redoDepth)(editor.currentView.state) === 0,
            order: 17
        }
    ]
})


}),
"./js/modules/editor/menus/toolbar/view.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ToolbarView: function() { return ToolbarView; }
});
/* import */ var diff_dom__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/diff-dom@5.2.1/node_modules/diff-dom/dist/module.js");


class ToolbarView {
    constructor(editorView, options) {
        this.editorView = editorView
        this.options = options

        this.editor = this.options.editor
        if (!this.editor.menu.toolbarViews) {
            this.editor.menu.toolbarViews = []
        }
        this.editor.menu.toolbarViews.push(this)

        this.dd = new diff_dom__rspack_import_0.DiffDOM({
            valueDiffing: false
        })
        this.sideMargins = 20 + 20 // CSS sets left margin to 14px + 46 px for left most button and we want the same margin on both sides
        this.availableWidth = window.innerWidth - this.sideMargins
        this.openedMenu = false
        this.listeners = {}

        if (editorView === this.options.editor.view) {
            // only do this when called for the main editor (not footnote editor)
            this.removeUnavailable(this.options.editor.menu.toolbarModel)
        }

        this.bindEvents()
        this.update()
    }

    removeUnavailable(menu) {
        // Remove those menu items from the menu model that are not available for this document.
        // Used for example for mark or element buttons that aren't permitted according to the
        // document template.
        menu.content = menu.content.filter(item => {
            if (item.available && !item.available(this.editor)) {
                return false
            } else if (item.type === "menu") {
                this.removeUnavailable(item)
            }
            return true
        })
    }

    bindEvents() {
        this.listeners.onclick = event => this.onclick(event)
        document.body.addEventListener("click", this.listeners.onclick)
    }

    destroy() {
        document.body.removeEventListener("click", this.listeners.onclick)
        this.editor.menu.toolbarViews = this.editor.menu.toolbarViews.filter(
            view => view !== this
        )
    }

    onResize() {
        // recalculate menu if needed
        this.availableWidth = window.innerWidth - this.sideMargins
        this.update()
    }

    onclick(event) {
        if (this.editorView !== this.editor.currentView) {
            // the other editor must be active
            return
        }
        const target = event.target
        if (
            target.matches(
                ".editor-toolbar .more-button li:not(.disabled), .editor-toolbar .more-button li:not(.disabled) *"
            )
        ) {
            let menuNumber = 0
            let seekItem = target.closest("li")
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            seekItem =
                seekItem.parentElement.parentElement.parentElement.parentElement
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            const menuItem = this.editor.menu.toolbarModel.content[menuNumber]
            // if it is a menu, open it. Otherwise execute an
            // associated action.
            if (menuItem.type === "menu") {
                menuItem.open = true
                this.openedMenu = menuNumber
                event.preventDefault()
                this.update()
            } else if (menuItem.action) {
                event.preventDefault()
                const focus = menuItem.action(this.editor)
                this.editor.menu.toolbarModel.openMore = false
                this.update()
                if (focus !== false) {
                    this.editor.currentView.focus()
                }
            }
        } else if (
            target.matches(
                ".editor-toolbar .more-button, .editor-toolbar .more-button *"
            )
        ) {
            this.editor.menu.toolbarModel.openMore = true
            if (this.openedMenu) {
                this.editor.menu.toolbarModel.content[this.openedMenu].open =
                    false
            }
            this.update()
        } else if (
            target.matches(
                ".editor-toolbar li:not(.disabled), .editor-toolbar li:not(.disabled) *"
            )
        ) {
            // A toolbar menu item was clicked. We just need to
            // find out which one
            let itemNumber = 0
            let seekItem = target.closest("li")
            while (seekItem.previousElementSibling) {
                itemNumber++
                seekItem = seekItem.previousElementSibling
            }
            seekItem =
                seekItem.parentElement.parentElement.parentElement.parentElement
            let menuNumber = 0
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            event.preventDefault()
            const focus = this.editor.menu.toolbarModel.content[
                menuNumber
            ].content[itemNumber].action(this.editor)
            this.editor.menu.toolbarModel.content[menuNumber].open = false
            this.openedMenu = false
            this.update()
            if (focus !== false) {
                this.editor.currentView.focus()
            }
        } else if (
            target.matches(
                ".editor-toolbar > div:not(.disabled), .editor-toolbar > div:not(.disabled) *"
            )
        ) {
            // A menu item has been clicked, lets find out which one.
            let menuNumber = 0
            let seekItem = target.closest("div.ui-buttonset")
            while (seekItem.previousElementSibling) {
                menuNumber++
                seekItem = seekItem.previousElementSibling
            }
            const menuItem = this.editor.menu.toolbarModel.content[menuNumber]
            // if it is a menu, open it. Otherwise execute an
            // associated action.
            if (menuItem.type === "menu") {
                menuItem.open = true
                this.openedMenu = menuNumber
                this.editor.menu.toolbarModel.openMore = false
                event.preventDefault()
                this.update()
            } else if (menuItem.action) {
                event.preventDefault()
                const focus = menuItem.action(this.editor)
                this.update()
                if (focus !== false) {
                    this.editor.currentView.focus()
                }
            }
        } else if (
            this.openedMenu !== false ||
            this.editor.menu.toolbarModel.openMore
        ) {
            if (this.openedMenu !== false) {
                this.editor.menu.toolbarModel.content[this.openedMenu].open =
                    false
            }
            this.editor.menu.toolbarModel.openMore = false
            this.openedMenu = false
            this.update()
        }
    }

    update() {
        if (this.editorView !== this.editor.currentView) {
            // the other editor must be active
            return
        }
        let spaceCounter = this.availableWidth
        let menuIndexToDrop = false
        this.editor.menu.toolbarModel.content.some((menuItem, index) => {
            switch (menuItem.type) {
                case "info":
                    spaceCounter -= 94
                    break
                case "menu":
                    spaceCounter -= 138
                    break
                default:
                    spaceCounter -= 52
            }
            if (spaceCounter < 0) {
                menuIndexToDrop = Math.max(index - 2, 3) // We need the space of two buttons for the more button
                return true
            }
        })
        const toolbarEl = (document.querySelector("#toolbar") || {})
            .firstElementChild
        if (!toolbarEl) {
            return
        }
        const diff = this.dd.diff(
            toolbarEl,
            this.getToolbarHTML(menuIndexToDrop)
        )
        this.dd.apply(toolbarEl, diff)
    }

    getToolbarHTML(menuIndexToDrop) {
        return `<div>
            <div class="editor-toolbar">
                ${this.editor.menu.toolbarModel.content
                    .map((menuItem, index) => {
                        if (!menuIndexToDrop || index < menuIndexToDrop) {
                            return `
                            <div class="ui-buttonset${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}">
                                ${this.getToolbarMenuItemHTML(menuItem, index)}
                            </div>
                        `
                        } else {
                            return ""
                        }
                    })
                    .join("")}
                ${this.getMoreButtonHTML(menuIndexToDrop)}
            </div>
        </div>`
    }

    getToolbarMenuItemHTML(menuItem, _index) {
        let returnValue
        switch (menuItem.type) {
            case "info":
                returnValue = this.getInfoHTML(menuItem)
                break
            case "menu":
                returnValue = this.getDropdownHTML(menuItem)
                break
            case "button":
                returnValue = this.getButtonHTML(menuItem)
                break
            default:
                returnValue = ""
                break
        }
        return returnValue
    }

    getMoreButtonHTML(menuIndexToDrop) {
        if (menuIndexToDrop) {
            return `
                <div class="ui-buttonset more-button">
                    <div class="multi-buttons">
                        <span class="multi-buttons-cover fw-button fw-white fw-large edit-button">
                            ${gettext("More")}
                        </span>
                        ${this.getMoreButtonListHTML(menuIndexToDrop)}
                    </div>
                </div>
            `
        } else {
            return ""
        }
    }

    getMoreButtonListHTML(menuIndexToDrop) {
        if (this.editor.menu.toolbarModel.openMore) {
            const remainingItems =
                this.editor.menu.toolbarModel.content.slice(menuIndexToDrop)
            return `
                <div class="fw-pulldown fw-left" style="display: block;">
                    <ul>${remainingItems.map(menuOption => this.getDropdownOptionHTML(menuOption)).join("")}</ul>
                </div>
            `
        } else {
            return ""
        }
    }

    getInfoHTML(menuItem) {
        return `<div class="info">${menuItem.show(this.editor)}</div>`
    }

    getDropdownHTML(menuItem) {
        return `
        <div class="multi-buttons">
            <span class="multi-buttons-cover fw-button fw-white fw-large edit-button${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}">
                ${menuItem.show(this.editor)}
            </span>
            ${this.getDropdownListHTML(menuItem)}
        </div>
        `
    }

    getDropdownListHTML(menuItem) {
        if (menuItem.open) {
            return `<div class="fw-pulldown fw-left" style="display: block;"><ul>${menuItem.content.map(menuOption => this.getDropdownOptionHTML(menuOption)).join("")}</ul></div>`
        } else {
            return ""
        }
    }

    getDropdownOptionHTML(menuOption) {
        return `
        <li class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only${menuOption.disabled && menuOption.disabled(this.editor) ? " disabled" : ""}" role="button" aria-disabled="false">
            <span class="ui-button-text">
                <input type="radio" >
                <label class="fw-pulldown-item">${menuOption.title}</label>
            </span>
        </li>
        `
    }

    getButtonHTML(menuItem) {
        return `
        <button aria-label="${menuItem.title}" class="fw-button fw-white fw-large fw-square edit-button${menuItem.disabled && menuItem.disabled(this.editor) ? " disabled" : ""}${menuItem.selected && menuItem.selected(this.editor) ? " ui-state-active" : ""}${menuItem.class ? ` ${menuItem.class(this.editor)}` : ""}" title="${menuItem.title}" >
            <span class="ui-button-text">
                <i class="fa fa-${typeof menuItem.icon === "function" ? menuItem.icon(this.editor) : menuItem.icon}"></i>
            </span>
        </button>`
    }
}


}),
"./js/modules/editor/navigator/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModNavigator: function() { return ModNavigator; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");

class ModNavigator {
    constructor(editor) {
        editor.mod.navigator = this
        this.editor = editor
        this.navigatorEl = document.querySelector("#navigator")
        this.listeners = {}
        this.navigatorFilters = editor.menu.navigatorFilterModel.content
        this.defaultFilters = ["heading1", "heading2", "heading3"]

        this.lastSelectedTarget = null
    }

    init() {
        this.render()
        this.bindEvents()
    }

    render() {
        this.navigatorEl.innerHTML = this.getNavigatorTemplate()
    }

    bindEvents() {
        document.body.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, "#navigator-button", el):
                    if (this.navigatorEl.classList.contains("opened")) {
                        this.closeNavigator()
                    } else {
                        const navigatorListEl =
                            document.getElementById("navigator-list")
                        if (navigatorListEl) {
                            navigatorListEl.innerHTML =
                                this.populateNavigator() || "" //Populating the list
                        }
                        this.openNavigator()
                        const activeHeading = this.navigatorEl.querySelector(
                            "#navigator-list .active-heading a"
                        )
                        if (activeHeading) {
                            activeHeading.focus()
                        } else {
                            const firstFocusable =
                                this.navigatorEl.querySelector(
                                    "#navigator-list [href]"
                                )
                            if (firstFocusable) {
                                firstFocusable.focus()
                            }
                        }
                    }
                    break
                case (0,_common__rspack_import_0.findTarget)(event, "#navigator-list a", el): {
                    event.preventDefault()
                    event.stopImmediatePropagation()
                    const target = el.target.getAttribute("href").slice(1)

                    if (target == "title") {
                        this.editor.scrollPosIntoView(1, this.editor.view)
                        this.lastSelectedTarget = "title"
                    } else if (target == "bibliography") {
                        this.editor.scrollBibliographyIntoView(target)
                    } else {
                        // Store the selected target ID for later focus
                        this.lastSelectedTarget = target
                        this.editor.scrollIdIntoView(target)
                        this.switchActiveHeading(el.target.parentNode)
                    }

                    // Keep focus on the navigation menu item
                    el.target.focus()
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, "#navigator-filter-icon", el): {
                    const navigatorFilterEl =
                        document.getElementById("navigator-filter")
                    if (navigatorFilterEl?.classList.contains("hide")) {
                        this.showFilters()
                    } else {
                        this.hideFilters()
                    }
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, "#navigator-filter-back", el): {
                    this.defaultFilters = []
                    document
                        .querySelectorAll("#navigator-filter input")
                        .forEach(item => {
                            if (item.checked) {
                                this.defaultFilters.push(item.id)
                            }
                        })
                    const navigatorListEl =
                        document.getElementById("navigator-list")
                    if (navigatorListEl) {
                        navigatorListEl.innerHTML =
                            this.populateNavigator() || ""
                    }
                    this.hideFilters()
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, "input", el):
                    break
                case (0,_common__rspack_import_0.findTarget)(event, "label", el):
                    break
                default:
                    this.closeNavigator()
                    break
            }
        })

        document.body
            .querySelector("#navigator-list")
            .addEventListener("mouseover", () => {
                document.body.classList.add("no-scroll")
            })
        document.body
            .querySelector("#navigator-list")
            .addEventListener("mouseout", () => {
                document.body.classList.remove("no-scroll")
            })
        document.body.addEventListener("keydown", event => {
            // Alt+n shortcut to toggle navigator
            if (event.altKey && event.key.toLowerCase() === "n") {
                event.preventDefault()
                if (this.navigatorEl.classList.contains("opened")) {
                    this.closeNavigator()
                } else {
                    const navigatorListEl =
                        document.getElementById("navigator-list")
                    if (navigatorListEl) {
                        navigatorListEl.innerHTML =
                            this.populateNavigator() || ""
                    }
                    this.openNavigator()
                    const activeHeading = this.navigatorEl.querySelector(
                        "#navigator-list .active-heading a"
                    )
                    if (activeHeading) {
                        activeHeading.focus()
                    } else {
                        const firstFocusable = this.navigatorEl.querySelector(
                            "#navigator-list [href]"
                        )
                        if (firstFocusable) {
                            firstFocusable.focus()
                        }
                    }
                }
            }

            const navigatorEl = document.getElementById("navigator")

            if (!navigatorEl || !navigatorEl.classList.contains("opened")) {
                // If the navigator is not opened, do nothing
                return
            }

            // Inside the navigator, handle keyboard navigation

            if (event.key === "Escape") {
                this.closeNavigator()
                return
            }

            if (event.key === "Enter" || event.key === " ") {
                event.preventDefault()
                event.stopImmediatePropagation()
                const activeElement = document.activeElement
                if (activeElement && activeElement.tagName === "A") {
                    activeElement.click()
                }
            }

            // Tab key navigation (keep focus inside dialog)
            if (event.key === "Tab") {
                const focusableElements = navigatorEl.querySelectorAll(
                    'button, [href], input, [tabindex="0"]'
                )
                if (focusableElements.length > 0) {
                    const firstElement = focusableElements[0]
                    const lastElement =
                        focusableElements[focusableElements.length - 1]

                    if (
                        event.shiftKey &&
                        document.activeElement === firstElement
                    ) {
                        event.preventDefault()
                        lastElement.focus()
                    } else if (
                        !event.shiftKey &&
                        document.activeElement === lastElement
                    ) {
                        event.preventDefault()
                        firstElement.focus()
                    }
                }
                return
            }

            const navigatorListEl = document.getElementById("navigator-list")

            // Arrow key navigation within the list
            if (
                !navigatorListEl.classList.contains("hide") &&
                (event.key === "ArrowDown" || event.key === "ArrowUp")
            ) {
                event.preventDefault()

                const links = navigatorListEl.querySelectorAll("a")
                if (links.length === 0) {
                    return
                }

                let currentIndex = -1
                links.forEach((link, index) => {
                    if (document.activeElement === link) {
                        currentIndex = index
                    }
                })

                let newIndex
                if (event.key === "ArrowDown") {
                    newIndex =
                        currentIndex < links.length - 1 ? currentIndex + 1 : 0
                } else {
                    // ArrowUp
                    newIndex =
                        currentIndex > 0 ? currentIndex - 1 : links.length - 1
                }

                links[newIndex].focus()
                return
            }

            const navigatorFilterEl =
                document.getElementById("navigator-filter")

            // Arrow key navigation within the filter menu
            if (
                !navigatorFilterEl.classList.contains("hide") &&
                (event.key === "ArrowDown" || event.key === "ArrowUp")
            ) {
                event.preventDefault()

                const checkboxes = navigatorFilterEl.querySelectorAll(
                    'input[type="checkbox"]'
                )
                if (checkboxes.length === 0) {
                    return
                }

                let currentIndex = -1
                checkboxes.forEach((checkbox, index) => {
                    if (document.activeElement === checkbox) {
                        currentIndex = index
                    }
                })

                let newIndex
                if (event.key === "ArrowDown") {
                    newIndex =
                        currentIndex < checkboxes.length - 1
                            ? currentIndex + 1
                            : 0
                } else {
                    // ArrowUp
                    newIndex =
                        currentIndex > 0
                            ? currentIndex - 1
                            : checkboxes.length - 1
                }

                checkboxes[newIndex].focus()
                return
            }
        })
    }

    switchActiveHeading(new_heading) {
        Array.prototype.forEach.call(
            document.querySelectorAll("#navigator-list .active-heading"),
            active_heading => active_heading.classList.remove("active-heading")
        )
        new_heading.classList.add("active-heading")
    }

    openNavigator() {
        const navigatorEl = document.getElementById("navigator")
        const navigatorButtonEl = document.getElementById("navigator-button")
        const navigatorFilterEl = document.getElementById("navigator-filter")
        const navigatorListEl = document.getElementById("navigator-list")
        const navigatorFilterBackEl = document.getElementById(
            "navigator-filter-back"
        )
        const navigatorFilterIconEl = document.getElementById(
            "navigator-filter-icon"
        )
        if (
            !navigatorEl ||
            !navigatorFilterEl ||
            !navigatorListEl ||
            !navigatorFilterBackEl ||
            !navigatorFilterIconEl
        ) {
            return
        }
        navigatorEl.classList.add("opened")
        navigatorFilterEl.classList.add("hide")
        navigatorListEl.classList.remove("hide")
        navigatorFilterBackEl.classList.add("hide")
        navigatorFilterIconEl.classList.remove("hide")
        navigatorButtonEl.setAttribute("aria-expanded", "true")
        this.scrollToActiveHeading()
    }

    scrollToActiveHeading() {
        const listEl = document.getElementById("navigator-list")
        const activeHeading = listEl?.querySelector(".active-heading")
        if (activeHeading) {
            activeHeading.scrollIntoView()
        }
    }

    closeNavigator() {
        const navigatorEl = document.getElementById("navigator")
        const navigatorButtonEl = document.getElementById("navigator-button")
        if (navigatorEl) {
            navigatorEl.classList.remove("opened")
            navigatorButtonEl.setAttribute("aria-expanded", "false")
        }

        if (this.lastSelectedTarget) {
            const target =
                this.lastSelectedTarget == "title"
                    ? document.body.querySelector(`div.doc-title`)
                    : document.body.querySelector(`#${this.lastSelectedTarget}`)
            if (target) {
                // Set selection at end of target.
                const range = document.createRange()
                const selection = window.getSelection()
                range.selectNodeContents(target)
                range.collapse()
                selection.removeAllRanges()
                selection.addRange(range)
                target.focus()
            }
            this.lastSelectedTarget = null
        }
    }

    showFilters() {
        const navigatorFilterEl = document.getElementById("navigator-filter")
        const navigatorListEl = document.getElementById("navigator-list")
        const navigatorFilterBackEl = document.getElementById(
            "navigator-filter-back"
        )
        const navigatorFilterIconEl = document.getElementById(
            "navigator-filter-icon"
        )
        if (
            !navigatorFilterEl ||
            !navigatorFilterBackEl ||
            !navigatorListEl ||
            !navigatorFilterIconEl
        ) {
            return
        }
        navigatorFilterEl.classList.remove("hide")
        navigatorFilterBackEl.classList.remove("hide")
        navigatorListEl.classList.add("hide")
        navigatorFilterIconEl.classList.add("hide")
        //populating the filter list
        navigatorFilterEl.innerHTML = this.populateNavFilter()
    }

    hideFilters() {
        const navigatorFilterEl = document.getElementById("navigator-filter")
        const navigatorListEl = document.getElementById("navigator-list")
        const navigatorFilterBackEl = document.getElementById(
            "navigator-filter-back"
        )
        const navigatorFilterIconEl = document.getElementById(
            "navigator-filter-icon"
        )
        if (
            !navigatorFilterEl ||
            !navigatorFilterBackEl ||
            !navigatorListEl ||
            !navigatorFilterIconEl
        ) {
            return
        }
        navigatorFilterEl.classList.add("hide")
        navigatorFilterBackEl.classList.add("hide")
        navigatorListEl.classList.remove("hide")
        navigatorFilterIconEl.classList.remove("hide")

        this.scrollToActiveHeading()
    }

    populateNavigator() {
        const currentPos = this.editor.view.state.selection.$head.pos
        const title =
            document.body.querySelector("div.doc-title")?.innerText ||
            gettext("Untitled Document")
        const items = [
            {
                id: "title",
                textContent: title,
                type: {name: "h1"}
            }
        ]
        let nearestHeader = ""
        this.editor.view.state.doc.descendants((node, pos) => {
            if (node.attrs?.hidden) {
                return false
            } else if (
                this.defaultFilters.includes(node.type.name) &&
                node.textContent !== ""
            ) {
                if (pos <= currentPos) {
                    nearestHeader = node
                } else if (nearestHeader !== "") {
                    items[items.length - 1] = Object.assign(
                        {},
                        items[items.length - 1],
                        {
                            class: "active-heading"
                        }
                    )
                    nearestHeader = ""
                }
                items.push({
                    id: node.attrs.id,
                    textContent: node.textContent,
                    type: node.type
                })
            }
        })
        const bibHeader = document.querySelector("h1.doc-bibliography-header")
        if (bibHeader) {
            items.push({
                id: "bibliography",
                textContent: bibHeader.innerText,
                type: {name: "h1"}
            })
        }
        if (items.length) {
            return this.navigatorHTML(items)
        } else {
            return false
        }
    }

    populateNavFilter() {
        return this.navigatorFilters
            .map(item => {
                const level = item.level
                const id = `heading${level}`
                return `<div role="menuitem">
                            <input type="checkbox" class="form-checkbox" id="${id}" ${this.inDefault(level)} aria-labelledby="label-${id}" />
                            <label id="label-${id}" class="navigator-label" for="${id}">${item.title}</label>
                        </div>`
            })
            .join("")
    }

    inDefault(level) {
        if (this.defaultFilters.includes("heading" + level)) {
            return "checked"
        } else {
            return ""
        }
    }

    navigatorHTML(items) {
        return `
            ${items
                .map(item => {
                    const level = item.type.name.substr(-1)
                    const className = item.class ? ` class="${item.class}"` : ""
                    return `<h${level}${className}><a href="#${item.id}" tabindex="0">${(0,_common__rspack_import_0.escapeText)(item.textContent)}</a></h${level}>`
                })
                .join("")}`
    }

    getNavigatorTemplate() {
        return `
            <div id="navigator-content" role="dialog" aria-labelledby="navigator-header">
                <div class="header-container">
                    <button id="navigator-filter-back" class="hide" aria-label="${gettext("Back to navigator")}" tabindex="0">
                        <i class="fas fa-arrow-left"></i>
                    </button>
                    <h1 id="navigator-header" class="header">${gettext("Document Navigator")}</h1>
                    <button id="navigator-filter-icon" aria-label="${gettext("Navigator settings")}" tabindex="0">
                        <i class="fas fa-cog"></i>
                    </button>
                </div>
                <div id="navigator-list" role="navigation" aria-label="${gettext("Document headings")}">
                </div>
                <div id="navigator-filter" class="hide" role="menu" aria-label="${gettext("Filter options")}">
                </div>
            </div>
            <button id="navigator-button"
                aria-expanded="false"
                aria-label="${gettext("Toggle document navigator")}"
                title="${gettext("Document N\u0332avigator")}">
                <span class="navigator-arrow-icon"><i class="fas fa-scroll"></i></span>
            </button>
            `
    }
}


}),
"./js/modules/editor/no_collab_save/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  NoCollabSave: function() { return NoCollabSave; }
});
/* import */ var prosemirror_collab__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _collab_merge_recreate_transform__rspack_import_1 = __webpack_require__("./js/modules/editor/collab/merge/recreate_transform.js");





/**
 * Handles periodic saving and conflict resolution for the editor
 * when COLLABORATIVE_EDITING is disabled (no WebSocket).
 */
class NoCollabSave {
    constructor(editor) {
        this.editor = editor
        this.saveInterval = null
        this.savePromise = null
        this._beforeUnloadHandler = null
        this._lastSaveTime = 0
    }

    start() {
        if (this.saveInterval) {
            return
        }
        // Save every 10 seconds when the document is loaded and writable.
        this.saveInterval = window.setInterval(() => {
            if (
                !this.editor.waitingForDocument &&
                !this.savePromise &&
                this.editor.docInfo.access_rights === "write" &&
                this._hasUnsavedChanges()
            ) {
                this.save()
            }
        }, 10000)

        // For non-E2EE documents, try to save on tab close / page hide.
        // E2EE encryption is async and cannot run reliably in unload handlers.
        if (!this.editor.e2ee?.encrypted) {
            this._beforeUnloadHandler = () => {
                if (this._hasUnsavedChanges()) {
                    this._save(true)
                }
            }
            window.addEventListener("beforeunload", this._beforeUnloadHandler)
            window.addEventListener("pagehide", this._beforeUnloadHandler)
        }
    }

    stop() {
        if (this.saveInterval) {
            window.clearInterval(this.saveInterval)
            this.saveInterval = null
        }
        if (this._beforeUnloadHandler) {
            window.removeEventListener(
                "beforeunload",
                this._beforeUnloadHandler
            )
            window.removeEventListener("pagehide", this._beforeUnloadHandler)
            this._beforeUnloadHandler = null
        }
    }

    async save() {
        if (this.savePromise) {
            return this.savePromise
        }
        this.savePromise = this._save()
        try {
            await this.savePromise
        } finally {
            this.savePromise = null
        }
    }

    _hasUnsavedChanges() {
        return (
            this.editor.docInfo.updated.getTime() > this._lastSaveTime
        )
    }

    async _save(keepalive = false) {
        const doc = this.editor.getDoc({use_current_view: true})
        let payload = {
            id: this.editor.docInfo.id,
            content: doc.content,
            comments: this.editor.mod.comments.store.comments,
            bibliography: this.editor.mod.db.bibDB.db,
            title: doc.title,
            version: this.editor.docInfo.version,
            image_updates: this.editor.mod.db.imageDB.unsentEvents()
        }

        if (
            this.editor.e2ee &&
            this.editor.e2ee.encrypted &&
            this.editor.e2ee.snapshotManager
        ) {
            const snapshot =
                await this.editor.e2ee.snapshotManager.getEncryptedSnapshot()
            if (snapshot) {
                payload = {
                    id: this.editor.docInfo.id,
                    content: snapshot.content,
                    comments: snapshot.comments,
                    bibliography: snapshot.bibliography,
                    title: snapshot.title,
                    version: snapshot.v,
                    e2ee_salt: snapshot.e2ee_salt,
                    e2ee_iterations: snapshot.e2ee_iterations,
                    e2ee_snapshot_version: snapshot.v
                }
            }
        }

        try {
            const {json, status} = await (0,_common__rspack_import_0.jsonPostJson)(
                "/api/document/save/",
                payload,
                null,
                {},
                keepalive
            )
            if (status === 409) {
                if (!keepalive) {
                    await this._handleVersionConflict()
                }
                return
            }
            if (json.version !== undefined) {
                this.editor.docInfo.version = json.version
            }
            this.editor.docInfo.updated = new Date()
            this.editor.docInfo.confirmedDoc = this.editor.view.state.doc
            this._lastSaveTime = Date.now()
            // Clear unsent event queues since the full state has been saved.
            this.editor.mod.db.bibDB.unsent = []
            this.editor.mod.db.imageDB.unsent = []
            this.editor.mod.comments.store.unsent = []
        } catch (error) {
            if (!keepalive) {
                console.error("Failed to save document:", error)
                ;(0,_common__rspack_import_0.addAlert)("error", gettext("Could not save document."))
            }
        }
    }

    async _handleVersionConflict() {
        const payload = {id: this.editor.docInfo.id}
        if (this.editor.docInfo.token) {
            payload.token = this.editor.docInfo.token
        }
        try {
            const {json} = await (0,_common__rspack_import_0.jsonPostJson)(
                "/api/document/get_doc_data/",
                payload
            )
            const serverDoc = this.editor.schema.nodeFromJSON(json.doc.content)
            const currentDoc = this.editor.view.state.doc
            const confirmedDoc = this.editor.docInfo.confirmedDoc

            const remoteTr = (0,_collab_merge_recreate_transform__rspack_import_1.recreateTransform)(confirmedDoc, serverDoc)
            const localTr = (0,_collab_merge_recreate_transform__rspack_import_1.recreateTransform)(confirmedDoc, currentDoc)

            const mappedSteps = []
            remoteTr.steps.forEach(step => {
                const mapped = step.map(localTr.mapping)
                if (mapped) {
                    mappedSteps.push(mapped)
                }
            })

            if (mappedSteps.length) {
                this.editor.view.dispatch(
                    (0,prosemirror_collab__rspack_import_2.receiveTransaction)(
                        this.editor.view.state,
                        mappedSteps,
                        mappedSteps.map(() => "remote")
                    ).setMeta("remote", true)
                )
            }

            // Update metadata from server
            this.editor.mod.db.bibDB.setDB(json.doc.bibliography)
            this.editor.mod.db.imageDB.setDB(json.doc.images)
            this.editor.mod.comments.store.loadComments(json.doc.comments)
            this.editor.docInfo.version = json.doc.v
            this.editor.docInfo.confirmedDoc = this.editor.view.state.doc
            this.editor.docInfo.updated = new Date(json.time)
            this.editor.mod.footnotes.fnEditor.renderAllFootnotes()

            // Retry save with the merged document
            await this.save()
        } catch (error) {
            console.error("Failed to handle version conflict:", error)
            ;(0,_common__rspack_import_0.addAlert)(
                "error",
                gettext(
                    "Document has been modified by another user. Please reload the page."
                )
            )
        }
    }
}


}),
"./js/modules/editor/state_plugins/access_rights.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  accessRightsPlugin: function() { return accessRightsPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");



const key = new prosemirror_state__rspack_import_1.PluginKey("accessRights")

const accessRightsPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        filterTransaction: (tr, _state) => {
            let allowed = true
            const remote = tr.getMeta("remote")
            if (remote) {
                return allowed
            }

            if (
                ___rspack_import_0.READ_ONLY_ROLES.includes(
                    options.editor.docInfo.access_rights
                ) &&
                tr.docChanged
            ) {
                // User only has read access. Don't allow anything.
                allowed = false
            } else if (
                ___rspack_import_0.COMMENT_ONLY_ROLES.includes(
                    options.editor.docInfo.access_rights
                )
            ) {
                //User has a comment-only role (commentator, editor or reviewer)

                //Check all transaction steps. If step type not allowed = prohibit
                //check if in allowed array. if false - exit loop
                if (
                    !tr.steps.every(
                        step =>
                            (step.jsonID === "addMark" ||
                                step.jsonID === "removeMark") &&
                            step.mark.type.name === "comment"
                    )
                ) {
                    allowed = false
                }
            }

            return allowed
        }
    })


}),
"./js/modules/editor/state_plugins/citation_render.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  citationRenderPlugin: function() { return citationRenderPlugin; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");



const key = new prosemirror_state__rspack_import_0.PluginKey("citationRender")

const citationRenderPlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init() {
                return {action: false}
            },
            apply(tr, _prev, oldState, _state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }
                let {action} = this.getState(oldState)

                if (action || tr.getMeta("settings")) {
                    return {action} // We already need to reset the bibliography or another setting is used. Don't bother checking for more reasons to do so.
                }
                tr.steps.forEach((step, index) => {
                    if (
                        step instanceof prosemirror_transform__rspack_import_1.ReplaceStep ||
                        step instanceof prosemirror_transform__rspack_import_1.ReplaceAroundStep
                    ) {
                        if (step.from !== step.to) {
                            tr.docs[index].nodesBetween(
                                step.from,
                                step.to,
                                node => {
                                    if (node.type.name === "citation") {
                                        // A citation was replaced. We need to reset
                                        action = "reset"
                                    } else if (
                                        !action &&
                                        node.type.name === "footnote"
                                    ) {
                                        action = "numbers"
                                    }
                                }
                            )
                        }
                        if (step.slice?.content) {
                            step.slice.content.descendants(node => {
                                if (node.type.name === "citation") {
                                    // A citation was added. We need to reset
                                    action = "reset"
                                } else if (
                                    !action &&
                                    node.type.name === "footnote"
                                ) {
                                    action = "numbers"
                                }
                            })
                        }
                    }
                })

                return {action}
            }
        },
        view(_view) {
            options.editor.mod.citations.resetCitations()
            return {
                update: (view, _prevState) => {
                    const {action} = key.getState(view.state)
                    if (action === "reset") {
                        options.editor.mod.citations.resetCitations()
                        const tr = view.state.tr.setMeta(key, {action: false})
                        view.dispatch(tr)
                    } else if (action === "numbers") {
                        options.editor.mod.citations.footnoteNumberOverride()
                        const tr = view.state.tr.setMeta(key, {action: false})
                        view.dispatch(tr)
                    } else if (view.dom.querySelector(".citation:empty")) {
                        options.editor.mod.citations.resetCitations()
                    }
                },
                destroy: () => {
                    options.editor.mod.citations.resetCitations()
                }
            }
        }
    })


}),
"./js/modules/editor/state_plugins/clipboard.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  clipboardPlugin: function() { return clipboardPlugin; },
  getPasteRange: function() { return getPasteRange; },
  resetPasteRange: function() { return resetPasteRange; }
});
/* import */ var fix_utf8__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fix-utf8@2.0.1/node_modules/fix-utf8/index.js");
/* import */ var prosemirror_model__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _clipboard_copy__rspack_import_1 = __webpack_require__("./js/modules/editor/clipboard/copy/index.js");
/* import */ var _clipboard_paste__rspack_import_2 = __webpack_require__("./js/modules/editor/clipboard/paste/index.js");
/* import */ var _document_template__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/document_template.js");










const key = new prosemirror_state__rspack_import_4.PluginKey("clipboard")

const getPasteRange = state => {
    const {pasteRange} = key.getState(state)
    return pasteRange
}

const resetPasteRange = tr => {
    tr.setMeta(key, {pasteRange: null})
}

// Filter a fragment to only include allowed nodes and marks
// parentNode is optional - used to check if converted nodes are valid children
const filterFragment = (
    fragment,
    allowedElements,
    allowedMarks,
    schema,
    parentNode = null
) => {
    const nodes = []

    fragment.forEach(node => {
        // Check if this node type is allowed
        const nodeTypeAllowed =
            !allowedElements || allowedElements.includes(node.type.name)

        if (!nodeTypeAllowed) {
            // Try to convert to paragraph if allowed
            if (
                allowedElements &&
                allowedElements.includes("paragraph") &&
                node.isBlock
            ) {
                const paragraph = schema.nodes.paragraph
                if (paragraph) {
                    // Check if paragraph can be a child of the parent node
                    const canUseParagraph =
                        !parentNode ||
                        parentNode.type.contentMatch.matchType(paragraph)

                    if (canUseParagraph) {
                        // Convert the node to a paragraph, recursively filtering its content
                        const filteredContent =
                            node.content.size > 0
                                ? filterFragment(
                                      node.content,
                                      allowedElements,
                                      allowedMarks,
                                      schema,
                                      node
                                  )
                                : prosemirror_model__rspack_import_5.Fragment.empty
                        nodes.push(paragraph.create(null, filteredContent))
                    } else if (parentNode && parentNode.isTextblock) {
                        // Parent is a text block but doesn't allow paragraphs
                        // Extract text content and add as plain text
                        const textContent = node.textContent
                        if (textContent) {
                            nodes.push(schema.text(textContent))
                        }
                    } else {
                        // Can't convert to paragraph, extract and process children
                        const filteredContent = filterFragment(
                            node.content,
                            allowedElements,
                            allowedMarks,
                            schema,
                            parentNode
                        )
                        filteredContent.forEach(child => nodes.push(child))
                    }
                }
            } else if (node.isBlock) {
                // Extract content from disallowed block nodes
                const filteredContent = filterFragment(
                    node.content,
                    allowedElements,
                    allowedMarks,
                    schema,
                    parentNode
                )
                filteredContent.forEach(child => nodes.push(child))
            } else {
                // For inline nodes, just skip them
            }
            return
        }

        // Node type is allowed, but filter marks
        let filteredNode = node
        if (allowedMarks !== false && node.marks.length > 0) {
            const filteredMarks = node.marks.filter(mark =>
                allowedMarks.includes(mark.type.name)
            )
            if (filteredMarks.length !== node.marks.length) {
                filteredNode = node.mark(filteredMarks)
            }
        }

        // Recursively filter content
        if (filteredNode.content.size > 0) {
            const filteredContent = filterFragment(
                filteredNode.content,
                allowedElements,
                allowedMarks,
                schema,
                filteredNode
            )
            filteredNode = filteredNode.copy(filteredContent)
        }

        nodes.push(filteredNode)
    })

    return prosemirror_model__rspack_import_5.Fragment.from(nodes)
}

const clipboardPlugin = options => {
    let shiftPressed = false
    return new prosemirror_state__rspack_import_4.Plugin({
        key,
        state: {
            init() {
                return {
                    pasteRange: null
                }
            },
            apply(tr, _prev, oldState, _state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }
                const uiEventMeta = tr.getMeta("uiEvent")
                let pasteRange
                if (uiEventMeta && ["paste", "drop"].includes(uiEventMeta)) {
                    pasteRange = [
                        oldState.selection.from,
                        oldState.selection.to
                    ]
                    tr.steps.forEach(step => {
                        if (step instanceof prosemirror_transform__rspack_import_6.ReplaceStep) {
                            // paste and drop TRs will usually just consist of a single ReplaceStep
                            pasteRange = [step.from, step.to]
                        }
                        const stepMap = step.getMap()
                        pasteRange = [
                            stepMap.map(pasteRange[0], -1),
                            stepMap.map(pasteRange[1], 1)
                        ] // map through step
                    })
                } else {
                    pasteRange = this.getState(oldState)?.pasteRange
                    if (pasteRange && tr.docChanged) {
                        const from = tr.mapping.mapResult(pasteRange[0], -1)
                        const to = tr.mapping.mapResult(pasteRange[1], 1)
                        if (from.deleted || to.deleted) {
                            pasteRange = null
                        } else {
                            pasteRange = [from.pos, to.pos]
                        }
                    }
                }

                return {
                    pasteRange
                }
            }
        },
        props: {
            handleKeyDown: (_view, event) => {
                shiftPressed = event.shiftKey
                return false
            },
            handleDrop: (view, event, slice, moved) => {
                shiftPressed = event.shiftKey
                if (moved || (slice && slice.size)) {
                    return false // Something other than en empty plain text string from outside. Handled by PM already.
                }
                const eventPos = view.posAtCoords({
                    left: event.clientX,
                    top: event.clientY
                })
                if (!eventPos) {
                    return false
                }
                const $mouse = view.state.doc.resolve(eventPos.pos)
                if (!$mouse) {
                    return false
                }
                const tr = view.state.tr
                tr.setSelection(new prosemirror_state__rspack_import_4.TextSelection($mouse))
                view.dispatch(tr)
                return true
            },
            transformPastedHTML: (inHTML, view) => {
                const {pasteRange} = key.getState(view.state)

                if (pasteRange) {
                    // a previous paste operation has been interrupted by a new one. Cancel.
                    return ""
                }
                if (shiftPressed) {
                    return inHTML
                }
                const ph = new _clipboard_paste__rspack_import_2.HTMLPaste(
                    options.editor,
                    inHTML,
                    options.viewType,
                    view
                )
                return ph.getOutput()
            },
            transformPastedText: (inText, _plain, view) => {
                const {pasteRange} = key.getState(view.state)

                if (pasteRange) {
                    // a previous paste operation has been interrupted by a new one. Cancel.
                    return ""
                }
                if (shiftPressed) {
                    return inText
                }
                // Chrome on Linux has an encoding problem:
                // it recognizes UTF as Windows 1252. Bug has been filed. This is a temp
                // solution for western European languages.
                // https://bugs.chromium.org/p/chromium/issues/detail?id=760613
                const fixedText = (0,fix_utf8__rspack_import_0["default"])(inText)
                const ph = new _clipboard_paste__rspack_import_2.TextPaste(
                    options.editor,
                    fixedText,
                    options.editor.currentView
                )
                ph.init()
                return fixedText // We need to analyze it asynchronously, so we always need to turn this into an empty string for now.
            },
            clipboardSerializer:
                options.viewType === "main"
                    ? (0,_clipboard_copy__rspack_import_1.docClipboardSerializer)(options.editor)
                    : (0,_clipboard_copy__rspack_import_1.fnClipboardSerializer)(options.editor),
            transformPasted: (slice, view, plain) => {
                // Only filter if not plain text paste
                if (plain) {
                    return slice
                }

                // Get allowed elements and marks at the current selection position
                const {elements, marks} = (0,_document_template__rspack_import_3.getAllowedElementsAndMarks)(view.state)

                // If no restrictions, return slice unchanged
                if (elements === false && marks === false) {
                    return slice
                }

                // Get the parent node at the selection position to check compatibility
                const $pos = view.state.selection.$from
                const parentNode = $pos.parent

                // Filter the slice content
                const filteredContent = filterFragment(
                    slice.content,
                    elements,
                    marks,
                    view.state.schema,
                    parentNode
                )

                // Return new slice with filtered content
                return new prosemirror_model__rspack_import_5.Slice(
                    filteredContent,
                    slice.openStart,
                    slice.openEnd
                )
            }
        }
    })
}


}),
"./js/modules/editor/state_plugins/code_block.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  codeBlockPlugin: function() { return codeBlockPlugin; }
});
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("codeBlockMenu")

class CodeBlockView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options
        this.dom = document.createElement("div")
        this.dom.classList.add("code-block-wrapper")

        // Add category label and title if present
        if (this.node.attrs.category || this.node.attrs.title) {
            this.addLabel()
        }

        // Use DOMSerializer to create the content
        this.serializer = prosemirror_model__rspack_import_2.DOMSerializer.fromSchema(node.type.schema)
        const preElement = this.serializer.serializeNode(this.node)
        preElement.classList.forEach(className =>
            this.dom.classList.add(className)
        )
        preElement.classList.value = ""
        this.dom.appendChild(preElement)

        // The contentDOM should be the <code> element inside <pre>
        this.contentDOM = preElement.querySelector("code")

        // Add menu button
        this.menuButton = document.createElement("button")
        this.menuButton.classList.add("code-block-menu-btn")
        this.menuButton.innerHTML =
            '<span class="dot-menu-icon"><i class="fa fa-ellipsis-v"></i></span>'
        this.dom.insertBefore(this.menuButton, this.dom.firstChild)

        // Add language badge if language is set
        if (this.node.attrs.language) {
            this.addLanguageBadge()
        }
    }

    addLabel() {
        const {category, title, id} = this.node.attrs
        const language = this.view.state.doc.attrs.language
        const categories = {}

        // Count code blocks by category
        this.view.state.doc.descendants(node => {
            if (node.attrs.track?.find(track => track.type === "deletion")) {
                return true
            }
            if (
                node.type.name === "code_block" &&
                node.attrs.category &&
                node.attrs.id
            ) {
                if (!categories[node.attrs.category]) {
                    categories[node.attrs.category] = 0
                }
                categories[node.attrs.category]++

                if (node.attrs.id === id) {
                    // Found our position, stop counting
                    return false
                }
            }
        })

        const label = document.createElement("div")
        label.classList.add("code-block-label")

        if (category && id) {
            label.dataset.id = id
            const {CATS} = __webpack_require__("./js/modules/schema/i18n.js")
            const categoryLabel = CATS[category]?.[language] || category
            const number = categories[category] || 1

            const labelText = title
                ? `${categoryLabel} ${number}: ${title}`
                : `${categoryLabel} ${number}`

            label.innerHTML = `<span class="label-text">${labelText}</span>`
        } else if (title) {
            // Title without category
            label.innerHTML = `<span class="label-text">${title}</span>`
        }

        this.dom.appendChild(label)
    }

    addLanguageBadge() {
        const badge = document.createElement("div")
        badge.classList.add("code-block-language-badge")
        badge.textContent = this.node.attrs.language
        this.dom.appendChild(badge)
    }

    stopEvent(event) {
        let stopped = false
        if (event.type === "mousedown") {
            const composedPath = event.composedPath()
            if (composedPath.includes(this.menuButton)) {
                stopped = true
                const tr = this.view.state.tr
                const $pos = this.view.state.doc.resolve(this.getPos())
                tr.setSelection(new prosemirror_state__rspack_import_1.NodeSelection($pos))
                this.view.dispatch(tr)
                const contentMenu = new _common__rspack_import_0.ContentMenu({
                    menu: this.options.editor.menu.codeBlockMenuModel,
                    width: 280,
                    page: this.options.editor,
                    menuPos: {
                        X: Number.parseInt(event.pageX) + 20,
                        Y: Number.parseInt(event.pageY) - 100
                    },
                    onClose: () => {
                        this.view.focus()
                    }
                })
                contentMenu.open()
            } else if (
                composedPath.includes(this.dom) &&
                !composedPath.find(el => el.matches && el.matches("code"))
            ) {
                stopped = true
                const tr = this.view.state.tr
                const $pos = this.view.state.doc.resolve(this.getPos())
                tr.setSelection(new prosemirror_state__rspack_import_1.NodeSelection($pos))
                this.view.dispatch(tr)
            }
        }

        return stopped
    }

    update(node) {
        if (node.type !== this.node.type) {
            return false
        }

        const oldNode = this.node
        this.node = node

        // Update language badge if changed
        if (node.attrs.language !== oldNode.attrs.language) {
            const existingBadge = this.dom.querySelector(
                ".code-block-language-badge"
            )
            if (existingBadge) {
                existingBadge.remove()
            }
            if (node.attrs.language) {
                this.addLanguageBadge()
            }
        }

        // Update label if category, title, or id changed
        if (
            node.attrs.category !== oldNode.attrs.category ||
            node.attrs.title !== oldNode.attrs.title ||
            node.attrs.id !== oldNode.attrs.id
        ) {
            const existingLabel = this.dom.querySelector(".code-block-label")
            if (existingLabel) {
                existingLabel.remove()
            }
            if (node.attrs.category || node.attrs.title) {
                this.addLabel()
            }
        }

        return true
    }
}

const codeBlockPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        state: {
            init(_config, _state) {
                if (options.editor.docInfo.access_rights === "write") {
                    this.spec.props.nodeViews["code_block"] = (
                        node,
                        view,
                        getPos
                    ) => new CodeBlockView(node, view, getPos, options)
                }
                return {}
            },
            apply(_tr, prev) {
                return prev
            }
        },
        props: {
            nodeViews: {}
        }
    })


}),
"./js/modules/editor/state_plugins/collab_carets.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  collabCaretsPlugin: function() { return collabCaretsPlugin; },
  getSelectionUpdate: function() { return getSelectionUpdate; },
  removeCollaboratorSelection: function() { return removeCollaboratorSelection; },
  updateCollaboratorSelection: function() { return updateCollaboratorSelection; }
});
/* import */ var prosemirror_collab__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-collab@1.3.1/node_modules/prosemirror-collab/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");




const key = new prosemirror_state__rspack_import_0.PluginKey("collabCarets")

const getSelectionUpdate = state => {
    const {caretUpdate} = key.getState(state)
    return caretUpdate
}

const updateCollaboratorSelection = (state, collaborator, data) => {
    let {decos, caretPositions} = key.getState(state)

    const oldCarPos = caretPositions.find(
        carPos => carPos.sessionId === data.session_id
    )

    if (oldCarPos) {
        caretPositions = caretPositions.filter(carPos => carPos !== oldCarPos)
        const removeDecos = decos
            .find()
            .filter(deco => deco.spec === oldCarPos.decoSpec)
        decos = decos.remove(removeDecos)
    }

    const widgetDom = document.createElement("div")
    const className = `user-${collaborator.id}`
    widgetDom.classList.add("caret")
    widgetDom.classList.add(className)
    widgetDom.innerHTML = '<div class="caret-head"></div>'
    widgetDom.firstChild.classList.add(className)
    const tooltip = collaborator.name
    widgetDom.title = tooltip
    widgetDom.firstChild.title = tooltip
    const decoSpec = {id: data.session_id} // We will compare the decoSpec object. Id not really needed.
    const newCarPos = {
        sessionId: data.session_id,
        userId: collaborator.id,
        decoSpec,
        anchor: data.anchor,
        head: data.head
    }
    caretPositions.push(newCarPos)

    const widgetDeco = prosemirror_view__rspack_import_1.Decoration.widget(data.head, widgetDom, decoSpec),
        addDecos = [widgetDeco]

    if (data.anchor !== data.head) {
        const from = data.head > data.anchor ? data.anchor : data.head,
            to = data.anchor > data.head ? data.anchor : data.head,
            inlineDeco = prosemirror_view__rspack_import_1.Decoration.inline(
                from,
                to,
                {
                    class: `user-bg-${collaborator.id}`
                },
                decoSpec
            )
        addDecos.push(inlineDeco)
    }
    decos = decos.add(state.doc, addDecos)

    const tr = state.tr.setMeta(key, {
        decos,
        caretPositions,
        caretUpdate: false
    })
    return tr
}

const removeCollaboratorSelection = (state, data) => {
    let {decos, caretPositions} = key.getState(state)

    const caretPosition = caretPositions.find(
        carPos => carPos.sessionId === data.session_id
    )

    if (caretPosition) {
        caretPositions = caretPositions.filter(
            carPos => carPos !== caretPosition
        )
        const removeDecos = decos
            .find()
            .filter(deco => deco.spec === caretPosition.decoSpec)
        decos = decos.remove(removeDecos)
        const tr = state.tr.setMeta(key, {
            decos,
            caretPositions,
            caretUpdate: false
        })
        return tr
    }
    return false
}

const collabCaretsPlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init() {
                return {
                    caretPositions: [],
                    decos: prosemirror_view__rspack_import_1.DecorationSet.empty,
                    caretUpdate: false
                }
            },
            apply(tr, _prev, oldState, state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }
                let {decos, caretPositions} = this.getState(oldState),
                    caretUpdate = false

                decos = decos.map(tr.mapping, tr.doc, {
                    onRemove: decoSpec => {
                        caretPositions = caretPositions.filter(
                            carPos => carPos.decoSpec !== decoSpec
                        )
                    }
                })

                if (
                    tr.selectionSet &&
                    !(0,prosemirror_collab__rspack_import_2.sendableSteps)(state) &&
                    !["review", "review-tracked"].includes(
                        options.editor.docInfo.access_rights
                    )
                ) {
                    caretUpdate = {
                        anchor: tr.selection.anchor,
                        head: tr.selection.head
                    }
                }

                return {
                    decos,
                    caretPositions,
                    caretUpdate
                }
            }
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        }
    })


}),
"./js/modules/editor/state_plugins/comments.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  addCommentDuringCreationDecoration: function() { return addCommentDuringCreationDecoration; },
  commentsPlugin: function() { return commentsPlugin; },
  getCommentDuringCreationDecoration: function() { return getCommentDuringCreationDecoration; },
  removeCommentDuringCreationDecoration: function() { return removeCommentDuringCreationDecoration; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var prosemirror_view__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");




const key = new prosemirror_state__rspack_import_0.PluginKey("comments")

const commentDuringCreationDecoSpec = {}

const moveComment = (doc, id, pos) => {
    // The content to which a comment was linked has been removed.
    // We need to find text close to the position to which we can link
    // comment.
    if (!pos) {
        // The position is linked to the start of the document. The minimum start
        // position is 2
        pos = 2
    }
    let posFrom = pos - 1
    let posTo = pos
    // We move backward through the document, trying to pick a start position
    // the depth is 1 between document parts, and comments should be moved
    // across these.
    // We decrease the from position until there is some text between posFrom
    // and posTo or until we hit the start of the document part.
    while (
        doc.resolve(posFrom).depth > 1 &&
        !doc.textBetween(posFrom, posTo).length &&
        posFrom
    ) {
        posFrom--
    }
    // If we ended up reaching a document part boundary rather than finding
    // text, we try again, this time moving in the opposite direction.
    // We start at the original position and then increase posTo
    if (doc.resolve(posFrom).depth === 1) {
        posFrom = posTo
        posTo++
        while (
            doc.resolve(posTo).depth > 1 &&
            !doc.textBetween(posFrom, posTo).length
        ) {
            posTo++
        }

        // If also the increase of posTo only made us reach a document part
        // boundary, it means all text has been removed in this document part.
        // Try placing it on the first letter of the title and otherwise give up.
        if (doc.resolve(posTo).depth === 1) {
            if (doc.textBetween(2, 3).length) {
                posFrom = 2
                posTo = 3
            } else {
                return
            }
        }
    }
    const markType = doc.type.schema.marks.comment.create({id})
    return new prosemirror_transform__rspack_import_1.AddMarkStep(posFrom, posTo, markType)
}

const addCommentDuringCreationDecoration = (state, tr) => {
    if (!tr.selection.from || tr.selection.from === tr.selection.to) {
        return false
    }
    let {decos} = key.getState(state)

    const commentDuringCreationDeco = decos.find(
        undefined,
        undefined,
        spec => spec === commentDuringCreationDecoSpec
    )

    if (commentDuringCreationDeco) {
        decos = decos.remove([commentDuringCreationDeco])
    }

    decos = decos.add(tr.doc, [
        prosemirror_view__rspack_import_2.Decoration.inline(
            tr.selection.from,
            tr.selection.to,
            {class: "active-comment"},
            commentDuringCreationDecoSpec
        )
    ])

    return tr.setMeta(key, {decos})
}

const removeCommentDuringCreationDecoration = (state, tr) => {
    let {decos} = key.getState(state)

    const commentDuringCreationDecos = decos.find(
        undefined,
        undefined,
        spec => spec === commentDuringCreationDecoSpec
    )

    if (!commentDuringCreationDecos) {
        return false
    }
    decos = decos.remove(commentDuringCreationDecos)

    return tr.setMeta(key, {decos})
}

const getCommentDuringCreationDecoration = state => {
    const {decos} = key.getState(state)

    const deco = decos.find(
        undefined,
        undefined,
        spec => spec === commentDuringCreationDecoSpec
    )

    if (deco.length) {
        return deco[0]
    } else {
        return false
    }
}

const commentsPlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init() {
                return {
                    decos: prosemirror_view__rspack_import_2.DecorationSet.empty
                }
            },
            apply(tr, _prev, oldState, _state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }
                let {decos} = this.getState(oldState)

                decos = decos.map(tr.mapping, tr.doc, {
                    onRemove: _decoSpec => {
                        // comment text has been deleted, cancel comment creation.
                        options.editor.mod.comments.interactions.deleteComment(
                            -1
                        )
                    }
                })

                return {
                    decos
                }
            }
        },
        appendTransaction: (trs, _oldState, state) => {
            // Check if any of the transactions are local.
            if (trs.every(tr => tr.getMeta("remote"))) {
                // All transactions are remote. Give up.
                return
            }

            const deletedComments = {}
            // Check what area is affected

            trs.forEach(tr => {
                Object.keys(deletedComments).forEach(commentId => {
                    // map positions from earlier transactions
                    deletedComments[commentId] = tr.mapping.map(
                        deletedComments[commentId]
                    )
                })
                tr.steps.forEach((step, index) => {
                    if (
                        step.jsonID === "replace" ||
                        step.jsonID === "replaceAround"
                    ) {
                        if (step.from !== step.to) {
                            tr.docs[index].nodesBetween(
                                step.from,
                                step.to,
                                node => {
                                    node.marks
                                        .filter(
                                            mark =>
                                                mark.type.name === "comment" &&
                                                mark.attrs.id
                                        )
                                        .map(mark => mark.attrs.id)
                                        .forEach(commentId => {
                                            if (
                                                !deletedComments.hasOwnProperty(
                                                    commentId
                                                )
                                            ) {
                                                deletedComments[commentId] =
                                                    step.from
                                            }
                                        })
                                }
                            )
                        }
                    }
                })
            })
            if (!Object.keys(deletedComments).length) {
                return
            }

            // We try to see if the deleted comments are still to be found in
            // another part of the document.
            state.doc.descendants(node => {
                if (!node.isInline && !node.isLeaf) {
                    return
                }

                node.marks
                    .filter(
                        mark => mark.type.name === "comment" && mark.attrs.id
                    )
                    .map(mark => mark.attrs.id)
                    .forEach(commentId => {
                        if (deletedComments.hasOwnProperty(commentId)) {
                            delete deletedComments[commentId]
                        }
                    })
            })
            if (!Object.keys(deletedComments).length) {
                return
            }
            let steps = [],
                tr = state.tr
            Object.entries(deletedComments).forEach(([id, pos]) => {
                steps.push(moveComment(state.doc, id, pos))
            })
            if (!steps.every(step => step)) {
                // At least one comment was placed in a part where it couldn't be put,
                // and additionally it could not be put in the title either because it
                // had no content.
                // Add an empty space to the title, increase all positions, by 1, then
                // try again.
                tr = state.tr.insertText(" ", 2)
                state = tr.state
                steps = []
                Object.entries(deletedComments).forEach(([id, pos]) => {
                    pos++
                    steps.push(moveComment(tr.doc, id, pos))
                })
                // If the step still is not working. give up. This should never happen.
                steps = steps.filter(step => step)
            }
            steps.forEach(step => tr.step(step))
            return tr
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        }
    })


}),
"./js/modules/editor/state_plugins/contributor_input/add_button.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  AddButton: function() { return AddButton; }
});
/* import */ var w3c_keyname__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/w3c-keyname@2.2.8/node_modules/w3c-keyname/index.js");
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _dialogs__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/index.js");
/* import */ var _helpers__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/helpers.js");






class AddButton {
    constructor(dom, getNode, getPos, view) {
        this.dom = dom
        this.getNode = getNode
        this.getPos = getPos
        this.view = view

        this.button = null
    }

    init() {
        const node = this.getNode()
        const nodeTitle = node.attrs.item_title
        this.dom.insertAdjacentHTML(
            "beforeend",
            `<button class="fw-button fw-light">${gettext("Add")} ${nodeTitle.toLowerCase()}...</button>`
        )
        this.button = this.dom.lastElementChild
        this.button.addEventListener("click", event =>
            this.handleActivation(event)
        )
        this.button.addEventListener("keydown", event =>
            this.handleKeyDown(event)
        )
    }

    handleKeyDown(event) {
        const key = (0,w3c_keyname__rspack_import_0.keyName)(event)
        switch (key) {
            case "Enter":
            case " ":
                this.handleActivation(event)
                break
            case "ArrowRight":
            case "ArrowDown":
                if (this.handleArrowDown()) {
                    event.preventDefault()
                }
                break
            case "ArrowLeft":
                if (this.handleArrowLeft()) {
                    event.preventDefault()
                }
                break
            case "ArrowUp":
                if (this.handleArrowUp()) {
                    event.preventDefault()
                }
                break
            default:
                break
        }
    }

    handleActivation(event) {
        event.preventDefault()
        const node = this.getNode()
        const dialog = new _dialogs__rspack_import_1.ContributorDialog(node, this.view)
        dialog.init()
    }

    handleArrowLeft() {
        const node = this.getNode()
        if (node.nodeSize > 2) {
            // At least one contributor
            const startPos = this.getPos() + node.nodeSize - 2
            this.view.dispatch(
                this.view.state.tr.setSelection(
                    prosemirror_state__rspack_import_3.NodeSelection.create(this.view.state.doc, startPos)
                )
            )
            this.view.focus()
            return true
        } else {
            // There is no tag
            return this.handleArrowUp()
        }
    }

    handleArrowUp() {
        // We jump to the section before this one.
        const startPos = this.getPos()

        const newSelection = (0,_helpers__rspack_import_2.nextSelection)(this.view.state, startPos, -1)

        if (!newSelection) {
            return false
        }

        this.view.dispatch(this.view.state.tr.setSelection(newSelection))
        this.view.focus()
        return true
    }

    handleArrowDown() {
        // Move the cursor beyond the contributors part
        const node = this.getNode()
        const pos = this.getPos() + node.nodeSize + 1

        const newSelection = (0,_helpers__rspack_import_2.nextSelection)(this.view.state, pos, 1)

        if (!newSelection) {
            return false
        }

        this.view.dispatch(this.view.state.tr.setSelection(newSelection))
        this.view.focus()
        return true
    }

    hasFocus() {
        return this.button === window.document.activeElement
    }

    focus() {
        if (this.button) {
            this.button.focus()
        }
    }
}


}),
"./js/modules/editor/state_plugins/contributor_input/dropup.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  createDropUp: function() { return createDropUp; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _dialogs__rspack_import_1 = __webpack_require__("./js/modules/editor/dialogs/index.js");



/** @typedef {{text: string, className: string, action: Function}} MenuOption */

/**
 * Contributor drop-up menu widget
 * Shows a menu above the selected contributor with options like "Edit"
 * Supports keyboard navigation and mouse interactions
 */
class ContributorDropUp {
    /**
     * @param {Object} selection - ProseMirror selection object
     * @param {Object} view - ProseMirror editor view
     * @param {Array<MenuOption>} [menuOptions] - Optional custom menu options
     */
    constructor(selection, view, menuOptions = null) {
        this.selection = selection
        this.view = view
        this.parentNode = selection.$anchor.parent
        this.focusedIndex = -1
        this.menuOptions = menuOptions || this.getDefaultMenuOptions()
        // Flag to prevent blur handler from closing drop-up when opening dialog
        this.openingDialog = false
        this.dropUp = this.createDropUp()
        this.setupEventHandlers()
    }

    /**
     * Get default menu options for contributor drop-up
     * @returns {Array<MenuOption>} Default menu options
     */
    getDefaultMenuOptions() {
        return [
            {
                text: gettext("Edit"),
                className: "edit-contributor",
                action: () => this.openContributorDialog()
            }
        ]
    }

    /**
     * Create the drop-up DOM element
     * @returns {HTMLElement} The drop-up element
     */
    createDropUp() {
        const dropUp = document.createElement("span")
        const requiredPx = 120

        dropUp.classList.add("drop-up-outer")

        // Generate HTML for each menu option
        const optionsHTML = this.menuOptions
            .map(
                (option, index) =>
                    `<li class="drop-up-option ${option.className}"
                       data-index="${index}"
                       tabindex="-1">${(0,_common__rspack_import_0.escapeText)(option.text)}</li>`
            )
            .join("")

        dropUp.innerHTML = (0,_common__rspack_import_0.noSpaceTmp)`
            <div class="link drop-up-inner" style="top: -${requiredPx}px;">
                <div class="drop-up-head">
                    <div>${(0,_common__rspack_import_0.escapeText)(this.parentNode.attrs.item_title)}</div>
                </div>
                <ul class="drop-up-options">
                    ${optionsHTML}
                </ul>
            </div>`

        return dropUp
    }

    /**
     * Set up all event handlers for the drop-up
     */
    setupEventHandlers() {
        const optionsList = this.dropUp.querySelector(".drop-up-options")
        const options = this.dropUp.querySelectorAll(".drop-up-option")

        // Make the options list focusable so it can receive keyboard events
        optionsList.setAttribute("tabindex", "0")

        // Setup handlers for each option
        options.forEach((option, index) => {
            // Mouse enter for visual highlight
            option.addEventListener("mouseenter", () => {
                this.focusOption(index)
            })

            // Mouse leave to remove highlight
            option.addEventListener("mouseleave", () => {
                this.unfocusOption(index)
            })

            // Click to activate the option
            option.addEventListener("click", event => {
                event.preventDefault()
                this.activateOption(index)
            })

            // Mousedown to prevent blur issues and stop propagation
            option.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopPropagation()
            })
        })

        // Keyboard navigation for the options list
        optionsList.addEventListener("keydown", event => {
            this.handleKeyDown(event)
        })

        // Focus handler - focus first option when list gets focus
        optionsList.addEventListener("focus", () => {
            this.focusOption(0)
        })

        // Blur handler to close the drop-up
        optionsList.addEventListener("blur", () => {
            // Use requestAnimationFrame to check if focus moved outside drop-up
            requestAnimationFrame(() => {
                // If we're opening a dialog, don't close (prevents deselecting contributor)
                if (this.openingDialog) {
                    this.openingDialog = false
                    return
                }
                // Close drop-up if focus moved outside it
                if (!this.dropUp.contains(document.activeElement)) {
                    this.closeDropUp()
                }
            })
        })

        // Prevent mousedown events on the drop-up from propagating to ProseMirror
        this.dropUp.addEventListener("mousedown", event => {
            event.stopPropagation()
        })
    }

    /**
     * Handle keyboard navigation events
     * @param {KeyboardEvent} event - Keyboard event
     */
    handleKeyDown(event) {
        const optionCount = this.menuOptions.length

        switch (event.key) {
            case "ArrowDown":
                event.preventDefault()
                event.stopPropagation()
                // If at last option and there's more than one option, close drop-up and return to contributor
                if (this.focusedIndex === optionCount - 1 && optionCount > 1) {
                    this.closeDropUp()
                    return
                }
                // Move to next option
                this.focusedIndex = (this.focusedIndex + 1) % optionCount
                this.updateFocusedOption()
                break

            case "ArrowUp":
                event.preventDefault()
                event.stopPropagation()
                // If at first option and there's more than one option, close drop-up and return to contributor
                if (this.focusedIndex === 0 && optionCount > 1) {
                    this.closeDropUp()
                    return
                }
                // Move to previous option
                this.focusedIndex =
                    (this.focusedIndex - 1 + optionCount) % optionCount
                this.updateFocusedOption()
                break

            case "Enter":
            case " ":
                event.preventDefault()
                if (this.focusedIndex >= 0) {
                    this.activateOption(this.focusedIndex)
                }
                break

            case "Escape":
                event.preventDefault()
                event.stopPropagation()
                this.closeDropUp()
                break

            case "Home":
                event.preventDefault()
                event.stopPropagation()
                this.focusOption(0)
                break

            case "End":
                event.preventDefault()
                event.stopPropagation()
                this.focusOption(optionCount - 1)
                break
        }
    }

    /**
     * Focus a specific option by index
     * @param {number} index - Index of the option to focus
     */
    focusOption(index) {
        const options = this.dropUp.querySelectorAll(".drop-up-option")
        // Unfocus previous option
        if (this.focusedIndex >= 0 && this.focusedIndex < options.length) {
            options[this.focusedIndex].classList.remove("focused")
        }
        // Focus new option
        this.focusedIndex = index
        if (options[index]) {
            options[index].classList.add("focused")
        }
    }

    /**
     * Unfocus a specific option by index (used for mouse leave)
     * @param {number} index - Index of the option to unfocus
     */
    unfocusOption(index) {
        const options = this.dropUp.querySelectorAll(".drop-up-option")
        if (options[index]) {
            options[index].classList.remove("focused")
        }
        // Only reset focusedIndex if this was the focused option
        if (this.focusedIndex === index) {
            this.focusedIndex = -1
        }
    }

    /**
     * Update visual state of the currently focused option
     */
    updateFocusedOption() {
        const options = this.dropUp.querySelectorAll(".drop-up-option")
        // Remove focused class from all options
        options.forEach(option => {
            option.classList.remove("focused")
        })
        // Add focused class to current option
        if (this.focusedIndex >= 0 && this.focusedIndex < options.length) {
            options[this.focusedIndex].classList.add("focused")
        }
    }

    /**
     * Activate a menu option by index
     * @param {number} index - Index of the option to activate
     */
    activateOption(index) {
        if (index >= 0 && index < this.menuOptions.length) {
            const option = this.menuOptions[index]
            if (option.action && typeof option.action === "function") {
                option.action()
            }
        }
    }

    /**
     * Open the contributor edit dialog
     */
    openContributorDialog() {
        // Set flag to prevent blur handler from closing drop-up or deselecting contributor
        this.openingDialog = true
        // Open the dialog - it will take focus from drop-up
        const dialog = new _dialogs__rspack_import_1.ContributorDialog(
            this.parentNode,
            this.view,
            this.selection.node.attrs
        )
        dialog.init()
    }

    /**
     * Close the drop-up by deselecting the contributor
     */
    closeDropUp() {
        // Set a text selection to deselect the contributor, which will remove the drop-up
        const tr = this.view.state.tr.setSelection(
            this.view.state.selection.constructor.near(
                this.view.state.doc.resolve(this.selection.from)
            )
        )
        this.view.dispatch(tr)
        this.view.focus()
    }

    /**
     * Get the DOM element for the drop-up
     * @returns {HTMLElement} The drop-up DOM element
     */
    getDOM() {
        return this.dropUp
    }
}

/**
 * Create a contributor drop-up menu
 * @param {Object} selection - ProseMirror selection object
 * @param {Object} view - ProseMirror editor view
 * @param {Array<MenuOption>} [menuOptions] - Optional custom menu options
 * @returns {HTMLElement} The drop-up DOM element
 */
const createDropUp = (selection, view, menuOptions = null) => {
    const dropUp = new ContributorDropUp(selection, view, menuOptions)
    return dropUp.getDOM()
}


}),
"./js/modules/editor/state_plugins/contributor_input/helpers.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  nextSelection: function() { return nextSelection; }
});
/* import */ var prosemirror_gapcursor__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");



const nextSelection = (state, pos, dir) => {
    let newSelection
    let newPos = pos
    let $newPos

    while (!newSelection) {
        newPos += dir
        if (newPos === 0 || newPos === state.doc.nodeSize) {
            // Could not find any valid position
            break
        }
        $newPos = state.doc.resolve(newPos)
        if ($newPos.parent.inlineContent) {
            newSelection = new prosemirror_state__rspack_import_0.TextSelection($newPos)
        } else if (prosemirror_gapcursor__rspack_import_1.GapCursor.valid($newPos)) {
            newSelection = new prosemirror_gapcursor__rspack_import_1.GapCursor($newPos)
        }
    }

    return newSelection
}


}),
"./js/modules/editor/state_plugins/contributor_input/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ContributorsPartView: function() { return /* reexport safe */ _node_view__rspack_import_1.ContributorsPartView; },
  contributorInputPlugin: function() { return /* reexport safe */ _plugin__rspack_import_0.contributorInputPlugin; }
});
/* import */ var _plugin__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/plugin.js");
/* import */ var _node_view__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/node_view.js");




}),
"./js/modules/editor/state_plugins/contributor_input/node_view.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ContributorsPartView: function() { return ContributorsPartView; }
});
/* import */ var w3c_keyname__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/w3c-keyname@2.2.8/node_modules/w3c-keyname/index.js");
/* import */ var _document_template__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/document_template.js");
/* import */ var _add_button__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/add_button.js");





class ContributorsPartView {
    constructor(node, view, getPos) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.dom = document.createElement("div")
        this.dom.classList.add("doc-part")
        this.dom.classList.add(`doc-${this.node.type.name}`)
        this.dom.classList.add(`doc-${this.node.attrs.id}`)
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = true
        }

        this.contentDOM = document.createElement("span")
        this.contentDOM.classList.add("contributors-inner")
        this.contentDOM.contentEditable = node.attrs.locking !== "fixed"
        this.dom.appendChild(this.contentDOM)
        if (node.attrs.locking !== "fixed") {
            this.addButton = new _add_button__rspack_import_2.AddButton(
                this.dom,
                () => this.getNode(),
                this.getPos,
                this.view
            )
            this.addButton.init()
        }

        if (node.attrs.deleted) {
            (0,_document_template__rspack_import_1.addDeletedPartWidget)(this.dom, view, getPos)
        }
    }

    stopEvent(event) {
        // Trap events for addButton
        if (["click", "mousedown"].includes(event.type)) {
            return false
        } else if (!this.addButton || this.node.attrs.locking === "fixed") {
            return false
        } else if (this.addButton.hasFocus() && event.type === "keydown") {
            return true
        } else {
            return false
        }
    }

    update(node, _decorations, _innerDecorations) {
        this.node = node
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = true
        } else {
            delete this.dom.dataset.hidden
        }
        return true
    }

    getNode() {
        return this.node
    }

    setSelection(anchor, head, _root) {
        if (anchor === head && this.view.hasFocus()) {
            // We must be in last position.
            // Activate the tag input tag editor.
            this.addButton.focus()
        }
    }

    ignoreMutation(_record) {
        if (this.addButton?.hasFocus()) {
            return true
        }
        return false
    }
}


}),
"./js/modules/editor/state_plugins/contributor_input/plugin.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  contributorInputPlugin: function() { return contributorInputPlugin; }
});
/* import */ var prosemirror_state__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _dialogs__rspack_import_0 = __webpack_require__("./js/modules/editor/dialogs/index.js");
/* import */ var _dropup__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/dropup.js");
/* import */ var _helpers__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/helpers.js");
/* import */ var _node_view__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/node_view.js");
/**
 * Contributor input plugin for ProseMirror editor
 *
 * Manages contributor selection, drop-up menu, and editing dialog
 * Features:
 * - Auto-select contributor nodes when navigating in contributors_part
 * - Show drop-up menu when contributor is selected
 * - Support keyboard navigation for contributors and drop-up menu
 * - Handle contributor addition/editing via dialog
 */








const key = new prosemirror_state__rspack_import_4.PluginKey("contributorInput")

/**
 * Create the contributor input plugin
 * @param {Object} options - Plugin options with editor reference
 * @returns {Plugin} ProseMirror plugin instance
 */
const contributorInputPlugin = options =>
    new prosemirror_state__rspack_import_4.Plugin({
        key,
        state: {
            /**
             * Initialize plugin state
             * @param {Object} _config - Plugin config (unused)
             * @param {Object} state - Initial ProseMirror state
             * @returns {Object} Initial plugin state
             */
            init(_config, state) {
                const decos = prosemirror_view__rspack_import_5.DecorationSet.empty
                if (options.editor.docInfo.access_rights === "write") {
                    this.spec.props.nodeViews["contributors_part"] = (
                        node,
                        view,
                        getPos
                    ) => new _node_view__rspack_import_3.ContributorsPartView(node, view, getPos)
                }

                // Find all contributors_part nodes in document to track their positions
                const contributorsPartPositions = []
                state.doc.descendants((node, pos) => {
                    if (node.type.name === "contributors_part") {
                        contributorsPartPositions.push({
                            start: pos,
                            end: pos + node.nodeSize
                        })
                    }
                })

                return {contributorsPartPositions, decos}
            },
            /**
             * Apply a transaction to plugin state
             * @param {Transaction} tr - Transaction being applied
             * @param {Object} prev - Previous plugin state
             * @param {Object} oldState - ProseMirror state before transaction
             * @param {Object} state - ProseMirror state after transaction
             * @returns {Object} New plugin state
             */
            apply(tr, prev, _oldState, state) {
                let {decos, contributorsPartPositions} = prev
                // If document was modified, update all positions
                if (tr.docChanged) {
                    contributorsPartPositions = contributorsPartPositions.map(
                        range => ({
                            start: tr.mapping.map(range.start),
                            end: tr.mapping.map(range.end)
                        })
                    )
                    decos = decos.map(tr.mapping, tr.doc)
                }
                if (options.editor.docInfo.access_rights !== "write") {
                    return {
                        contributorsPartPositions,
                        decos
                    }
                }
                if (tr.selectionSet) {
                    // Always remove any existing contributor drop-up before adding a new one
                    // This ensures only one drop-up is ever shown at any time
                    const oldDropUpDeco = decos.find(
                        null,
                        null,
                        spec => spec.id === "contributorDropUp"
                    )
                    if (oldDropUpDeco && oldDropUpDeco.length) {
                        decos = decos.remove(oldDropUpDeco)
                    }

                    if (
                        state.selection.jsonID === "node" &&
                        state.selection.node.type.name === "contributor" &&
                        state.selection.$anchor.node(1).attrs.locking !==
                            "fixed"
                    ) {
                        const dropUpDeco = prosemirror_view__rspack_import_5.Decoration.widget(
                            state.selection.from,
                            (0,_dropup__rspack_import_1.createDropUp)(state.selection, options.editor.view),
                            {
                                side: -1,
                                stopEvent: event => {
                                    // Get drop-up element to check if it exists in DOM
                                    const dropUpEl =
                                        document.querySelector(".drop-up-outer")

                                    if (!dropUpEl) {
                                        // Drop-up not in DOM, let events pass through
                                        return false
                                    }

                                    // Stop mouse events targeting the drop-up
                                    if (
                                        event.type === "mousedown" ||
                                        event.type === "click"
                                    ) {
                                        return event.target.closest(
                                            ".drop-up-outer"
                                        )
                                    }

                                    // Stop keyboard events that should be handled by the drop-up
                                    if (event.type === "keydown") {
                                        // Check if event target is within drop-up or if drop-up has focus
                                        const isTargetInDropUp =
                                            event.target.closest(
                                                ".drop-up-outer"
                                            )
                                        const dropUpHasFocus =
                                            dropUpEl.contains(
                                                document.activeElement
                                            )

                                        // Keys that should be handled by the drop-up
                                        const keysHandled = [
                                            "ArrowDown",
                                            "ArrowUp",
                                            "Enter",
                                            " ",
                                            "Escape",
                                            "Home",
                                            "End"
                                        ]

                                        // Arrow-Right and Arrow-Left should always pass through for contributor navigation
                                        if (
                                            event.key === "ArrowRight" ||
                                            event.key === "ArrowLeft"
                                        ) {
                                            return false
                                        }

                                        // Only stop the event if it's a handled key and either:
                                        // 1. The event target is within the drop-up, or
                                        // 2. The drop-up has focus
                                        if (
                                            keysHandled.includes(event.key) &&
                                            (isTargetInDropUp || dropUpHasFocus)
                                        ) {
                                            return true
                                        }
                                    }
                                    return false
                                },
                                id: "contributorDropUp"
                            }
                        )

                        decos = decos.add(state.doc, [dropUpDeco])
                    }
                }

                return {
                    contributorsPartPositions,
                    decos
                }
            }
        },
        props: {
            nodeViews: {},
            decorations(state) {
                const {decos} = this.getState(state)

                return decos
            },
            /**
             * Handle keyboard events
             * @param {EditorView} view - ProseMirror editor view
             * @param {KeyboardEvent} event - Keyboard event
             * @returns {boolean} True if event was handled
             */
            handleKeyDown(view, event) {
                const isContributorSelected =
                    view.state.selection.node?.type.name === "contributor"
                const dropUpEl = document.querySelector(".drop-up-options")
                const dropUpHasFocus =
                    dropUpEl && dropUpEl.contains(document.activeElement)

                // If drop-up is focused, let it handle all keyboard events
                if (dropUpHasFocus) {
                    return false
                }

                // Arrow-Up on a selected contributor should focus the drop-up menu
                if (event.key === "ArrowUp" && isContributorSelected) {
                    event.preventDefault()
                    if (dropUpEl) {
                        dropUpEl.focus()
                    }
                    return true
                }

                // Space key on a selected contributor opens the dialog (when drop-up not focused)
                if (event.key === " " && isContributorSelected) {
                    const dialog = new _dialogs__rspack_import_0.ContributorDialog(
                        view.state.selection.$anchor.parent,
                        view,
                        view.state.selection.node.attrs
                    )
                    dialog.init()
                    return true
                }

                // For all other cases, let ProseMirror handle the key
                return false
            }
        },
        /**
         * Append additional transactions based on selection changes
         * Handles auto-selection of contributor nodes when navigating in contributors_part
         * @param {Array<Transaction>} trs - Transactions being applied
         * @param {Object} oldState - State before transactions
         * @param {Object} newState - State after transactions
         * @returns {Transaction|null} Additional transaction to apply
         */
        appendTransaction: (trs, oldState, newState) => {
            // If selection is not collapsed or not changed, don't do anything
            if (
                newState.selection.from !== newState.selection.to ||
                !trs.some(tr => tr.selectionSet)
            ) {
                return
            }

            const selectionPos = newState.selection.from
            const pluginState = key.getState(newState)

            // Check if selection is within any contributors_part node to handle contributor navigation
            const contributorsPartRange =
                pluginState.contributorsPartPositions.find(
                    range =>
                        selectionPos > range.start && selectionPos < range.end
                )

            if (contributorsPartRange) {
                const oldSelectionPos = oldState.selection.from

                if (selectionPos + 1 === contributorsPartRange.end) {
                    // Selection is at end of contributors_part.
                    // Put caret onto contributor add button if write access is present.
                    // Otherwise, move caret beyond contributors_part.
                    if (options.editor.docInfo.access_rights === "write") {
                        // contributor add button will be activated by node view.
                        return
                    }
                    if (oldSelectionPos < selectionPos) {
                        const newSelection = (0,_helpers__rspack_import_2.nextSelection)(
                            newState,
                            contributorsPartRange.end,
                            1
                        )
                        if (!newSelection) {
                            // Cannot find a location. Give up.
                            return
                        }

                        return newState.tr.setSelection(newSelection)
                    }
                }

                const selectedNodePos =
                    oldSelectionPos < selectionPos
                        ? selectionPos
                        : selectionPos - 1

                if (selectedNodePos === contributorsPartRange.start) {
                    // selection is at start of contributors node. Find previous possible selection location.
                    const newSelection = (0,_helpers__rspack_import_2.nextSelection)(
                        newState,
                        selectedNodePos,
                        -1
                    )
                    if (!newSelection) {
                        // Cannot find a location. Give up.
                        return
                    }
                    return newState.tr.setSelection(newSelection)
                } else {
                    // Select an entire contributor node
                    return newState.tr.setSelection(
                        prosemirror_state__rspack_import_4.NodeSelection.create(newState.doc, selectedNodePos)
                    )
                }
            }

            return null
        }
    })


}),
"./js/modules/editor/state_plugins/document_template.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  PartView: function() { return PartView; },
  addDeletedPartWidget: function() { return addDeletedPartWidget; },
  checkProtectedInSelection: function() { return checkProtectedInSelection; },
  documentTemplatePlugin: function() { return documentTemplatePlugin; },
  getAllowedElementsAndMarks: function() { return getAllowedElementsAndMarks; },
  getProtectedRanges: function() { return getProtectedRanges; }
});
/* import */ var prosemirror_model__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");



const key = new prosemirror_state__rspack_import_0.PluginKey("documentTemplate")

function addDeletedPartWidget(dom, view, getPos) {
    dom.classList.add("doc-deleted")
    dom.insertAdjacentHTML(
        "beforeend",
        '<div class="remove-doc-part"><i class="fa fa-trash-alt"></i></div>'
    )
    const removeButton = dom.lastElementChild
    removeButton.addEventListener("click", () => {
        const from = getPos(),
            to = from + view.state.doc.nodeAt(from).nodeSize,
            tr = view.state.tr
        tr.delete(from, to)
        tr.setMeta("deleteUnusedSection", true)
        view.dispatch(tr)
    })
}
function getProtectedRanges(state) {
    return key.getState(state).protectedRanges
}

function getAllowedElementsAndMarks(state) {
    // Get the allowed elements and marks at the current selection position
    const {$anchor} = state.selection
    const docPart = $anchor.node(1) // Get the part node (richtext_part, heading_part, etc.)

    if (!docPart) {
        return {elements: false, marks: false}
    }

    const allowedElements = docPart.attrs.elements
        ? docPart.attrs.elements.concat(
              "table_caption",
              "table_body",
              "table_row",
              "table_cell",
              "table_header",
              "list_item",
              "text"
          )
        : false

    const allowedMarks = docPart.attrs.marks
        ? docPart.attrs.marks.concat(
              "insertion",
              "deletion",
              "comment",
              "anchor"
          )
        : false

    return {elements: allowedElements, marks: allowedMarks}
}

function checkProtectedInSelection(state) {
    // Checks whether there is a protected range
    // within a selection
    const anchorDocPart = state.selection.$anchor.node(1),
        headDocPart = state.selection.$head.node(1)

    // If the protection is of header/start check if selection falls within the
    // protected range
    if (
        ["start", "header"].includes(anchorDocPart.attrs.locking) ||
        ["start", "header"].includes(headDocPart.attrs.locking)
    ) {
        const protectedRanges = key.getState(state).protectedRanges,
            start = state.selection.from,
            end = state.selection.to
        if (
            protectedRanges.find(
                ({from, to}) =>
                    !(
                        (start <= from && end <= from) ||
                        (start >= to && end >= to)
                    )
            )
        ) {
            return true
        }
    }
    return (
        anchorDocPart.attrs.locking === "fixed" ||
        headDocPart.attrs.locking === "fixed"
    )
}

class PartView {
    constructor(node, view, getPos) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.dom = document.createElement("div")
        this.dom.classList.add("doc-part")
        this.dom.classList.add(`doc-${this.node.type.name}`)
        this.dom.classList.add(`doc-${this.node.attrs.id}`)
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = true
        }
        if (node.attrs.deleted) {
            this.contentDOM = this.dom.appendChild(
                document.createElement("div")
            )
            addDeletedPartWidget(this.dom, view, getPos)
        } else {
            this.contentDOM = this.dom
        }
    }

    stopEvent() {
        return false
    }
}

const documentTemplatePlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init(_config, state) {
                if (options.editor.docInfo.access_rights === "write") {
                    this.spec.props.nodeViews["richtext_part"] = (
                        node,
                        view,
                        getPos
                    ) => new PartView(node, view, getPos)
                    this.spec.props.nodeViews["heading_part"] = (
                        node,
                        view,
                        getPos
                    ) => new PartView(node, view, getPos)
                    this.spec.props.nodeViews["table_part"] = (
                        node,
                        view,
                        getPos
                    ) => new PartView(node, view, getPos)
                    // Tags and Contributors have node views defined in tag_input and contributor_input.
                    // TOCs have node views defined in toc_render.
                }

                const protectedRanges = [
                    {from: 0, to: 1} // article node
                ]
                state.doc.forEach((node, pos) => {
                    const from = pos
                    let to = from
                    if (node.attrs.locking === "fixed") {
                        to = from + node.nodeSize
                    } else if (node.attrs.locking === "header") {
                        // only relevant for tables
                        to =
                            from +
                            1 +
                            1 +
                            1 +
                            node.firstChild.firstChild.nodeSize // + 1 for the part node + 1 for the table + 1 for the first row
                    } else if (node.attrs.locking === "start") {
                        let initialFragment = prosemirror_model__rspack_import_1.Fragment.fromJSON(
                            options.editor.schema,
                            node.attrs.initial
                        )
                        let protectionSize = initialFragment.size
                        if (initialFragment.lastChild?.isTextblock) {
                            protectionSize -= 1 // We allow writing at the end of the last text block.
                            if (initialFragment.lastChild.nodeSize === 2) {
                                // The last text block is empty, so we remove all protection from it, even node type
                                protectionSize -= 1
                            }
                            initialFragment = initialFragment.cut(
                                0,
                                protectionSize
                            )
                        }
                        if (
                            node.content.size >= protectionSize &&
                            initialFragment.eq(
                                node.slice(0, protectionSize).content
                            )
                        ) {
                            // We only add protection if the start of the current content corresponds to the
                            // initial content. This may not be the case if the template has been changed.
                            to = from + 1 + protectionSize // + 1 for inside the part node
                        }
                    }
                    protectedRanges.push({from, to})
                })

                return {
                    protectedRanges
                }
            },
            apply(tr, _prev, oldState, _state) {
                let {protectedRanges} = this.getState(oldState)
                protectedRanges = protectedRanges.map(marker => ({
                    from: tr.mapping.map(marker.from, 1),
                    to: tr.mapping.map(marker.to, -1)
                }))
                return {
                    protectedRanges
                }
            }
        },
        props: {
            nodeViews: {}
        },
        filterTransaction: (tr, state) => {
            if (
                !tr.docChanged ||
                tr.getMeta("fixIds") ||
                tr.getMeta("remote") ||
                tr.getMeta("track") ||
                tr.getMeta("fromFootnote") ||
                tr.getMeta("deleteUnusedSection") ||
                tr.getMeta("settings") ||
                ["historyUndo", "historyRedo"].includes(tr.getMeta("inputType"))
            ) {
                return true
            }
            if (state.doc.childCount !== tr.doc.childCount) {
                return false
            }
            const {protectedRanges} = key.getState(state)
            let allowed = true

            let changingRanges = []

            // We map all changes back to the document before changes have been applied.
            tr.steps
                .slice()
                .reverse()
                .forEach(step => {
                    const map = step.getMap()
                    if (changingRanges.length) {
                        const mapInv = map.invert()
                        changingRanges = changingRanges.map(range => ({
                            start: mapInv.map(range.start, -1),
                            end: mapInv.map(range.end, 1)
                        }))
                    }
                    if (["removeMark", "addMark"].includes(step.jsonID)) {
                        changingRanges.push({start: step.from, end: step.to})
                    }
                    map.forEach((start, end) => {
                        changingRanges.push({start, end})
                    })
                })
            changingRanges.forEach(({start, end}) => {
                if (
                    protectedRanges.find(
                        ({from, to}) =>
                            !(
                                (start <= from && end <= from) ||
                                (start >= to && end >= to)
                            )
                    )
                ) {
                    allowed = false
                }
            })

            let allowedElements = false,
                allowedMarks = false

            changingRanges.forEach(range =>
                state.doc.nodesBetween(
                    range.from,
                    range.to,
                    (node, pos, parent, _index) => {
                        if (parent === tr.doc) {
                            allowedElements = node.attrs.elements
                                ? node.attrs.elements.concat(
                                      "table_caption",
                                      "table_body",
                                      "table_row",
                                      "table_cell",
                                      "table_header",
                                      "list_item",
                                      "text"
                                  )
                                : false
                            allowedMarks = node.attrs.marks
                                ? node.attrs.marks.concat(
                                      "insertion",
                                      "deletion",
                                      "comment",
                                      "anchor"
                                  )
                                : false
                            return allowed
                        }
                        if (pos < range.from) {
                            return true
                        }
                        if (
                            allowedElements &&
                            !allowedElements.includes(node.type.name)
                        ) {
                            allowed = false
                        } else if (allowedMarks) {
                            node.marks.forEach(mark => {
                                if (!allowedMarks.includes(mark.type.name)) {
                                    allowed = false
                                }
                            })
                        }
                    }
                )
            )

            return allowed
        }
    })


}),
"./js/modules/editor/state_plugins/figure.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  figurePlugin: function() { return figurePlugin; }
});
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("figureMenu")

class FigureView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options
        this.dom = document.createElement("div")
        this.dom.classList.add("figure")
        this.serializer = prosemirror_model__rspack_import_2.DOMSerializer.fromSchema(node.type.schema)
        const contentDOM = this.serializer.serializeNode(this.node)
        contentDOM.classList.forEach(className =>
            this.dom.classList.add(className)
        )
        contentDOM.classList.value = ""
        this.dom.appendChild(contentDOM)
        this.contentDOM = contentDOM
        this.menuButton = document.createElement("button")
        this.menuButton.classList.add("figure-menu-btn")
        this.menuButton.innerHTML =
            '<span class="dot-menu-icon"><i class="fa fa-ellipsis-v"></i></span>'
        this.dom.insertBefore(this.menuButton, this.dom.firstChild)
    }

    stopEvent(event) {
        let stopped = false
        if (event.type === "mousedown") {
            const composedPath = event.composedPath()
            if (composedPath.includes(this.menuButton)) {
                stopped = true
                const tr = this.view.state.tr
                const $pos = this.view.state.doc.resolve(this.getPos())
                tr.setSelection(new prosemirror_state__rspack_import_1.NodeSelection($pos))
                this.view.dispatch(tr)
                const contentMenu = new _common__rspack_import_0.ContentMenu({
                    menu: this.options.editor.menu.figureMenuModel,
                    width: 280,
                    page: this.options.editor,
                    menuPos: {
                        X: Number.parseInt(event.pageX) + 20,
                        Y: Number.parseInt(event.pageY) - 100
                    },
                    onClose: () => {
                        this.view.focus()
                    }
                })
                contentMenu.open()
            } else if (
                composedPath.includes(this.dom) &&
                !composedPath.find(el => el.matches && el.matches("figcaption"))
            ) {
                stopped = true
                const tr = this.view.state.tr
                const $pos = this.view.state.doc.resolve(this.getPos())
                tr.setSelection(new prosemirror_state__rspack_import_1.NodeSelection($pos))
                this.view.dispatch(tr)
            }
        }

        return stopped
    }
}

class FigureCaptionView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options

        this.dom = document.createElement("figcaption")
        this.dom.innerHTML = '<span class="text"></span>'
        this.contentDOM = this.dom.lastElementChild
    }
}

const figurePlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        state: {
            init(_config, _state) {
                if (options.editor.docInfo.access_rights === "write") {
                    this.spec.props.nodeViews["figure"] = (
                        node,
                        view,
                        getPos
                    ) => new FigureView(node, view, getPos, options)
                }
                this.spec.props.nodeViews["figure_caption"] = (
                    node,
                    view,
                    getPos
                ) => new FigureCaptionView(node, view, getPos, options)
                return {}
            },
            apply(_tr, prev) {
                return prev
            }
        },
        props: {
            nodeViews: {}
        }
    })


}),
"./js/modules/editor/state_plugins/footnote_markers.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  findFootnoteMarkers: function() { return findFootnoteMarkers; },
  footnoteMarkersPlugin: function() { return footnoteMarkersPlugin; },
  getFootnoteMarkerContents: function() { return getFootnoteMarkerContents; },
  getFootnoteMarkers: function() { return getFootnoteMarkers; },
  updateFootnoteMarker: function() { return updateFootnoteMarker; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");


const key = new prosemirror_state__rspack_import_0.PluginKey("footnoteMarkers")

const findFootnoteMarkers = (fromPos, toPos, doc) => {
    const footnoteMarkers = []
    doc.nodesBetween(fromPos, toPos, (node, pos) => {
        if (!node.isInline) {
            return
        }
        if (node.type.name === "footnote") {
            const from = pos
            const to = pos + node.nodeSize
            const footnoteMarker = {from, to}
            footnoteMarkers.push(footnoteMarker)
        }
    })
    return footnoteMarkers
}

const getAddedRanges = tr => {
    /* find ranges of the current document that have been added by means of
     * a transaction.
     */
    let ranges = []
    tr.steps.forEach((step, index) => {
        if (step.jsonID === "replace" || step.jsonID === "replaceWrap") {
            ranges.push({from: step.from, to: step.to})
        }
        const map = tr.mapping.maps[index]
        ranges = ranges.map(range => ({
            from: map.map(range.from, -1),
            to: map.map(range.to, 1)
        }))
    })

    const nonOverlappingRanges = []

    ranges.forEach(range => {
        let addedRange = false
        nonOverlappingRanges.forEach(noRange => {
            if (
                !addedRange &&
                range.from <= noRange.from &&
                range.to >= noRange.from
            ) {
                noRange.from = range.from
                noRange.to = noRange.to > range.to ? noRange.to : range.to
                addedRange = true
            } else if (
                !addedRange &&
                range.from <= noRange.to &&
                range.to >= noRange.to
            ) {
                noRange.from =
                    noRange.from < range.from ? noRange.from : range.from
                noRange.to = range.to
                addedRange = true
            }
        })
        if (!addedRange) {
            nonOverlappingRanges.push(range)
        }
    })

    return nonOverlappingRanges
}

const getFootnoteMarkerContents = state => {
    const fnState = key.getState(state)
    if (!fnState || !fnState.fnMarkers) {
        return []
    }
    const fnMarkers = fnState.fnMarkers
    return fnMarkers.map(
        fnMarker => state.doc.nodeAt(fnMarker.from).attrs.footnote
    )
}

const updateFootnoteMarker = (state, tr, index, content) => {
    const {fnMarkers} = key.getState(state)

    const footnote = fnMarkers[index]
    const node = state.doc.nodeAt(footnote.from)
    if (node.attrs.footnote === content) {
        return
    }
    tr.setNodeMarkup(footnote.from, node.type, {
        footnote: content
    })
    tr.setMeta("fromFootnote", true)
    return
}

const getFootnoteMarkers = state => {
    const {fnMarkers} = key.getState(state)
    return fnMarkers
}

const footnoteMarkersPlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init(_config, state) {
                const fnMarkers = []
                state.doc.descendants((node, pos) => {
                    if (node.type.name === "footnote") {
                        fnMarkers.push({
                            from: pos,
                            to: pos + node.nodeSize
                        })
                    }
                })

                return {
                    fnMarkers
                }
            },
            apply(tr, _prev, oldState, state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update of a footnote marker,
                    // return values from meta instead of previous values
                    // to prevent deletion of decoration
                    return meta
                }

                let {fnMarkers} = this.getState(oldState)

                if (!tr.docChanged) {
                    return {
                        fnMarkers
                    }
                }

                const remote = tr.getMeta("remote"),
                    fromFootnote = tr.getMeta("fromFootnote"),
                    ranges = getAddedRanges(tr),
                    deletedFootnotesIndexes = []
                fnMarkers = fnMarkers
                    .map(marker => ({
                        from: tr.mapping.map(marker.from, 1),
                        to: tr.mapping.map(marker.to, -1)
                    }))
                    .filter((marker, index) => {
                        if (marker.from !== marker.to - 1) {
                            // Add in reverse order as highest numbers need to be deleted
                            // first so that index numbers of lower numbers continue
                            // to be valid when these are deleted. Only relevant when
                            // several footnotes are deleted simultaneously.
                            deletedFootnotesIndexes.unshift(index)
                            return false
                        }
                        return true
                    })
                if (fromFootnote) {
                    return {fnMarkers}
                }
                const footTr =
                    options.editor.mod.footnotes.fnEditor.view.state.tr

                footTr.setMeta("fromMain", true)

                deletedFootnotesIndexes.forEach(index =>
                    options.editor.mod.footnotes.fnEditor.removeFootnote(
                        index,
                        footTr
                    )
                )
                ranges.forEach(range => {
                    let newFootnotes = findFootnoteMarkers(
                        range.from,
                        range.to,
                        tr.doc
                    )
                    if (newFootnotes.length) {
                        const firstFn = newFootnotes[0]
                        let offset = fnMarkers.findIndex(
                            marker => marker.from > firstFn.from
                        )
                        if (offset < 0) {
                            offset = fnMarkers.length
                        }
                        if (remote) {
                            newFootnotes = newFootnotes.filter(
                                // In case of remote trasnactions, we cannot mark them as coming from footnote, so we
                                // will need to remove duplicates instead.
                                newMarker =>
                                    fnMarkers.find(
                                        oldMarker =>
                                            oldMarker.from === newMarker.from
                                    )
                                        ? false
                                        : true
                            )
                        } else {
                            newFootnotes.forEach((footnote, index) => {
                                const fnContent = state.doc.nodeAt(
                                    footnote.from
                                ).attrs.footnote
                                options.editor.mod.footnotes.fnEditor.renderFootnote(
                                    fnContent,
                                    offset + index,
                                    footTr
                                )
                            })
                        }
                        fnMarkers = fnMarkers
                            .concat(newFootnotes)
                            .sort((a, b) => (a.from > b.from ? 1 : -1))
                    }
                })

                const footMeta = tr.getMeta("toFoot")

                if (footMeta) {
                    Object.entries(footMeta).forEach(([key, value]) => {
                        footTr.setMeta(key, value)
                    })
                }

                if (footTr.docChanged || footMeta) {
                    tr.setMeta("footTr", footTr)
                }

                return {
                    fnMarkers
                }
            }
        },
        view(_editorView) {
            return {
                update: (_view, _prevState) => {
                    options.editor.mod.footnotes.layout.updateDOM()
                }
            }
        }
    })


}),
"./js/modules/editor/state_plugins/headerbar.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  headerbarPlugin: function() { return headerbarPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _menus__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("header")
const headerbarPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        view(editorView) {
            return new _menus__rspack_import_0.HeaderbarView(editorView, options)
        }
    })


}),
"./js/modules/editor/state_plugins/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ContributorsPartView: function() { return /* reexport safe */ _contributor_input__rspack_import_1.ContributorsPartView; },
  TagsPartView: function() { return /* reexport safe */ _tag_input__rspack_import_19.TagsPartView; },
  accessRightsPlugin: function() { return /* reexport safe */ _access_rights__rspack_import_0.accessRightsPlugin; },
  addCommentDuringCreationDecoration: function() { return /* reexport safe */ _comments__rspack_import_5.addCommentDuringCreationDecoration; },
  checkProtectedInSelection: function() { return /* reexport safe */ _document_template__rspack_import_6.checkProtectedInSelection; },
  citationRenderPlugin: function() { return /* reexport safe */ _citation_render__rspack_import_2.citationRenderPlugin; },
  clipboardPlugin: function() { return /* reexport safe */ _clipboard__rspack_import_3.clipboardPlugin; },
  codeBlockPlugin: function() { return /* reexport safe */ _code_block__rspack_import_18.codeBlockPlugin; },
  collabCaretsPlugin: function() { return /* reexport safe */ _collab_carets__rspack_import_4.collabCaretsPlugin; },
  commentsPlugin: function() { return /* reexport safe */ _comments__rspack_import_5.commentsPlugin; },
  contributorInputPlugin: function() { return /* reexport safe */ _contributor_input__rspack_import_1.contributorInputPlugin; },
  deactivateAllSelectedChanges: function() { return /* reexport safe */ _track__rspack_import_22.deactivateAllSelectedChanges; },
  deselectSearchMatch: function() { return /* reexport safe */ _search__rspack_import_23.deselectSearchMatch; },
  documentTemplatePlugin: function() { return /* reexport safe */ _document_template__rspack_import_6.documentTemplatePlugin; },
  endSearch: function() { return /* reexport safe */ _search__rspack_import_23.endSearch; },
  figurePlugin: function() { return /* reexport safe */ _figure__rspack_import_17.figurePlugin; },
  findFootnoteMarkers: function() { return /* reexport safe */ _footnote_markers__rspack_import_7.findFootnoteMarkers; },
  footnoteMarkersPlugin: function() { return /* reexport safe */ _footnote_markers__rspack_import_7.footnoteMarkersPlugin; },
  getAllowedElementsAndMarks: function() { return /* reexport safe */ _document_template__rspack_import_6.getAllowedElementsAndMarks; },
  getCommentDuringCreationDecoration: function() { return /* reexport safe */ _comments__rspack_import_5.getCommentDuringCreationDecoration; },
  getFootnoteMarkerContents: function() { return /* reexport safe */ _footnote_markers__rspack_import_7.getFootnoteMarkerContents; },
  getFootnoteMarkers: function() { return /* reexport safe */ _footnote_markers__rspack_import_7.getFootnoteMarkers; },
  getInternalTargets: function() { return /* reexport safe */ _links__rspack_import_10.getInternalTargets; },
  getProtectedRanges: function() { return /* reexport safe */ _document_template__rspack_import_6.getProtectedRanges; },
  getSearchMatches: function() { return /* reexport safe */ _search__rspack_import_23.getSearchMatches; },
  getSelectedChanges: function() { return /* reexport safe */ _track__rspack_import_22.getSelectedChanges; },
  getSelectionUpdate: function() { return /* reexport safe */ _collab_carets__rspack_import_4.getSelectionUpdate; },
  headerbarPlugin: function() { return /* reexport safe */ _headerbar__rspack_import_8.headerbarPlugin; },
  jumpHiddenNodesPlugin: function() { return /* reexport safe */ _jump_hidden_nodes__rspack_import_9.jumpHiddenNodesPlugin; },
  linksPlugin: function() { return /* reexport safe */ _links__rspack_import_10.linksPlugin; },
  marginboxesPlugin: function() { return /* reexport safe */ _marginboxes__rspack_import_11.marginboxesPlugin; },
  orderedListMenuPlugin: function() { return /* reexport safe */ _ordered_list_menu__rspack_import_12.orderedListMenuPlugin; },
  placeholdersPlugin: function() { return /* reexport safe */ _placeholders__rspack_import_13.placeholdersPlugin; },
  removeCollaboratorSelection: function() { return /* reexport safe */ _collab_carets__rspack_import_4.removeCollaboratorSelection; },
  removeCommentDuringCreationDecoration: function() { return /* reexport safe */ _comments__rspack_import_5.removeCommentDuringCreationDecoration; },
  searchPlugin: function() { return /* reexport safe */ _search__rspack_import_23.searchPlugin; },
  selectNextSearchMatch: function() { return /* reexport safe */ _search__rspack_import_23.selectNextSearchMatch; },
  selectPreviousSearchMatch: function() { return /* reexport safe */ _search__rspack_import_23.selectPreviousSearchMatch; },
  selectionMenuPlugin: function() { return /* reexport safe */ _selection_menu__rspack_import_14.selectionMenuPlugin; },
  setSearchTerm: function() { return /* reexport safe */ _search__rspack_import_23.setSearchTerm; },
  setSelectedChanges: function() { return /* reexport safe */ _track__rspack_import_22.setSelectedChanges; },
  settingsPlugin: function() { return /* reexport safe */ _settings__rspack_import_15.settingsPlugin; },
  tablePlugin: function() { return /* reexport safe */ _table__rspack_import_16.tablePlugin; },
  tagInputPlugin: function() { return /* reexport safe */ _tag_input__rspack_import_19.tagInputPlugin; },
  tocRenderPlugin: function() { return /* reexport safe */ _toc_render__rspack_import_20.tocRenderPlugin; },
  toolbarPlugin: function() { return /* reexport safe */ _toolbar__rspack_import_21.toolbarPlugin; },
  trackPlugin: function() { return /* reexport safe */ _track__rspack_import_22.trackPlugin; },
  updateCollaboratorSelection: function() { return /* reexport safe */ _collab_carets__rspack_import_4.updateCollaboratorSelection; },
  updateFootnoteMarker: function() { return /* reexport safe */ _footnote_markers__rspack_import_7.updateFootnoteMarker; }
});
/* import */ var _access_rights__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/access_rights.js");
/* import */ var _contributor_input__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/contributor_input/index.js");
/* import */ var _citation_render__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/citation_render.js");
/* import */ var _clipboard__rspack_import_3 = __webpack_require__("./js/modules/editor/state_plugins/clipboard.js");
/* import */ var _collab_carets__rspack_import_4 = __webpack_require__("./js/modules/editor/state_plugins/collab_carets.js");
/* import */ var _comments__rspack_import_5 = __webpack_require__("./js/modules/editor/state_plugins/comments.js");
/* import */ var _document_template__rspack_import_6 = __webpack_require__("./js/modules/editor/state_plugins/document_template.js");
/* import */ var _footnote_markers__rspack_import_7 = __webpack_require__("./js/modules/editor/state_plugins/footnote_markers.js");
/* import */ var _headerbar__rspack_import_8 = __webpack_require__("./js/modules/editor/state_plugins/headerbar.js");
/* import */ var _jump_hidden_nodes__rspack_import_9 = __webpack_require__("./js/modules/editor/state_plugins/jump_hidden_nodes.js");
/* import */ var _links__rspack_import_10 = __webpack_require__("./js/modules/editor/state_plugins/links.js");
/* import */ var _marginboxes__rspack_import_11 = __webpack_require__("./js/modules/editor/state_plugins/marginboxes.js");
/* import */ var _ordered_list_menu__rspack_import_12 = __webpack_require__("./js/modules/editor/state_plugins/ordered_list_menu.js");
/* import */ var _placeholders__rspack_import_13 = __webpack_require__("./js/modules/editor/state_plugins/placeholders.js");
/* import */ var _selection_menu__rspack_import_14 = __webpack_require__("./js/modules/editor/state_plugins/selection_menu.js");
/* import */ var _settings__rspack_import_15 = __webpack_require__("./js/modules/editor/state_plugins/settings.js");
/* import */ var _table__rspack_import_16 = __webpack_require__("./js/modules/editor/state_plugins/table.js");
/* import */ var _figure__rspack_import_17 = __webpack_require__("./js/modules/editor/state_plugins/figure.js");
/* import */ var _code_block__rspack_import_18 = __webpack_require__("./js/modules/editor/state_plugins/code_block.js");
/* import */ var _tag_input__rspack_import_19 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/index.js");
/* import */ var _toc_render__rspack_import_20 = __webpack_require__("./js/modules/editor/state_plugins/toc_render.js");
/* import */ var _toolbar__rspack_import_21 = __webpack_require__("./js/modules/editor/state_plugins/toolbar.js");
/* import */ var _track__rspack_import_22 = __webpack_require__("./js/modules/editor/state_plugins/track/index.js");
/* import */ var _search__rspack_import_23 = __webpack_require__("./js/modules/editor/state_plugins/search.js");


























}),
"./js/modules/editor/state_plugins/jump_hidden_nodes.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  jumpHiddenNodesPlugin: function() { return jumpHiddenNodesPlugin; }
});
/* import */ var prosemirror_gapcursor__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");



// A plugin that makes sure that the selecion is not put into a node that has been
// hidden.

const posHidden = $pos => {
    let hidden = false
    for (let i = $pos.depth; i > 0; i--) {
        const node = $pos.node(i)
        if (
            node.attrs.hidden ||
            (["table_caption", "figure_caption"].includes(node.type.name) &&
                $pos.node(i - 1).attrs.caption === false)
        ) {
            hidden = true
        }
    }
    return hidden
}

const key = new prosemirror_state__rspack_import_0.PluginKey("jump-hidden-nodes")
const jumpHiddenNodesPlugin = _options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        appendTransaction: (trs, oldState, state) => {
            if (state.selection.from !== state.selection.to) {
                // Only applies to collapsed selection
                return
            }
            const selectionSet = trs.find(tr => tr.selectionSet)

            if (selectionSet && posHidden(state.selection.$from)) {
                const dir =
                    state.selection.from > oldState.selection.from ? 1 : -1
                let newPos = state.selection.from,
                    hidden = true,
                    validTextSelection = false,
                    validGapCursor = false,
                    $pos
                while (hidden || (!validGapCursor && !validTextSelection)) {
                    newPos += dir
                    if (newPos === 0 || newPos === state.doc.nodeSize) {
                        // Could not find any valid position
                        return
                    }
                    $pos = state.doc.resolve(newPos)
                    validTextSelection = $pos.parent.inlineContent
                    validGapCursor = prosemirror_gapcursor__rspack_import_1.GapCursor.valid($pos)
                    hidden = posHidden($pos)
                }
                const selection = validTextSelection
                    ? new prosemirror_state__rspack_import_0.TextSelection($pos)
                    : new prosemirror_gapcursor__rspack_import_1.GapCursor($pos)
                return state.tr.setSelection(selection)
            }
        }
    })


}),
"./js/modules/editor/state_plugins/links.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  getInternalTargets: function() { return getInternalTargets; },
  linksPlugin: function() { return linksPlugin; }
});
/* import */ var prosemirror_state__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var prosemirror_view__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_common__rspack_import_1 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _schema_i18n__rspack_import_2 = __webpack_require__("./js/modules/schema/i18n.js");
/* import */ var _dialogs__rspack_import_3 = __webpack_require__("./js/modules/editor/dialogs/index.js");









const key = new prosemirror_state__rspack_import_4.PluginKey("links")

const copyLink = href => {
    const textarea = document.createElement("textarea")
    textarea.textContent = href
    textarea.style.position = "fixed" // Prevent scrolling to bottom of page in MS Edge.
    document.body.appendChild(textarea)
    textarea.select()
    try {
        document.execCommand("copy") // Security exception may be thrown by some browsers.
        document.body.removeChild(textarea)
        ;(0,_common__rspack_import_0.addAlert)("info", gettext("Link copied to clipboard"))
    } catch (_ex) {
        (0,_common__rspack_import_0.addAlert)(
            "info",
            gettext("Copy to clipboard failed. Please copy manually.")
        )
    }
}

const nonDeletedTextContent = node => {
    let text = ""
    node.descendants(subNode => {
        if (
            subNode.isText &&
            !subNode.marks.find(mark => mark.type.name === "deletion")
        ) {
            text += subNode.text
        }
    })
    return text
}

const getInternalTargets = (state, language, editor) => {
    const internalTargets = []
    const anchors = {}
    const categories = {}

    state.doc.descendants(node => {
        if (node.attrs.track?.find(track => track.type === "deletion")) {
            return true
        }
        if (node.type.groups.includes("heading")) {
            const textContent = nonDeletedTextContent(node)
            if (textContent.length) {
                internalTargets.push({
                    id: node.attrs.id,
                    text: textContent
                })
            }
            return true
        }

        if (
            ["figure", "table"].includes(node.type.name) &&
            node.attrs.category &&
            node.attrs.category !== "none"
        ) {
            if (!categories[node.attrs.category]) {
                categories[node.attrs.category] = 0
            }
            categories[node.attrs.category]++

            internalTargets.push({
                id: node.attrs.id,
                text:
                    editor === "main"
                        ? `${_schema_i18n__rspack_import_2.CATS[node.attrs.category][language]} ${categories[node.attrs.category]}`
                        : `${_schema_i18n__rspack_import_2.CATS[node.attrs.category][language]} ${categories[node.attrs.category]}A`
            })
            return true
        }

        if (
            node.type.name === "code_block" &&
            node.attrs.category &&
            node.attrs.id
        ) {
            if (!categories[node.attrs.category]) {
                categories[node.attrs.category] = 0
            }
            categories[node.attrs.category]++

            const categoryLabel =
                _schema_i18n__rspack_import_2.CATS[node.attrs.category]?.[language] || node.attrs.category
            const text = node.attrs.title
                ? `${categoryLabel} ${categories[node.attrs.category]}: ${node.attrs.title}`
                : `${categoryLabel} ${categories[node.attrs.category]}`

            internalTargets.push({
                id: node.attrs.id,
                text: editor === "main" ? text : `${text}A`
            })
            return true
        }

        if (node.type.name === "text") {
            const anchor = node.marks.find(mark => mark.type.name === "anchor")
            if (anchor) {
                anchors[anchor.attrs.id] =
                    (anchors[anchor.attrs.id] || "") + node.text
            }
        }
    })
    Object.entries(anchors).forEach(([id, text]) =>
        internalTargets.push({id, text})
    )

    return internalTargets
}

const linksPlugin = options => {
    function getUrl(state, oldState, oldUrl) {
        const id = state.selection.$head.parent.attrs.id,
            mark = state.selection.$head
                .marks()
                .find(mark => mark.type.name === "anchor")
        let newUrl = oldUrl.split("#")[0]
        if (mark) {
            newUrl += `#${mark.attrs.id}`
        } else if (id) {
            newUrl += `#${id}`
        }
        const changed = oldUrl === newUrl ? false : true
        // TODO: Should the following be moved to a view?
        // Not sure if this counts as a DOM update.
        if (changed && options.editor.currentView.state === oldState) {
            window.history.replaceState("", "", newUrl)
        }
        return newUrl
    }

    function getLinkMark(state) {
        return state.selection.$head
            .marks()
            .find(mark => mark.type.name === "link")
    }

    function getAnchorMark(state) {
        return state.selection.$head
            .marks()
            .find(mark => mark.type.name === "anchor")
    }

    function getCrossReference(state) {
        return state.selection instanceof prosemirror_state__rspack_import_4.NodeSelection
            ? state.selection.node.type.name == "cross_reference"
                ? state.selection.node
                : undefined
            : undefined
    }

    function getDecos(state) {
        const $head = state.selection.$head
        const currentMarks = [],
            linkMark = $head.marks().find(mark => mark.type.name === "link"),
            anchorMark = $head.marks().find(mark => mark.type.name === "anchor")
        const crossRef =
            state.selection instanceof prosemirror_state__rspack_import_4.NodeSelection
                ? state.selection.node.type.name == "cross_reference"
                    ? state.selection.node
                    : undefined
                : undefined

        if (linkMark) {
            currentMarks.push(linkMark)
        }
        if (anchorMark) {
            currentMarks.push(anchorMark)
        }
        if (crossRef) {
            currentMarks.push(crossRef)
        }
        if (!currentMarks.length) {
            return prosemirror_view__rspack_import_5.DecorationSet.empty
        }

        let startPos = $head.start() // position of block start.
        if (crossRef) {
            // For cross ref , take the end pos of the node
            startPos = state.selection.from + crossRef.nodeSize
        } else {
            let index = $head.index()
            while (
                index < $head.parent.childCount - 1 &&
                currentMarks.some(mark =>
                    mark.isInSet($head.parent.child(index + 1).marks)
                )
            ) {
                index++
            }
            for (let i = 0; i <= index; i++) {
                startPos += $head.parent.child(i).nodeSize
            }
        }
        const dom = createDropUp(linkMark, anchorMark, crossRef, $head),
            deco = prosemirror_view__rspack_import_5.Decoration.widget(startPos, dom)
        return prosemirror_view__rspack_import_5.DecorationSet.create(state.doc, [deco])
    }

    function createDropUp(linkMark, anchorMark, crossRef, $head) {
        const dropUp = document.createElement("span"),
            editor = options.editor,
            writeAccess =
                editor.docInfo.access_rights === "write" ? true : false,
            editAccess = ["write", "write-tracked", "review-tracked"].includes(
                editor.docInfo.access_rights
            )
                ? true
                : false
        let linkType,
            linkHref,
            anchorHref,
            requiredPx = 10

        if (linkMark) {
            linkType = linkMark.attrs.href[0] === "#" ? "internal" : "external"
            linkHref =
                linkType === "internal"
                    ? window.location.href.split("#")[0] + linkMark.attrs.href
                    : linkMark.attrs.href
            requiredPx += 120
        }

        if (anchorMark) {
            anchorHref =
                window.location.href.split("#")[0] + "#" + anchorMark.attrs.id
            requiredPx += 92
        }

        dropUp.classList.add("drop-up-outer")

        dropUp.innerHTML = (0,_common__rspack_import_0.noSpaceTmp)`
            <div class="link drop-up-inner" style="top: -${requiredPx}px;">
                ${
                    linkMark
                        ? `<div class="drop-up-head">
                        ${
                            linkMark.attrs.title
                                ? `<div class="link-title">${gettext("Title")}:&nbsp;${linkMark.attrs.title}</div>`
                                : ""
                        }
                        <div class="link-href">
                            <a class="href${linkType === "internal" ? " internal" : ""}" ${linkType === "external" ? 'target="_blank"' : ""} href="${linkHref}">
            		            ${linkHref}
            		        </a>
                        </div>
                    </div>
                    <ul class="drop-up-options">
                        <li class="copy-link" title="${gettext("Copy link")}">
                            ${gettext("Copy link")}
                        </li>
                        ${
                            writeAccess
                                ? `<li class="edit-link" title="${gettext("Edit link")}">
                                ${gettext("Edit")}
                            </li>
                            <li class="remove-link" title="${gettext("Remove link")}">
                                ${gettext("Remove")}
                            </li>`
                                : ""
                        }
                    </ul>`
                        : ""
                }
                ${
                    anchorMark
                        ? `<div class="drop-up-head">
                        <div class="link-title">${gettext("Anchor")}</div>
                        <div class="link-href">
                        <a class="href" target="_blank" href="${anchorHref}">
                            ${anchorHref}
                        </a>
                        </div>
                    </div>
                    <ul class="drop-up-options">
                        <li class="copy-anchor" title="${gettext("Copy anchor")}">
                            ${gettext("Copy anchor")}
                        </li>
                        ${
                            writeAccess
                                ? `<li class="remove-anchor" title="${gettext("Remove anchor")}">
                                ${gettext("Remove")}
                            </li>`
                                : ""
                        }
                    </ul>`
                        : ""
                }
${
    crossRef
        ? `<div class="drop-up-head" ${editAccess ? "" : 'style="border-radius:6px;"'}>
                        <div class="link-title">${gettext("Cross Reference")}</div>
                        <div class="link-href">
                        <span>
                            ${crossRef.attrs.title ? crossRef.attrs.title : "Target Lost"}
                        </a>
                        </div>
                    </div>
                        ${
                            editAccess
                                ? `<ul class="drop-up-options">
        <li class="edit-crossRef" title="${gettext("Edit cross reference")}">
                                ${gettext("Edit")}
                            </li>
                            <li class="remove-crossRef" title="${gettext("Remove cross reference")}">
                                ${gettext("Remove")}
                            </li>
                            </ul>`
                                : ""
                        }
                    `
        : ""
}
            </div>`

        if (linkType === "internal") {
            const el = dropUp.querySelector("a.internal")
            el.addEventListener("click", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                options.editor.scrollIdIntoView(linkMark.attrs.href.slice(1))
            })
        }

        const copyLinkHref = dropUp.querySelector(".copy-link")
        if (copyLinkHref) {
            copyLinkHref.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                copyLink(linkHref)
            })
        }
        const copyAnchorHref = dropUp.querySelector(".copy-anchor")
        if (copyAnchorHref) {
            copyAnchorHref.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                copyLink(anchorHref)
            })
        }

        const editLink = dropUp.querySelector(".edit-link")
        if (editLink) {
            editLink.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                const dialog = new _dialogs__rspack_import_3.LinkDialog(editor)
                dialog.init()
            })
        }

        const editCrossRef = dropUp.querySelector(".edit-crossRef")
        if (editCrossRef) {
            editCrossRef.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                const dialog = new _dialogs__rspack_import_3.LinkDialog(editor)
                dialog.init()
            })
        }

        const removeLink = dropUp.querySelector(".remove-link")
        if (removeLink) {
            removeLink.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                editor.view.dispatch(
                    editor.view.state.tr.removeMark(
                        $head.start(),
                        $head.end(),
                        linkMark
                    )
                )
            })
        }

        const removeAnchor = dropUp.querySelector(".remove-anchor")
        if (removeAnchor) {
            removeAnchor.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                editor.view.dispatch(
                    editor.view.state.tr.removeMark(
                        $head.start(),
                        $head.end(),
                        anchorMark
                    )
                )
            })
        }

        const removeCrossRef = dropUp.querySelector(".remove-crossRef")
        if (removeCrossRef) {
            removeCrossRef.addEventListener("mousedown", event => {
                event.preventDefault()
                event.stopImmediatePropagation()
                editor.view.dispatch(
                    editor.view.state.tr.delete($head.pos - 1, $head.pos)
                )
            })
        }
        return dropUp
    }

    return new prosemirror_state__rspack_import_4.Plugin({
        key,
        state: {
            init() {
                return {
                    url: window.location.href,
                    decos: prosemirror_view__rspack_import_5.DecorationSet.empty,
                    linkMark: false
                }
            },
            apply(tr, _prev, oldState, state) {
                let {url, decos, linkMark, anchorMark, crossReference} =
                    this.getState(oldState)
                url = getUrl(state, oldState, url)
                const newLinkMark = getLinkMark(state)
                const newAnchorMark = getAnchorMark(state)
                const newCrossReference = getCrossReference(state)
                if (
                    newLinkMark === linkMark &&
                    newAnchorMark === anchorMark &&
                    newCrossReference === crossReference
                ) {
                    decos = decos.map(tr.mapping, tr.doc)
                } else {
                    decos = getDecos(state)
                    linkMark = newLinkMark
                    anchorMark = newAnchorMark
                    crossReference = newCrossReference
                }
                if (!tr.getMeta("remote")) {
                    // We look for changes to figures or headings.
                    let foundIdElement = false // found heading or figure
                    let ranges = []
                    tr.steps.forEach((step, index) => {
                        ranges.push([step.from, step.to])
                        tr.docs[index].nodesBetween(
                            step.from,
                            step.to,
                            node => {
                                if (
                                    node.type.groups.includes("heading") ||
                                    node.type.name === "figure"
                                ) {
                                    foundIdElement = true
                                }
                            }
                        )
                        ranges = ranges.map(range => {
                            return [
                                tr.mapping.maps[index].map(range[0], -1),
                                tr.mapping.maps[index].map(range[1], 1)
                            ]
                        })
                    })
                    let foundAnchorWithoutId = false // found an anchor without an ID
                    ranges.forEach(range => {
                        state.doc.nodesBetween(range[0], range[1], node => {
                            if (
                                !foundIdElement &&
                                (node.type.groups.includes("heading") ||
                                    [
                                        "figure",
                                        "table",
                                        "bullet_list",
                                        "ordered_list"
                                    ].includes(node.type.name))
                            ) {
                                foundIdElement = true
                            }
                            if (!foundAnchorWithoutId) {
                                node.marks.forEach(mark => {
                                    if (
                                        mark.type.name === "anchor" &&
                                        !mark.attrs.id
                                    ) {
                                        foundAnchorWithoutId = true
                                    }
                                })
                            }
                        })
                    })

                    if (foundIdElement || foundAnchorWithoutId) {
                        const linkUpdate = {foundAnchorWithoutId}
                        tr.setMeta("linkUpdate", linkUpdate)
                        if (
                            oldState.schema === options.editor.view.state.schema
                        ) {
                            tr.setMeta("toFoot", {linkUpdate: true})
                        } else {
                            tr.setMeta("toMain", {linkUpdate: true})
                        }
                    }
                }

                return {
                    url,
                    decos,
                    linkMark,
                    anchorMark,
                    crossReference
                }
            }
        },
        appendTransaction: (trs, oldState, newState) => {
            // Check if any of the transactions are local.
            if (trs.every(tr => !tr.getMeta("linkUpdate"))) {
                // All transactions are remote or don't change anything. Give up.
                return
            }

            const foundAnchorWithoutId = trs.find(tr => {
                const linkUpdate = tr.getMeta("linkUpdate")
                return linkUpdate && linkUpdate.foundAnchorWithoutId
            })
            // ID should not be found in the other pm either. So we look through
            // those as well.
            let otherState, language
            if (oldState.schema === options.editor.view.state.schema) {
                otherState = options.editor.mod.footnotes.fnEditor.view.state
                language = newState.doc.attrs.language
            } else {
                otherState = options.editor.view.state
                language = options.editor.view.state.doc.attrs.language
            }

            const internalTargets = getInternalTargets(
                newState,
                language,
                oldState.schema === options.editor.view.state.schema
                    ? "main"
                    : "foot"
            ).concat(
                getInternalTargets(
                    otherState,
                    language,
                    oldState.schema === options.editor.view.state.schema
                        ? "foot"
                        : "main"
                )
            )

            // Check if there are any headings or figures in the affected range.
            // Otherwise, skip.

            // Check that unique IDs only exist once in the document and that the
            // text values are up to date for all IDs if they are referenced.
            //
            // If an ID is used more than once, add steps to change the ID of all
            // but the first occurence.
            const ids = []

            otherState.doc.descendants(node => {
                if (
                    node.type.groups.includes("heading") ||
                    ["figure", "table", "bullet_list", "ordered_list"].includes(
                        node.type.name
                    )
                ) {
                    ids.push(node.attrs.id)
                }
            })

            const newTr = newState.tr.setMeta("fixIds", true)

            newState.doc.descendants((node, pos) => {
                if (
                    node.type.groups.includes("heading") ||
                    ["figure", "table", "bullet_list", "ordered_list"].includes(
                        node.type.name
                    )
                ) {
                    if (ids.includes(node.attrs.id) || !node.attrs.id) {
                        // Add node if the id is false (default) or it is present twice
                        const randomIdGenerator = node.type.groups.includes(
                            "heading"
                        )
                            ? _schema_common__rspack_import_1.randomHeadingId
                            : node.type.name === "figure"
                              ? _schema_common__rspack_import_1.randomFigureId
                              : node.type.name === "table"
                                ? _schema_common__rspack_import_1.randomTableId
                                : _schema_common__rspack_import_1.randomListId
                        let id

                        while (!id || ids.includes(id)) {
                            id = randomIdGenerator()
                        }

                        const attrs = Object.assign({}, node.attrs, {id})

                        // Because we only change attributes, positions should stay the
                        // the same throughout all our extra steps. We therefore do no
                        // mapping of positions through these steps.
                        newTr.setNodeMarkup(pos, null, attrs)

                        ids.push(id)
                    } else {
                        ids.push(node.attrs.id)
                    }
                } else if (
                    node.type.name === "cross_reference" &&
                    !internalTargets.find(
                        it =>
                            it.id === node.attrs.id &&
                            it.text === node.attrs.title
                    )
                ) {
                    const iTarget = internalTargets.find(
                        it => it.id === node.attrs.id
                    )
                    const attrs = Object.assign({}, node.attrs, {
                        title: iTarget ? iTarget.text : null
                    })
                    newTr.setNodeMarkup(pos, null, attrs)
                }
                node.marks.forEach(mark => {
                    if (
                        mark.type.name === "link" &&
                        mark.attrs.href[0] === "#" &&
                        !internalTargets.find(
                            it =>
                                it.id === mark.attrs.href.slice(1) &&
                                it.text === node.attrs.title
                        )
                    ) {
                        const iTarget = internalTargets.find(
                            it => it.id === mark.attrs.href.slice(1)
                        )
                        const attrs = Object.assign({}, mark.attrs, {
                            title: iTarget ? iTarget.text : null
                        })
                        newTr.addMark(
                            pos,
                            pos + node.nodeSize,
                            newState.schema.marks.link.create(attrs)
                        )
                    }
                })
            })

            // Remove anchor marks without ID
            if (foundAnchorWithoutId) {
                const markType = newState.schema.marks.anchor.create({
                    id: false
                })
                newTr.step(
                    new prosemirror_transform__rspack_import_6.RemoveMarkStep(0, newState.doc.content.size, markType)
                )
            }

            return newTr
        },
        props: {
            handleDOMEvents: {
                focus: (view, _event) => {
                    const {url} = key.getState(view.state)
                    window.history.replaceState("", "", url)
                }
            },
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        }
    })
}


}),
"./js/modules/editor/state_plugins/marginboxes.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  marginboxesPlugin: function() { return marginboxesPlugin; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");


const key = new prosemirror_state__rspack_import_0.PluginKey("marginboxes")
const marginboxesPlugin = options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        view(_editorState) {
            return {
                update: (view, _prevState) => {
                    options.editor.mod.marginboxes.view(view)
                }
            }
        }
    })


}),
"./js/modules/editor/state_plugins/ordered_list_menu.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  orderedListMenuPlugin: function() { return orderedListMenuPlugin; }
});
/* import */ var prosemirror_state__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");




const key = new prosemirror_state__rspack_import_2.PluginKey("tableMenu")

class OrderedListView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options
        this.dom = document.createElement("div")
        this.dom.classList.add("content-container")
        this.dom.id = node.attrs.id
        this.menuButton = document.createElement("button")
        this.menuButton.classList.add("content-menu-btn")
        this.menuButton.innerHTML =
            '<span class="dot-menu-icon"><i class="fa fa-ellipsis-v"></i></span>'
        this.dom.appendChild(this.menuButton)
        const orderedList = document.createElement("ol")
        if (node.attrs.order !== 1) {
            orderedList.start = node.attrs.order
        }
        if (node.attrs.track?.length) {
            orderedList.dataset.track = JSON.stringify(node.attrs.track)
        }
        this.contentDOM = this.dom.appendChild(orderedList)
        this.dom.appendChild(this.contentDOM)
    }

    stopEvent(event) {
        let stopped = false
        if (
            event.type === "mousedown" &&
            event.composedPath().includes(this.menuButton)
        ) {
            stopped = true
            const tr = this.view.state.tr
            const $pos = this.view.state.doc.resolve(this.getPos())
            tr.setSelection(prosemirror_state__rspack_import_2.Selection.findFrom($pos, 1, true))
            this.view.dispatch(tr)
            const contentMenu = new _common__rspack_import_1.ContentMenu({
                menu: this.options.editor.menu.orderedListMenuModel,
                width: 280,
                page: this.options.editor,
                menuPos: {
                    X: Number.parseInt(event.pageX) + 20,
                    Y: Number.parseInt(event.pageY) - 100
                },
                onClose: () => {
                    this.view.focus()
                }
            })
            contentMenu.open()
        }
        return stopped
    }
}

const orderedListMenuPlugin = options =>
    new prosemirror_state__rspack_import_2.Plugin({
        key,
        state: {
            init(_config, _state) {
                if (
                    ___rspack_import_0.WRITE_ROLES.includes(options.editor.docInfo.access_rights)
                ) {
                    this.spec.props.nodeViews["ordered_list"] = (
                        node,
                        view,
                        getPos
                    ) => new OrderedListView(node, view, getPos, options)
                }
                return {}
            },
            apply(_tr, prev) {
                return prev
            }
        },
        props: {
            nodeViews: {}
        }
    })


}),
"./js/modules/editor/state_plugins/placeholders.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  placeholdersPlugin: function() { return placeholdersPlugin; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");



const key = new prosemirror_state__rspack_import_0.PluginKey("placeholders")
const placeholdersPlugin = options => {
    function calculatePlaceHolderDecorations(state) {
        const anchor = state.selection.$anchor
        const head = state.selection.$head
        if (!anchor || !head) {
            return
        }
        const anchorPart = anchor.node(1)
        const headPart = head.node(1)
        if (!anchorPart || !headPart) {
            return
        }
        const currentPart = anchorPart === headPart ? anchorPart : false

        const decorations = []

        state.doc.forEach((partElement, offset) => {
            if (
                (partElement.isTextblock && partElement.childCount === 0) ||
                (!partElement.isTextblock && partElement.nodeSize === 4)
            ) {
                if (
                    [
                        state.schema.nodes["tags_part"],
                        state.schema.nodes["contributors_part"]
                    ].includes(partElement.type) &&
                    options.editor.docInfo.access_rights === "write"
                ) {
                    // We don't need to render placeholders for these kinds
                    // of nodes in write mode as their nodeviews will take
                    // care of that.
                    return
                }

                const text =
                    partElement.type === state.schema.nodes["title"]
                        ? `${gettext("Title")}...`
                        : `${partElement.attrs.title}...`
                const placeHolder = document.createElement("span")
                placeHolder.classList.add("placeholder")
                placeHolder.setAttribute("data-placeholder", text)
                if (currentPart === partElement) {
                    placeHolder.classList.add("selected")
                }
                let position = 1 + offset
                // position of decorator: 2 to get inside (doc (1))
                if (!partElement.isTextblock) {
                    // In block nodes that are not text blocks (body + abstract)
                    // place inside the first child node (a paragraph).
                    position += 1
                }
                decorations.push(
                    prosemirror_view__rspack_import_1.Decoration.widget(position, placeHolder, {
                        side: 1
                    })
                )
            } else if (
                ["richtext_part", "table_part"].includes(partElement.type.name)
            ) {
                partElement.descendants((node, pos) => {
                    if (
                        ["figure", "table"].includes(node.type.name) &&
                        !node.attrs.caption
                    ) {
                        return false
                    }
                    if (
                        ["figure_caption", "table_caption"].includes(
                            node.type.name
                        ) &&
                        node.childCount === 0 &&
                        state.selection.$anchor.parent !== node
                    ) {
                        decorations.push(
                            prosemirror_view__rspack_import_1.Decoration.node(
                                1 + offset + pos,
                                1 + offset + pos + node.nodeSize,
                                {
                                    class: "empty",
                                    "data-placeholder": `${gettext("Caption")}...`
                                }
                            )
                        )
                    }
                })
            }
        })

        return decorations.length
            ? prosemirror_view__rspack_import_1.DecorationSet.create(state.doc, decorations)
            : false
    }

    return new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init(_config, state) {
                return {
                    decos: calculatePlaceHolderDecorations(state)
                }
            },
            apply(tr, _prev, oldState, state) {
                let {decos} = this.getState(oldState)
                if (tr.docChanged || tr.selectionSet) {
                    decos = calculatePlaceHolderDecorations(state)
                }
                return {
                    decos
                }
            }
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        }
    })
}


}),
"./js/modules/editor/state_plugins/search.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  deselectSearchMatch: function() { return deselectSearchMatch; },
  endSearch: function() { return endSearch; },
  getSearchMatches: function() { return getSearchMatches; },
  searchPlugin: function() { return searchPlugin; },
  selectNextSearchMatch: function() { return selectNextSearchMatch; },
  selectPreviousSearchMatch: function() { return selectPreviousSearchMatch; },
  setSearchTerm: function() { return setSearchTerm; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");



const key = new prosemirror_state__rspack_import_0.PluginKey("search")

function findMatches(doc, term) {
    let stringObj = false,
        matches = []

    if (!term.length) {
        return matches
    }

    doc.descendants((node, pos, parent) => {
        if (
            !node.isInline ||
            node.marks.find(mark => mark.type.name === "deletion")
        ) {
            return
        }
        if (stringObj && (parent !== stringObj.parent || !node.isText)) {
            matches = matches.concat(findTerm(term, stringObj))
            stringObj = false
        }

        if (node.isText) {
            if (!stringObj) {
                stringObj = {parent, pos: [], text: ""}
            }
            stringObj.text += node.text
            stringObj.pos.push([pos, pos + node.text.length])
        }
    })
    if (stringObj) {
        matches = matches.concat(findTerm(term, stringObj))
    }
    return matches
}

// Find search term within stringObjects (strings that consist of several text nodes that hang together)
function findTerm(term, stringObj) {
    const matches = []
    let index = 0,
        foundIndex
    while ((foundIndex = stringObj.text.indexOf(term, index)) !== -1) {
        index = foundIndex + term.length

        matches.push({
            from: transPos(foundIndex, stringObj.pos),
            to: transPos(foundIndex + term.length, stringObj.pos)
        })
    }
    return matches
}

// Translate the start and end position of the serach term within the strings to document positions
function transPos(index, pos) {
    let findIndex = index,
        posIndex = 0
    while (findIndex > pos[posIndex][1] - pos[posIndex][0]) {
        findIndex -= pos[posIndex][1] - pos[posIndex][0]
        posIndex++
    }
    return pos[posIndex][0] + findIndex
}

function matchesToDecos(doc, matches, selected) {
    if (!matches.length) {
        return prosemirror_view__rspack_import_1.DecorationSet.empty
    }
    const decorations = matches.map((match, index) => {
        return prosemirror_view__rspack_import_1.Decoration.inline(match.from, match.to, {
            class: `search${index === selected ? " selected" : ""}`
        })
    })
    return prosemirror_view__rspack_import_1.DecorationSet.create(doc, decorations)
}

const setSearchTerm = (
    state,
    term,
    selected = false,
    listener = false
) => {
    const matches = findMatches(state.doc, term),
        decos = matchesToDecos(state.doc, matches, selected)

    selected =
        selected !== false && matches.length > selected
            ? selected
            : matches.length
              ? matches.length - 1
              : false

    const tr = state.tr.setMeta(key, {
        term,
        decos,
        matches,
        selected,
        listener
    })

    return {tr, matches, selected}
}

const endSearch = state =>
    state.tr.setMeta(key, {
        term: "",
        decos: prosemirror_view__rspack_import_1.DecorationSet.empty,
        matches: [],
        selected: 0,
        listener: false
    })

const selectNextSearchMatch = state => {
    const pluginState = key.getState(state),
        {term, matches, listener} = pluginState
    let {selected} = pluginState

    if (selected === false) {
        selected = matches.length
    }
    if (selected < matches.length - 1) {
        selected++
    } else {
        selected = 0
    }
    const decos = matchesToDecos(state.doc, matches, selected)
    return state.tr.setMeta(key, {
        term,
        decos,
        matches,
        selected,
        listener
    })
}

const selectPreviousSearchMatch = state => {
    const pluginState = key.getState(state),
        {term, matches, listener} = pluginState
    let {selected} = pluginState

    if (selected === false) {
        selected = 0
    }
    if (selected > 0) {
        selected--
    } else {
        selected = matches.length - 1
    }
    const decos = matchesToDecos(state.doc, matches, selected)
    return state.tr.setMeta(key, {
        term,
        decos,
        matches,
        selected,
        listener
    })
}

const deselectSearchMatch = state => {
    const {term, matches, listener} = key.getState(state),
        selected = false,
        decos = matchesToDecos(state.doc, matches, selected)
    return state.tr.setMeta(key, {
        term,
        decos,
        matches,
        selected,
        listener
    })
}

const getSearchMatches = state => {
    const {matches, selected} = key.getState(state)
    return {matches, selected}
}

const searchPlugin = _options =>
    new prosemirror_state__rspack_import_0.Plugin({
        key,
        state: {
            init() {
                return {
                    term: "",
                    decos: prosemirror_view__rspack_import_1.DecorationSet.empty,
                    matches: [],
                    selected: 0,
                    listener: false
                }
            },
            apply(tr, _prev, oldState, state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }

                const pluginState = this.getState(oldState),
                    {term, listener} = pluginState
                let {matches, decos, selected} = pluginState

                if (term === "" || !tr.docChanged) {
                    return {
                        term,
                        decos, // empty if term === ''
                        matches, // empty if term === ''
                        selected, // 0 if term === ''
                        listener // false is dialog not open
                    }
                }

                // The document is changing while the search window is open. We redo the search for the entire doc.
                // TODO: Optimize for speed by only recalculating the part of the doc that was changed.
                matches = findMatches(state.doc, term)
                if (selected !== false && selected >= matches.length) {
                    if (matches.length) {
                        selected = matches.length - 1
                    } else {
                        selected = false
                    }
                }
                decos = matchesToDecos(state.doc, matches, selected)

                return {
                    term,
                    decos,
                    matches,
                    selected,
                    listener
                }
            }
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            }
        },
        view(_editorState) {
            return {
                update: (view, _prevState) => {
                    const {listener} = key.getState(view.state)
                    if (listener) {
                        listener.onUpdate()
                    }
                }
            }
        }
    })


}),
"./js/modules/editor/state_plugins/selection_menu.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  selectionMenuPlugin: function() { return selectionMenuPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _menus__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("toolbar")
const selectionMenuPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        view(editorView) {
            return new _menus__rspack_import_0.SelectionMenuView(editorView, options)
        }
    })


}),
"./js/modules/editor/state_plugins/settings.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  settingsPlugin: function() { return settingsPlugin; }
});
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_const__rspack_import_1 = __webpack_require__("./js/modules/schema/const.js");
/* import */ var _schema_i18n__rspack_import_2 = __webpack_require__("./js/modules/schema/i18n.js");






const key = new prosemirror_state__rspack_import_3.PluginKey("settings")

const settingsPlugin = options => {
    const fixSettings = settings => {
        const fixedSettings = Object.assign({}, settings)
        let changed = false

        Object.keys(settings).forEach(key => {
            const value = settings[key]
            switch (key) {
                case "documentstyle":
                    if (
                        !options.editor.mod.documentTemplate.documentStyles.find(
                            d => d.slug === value
                        ) &&
                        options.editor.mod.documentTemplate.documentStyles
                            .length
                    ) {
                        fixedSettings[key] =
                            options.editor.mod.documentTemplate.documentStyles[0].slug
                        changed = true
                    }
                    break
                case "citationstyle":
                    if (
                        !settings.citationstyles.includes(value) &&
                        settings.citationstyles.length
                    ) {
                        fixedSettings[key] = settings.citationstyles[0]
                        changed = true
                    }
                    break
            }
        })
        if (changed) {
            return fixedSettings
        } else {
            return false
        }
    }

    const updateSettings = (newSettings, oldSettings) => {
        let settingsValid = true
        Object.keys(newSettings).forEach(key => {
            const newValue = newSettings[key]
            if (oldSettings[key] !== newValue) {
                switch (key) {
                    case "documentstyle":
                        if (newValue.length) {
                            updateDocStyleCSS(newValue)
                        } else {
                            settingsValid = false
                        }
                        break
                    case "citationstyle":
                        if (newValue.length) {
                            options.editor.mod.citations.resetCitations()
                        } else {
                            settingsValid = false
                        }
                        break
                    case "language":
                        if (newValue.length) {
                            const lang = _schema_const__rspack_import_1.LANGUAGES.find(
                                lang => lang[0] === newValue
                            )
                            document
                                .querySelectorAll(".ProseMirror")
                                .forEach(el => (el.dir = lang[2]))
                            options.editor.docInfo.dir = lang[2]
                            updateLanguageCSS(newValue)
                        } else {
                            settingsValid = false
                        }
                        break
                }
            }
        })
        return settingsValid
    }

    /** Update the stylesheet used for the docStyle
     */
    const updateDocStyleCSS = docStyleId => {
        const docStyle =
            options.editor.mod.documentTemplate.documentStyles.find(
                doc_style => doc_style.slug === docStyleId
            ) ||
            (options.editor.mod.documentTemplate.documentStyles.length
                ? options.editor.mod.documentTemplate.documentStyles[0]
                : {contents: "", documentstylefile_set: []})

        let docStyleCSS = docStyle.contents
        docStyle.documentstylefile_set.forEach(
            ([url, filename]) =>
                (docStyleCSS = docStyleCSS.replace(
                    new RegExp(filename, "g"),
                    url
                ))
        )

        let docStyleEl = document.getElementById("document-style")

        if (!docStyleEl) {
            docStyleEl = document.createElement("style")
            docStyleEl.id = "document-style"
            document.body.appendChild(docStyleEl)
        }

        docStyleEl.innerHTML = docStyleCSS

        // TODO: Find a way that is more reliable than a timeout to check
        // for font loading.
        window.setTimeout(() => {
            options.editor.mod.marginboxes.updateDOM()
            options.editor.mod.footnotes.layout.updateDOM()
        }, 250)
    }

    const updateLanguageCSS = language => {
        let langStyleEl = document.getElementById("language-style")
        if (!langStyleEl) {
            langStyleEl = document.createElement("style")
            langStyleEl.id = "language-style"
            document.body.appendChild(langStyleEl)
        }

        langStyleEl.innerHTML = `
        /* Numbering in editor */

        figure[data-category='figure'] figcaption::before {
            counter-increment: cat-figure;
            content: '${_schema_i18n__rspack_import_2.CATS.figure[language]} ' counter(cat-figure);
        }

        #footnote-box-container figure[data-category='figure'] figcaption::before {
            content: '${_schema_i18n__rspack_import_2.CATS.figure[language]} ' counter(cat-figure) 'A';
        }

        figure[data-category='equation'] figcaption::before {
            counter-increment: cat-equation;
            content: '${_schema_i18n__rspack_import_2.CATS.equation[language]} ' counter(cat-equation);
        }

        #footnote-box-container figure[data-category='euqation'] figcaption::before {
            content: '${_schema_i18n__rspack_import_2.CATS.equation[language]} ' counter(cat-equation) 'A';
        }

        figure[data-category='photo'] figcaption::before {
            counter-increment: cat-photo;
            content: '${_schema_i18n__rspack_import_2.CATS.photo[language]} ' counter(cat-photo);
        }

        #footnote-box-container figure[data-category='photo'] figcaption::before {
            content: '${_schema_i18n__rspack_import_2.CATS.photo[language]} ' counter(cat-photo) 'A';
        }

        figure[data-category='table'] figcaption::before,
        table[data-category='table'] caption::before {
            counter-increment: cat-table;
            content: '${_schema_i18n__rspack_import_2.CATS.table[language]} ' counter(cat-table);
        }

        #footnote-box-container figure[data-category='table'] figcaption::before ,
        #footnote-box-container table[data-category='table'] caption::before {
            content: '${_schema_i18n__rspack_import_2.CATS.table[language]} ' counter(cat-table) 'A';
        }`
    }

    return new prosemirror_state__rspack_import_3.Plugin({
        key,
        appendTransaction(trs, _oldState, newState) {
            // Ensure that there are always settings set.
            if (trs.every(tr => tr.getMeta("remote") || tr.from > 0)) {
                // All transactions are remote. Give up.
                return false
            }
            const lastTr = trs[trs.length - 1]
            const attrs = lastTr.doc.attrs
            const fixedSettings = fixSettings(attrs)

            if (!fixedSettings) {
                return false
            }

            const tr = newState.tr
            Object.entries(fixedSettings).forEach(([key, value]) => {
                tr.setDocAttribute(key, value)
            })
            tr.setMeta("settings", true)

            return tr
        },
        view(view) {
            if (!updateSettings(view.state.doc.attrs, {})) {
                setTimeout(() => {
                    const tr = view.state.tr
                    const fixedSettings = fixSettings(view.state.doc.attrs)
                    Object.entries(fixedSettings).forEach(([key, value]) => {
                        tr.setDocAttribute(key, value)
                    })
                    tr.setMeta("settings", true)
                    view.dispatch(tr)
                }, 0)
            }
            return {
                update: (view, prevState) => {
                    updateSettings(view.state.doc.attrs, prevState.doc.attrs)
                    ;(0,_common__rspack_import_0.setDocTitle)(
                        view.state.doc.firstChild.textContent,
                        options.editor.app
                    )
                }
            }
        }
    })
}


}),
"./js/modules/editor/state_plugins/table.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  tablePlugin: function() { return tablePlugin; }
});
/* import */ var prosemirror_state__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");




const key = new prosemirror_state__rspack_import_2.PluginKey("table")

class TableView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options
        this.dom = document.createElement("div")
        this.dom.classList.add(
            `table-${node.attrs.width}`,
            `table-${node.attrs.aligned}`,
            "content-container"
        )
        this.dom.id = node.attrs.id
        this.menuButton = document.createElement("button")
        this.menuButton.classList.add("content-menu-btn")
        this.menuButton.innerHTML =
            '<span class="dot-menu-icon"><i class="fa fa-ellipsis-v"></i></span>'
        this.dom.appendChild(this.menuButton)
        const dom = document.createElement("table")
        if (node.attrs.track.length) {
            dom.dataset.track = JSON.stringify(node.attrs.track)
        }
        dom.id = node.attrs.id
        dom.dataset.width = node.attrs.width
        dom.dataset.aligned = node.attrs.aligned
        dom.dataset.layout = node.attrs.layout
        dom.class = `table-${node.attrs.width} table-${node.attrs.aligned} table-${node.attrs.layout}`
        dom.dataset.category = node.attrs.category
        if (!node.attrs.caption) {
            dom.dataset.captionHidden = true
        }
        this.contentDOM = dom
        this.dom.appendChild(this.contentDOM)
    }

    stopEvent(event) {
        let stopped = false
        if (
            event.type === "mousedown" &&
            event.composedPath().includes(this.menuButton)
        ) {
            stopped = true
            if (!isSelectedTableClicked(this.view.state, this.getPos())) {
                const tr = this.view.state.tr
                const $pos = this.view.state.doc.resolve(this.getPos())
                tr.setSelection(prosemirror_state__rspack_import_2.Selection.findFrom($pos, 1, true))
                this.view.dispatch(tr)
            }
            const contentMenu = new _common__rspack_import_1.ContentMenu({
                menu: this.options.editor.menu.tableMenuModel,
                width: 280,
                page: this.options.editor,
                menuPos: {
                    X: Number.parseInt(event.pageX) + 20,
                    Y: Number.parseInt(event.pageY) - 100
                },
                onClose: () => {
                    this.view.focus()
                }
            })
            contentMenu.open()
        }
        return stopped
    }
}

class TableCaptionView {
    constructor(node, view, getPos, options) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.options = options

        this.dom = document.createElement("caption")
        this.dom.innerHTML = '<span class="text"></span>'
        this.contentDOM = this.dom.lastElementChild
    }
}

const isSelectedTableClicked = (state, $pos) => {
    const pathArr = state.selection.$head.path
    for (let i = 0; i < pathArr.length; i++) {
        if (
            pathArr[i].type &&
            pathArr[i].type.name &&
            pathArr[i].type.name === "table" &&
            pathArr[i - 1] === $pos
        ) {
            return true
        }
    }
    return false
}

const tablePlugin = options =>
    new prosemirror_state__rspack_import_2.Plugin({
        key,
        state: {
            init(_config, _state) {
                if (
                    ___rspack_import_0.WRITE_ROLES.includes(options.editor.docInfo.access_rights)
                ) {
                    this.spec.props.nodeViews["table"] = (node, view, getPos) =>
                        new TableView(node, view, getPos, options)
                }
                this.spec.props.nodeViews["table_caption"] = (
                    node,
                    view,
                    getPos
                ) => new TableCaptionView(node, view, getPos, options)

                return {}
            },
            apply(_tr, prev) {
                return prev
            }
        },
        props: {
            nodeViews: {}
        }
    })


}),
"./js/modules/editor/state_plugins/tag_input/helpers.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  nextSelection: function() { return nextSelection; },
  submitTag: function() { return submitTag; }
});
/* import */ var prosemirror_gapcursor__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-gapcursor@1.3.2/node_modules/prosemirror-gapcursor/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");



const nextSelection = (state, pos, dir) => {
    let newSelection
    let newPos = pos
    let $newPos

    while (!newSelection) {
        newPos += dir
        if (newPos === 0 || newPos === state.doc.nodeSize) {
            // Could not find any valid position
            break
        }
        $newPos = state.doc.resolve(newPos)
        if ($newPos.parent.inlineContent) {
            newSelection = new prosemirror_state__rspack_import_0.TextSelection($newPos)
        } else if (prosemirror_gapcursor__rspack_import_1.GapCursor.valid($newPos)) {
            newSelection = new prosemirror_gapcursor__rspack_import_1.GapCursor($newPos)
        }
    }

    return newSelection
}

const submitTag = (tagInputView, view, getPos) => {
    const tagState = tagInputView.state
    const selectionTo = tagState.selection.to
    const tag =
        selectionTo > 1
            ? tagState.doc.textBetween(1, selectionTo)
            : tagState.doc.textContent
    if (tag.length) {
        try {
            const eState = view.state,
                startPos = getPos()

            // Validate that the position is still valid
            if (
                startPos === null ||
                startPos === undefined ||
                startPos < 0 ||
                startPos >= eState.doc.content.size
            ) {
                // Position is out of range, node was likely removed or moved
                return
            }

            const nodeAtPos = view.state.doc.nodeAt(startPos)
            if (!nodeAtPos) {
                // Node no longer exists at this position
                return
            }

            const pos = startPos + nodeAtPos.nodeSize - 1,
                node = eState.schema.nodes.tag.create({tag})
            view.dispatch(view.state.tr.insert(pos, node))
            tagInputView.dispatch(
                tagState.tr.delete(
                    1,
                    selectionTo > 1 ? selectionTo : tagState.doc.nodeSize - 3
                )
            )
        } catch (_e) {
            // If there's any error during tag submission (e.g., position became invalid),
            // silently fail to avoid disrupting the user's workflow
        }
    }
}


}),
"./js/modules/editor/state_plugins/tag_input/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  TagsPartView: function() { return /* reexport safe */ _node_view__rspack_import_1.TagsPartView; },
  tagInputPlugin: function() { return /* reexport safe */ _plugin__rspack_import_0.tagInputPlugin; }
});
/* import */ var _plugin__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/plugin.js");
/* import */ var _node_view__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/node_view.js");




}),
"./js/modules/editor/state_plugins/tag_input/node_view.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  TagsPartView: function() { return TagsPartView; }
});
/* import */ var _document_template__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/document_template.js");
/* import */ var _plugin__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/plugin.js");
/* import */ var _tag_editor__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/tag_editor.js");







class TagsPartView {
    constructor(node, view, getPos) {
        this.node = node
        this.view = view
        this.getPos = getPos
        this.dom = document.createElement("div")
        this.dom.classList.add("doc-part")
        this.dom.classList.add(`doc-${this.node.type.name}`)
        this.dom.classList.add(`doc-${this.node.attrs.id}`)
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = true
        }

        this.contentDOM = document.createElement("span")
        this.contentDOM.classList.add("tags-inner")
        this.contentDOM.contentEditable = node.attrs.locking !== "fixed"
        this.dom.appendChild(this.contentDOM)
        if (node.attrs.locking !== "fixed") {
            const [tagInputDOM, tagInputView] = (0,_tag_editor__rspack_import_2.createTagEditor)(
                view,
                getPos,
                () => this.getNode()
            )
            this.tagInputView = tagInputView
            this.dom.appendChild(tagInputDOM)
        }

        if (node.attrs.deleted) {
            (0,_document_template__rspack_import_0.addDeletedPartWidget)(this.dom, view, getPos)
        }
    }

    stopEvent(event) {
        // Trap events for tagInputView
        if (["click", "mousedown"].includes(event.type)) {
            return false
        } else if (!this.tagInputView || this.node.attrs.locking === "fixed") {
            return false
        } else if (event.type === "keydown" && this.tagInputView.hasFocus()) {
            return true
        } else {
            return false
        }
    }

    update(node, _decorations, _innerDecorations) {
        this.node = node
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = true
        } else {
            delete this.dom.dataset.hidden
        }
        return true
    }

    getNode() {
        return this.node
    }

    setSelection(anchor, head, _root) {
        if (anchor === head && this.view.hasFocus()) {
            // Check if we should prevent refocusing (e.g., user just clicked on a tag)
            if ((0,_plugin__rspack_import_1.shouldPreventTagInputFocus)()) {
                return
            }
            // We must be in last position.
            // Activate the tag input tag editor.
            this.tagInputView.focus()
        }
    }

    ignoreMutation(_record) {
        if (this.tagInputView?.hasFocus()) {
            return true
        }
        return false
    }
}


}),
"./js/modules/editor/state_plugins/tag_input/plugin.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  shouldPreventTagInputFocus: function() { return shouldPreventTagInputFocus; },
  tagInputPlugin: function() { return tagInputPlugin; }
});
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _helpers__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/helpers.js");
/* import */ var _node_view__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/node_view.js");
/* import */ var _tag_editor__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/tag_editor.js");







const key = new prosemirror_state__rspack_import_3.PluginKey("tagInput")

// Track mouse click state across transactions using closure variable
let mouseClickActive = false
let mouseClickTimer = null
// Track when we've explicitly blurred the tag input to prevent refocus
let tagInputJustBlurred = false

// Export function to check if we should prevent tag input focus
const shouldPreventTagInputFocus = () => tagInputJustBlurred

const tagInputPlugin = options =>
    new prosemirror_state__rspack_import_3.Plugin({
        key,
        state: {
            init(_config, state) {
                if (options.editor.docInfo.access_rights === "write") {
                    this.spec.props.nodeViews["tags_part"] = (
                        node,
                        view,
                        getPos
                    ) => new _node_view__rspack_import_1.TagsPartView(node, view, getPos)
                }

                // Find all tags_part nodes in the document
                const tagsPartPositions = []
                state.doc.descendants((node, pos) => {
                    if (node.type.name === "tags_part") {
                        tagsPartPositions.push({
                            start: pos,
                            end: pos + node.nodeSize
                        })
                    }
                })

                return {tagsPartPositions}
            },
            apply(tr, prev) {
                // If the document was modified, update all positions
                if (tr.docChanged) {
                    const newPositions = prev.tagsPartPositions.map(range => ({
                        start: tr.mapping.map(range.start),
                        end: tr.mapping.map(range.end)
                    }))

                    return {tagsPartPositions: newPositions}
                }
                return prev
            }
        },
        props: {
            nodeViews: {},
            handleDOMEvents: {
                mousedown(view, event) {
                    // Mark that this selection change came from a mouse click
                    // Set the closure variable to indicate a mouse click is happening
                    mouseClickActive = true

                    // Clear any existing timer
                    if (mouseClickTimer) {
                        clearTimeout(mouseClickTimer)
                    }

                    // Reset after a delay to allow all related transactions to process
                    // Including blur handlers and any delayed events
                    mouseClickTimer = setTimeout(() => {
                        mouseClickActive = false
                        mouseClickTimer = null
                    }, 100)

                    // Check if we're clicking outside a focused tag input
                    const activeElement = document.activeElement
                    // Check if active element is inside a tag-input container
                    const tagInputContainer =
                        activeElement?.closest(".tag-input")
                    const clickedInsideTagInput =
                        event.target.closest(".tag-input")
                    // Check if clicking on a tag within the tags_part (but not the tag input)
                    const clickedOnTag =
                        event.target.closest(".tag") ||
                        event.target.closest(".doc-tags_part")

                    // If a tag input has focus and we're clicking outside it (including on existing tags), submit and blur it
                    if (
                        tagInputContainer &&
                        (!clickedInsideTagInput || clickedOnTag)
                    ) {
                        // Get the stored references from WeakMap
                        const refs = _tag_editor__rspack_import_2.tagInputReferences.get(tagInputContainer)
                        if (refs) {
                            const {tagInputView, mainView, getPos} = refs

                            // Submit the tag immediately with the valid position
                            ;(0,_helpers__rspack_import_0.submitTag)(tagInputView, mainView, getPos)

                            // If we clicked outside the tags_part entirely, set selection to clicked position
                            // If we clicked on a tag, let the normal click handling select it
                            if (!clickedOnTag) {
                                // Now set the selection based on where the user clicked
                                // We need to do this synchronously before appendTransaction runs
                                const clickPos = view.posAtCoords({
                                    left: event.clientX,
                                    top: event.clientY
                                })
                                if (clickPos) {
                                    const tr = view.state.tr.setSelection(
                                        prosemirror_state__rspack_import_3.TextSelection.create(
                                            view.state.doc,
                                            clickPos.pos
                                        )
                                    )
                                    view.dispatch(tr)
                                    view.focus()
                                }
                            } else {
                                // Blur the tag input first, then focus the main view

                                // Set flag to prevent tag input from being refocused
                                tagInputJustBlurred = true

                                // Blur the tag input
                                tagInputView.dom.blur()

                                // Find the position of the clicked tag and select it
                                const clickPos = view.posAtCoords({
                                    left: event.clientX,
                                    top: event.clientY
                                })
                                if (clickPos) {
                                    // Try to create a NodeSelection on the tag
                                    try {
                                        const node = view.state.doc.nodeAt(
                                            clickPos.pos
                                        )
                                        if (node && node.type.name === "tag") {
                                            const tr =
                                                view.state.tr.setSelection(
                                                    prosemirror_state__rspack_import_3.NodeSelection.create(
                                                        view.state.doc,
                                                        clickPos.pos
                                                    )
                                                )
                                            view.dispatch(tr)
                                        } else {
                                            // If not directly on a tag, try the position before
                                            const nodeBefore =
                                                view.state.doc.nodeAt(
                                                    clickPos.pos - 1
                                                )
                                            if (
                                                nodeBefore &&
                                                nodeBefore.type.name === "tag"
                                            ) {
                                                const tr =
                                                    view.state.tr.setSelection(
                                                        prosemirror_state__rspack_import_3.NodeSelection.create(
                                                            view.state.doc,
                                                            clickPos.pos - 1
                                                        )
                                                    )
                                                view.dispatch(tr)
                                            }
                                        }
                                    } catch {
                                        // Silently ignore selection errors
                                    }
                                }

                                view.focus()

                                // Reset the flag after a short delay
                                setTimeout(() => {
                                    tagInputJustBlurred = false
                                }, 150)
                            }
                        }
                    }

                    return false
                }
            }
        },
        appendTransaction: (trs, oldState, newState) => {
            // If selection is not collapsed or not changed, don't do anything
            if (
                newState.selection.from !== newState.selection.to ||
                !trs.some(tr => tr.selectionSet)
            ) {
                return
            }

            const selectionPos = newState.selection.from
            const pluginState = key.getState(newState)

            // If this selection change came from a mouse click, don't interfere
            if (mouseClickActive) {
                return
            }

            // If selection is already a NodeSelection on a tag, don't interfere
            if (newState.selection instanceof prosemirror_state__rspack_import_3.NodeSelection) {
                const selectedNode = newState.selection.node
                if (selectedNode && selectedNode.type.name === "tag") {
                    return
                }
            }

            // Check if selection is within any tags_part node
            const tagsPartRange = pluginState.tagsPartPositions.find(
                range => selectionPos > range.start && selectionPos < range.end
            )

            if (tagsPartRange) {
                const oldSelectionPos = oldState.selection.from

                if (selectionPos + 1 === tagsPartRange.end) {
                    // Selection is at end of tags_part.
                    // Put caret into tag editor if write access is present.
                    // Otherwise, move caret beyond tags_part.
                    if (options.editor.docInfo.access_rights === "write") {
                        // tag editor will be activated by node view.
                        return
                    }
                    if (oldSelectionPos < selectionPos) {
                        const newSelection = (0,_helpers__rspack_import_0.nextSelection)(
                            newState,
                            tagsPartRange.end,
                            1
                        )
                        if (!newSelection) {
                            // Cannot find a location. Give up.
                            return
                        }

                        return newState.tr.setSelection(newSelection)
                    }
                }

                const selectedNodePos =
                    oldSelectionPos < selectionPos
                        ? selectionPos
                        : selectionPos - 1

                if (selectedNodePos === tagsPartRange.start) {
                    // selection is at start of tags node. Find previous possible selection location.
                    const newSelection = (0,_helpers__rspack_import_0.nextSelection)(
                        newState,
                        selectedNodePos,
                        -1
                    )
                    if (!newSelection) {
                        // Cannot find a location. Give up.
                        return
                    }
                    return newState.tr.setSelection(newSelection)
                } else {
                    // Select an entire tag node
                    return newState.tr.setSelection(
                        prosemirror_state__rspack_import_3.NodeSelection.create(newState.doc, selectedNodePos)
                    )
                }
            }

            return null
        }
    })


}),
"./js/modules/editor/state_plugins/tag_input/tag_editor.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  createTagEditor: function() { return createTagEditor; },
  tagInputReferences: function() { return tagInputReferences; }
});
/* import */ var prosemirror_history__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-history@1.3.2/node_modules/prosemirror-history/dist/index.js");
/* import */ var prosemirror_keymap__rspack_import_6 = __webpack_require__("./node_modules/.pnpm/prosemirror-keymap@1.2.2/node_modules/prosemirror-keymap/dist/index.js");
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _helpers__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/helpers.js");
/* import */ var _tag_editor_plugins__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/tag_input/tag_editor_plugins.js");










// WeakMap to store tag input references for access from the plugin
const tagInputReferences = new WeakMap()

const doc = {content: "tag"},
    tag = {
        content: "inline*",
        parseDOM: [{tag: "div.tag-input-editor"}],
        toDOM() {
            return [
                "div",
                {
                    class: "tag-input-editor"
                },
                0
            ]
        }
    },
    text = {group: "inline"}

const schema = new prosemirror_model__rspack_import_2.Schema({
    nodes: {doc, tag, text},
    marks: {}
})

const ArrowLeft = (state, dispatch, getNode, view, getPos) => {
    // If we're at the leftmost position (position 1), stopEvent will handle moving out of the tag
    if (state.selection.to > 1) {
        // Inside the tag input, move caret left normally
        const tr = state.tr.setSelection(
            prosemirror_state__rspack_import_3.TextSelection.create(
                state.doc,
                state.selection.from - 1,
                state.selection.to - 1
            )
        )
        dispatch(tr)
        return true
    } else {
        const node = getNode()
        // Exit tag input to the left
        if (node.nodeSize > 2) {
            // At least one tag
            const startPos = getPos() + node.nodeSize - 2
            view.dispatch(
                view.state.tr.setSelection(
                    prosemirror_state__rspack_import_3.NodeSelection.create(view.state.doc, startPos)
                )
            )
            view.focus()
            return true
        } else {
            // There is no tag
            return ArrowUp(state, dispatch, getNode, view, getPos)
        }
    }
}

const ArrowUp = (_state, _dispatch, _getNode, view, getPos) => {
    // We jump to the section before this one.
    const startPos = getPos()

    const newSelection = (0,_helpers__rspack_import_0.nextSelection)(view.state, startPos, -1)

    if (!newSelection) {
        return false
    }

    view.dispatch(view.state.tr.setSelection(newSelection))
    view.focus()
    return true
}

const ArrowRight = (state, dispatch, getNode, view, getPos) => {
    const docSize = state.doc.nodeSize - 3
    // If we're at the rightmost position, stopEvent will handle moving out of the tag
    if (state.selection.from < docSize) {
        // Inside the tag input, move caret right normally
        const tr = state.tr.setSelection(
            prosemirror_state__rspack_import_3.TextSelection.create(
                state.doc,
                state.selection.from + 1,
                state.selection.from + 1
            )
        )
        dispatch(tr)
        return true
    } else {
        return ArrowDown(state, dispatch, getNode, view, getPos)
    }
}

const ArrowDown = (_state, _dispatch, getNode, view, getPos) => {
    // We are at the end of the tag input. Move the cursor beyond
    const node = getNode()
    const startPos = getPos(),
        pos = startPos + node.nodeSize + 1

    const newSelection = (0,_helpers__rspack_import_0.nextSelection)(view.state, pos, 1)

    if (!newSelection) {
        return false
    }

    view.dispatch(view.state.tr.setSelection(newSelection))
    view.focus()
    return true
}

const createTagEditor = (view, getPos, getNode) => {
    const dom = document.createElement("div")
    dom.classList.add("tag-input")
    dom.setAttribute("contenteditable", false)
    const node = getNode()

    const tagInputView = new prosemirror_view__rspack_import_4.EditorView(dom, {
        state: prosemirror_state__rspack_import_3.EditorState.create({
            schema,
            doc: schema.nodeFromJSON({
                type: "doc",
                content: [
                    {
                        type: "tag",
                        content: []
                    }
                ]
            }),
            plugins: [
                (0,prosemirror_history__rspack_import_5.history)(),
                (0,_tag_editor_plugins__rspack_import_1.placeholderPlugin)(node.attrs.item_title),
                (0,_tag_editor_plugins__rspack_import_1.pastePlugin)(view),
                (0,prosemirror_keymap__rspack_import_6.keymap)({
                    "Mod-z": prosemirror_history__rspack_import_5.undo,
                    "Mod-shift-z": prosemirror_history__rspack_import_5.undo,
                    "Mod-y": prosemirror_history__rspack_import_5.redo,
                    Enter: (_state, _dispatch, tagInputView) =>
                        (0,_helpers__rspack_import_0.submitTag)(tagInputView, view, getPos),
                    ArrowLeft: (state, dispatch, _tagInputView) =>
                        ArrowLeft(state, dispatch, getNode, view, getPos),
                    ArrowRight: (state, dispatch, _tagInputView) =>
                        ArrowRight(state, dispatch, getNode, view, getPos),
                    ArrowUp: (state, dispatch, _tagInputView) =>
                        ArrowUp(state, dispatch, getNode, view, getPos),
                    ArrowDown: (state, dispatch, _tagInputView) =>
                        ArrowDown(state, dispatch, getNode, view, getPos)
                })
            ]
        }),
        handleDOMEvents: {
            blur: (tagInputView, event) => {
                // Handle blur event
                event.preventDefault()
                // Set a timeout so that change of focus can take place first
                window.setTimeout(() => {
                    // Check if getPos still returns a valid position
                    const pos = getPos()
                    if (pos !== undefined && pos !== null) {
                        (0,_helpers__rspack_import_0.submitTag)(tagInputView, view, getPos)
                    }
                }, 1)
            },
            focus: (tagInputView, _event) => {
                const startPos = getPos(),
                    pos =
                        startPos + view.state.doc.nodeAt(startPos).nodeSize - 1,
                    $pos = view.state.doc.resolve(pos)
                view.dispatch(
                    view.state.tr.setSelection(new prosemirror_state__rspack_import_3.TextSelection($pos))
                )
                tagInputView.focus()
            }
        },
        handleTextInput: (_view, _from, _to, text) => {
            if ([",", ".", ";"].includes(text)) {
                (0,_helpers__rspack_import_0.submitTag)(tagInputView, view, getPos)
                return true
            }
        },
        dispatchTransaction: tr => {
            const newState = tagInputView.state.apply(tr)
            tagInputView.updateState(newState)
        }
    })

    // Store references in WeakMap for access from the plugin
    tagInputReferences.set(dom, {
        tagInputView,
        mainView: view,
        getPos
    })

    return [dom, tagInputView]
}


}),
"./js/modules/editor/state_plugins/tag_input/tag_editor_plugins.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  pastePlugin: function() { return pastePlugin; },
  placeholderPlugin: function() { return placeholderPlugin; }
});
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");



const placeholderPlugin = nodeTitle =>
    new prosemirror_state__rspack_import_0.Plugin({
        props: {
            decorations: state => {
                const doc = state.doc
                if (
                    doc.childCount === 1 &&
                    doc.firstChild.isTextblock &&
                    doc.firstChild.content.size === 0
                ) {
                    const placeHolder = document.createElement("span")
                    placeHolder.classList.add("placeholder")
                    // There is only one field, so we know the selection is there
                    placeHolder.classList.add("selected")
                    placeHolder.setAttribute(
                        "data-placeholder",
                        `${gettext("Add")} ${nodeTitle.toLowerCase()}...`
                    )
                    return prosemirror_view__rspack_import_1.DecorationSet.create(doc, [
                        prosemirror_view__rspack_import_1.Decoration.widget(1, placeHolder)
                    ])
                }
            }
        }
    })

const pastePlugin = editorView => {
    return new prosemirror_state__rspack_import_0.Plugin({
        props: {
            handleDOMEvents: {
                paste(_view, event) {
                    const html = event.clipboardData.getData("text/html"),
                        text = event.clipboardData.getData("text/plain"),
                        slice = text
                            .split(/[,;.]/)
                            .map(item => item.trim())
                            .filter(item => item.length)
                    let tags
                    if (text && !html) {
                        tags = slice
                    } else {
                        // Make a paste DOM document
                        const clipboardDoc =
                            document.implementation.createHTMLDocument(
                                "paste document"
                            )
                        const pasteHTML = document.createElement("body")
                        clipboardDoc.body.appendChild(pasteHTML)
                        pasteHTML.innerHTML = html
                        tags = Array.from(
                            pasteHTML.querySelectorAll("span.tag")
                        ).map(tag => tag.textContent)
                    }
                    if (!tags.length) {
                        return
                    }
                    const pos = editorView.state.selection.from
                    const tr = editorView.state.tr
                    tags.reverse().forEach(tag => {
                        const node = editorView.state.schema.nodes.tag.create({
                            tag
                        })
                        tr.insert(pos, node)
                    })
                    editorView.dispatch(tr)
                    return true
                }
            }
        }
    })
}


}),
"./js/modules/editor/state_plugins/toc_render.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  tocRenderPlugin: function() { return tocRenderPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("tocRender")

function getTocItems(decorations) {
    let tocItems

    decorations.find(deco => {
        if (deco.spec.tocItems) {
            tocItems = deco.spec.tocItems
            return true
        }
        return false
    })
    return tocItems
}

function tocHTML(tocItems, title) {
    return `<h1 class="toc">${(0,_common__rspack_import_0.escapeText)(title)}</h1>
    ${tocItems
        .map(item => {
            const level = item.type.name.substr(-1)
            return `<h${level}><a href="#${item.id}">${(0,_common__rspack_import_0.escapeText)(item.textContent)}</a></h${level}>`
        })
        .join("")}`
}

class ToCView {
    constructor(node, _view, _getPos, decorations, options) {
        this.dom = document.createElement("div")
        this.dom.classList.add("doc-part", "table-of-contents")
        if (node.attrs.hidden) {
            this.dom.dataset.hidden = "true"
        }
        const tocItems = getTocItems(decorations) || []
        this.dom.innerHTML = tocHTML(tocItems, node.attrs.title)
        this.dom.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, "a", el):
                    event.preventDefault()
                    event.stopImmediatePropagation()
                    el.id = el.target.getAttribute("href").slice(1)
                    options.editor.scrollIdIntoView(el.id)
                    break
            }
        })
    }

    update(node, decorations) {
        const tocItems = getTocItems(decorations)
        if (!tocItems) {
            return
        }
        this.dom.innerHTML = tocHTML(tocItems, node.attrs.title)
    }
}

function getDecos(state) {
    const decoPos = [],
        tocItems = []
    let decos = prosemirror_view__rspack_import_2.DecorationSet.empty
    state.doc.descendants((node, offset) => {
        if (node.attrs?.hidden) {
            return false
        } else if (node.type.name === "table_of_contents") {
            decoPos.push(offset)
        } else if (node.type.groups.includes("heading")) {
            tocItems.push({
                id: node.attrs.id,
                textContent: node.textContent,
                type: node.type
            })
        }
    })
    if (decoPos.length) {
        decos = prosemirror_view__rspack_import_2.DecorationSet.create(
            state.doc,
            decoPos.map(pos =>
                prosemirror_view__rspack_import_2.Decoration.node(
                    pos,
                    pos + 1,
                    {}, // We don't actually need to change anything about the node
                    {tocItems} // We communicate the toc items to the node view
                )
            )
        )
    }
    return decos
}

const tocRenderPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        state: {
            init(_config, state) {
                const decos = getDecos(state)
                return {decos}
            },
            apply(tr, _prev, oldState, state) {
                let {decos} = this.getState(oldState),
                    updateToc = false
                tr.mapping.maps.forEach((map, index) =>
                    map.forEach((oldStart, oldEnd, newStart, newEnd) => {
                        const oldDoc = tr.docs[index],
                            newDoc = tr.docs[index + 1] || tr.doc // tr.doc if it is the last step
                        let hidden = false

                        oldDoc.nodesBetween(oldStart, oldEnd, node => {
                            if (updateToc) {
                                return false
                            } else if (node.type.groups.includes("heading")) {
                                updateToc = true
                            } else if (node.attrs?.hidden) {
                                hidden = true // was hidden
                            }
                            return true
                        })
                        newDoc.nodesBetween(newStart, newEnd, node => {
                            if (updateToc) {
                                return false
                            } else if (node.type.groups.includes("heading")) {
                                updateToc = true
                            } else if (node.attrs?.hidden) {
                                hidden = hidden ? false : true // is hidden
                            }
                        })
                        if (hidden) {
                            // hidden has changed
                            updateToc = true
                        }
                    })
                )
                // There has been a change that should cause a table of contents update.
                // So we place a decoration around each toc-node which should cause it to
                // update.
                // See https://discuss.prosemirror.net/t/how-can-i-communicate-from-a-plugin-to-a-custom-nodeview/952/2

                if (updateToc) {
                    decos = getDecos(state)
                } else {
                    decos = decos.map(tr.mapping, tr.doc)
                }

                return {decos}
            }
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            },
            nodeViews: {
                table_of_contents: (node, view, getPos, decorations) =>
                    new ToCView(node, view, getPos, decorations, options)
            }
        }
    })


}),
"./js/modules/editor/state_plugins/toolbar.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  toolbarPlugin: function() { return toolbarPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var _menus__rspack_import_0 = __webpack_require__("./js/modules/editor/menus/index.js");




const key = new prosemirror_state__rspack_import_1.PluginKey("toolbar")
const toolbarPlugin = options =>
    new prosemirror_state__rspack_import_1.Plugin({
        key,
        view(editorView) {
            return new _menus__rspack_import_0.ToolbarView(editorView, options)
        }
    })


}),
"./js/modules/editor/state_plugins/track/find_selected_changes.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  findSelectedChanges: function() { return findSelectedChanges; }
});
/* import */ var _helpers__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/track/helpers.js");


function findSelectedChanges(state) {
    const selection = state.selection,
        selectedChanges = {
            insertion: false,
            deletion: false,
            formatChange: false
        }
    let insertionPos = false,
        deletionPos = false,
        formatChangePos = false,
        insertionMark,
        deletionMark,
        formatChangeMark,
        insertionSize,
        deletionSize,
        formatChangeSize

    if (selection.empty) {
        const resolvedPos = state.doc.resolve(selection.from),
            marks = resolvedPos.marks()
        if (marks) {
            insertionMark = marks.find(
                mark => mark.type.name === "insertion" && !mark.attrs.approved
            )
            if (insertionMark) {
                insertionPos = selection.from
            }
            deletionMark = marks.find(mark => mark.type.name === "deletion")
            if (deletionMark) {
                deletionPos = selection.from
            }
            formatChangeMark = marks.find(
                mark => mark.type.name === "format_change"
            )
            if (formatChangeMark) {
                formatChangePos = selection.from
            }
        }
    } else {
        state.doc.nodesBetween(selection.from, selection.to, (node, pos) => {
            if (pos < selection.from) {
                return true
            }
            if (!insertionMark) {
                insertionMark = node.attrs.track
                    ? node.attrs.track.find(
                          trackAttr => trackAttr.type === "insertion"
                      )
                    : node.marks.find(
                          mark =>
                              mark.type.name === "insertion" &&
                              !mark.attrs.approved
                      )
                if (insertionMark) {
                    insertionPos = pos
                    if (!node.isInline) {
                        insertionSize = node.nodeSize
                    }
                }
            }
            if (!deletionMark) {
                deletionMark = node.attrs.track
                    ? node.attrs.track.find(
                          trackAttr => trackAttr.type === "deletion"
                      )
                    : node.marks.find(mark => mark.type.name === "deletion")
                if (deletionMark) {
                    deletionPos = pos
                    if (!node.isInline) {
                        deletionSize = node.nodeSize
                    }
                }
            }
            if (!formatChangeMark) {
                formatChangeMark = node.marks.find(
                    mark => mark.type.name === "format_change"
                )
                if (formatChangeMark) {
                    formatChangePos = pos
                    if (!node.isInline) {
                        formatChangeSize = node.nodeSize
                    }
                }
            }
        })
    }
    if (insertionMark) {
        selectedChanges.insertion = insertionSize
            ? {from: insertionPos, to: insertionPos + insertionSize}
            : (0,_helpers__rspack_import_0.getFromToMark)(state.doc, insertionPos, insertionMark)
    }

    if (deletionMark) {
        selectedChanges.deletion = deletionSize
            ? {from: deletionPos, to: deletionPos + deletionSize}
            : (0,_helpers__rspack_import_0.getFromToMark)(state.doc, deletionPos, deletionMark)
    }

    if (formatChangeMark) {
        selectedChanges.formatChange = formatChangeSize
            ? {from: formatChangePos, to: formatChangePos + formatChangeSize}
            : (0,_helpers__rspack_import_0.getFromToMark)(state.doc, formatChangePos, formatChangeMark)
    }
    return selectedChanges
}


}),
"./js/modules/editor/state_plugins/track/helpers.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  deactivateAllSelectedChanges: function() { return deactivateAllSelectedChanges; },
  getFromToMark: function() { return getFromToMark; },
  getSelectedChanges: function() { return getSelectedChanges; },
  setSelectedChanges: function() { return setSelectedChanges; }
});
/* import */ var prosemirror_view__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _plugin__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/track/plugin.js");




function getSelectedChanges(state) {
    const keyState = _plugin__rspack_import_0.key.getState(state)
    if (!keyState) {
        return {}
    }
    const {decos} = keyState

    const insertion = decos.find(
            undefined,
            undefined,
            spec => spec === _plugin__rspack_import_0.selectedInsertionSpec
        )[0],
        deletion = decos.find(
            undefined,
            undefined,
            spec => spec === _plugin__rspack_import_0.selectedDeletionSpec
        )[0],
        format_change = decos.find(
            undefined,
            undefined,
            spec => spec === _plugin__rspack_import_0.selectedChangeFormatSpec
        )[0],
        block_change = decos.find(
            undefined,
            undefined,
            spec => spec === _plugin__rspack_import_0.selectedChangeBlockSpec
        )[0]

    return {insertion, deletion, format_change, block_change}
}

function setSelectedChanges(state, type, pos) {
    const tr = state.tr,
        node = tr.doc.nodeAt(pos),
        mark = node.attrs.track
            ? node.attrs.track.find(trackAttr => trackAttr.type === type)
            : node.marks.find(mark => mark.type.name === type)
    if (!mark) {
        return
    }
    const selectedChange = node.isInline
        ? getFromToMark(tr.doc, pos, mark)
        : {from: pos, to: pos + node.nodeSize}
    let decos = prosemirror_view__rspack_import_1.DecorationSet.empty,
        spec
    if (type === "insertion") {
        spec = _plugin__rspack_import_0.selectedInsertionSpec
    } else if (type === "deletion") {
        spec = _plugin__rspack_import_0.selectedDeletionSpec
    } else if (type === "format_change") {
        spec = _plugin__rspack_import_0.selectedChangeFormatSpec
    } else if (type === "block_change") {
        spec = _plugin__rspack_import_0.selectedChangeBlockSpec
    }
    const decoType = node.isInline ? prosemirror_view__rspack_import_1.Decoration.inline : prosemirror_view__rspack_import_1.Decoration.node
    decos = decos.add(tr.doc, [
        decoType(
            selectedChange.from,
            selectedChange.to,
            {
                class: `selected-${type}`
            },
            spec
        )
    ])
    return tr.setMeta(_plugin__rspack_import_0.key, {decos}).setMeta("track", true)
}

function deactivateAllSelectedChanges(tr) {
    const pluginState = {
        decos: prosemirror_view__rspack_import_1.DecorationSet.empty
    }
    return tr.setMeta(_plugin__rspack_import_0.key, pluginState).setMeta("track", true)
}

// From https://discuss.prosemirror.net/t/expanding-the-selection-to-the-active-mark/478/2 with some bugs fixed
function getFromToMark(doc, pos, mark) {
    const $pos = doc.resolve(pos),
        parent = $pos.parent
    const start = parent.childAfter($pos.parentOffset)
    if (!start.node) {
        return null
    }
    let startIndex = $pos.index(),
        startPos = $pos.start() + start.offset
    while (startIndex > 0 && mark.isInSet(parent.child(startIndex - 1).marks)) {
        startPos -= parent.child(--startIndex).nodeSize
    }
    let endIndex = $pos.index() + 1,
        endPos = $pos.start() + start.offset + start.node.nodeSize
    while (
        endIndex < parent.childCount &&
        mark.isInSet(parent.child(endIndex).marks)
    ) {
        endPos += parent.child(endIndex++).nodeSize
    }
    return {from: startPos, to: endPos}
}


}),
"./js/modules/editor/state_plugins/track/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  deactivateAllSelectedChanges: function() { return /* reexport safe */ _helpers__rspack_import_1.deactivateAllSelectedChanges; },
  getSelectedChanges: function() { return /* reexport safe */ _helpers__rspack_import_1.getSelectedChanges; },
  setSelectedChanges: function() { return /* reexport safe */ _helpers__rspack_import_1.setSelectedChanges; },
  trackPlugin: function() { return /* reexport safe */ _plugin__rspack_import_0.trackPlugin; }
});
/* import */ var _plugin__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/track/plugin.js");
/* import */ var _helpers__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/track/helpers.js");




}),
"./js/modules/editor/state_plugins/track/plugin.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  key: function() { return key; },
  selectedChangeBlockSpec: function() { return selectedChangeBlockSpec; },
  selectedChangeFormatSpec: function() { return selectedChangeFormatSpec; },
  selectedDeletionSpec: function() { return selectedDeletionSpec; },
  selectedInsertionSpec: function() { return selectedInsertionSpec; },
  trackPlugin: function() { return trackPlugin; }
});
/* import */ var prosemirror_state__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_view__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-view@1.38.1/node_modules/prosemirror-view/dist/index.js");
/* import */ var _find_selected_changes__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/track/find_selected_changes.js");





const key = new prosemirror_state__rspack_import_1.PluginKey("track")
const selectedInsertionSpec = {}
const selectedDeletionSpec = {}
const selectedChangeFormatSpec = {}
const selectedChangeBlockSpec = {}

function trackPlugin(options) {
    return new prosemirror_state__rspack_import_1.Plugin({
        key,
        state: {
            init(_config, state) {
                // Make sure there are colors for all users who have left marks
                // in the document and that they are registered as past
                // participants for the marginbox filter.
                const users = {}
                users[options.editor.user.id] = options.editor.user.name
                state.doc.descendants(node => {
                    if (node.attrs.track) {
                        node.attrs.track.forEach(track => {
                            if (!users[track.user] && track.user !== 0) {
                                users[track.user] = track.username
                            }
                        })
                    } else {
                        node.marks.forEach(mark => {
                            if (
                                [
                                    "deletion",
                                    "insertion",
                                    "format_change"
                                ].includes(mark.type.name) &&
                                !users[mark.attrs.user] &&
                                mark.attrs.user !== 0
                            ) {
                                users[mark.attrs.user] = mark.attrs.username
                            }
                        })
                    }
                })

                if (options.editor.mod.collab) {
                    Object.entries(users).forEach(([id, userName]) => {
                        const userId = Number.parseInt(id)
                        options.editor.mod.collab.colors.ensureUserColor(userId)
                        if (
                            !options.editor.mod.collab.pastParticipants.find(
                                participant => participant.id === userId
                            )
                        ) {
                            options.editor.mod.collab.pastParticipants.push({
                                id: userId,
                                name: userName
                            })
                        }
                    })
                }

                return {
                    decos: prosemirror_view__rspack_import_2.DecorationSet.empty
                }
            },
            apply(tr, _prev, oldState, state) {
                const meta = tr.getMeta(key)
                if (meta) {
                    // There has been an update, return values from meta instead
                    // of previous values
                    return meta
                }

                let {decos} = this.getState(oldState)

                if (tr.selectionSet) {
                    const {insertion, deletion, formatChange} =
                        (0,_find_selected_changes__rspack_import_0.findSelectedChanges)(state)
                    decos = prosemirror_view__rspack_import_2.DecorationSet.empty
                    const decoType = tr.selection.node
                        ? prosemirror_view__rspack_import_2.Decoration.node
                        : prosemirror_view__rspack_import_2.Decoration.inline
                    if (insertion) {
                        decos = decos.add(tr.doc, [
                            decoType(
                                insertion.from,
                                insertion.to,
                                {
                                    class: "selected-insertion"
                                },
                                selectedInsertionSpec
                            )
                        ])
                    }
                    if (deletion) {
                        decos = decos.add(tr.doc, [
                            decoType(
                                deletion.from,
                                deletion.to,
                                {
                                    class: "selected-deletion"
                                },
                                selectedDeletionSpec
                            )
                        ])
                    }
                    if (formatChange) {
                        decos = decos.add(tr.doc, [
                            decoType(
                                formatChange.from,
                                formatChange.to,
                                {
                                    class: "selected-format_change"
                                },
                                selectedChangeFormatSpec
                            )
                        ])
                    }
                } else {
                    decos = decos.map(tr.mapping, tr.doc)
                }
                return {
                    decos
                }
            }
        },
        props: {
            decorations(state) {
                const {decos} = this.getState(state)
                return decos
            },
            handleDOMEvents: {
                focus: (_view, _event) => {
                    options.editor.mod.comments.interactions.deactivateSelectedChanges()
                }
            }
        }
    })
}


}),
"./js/modules/editor/tools/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  KeyBindingsDialog: function() { return /* reexport safe */ _key_bindings__rspack_import_1.KeyBindingsDialog; },
  SearchReplaceDialog: function() { return /* reexport safe */ _search_replace__rspack_import_2.SearchReplaceDialog; },
  WordCountDialog: function() { return /* reexport safe */ _word_count__rspack_import_0.WordCountDialog; }
});
/* import */ var _word_count__rspack_import_0 = __webpack_require__("./js/modules/editor/tools/word_count/index.js");
/* import */ var _key_bindings__rspack_import_1 = __webpack_require__("./js/modules/editor/tools/key_bindings/index.js");
/* import */ var _search_replace__rspack_import_2 = __webpack_require__("./js/modules/editor/tools/search_replace/index.js");





}),
"./js/modules/editor/tools/key_bindings/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  KeyBindingsDialog: function() { return KeyBindingsDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _templates__rspack_import_1 = __webpack_require__("./js/modules/editor/tools/key_bindings/templates.js");


/* This is an adaptation of question.mark for Fidus Writer http://fiduswriter.org
 * originally by Gabriel Lopez <gabriel.marcos.lopez@gmail.com>
 */

class KeyBindingsDialog {
    constructor(editor) {
        this.editor = editor
    }

    init() {
        const dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Keyboard Shortcuts"),
            body: (0,_templates__rspack_import_1.keyBindingsTemplate)(),
            width: 850,
            buttons: [{type: "close"}]
        })
        dialog.open()
    }
}


}),
"./js/modules/editor/tools/key_bindings/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  keyBindingsTemplate: function() { return keyBindingsTemplate; }
});
const keyBindingsTemplate = () =>
    `<div id="helpListWrap" class="help-list-wrap">
        <ul class="help-list">
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+p</span></kbd>
            <span class="help-key-def">${gettext("Print")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+s</span></kbd>
            <span class="help-key-def">${gettext("Save revision")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>Shift+CTRL+/</span></kbd>
            <span class="help-key-def">${gettext("Show keyboard shortcuts")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+V</span></kbd>
            <span class="help-key-def">${gettext("Paste with content detection")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>Shift+CTRL+V</span></kbd>
            <span class="help-key-def">${gettext("Paste without content detection")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>Drop (from drag)</span></kbd>
            <span class="help-key-def">${gettext("Paste with content detection")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>Shift+Drop (from drag)</span></kbd>
            <span class="help-key-def">${gettext("Paste without content detection")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+H</span></kbd>
            <span class="help-key-def">${gettext("Search and replace")}</span>
          </li>
        </ul><!-- .help-list -->
        <ul class="help-list">
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+B</span></kbd>
            <span class="help-key-def">${gettext("Bold")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+I</span></kbd>
            <span class="help-key-def">${gettext("Italics")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+[</span></kbd>
            <kbd class="fw-button fw-light fw-small"><span>CTRL+&lt;</span></kbd>
            <span class="help-key-def">${gettext("Lift")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+Shift+8</span></kbd>
            <span class="help-key-def">${gettext("Wrap selection in bullet list")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+Shift+9</span></kbd>
            <span class="help-key-def">${gettext("Wrap selection in numbered list")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+Z</span></kbd>
            <span class="help-key-def">${gettext("Undo")}</span>
          </li>
          <li class="help-key-unit">
            <kbd class="fw-button fw-light fw-small"><span>CTRL+Shift+Z</span></kbd>
            <span class="help-key-def">${gettext("Redo")}</span>
          </li>
        </ul><!-- .help-list -->
      </div><!-- .help-list-wrap -->`


}),
"./js/modules/editor/tools/search_replace/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SearchReplaceDialog: function() { return SearchReplaceDialog; }
});
/* import */ var ___rspack_import_0 = __webpack_require__("./js/modules/editor/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _state_plugins__rspack_import_2 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _templates__rspack_import_3 = __webpack_require__("./js/modules/editor/tools/search_replace/templates.js");





class SearchReplaceDialog {
    constructor(editor) {
        this.editor = editor
        this.dialog = false
        this.matches = {matches: [], selected: false}
        this.fnMatches = {matches: [], selected: false}
        this.canWrite =
            ___rspack_import_0.READ_ONLY_ROLES.includes(this.editor.docInfo.access_rights) ||
            ___rspack_import_0.COMMENT_ONLY_ROLES.includes(this.editor.docInfo.access_rights)
                ? false
                : true
    }

    init() {
        let buttons = [
            {
                text: gettext("Previous"),
                classes: "fw-light disabled",
                click: () => {
                    if (this.matches.selected !== false) {
                        if (
                            this.matches.selected > 0 ||
                            !this.fnMatches.matches.length
                        ) {
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectPreviousSearchMatch)(
                                    this.editor.view.state
                                )
                            )
                        } else {
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.deselectSearchMatch)(this.editor.view.state)
                            )
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectPreviousSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                        }
                    } else if (this.fnMatches.selected !== false) {
                        if (
                            this.fnMatches.selected > 0 ||
                            !this.matches.matches.length
                        ) {
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectPreviousSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                        } else {
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.deselectSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectPreviousSearchMatch)(
                                    this.editor.view.state
                                )
                            )
                        }
                    }
                }
            },
            {
                text: gettext("Next"),
                classes: "fw-light disabled",
                click: () => {
                    if (this.matches.selected !== false) {
                        if (
                            this.matches.selected <
                                this.matches.matches.length - 1 ||
                            !this.fnMatches.matches.length
                        ) {
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectNextSearchMatch)(this.editor.view.state)
                            )
                        } else {
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.deselectSearchMatch)(this.editor.view.state)
                            )
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectNextSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                        }
                    } else if (this.fnMatches.selected !== false) {
                        if (
                            this.fnMatches.selected <
                                this.fnMatches.matches.length - 1 ||
                            !this.matches.matches.length
                        ) {
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectNextSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                        } else {
                            this.editor.mod.footnotes.fnEditor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.deselectSearchMatch)(
                                    this.editor.mod.footnotes.fnEditor.view
                                        .state
                                )
                            )
                            this.editor.view.dispatch(
                                (0,_state_plugins__rspack_import_2.selectNextSearchMatch)(this.editor.view.state)
                            )
                        }
                    }
                }
            }
        ]

        if (this.canWrite) {
            buttons = buttons.concat([
                {
                    text: gettext("Replace"),
                    classes: "fw-dark disabled",
                    click: () => {
                        if (this.matches.selected !== false) {
                            const match =
                                this.matches.matches[this.matches.selected]
                            const originalDoc = this.editor.view.state.doc
                            const tr = this.editor.view.state.tr.insertText(
                                this.replaceInput.value,
                                match.from,
                                match.to
                            )
                            this.editor.view.dispatch(tr)
                            // In case there was a match within protected range , the change
                            // would be rejected. Show alert when replace is successfull.
                            if (!this.editor.view.state.doc.eq(originalDoc)) {
                                (0,_common__rspack_import_1.addAlert)(
                                    "info",
                                    gettext("Text replaced successfully")
                                )
                            }
                        } else if (this.fnMatches.selected !== false) {
                            const match =
                                this.fnMatches.matches[this.fnMatches.selected]
                            const originalDoc =
                                this.editor.mod.footnotes.fnEditor.view.state
                                    .doc
                            const tr =
                                this.editor.mod.footnotes.fnEditor.view.state.tr.insertText(
                                    this.replaceInput.value,
                                    match.from,
                                    match.to
                                )
                            this.editor.mod.footnotes.fnEditor.view.dispatch(tr)
                            // In case there was a match within protected range , the change
                            // would be rejected. Show alert when replace is successfull.
                            if (
                                !this.editor.mod.footnotes.fnEditor.view.state.doc.eq(
                                    originalDoc
                                )
                            ) {
                                (0,_common__rspack_import_1.addAlert)(
                                    "info",
                                    gettext("Text replaced successfully")
                                )
                            }
                        }
                    }
                },
                {
                    text: gettext("Replace All"),
                    classes: "fw-dark disabled",
                    click: () => {
                        if (this.matches.matches.length) {
                            const tr = this.editor.view.state.tr
                            const originalDoc = this.editor.view.state.doc
                            const protectedRanges = (0,_state_plugins__rspack_import_2.getProtectedRanges)(
                                this.editor.view.state
                            )
                            const matches = this.matches.matches.slice()
                            while (matches.length) {
                                const match = matches.pop() // We take them backward so that there is no need for mapping steps
                                if (
                                    !protectedRanges.find(
                                        ({from, to}) =>
                                            !(
                                                (match.from <= from &&
                                                    match.to <= from) ||
                                                (match.from >= to &&
                                                    match.to >= to)
                                            )
                                    )
                                ) {
                                    // Ignore matches within protected ranges.
                                    tr.insertText(
                                        this.replaceInput.value,
                                        match.from,
                                        match.to
                                    )
                                }
                            }
                            this.editor.view.dispatch(tr)
                            // In case there was a match within protected range , the change
                            // would be rejected. Show alert when replace is successfull.
                            if (!this.editor.view.state.doc.eq(originalDoc)) {
                                (0,_common__rspack_import_1.addAlert)(
                                    "info",
                                    gettext("Text replaced successfully")
                                )
                            }
                        }
                        if (this.fnMatches.matches.length) {
                            const tr =
                                this.editor.mod.footnotes.fnEditor.view.state.tr
                            const originalDoc =
                                this.editor.mod.footnotes.fnEditor.view.state
                                    .doc
                            const matches = this.fnMatches.matches.slice()
                            while (matches.length) {
                                const match = matches.pop() // We take them backward so that there is no need for mapping steps
                                tr.insertText(
                                    this.replaceInput.value,
                                    match.from,
                                    match.to
                                )
                            }
                            this.editor.mod.footnotes.fnEditor.view.dispatch(tr)
                            // In case there was a match within protected range , the change
                            // would be rejected. Show alert when replace is successfull.
                            if (
                                !this.editor.mod.footnotes.fnEditor.view.state.doc.eq(
                                    originalDoc
                                )
                            ) {
                                (0,_common__rspack_import_1.addAlert)(
                                    "info",
                                    gettext("Text replaced successfully")
                                )
                            }
                        }
                    }
                }
            ])
        }

        buttons = buttons.concat([{type: "close"}])

        this.dialog = new _common__rspack_import_1.Dialog({
            title: this.canWrite
                ? gettext("Search and replace")
                : gettext("Search"),
            body: (0,_templates__rspack_import_3.searchDialogTemplate)({canWrite: this.canWrite}),
            buttons,
            blur: false,
            onClose: () => {
                this.endSearch()
                this.editor.currentView.focus()
            },
            note: {
                display: false,
                text: gettext(
                    "Please note that there are match(es) within non-editable parts of the document, these match(es) won't be replaced when using 'replace' or 'replace all'"
                )
            }
        })

        this.dialog.open()

        this.searchInput = this.dialog.dialogEl.querySelector(".search")
        this.replaceInput = this.dialog.dialogEl.querySelector(".replace")
        this.resultCountEl = this.dialog.dialogEl.querySelector(
            ".search-result-count"
        )
        this.dialog.dialogEl.querySelector("input[type=text]").focus()

        this.bind()
    }

    setButtonState() {
        if (this.matches.matches.length + this.fnMatches.matches.length > 1) {
            this.dialog.buttons[0].classes = "fw-light"
            this.dialog.buttons[1].classes = "fw-light"
        } else {
            this.dialog.buttons[0].classes = "fw-light disabled"
            this.dialog.buttons[1].classes = "fw-light disabled"
        }
        if (this.canWrite) {
            if (this.matches.matches.length || this.fnMatches.matches.length) {
                this.dialog.buttons[2].classes = "fw-dark"
                this.dialog.buttons[3].classes = "fw-dark"
            } else {
                this.dialog.buttons[2].classes = "fw-dark disabled"
                this.dialog.buttons[3].classes = "fw-dark disabled"
            }
        }

        this.dialog.refreshButtons()
    }

    setNoteState() {
        const protectedRanges = (0,_state_plugins__rspack_import_2.getProtectedRanges)(this.editor.view.state)
        const matches = this.matches.matches.slice()
        let matchWithinPR = false
        while (matches.length && !matchWithinPR) {
            const match = matches.pop()
            if (
                protectedRanges.find(
                    ({from, to}) =>
                        !(
                            (match.from <= from && match.to <= from) ||
                            (match.from >= to && match.to >= to)
                        )
                )
            ) {
                matchWithinPR = true
            }
        }

        if (matchWithinPR) {
            this.dialog.note.display = true
        } else {
            this.dialog.note.display = false
        }
        this.dialog.refreshNote()
    }

    updateResultCount() {
        const totalMatches =
            this.matches.matches.length + this.fnMatches.matches.length
        this.resultCountEl.textContent = `(${totalMatches})`
    }

    bind() {
        this.searchInput.addEventListener("input", () => {
            this.search(this.searchInput.value)
            this.setNoteState()
        })
        if (this.canWrite) {
            this.replaceInput.addEventListener("input", () => {
                this.setButtonState()
            })
        }
    }

    onUpdate() {
        this.matches = (0,_state_plugins__rspack_import_2.getSearchMatches)(this.editor.view.state)
        this.fnMatches = (0,_state_plugins__rspack_import_2.getSearchMatches)(
            this.editor.mod.footnotes.fnEditor.view.state
        )
        const selectedSearch = document.querySelector(
            "#paper-editable .search.selected"
        )

        if (selectedSearch) {
            selectedSearch.scrollIntoView(false)
        }
        // listener for change in views
        this.setButtonState()
        this.updateResultCount()
    }

    search(term) {
        const setSearchTermResult = (0,_state_plugins__rspack_import_2.setSearchTerm)(
            this.editor.view.state,
            term,
            this.editor.view === this.editor.currentView ? 0 : false,
            this
        )
        let {tr} = setSearchTermResult
        const {matches, selected} = setSearchTermResult
        const setSearchTermResultFn = (0,_state_plugins__rspack_import_2.setSearchTerm)(
            this.editor.mod.footnotes.fnEditor.view.state,
            term,
            this.editor.mod.footnotes.fnEditor.view === this.editor.currentView
                ? 0
                : false
        )
        let {tr: fnTr} = setSearchTermResultFn
        const {matches: fnMatches, selected: fnSelected} = setSearchTermResultFn
        if (
            selected === false &&
            fnSelected === false &&
            (matches.length || fnMatches.length)
        ) {
            if (matches.length) {
                tr = (0,_state_plugins__rspack_import_2.setSearchTerm)(this.editor.view.state, term, 0, this).tr
            } else {
                fnTr = (0,_state_plugins__rspack_import_2.setSearchTerm)(
                    this.editor.mod.footnotes.fnEditor.view.state,
                    term,
                    0
                ).tr
            }
        }
        this.editor.mod.footnotes.fnEditor.view.dispatch(fnTr)
        this.editor.view.dispatch(tr)
        this.updateResultCount()
    }

    endSearch() {
        this.editor.view.dispatch((0,_state_plugins__rspack_import_2.endSearch)(this.editor.view.state))
        this.editor.mod.footnotes.fnEditor.view.dispatch(
            (0,_state_plugins__rspack_import_2.endSearch)(this.editor.mod.footnotes.fnEditor.view.state)
        )
    }
}


}),
"./js/modules/editor/tools/search_replace/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  searchDialogTemplate: function() { return searchDialogTemplate; }
});
const searchDialogTemplate = ({canWrite}) => `<table class="fw-dialog-table">
    <tbody>
        <tr>
            <th><h4 class="fw-tablerow-title">${gettext("Find")}</h4></th>
            <td class="entry-field">
                <input type="text" class="search">
                <div class="search-result-count"></div>
            </td>
        </tr>
        ${
            canWrite
                ? `<tr>
                <th><h4 class="fw-tablerow-title">${gettext("Replace with")}</h4></th>
                <td class="entry-field"><input type="text" class="replace"></td>
            </tr>`
                : ""
        }
    </tbody>
</table>`


}),
"./js/modules/editor/tools/word_count/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  WordCountDialog: function() { return WordCountDialog; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_text__rspack_import_1 = __webpack_require__("./js/modules/schema/text.js");
/* import */ var _templates__rspack_import_2 = __webpack_require__("./js/modules/editor/tools/word_count/templates.js");





class WordCountDialog {
    constructor(editor) {
        this.editor = editor
    }

    init() {
        const dialog = new _common__rspack_import_0.Dialog({
            title: gettext("Word counter"),
            body: (0,_templates__rspack_import_2.wordCounterDialogTemplate)(this.countWords()),
            buttons: [{type: "close"}]
        })
        dialog.open()
    }

    countWords() {
        const textContent = (0,_schema_text__rspack_import_1.getNonDeletedTextContent)(
                this.editor.view.state.doc
            ),
            footnoteContent = (0,_schema_text__rspack_import_1.getNonDeletedTextContent)(
                this.editor.mod.footnotes.fnEditor.view.state.doc
            ),
            bibliographyContent =
                document.querySelector(".doc-bibliography").textContent
        const docContent =
            textContent + " " + footnoteContent + " " + bibliographyContent
        const docNumChars = docContent.split("\n").join("").length - 2 // Subtract two for added spaces
        const docWords = docContent.split(/[\n ]+/)

        const docNumNoSpace = docWords.join("").length
        const docNumWords = docNumNoSpace ? docWords.length : 0

        const selectionContent = (0,_schema_text__rspack_import_1.getNonDeletedTextContent)(
            this.editor.currentView.state.doc.cut(
                this.editor.currentView.state.selection.from,
                this.editor.currentView.state.selection.to
            )
        )
        const selectionNumChars = selectionContent.split("\n").join("").length
        const selectionWords = selectionContent.split(/[\n ]+/)
        const selectionNumNoSpace = selectionWords.join("").length
        const selectionNumWords = selectionNumNoSpace
            ? selectionWords.length
            : 0

        return {
            docNumWords,
            docNumNoSpace,
            docNumChars,
            selectionNumWords,
            selectionNumNoSpace,
            selectionNumChars
        }
    }
}


}),
"./js/modules/editor/tools/word_count/templates.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  wordCounterDialogTemplate: function() { return wordCounterDialogTemplate; }
});
const wordCounterDialogTemplate = ({
    docNumWords,
    docNumNoSpace,
    docNumChars,
    selectionNumWords,
    selectionNumNoSpace,
    selectionNumChars
}) =>
    `<table class="fw-data-table">
        <thead class="fw-data-table-header">
            <tr>
                <th>&nbsp;${gettext("Number of")}&nbsp;</th>
                <th>&nbsp;${gettext("Document")}&nbsp;</th>
                <th>&nbsp;${gettext("Selection")}&nbsp;</th>
            </tr>
        </thead>
        <tbody class="fw-word-counter-tbody">
            <tr>
                <td>&nbsp;${gettext("Words")}&nbsp;</td>
                <td>&nbsp;${docNumWords}&nbsp;</td>
                <td>&nbsp;${selectionNumWords}&nbsp;</td>
            </tr>
            <tr>
                <td>&nbsp;${gettext("Characters without blanks")}&nbsp;</td>
                <td>&nbsp;${docNumNoSpace}&nbsp;</td>
                <td>&nbsp;${selectionNumNoSpace}&nbsp;</td>
            </tr>
            <tr>
                <td>&nbsp;${gettext("Characters with blanks")}&nbsp;</td>
                <td>&nbsp;${docNumChars}&nbsp;</td>
                <td>&nbsp;${selectionNumChars}&nbsp;</td>
            </tr>
        </tbody>
    </table>`


}),
"./js/modules/editor/track/accept.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  accept: function() { return accept; }
});
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _state_plugins__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _delete__rspack_import_1 = __webpack_require__("./js/modules/editor/track/delete.js");






const accept = (type, pos, view) => {
    const tr = view.state.tr.setMeta("track", true),
        map = new prosemirror_transform__rspack_import_2.Mapping()
    let reachedEnd = false
    const trackMark = view.state.doc
        .nodeAt(pos)
        .marks.find(mark => mark.type.name === type)
    view.state.doc.nodesBetween(
        pos,
        view.state.doc.nodeSize - 2,
        (node, nodePos) => {
            if (nodePos < pos) {
                return true
            }
            if (reachedEnd) {
                return false
            }
            if (!node.isInline) {
                reachedEnd = true
            } else if (!trackMark.isInSet(node.marks)) {
                reachedEnd = true
                return false
            }
            // Traverse only those nodes which have the track marks.
            if (
                trackMark === undefined ||
                (trackMark && trackMark.isInSet(node.marks))
            ) {
                if (type === "deletion") {
                    (0,_delete__rspack_import_1.deleteNode)(tr, node, nodePos, map, true)
                } else if (type === "insertion") {
                    if (node.attrs.track) {
                        const track = node.attrs.track.filter(
                            track => track.type !== "insertion"
                        )
                        if (node.attrs.track.length === track) {
                            return true
                        }
                        tr.setNodeMarkup(
                            map.map(nodePos),
                            null,
                            Object.assign({}, node.attrs, {track}),
                            node.marks
                        )
                        // Special case: first paragraph in list item by same user -- will also be accepted.
                        if (
                            node.type.name === "list_item" &&
                            node.child(0) &&
                            node.child(0).type.name === "paragraph"
                        ) {
                            reachedEnd = false
                        }
                    } else {
                        tr.step(
                            new prosemirror_transform__rspack_import_2.AddMarkStep(
                                map.map(nodePos),
                                map.map(nodePos + node.nodeSize),
                                view.state.schema.marks.insertion.create(
                                    Object.assign({}, trackMark.attrs, {
                                        approved: true
                                    })
                                )
                            )
                        )
                    }
                } else if (type === "format_change") {
                    tr.step(
                        new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                            map.map(nodePos),
                            map.map(nodePos + node.nodeSize),
                            trackMark
                        )
                    )
                } else if (type === "block_change") {
                    const track = node.attrs.track.filter(
                        track => track.type !== "block_change"
                    )
                    tr.setNodeMarkup(
                        map.map(nodePos),
                        null,
                        Object.assign({}, node.attrs, {track}),
                        node.marks
                    )
                }
                return true
            }
            return true
        }
    )

    ;(0,_state_plugins__rspack_import_0.deactivateAllSelectedChanges)(tr)

    if (tr.steps.length) {
        view.dispatch(tr)
    }
}


}),
"./js/modules/editor/track/accept_all.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  acceptAll: function() { return acceptAll; }
});
/* import */ var prosemirror_model__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _state_plugins__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _delete__rspack_import_1 = __webpack_require__("./js/modules/editor/track/delete.js");







const acceptAll = (view, from = 0, to = false) => {
    if (!to) {
        to = view.state.doc.content.size
    }
    const tr = view.state.tr.setMeta("track", true),
        map = new prosemirror_transform__rspack_import_2.Mapping()
    view.state.doc.nodesBetween(from, to, (node, pos) => {
        if (pos < from && !node.isInline) {
            return true
        }
        let deletedNode = false
        if (node.attrs.track?.find(track => track.type === "deletion")) {
            (0,_delete__rspack_import_1.deleteNode)(tr, node, pos, map, true)
            deletedNode = true
        } else if (node.marks?.find(mark => mark.type.name === "deletion")) {
            const delStep = new prosemirror_transform__rspack_import_2.ReplaceStep(
                map.map(Math.max(pos, from)),
                map.map(Math.min(pos + node.nodeSize, to)),
                prosemirror_model__rspack_import_3.Slice.empty
            )
            tr.step(delStep)
            map.appendMap(delStep.getMap())
            deletedNode = true
        } else if (
            node.attrs.track?.find(track => track.type === "insertion")
        ) {
            const track = node.attrs.track.filter(
                track => track.type !== "insertion"
            )
            tr.setNodeMarkup(
                map.map(pos),
                null,
                Object.assign({}, node.attrs, {track}),
                node.marks
            )
        } else if (
            node.marks?.find(
                mark => mark.type.name === "insertion" && !mark.attrs.approved
            )
        ) {
            const mark = node.marks.find(
                    mark => mark.type.name === "insertion"
                ),
                attrs = Object.assign({}, mark.attrs, {approved: true})
            tr.step(
                new prosemirror_transform__rspack_import_2.AddMarkStep(
                    map.map(Math.max(pos, from)),
                    map.map(Math.min(pos + node.nodeSize, to)),
                    view.state.schema.marks.insertion.create(attrs)
                )
            )
        }
        const formatChangeMark = node.marks.find(
            mark => mark.type.name === "format_change"
        )
        if (node.isInline && !deletedNode && formatChangeMark) {
            tr.step(
                new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                    map.map(Math.max(pos, from)),
                    map.map(Math.min(pos + node.nodeSize, to)),
                    formatChangeMark
                )
            )
        }

        if (!node.isInline && !deletedNode && node.attrs.track) {
            const blockChangeTrack = node.attrs.track.find(
                track => track.type === "block_change"
            )
            if (blockChangeTrack) {
                const track = node.attrs.track.filter(
                    track => track !== blockChangeTrack
                )
                tr.setNodeMarkup(
                    map.map(pos),
                    null,
                    Object.assign({}, node.attrs, {track}),
                    node.marks
                )
            }
        }

        return true
    })

    ;(0,_state_plugins__rspack_import_0.deactivateAllSelectedChanges)(tr)

    if (tr.steps.length) {
        view.dispatch(tr)
    }
}


}),
"./js/modules/editor/track/accept_all_no_insertions.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  acceptAllNoInsertions: function() { return acceptAllNoInsertions; }
});
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _schema_footnotes__rspack_import_0 = __webpack_require__("./js/modules/schema/footnotes.js");
/* import */ var _delete__rspack_import_1 = __webpack_require__("./js/modules/editor/track/delete.js");






function acceptAllNoInsertions(doc) {
    // Same as accept all and additionally remove insertion marks/tracks
    // and no dependence on view.
    const tr = new prosemirror_transform__rspack_import_2.Transform(doc),
        map = new prosemirror_transform__rspack_import_2.Mapping()
    doc.descendants((node, pos) => {
        const deletionTrack = node.attrs.track
                ? node.attrs.track.find(track => track.type === "deletion")
                : node.marks.find(mark => mark.type.name === "deletion"),
            insertionTrack = node.attrs.track
                ? node.attrs.track.find(track => track.type === "insertion")
                : node.marks.find(mark => mark.type.name === "insertion"),
            formatChangeMark = node.marks.find(
                mark => mark.type.name === "format_change"
            ),
            blockChangeTrack = node.attrs.track
                ? node.attrs.track.find(track => track.name === "block_change")
                : false

        if (node.type.name === "footnote" && node.attrs.footnote) {
            const fnDoc = _schema_footnotes__rspack_import_0.fnSchema.nodeFromJSON({
                    type: "doc",
                    content: [
                        {
                            type: "footnotecontainer",
                            content: node.attrs.footnote
                        }
                    ]
                }),
                cleanFnDoc = acceptAllNoInsertions(fnDoc),
                fnChange = !fnDoc.eq(cleanFnDoc)
            if (fnChange) {
                tr.setNodeMarkup(
                    map.map(pos),
                    null,
                    Object.assign({}, node.attrs, {
                        footnote: cleanFnDoc.firstChild.toJSON().content
                    }),
                    node.marks
                )
            }
        }
        if (deletionTrack) {
            (0,_delete__rspack_import_1.deleteNode)(tr, node, pos, map, true)
            if (node.isInline) {
                return false
            } else {
                // Deleting a block level node will in many cases not delete the contents of the node but rather
                // merge it with the previous node.
                return true
            }
        } else if (insertionTrack) {
            if (node.isInline) {
                tr.step(
                    new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                        map.map(pos),
                        map.map(pos + node.nodeSize),
                        insertionTrack
                    )
                )
            } else {
                const track = node.attrs.track.filter(
                    track => track !== insertionTrack
                )
                tr.setNodeMarkup(
                    map.map(pos),
                    null,
                    Object.assign({}, node.attrs, {track}),
                    node.marks
                )
            }
        }
        if (node.isInline && formatChangeMark) {
            tr.step(
                new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                    map.map(pos),
                    map.map(pos + node.nodeSize),
                    formatChangeMark
                )
            )
        }
        if (blockChangeTrack) {
            const track = node.attrs.track.filter(
                track => track.type !== blockChangeTrack
            )
            tr.setNodeMarkup(
                map.map(pos),
                null,
                Object.assign({}, node.attrs, {track}),
                node.marks
            )
        }
        return true
    })
    return tr.doc
}


}),
"./js/modules/editor/track/amend_transaction.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  amendTransaction: function() { return amendTransaction; },
  trackedTransaction: function() { return trackedTransaction; }
});
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_state__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");





function markInsertion(tr, from, to, user, date1, date10, approved) {
    const insertionMark = tr.doc.type.schema.marks.insertion.create({
        user: user.id,
        username: user.username,
        date: date10,
        approved
    })
    // Add insertion mark also to block nodes (figures, text blocks) but not table cells/rows and lists.
    tr.doc.nodesBetween(from, to, (node, pos) => {
        if (node.isInline) {
            tr.removeMark(
                Math.max(from, pos),
                Math.min(pos + node.nodeSize, to),
                tr.doc.type.schema.marks.deletion
            )
            tr.removeMark(
                Math.max(from, pos),
                Math.min(pos + node.nodeSize, to),
                tr.doc.type.schema.marks.insertion
            )
            tr.addMark(
                Math.max(from, pos),
                Math.min(pos + node.nodeSize, to),
                insertionMark
            )
            return false
        } else if (
            pos < from ||
            ["bullet_list", "ordered_list"].includes(node.type.name)
        ) {
            return true
        } else if (["table_row", "table_cell"].includes(node.type.name)) {
            return false
        }
        if (node.attrs.track) {
            const track = []
            if (!approved) {
                track.push({
                    type: "insertion",
                    user: user.id,
                    username: user.username,
                    date: date1
                })
            }
            tr.setNodeMarkup(
                pos,
                null,
                Object.assign({}, node.attrs, {track}),
                node.marks
            )
        }
        if (["figure", "table"].includes(node.type.name)) {
            // A table or figure was inserted. We don't add track marks to elements inside of it.
            return false
        }
    })
}

function markDeletion(tr, from, to, user, date1, date10) {
    const deletionMark = tr.doc.type.schema.marks.deletion.create({
        user: user.id,
        username: user.username,
        date: date10
    })
    let firstTableCellChild = false
    let listItem = false
    const deletionMap = new prosemirror_transform__rspack_import_1.Mapping()
    // Add deletion mark to block nodes (figures, text blocks) and find already deleted inline nodes (and leave them alone)
    tr.doc.nodesBetween(from, to, (node, pos, _parent, _index) => {
        if (pos < from && node.type.name === "table_cell") {
            firstTableCellChild = true
            return true
        } else if ((pos < from && node.isBlock) || firstTableCellChild) {
            firstTableCellChild = false
            return true
        } else if (["table_row", "table_cell"].includes(node.type.name)) {
            return false
        } else if (
            node.isInline &&
            node.marks.find(
                mark =>
                    mark.type.name === "insertion" &&
                    mark.attrs.user === user.id &&
                    !mark.attrs.approved
            )
        ) {
            const removeStep = new prosemirror_transform__rspack_import_1.ReplaceStep(
                deletionMap.map(Math.max(from, pos)),
                deletionMap.map(Math.min(to, pos + node.nodeSize)),
                prosemirror_model__rspack_import_2.Slice.empty
            )
            if (!tr.maybeStep(removeStep).failed) {
                deletionMap.appendMap(removeStep.getMap())
            }
        } else if (
            node.isInline &&
            !node.marks.find(mark => mark.type.name === "deletion")
        ) {
            tr.addMark(
                deletionMap.map(Math.max(from, pos)),
                deletionMap.map(Math.min(to, pos + node.nodeSize)),
                deletionMark
            )
        } else if (
            !node.attrs.track?.find(
                trackAttr => trackAttr.type === "deletion"
            ) &&
            !["bullet_list", "ordered_list"].includes(node.type.name)
        ) {
            if (
                node.attrs.track?.find(
                    trackAttr =>
                        trackAttr.type === "insertion" &&
                        trackAttr.user === user.id
                )
            ) {
                let removeStep
                // user has created element. so (s)he is allowed to delete it again.
                if (node.isTextblock && to < pos + node.nodeSize) {
                    // The node is a textblock. So we need to merge into the last possible
                    // position inside the last text block.
                    const selectionBefore = prosemirror_state__rspack_import_3.Selection.findFrom(
                        tr.doc.resolve(pos),
                        -1
                    )
                    if (selectionBefore instanceof prosemirror_state__rspack_import_3.TextSelection) {
                        removeStep = new prosemirror_transform__rspack_import_1.ReplaceStep(
                            deletionMap.map(selectionBefore.$anchor.pos),
                            deletionMap.map(to),
                            prosemirror_model__rspack_import_2.Slice.empty
                        )
                    }
                } else {
                    removeStep = new prosemirror_transform__rspack_import_1.ReplaceStep(
                        deletionMap.map(Math.max(from, pos)),
                        deletionMap.map(Math.min(to, pos + node.nodeSize)),
                        prosemirror_model__rspack_import_2.Slice.empty
                    )
                }

                if (!tr.maybeStep(removeStep).failed) {
                    deletionMap.appendMap(removeStep.getMap())
                }
                if (node.type.name === "list_item" && listItem) {
                    listItem = false
                }
            } else if (node.attrs.track) {
                if (node.type.name === "list_item") {
                    listItem = true
                } else if (listItem) {
                    // The first child of the first list item (likely a par) will not be merged with the paragraph
                    // before it.
                    listItem = false
                    return
                }
                const track = node.attrs.track.slice()
                track.push({
                    type: "deletion",
                    user: user.id,
                    username: user.username,
                    date: date1
                })
                tr.setNodeMarkup(
                    deletionMap.map(pos),
                    null,
                    Object.assign({}, node.attrs, {track}),
                    node.marks
                )
            }
            if (node.type.name === "figure") {
                return false
            }
        }
    })

    return deletionMap
}

function markWrapping(tr, pos, oldNode, newNode, user, date1) {
    let track = oldNode.attrs.track.slice(),
        blockTrack = track.find(track => track.type === "block_change")

    if (blockTrack) {
        track = track.filter(track => track !== blockTrack)
        if (
            blockTrack.before.type !== newNode.type.name ||
            blockTrack.before.attrs.level !== newNode.attrs.level
        ) {
            blockTrack = {
                type: "block_change",
                user: user.id,
                username: user.username,
                date: date1,
                before: blockTrack.before
            }
            track.push(blockTrack)
        }
    } else {
        blockTrack = {
            type: "block_change",
            user: user.id,
            username: user.username,
            date: date1,
            before: {type: oldNode.type.name, attrs: oldNode.attrs}
        }
        if (blockTrack.before.attrs.id) {
            delete blockTrack.before.attrs.id
        }
        if (blockTrack.before.attrs.track) {
            delete blockTrack.before.attrs.track
        }
        track.push(blockTrack)
    }
    if (tr.doc.nodeAt(pos)) {
        tr.setNodeMarkup(pos, null, Object.assign({}, newNode.attrs, {track}))
    }
}

function amendTransaction(tr, state, editor) {
    if (
        !tr.steps.length ||
        (tr.meta &&
            !Object.keys(tr.meta).every(
                // Only replace TRs that have no metadata or only inputType metadata
                metadata => ["inputType", "uiEvent", "paste"].includes(metadata)
            )) ||
        // don't replace history TRs
        ["historyUndo", "historyRedo"].includes(tr.getMeta("inputType"))
    ) {
        // None of the transactions change the doc, or all are remote, come from footnotes,
        // are footnote creations, history or fixing IDs. Give up.
        return tr
    } else {
        return trackedTransaction(
            tr,
            state,
            editor.user,
            !editor.view.state.doc.attrs.tracked &&
                !["write-tracked", "review-tracked"].includes(
                    editor.docInfo.access_rights
                ),
            Date.now() - editor.clientTimeAdjustment
        )
    }
}

function trackedTransaction(tr, state, user, approved, date) {
    const newTr = state.tr,
        map = new prosemirror_transform__rspack_import_1.Mapping(),
        date10 = Math.floor(date / 600000) * 10, // 10 minute interval
        date1 = Math.floor(date / 60000), // 1 minute interval
        // We only insert content if this is not directly a tr for cell deletion. This is because tables delete rows by deleting the
        // content of each cell and replacing it with an empty paragraph.
        cellDeleteTr =
            ["deleteContentBackward", "deleteContentForward"].includes(
                tr.getMeta("inputType")
            ) && state.selection instanceof prosemirror_tables__rspack_import_0.CellSelection
    tr.steps.forEach((originalStep, originalStepIndex) => {
        const step = originalStep.map(map),
            doc = newTr.doc
        if (!step) {
            return
        }
        if (step instanceof prosemirror_transform__rspack_import_1.ReplaceStep) {
            const newStep = approved
                ? step
                : step.slice.size && !cellDeleteTr
                  ? new prosemirror_transform__rspack_import_1.ReplaceStep(
                        step.to, // We insert all the same steps, but with "from"/"to" both set to "to" in order not to delete content. Mapped as needed.
                        step.to,
                        step.slice,
                        step.structure
                    )
                  : false
            // We didn't apply the original step in its original place. We adjust the map accordingly.
            const invertStep = originalStep
                .invert(tr.docs[originalStepIndex])
                .map(map)
            map.appendMap(invertStep.getMap())
            if (newStep) {
                const trTemp = state.apply(newTr).tr
                if (!trTemp.maybeStep(newStep).failed) {
                    const mappedNewStepTo = newStep.getMap().map(newStep.to)
                    markInsertion(
                        trTemp,
                        newStep.from,
                        mappedNewStepTo,
                        user,
                        date1,
                        date10,
                        approved
                    )
                    // We condense it down to a single replace step.
                    const condensedStep = new prosemirror_transform__rspack_import_1.ReplaceStep(
                        newStep.from,
                        newStep.to,
                        trTemp.doc.slice(newStep.from, mappedNewStepTo)
                    )
                    newTr.step(condensedStep)
                    const mirrorIndex = map.maps.length - 1
                    map.appendMap(condensedStep.getMap(), mirrorIndex)
                    if (!newTr.selection.eq(trTemp.selection)) {
                        newTr.setSelection(
                            prosemirror_state__rspack_import_3.Selection.fromJSON(trTemp.selection.toJSON())
                        )
                    }
                }
            }
            if (!approved && step.from !== step.to) {
                map.appendMap(
                    markDeletion(newTr, step.from, step.to, user, date1, date10)
                )
            }
        } else if (approved) {
            newTr.step(step)
        } else if (step instanceof prosemirror_transform__rspack_import_1.ReplaceAroundStep) {
            if (step.from === step.gapFrom && step.to === step.gapTo) {
                // wrapped in something
                newTr.step(step)
                const from = step.getMap().map(step.from, -1)
                const to = step.getMap().map(step.gapFrom)
                markInsertion(newTr, from, to, user, date1, date10, false)
            } else if (
                !step.slice.size ||
                step.slice.content.content.length === 2
            ) {
                // unwrapped from something
                const invertStep = originalStep
                    .invert(tr.docs[originalStepIndex])
                    .map(map)
                map.appendMap(invertStep.getMap())
                map.appendMap(
                    markDeletion(
                        newTr,
                        step.from,
                        step.gapFrom,
                        user,
                        date1,
                        date10
                    )
                )
            } else if (
                step.slice.size === 2 &&
                step.gapFrom - step.from === 1 &&
                step.to - step.gapTo === 1
            ) {
                // Replaced one wrapping with another
                newTr.step(step)
                const oldNode = doc.nodeAt(step.from)
                if (oldNode.attrs.track) {
                    markWrapping(
                        newTr,
                        step.from,
                        oldNode,
                        step.slice.content.firstChild,
                        user,
                        date1
                    )
                }
            } else {
                newTr.step(step)
                const ranges = [
                    {
                        from: step.getMap().map(step.from, -1),
                        to: step.getMap().map(step.gapFrom)
                    },
                    {
                        from: step.getMap().map(step.gapTo, -1),
                        to: step.getMap().map(step.to)
                    }
                ]
                ranges.forEach(range =>
                    doc.nodesBetween(range.from, range.to, (_node, pos) => {
                        if (pos < range.from) {
                            return true
                        }
                        markInsertion(
                            newTr,
                            range.from,
                            range.to,
                            user,
                            date1,
                            date10,
                            false
                        )
                    })
                )
            }
        } else if (step instanceof prosemirror_transform__rspack_import_1.AddMarkStep) {
            doc.nodesBetween(step.from, step.to, (node, pos) => {
                if (!node.isInline) {
                    return true
                }
                if (node.marks.find(mark => mark.type.name === "deletion")) {
                    return false
                } else {
                    newTr.addMark(
                        Math.max(step.from, pos),
                        Math.min(step.to, pos + node.nodeSize),
                        step.mark
                    )
                }
                if (
                    ["em", "strong", "underline"].includes(
                        step.mark.type.name
                    ) &&
                    !node.marks.find(mark => mark.type === step.mark.type)
                ) {
                    const formatChangeMark = node.marks.find(
                        mark => mark.type.name === "format_change"
                    )
                    let after, before
                    if (formatChangeMark) {
                        if (
                            formatChangeMark.attrs.before.includes(
                                step.mark.type.name
                            )
                        ) {
                            before = formatChangeMark.attrs.before.filter(
                                markName => markName !== step.mark.type.name
                            )
                            after = formatChangeMark.attrs.after
                        } else {
                            before = formatChangeMark.attrs.before
                            after = formatChangeMark.attrs.after.concat(
                                step.mark.type.name
                            )
                        }
                    } else {
                        before = []
                        after = [step.mark.type.name]
                    }
                    if (after.length || before.length) {
                        newTr.addMark(
                            Math.max(step.from, pos),
                            Math.min(step.to, pos + node.nodeSize),
                            state.schema.marks.format_change.create({
                                user: user.id,
                                username: user.username,
                                date: date10,
                                before,
                                after
                            })
                        )
                    } else if (formatChangeMark) {
                        newTr.removeMark(
                            Math.max(step.from, pos),
                            Math.min(step.to, pos + node.nodeSize),
                            formatChangeMark
                        )
                    }
                }
            })
        } else if (step instanceof prosemirror_transform__rspack_import_1.RemoveMarkStep) {
            doc.nodesBetween(step.from, step.to, (node, pos) => {
                if (!node.isInline) {
                    return true
                }
                if (node.marks.find(mark => mark.type.name === "deletion")) {
                    return false
                } else {
                    newTr.removeMark(
                        Math.max(step.from, pos),
                        Math.min(step.to, pos + node.nodeSize),
                        step.mark
                    )
                }

                if (
                    ["em", "strong", "underline"].includes(
                        step.mark.type.name
                    ) &&
                    node.marks.find(mark => mark.type === step.mark.type)
                ) {
                    const formatChangeMark = node.marks.find(
                        mark => mark.type.name === "format_change"
                    )
                    let after, before
                    if (formatChangeMark) {
                        if (
                            formatChangeMark.attrs.after.includes(
                                step.mark.type.name
                            )
                        ) {
                            after = formatChangeMark.attrs.after.filter(
                                markName => markName !== step.mark.type.name
                            )
                            before = formatChangeMark.attrs.before
                        } else {
                            after = formatChangeMark.attrs.after
                            before = formatChangeMark.attrs.before.concat(
                                step.mark.type.name
                            )
                        }
                    } else {
                        after = []
                        before = [step.mark.type.name]
                    }
                    if (after.length || before.length) {
                        newTr.addMark(
                            Math.max(step.from, pos),
                            Math.min(step.to, pos + node.nodeSize),
                            state.schema.marks.format_change.create({
                                user: user.id,
                                username: user.username,
                                date: date10,
                                before,
                                after
                            })
                        )
                    } else if (formatChangeMark) {
                        newTr.removeMark(
                            Math.max(step.from, pos),
                            Math.min(step.to, pos + node.nodeSize),
                            formatChangeMark
                        )
                    }
                }
            })
        }
    })

    // We copy the input type meta data from the original transaction.
    if (tr.getMeta("inputType")) {
        newTr.setMeta("inputType", tr.getMeta("inputType"))
    }
    if (tr.getMeta("uiEvent")) {
        newTr.setMeta("uiEvent", tr.getMeta("uiEvent"))
    }

    if (tr.selectionSet) {
        if (
            tr.selection instanceof prosemirror_state__rspack_import_3.TextSelection &&
            (tr.selection.from < state.selection.from ||
                tr.getMeta("inputType") === "deleteContentBackward")
        ) {
            const caretPos = map.map(tr.selection.from, -1)
            newTr.setSelection(new prosemirror_state__rspack_import_3.TextSelection(newTr.doc.resolve(caretPos)))
        } else {
            newTr.setSelection(tr.selection.map(newTr.doc, map))
        }
    }
    if (tr.storedMarksSet) {
        newTr.setStoredMarks(tr.storedMarks)
    }
    if (tr.scrolledIntoView) {
        newTr.scrollIntoView()
    }

    return newTr
}


}),
"./js/modules/editor/track/delete.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  deleteNode: function() { return deleteNode; }
});
/* import */ var prosemirror_model__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_schema_list__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-list@1.5.1/node_modules/prosemirror-schema-list/dist/index.js");
/* import */ var prosemirror_state__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-state@1.4.3/node_modules/prosemirror-state/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");





const deleteNode = (tr, node, nodePos, map, accept) => {
    // Delete a node either because a deletion has been accepted or an insertion rejected.
    const newNodePos = map.map(nodePos),
        trackType = accept ? "deletion" : "insertion"
    let delStep
    if (node.isTextblock) {
        const selectionBefore = prosemirror_state__rspack_import_0.Selection.findFrom(
            tr.doc.resolve(newNodePos),
            -1
        )
        if (selectionBefore instanceof prosemirror_state__rspack_import_0.TextSelection) {
            const start = selectionBefore.$anchor.pos,
                end = newNodePos + 1
            let allowMerge = true
            // Make sure there is no isolating nodes inbetween.
            tr.doc.nodesBetween(start, end, (node, pos) => {
                if (pos < start) {
                    return true
                }
                if (node.type.spec.isolating) {
                    allowMerge = false
                }
            })
            if (allowMerge) {
                delStep = (0,prosemirror_transform__rspack_import_1.replaceStep)(tr.doc, start, end)
            } else {
                const track = node.attrs.track.filter(
                    track => track.type !== trackType
                )
                tr.setNodeMarkup(
                    newNodePos,
                    null,
                    Object.assign({}, node.attrs, {track}),
                    node.marks
                )
            }
        } else {
            // There is a block node right in front of it that cannot be removed. Give up. (table/figure/etc.)
            const track = node.attrs.track.filter(
                track => track.type !== trackType
            )
            tr.setNodeMarkup(
                newNodePos,
                null,
                Object.assign({}, node.attrs, {track}),
                node.marks
            )
        }
    } else if (node.isLeaf || ["figure", "table"].includes(node.type.name)) {
        delStep = new prosemirror_transform__rspack_import_1.ReplaceStep(
            newNodePos,
            map.map(nodePos + node.nodeSize),
            prosemirror_model__rspack_import_2.Slice.empty
        )
    } else if (node.type === tr.doc.type.schema.nodes["list_item"]) {
        const state = prosemirror_state__rspack_import_0.EditorState.create({
            doc: tr.doc,
            selection: prosemirror_state__rspack_import_0.Selection.findFrom(tr.doc.resolve(newNodePos), 1)
        })
        ;(0,prosemirror_schema_list__rspack_import_3.liftListItem)(node.type)(state, newTr => {
            newTr.steps.forEach(step => {
                tr.step(step)
                map.appendMap(step.getMap())
            })
        })
    } else {
        const end = map.map(nodePos + node.nodeSize)
        delStep = new prosemirror_transform__rspack_import_1.ReplaceAroundStep(
            newNodePos,
            end,
            newNodePos + 1,
            end - 1,
            prosemirror_model__rspack_import_2.Slice.empty,
            0,
            true
        )
    }
    if (delStep) {
        tr.step(delStep)
        const stepMap = delStep.getMap()
        map.appendMap(stepMap)
    }
}


}),
"./js/modules/editor/track/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModTrack: function() { return /* reexport safe */ _mod__rspack_import_0.ModTrack; },
  acceptAll: function() { return /* reexport safe */ _accept_all__rspack_import_3.acceptAll; },
  acceptAllNoInsertions: function() { return /* reexport safe */ _accept_all_no_insertions__rspack_import_1.acceptAllNoInsertions; },
  amendTransaction: function() { return /* reexport safe */ _amend_transaction__rspack_import_2.amendTransaction; },
  rejectAll: function() { return /* reexport safe */ _reject_all__rspack_import_4.rejectAll; },
  trackedTransaction: function() { return /* reexport safe */ _amend_transaction__rspack_import_2.trackedTransaction; }
});
/* import */ var _mod__rspack_import_0 = __webpack_require__("./js/modules/editor/track/mod.js");
/* import */ var _accept_all_no_insertions__rspack_import_1 = __webpack_require__("./js/modules/editor/track/accept_all_no_insertions.js");
/* import */ var _amend_transaction__rspack_import_2 = __webpack_require__("./js/modules/editor/track/amend_transaction.js");
/* import */ var _accept_all__rspack_import_3 = __webpack_require__("./js/modules/editor/track/accept_all.js");
/* import */ var _reject_all__rspack_import_4 = __webpack_require__("./js/modules/editor/track/reject_all.js");







}),
"./js/modules/editor/track/mod.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ModTrack: function() { return ModTrack; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _state_plugins__rspack_import_1 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _accept__rspack_import_2 = __webpack_require__("./js/modules/editor/track/accept.js");
/* import */ var _accept_all__rspack_import_3 = __webpack_require__("./js/modules/editor/track/accept_all.js");
/* import */ var _reject__rspack_import_4 = __webpack_require__("./js/modules/editor/track/reject.js");
/* import */ var _reject_all__rspack_import_5 = __webpack_require__("./js/modules/editor/track/reject_all.js");








// Helper functions related to tracked changes
class ModTrack {
    constructor(editor) {
        editor.mod.track = this
        this.editor = editor
        this.bindEvents()
    }

    bindEvents() {
        // Bind all the click events related to track changes
        document.body.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_0.findTarget)(event, ".track-accept", el): {
                    let boxNumber = 0
                    let seekItem = el.target.closest(".margin-box")
                    while (seekItem.previousElementSibling) {
                        boxNumber += 1
                        seekItem = seekItem.previousElementSibling
                    }
                    const box =
                        this.editor.mod.marginboxes.marginBoxes[boxNumber]
                    ;(0,_accept__rspack_import_2.accept)(
                        el.target.dataset.type,
                        box.pos,
                        box.view === "main"
                            ? this.editor.view
                            : this.editor.mod.footnotes.fnEditor.view
                    )
                    // Activate the next margin box with the same number
                    const newBox =
                        this.editor.mod.marginboxes.marginBoxes[boxNumber]
                    if (newBox) {
                        this.editor.mod.track.activateTrack(
                            newBox.view,
                            newBox.type,
                            newBox.pos
                        )
                    }
                    break
                }
                case (0,_common__rspack_import_0.findTarget)(event, ".track-reject", el): {
                    let boxNumber = 0
                    let seekItem = el.target.closest(".margin-box")
                    while (seekItem.previousElementSibling) {
                        boxNumber += 1
                        seekItem = seekItem.previousElementSibling
                    }
                    const box =
                        this.editor.mod.marginboxes.marginBoxes[boxNumber]
                    ;(0,_reject__rspack_import_4.reject)(
                        el.target.dataset.type,
                        box.pos,
                        box.view === "main"
                            ? this.editor.view
                            : this.editor.mod.footnotes.fnEditor.view
                    )
                    // Activate the next margin box with the same number
                    const newBox =
                        this.editor.mod.marginboxes.marginBoxes[boxNumber]
                    if (newBox) {
                        this.editor.mod.track.activateTrack(
                            newBox.view,
                            newBox.type,
                            newBox.pos
                        )
                    }
                    break
                }
                default:
                    break
            }
        })
    }

    activateTrack(viewName, type, pos) {
        this.editor.mod.comments.interactions.deactivateAll()
        const view =
            viewName === "main"
                ? this.editor.view
                : this.editor.mod.footnotes.fnEditor.view
        const otherView =
            viewName === "main"
                ? this.editor.mod.footnotes.fnEditor.view
                : this.editor.view
        // remove all selected changes in other view
        otherView.dispatch((0,_state_plugins__rspack_import_1.deactivateAllSelectedChanges)(otherView.state.tr))
        // activate selected change in relevant view
        const tr = (0,_state_plugins__rspack_import_1.setSelectedChanges)(view.state, type, pos)
        if (tr) {
            this.editor.currentView = view
            view.dispatch(tr)
        }
    }

    rejectAll() {
        (0,_reject_all__rspack_import_5.rejectAll)(this.editor.mod.footnotes.fnEditor.view)
        ;(0,_reject_all__rspack_import_5.rejectAll)(this.editor.view)
    }

    acceptAll() {
        (0,_accept_all__rspack_import_3.acceptAll)(this.editor.mod.footnotes.fnEditor.view)
        ;(0,_accept_all__rspack_import_3.acceptAll)(this.editor.view)
    }
}


}),
"./js/modules/editor/track/reject.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  reject: function() { return reject; }
});
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _state_plugins__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _delete__rspack_import_1 = __webpack_require__("./js/modules/editor/track/delete.js");





const reject = (type, pos, view) => {
    const tr = view.state.tr.setMeta("track", true),
        map = new prosemirror_transform__rspack_import_2.Mapping()
    let reachedEnd = false,
        inlineChange = false
    const trackMark = view.state.doc
        .nodeAt(pos)
        .marks.find(mark => mark.type.name === type)
    view.state.doc.nodesBetween(
        pos,
        view.state.doc.nodeSize - 2,
        (node, nodePos) => {
            if (nodePos < pos) {
                return true
            }
            if (reachedEnd) {
                return false
            }
            if (!node.isInline) {
                reachedEnd = true // Changes on inline nodes are applied/reject until next non-inline node. Non-inline node changes are only applied that one node by default.
                if (inlineChange) {
                    // Change has already affected inline node. Don't apply to block level.
                    return false
                }
            } else if (!trackMark.isInSet(node.marks)) {
                reachedEnd = true
                return false
            } else {
                inlineChange = true
            }
            if (type === "insertion") {
                (0,_delete__rspack_import_1.deleteNode)(tr, node, nodePos, map, false)
            } else if (type === "deletion") {
                if (node.attrs.track) {
                    const track = node.attrs.track.filter(
                        track => track.type !== "deletion"
                    )
                    tr.setNodeMarkup(
                        map.map(nodePos),
                        null,
                        Object.assign({}, node.attrs, {track}),
                        node.marks
                    )
                    reachedEnd = true
                } else {
                    tr.removeMark(
                        map.map(nodePos),
                        map.map(nodePos + node.nodeSize),
                        view.state.schema.marks.deletion
                    )
                }
            } else if (type === "format_change") {
                trackMark.attrs.before.forEach(oldMark =>
                    tr.step(
                        new prosemirror_transform__rspack_import_2.AddMarkStep(
                            map.map(nodePos),
                            map.map(nodePos + node.nodeSize),
                            view.state.schema.marks[oldMark].create()
                        )
                    )
                )
                trackMark.attrs.after.forEach(newMark => {
                    tr.step(
                        new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                            map.map(nodePos),
                            map.map(nodePos + node.nodeSize),
                            node.marks.find(mark => mark.type.name === newMark)
                        )
                    )
                })
                tr.step(
                    new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                        map.map(nodePos),
                        map.map(nodePos + node.nodeSize),
                        trackMark
                    )
                )
            } else if (type === "block_change") {
                const blockChangeTrack = node.attrs.track?.find(
                        track => track.type === "block_change"
                    ),
                    track = node.attrs.track?.filter(
                        track => track !== blockChangeTrack
                    )
                if (!blockChangeTrack) {
                    return true
                }
                tr.setNodeMarkup(
                    map.map(nodePos),
                    view.state.schema.nodes[blockChangeTrack.before.type],
                    Object.assign(
                        {},
                        node.attrs,
                        blockChangeTrack.before.attrs,
                        {
                            track
                        }
                    ),
                    node.marks
                )
            }
            return true
        }
    )

    ;(0,_state_plugins__rspack_import_0.deactivateAllSelectedChanges)(tr)

    if (tr.steps.length) {
        view.dispatch(tr)
    }
}


}),
"./js/modules/editor/track/reject_all.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  rejectAll: function() { return rejectAll; }
});
/* import */ var prosemirror_model__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_transform__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/prosemirror-transform@1.10.3/node_modules/prosemirror-transform/dist/index.js");
/* import */ var _state_plugins__rspack_import_0 = __webpack_require__("./js/modules/editor/state_plugins/index.js");
/* import */ var _delete__rspack_import_1 = __webpack_require__("./js/modules/editor/track/delete.js");







const rejectAll = (view, from = 0, to = false) => {
    if (!to) {
        to = view.state.doc.content.size
    }
    const tr = view.state.tr.setMeta("track", true),
        map = new prosemirror_transform__rspack_import_2.Mapping()
    view.state.doc.nodesBetween(from, to, (node, pos) => {
        if (pos < from && !node.isInline) {
            return true
        }
        let deletedNode = false
        if (node.attrs.track?.find(track => track.type === "insertion")) {
            (0,_delete__rspack_import_1.deleteNode)(tr, node, pos, map, false)
            deletedNode = true
        } else if (
            node.marks?.find(
                mark => mark.type.name === "insertion" && !mark.attrs.approved
            )
        ) {
            const delStep = new prosemirror_transform__rspack_import_2.ReplaceStep(
                map.map(Math.max(pos, from)),
                map.map(Math.min(pos + node.nodeSize, to)),
                prosemirror_model__rspack_import_3.Slice.empty
            )
            tr.step(delStep)
            map.appendMap(delStep.getMap())
            deletedNode = true
        } else if (node.attrs.track?.find(track => track.type === "deletion")) {
            const track = node.attrs.track.filter(
                track => track.type !== "deletion"
            )
            tr.setNodeMarkup(
                map.map(pos),
                null,
                Object.assign({}, node.attrs, {track}),
                node.marks
            )
        } else if (node.marks?.find(mark => mark.type.name === "deletion")) {
            tr.removeMark(
                map.map(Math.max(pos, from)),
                map.map(Math.min(pos + node.nodeSize, to)),
                view.state.schema.marks.deletion
            )
        }
        const formatChangeMark = node.marks.find(
            mark => mark.type.name === "format_change"
        )

        if (node.isInline && !deletedNode && formatChangeMark) {
            formatChangeMark.attrs.before.forEach(oldMark =>
                tr.step(
                    new prosemirror_transform__rspack_import_2.AddMarkStep(
                        map.map(Math.max(pos, from)),
                        map.map(Math.min(pos + node.nodeSize, to)),
                        view.state.schema.marks[oldMark].create()
                    )
                )
            )
            formatChangeMark.attrs.after.forEach(newMark => {
                tr.step(
                    new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                        map.map(Math.max(pos, from)),
                        map.map(Math.min(pos + node.nodeSize, to)),
                        node.marks.find(mark => mark.type.name === newMark)
                    )
                )
            })

            tr.step(
                new prosemirror_transform__rspack_import_2.RemoveMarkStep(
                    map.map(Math.max(pos, from)),
                    map.map(Math.min(pos + node.nodeSize, to)),
                    formatChangeMark
                )
            )
        }
        if (!node.isInline && !deletedNode && node.attrs.track) {
            const blockChangeTrack = node.attrs.track.find(
                track => track.type === "block_change"
            )
            if (blockChangeTrack) {
                const track = node.attrs.track.filter(
                    track => track !== blockChangeTrack
                )
                tr.setNodeMarkup(
                    map.map(pos),
                    view.state.schema.nodes[blockChangeTrack.before.type],
                    Object.assign(
                        {},
                        node.attrs,
                        blockChangeTrack.before.attrs,
                        {
                            track
                        }
                    ),
                    node.marks
                )
            }
        }

        return true
    })

    ;(0,_state_plugins__rspack_import_0.deactivateAllSelectedChanges)(tr)

    if (tr.steps.length) {
        view.dispatch(tr)
    }
}


}),
"./js/modules/exporter/native/copy.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SaveCopy: function() { return SaveCopy; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _editor_e2ee_encryptor__rspack_import_1 = __webpack_require__("./js/modules/editor/e2ee/encryptor.js");
/* import */ var _editor_e2ee_key_manager__rspack_import_2 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _editor_e2ee_password_dialog__rspack_import_3 = __webpack_require__("./js/modules/editor/e2ee/password-dialog.js");
/* import */ var _importer_native__rspack_import_4 = __webpack_require__("./js/modules/importer/native/index.js");
/* import */ var _shrink__rspack_import_5 = __webpack_require__("./js/modules/exporter/native/shrink.js");







/* Saves a copy of the document. The owner may change in that process, if the
  old document was owned by someone else than the current user.
*/
class SaveCopy {
    constructor(
        doc,
        bibDB,
        imageDB,
        newUser,
        importId = null,
        e2eeOptions = null
    ) {
        this.doc = doc
        this.bibDB = bibDB
        this.imageDB = imageDB
        this.newUser = newUser
        this.importId = importId
        this.e2eeOptions = e2eeOptions // {sourceKey?: CryptoKey, targetE2EE?: boolean, targetPassword?: string}
    }

    init() {
        let shrinkerPromise
        if (this.doc.e2ee) {
            // Decrypt the document content first
            shrinkerPromise = this._decryptDocument().then(decryptedDoc => {
                const shrinker = new _shrink__rspack_import_5.ShrinkFidus(
                    decryptedDoc,
                    this.imageDB,
                    this.bibDB
                )
                return shrinker.init()
            })
        } else {
            const shrinker = new _shrink__rspack_import_5.ShrinkFidus(this.doc, this.imageDB, this.bibDB)
            shrinkerPromise = shrinker.init()
        }
        return shrinkerPromise
            .then(({doc, shrunkImageDB, shrunkBibDB, httpIncludes}) => {
                let targetE2EEPromise
                if (this.e2eeOptions && this.e2eeOptions.targetE2EE) {
                    targetE2EEPromise = this._setupTargetE2EE(
                        doc,
                        shrunkImageDB
                    )
                } else {
                    targetE2EEPromise = Promise.resolve({
                        doc,
                        e2eeOptions: null
                    })
                }
                return targetE2EEPromise.then(
                    ({doc: encryptedDoc, e2eeOptions}) => {
                        const importerE2EEOptions = e2eeOptions || {}
                        if (this.e2eeOptions && this.e2eeOptions.sourceKey) {
                            importerE2EEOptions.sourceKey =
                                this.e2eeOptions.sourceKey
                        }
                        const importer = new _importer_native__rspack_import_4.NativeImporter(
                            encryptedDoc,
                            shrunkBibDB,
                            shrunkImageDB,
                            httpIncludes,
                            this.newUser,
                            this.importId,
                            (0,_common__rspack_import_0.longFilePath)(
                                doc.title,
                                doc.path,
                                `${gettext("Copy of")} `
                            ),
                            null,
                            importerE2EEOptions
                        )
                        return importer.init()
                    }
                )
            })
            .then(({doc, docInfo}) => {
                (0,_common__rspack_import_0.addAlert)(
                    "info",
                    `${(0,_common__rspack_import_0.escapeText)(doc.title)} ${gettext(" successfully copied.")}`
                )
                return Promise.resolve({doc, docInfo})
            })
    }

    async _decryptDocument() {
        const key = this.e2eeOptions?.sourceKey
        if (!key) {
            throw new Error("Missing source E2EE key for decryption")
        }
        const decryptedDoc = Object.assign({}, this.doc)
        if (typeof decryptedDoc.content === "string") {
            decryptedDoc.content = await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.decryptObject(
                decryptedDoc.content,
                key
            )
        }
        if (typeof decryptedDoc.comments === "string") {
            decryptedDoc.comments = await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.decryptObject(
                decryptedDoc.comments,
                key
            )
        }
        if (typeof decryptedDoc.bibliography === "string") {
            decryptedDoc.bibliography = await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.decryptObject(
                decryptedDoc.bibliography,
                key
            )
        }
        // Also update this.bibDB.db if it holds the encrypted bibliography
        if (this.bibDB && typeof this.bibDB.db === "string") {
            this.bibDB.db = decryptedDoc.bibliography
        }
        // Decrypt image copyright if it was encrypted.
        // Image file decryption is handled by NativeImporter
        // after GetImages fetches the encrypted file from the URL.
        if (this.imageDB && this.imageDB.db) {
            await Promise.all(
                Object.values(this.imageDB.db).map(async imageEntry => {
                    if (
                        typeof imageEntry.copyright === "string" &&
                        imageEntry.copyright.length > 0
                    ) {
                        try {
                            imageEntry.copyright =
                                await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.decryptObject(
                                    imageEntry.copyright,
                                    key
                                )
                        } catch (_e) {
                            // If decryption fails, leave as-is
                        }
                    }
                })
            )
        }
        return decryptedDoc
    }

    async _setupTargetE2EE(doc, shrunkImageDB) {
        const password = this.e2eeOptions.targetPassword
        if (!password) {
            throw new Error("Missing target E2EE password")
        }
        const salt = _editor_e2ee_key_manager__rspack_import_2.E2EEKeyManager.generateSalt()
        const saltBase64 = btoa(String.fromCharCode(...salt))
        const iterations = 600000
        const key = await _editor_e2ee_key_manager__rspack_import_2.E2EEKeyManager.deriveKey(password, salt, iterations)

        // Keep doc content/comments/bibliography as plaintext.
        // NativeImporter needs plaintext for template extraction and
        // reference ID translation. Encryption happens in saveDocument().
        const plainDoc = Object.assign({}, doc)

        // Encrypt images directly on shrunkImageDB so there is no need
        // for a separate sync step afterwards.
        if (shrunkImageDB) {
            await Promise.all(
                Object.values(shrunkImageDB).map(async imageEntry => {
                    if (imageEntry.file) {
                        try {
                            const encryptedFile =
                                await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.encryptImage(
                                    imageEntry.file,
                                    key
                                )
                            imageEntry.file = encryptedFile
                            imageEntry.original_file_type = imageEntry.file_type
                            imageEntry.file_type = "application/octet-stream"
                        } catch (_e) {
                            // If encryption fails, keep original file
                        }
                    }
                    // Encrypt copyright metadata
                    if (imageEntry.copyright) {
                        try {
                            imageEntry.copyright =
                                await _editor_e2ee_encryptor__rspack_import_1.E2EEEncryptor.encryptObject(
                                    imageEntry.copyright,
                                    key
                                )
                        } catch (_e) {
                            // If encryption fails, keep original
                        }
                    }
                })
            )
        }

        return {
            doc: plainDoc,
            e2eeOptions: {
                enabled: true,
                key: key,
                salt: saltBase64,
                iterations: iterations
            }
        }
    }
}


}),
"./js/modules/exporter/native/file.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ExportFidusFile: function() { return ExportFidusFile; }
});
/* import */ var downloadjs__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/downloadjs@1.4.7/node_modules/downloadjs/download.js");
/* import */ var downloadjs__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(downloadjs__rspack_import_0);
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _tools_file__rspack_import_2 = __webpack_require__("./js/modules/exporter/tools/file.js");
/* import */ var _shrink__rspack_import_3 = __webpack_require__("./js/modules/exporter/native/shrink.js");
/* import */ var _zip__rspack_import_4 = __webpack_require__("./js/modules/exporter/native/zip.js");







class ExportFidusFile {
    constructor(doc, bibDB, imageDB, includeTemplate = true, token = false) {
        this.doc = doc
        this.bibDB = bibDB
        this.imageDB = imageDB
        this.includeTemplate = includeTemplate
        this.token = token
        return this.init()
    }

    init() {
        const shrinker = new _shrink__rspack_import_3.ShrinkFidus(this.doc, this.imageDB, this.bibDB)
        return shrinker
            .init()
            .then(({doc, shrunkImageDB, shrunkBibDB, httpIncludes}) => {
                const zipper = new _zip__rspack_import_4.ZipFidus(
                    this.doc.id,
                    doc,
                    shrunkImageDB,
                    shrunkBibDB,
                    httpIncludes,
                    this.includeTemplate,
                    this.token
                )
                return zipper.init()
            })
            .then(blob =>
                downloadjs__rspack_import_0_default()(
                    blob,
                    (0,_tools_file__rspack_import_2.createSlug)((0,_common__rspack_import_1.shortFileTitle)(this.doc.title, this.doc.path)) +
                        ".fidus",
                    "application/fidus+zip"
                )
            )
    }
}


}),
"./js/modules/exporter/native/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ExportFidusFile: function() { return /* reexport safe */ _file__rspack_import_0.ExportFidusFile; },
  SaveCopy: function() { return /* reexport safe */ _copy__rspack_import_2.SaveCopy; },
  SaveRevision: function() { return /* reexport safe */ _revision__rspack_import_1.SaveRevision; }
});
/* import */ var _file__rspack_import_0 = __webpack_require__("./js/modules/exporter/native/file.js");
/* import */ var _revision__rspack_import_1 = __webpack_require__("./js/modules/exporter/native/revision.js");
/* import */ var _copy__rspack_import_2 = __webpack_require__("./js/modules/exporter/native/copy.js");





}),
"./js/modules/exporter/native/revision.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SaveRevision: function() { return SaveRevision; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _tools_file__rspack_import_1 = __webpack_require__("./js/modules/exporter/tools/file.js");
/* import */ var _shrink__rspack_import_2 = __webpack_require__("./js/modules/exporter/native/shrink.js");
/* import */ var _zip__rspack_import_3 = __webpack_require__("./js/modules/exporter/native/zip.js");





/** Create a Fidus Writer document and upload it to the server as a backup.
 * @function uploadNative
 * @param editor The editor from which to upload the document.
 */

class SaveRevision {
    constructor(doc, imageDB, bibDB, note, app) {
        this.doc = doc
        this.imageDB = imageDB
        this.bibDB = bibDB
        this.note = note
        this.app = app
    }

    init() {
        const shrinker = new _shrink__rspack_import_2.ShrinkFidus(this.doc, this.imageDB, this.bibDB)

        shrinker
            .init()
            .then(({shrunkImageDB, shrunkBibDB, httpIncludes}) => {
                const zipper = new _zip__rspack_import_3.ZipFidus(
                    this.doc.id,
                    this.doc,
                    shrunkImageDB,
                    shrunkBibDB,
                    httpIncludes
                )
                return zipper.init()
            })
            .then(blob => this.uploadRevision(blob))
            .catch(error => {
                (0,_common__rspack_import_0.addAlert)(
                    "error",
                    gettext("Revision file could not be generated.")
                )
                if (this.app.isOffline()) {
                    (0,_common__rspack_import_0.addAlert)(
                        "info",
                        gettext(
                            "You are currently offline. Please try again when you are back online."
                        )
                    )
                } else {
                    throw error
                }
            })
    }

    uploadRevision(blob) {
        (0,_common__rspack_import_0.jsonPost)(
            "/api/document/upload/",
            {
                note: this.note,
                document_id: this.doc.id
            },
            false,
            {
                file: {
                    file: blob,
                    filename: (0,_tools_file__rspack_import_1.createSlug)(this.doc.title) + ".fidus"
                }
            }
        )
            .then(
                () => {
                    (0,_common__rspack_import_0.addAlert)("success", gettext("Revision saved"))
                },
                () => {
                    (0,_common__rspack_import_0.addAlert)("error", gettext("Revision could not be saved."))
                    if (this.app.isOffline()) {
                        (0,_common__rspack_import_0.addAlert)(
                            "info",
                            gettext(
                                "You are currently offline. Please try again when you are back online."
                            )
                        )
                    }
                }
            )
            .catch(error => {
                throw error
            })
    }
}


}),
"./js/modules/exporter/native/shrink.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ShrinkFidus: function() { return ShrinkFidus; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _schema_document__rspack_import_1 = __webpack_require__("./js/modules/schema/document/index.js");
/* import */ var _schema_mini_json__rspack_import_2 = __webpack_require__("./js/modules/schema/mini_json.js");



// Generate a copy of the fidus doc, imageDB and bibDB with all clutter removed.
class ShrinkFidus {
    constructor(doc, imageDB, bibDB) {
        this.doc = doc
        this.imageDB = imageDB
        this.bibDB = bibDB
        this.imageList = []
        this.citeList = []
    }

    init() {
        const shrunkImageDB = {},
            httpIncludes = []

        ;(0,_common__rspack_import_0.addAlert)("info", gettext("File export has been initiated."))

        this.walkTree(this.doc.content)

        this.imageList = [...new Set(this.imageList)] // unique values

        this.imageList.forEach(itemId => {
            shrunkImageDB[itemId] = Object.assign({}, this.imageDB.db[itemId])
            // Remove parts that are connected to a particular user/server
            delete shrunkImageDB[itemId].cats
            delete shrunkImageDB[itemId].thumbnail
            delete shrunkImageDB[itemId].pk
            delete shrunkImageDB[itemId].added
            const imageUrl = shrunkImageDB[itemId].image
            const filename = `images/${imageUrl.split("/").pop()}`
            shrunkImageDB[itemId].image = filename
            httpIncludes.push({
                url: imageUrl,
                filename
            })
        })

        this.citeList = [...new Set(this.citeList)] // unique values

        const shrunkBibDB = {}
        this.citeList.forEach(itemId => {
            shrunkBibDB[itemId] = Object.assign({}, this.bibDB.db[itemId])
            // Remove the cats, as it is only a list of IDs for one
            // particular user/server.
            delete shrunkBibDB[itemId].cats
        })

        const docCopy = Object.assign({}, this.doc)

        // Remove items that aren't needed.
        delete docCopy.rights
        delete docCopy.version
        delete docCopy.comment_version
        delete docCopy.owner
        delete docCopy.id
        delete docCopy.is_owner
        delete docCopy.added
        delete docCopy.updated
        delete docCopy.revisions

        docCopy.content = (0,_schema_mini_json__rspack_import_2.toMiniJSON)(_schema_document__rspack_import_1.docSchema.nodeFromJSON(docCopy.content))

        return new Promise(resolve =>
            resolve({
                doc: docCopy,
                shrunkImageDB,
                shrunkBibDB,
                httpIncludes
            })
        )
    }

    walkTree(node) {
        switch (node.type) {
            case "citation":
                this.citeList = this.citeList.concat(
                    node.attrs.references.map(ref => ref.id)
                )
                break
            case "image":
                if (node.attrs.image !== false) {
                    this.imageList.push(node.attrs.image)
                }
                break
            case "footnote":
                if (node.attrs?.footnote) {
                    node.attrs.footnote.forEach(childNode =>
                        this.walkTree(childNode)
                    )
                }
                break
        }
        if (node.content) {
            node.content.forEach(childNode => this.walkTree(childNode))
        }
    }
}


}),
"./js/modules/exporter/native/zip.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ZipFidus: function() { return ZipFidus; }
});
/* import */ var _document_template__rspack_import_0 = __webpack_require__("./js/modules/document_template/index.js");
/* import */ var _schema__rspack_import_1 = __webpack_require__("./js/modules/schema/index.js");
/* import */ var _tools_zip__rspack_import_2 = __webpack_require__("./js/modules/exporter/tools/zip.js");



/** Create a zip blob for a shrunk fidus file.
 */

class ZipFidus {
    constructor(
        docId,
        doc,
        shrunkImageDB,
        shrunkBibDB,
        httpFiles,
        includeTemplate = true,
        token = false
    ) {
        this.docId = docId
        this.doc = doc
        this.shrunkImageDB = shrunkImageDB
        this.shrunkBibDB = shrunkBibDB
        this.httpFiles = httpFiles
        this.includeTemplate = includeTemplate
        this.token = token

        this.textFiles = [
            {
                filename: "document.json",
                contents: JSON.stringify(this.doc)
            },
            {
                filename: "images.json",
                contents: JSON.stringify(this.shrunkImageDB)
            },
            {
                filename: "bibliography.json",
                contents: JSON.stringify(this.shrunkBibDB)
            },
            {
                filename: "filetype-version",
                contents: _schema__rspack_import_1.FW_DOCUMENT_VERSION
            }
        ]
    }

    init() {
        if (!this.includeTemplate) {
            return this.createZip()
        }
        const templateExporter = new _document_template__rspack_import_0.DocumentTemplateExporter(
            this.docId,
            "/api/document/get_template_for_doc/",
            false,
            this.token
        )
        return templateExporter.init().then(() => {
            this.textFiles = this.textFiles.concat(templateExporter.textFiles)
            this.httpFiles = this.httpFiles.concat(templateExporter.httpFiles)
            return this.createZip()
        })
    }

    createZip() {
        const zipper = new _tools_zip__rspack_import_2.ZipFileCreator(
            this.textFiles,
            this.httpFiles,
            [],
            "application/fidus+zip"
        )
        return zipper.init()
    }
}


}),
"./js/modules/exporter/tools/file.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  createSlug: function() { return createSlug; }
});
const createSlug = str => {
    if (str === "") {
        str = gettext("Untitled")
    }
    str = str.replace(/[^a-zA-Z0-9\s]/g, "")
    str = str.toLowerCase()
    str = str.replace(/\s/g, "-")
    return str
}


}),
"./js/modules/exporter/tools/zip.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ZipFileCreator: function() { return ZipFileCreator; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");


/** Creates a zip file.
 * @function zipFileCreator
 * @param {list} textFiles A list of files in plain text format.
 * @param {list} binaryFiles A list fo files that have to be downloaded from the internet before being included.
 * @param {list} includeZips A list of zip files to be merged into the output zip file.
 * @param {string} [mimeType=application/zip] The mimetype of the file that is to be created.
 */

class ZipFileCreator {
    constructor(
        textFiles = [],
        binaryFiles = [],
        zipFiles = [],
        mimeType = "application/zip",
        date = new Date()
    ) {
        this.textFiles = textFiles
        this.binaryFiles = binaryFiles
        this.zipFiles = zipFiles
        this.mimeType = mimeType
        this.date = date
    }

    init() {
        return __webpack_require__.e(/* import() */ "vendors-node_modules_pnpm_jszip_3_10_1_node_modules_jszip_dist_jszip_min_js").then(__webpack_require__.t.bind(__webpack_require__, "./node_modules/.pnpm/jszip@3.10.1/node_modules/jszip/dist/jszip.min.js", 23)).then(({default: JSZip}) => {
            JSZip.defaults.date = this.date
            this.zipFs = new JSZip()
            if (this.mimeType !== "application/zip") {
                this.zipFs.file("mimetype", this.mimeType, {
                    compression: "STORE"
                })
            }

            return this.includeZips()
        })
    }

    includeZips() {
        const getZipBlobs = this.zipFiles.map(zipFile => {
            return (0,_common__rspack_import_0.get)(zipFile.url)
                .then(response => response.blob())
                .then(blob => (zipFile.blob = blob))
        })
        return Promise.all(getZipBlobs)
            .then(() => {
                return this.zipFiles.map(zipFile => {
                    const zipDir =
                        zipFile.directory === ""
                            ? this.zipFs
                            : this.zipFs.folder(zipFile.directory)
                    return zipDir.loadAsync(zipFile.blob)
                })
            })
            .then(() => this.createZip())
    }

    createZip() {
        this.textFiles.forEach(textFile => {
            this.zipFs.file(textFile.filename, textFile.contents, {
                compression: "DEFLATE"
            })
        })
        const blobPromises = this.binaryFiles.map(binaryFile =>
            (0,_common__rspack_import_0.get)(binaryFile.url)
                .then(response => response.blob())
                .then(blob =>
                    Promise.resolve({blob, filename: binaryFile.filename})
                )
        )
        return Promise.all(blobPromises).then(promises => {
            promises.forEach(promise =>
                this.zipFs.file(promise.filename, promise.blob, {
                    binary: true,
                    compression: "DEFLATE"
                })
            )
            return this.zipFs.generateAsync({
                type: "blob",
                mimeType: this.mimeType
            })
        })
    }

    // Legacy - remove in 3.12. Can be sued directly from function in common/blob.js
    convertDataURIToBlob(dataURI) {
        return (0,_common__rspack_import_0.convertDataURIToBlob)(dataURI)
    }
}


}),
"./js/modules/images/selection_dialog/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  ImageSelectionDialog: function() { return ImageSelectionDialog; }
});
/* import */ var simple_datatables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/simple-datatables@10.2.0/node_modules/simple-datatables/dist/module.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/common/index.js");




class ImageSelectionDialog {
    constructor(imageDB, userImageDB, imgId, page) {
        this.imageDB = imageDB
        this.userImageDB = userImageDB
        this.page = page
        this.imgId = imgId // a preselected image
        this.imgDb = "document" // the preselection image will always come from the document
        this.images = [] // images from both databases
    }

    isE2EE() {
        return this.page.e2ee?.encrypted === true
    }

    init() {
        this.images = Object.values(this.imageDB.db).map(image => {
            return {
                image,
                db: "document"
            }
        })
        Object.values(this.userImageDB.db).forEach(image => {
            if (this.imageDB.db[image.id]) {
                return
            }
            this.images.push({
                image,
                db: "user"
            })
        })
        const buttons = []
        const p = new Promise(resolve => {
            if (!this.page.app.isOffline()) {
                buttons.push({
                    text: gettext("Add new image"),
                    icon: "plus-circle",
                    click: () => {
                        __webpack_require__.e(/* import() */ "js_modules_images_edit_dialog_index_js-_2e941").then(__webpack_require__.bind(__webpack_require__, "./js/modules/images/edit_dialog/index.js")).then(({ImageEditDialog}) => {
                            const targetDB = this.isE2EE()
                                ? this.imageDB
                                : this.userImageDB
                            const imageUpload = new ImageEditDialog(
                                targetDB,
                                false,
                                this.page
                            )

                            resolve(
                                imageUpload.init().then(imageId => {
                                    this.imgId = imageId
                                    // For E2EE docs the image goes straight
                                    // into the document DB, not the user's.
                                    this.imgDb = this.isE2EE()
                                        ? "document"
                                        : "user"
                                    this.imageDialog.close()
                                    return this.init()
                                })
                            )
                        })
                    }
                })
            }

            buttons.push({
                text: gettext("Use image"),
                classes: "fw-dark",
                click: () => {
                    this.imageDialog.close()
                    resolve({id: this.imgId, db: this.imgDb})
                }
            })

            buttons.push({
                type: "cancel",
                click: () => {
                    this.imageDialog.close()
                    resolve((0,_common__rspack_import_1.cancelPromise)())
                }
            })
        })
        this.imageDialog = new _common__rspack_import_1.Dialog({
            buttons,
            width: 300,
            body: '<div class="image-selection-table"></div>',
            title: gettext("Images"),
            id: "select-image-dialog"
        })
        this.imageDialog.open()
        this.initTable()
        this.imageDialog.centerDialog()
        this.bindEvents()
        return p
    }

    initTable() {
        /* Initialize the overview table */
        const tableEl = document.createElement("table")
        tableEl.classList.add("fw-data-table")
        tableEl.classList.add("fw-small")
        this.imageDialog.dialogEl
            .querySelector("div.image-selection-table")
            .appendChild(tableEl)
        this.table = new simple_datatables__rspack_import_0.DataTable(tableEl, {
            searchable: true,
            paging: false,
            scrollY: "270px",
            labels: {
                noRows: gettext("No images available"), // Message shown when there are no images
                noResults: gettext("No images found"), // Message shown when no images are found after search
                placeholder: gettext("Search...") // placeholder for search field
            },
            template: (options, dom) => `<div class='${options.classes.top}'>
                <div class='${options.classes.search}'>
                    <input class='${options.classes.input}' placeholder='${options.labels.placeholder}' type='search' title='${options.labels.searchTitle}'${dom.id ? ` aria-controls="${dom.id}"` : ""}>
                </div>
            </div>
            <div class='${options.classes.container}' style='height: ${options.scrollY}; overflow-Y: auto;'></div>`,
            data: {
                headings: ["", gettext("Image"), gettext("Title"), ""],
                data: this.images.map(image => this.createTableRow(image))
            },
            columns: [
                {
                    select: 0,
                    hidden: true
                },
                {
                    select: [0, 2],
                    type: "string"
                },
                {
                    select: [1, 3],
                    sortable: false
                }
            ]
        })
        this.lastSort = {column: 0, dir: "asc"}

        this.table.on("datatable.sort", (column, dir) => {
            this.lastSort = {column, dir}
        })
    }

    createTableRow(image) {
        return [
            `${image.db}-${image.image.id}`,
            image.image.thumbnail === undefined
                ? `<img src="${image.image.image}" style="max-heigth:30px;max-width:30px;">`
                : `<img src="${image.image.thumbnail}" style="max-heigth:30px;max-width:30px;">`,
            (0,_common__rspack_import_1.escapeText)(image.image.title),
            image.db === this.imgDb && image.image.id === this.imgId
                ? [
                      {
                          nodeName: "i",
                          attributes: {
                              class: "fa fa-check",
                              "aria-hidden": "true"
                          }
                      }
                  ]
                : []
        ]
    }

    checkRow(dataIndex) {
        const row = this.table.data.data[dataIndex]
        if (!row) {
            return
        }
        const cell = row.cells[0]
        const [db, id] = cell.data.split("-").map(
            (val, index) => (index ? Number.parseInt(val) : val) // only parseInt id (where index > 0)
        )
        if (id === this.imgId) {
            this.imgId = false
        } else {
            this.imgId = id
        }
        this.imgDb = db
        this.table.data.data.forEach((row, index) => {
            if (index === dataIndex && this.imgId) {
                row.cells[3].data = [
                    {
                        nodeName: "i",
                        attributes: {
                            class: "fa fa-check",
                            "aria-hidden": "true"
                        }
                    }
                ]
            } else {
                row.cells[3].data = []
            }
        })
        this.table.refresh()
    }

    bindEvents() {
        // functions for the image selection dialog
        this.table.dom.addEventListener("click", event => {
            const el = {}
            switch (true) {
                case (0,_common__rspack_import_1.findTarget)(event, "tr", el): {
                    this.checkRow(Number.parseInt(el.target.dataset.index))
                    break
                }
                default:
                    break
            }
        })
    }
}


}),
"./js/modules/importer/native/file.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FidusFileImporter: function() { return FidusFileImporter; },
  MAX_FW_DOCUMENT_VERSION: function() { return MAX_FW_DOCUMENT_VERSION; },
  MIN_FW_DOCUMENT_VERSION: function() { return MIN_FW_DOCUMENT_VERSION; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _document_template__rspack_import_1 = __webpack_require__("./js/modules/document_template/index.js");
/* import */ var _schema__rspack_import_2 = __webpack_require__("./js/modules/schema/index.js");
/* import */ var _index__rspack_import_3 = __webpack_require__("./js/modules/importer/native/index.js");
/* import */ var _update__rspack_import_4 = __webpack_require__("./js/modules/importer/native/update.js");






/** The current Fidus Writer filetype version. The importer will not import from
 * a different version and the exporter will include this number in all exports.
 */
const MIN_FW_DOCUMENT_VERSION = 1.6,
    MAX_FW_DOCUMENT_VERSION = Number.parseFloat(_schema__rspack_import_2.FW_DOCUMENT_VERSION)

const TEXT_FILENAMES = [
    "mimetype",
    "filetype-version",
    "document.json",
    "images.json",
    "bibliography.json"
]

class FidusFileImporter {
    /* Process a packaged Fidus File, either through user upload, or by reloading
      a saved revision which was saved in the same ZIP-baseformat. */

    constructor(file, user, path = "", check = false, contacts = []) {
        this.file = file
        this.user = user
        this.path = path
        this.check = check // Whether the file needs to be checked for compliance with ZIP-format and whether authors of comments/changes are team members of current user.
        this.contacts = contacts

        this.textFiles = []
        this.otherFiles = []
        this.ok = false
        this.statusText = ""
        this.doc = null
        this.docInfo = null
        this.template = null
    }

    init() {
        // Check whether the file is a ZIP-file if check is not disabled.
        if (this.check === false) {
            return this.initZipFileRead()
        }
        return new Promise(resolve => {
            // use a BlobReader to read the zip from a Blob object
            const reader = new window.FileReader()
            reader.onloadend = () => {
                if (
                    reader.result.length > 60 &&
                    reader.result.substring(0, 2) === "PK"
                ) {
                    this.initZipFileRead().then(() => resolve(this))
                } else {
                    this.statusText = gettext(
                        "The uploaded file does not appear to be a Fidus Writer file."
                    )
                    resolve(this)
                }
            }
            reader.readAsText(this.file)
        })
    }

    initZipFileRead() {
        // Extract all the files that can be found in every fidus-file (not images)
        return Promise.resolve(/* import() */).then(__webpack_require__.t.bind(__webpack_require__, "./node_modules/.pnpm/jszip@3.10.1/node_modules/jszip/dist/jszip.min.js", 23))
            .then(({default: JSZip}) => new JSZip())
            .then(zipfs => zipfs.loadAsync(this.file))
            .then(zipfs => {
                const filenames = [],
                    p = []
                let validFile = true

                zipfs.forEach(filename => filenames.push(filename))

                TEXT_FILENAMES.forEach(filename => {
                    if (filenames.indexOf(filename) === -1) {
                        validFile = false
                    }
                })
                if (!validFile) {
                    this.statusText = gettext(
                        "The uploaded file does not appear to be a Fidus Writer file."
                    )
                    return
                }

                filenames
                    .filter(filename => !filename.endsWith("/"))
                    .forEach(filename => {
                        p.push(
                            new Promise(resolve => {
                                let fileType, fileList
                                if (
                                    ["mimetype", "filetype-version"].includes(
                                        filename
                                    ) ||
                                    filename.endsWith(".json")
                                ) {
                                    fileType = "string"
                                    fileList = this.textFiles
                                } else {
                                    fileType = "blob"
                                    fileList = this.otherFiles
                                }
                                zipfs.files[filename]
                                    .async(fileType)
                                    .then(content => {
                                        fileList.push({filename, content})
                                        resolve()
                                    })
                            })
                        )
                    })
                return Promise.all(p).then(() => this.processFidusFile())
            })
    }

    processFidusFile() {
        const filetypeVersion = Number.parseFloat(
                this.textFiles.find(
                    file => file.filename === "filetype-version"
                ).content
            ),
            mimeType = this.textFiles.find(
                file => file.filename === "mimetype"
            ).content
        if (
            mimeType === "application/fidus+zip" &&
            filetypeVersion >= MIN_FW_DOCUMENT_VERSION &&
            filetypeVersion <= MAX_FW_DOCUMENT_VERSION
        ) {
            // This seems to be a valid fidus file with current version number.
            const updatedFile = (0,_update__rspack_import_4.updateFile)(
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "document.json"
                        ).content
                    ),
                    filetypeVersion,
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "bibliography.json"
                        ).content
                    ),
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "images.json"
                        ).content
                    )
                ),
                {bibliography} = updatedFile,
                {images} = updatedFile
            let {doc} = updatedFile
            if (this.check) {
                doc = this.checkDocUsers(doc)
            }
            const templateFile = this.textFiles.find(
                file => file.filename === "template.json"
            )
            if (templateFile) {
                // A template is included in the file
                const templateDef = JSON.parse(templateFile.content)
                this.template = (0,_document_template__rspack_import_1.updateTemplateFile)(
                    templateDef.attrs.template,
                    templateDef,
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "exporttemplates.json"
                        ).content
                    ),
                    JSON.parse(
                        this.textFiles.find(
                            file => file.filename === "documentstyles.json"
                        ).content
                    ),
                    filetypeVersion
                )
                this.template.files = this.otherFiles.filter(
                    file =>
                        file.filename.startsWith("exporttemplates/") ||
                        file.filename.startsWith("documentstyles/")
                )
                this.otherFiles = this.otherFiles.filter(
                    file => !this.template.files.includes(file)
                )
            }
            const importer = new _index__rspack_import_3.NativeImporter(
                doc,
                bibliography,
                images,
                this.otherFiles,
                this.user,
                null,
                this.path.endsWith("/") ? this.path + doc.title : this.path,
                this.template
            )
            return importer.init().then(({doc, docInfo}) => {
                this.ok = true
                this.doc = doc
                this.docInfo = docInfo
                this.statusText = `${(0,_common__rspack_import_0.escapeText)(doc.title)} ${gettext("successfully imported.")}`
                return this
            })
        } else {
            // The file is not a Fidus Writer file.
            this.statusText =
                gettext(
                    "The uploaded file does not appear to be of the version used on this server: "
                ) + _schema__rspack_import_2.FW_DOCUMENT_VERSION
            return this
        }
    }

    checkDocUsers(doc) {
        // Check whether users mentioned in doc are known to current user and present on this server
        Object.values(doc.comments).forEach(comment => {
            if (
                !(
                    this.contacts.find(
                        member =>
                            member.id === comment.user &&
                            member.username === comment.username
                    ) ||
                    (this.user.id === comment.user &&
                        this.user.username === comment.username)
                )
            ) {
                // We could not find matching id/username accessible to current user, so we delete the user id from comment
                comment.user = 0
            }
            if (
                !(
                    !comment.assignedUser ||
                    this.contacts.find(
                        member =>
                            member.id === comment.assignedUser &&
                            member.username === comment.assignedUsername
                    ) ||
                    (this.user.id === comment.assignedUser &&
                        this.user.username === comment.assignedUsername)
                )
            ) {
                // We could not find matching id/username accessible to current user, so we delete the assignedUser id from comment
                comment.assignedUser = 0
            }
            if (comment.answers) {
                comment.answers.forEach(answer => {
                    if (
                        !(
                            this.contacts.find(
                                member =>
                                    member.id === answer.user &&
                                    member.username === answer.username
                            ) ||
                            (this.user.id === answer.user &&
                                this.user.username === answer.username)
                        )
                    ) {
                        // We could not find matching id/username accessible to current user, so we delete the user id from comment answer
                        answer.user = 0
                    }
                })
            }
        })
        this.checkDocUsersNode(doc.content)
        return doc
    }

    checkDocUsersNode(node) {
        // Check whether all users connected to insertion/deletion marks are known on this system.
        if (node.marks) {
            node.marks.forEach(mark => {
                if (["insertion", "deletion"].includes(mark.type)) {
                    if (
                        !(
                            this.contacts.find(
                                member =>
                                    member.id === mark.attrs.user &&
                                    member.username === mark.attrs.username
                            ) ||
                            (this.user.id === mark.attrs.user &&
                                this.user.username === mark.attrs.username)
                        )
                    ) {
                        // We could not find matching id/username accessible to current user, so we delete the user id from comment answer
                        mark.attrs.user = 0
                    }
                }
            })
        }
        if (node.content) {
            node.content.forEach(childNode => this.checkDocUsersNode(childNode))
        }
    }
}


}),
"./js/modules/importer/native/get_images.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  GetImages: function() { return GetImages; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");


class GetImages {
    constructor(images, entries) {
        this.images = images
        this.imageEntries = Object.values(this.images)
        this.entries = entries
        this.counter = 0
    }

    init() {
        if (this.entries.length > 0) {
            if (this.entries[0].hasOwnProperty("url")) {
                return this.getImageUrlEntry()
            } else {
                return this.getImageZipEntry()
            }
        } else {
            return Promise.resolve()
        }
    }

    getImageZipEntry() {
        if (this.counter < this.imageEntries.length) {
            return new Promise(resolve => {
                const f = this.entries.find(
                    entry =>
                        entry.filename === this.imageEntries[this.counter].image
                )
                if (!f) {
                    console.warn(
                        `Image ${this.imageEntries[this.counter].image} not found`,
                        this.imageEntries,
                        this.entries
                    )
                    this.counter++
                    return this.getImageZipEntry().then(() => {
                        resolve()
                    })
                }
                const fc = f.content
                this.imageEntries[this.counter]["file"] = new window.Blob(
                    [fc],
                    {
                        type: this.imageEntries[this.counter].file_type
                    }
                )
                this.counter++
                this.getImageZipEntry().then(() => {
                    resolve()
                })
            })
        } else {
            return Promise.resolve()
        }
    }

    getImageUrlEntry() {
        if (this.counter < this.imageEntries.length) {
            const getUrl = this.entries.find(
                entry =>
                    entry.filename ===
                    `images/${this.imageEntries[this.counter].image.split("/").pop()}`
            ).url
            return (0,_common__rspack_import_0.get)(getUrl)
                .then(response => response.blob())
                .then(blob => {
                    this.imageEntries[this.counter]["file"] = blob
                    this.counter++
                    return this.getImageUrlEntry()
                })
        } else {
            return Promise.resolve()
        }
    }
}


}),
"./js/modules/importer/native/import.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  NativeImporter: function() { return NativeImporter; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _document_template__rspack_import_1 = __webpack_require__("./js/modules/document_template/index.js");
/* import */ var _editor_e2ee_key_manager__rspack_import_2 = __webpack_require__("./js/modules/editor/e2ee/key-manager.js");
/* import */ var _get_images__rspack_import_3 = __webpack_require__("./js/modules/importer/native/get_images.js");





class NativeImporter {
    /* Save document information into the database */
    constructor(
        doc,
        bibliography,
        images,
        otherFiles,
        user,
        importId = null,
        requestedPath = "",
        template = null,
        e2eeOptions = null
    ) {
        this.doc = doc
        this.docId = false
        this.path = false
        this.bibliography = bibliography
        this.images = images
        this.otherFiles = otherFiles // Data of image files
        this.user = user
        this.importId = importId
        this.requestedPath = requestedPath
        this.template = template
        this.e2eeOptions = e2eeOptions // {enabled: boolean, key?: CryptoKey}
    }

    init() {
        const ImageTranslationTable = {}
        // We first create any new entries in the DB for images.
        const imageGetter = new _get_images__rspack_import_3.GetImages(this.images, this.otherFiles)
        return imageGetter
            .init()
            .then(() => {
                const missingImage = Object.values(this.images).find(
                    imageEntry => !imageEntry.file
                )
                if (missingImage) {
                    (0,_common__rspack_import_0.addAlert)(
                        "error",
                        `${gettext("Could not create document. Missing image file:")} ${missingImage.image}`
                    )
                    ;(0,_common__rspack_import_0.deactivateWait)()
                    throw new Error(`Missing image file: ${missingImage.image}`)
                }
            })
            .then(() => this.createDoc())
            .then(() => {
                if (!this.docId) {
                    return Promise.reject(new Error("document not created"))
                }
                return this.saveImages(this.images, ImageTranslationTable)
            })
            .then(() => {
                // We need to change some reference numbers in the document content
                this.translateReferenceIds(ImageTranslationTable)
                // We are good to go. All the used images and bibliography entries
                // exist in the DB for this user with the same numbers.
                // We can go ahead and create the new document entry in the
                // bibliography without any changes.
                return this.saveDocument()
            })
    }

    async _maybeDecryptImage(imageEntry) {
        if (
            !this.e2eeOptions ||
            !this.e2eeOptions.sourceKey ||
            !imageEntry.file
        ) {
            return
        }
        if (imageEntry.file_type !== "application/octet-stream") {
            return
        }
        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))
        const fileBuffer = await imageEntry.file.arrayBuffer()
        const bytes = new Uint8Array(fileBuffer)
        // Build binary string in chunks to avoid "Maximum call stack size exceeded"
        let binary = ""
        const chunkSize = 65536
        for (let i = 0; i < bytes.length; i += chunkSize) {
            const chunk = bytes.subarray(i, i + chunkSize)
            binary += String.fromCharCode.apply(null, chunk)
        }
        const base64 = btoa(binary)
        const decrypted = await E2EEEncryptor.decryptBufferToBase64(
            base64,
            this.e2eeOptions.sourceKey
        )
        const mime = imageEntry.original_file_type || "image/png"
        const byteCharacters = atob(decrypted)
        const byteNumbers = new Array(byteCharacters.length)
        for (let i = 0; i < byteCharacters.length; i++) {
            byteNumbers[i] = byteCharacters.charCodeAt(i)
        }
        const byteArray = new Uint8Array(byteNumbers)
        imageEntry.file = new Blob([byteArray], {type: mime})
        imageEntry.file_type = mime
    }

    async _maybeEncryptImage(imageEntry) {
        if (
            !this.e2eeOptions ||
            !this.e2eeOptions.enabled ||
            !this.e2eeOptions.key
        ) {
            return imageEntry.file
        }
        const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))
        return E2EEEncryptor.encryptImage(imageEntry.file, this.e2eeOptions.key)
    }

    saveImages(images, ImageTranslationTable) {
        const isE2EE = this.e2eeOptions && this.e2eeOptions.enabled
        const endpoint = isE2EE
            ? "/api/document/e2ee_image/"
            : "/api/document/import/image/"

        const sendPromises = Object.values(images).map(imageEntry => {
            return this._maybeDecryptImage(imageEntry)
                .then(() => this._maybeEncryptImage(imageEntry))
                .then(encryptedFile => {
                    const jsonData = {
                        doc_id: this.docId,
                        title: imageEntry.title,
                        copyright: imageEntry.copyright,
                        checksum: imageEntry.checksum
                    }
                    const files = {
                        image: {
                            file: encryptedFile,
                            filename: imageEntry.image.split("/").pop()
                        }
                    }
                    return (0,_common__rspack_import_0.jsonPostJson)(endpoint, jsonData, false, files)
                })
                .then(
                    ({json}) => (ImageTranslationTable[imageEntry.id] = json.id)
                )
                .catch(error => {
                    (0,_common__rspack_import_0.addAlert)(
                        "error",
                        `${gettext("Could not save Image")} ${imageEntry.checksum}`
                    )
                    throw error
                })
        })
        return Promise.all(sendPromises)
    }

    translateReferenceIds(ImageTranslationTable) {
        function walkTree(node) {
            switch (node.type) {
                case "image":
                    if (node.attrs.image !== false) {
                        node.attrs.image =
                            ImageTranslationTable[node.attrs.image]
                    }
                    break
                case "footnote":
                    if (node.attrs?.footnote) {
                        node.attrs.footnote.forEach(childNode => {
                            walkTree(childNode)
                        })
                    }
                    break
            }
            if (node.content) {
                node.content.forEach(childNode => {
                    walkTree(childNode)
                })
            }
        }
        walkTree(this.doc.content)
    }

    createDoc() {
        const template = this.template
            ? this.template
            : (0,_document_template__rspack_import_1.extractTemplate)(this.doc.content)

        const jsonData = {
            template: template.content,
            export_templates: template.exportTemplates,
            document_styles: template.documentStyles,
            import_id: this.importId
                ? this.importId
                : template.content.attrs.import_id,
            template_title: template.content.attrs.template,
            path: this.requestedPath
        }

        if (this.e2eeOptions && this.e2eeOptions.enabled) {
            jsonData.e2ee = true
            if (this.e2eeOptions.salt) {
                jsonData.e2ee_salt = this.e2eeOptions.salt
            }
            if (this.e2eeOptions.iterations) {
                jsonData.e2ee_iterations = this.e2eeOptions.iterations
            }
        }

        const files = {}
        if (template.files && template.files.length) {
            files.files = template.files.map(
                ({filename, content}) => new File([content], filename)
            )
        }

        // We create the document on the server so that we have an ID for it and
        // can link the images to it.
        return (0,_common__rspack_import_0.jsonPostJson)(
            "/api/document/import/create/",
            jsonData,
            false,
            files
        )
            .then(({json}) => {
                this.docId = json.id
                this.path = json.path
                this.e2ee = json.e2ee || false
            })
            .catch(error => {
                (0,_common__rspack_import_0.addAlert)("error", gettext("Could not create document"))
                throw error
            })
    }

    async saveDocument() {
        let saveData
        if (
            this.e2eeOptions &&
            this.e2eeOptions.enabled &&
            this.e2eeOptions.key
        ) {
            // For E2EE documents, encrypt content/comments/bibliography
            // before sending to the server.
            const {E2EEEncryptor} = await Promise.resolve(/* import() */).then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js"))
            const encryptedContent = await E2EEEncryptor.encryptObject(
                this.doc.content,
                this.e2eeOptions.key
            )
            const encryptedComments = await E2EEEncryptor.encryptObject(
                this.doc.comments || {},
                this.e2eeOptions.key
            )
            const encryptedBibliography = await E2EEEncryptor.encryptObject(
                this.bibliography,
                this.e2eeOptions.key
            )
            // Encrypt the title as well so it doesn't leak server-side.
            const encryptedTitle = await E2EEEncryptor.encrypt(
                this.doc.title,
                this.e2eeOptions.key
            )
            saveData = {
                id: this.docId,
                title: encryptedTitle,
                content: encryptedContent,
                comments: encryptedComments,
                bibliography: encryptedBibliography
            }
        } else {
            saveData = {
                id: this.docId,
                title: this.doc.title,
                content: this.doc.content,
                comments: this.doc.comments,
                bibliography: this.bibliography
            }
        }
        return (0,_common__rspack_import_0.jsonPostJson)("/api/document/import/", saveData)
            .then(({json}) => {
                const docInfo = {
                    is_owner: true,
                    access_rights: "write",
                    id: this.docId
                }
                this.doc.owner = {
                    id: this.user.id,
                    name: this.user.name,
                    avatar: this.user.avatar
                }
                this.doc.is_owner = true
                this.doc.version = 0
                this.doc.comment_version = 0
                this.doc.id = this.docId
                this.doc.added = json.added
                this.doc.updated = json.updated
                this.doc.revisions = []
                this.doc.rights = "write"
                this.doc.path = this.path
                this.doc.e2ee = this.e2ee || false
                // Cache the E2EE key in sessionStorage so the user doesn't have
                // to re-enter the password when opening the document again.
                if (
                    this.e2eeOptions &&
                    this.e2eeOptions.enabled &&
                    this.e2eeOptions.key &&
                    this.docId
                ) {
                    _editor_e2ee_key_manager__rspack_import_2.E2EEKeyManager.storeKeyInSession(
                        this.docId,
                        this.e2eeOptions.key
                    )
                }
                return {doc: this.doc, docInfo}
            })
            .catch(error => {
                (0,_common__rspack_import_0.addAlert)(
                    "error",
                    `${gettext("Could not save ")} ${(0,_common__rspack_import_0.shortFileTitle)(this.doc.title, this.doc.path)}`
                )
                throw error
            })
    }
}


}),
"./js/modules/importer/native/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FidusFileImporter: function() { return /* reexport safe */ _file__rspack_import_0.FidusFileImporter; },
  NativeImporter: function() { return /* reexport safe */ _import__rspack_import_1.NativeImporter; }
});
/* import */ var _file__rspack_import_0 = __webpack_require__("./js/modules/importer/native/file.js");
/* import */ var _import__rspack_import_1 = __webpack_require__("./js/modules/importer/native/import.js");




}),
"./js/modules/importer/native/update.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  updateFile: function() { return updateFile; }
});
/* import */ var _schema_convert__rspack_import_0 = __webpack_require__("./js/modules/schema/convert.js");


function updateFile(doc, filetypeVersion, bibliography, images) {
    // update bibliography -- currently not needed
    // bibliography = updateBib(bibliography)
    if (filetypeVersion < 3.2) {
        Object.values(images).forEach(
            image =>
                (image.copyright = {
                    holder: false,
                    year: false,
                    freeToRead: true,
                    licenses: []
                })
        )
    }
    if (filetypeVersion < 3.3) {
        doc.content = doc.contents
        delete doc.contents
    }
    if (filetypeVersion < 2.0) {
        // Before 2.0, version numbers of the doc and of the file differed.
        doc = (0,_schema_convert__rspack_import_0.updateDoc)(doc, doc.settings["doc_version"], bibliography)
    } else {
        doc = (0,_schema_convert__rspack_import_0.updateDoc)(doc, filetypeVersion, bibliography)
    }

    return {doc, bibliography, images}
}


}),
"./js/modules/schema/common/annotate.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  annotation_tag: function() { return annotation_tag; },
  comment: function() { return comment; },
  randomCommentId: function() { return randomCommentId; }
});
// Annotation tag is not used by the core Fidus Writer editor, but can be used by plugins that need to add annotation capability.
const annotation_tag = {
    attrs: {
        type: {
            default: "" // Make this a string unique to your plugin so that you avoid handling tags of other plugins. For example 'rdfa' for an rdfa-tagging plugin.
        },
        key: {
            default: "" // key or variable/tag name
        },
        value: {
            default: "" // value of variable/tag
        }
    },
    inclusive: false,
    excludes: "", // allows several tags on the same content.
    group: "annotation",
    parseDOM: [
        {
            tag: "span.annotation-tag[data-type]",
            getAttrs(dom) {
                return {
                    type: dom.dataset.type,
                    key: dom.dataset.key ? dom.dataset.key : "",
                    value: dom.dataset.value ? dom.dataset.value : ""
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {
            class: "annotation-tag",
            "data-type": node.attrs.type
        }
        if (node.attrs.key?.length) {
            attrs["data-key"] = node.attrs.key
        }
        if (node.attrs.value?.length) {
            attrs["data-value"] = node.attrs.value
        }
        return ["span", attrs]
    }
}

const comment = {
    attrs: {
        id: {
            default: false
        }
    },
    inclusive: false,
    excludes: "",
    group: "annotation",
    parseDOM: [
        {
            tag: "span.comment[data-id]",
            getAttrs(dom) {
                return {
                    id: Number.parseInt(dom.dataset.id)
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: "comment",
                "data-id": node.attrs.id
            }
        ]
    }
}

function randomCommentId() {
    return String(Math.floor(Math.random() * 0xffffffff))
}


}),
"./js/modules/schema/common/base.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  blockquote: function() { return blockquote; },
  code: function() { return code; },
  horizontal_rule: function() { return horizontal_rule; },
  paragraph: function() { return paragraph; },
  sub: function() { return sub; },
  sup: function() { return sup; },
  underline: function() { return underline; }
});
/* import */ var _track__rspack_import_0 = __webpack_require__("./js/modules/schema/common/track.js");


// :: NodeSpec A plain paragraph textblock. Represented in the DOM
// as a `<p>` element.
const paragraph = {
    group: "block",
    content: "inline*",
    attrs: {
        track: {
            default: []
        }
    },
    parseDOM: [
        {
            tag: "p",
            getAttrs(dom) {
                return {
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs =
            node.attrs.track && node.attrs.track.length
                ? {"data-track": JSON.stringify(node.attrs.track)}
                : {}
        return ["p", attrs, 0]
    }
}

// :: NodeSpec A blockquote (`<blockquote>`) wrapping one or more blocks.
const blockquote = {
    content: "block+",
    group: "block",
    attrs: {
        track: {
            default: []
        }
    },
    marks: "annotation",
    defining: true,
    parseDOM: [
        {
            tag: "blockquote",
            getAttrs(dom) {
                return {
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs =
            node.attrs.track && node.attrs.track.length
                ? {"data-track": JSON.stringify(node.attrs.track)}
                : {}
        return ["blockquote", attrs, 0]
    }
}

// :: NodeSpec A horizontal rule (`<hr>`).
const horizontal_rule = {
    group: "block",
    attrs: {
        track: {
            default: []
        }
    },
    parseDOM: [
        {
            tag: "hr",
            getAttrs(dom) {
                return {
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs =
            node.attrs.track && node.attrs.track.length
                ? {"data-track": JSON.stringify(node.attrs.track)}
                : {}
        return ["hr", attrs]
    }
}

const underline = {
    parseDOM: [{tag: "span.underline"}],
    toDOM() {
        return ["span", {class: "underline"}, 0]
    }
}

const sup = {
    parseDOM: [{tag: "sup"}],
    toDOM() {
        return ["sup", 0]
    },
    excludes: "sub"
}

const sub = {
    parseDOM: [{tag: "sub"}],
    toDOM() {
        return ["sub", 0]
    },
    excludes: "sup"
}

const code = {
    parseDOM: [{tag: "code"}],
    toDOM() {
        return ["code", 0]
    },
    excludes: "strong em underline link sup sub"
}


}),
"./js/modules/schema/common/citation.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  citation: function() { return citation; }
});
function parseReferences(str) {
    if (!str) {
        return []
    }
    let references
    try {
        references = JSON.parse(str)
    } catch (_error) {
        return []
    }
    if (!Array.isArray(references)) {
        return []
    }
    return references
        .filter(
            ref => ref.hasOwnProperty("id") // ensure there is an id.
        )
        .map(ref => {
            const mRef = {id: ref.id}
            if (ref.locator) {
                mRef.locator = ref.locator
            }
            if (ref.prefix) {
                mRef.prefix = ref.prefix
            }
            return mRef
        })
}

const citation = {
    inline: true,
    group: "inline",
    attrs: {
        format: {
            default: "autocite" // "autocite" or "textcite"
        },
        references: {
            default: [] // array of {id[, locator][, prefix]}
        }
    },
    parseDOM: [
        {
            tag: "span.citation",
            getAttrs(dom) {
                return {
                    format: dom.dataset.format || "",
                    references: parseReferences(dom.dataset.references)
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: "citation",
                "data-format": node.attrs.format,
                "data-references": JSON.stringify(node.attrs.references)
            }
        ]
    }
}


}),
"./js/modules/schema/common/equation.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  equation: function() { return equation; }
});
const equation = {
    inline: true,
    group: "inline",
    attrs: {
        equation: {
            default: ""
        }
    },
    parseDOM: [
        {
            tag: "span.equation",
            getAttrs(dom) {
                return {
                    equation: dom.dataset.equation
                }
            }
        }
    ],
    toDOM(node) {
        const dom = document.createElement("span")
        dom.dataset.equation = node.attrs.equation
        dom.classList.add("equation")
        __webpack_require__.e(/* import() */ "vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs").then(__webpack_require__.bind(__webpack_require__, "./node_modules/.pnpm/mathlive@0.104.0/node_modules/mathlive/dist/mathlive.mjs")).then(MathLive => {
            dom.innerHTML = MathLive.convertLatexToMarkup(node.attrs.equation, {
                mathstyle: "textstyle"
            })
        })
        dom.setAttribute("contenteditable", "false")
        return dom
    }
}


}),
"./js/modules/schema/common/figure.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  figure: function() { return figure; },
  figure_caption: function() { return figure_caption; },
  figure_equation: function() { return figure_equation; },
  image: function() { return image; },
  randomFigureId: function() { return randomFigureId; }
});
/* import */ var _track__rspack_import_0 = __webpack_require__("./js/modules/schema/common/track.js");


function randomFigureId() {
    return "F" + Math.round(Math.random() * 10000000) + 1
}

let imageDBBroken = false

const figure = {
    inline: false,
    allowGapCursor: false,
    selectable: true,
    group: "block",
    attrs: {
        category: {default: "none"},
        caption: {default: false},
        id: {default: false},
        track: {default: []},
        aligned: {default: "center"},
        width: {default: "100"}
    },
    content:
        "figure_caption image|figure_caption figure_equation|image figure_caption|figure_equation figure_caption",
    parseDOM: [
        {
            tag: "figure",
            getAttrs(dom) {
                return {
                    category: dom.dataset.category,
                    caption: !!dom.dataset.captionHidden,
                    id: dom.id,
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track),
                    aligned: dom.dataset.aligned,
                    width: dom.dataset.width,
                    diff: dom.dataset.diff
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {
            id: node.attrs.id,
            class: `aligned-${node.attrs.aligned} image-width-${node.attrs.width}`,
            "data-aligned": node.attrs.aligned,
            "data-width": node.attrs.width,
            "data-category": node.attrs.category
        }
        if (!node.attrs.caption) {
            attrs["data-caption-hidden"] = true
        }
        if (node.attrs.track?.length) {
            attrs["data-track"] = JSON.stringify(node.attrs.track)
        }
        return ["figure", attrs, 0]
    }
}

const image = {
    selectable: false,
    draggable: false,
    attrs: {
        image: {default: false}
    },
    parseDOM: [
        {
            tag: "img",
            getAttrs(dom) {
                const image = Number.parseInt(dom.dataset.image)
                return {
                    image: isNaN(image) ? false : image
                }
            }
        }
    ],
    toDOM(node) {
        const dom = document.createElement("img")
        if (node.attrs.image !== false) {
            dom.dataset.image = node.attrs.image
            const imageDB = node.type.schema.cached.imageDB
            if (imageDB) {
                const imageEntry = imageDB.db[node.attrs.image]
                if (imageEntry?.image) {
                    const isEncrypted =
                        imageEntry.file_type === "application/octet-stream"
                    if (isEncrypted) {
                        const editor = imageDB.mod?.editor
                        const key = editor?.e2ee?.key
                        if (key) {
                            __webpack_require__.e(/* import() */ "js_modules_editor_e2ee_encryptor_js").then(__webpack_require__.bind(__webpack_require__, "./js/modules/editor/e2ee/encryptor.js")).then(
                                ({E2EEEncryptor}) => {
                                    E2EEEncryptor.decryptImageToUrl(
                                        imageEntry.image,
                                        key,
                                        imageEntry.original_file_type ||
                                            "image/png"
                                    )
                                        .then(url => {
                                            dom.setAttribute("src", url)
                                            dom.dataset.imageSrc = url
                                        })
                                        .catch(() => {
                                            dom.setAttribute(
                                                "src",
                                                staticUrl("img/error.avif")
                                            )
                                        })
                                }
                            )
                        } else {
                            dom.setAttribute("src", staticUrl("img/error.avif"))
                        }
                    } else {
                        const imgSrc = imageEntry.image
                        dom.setAttribute("src", imgSrc)
                        dom.dataset.imageSrc = imgSrc
                    }
                } else {
                    /* The image was not present in the imageDB -- possibly because a collaborator just added it.
                    Try to reload the imageDB, but only once. If the image cannot be found in the updated
                    imageDB, do not attempt at reloading the imageDB if an image cannot be
                    found. */
                    if (imageDBBroken) {
                        dom.setAttribute("src", staticUrl("img/error.avif"))
                    } else {
                        imageDB.getDB().then(() => {
                            const refreshedEntry = imageDB.db[node.attrs.image]
                            if (refreshedEntry?.image) {
                                dom.setAttribute("src", refreshedEntry.image)
                                dom.dataset.imageSrc = refreshedEntry.image
                            } else {
                                imageDBBroken = true
                            }
                        })
                    }
                }
            }
        }
        return dom
    }
}

const figure_equation = {
    selectable: false,
    draggable: false,
    attrs: {
        equation: {
            default: false
        }
    },
    parseDOM: [
        {
            tag: "div.figure-equation[data-equation]",
            getAttrs(dom) {
                return {
                    equation: dom.dataset.equation
                }
            }
        }
    ],
    toDOM(node) {
        const dom = document.createElement("div")
        dom.dataset.equation = node.attrs.equation
        dom.classList.add("figure-equation")
        if (node.attrs.equation !== false) {
            __webpack_require__.e(/* import() */ "vendors-node_modules_pnpm_mathlive_0_104_0_node_modules_mathlive_dist_mathlive_mjs").then(__webpack_require__.bind(__webpack_require__, "./node_modules/.pnpm/mathlive@0.104.0/node_modules/mathlive/dist/mathlive.mjs")).then(MathLive => {
                dom.innerHTML = MathLive.convertLatexToMarkup(
                    node.attrs.equation,
                    {
                        mathstyle: "displaystyle"
                    }
                )
            })
        }
        return dom
    }
}

const figure_caption = {
    isolating: true,
    defining: true,
    content: "inline*",
    parseDOM: [{tag: "figcaption span.text"}],
    toDOM() {
        return [
            "figcaption",
            ["span", {class: "label"}],
            ["span", {class: "text"}, 0]
        ]
    }
}


}),
"./js/modules/schema/common/heading.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  heading1: function() { return heading1; },
  heading2: function() { return heading2; },
  heading3: function() { return heading3; },
  heading4: function() { return heading4; },
  heading5: function() { return heading5; },
  heading6: function() { return heading6; },
  randomHeadingId: function() { return randomHeadingId; }
});
/* import */ var _track__rspack_import_0 = __webpack_require__("./js/modules/schema/common/track.js");


const randomHeadingId = () => {
    return `H${Math.round(Math.random() * 10000000) + 1}`
}

const createHeading = level => ({
    group: "block heading",
    content: "inline*",
    marks: "_",
    defining: true,
    attrs: {
        id: {
            default: false
        },
        track: {
            default: []
        }
    },
    parseDOM: [
        {
            tag: `h${level}`,
            getAttrs(dom) {
                return {
                    id: dom.id,
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {id: node.attrs.id}
        ;(0,_track__rspack_import_0.addTracks)(node, attrs)
        return [`h${level}`, attrs, 0]
    }
})

const heading1 = createHeading(1)
const heading2 = createHeading(2)
const heading3 = createHeading(3)
const heading4 = createHeading(4)
const heading5 = createHeading(5)
const heading6 = createHeading(6)


}),
"./js/modules/schema/common/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  anchor: function() { return /* reexport safe */ _reference__rspack_import_7.anchor; },
  annotation_tag: function() { return /* reexport safe */ _annotate__rspack_import_0.annotation_tag; },
  blockquote: function() { return /* reexport safe */ _base__rspack_import_1.blockquote; },
  bullet_list: function() { return /* reexport safe */ _list__rspack_import_6.bullet_list; },
  citation: function() { return /* reexport safe */ _citation__rspack_import_2.citation; },
  code: function() { return /* reexport safe */ _base__rspack_import_1.code; },
  comment: function() { return /* reexport safe */ _annotate__rspack_import_0.comment; },
  cross_reference: function() { return /* reexport safe */ _reference__rspack_import_7.cross_reference; },
  deletion: function() { return /* reexport safe */ _track__rspack_import_9.deletion; },
  equation: function() { return /* reexport safe */ _equation__rspack_import_3.equation; },
  figure: function() { return /* reexport safe */ _figure__rspack_import_4.figure; },
  figure_caption: function() { return /* reexport safe */ _figure__rspack_import_4.figure_caption; },
  figure_equation: function() { return /* reexport safe */ _figure__rspack_import_4.figure_equation; },
  format_change: function() { return /* reexport safe */ _track__rspack_import_9.format_change; },
  heading1: function() { return /* reexport safe */ _heading__rspack_import_5.heading1; },
  heading2: function() { return /* reexport safe */ _heading__rspack_import_5.heading2; },
  heading3: function() { return /* reexport safe */ _heading__rspack_import_5.heading3; },
  heading4: function() { return /* reexport safe */ _heading__rspack_import_5.heading4; },
  heading5: function() { return /* reexport safe */ _heading__rspack_import_5.heading5; },
  heading6: function() { return /* reexport safe */ _heading__rspack_import_5.heading6; },
  horizontal_rule: function() { return /* reexport safe */ _base__rspack_import_1.horizontal_rule; },
  image: function() { return /* reexport safe */ _figure__rspack_import_4.image; },
  insertion: function() { return /* reexport safe */ _track__rspack_import_9.insertion; },
  link: function() { return /* reexport safe */ _reference__rspack_import_7.link; },
  list_item: function() { return /* reexport safe */ _list__rspack_import_6.list_item; },
  ordered_list: function() { return /* reexport safe */ _list__rspack_import_6.ordered_list; },
  paragraph: function() { return /* reexport safe */ _base__rspack_import_1.paragraph; },
  parseTracks: function() { return /* reexport safe */ _track__rspack_import_9.parseTracks; },
  randomAnchorId: function() { return /* reexport safe */ _reference__rspack_import_7.randomAnchorId; },
  randomCommentId: function() { return /* reexport safe */ _annotate__rspack_import_0.randomCommentId; },
  randomFigureId: function() { return /* reexport safe */ _figure__rspack_import_4.randomFigureId; },
  randomHeadingId: function() { return /* reexport safe */ _heading__rspack_import_5.randomHeadingId; },
  randomListId: function() { return /* reexport safe */ _list__rspack_import_6.randomListId; },
  randomTableId: function() { return /* reexport safe */ _table__rspack_import_8.randomTableId; },
  sub: function() { return /* reexport safe */ _base__rspack_import_1.sub; },
  sup: function() { return /* reexport safe */ _base__rspack_import_1.sup; },
  table: function() { return /* reexport safe */ _table__rspack_import_8.table; },
  table_body: function() { return /* reexport safe */ _table__rspack_import_8.table_body; },
  table_caption: function() { return /* reexport safe */ _table__rspack_import_8.table_caption; },
  table_cell: function() { return /* reexport safe */ _table__rspack_import_8.table_cell; },
  table_header: function() { return /* reexport safe */ _table__rspack_import_8.table_header; },
  table_row: function() { return /* reexport safe */ _table__rspack_import_8.table_row; },
  underline: function() { return /* reexport safe */ _base__rspack_import_1.underline; }
});
/* import */ var _annotate__rspack_import_0 = __webpack_require__("./js/modules/schema/common/annotate.js");
/* import */ var _base__rspack_import_1 = __webpack_require__("./js/modules/schema/common/base.js");
/* import */ var _citation__rspack_import_2 = __webpack_require__("./js/modules/schema/common/citation.js");
/* import */ var _equation__rspack_import_3 = __webpack_require__("./js/modules/schema/common/equation.js");
/* import */ var _figure__rspack_import_4 = __webpack_require__("./js/modules/schema/common/figure.js");
/* import */ var _heading__rspack_import_5 = __webpack_require__("./js/modules/schema/common/heading.js");
/* import */ var _list__rspack_import_6 = __webpack_require__("./js/modules/schema/common/list.js");
/* import */ var _reference__rspack_import_7 = __webpack_require__("./js/modules/schema/common/reference.js");
/* import */ var _table__rspack_import_8 = __webpack_require__("./js/modules/schema/common/table.js");
/* import */ var _track__rspack_import_9 = __webpack_require__("./js/modules/schema/common/track.js");












}),
"./js/modules/schema/common/list.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  bullet_list: function() { return bullet_list; },
  list_item: function() { return list_item; },
  ordered_list: function() { return ordered_list; },
  randomListId: function() { return randomListId; }
});
/* import */ var _track__rspack_import_0 = __webpack_require__("./js/modules/schema/common/track.js");


// :: NodeSpec
// An ordered list [node spec](#model.NodeSpec). Has a single
// attribute, `order`, which determines the number at which the list
// starts counting, and defaults to 1. Represented as an `<ol>`
// element.
const ordered_list = {
    group: "block",
    content: "list_item+",
    attrs: {
        id: {default: false},
        order: {default: 1},
        track: {default: []}
    },
    parseDOM: [
        {
            tag: "ol",
            getAttrs(dom) {
                return {
                    id: dom.id,
                    order: dom.hasAttribute("start")
                        ? +dom.getAttribute("start")
                        : 1,
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {id: node.attrs.id}
        if (node.attrs.order !== 1) {
            attrs.start = node.attrs.order
        }
        (0,_track__rspack_import_0.addTracks)(node, attrs)
        return ["ol", attrs, 0]
    }
}

function randomListId() {
    return "L" + Math.round(Math.random() * 10000000) + 1
}

// :: NodeSpec
// A bullet list node spec, represented in the DOM as `<ul>`.
const bullet_list = {
    group: "block",
    content: "list_item+",
    attrs: {
        id: {default: false},
        track: {default: []}
    },
    parseDOM: [
        {
            tag: "ul",
            getAttrs(dom) {
                return {
                    id: dom.id,
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {id: node.attrs.id}
        ;(0,_track__rspack_import_0.addTracks)(node, attrs)
        return ["ul", attrs, 0]
    }
}

// :: NodeSpec
// A list item (`<li>`) spec.
const list_item = {
    content: "block+",
    marks: "annotation",
    attrs: {
        track: {default: []}
    },
    parseDOM: [
        {
            tag: "li",
            getAttrs(dom) {
                return {
                    track: (0,_track__rspack_import_0.parseTracks)(dom.dataset.track)
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {}
        ;(0,_track__rspack_import_0.addTracks)(node, attrs)
        return ["li", attrs, 0]
    },
    defining: true
}


}),
"./js/modules/schema/common/reference.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  anchor: function() { return anchor; },
  cross_reference: function() { return cross_reference; },
  link: function() { return link; },
  randomAnchorId: function() { return randomAnchorId; }
});
const cross_reference = {
    inline: true,
    group: "inline",
    attrs: {
        id: {
            default: false
        },
        title: {
            default: null // title === null means that the target is gone
        }
    },
    parseDOM: [
        {
            tag: "span.cross-reference[data-id][data-title]",
            getAttrs(dom) {
                return {
                    id: dom.dataset.id,
                    title: dom.dataset.title
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: `cross-reference${node.attrs.title ? "" : " missing-target"}`,
                "data-id": node.attrs.id,
                "data-title": node.attrs.title
            },
            node.attrs.title ? node.attrs.title : gettext("Missing Target")
        ]
    }
}

const link = {
    attrs: {
        href: {},
        title: {
            default: ""
        }
    },
    inclusive: false,
    parseDOM: [
        {
            tag: "a[href]",
            getAttrs(dom) {
                return {
                    href: dom.getAttribute("href"),
                    title: dom.getAttribute("title")
                }
            }
        }
    ],
    toDOM(node) {
        const {href, title} = node.attrs
        const attrs =
            title || href.charAt(0) !== "#"
                ? {href, title}
                : {
                      href,
                      title: gettext("Missing target"),
                      class: "missing-target"
                  }
        return ["a", attrs, 0]
    }
}

const randomAnchorId = () => {
    return `A${Math.round(Math.random() * 10000000) + 1}`
}

const anchor = {
    attrs: {
        id: {
            default: false
        }
    },
    inclusive: false,
    group: "annotation",
    parseDOM: [
        {
            tag: "span.anchor[data-id]",
            getAttrs(dom) {
                return {
                    id: dom.dataset.id
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: "anchor",
                "data-id": node.attrs.id
            }
        ]
    }
}


}),
"./js/modules/schema/common/table.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  randomTableId: function() { return randomTableId; },
  table: function() { return table; },
  table_body: function() { return table_body; },
  table_caption: function() { return table_caption; },
  table_cell: function() { return table_cell; },
  table_header: function() { return table_header; },
  table_row: function() { return table_row; }
});
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var _track__rspack_import_1 = __webpack_require__("./js/modules/schema/common/track.js");




const table = {
    inline: false,
    group: "block",
    tableRole: "table",
    attrs: {
        id: {default: false},
        track: {default: []},
        width: {default: "100"},
        aligned: {default: "center"},
        layout: {default: "fixed"},
        category: {default: "none"},
        caption: {default: false}
    },
    content: "table_caption table_body",
    parseDOM: [
        {
            tag: "table",
            getAttrs(dom) {
                const track = (0,_track__rspack_import_1.parseTracks)(dom.dataset.track),
                    width = dom.dataset.width,
                    aligned = width === "100" ? "center" : dom.dataset.aligned,
                    layout = dom.dataset.layout,
                    id = dom.id || dom.dataset.id
                return {
                    track,
                    width,
                    aligned,
                    layout,
                    id,
                    category: dom.dataset.category,
                    caption: !!dom.dataset.captionHidden
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {
            id: node.attrs.id,
            class: `table-${node.attrs.width} table-${node.attrs.aligned} table-${node.attrs.layout}`,
            "data-width": node.attrs.width,
            "data-aligned": node.attrs.aligned,
            "data-layout": node.attrs.layout,
            "data-category": node.attrs.category
        }
        if (node.attrs.track.length) {
            attrs["data-track"] = JSON.stringify(node.attrs.track)
        }
        if (!node.attrs.caption) {
            attrs["data-caption-hidden"] = true
        }
        return ["table", attrs, 0]
    }
}

const table_caption = {
    content: "inline*",
    parseDOM: [{tag: "caption span.text"}],
    toDOM() {
        return [
            "caption",
            ["span", {class: "label"}],
            ["span", {class: "text"}, 0]
        ]
    }
}

const origTableNodes = (0,prosemirror_tables__rspack_import_0.tableNodes)({
    cellContent: "block+"
})

const table_body = Object.assign({}, origTableNodes["table"], {
    parseDOM: [{tag: "tbody"}],
    toDOM() {
        return ["tbody", 0]
    }
})

const table_row = {
    content: "(table_cell | table_header)+",
    tableRole: "row",
    parseDOM: [{tag: "tr"}],
    toDOM() {
        return ["tr", 0]
    }
}

const table_header = Object.assign(
    {marks: "annotation"},
    origTableNodes["table_header"]
)

const table_cell = Object.assign(
    {marks: "annotation"},
    origTableNodes["table_cell"]
)

function randomTableId() {
    return "T" + Math.round(Math.random() * 10000000) + 1
}


}),
"./js/modules/schema/common/track.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  addTracks: function() { return addTracks; },
  deletion: function() { return deletion; },
  format_change: function() { return format_change; },
  insertion: function() { return insertion; },
  parseTracks: function() { return parseTracks; }
});
function parseTracks(str) {
    if (!str) {
        return []
    }
    let tracks
    try {
        tracks = JSON.parse(str)
    } catch (_error) {
        return []
    }
    if (!Array.isArray(tracks)) {
        return []
    }
    return tracks.filter(
        (
            track // ensure required fields are present
        ) =>
            track.hasOwnProperty("user") &&
            track.hasOwnProperty("username") &&
            track.hasOwnProperty("date")
    )
}

function addTracks(node, attrs) {
    if (node.attrs.track?.length) {
        attrs["data-track"] = JSON.stringify(node.attrs.track)
    }
}

const deletion = {
    attrs: {
        user: {
            default: 0
        },
        username: {
            default: ""
        },
        date: {
            default: 0
        }
    },
    inclusive: false,
    group: "track",
    parseDOM: [
        {
            tag: "span.deletion",
            getAttrs(dom) {
                return {
                    user: Number.parseInt(dom.dataset.user),
                    username: dom.dataset.username,
                    date: Number.parseInt(dom.dataset.date)
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: `deletion user-${node.attrs.user}`,
                "data-user": node.attrs.user,
                "data-username": node.attrs.username,
                "data-date": node.attrs.date
            }
        ]
    }
}

function parseFormatList(str) {
    if (!str) {
        return []
    }
    let formatList
    try {
        formatList = JSON.parse(str)
    } catch (_error) {
        return []
    }
    if (!Array.isArray(formatList)) {
        return []
    }
    return formatList.filter(format => typeof format === "string") // ensure there are only strings in list
}

const format_change = {
    attrs: {
        user: {
            default: 0
        },
        username: {
            default: ""
        },
        date: {
            default: 0
        },
        before: {
            default: []
        },
        after: {
            default: []
        }
    },
    inclusive: false,
    group: "track",
    parseDOM: [
        {
            tag: "span.format-change",
            getAttrs(dom) {
                return {
                    user: Number.parseInt(dom.dataset.user),
                    username: dom.dataset.username,
                    date: Number.parseInt(dom.dataset.date),
                    before: parseFormatList(dom.dataset.before),
                    after: parseFormatList(dom.dataset.after)
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: `format-change user-${node.attrs.user}`,
                "data-user": node.attrs.user,
                "data-username": node.attrs.username,
                "data-date": node.attrs.date,
                "data-before": JSON.stringify(node.attrs.before),
                "data-after": JSON.stringify(node.attrs.after)
            }
        ]
    }
}

const insertion = {
    attrs: {
        user: {
            default: 0
        },
        username: {
            default: ""
        },
        date: {
            default: 0
        },
        approved: {
            default: true
        }
    },
    inclusive: false,
    group: "track",
    parseDOM: [
        {
            tag: "span.insertion",
            getAttrs(dom) {
                return {
                    user: Number.parseInt(dom.dataset.user),
                    username: dom.dataset.username,
                    date: Number.parseInt(dom.dataset.date),
                    inline: true,
                    approved: false
                }
            }
        },
        {
            tag: "span.approved-insertion",
            getAttrs(dom) {
                return {
                    user: Number.parseInt(dom.dataset.user),
                    username: dom.dataset.username,
                    date: Number.parseInt(dom.dataset.date),
                    inline: true,
                    approved: true
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "span",
            {
                class: node.attrs.approved
                    ? "approved-insertion"
                    : `insertion user-${node.attrs.user}`,
                "data-user": node.attrs.user,
                "data-username": node.attrs.username,
                "data-date": node.attrs.date
            }
        ]
    }
}


}),
"./js/modules/schema/const.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  LANGUAGES: function() { return LANGUAGES; },
  PAPER_SIZES: function() { return PAPER_SIZES; }
});
const LANGUAGES = [
    ["af-ZA", gettext("Afrikaans"), "ltr"],
    ["sq-AL", gettext("Albanian"), "ltr"],
    ["ar", gettext("Arabic"), "rtl"],
    ["ast", gettext("Asturian"), "ltr"],
    ["be", gettext("Belarusian"), "ltr"],
    ["br", gettext("Breton"), "ltr"],
    ["bg", gettext("Bulgarian"), "ltr"],
    ["ca", gettext("Catalan"), "ltr"],
    ["ca-ES-Valencia", gettext("Catalan (Valencia)"), "ltr"],
    ["zh-CN", gettext("Chinese (Simplified)"), "ltr"],
    ["da", gettext("Danish"), "ltr"],
    ["nl", gettext("Dutch"), "ltr"],
    ["en-AU", gettext("English (Australia)"), "ltr"],
    ["en-CA", gettext("English (Canada)"), "ltr"],
    ["en-NZ", gettext("English (New Zealand)"), "ltr"],
    ["en-ZA", gettext("English (South Africa)"), "ltr"],
    ["en-GB", gettext("English (United Kingdom)"), "ltr"],
    ["en-US", gettext("English (United States)"), "ltr"],
    ["eo", gettext("Esperanto"), "ltr"],
    ["fr", gettext("French"), "ltr"],
    ["gl", gettext("Galician"), "ltr"],
    ["de-DE", gettext("German (Germany)"), "ltr"],
    ["de-AU", gettext("German (Austria)"), "ltr"],
    ["de-CH", gettext("German (Switzerland)"), "ltr"],
    ["el", gettext("Greek"), "ltr"],
    ["he", gettext("Hebrew"), "rtl"],
    ["is", gettext("Icelandic"), "ltr"],
    ["it", gettext("Italian"), "ltr"],
    ["ja", gettext("Japanese"), "ltr"],
    ["km", gettext("Khmer"), "ltr"],
    ["lt", gettext("Lithuanian"), "ltr"],
    ["ml", gettext("Malayalam"), "ltr"],
    ["nb-NO", gettext("Norwegian (bokmål)"), "ltr"],
    ["nn-NO", gettext("Norwegian (nynorsk)"), "ltr"],
    ["fa", gettext("Persian"), "rtl"],
    ["pl", gettext("Polish"), "ltr"],
    ["pt-BR", gettext("Portuguese (Brazil)"), "ltr"],
    ["pt-PT", gettext("Portuguese (Portugal)"), "ltr"],
    ["ro", gettext("Romanian"), "ltr"],
    ["ru", gettext("Russian"), "ltr"],
    ["tr", gettext("Turkish"), "ltr"],
    ["sr-SP-Cy", gettext("Serbian (Cyrillic)"), "ltr"],
    ["sr-SP-Lt", gettext("Serbian (Latin)"), "ltr"],
    ["sk", gettext("Slovak"), "ltr"],
    ["sl", gettext("Slovenian"), "ltr"],
    ["es", gettext("Spanish"), "ltr"],
    ["sv", gettext("Swedish"), "ltr"],
    ["ta", gettext("Tamil"), "ltr"],
    ["tl", gettext("Tagalog"), "ltr"],
    ["uk", gettext("Ukrainian"), "ltr"]
]

const PAPER_SIZES = [
    /* [Fidus Writer value, CSS value]*/
    ["A4", "A4"],
    ["US Letter", "letter"]
]


}),
"./js/modules/schema/convert.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  getSettings: function() { return getSettings; },
  updateDoc: function() { return updateDoc; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/schema/common/index.js");
/* To convert to and from how the document is stored in the database to how ProseMirror expects it.
 We use the DOM import for ProseMirror as the JSON we store in the database is really jsonized HTML.
*/



const getSettings = pmDoc => {
    const settings = JSON.parse(JSON.stringify(pmDoc.attrs))
    return settings
}

const updateDoc = (doc, docVersion, bibliography = false) => {
    /* This is to clean documents taking all the accepted formatting from older
       versions and outputting the current version of the doc format.
       Notice that the docVersion isn't the same as the version of the FW export
       file in Fidus Writer < 3.2 (docVersion/FW file versions versions -1.X).
       While the FW file version also says something about what files could be
       available inside the FW zip, the doc_version refers to how the data is
       stored in those files.
       In general, an update to the doc_version will likely also trigger an
       update to the version of the FW export file, the reverse is not always
       true.
    */

    switch (docVersion) {
        // Import from versions up to 3.0 no longer supported starting with Fidus Writer 3.5
        case 1: // Fidus Writer 3.1 prerelease
            doc = convertDocV1(doc)
            doc = convertDocV11(doc)
            doc = convertDocV12(doc)
            doc = convertDocV13(doc, bibliography)
            doc = convertDocV20(doc)
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 1.1: // Fidus Writer 3.1
            doc = convertDocV11(doc)
            doc = convertDocV12(doc)
            doc = convertDocV13(doc, bibliography)
            doc = convertDocV20(doc)
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 1.2: // Fidus Writer 3.2
            doc = convertDocV12(doc)
            doc = convertDocV13(doc, bibliography)
            doc = convertDocV20(doc)
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 1.3: // Fidus Writer 3.3 prerelease
            doc = convertDocV13(doc, bibliography)
            doc = convertDocV20(doc)
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 2.0: // Fidus Writer 3.3
            doc = convertDocV20(doc)
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 2.1: // Fidus Writer 3.4
            doc = convertDocV21(doc)
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 2.2: // Fidus Writer 3.5.7
            doc = convertDocV22(doc)
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 2.3: // Fidus Writer 3.5.10
            doc = convertDocV23(doc)
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 3.0: // Fidus Writer 3.6
            doc = convertDocV30(doc)
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 3.1: // Fidus Writer 3.7
            doc = convertDocV31(doc)
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 3.2: // Fidus Writer 3.8
            doc = convertDocV32(doc)
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 3.3: // Fidus Writer 3.9
            doc = convertDocV33(doc)
            doc = convertDocV34(doc)
            break
        case 3.4: // Fidus Writer 3.10
            doc = convertDocV34(doc)
            break
        case 3.5: // Fidus Writer 3.12
            break
    }
    return doc
}

const convertDocV1 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV1(returnDoc.content)
    return returnDoc
}

const convertNodeV1 = node => {
    let prefixes, locators, ids, references
    switch (node.type) {
        case "citation":
            prefixes = node.attrs.bibBefore
                ? node.attrs.bibBefore.split(",,,")
                : []
            locators = node.attrs.bibPage ? node.attrs.bibPage.split(",,,") : []
            ids = node.attrs.bibEntry ? node.attrs.bibEntry.split(",") : []
            references = ids.map((id, index) => {
                const returnObj = {id: Number.parseInt(id)}
                if (prefixes[index] !== "") {
                    returnObj["prefix"] = prefixes[index]
                }
                if (locators[index] !== "") {
                    returnObj["locator"] = locators[index]
                }
                return returnObj
            })
            node.attrs = {
                format: node.attrs.bibFormat,
                references
            }
            break
        case "footnote":
            if (node.attrs?.footnote) {
                node.attrs.footnote.forEach(childNode => {
                    convertNodeV1(childNode)
                })
            }
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV1(childNode)
        })
    }
}

const convertDocV11 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV11(returnDoc.content)
    return returnDoc
}

const convertNodeV11 = (node, ids = []) => {
    let blockId
    switch (node.type) {
        case "heading":
            blockId = node.attrs.id
            while (!blockId || ids.includes(blockId)) {
                blockId = (0,_common__rspack_import_1.randomHeadingId)()
            }
            node.attrs.id = blockId
            ids.push(blockId)
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV11(childNode, ids)
        })
    }
}

const convertDocV12 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV12(returnDoc.content)
    return returnDoc
}

const convertNodeV12 = (node, ids = []) => {
    let blockId
    switch (node.type) {
        case "figure":
            blockId = node.attrs.id
            while (!blockId || ids.includes(blockId)) {
                blockId = (0,_common__rspack_import_1.randomFigureId)()
            }
            node.attrs.id = blockId
            ids.push(blockId)
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV12(childNode, ids)
        })
    }
}

const convertDocV13 = (doc, bibliography) => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    delete returnDoc.settings
    delete returnDoc.metadata
    returnDoc.bibliography = {}
    returnDoc.imageIds = []
    convertNodeV13(
        returnDoc.content,
        returnDoc.bibliography,
        bibliography,
        returnDoc.imageIds
    )
    return returnDoc
}

const convertNodeV13 = (node, shrunkBib, fullBib, imageIds) => {
    let authorsText, keywordsText
    switch (node.type) {
        case "article":
            node.attrs.language = "en-US"
            break
        case "authors":
            authorsText = node.content
                ? node.content.reduce(
                      (text, item) =>
                          item.type === "text" ? text + item.text : text,
                      ""
                  )
                : ""
            node.content = authorsText
                .split(/[,;]/g)
                .map(authorString => {
                    const author = authorString.trim()
                    if (!author.length) {
                        return false
                    }
                    const authorParts = author.split(" ")
                    return {
                        type: "author",
                        attrs: {
                            firstname:
                                authorParts.length > 1
                                    ? authorParts.shift()
                                    : false,
                            lastname: authorParts.join(" "),
                            institution: false,
                            email: false
                        }
                    }
                })
                .filter(authorObj => authorObj)
            if (!node.content.length) {
                delete node.content
            }
            break
        case "citation":
            node.attrs.references.forEach(ref => {
                let item = fullBib[ref.id]
                if (!item) {
                    item = {
                        fields: {title: [{type: "text", text: "Deleted"}]},
                        bib_type: "misc",
                        entry_key: "FidusWriter"
                    }
                }
                item = Object.assign({}, item)
                delete item.cats
                shrunkBib[ref.id] = item
            })
            break
        case "keywords":
            keywordsText = node.content
                ? node.content.reduce(
                      (text, item) =>
                          item.type === "text" ? text + item.text : text,
                      ""
                  )
                : ""
            node.content = keywordsText
                .split(/[,;]/g)
                .map(keywordString => {
                    const keyword = keywordString.trim()
                    if (!keyword.length) {
                        return false
                    }
                    return {
                        type: "keyword",
                        attrs: {
                            keyword
                        }
                    }
                })
                .filter(keywordObj => keywordObj)
            if (!node.content.length) {
                delete node.content
            }
            break
        case "figure":
            if (isNaN(Number.parseInt(node.attrs.image))) {
                node.attrs.image = false
            } else {
                imageIds.push(Number.parseInt(node.attrs.image))
            }
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV13(childNode, shrunkBib, fullBib, imageIds)
        })
    }
}

const convertDocV20 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    delete returnDoc.added
    delete returnDoc.is_owner
    delete returnDoc.revisions
    delete returnDoc.rights
    delete returnDoc.updated
    if (returnDoc.content.attrs) {
        returnDoc.content.attrs.tracked = false
    }
    Object.values(returnDoc.comments).forEach(comment => {
        comment.username = comment.userName
        comment.isMajor = comment["review:isMajor"]
        delete comment.userAvatar
        delete comment.userName
        delete comment["review:isMajor"]
        if (comment.answers) {
            comment.answers.forEach(answer => {
                answer.username = answer.userName
                delete answer.userAvatar
                delete answer.userName
            })
        }
    })
    return returnDoc
}

const convertNodeV21 = node => {
    let commentMark
    if (
        node.marks &&
        (commentMark = node.marks.find(mark => mark.type === "comment"))
    ) {
        commentMark.attrs.id = String(commentMark.attrs.id)
    }
    if (node.content) {
        node.content.forEach(childNode => convertNodeV21(childNode))
    }
}

const convertDocV21 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV21(returnDoc.content)
    Object.entries(returnDoc.comment).forEach(([commentId, comment]) => {
        delete comment.id
        comment.assignedUser = false
        comment.assignedUsername = false
        comment.resolved = false
        comment.comment = comment.comment.split("\n").map(text => ({
            type: "paragraph",
            content: [{type: "text", text}]
        }))
        if (comment.answers) {
            comment.answers.forEach(answer => {
                answer.id = answer.answerId
                    ? String(answer.answerId)
                    : answer.id && String(answer.id) !== String(commentId)
                      ? String(answer.id)
                      : String(Math.floor(Math.random() * 0xffffffff))
                delete answer.answerId
                answer.answer = answer.answer.split("\n").map(text => ({
                    type: "paragraph",
                    content: [{type: "text", text}]
                }))
            })
        }
    })
    return returnDoc
}

const convertNodeV22 = (node, imageIds) => {
    switch (node.type) {
        case "figure":
            if (!isNaN(Number.parseInt(node.attrs.image))) {
                imageIds.push(Number.parseInt(node.attrs.image))
            }
            break
        default:
            break
    }
    if (node.content) {
        const deleteChildren = []
        node.content.forEach(childNode => {
            if (childNode.type === "text" && !childNode.text.length) {
                deleteChildren.push(childNode)
            } else {
                convertNodeV22(childNode, imageIds)
            }
        })
        node.content = node.content.filter(
            childNode => !deleteChildren.includes(childNode)
        )
    }
}

const convertDocV22 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    returnDoc.imageIds = []
    convertNodeV22(returnDoc.content, returnDoc.imageIds)
    Object.entries(returnDoc.comment).forEach(([_commentId, comment]) => {
        comment.comment.forEach(commentNode =>
            convertNodeV22(commentNode, returnDoc.imageIds)
        )
        if (comment.answers) {
            comment.answers.forEach(answer => {
                answer.answer.forEach(answerNode =>
                    convertNodeV22(answerNode, returnDoc.imageIds)
                )
            })
        }
    })
    return returnDoc
}

const v23ExtraAttrs = {
    languages: [
        "af-ZA",
        "sq-AL",
        "ar",
        "ast",
        "be",
        "br",
        "bg",
        "ca",
        "ca-ES-Valencia",
        "zh-CN",
        "da",
        "nl",
        "en-AU",
        "en-CA",
        "en-NZ",
        "en-ZA",
        "en-GB",
        "en-US",
        "eo",
        "fr",
        "gl",
        "de-DE",
        "de-AU",
        "de-CH",
        "el",
        "he",
        "is",
        "it",
        "ja",
        "km",
        "lt",
        "ml",
        "nb-NO",
        "nn-NO",
        "fa",
        "pl",
        "pt-BR",
        "pt-PT",
        "ro",
        "ru",
        "tr",
        "sr-SP-Cy",
        "sr-SP-Lt",
        "sk",
        "sl",
        "es",
        "sv",
        "ta",
        "tl",
        "uk"
    ],
    papersizes: ["A4", "US Letter"],
    footnote_marks: ["strong", "em", "link", "anchor"],
    footnote_elements: [
        "paragraph",
        "heading1",
        "heading2",
        "heading3",
        "heading4",
        "heading5",
        "heading6",
        "figure",
        "ordered_list",
        "bullet_list",
        "horizontal_rule",
        "equation",
        "citation",
        "blockquote",
        "table"
    ],
    template: "Standard Article"
}

const convertNodeV23 = node => {
    switch (node.type) {
        case "article":
            node.attrs = Object.assign({}, node.attrs, v23ExtraAttrs)
            break
        case "title":
            node.attrs = {
                title: "Title",
                id: "title"
            }
            break
        case "subtitle":
            node.type = "heading_part"
            node.attrs = {
                title: "Subtitle",
                id: "subtitle",
                locking: false,
                language: false,
                optional: "hidden",
                hidden: node.attrs.hidden,
                help: false,
                deleted: false,
                elements: ["heading1"],
                marks: ["strong", "em", "link", "anchor"]
            }
            node.content = [
                {
                    type: "heading1",
                    attrs: {
                        id: "H5302207",
                        track: []
                    },
                    content: node.content
                }
            ]
            break
        case "authors":
            node.type = "contributors_part"
            node.attrs = {
                title: "Authors",
                id: "authors",
                locking: false,
                language: false,
                optional: "hidden",
                hidden: node.attrs.hidden,
                help: false,
                deleted: false,
                item_title: "Author"
            }
            break
        case "author":
            node.type = "contributor"
            break
        case "abstract":
            node.type = "richtext_part"
            node.attrs = {
                title: "Abstract",
                id: "abstract",
                locking: false,
                language: false,
                optional: "hidden",
                hidden: node.attrs.hidden,
                help: false,
                deleted: false,
                elements: [
                    "paragraph",
                    "heading1",
                    "heading2",
                    "heading3",
                    "heading4",
                    "heading5",
                    "heading6",
                    "figure",
                    "ordered_list",
                    "bullet_list",
                    "horizontal_rule",
                    "equation",
                    "citation",
                    "blockquote",
                    "footnote",
                    "table"
                ],
                marks: ["strong", "em", "link", "anchor"]
            }
            break
        case "keywords":
            node.type = "tags_part"
            node.attrs = {
                title: "Keywords",
                id: "keywords",
                locking: false,
                language: false,
                optional: "hidden",
                hidden: node.attrs.hidden,
                help: false,
                deleted: false,
                item_title: "Keyword"
            }
            break
        case "keyword":
            node.type = "tag"
            node.attrs = {
                tag: node.attrs.keyword
            }
            break
        case "body":
            node.type = "richtext_part"
            node.attrs = {
                title: "Body",
                id: "body",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                deleted: false,
                elements: [
                    "paragraph",
                    "heading1",
                    "heading2",
                    "heading3",
                    "heading4",
                    "heading5",
                    "heading6",
                    "figure",
                    "ordered_list",
                    "bullet_list",
                    "horizontal_rule",
                    "equation",
                    "citation",
                    "blockquote",
                    "footnote",
                    "table"
                ],
                marks: ["strong", "em", "link", "anchor"]
            }
            break
        case "heading":
            node.type = `heading${node.attrs.level}`
            delete node.attrs.level
            break
        default:
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV23(childNode)
        })
    }
}

const convertDocV23 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV23(returnDoc.content)
    returnDoc.settings = Object.assign({}, returnDoc.settings, v23ExtraAttrs)
    return returnDoc
}

const convertNodeV30 = node => {
    if (node.attrs?.marks && node.attrs.marks.filter) {
        node.attrs.marks = node.attrs.marks.filter(mark => mark !== "anchor")
    }
    if (node.attrs?.footnote_marks) {
        node.attrs.footnote_marks = node.attrs.footnote_marks.filter(
            mark => mark !== "anchor"
        )
    }
    let attrs
    switch (node.type) {
        case "article":
            attrs = {
                documentstyle: "",
                tracked: false,
                citationstyle: "apa",
                language: "en-US",
                languages: [
                    "af-ZA",
                    "sq-AL",
                    "ar",
                    "ast",
                    "be",
                    "br",
                    "bg",
                    "ca",
                    "ca-ES-Valencia",
                    "zh-CN",
                    "da",
                    "nl",
                    "en-AU",
                    "en-CA",
                    "en-NZ",
                    "en-ZA",
                    "en-GB",
                    "en-US",
                    "eo",
                    "fr",
                    "gl",
                    "de-DE",
                    "de-AU",
                    "de-CH",
                    "el",
                    "he",
                    "is",
                    "it",
                    "ja",
                    "km",
                    "lt",
                    "ml",
                    "nb-NO",
                    "nn-NO",
                    "fa",
                    "pl",
                    "pt-BR",
                    "pt-PT",
                    "ro",
                    "ru",
                    "tr",
                    "sr-SP-Cy",
                    "sr-SP-Lt",
                    "sk",
                    "sl",
                    "es",
                    "sv",
                    "ta",
                    "tl",
                    "uk"
                ],
                papersize: "A4",
                papersizes: ["A4", "US Letter"],
                footnote_marks: ["strong", "em", "link"],
                footnote_elements: [
                    "paragraph",
                    "heading1",
                    "heading2",
                    "heading3",
                    "heading4",
                    "heading5",
                    "heading6",
                    "figure",
                    "ordered_list",
                    "bullet_list",
                    "horizontal_rule",
                    "equation",
                    "citation",
                    "blockquote",
                    "table"
                ]
            }
            break
        case "richtext_part":
            attrs = {
                title: "",
                id: "",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                initial: false,
                deleted: false,
                elements: [
                    "paragraph",
                    "heading1",
                    "heading2",
                    "heading3",
                    "heading4",
                    "heading5",
                    "heading6",
                    "figure",
                    "ordered_list",
                    "bullet_list",
                    "horizontal_rule",
                    "equation",
                    "citation",
                    "blockquote",
                    "footnote",
                    "table"
                ],
                marks: ["strong", "em", "link"],
                metadata: false
            }
            break
        case "heading_part":
            attrs = {
                title: "",
                id: "",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                initial: false,
                deleted: false,
                elements: ["heading1"],
                marks: ["strong", "em", "link"],
                metadata: false
            }
            break
        case "contributors_part":
            attrs = {
                title: "",
                id: "",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                initial: false,
                deleted: false,
                item_title: "Contributor",
                metadata: false
            }
            break
        case "tags_part":
            attrs = {
                title: "",
                id: "",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                initial: false,
                deleted: false,
                item_title: "Tag",
                metadata: false
            }
            break
        case "table_part":
            attrs = {
                title: "",
                id: "",
                locking: false,
                language: false,
                optional: false,
                hidden: false,
                help: false,
                initial: false,
                deleted: false,
                elements: [
                    "paragraph",
                    "heading1",
                    "heading2",
                    "heading3",
                    "heading4",
                    "heading5",
                    "heading6",
                    "figure",
                    "ordered_list",
                    "bullet_list",
                    "horizontal_rule",
                    "equation",
                    "citation",
                    "blockquote",
                    "footnote"
                ],
                marks: ["strong", "em", "link"],
                metadata: false
            }
            break
        case "table_of_contents":
            attrs = {
                title: "Table of Contents",
                id: "toc",
                optional: false,
                hidden: false
            }
            break
        case "separator_part":
            attrs = {
                id: "separator"
            }
            break
        case "title":
            attrs = {
                id: "title"
            }
            break
        case "contributor":
            attrs = {
                firstname: false,
                lastname: false,
                email: false,
                institution: false
            }
            break
        case "tag":
            attrs = {
                tag: ""
            }
            break
        case "footnote":
            attrs = {
                footnote: [
                    {
                        type: "paragraph"
                    }
                ]
            }
            break
        case "code_block":
        case "paragraph":
        case "blockquote":
        case "horizontal_rule":
        case "bullet_list":
        case "list_item":
            attrs = {
                track: []
            }
            break
        case "ordered_list":
            attrs = {
                order: 1,
                track: []
            }
            break
        case "citation":
            attrs = {
                format: "autocite",
                references: []
            }
            break
        case "equation":
            attrs = {
                equation: ""
            }
            break
        case "figure":
            attrs = {
                equation: "",
                image: false,
                figureCategory: "",
                caption: "",
                id: false,
                track: [],
                aligned: "center",
                width: "100"
            }
            break
        case "heading1":
        case "heading2":
        case "heading3":
        case "heading4":
        case "heading5":
        case "heading6":
            attrs = {
                id: false,
                track: []
            }
            break
        default:
            break
    }

    if (attrs && node.attrs) {
        for (const attr in attrs) {
            if (
                attr in node.attrs &&
                fast_deep_equal__rspack_import_0_default()(node.attrs[attr], attrs[attr])
            ) {
                delete node.attrs[attr]
            }
        }
        switch (node.type) {
            case "article": {
                if (node.attrs.language === "") {
                    delete node.attrs.language
                }
                const template = node.attrs.template || "default"
                node.attrs.import_id = template
                    .normalize("NFKC")
                    .replace(/[^\w\s-]/g, "")
                    .toLowerCase()
                    .trim()
                    .replace(/[-\s]+/g, "-")
                switch (node.attrs.citationstyle) {
                    case "harvard1":
                        node.attrs.citationstyle = "harvard-cite-them-right"
                        break
                    case "mla":
                        node.attrs.citationstyle = "modern-language-association"
                        break
                    case "american-anthropological-association":
                    case "chicago-author-date":
                    case "chicago-note-bibliography":
                    case "oxford-university-press-humsoc":
                    case "nature":
                        break
                    default:
                        delete node.attrs.citationstyle
                }
                break
            }
            case "title":
                delete node.attrs.title
                break
            default:
                break
        }
    }

    if (node.marks) {
        for (const mark in node.marks) {
            let attrs
            switch (mark.type) {
                case "comment":
                    attrs = {
                        id: false
                    }
                    break
                case "annotation_tag":
                    attrs = {
                        type: "",
                        key: "",
                        value: ""
                    }
                    break
                case "anchor":
                    attrs = {
                        id: false
                    }
                    break
                case "deletion":
                    attrs = {
                        user: 0,
                        username: "",
                        date: 0
                    }
                    break
                case "insertion":
                    attrs = {
                        user: 0,
                        username: "",
                        date: 0,
                        approved: true
                    }
                    break
                case "format_change":
                    attrs = {
                        user: 0,
                        username: "",
                        date: 0,
                        before: [],
                        after: []
                    }
                    break
            }
            if (attrs && mark.attrs) {
                for (const attr in attrs) {
                    if (
                        attr in mark.attrs &&
                        fast_deep_equal__rspack_import_0_default()(mark.attrs[attr], attrs[attr])
                    ) {
                        delete mark.attrs[attr]
                    }
                }
            }
        }
    }

    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV30(childNode)
        })
    }
}

const convertDocV30 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV30(returnDoc.content)
    return returnDoc
}

const convertDocV31 = doc => {
    // Conversion adds no new requirements. Version update is required so that
    // users don't try to open file in a previous FW file. That won't work as
    // additional syntax has been added (copyright + cross references).
    const returnDoc = JSON.parse(JSON.stringify(doc))
    return returnDoc
}

const convertNodeV32 = (node, ids = []) => {
    let blockId, attrs
    switch (node.type) {
        case "table":
            attrs = node.attrs || {}
            blockId = attrs.id
            while (!blockId || ids.includes(blockId)) {
                blockId = (0,_common__rspack_import_1.randomTableId)()
            }
            attrs.id = blockId
            attrs.caption = false
            node.attrs = attrs
            ids.push(blockId)
            node.content = [
                {type: "table_caption"},
                {
                    type: "table_body",
                    content: node.content
                }
            ]
            break
        case "table_cell":
            if (!node.content || !node.content.length) {
                node.content = [{type: "paragraph"}]
            }
            break
        case "table_header":
            if (!node.content || !node.content.length) {
                node.content = [{type: "paragraph"}]
            }
            break
        case "bullet_list":
        case "ordered_list":
            attrs = node.attrs || {}
            blockId = attrs.id
            while (!blockId || ids.includes(blockId)) {
                blockId = (0,_common__rspack_import_1.randomListId)()
            }
            attrs.id = blockId
            node.attrs = attrs
            ids.push(blockId)
            break
        case "figure": {
            attrs = node.attrs || {}
            if (attrs.figureCategory) {
                attrs.category = attrs.figureCategory
                delete attrs.figureCategory
            }
            node.content = []
            if (attrs.image) {
                node.content.push({type: "image", attrs: {image: attrs.image}})
            } else {
                node.content.push({
                    type: "figure_equation",
                    attrs: {equation: attrs.equation || ""}
                })
            }
            delete attrs.image
            delete attrs.equation

            const caption = {type: "figure_caption"}
            if (attrs.caption) {
                if (attrs.caption.length) {
                    caption.content = [{type: "text", text: attrs.caption}]
                    attrs.caption = true
                } else {
                    attrs.caption = false
                }
            } else {
                attrs.caption = false
            }
            if (attrs.category === "table") {
                node.content.unshift(caption)
            } else {
                node.content.push(caption)
            }
            node.attrs = attrs
            break
        }
        case "footnote":
            if (node.attrs?.footnote) {
                node.attrs.footnote.forEach(childNode => {
                    convertNodeV32(childNode, ids)
                })
            }
            break
    }
    if (node.content) {
        node.content.forEach(childNode => {
            convertNodeV32(childNode, ids)
        })
    }
    if (node.attrs?.initial) {
        node.attrs.initial.forEach(childNode => {
            convertNodeV32(childNode, ids)
        })
    }
}

const convertDocV32 = doc => {
    const returnDoc = JSON.parse(JSON.stringify(doc))
    convertNodeV32(returnDoc.content)
    return returnDoc
}

const convertDocV33 = doc => {
    // We just need to increase the version number so that documents cannot
    // be moved from a 3.10 to an 3.9 system, but 3.3 files should be readable
    // as 3.4 files.
    return JSON.parse(JSON.stringify(doc))
}

const convertDocV34 = doc => {
    // The top node needs to be changed from "article" to "doc".
    const returnDoc = JSON.parse(JSON.stringify(doc))
    returnDoc.content.type = "doc"
    return returnDoc
}


}),
"./js/modules/schema/document/content.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  code_block: function() { return code_block; },
  contributor: function() { return contributor; },
  footnote: function() { return footnote; },
  randomCodeBlockId: function() { return randomCodeBlockId; },
  tag: function() { return tag; }
});
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/common/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _footnotes_convert__rspack_import_2 = __webpack_require__("./js/modules/schema/footnotes_convert.js");




const randomCodeBlockId = () => {
    return `C${Math.round(Math.random() * 10000000) + 1}`
}

const contributor = {
    inline: true,
    draggable: true,
    attrs: {
        firstname: {default: false},
        lastname: {default: false},
        email: {default: false},
        institution: {default: false}
    },
    parseDOM: [
        {
            tag: "span.contributor",
            getAttrs(dom) {
                return {
                    firstname: dom.dataset.firstname,
                    lastname: dom.dataset.lastname,
                    email: dom.dataset.email,
                    institution: dom.dataset.institution
                }
            }
        }
    ],
    toDOM(node) {
        const dom = document.createElement("span")
        dom.classList.add("contributor")
        dom.dataset.firstname = node.attrs.firstname
        dom.dataset.lastname = node.attrs.lastname
        dom.dataset.email = node.attrs.email
        dom.dataset.institution = node.attrs.institution
        const content = []
        if (node.attrs.firstname) {
            content.push((0,_common__rspack_import_0.escapeText)(node.attrs.firstname))
        }
        if (node.attrs.lastname) {
            content.push((0,_common__rspack_import_0.escapeText)(node.attrs.lastname))
        }
        if (node.attrs.email) {
            content.push(
                `<i>${gettext("Email")}: ${(0,_common__rspack_import_0.escapeText)(node.attrs.email)}</i>`
            )
        }
        if (node.attrs.institution) {
            content.push(`(${(0,_common__rspack_import_0.escapeText)(node.attrs.institution)})`)
        }

        dom.innerHTML = content.join(" ")

        return dom
    }
}

const tag = {
    inline: true,
    draggable: true,
    attrs: {
        tag: {
            default: ""
        }
    },
    parseDOM: [
        {
            tag: "span.tag",
            getAttrs(dom) {
                return {
                    tag: dom.innerText
                }
            }
        }
    ],
    toDOM(node) {
        return ["span", {class: "tag"}, node.attrs.tag]
    }
}

const footnote = {
    inline: true,
    group: "inline",
    attrs: {
        footnote: {
            default: [
                {
                    type: "paragraph"
                }
            ]
        }
    },
    parseDOM: [
        {
            tag: "span.footnote-marker[data-footnote]",
            getAttrs(dom) {
                return {
                    footnote: (0,_footnotes_convert__rspack_import_2.htmlToFnNode)(dom.dataset.footnote)
                }
            }
        }
    ],
    toDOM(node) {
        const dom = document.createElement("span")
        dom.classList.add("footnote-marker")
        dom.dataset.footnote = (0,_footnotes_convert__rspack_import_2.fnNodeToHtml)(node.attrs.footnote)
        dom.innerHTML = "&nbsp;"
        return dom
    }
}

const code_block = {
    content: "text*",
    marks: "_",
    group: "block",
    code: true,
    defining: true,
    attrs: {
        track: {
            default: []
        },
        language: {
            default: ""
        },
        category: {
            default: ""
        },
        title: {
            default: ""
        },
        id: {
            default: ""
        }
    },
    parseDOM: [
        {
            tag: "pre",
            preserveWhitespace: "full",
            getAttrs(dom) {
                return {
                    track: (0,_common__rspack_import_1.parseTracks)(dom.dataset.track),
                    language: dom.dataset.language || "",
                    category: dom.dataset.category || "",
                    title: dom.dataset.title || "",
                    id: dom.dataset.id || ""
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {}
        if (node.attrs.track.length) {
            attrs["data-track"] = JSON.stringify(node.attrs.track)
        }
        if (node.attrs.language) {
            attrs["data-language"] = node.attrs.language
        }
        if (node.attrs.category) {
            attrs["data-category"] = node.attrs.category
        }
        if (node.attrs.title) {
            attrs["data-title"] = node.attrs.title
        }
        if (node.attrs.id) {
            attrs["data-id"] = node.attrs.id
        }
        return ["pre", attrs, ["code", 0]]
    }
}


}),
"./js/modules/schema/document/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  docSchema: function() { return docSchema; }
});
/* import */ var orderedmap__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/orderedmap@2.1.1/node_modules/orderedmap/dist/index.js");
/* import */ var prosemirror_model__rspack_import_5 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_schema_basic__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-basic@1.2.4/node_modules/prosemirror-schema-basic/dist/index.js");
/* import */ var _common__rspack_import_0 = __webpack_require__("./js/modules/schema/common/index.js");
/* import */ var _content__rspack_import_1 = __webpack_require__("./js/modules/schema/document/content.js");
/* import */ var _structure__rspack_import_2 = __webpack_require__("./js/modules/schema/document/structure.js");







const specNodes = orderedmap__rspack_import_3["default"].from({
    doc: _structure__rspack_import_2.doc,
    richtext_part: _structure__rspack_import_2.richtext_part,
    heading_part: _structure__rspack_import_2.heading_part,
    contributors_part: _structure__rspack_import_2.contributors_part,
    tags_part: _structure__rspack_import_2.tags_part,
    table_part: _structure__rspack_import_2.table_part,
    table_of_contents: _structure__rspack_import_2.table_of_contents,
    separator_part: _structure__rspack_import_2.separator_part,
    title: _structure__rspack_import_2.title,
    contributor: _content__rspack_import_1.contributor,
    tag: _content__rspack_import_1.tag,
    paragraph: _common__rspack_import_0.paragraph,
    blockquote: _common__rspack_import_0.blockquote,
    horizontal_rule: _common__rspack_import_0.horizontal_rule,
    figure: _common__rspack_import_0.figure,
    image: _common__rspack_import_0.image,
    figure_equation: _common__rspack_import_0.figure_equation,
    figure_caption: _common__rspack_import_0.figure_caption,
    heading1: _common__rspack_import_0.heading1,
    heading2: _common__rspack_import_0.heading2,
    heading3: _common__rspack_import_0.heading3,
    heading4: _common__rspack_import_0.heading4,
    heading5: _common__rspack_import_0.heading5,
    heading6: _common__rspack_import_0.heading6,
    code_block: _content__rspack_import_1.code_block,
    text: prosemirror_schema_basic__rspack_import_4.nodes.text,
    hard_break: prosemirror_schema_basic__rspack_import_4.nodes.hard_break,
    citation: _common__rspack_import_0.citation,
    equation: _common__rspack_import_0.equation,
    cross_reference: _common__rspack_import_0.cross_reference,
    footnote: _content__rspack_import_1.footnote,
    ordered_list: _common__rspack_import_0.ordered_list,
    bullet_list: _common__rspack_import_0.bullet_list,
    list_item: _common__rspack_import_0.list_item,
    table: _common__rspack_import_0.table,
    table_caption: _common__rspack_import_0.table_caption,
    table_body: _common__rspack_import_0.table_body,
    table_row: _common__rspack_import_0.table_row,
    table_cell: _common__rspack_import_0.table_cell,
    table_header: _common__rspack_import_0.table_header
})

const spec = {
    nodes: specNodes,
    marks: orderedmap__rspack_import_3["default"].from({
        em: prosemirror_schema_basic__rspack_import_4.marks.em,
        strong: prosemirror_schema_basic__rspack_import_4.marks.strong,
        link: _common__rspack_import_0.link,
        underline: _common__rspack_import_0.underline,
        sup: _common__rspack_import_0.sup,
        sub: _common__rspack_import_0.sub,
        code: _common__rspack_import_0.code,
        comment: _common__rspack_import_0.comment,
        annotation_tag: _common__rspack_import_0.annotation_tag,
        anchor: _common__rspack_import_0.anchor,
        deletion: _common__rspack_import_0.deletion,
        insertion: _common__rspack_import_0.insertion,
        format_change: _common__rspack_import_0.format_change
    })
}

const docSchema = new prosemirror_model__rspack_import_5.Schema(spec)


}),
"./js/modules/schema/document/structure.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  contributors_part: function() { return contributors_part; },
  doc: function() { return doc; },
  heading_part: function() { return heading_part; },
  richtext_part: function() { return richtext_part; },
  separator_part: function() { return separator_part; },
  table_of_contents: function() { return table_of_contents; },
  table_part: function() { return table_part; },
  tags_part: function() { return tags_part; },
  title: function() { return title; }
});
const doc = {
    content: "title part*",
    selectable: false,
    allowGapCursor: false,
    attrs: {
        documentstyle: {
            default: ""
        },
        tracked: {
            default: false
        },
        citationstyle: {
            // Default citation style
            default: "apa"
        },
        citationstyles: {
            // Available citation styles
            default: [
                "american-anthropological-association",
                "apa",
                "chicago-author-date",
                "chicago-note-bibliography",
                "harvard-cite-them-right", // previously harvard1
                "modern-language-association", // previously mla
                "nature",
                "oxford-university-press-humsoc"
            ]
        },
        language: {
            // Default language
            default: "en-US"
        },
        languages: {
            // Available languages
            default: [
                "af-ZA",
                "sq-AL",
                "ar",
                "ast",
                "be",
                "br",
                "bg",
                "ca",
                "ca-ES-Valencia",
                "zh-CN",
                "da",
                "nl",
                "en-AU",
                "en-CA",
                "en-NZ",
                "en-ZA",
                "en-GB",
                "en-US",
                "eo",
                "fr",
                "gl",
                "de-DE",
                "de-AU",
                "de-CH",
                "el",
                "he",
                "is",
                "it",
                "ja",
                "km",
                "lt",
                "ml",
                "nb-NO",
                "nn-NO",
                "fa",
                "pl",
                "pt-BR",
                "pt-PT",
                "ro",
                "ru",
                "tr",
                "sr-SP-Cy",
                "sr-SP-Lt",
                "sk",
                "sl",
                "es",
                "sv",
                "ta",
                "tl",
                "uk"
            ]
        },
        papersize: {
            // Default paper size
            default: "A4"
        },
        papersizes: {
            // Available paper sizes
            default: ["A4", "US Letter"]
        },
        footnote_marks: {
            default: ["strong", "em", "link"]
        },
        footnote_elements: {
            default: [
                "paragraph",
                "heading1",
                "heading2",
                "heading3",
                "heading4",
                "heading5",
                "heading6",
                "figure",
                "ordered_list",
                "bullet_list",
                "horizontal_rule",
                "equation",
                "citation",
                "cross_reference",
                "blockquote",
                "table"
            ]
        },
        bibliography_header: {
            default: {}
        },
        template: {
            default: ""
        },
        import_id: {
            default: ""
        },
        copyright: {
            default: {
                holder: false,
                year: false,
                freeToRead: true,
                licenses: []
            }
        },
        code_categories: {
            default: {
                listing: {
                    counter: 0,
                    enabled: true
                },
                example: {
                    counter: 0,
                    enabled: true
                },
                snippet: {
                    counter: 0,
                    enabled: false
                },
                tutorial: {
                    counter: 0,
                    enabled: false
                },
                exercise: {
                    counter: 0,
                    enabled: false
                },
                exercise_solution: {
                    counter: 0,
                    enabled: false
                }
            }
        },
        code_languages: {
            default: [
                "javascript",
                "python",
                "java",
                "cpp",
                "c",
                "csharp",
                "php",
                "ruby",
                "go",
                "rust",
                "swift",
                "kotlin",
                "typescript",
                "html",
                "css",
                "sql",
                "bash",
                "shell",
                "r",
                "matlab",
                "scala",
                "perl",
                "lua",
                "haskell",
                "xml",
                "json",
                "yaml",
                "markdown"
            ]
        }
    },
    parseDOM: [
        {
            tag: "div.doc",
            getAttrs(dom) {
                return {
                    papersize: dom.dataset.papersize,
                    citationstyle: dom.dataset.citationstyle,
                    documentstyle: dom.dataset.documentstyle
                }
            }
        }
    ],
    toDOM(node) {
        return [
            "div",
            {
                class: "doc",
                "data-papersize": node.attrs.papersize,
                "data-citationstyle": node.attrs.citationstyle,
                "data-documentstyle": node.attrs.documentstyle
            },
            0
        ]
    }
}

const partSpec = (type, content, attrs = {}) => ({
    content,
    group: "part",
    marks: "annotation track",
    isolating: true,
    attrs: Object.assign(
        {
            title: {
                default: ""
            },
            id: {
                default: ""
            },
            locking: {
                default: false
            },
            language: {
                default: false
            },
            optional: {
                default: false
            },
            hidden: {
                default: false
            },
            help: {
                default: false
            },
            initial: {
                default: false
            },
            deleted: {
                // used when a part is present in a document but not part of the document template due to template changes
                default: false
            }
        },
        attrs
    ),
    parseDOM: [
        {
            tag: `div.doc-${type}`,
            getAttrs(dom) {
                return {
                    hidden: dom.dataset.hidden === "true" ? true : false
                }
            }
        }
    ],
    toDOM(node) {
        const attrs = {
            class: `doc-part doc-${type} ${node.attrs.id ? `doc-${node.attrs.id}` : "doc-no-id"}`
        }
        if (node.attrs.hidden) {
            attrs["data-hidden"] = "true"
        }
        if (node.attrs.deleted) {
            attrs.class += " doc-deleted"
        }
        return ["div", attrs, 0]
    }
})

const richtext_part = partSpec("richtext", "block+", {
    elements: {
        default: [
            "paragraph",
            "heading1",
            "heading2",
            "heading3",
            "heading4",
            "heading5",
            "heading6",
            "code_block",
            "figure",
            "ordered_list",
            "bullet_list",
            "horizontal_rule",
            "equation",
            "citation",
            "cross_reference",
            "blockquote",
            "footnote",
            "table"
        ]
    },
    marks: {
        default: ["strong", "em", "link", "anchor", "sup", "sub", "code"]
    },
    metadata: {
        default: false
    }
})
const heading_part = partSpec("heading", "heading", {
    elements: {
        default: ["heading1"]
    },
    marks: {
        default: ["strong", "em", "link", "anchor", "sup", "sub", "code"]
    },
    metadata: {
        default: false
    }
})
const contributors_part = partSpec("contributors", "contributor*", {
    item_title: {
        default: "Contributor"
    },
    metadata: {
        default: false
    }
})
const tags_part = partSpec("tags", "tag*", {
    item_title: {
        default: "Tag"
    },
    metadata: {
        default: false
    }
})
const table_part = partSpec("table", "table", {
    elements: {
        default: [
            "paragraph",
            "heading1",
            "heading2",
            "heading3",
            "heading4",
            "heading5",
            "heading6",
            "code_block",
            "figure",
            "ordered_list",
            "bullet_list",
            "horizontal_rule",
            "equation",
            "citation",
            "blockquote",
            "footnote"
        ]
    },
    marks: {
        default: ["strong", "em", "link", "anchor", "sup", "sub", "code"]
    }
})

const table_of_contents = {
    group: "part",
    marks: "annotation track",
    defining: true,
    parseDOM: [
        {
            tag: "div.table-of-contents"
        }
    ],
    attrs: {
        title: {
            default: "Table of Contents"
        },
        id: {
            default: "toc"
        },
        optional: {
            default: false
        },
        hidden: {
            default: false
        }
    },
    toDOM(node) {
        const dom = document.createElement("div")
        dom.classList.add("doc-part", "table-of-contents")
        if (node.attrs.hidden) {
            dom.dataset.hidden = "true"
        }
        dom.innerHTML = `<h1 class="toc">${node.attrs.title}</h1>`
        return dom
    }
}

const separator_part = {
    marks: "annotation track",
    group: "part",
    defining: true,
    attrs: {
        id: {
            default: "separator"
        }
    },
    parseDOM: [
        {
            tag: "hr.doc-separator_part"
        }
    ],
    toDOM(node) {
        const dom = document.createElement("hr")
        dom.classList.add("doc-separator_part")
        dom.classList.add(`doc-${node.attrs.id}`)
        return dom
    }
}

const title = {
    content: "text*",
    marks: "annotation track",
    group: "fixedpart",
    defining: true,
    isolating: true,
    attrs: {
        id: {
            default: "title"
        }
    },
    parseDOM: [
        {
            tag: "div.doc-title"
        }
    ],
    toDOM(_node) {
        return [
            "div",
            {
                class: "doc-part doc-title"
            },
            0
        ]
    }
}


}),
"./js/modules/schema/export.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  SchemaExport: function() { return SchemaExport; }
});
/* import */ var _plugins_schema_export__rspack_import_0 = __webpack_require__("./js/plugins/schema_export/index.js");
/* import */ var _plugins_schema_export__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(_plugins_schema_export__rspack_import_0);
/* import */ var _document__rspack_import_1 = __webpack_require__("./js/modules/schema/document/index.js");



class SchemaExport {
    constructor() {
        this.schema = _document__rspack_import_1.docSchema
    }

    init() {
        this.activateFidusPlugins()
        const spec = {
            nodes: {},
            marks: {}
        }
        this.schema.spec.nodes.forEach(
            (key, value) => (spec.nodes[key] = value)
        )
        this.schema.spec.marks.forEach(
            (key, value) => (spec.marks[key] = value)
        )
        return JSON.stringify(spec)
    }

    activateFidusPlugins() {
        // Add plugins.
        this.plugins = {}

        Object.keys(_plugins_schema_export__rspack_import_0).forEach(plugin => {
            if (typeof _plugins_schema_export__rspack_import_0[plugin] === "function") {
                this.plugins[plugin] = new _plugins_schema_export__rspack_import_0[plugin](this)
                this.plugins[plugin].init()
            }
        })
    }
}


}),
"./js/modules/schema/footnotes.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  fnSchema: function() { return fnSchema; }
});
/* import */ var orderedmap__rspack_import_2 = __webpack_require__("./node_modules/.pnpm/orderedmap@2.1.1/node_modules/orderedmap/dist/index.js");
/* import */ var prosemirror_model__rspack_import_4 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var prosemirror_schema_basic__rspack_import_3 = __webpack_require__("./node_modules/.pnpm/prosemirror-schema-basic@1.2.4/node_modules/prosemirror-schema-basic/dist/index.js");
/* import */ var prosemirror_tables__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/prosemirror-tables@1.6.4/node_modules/prosemirror-tables/dist/index.js");
/* import */ var _common__rspack_import_1 = __webpack_require__("./js/modules/schema/common/index.js");






const footnotecontainer = {
    group: "part",
    selectable: false,
    content: "(block|table_block)+",
    marks: "annotation",
    parseDOM: [{tag: "div.footnote-container"}],
    toDOM(_node) {
        return ["div", {class: "footnote-container"}, 0]
    }
}

const doc = {
    content: "part*",
    selectable: false
}

const spec = {
    nodes: orderedmap__rspack_import_2["default"].from({
        doc,
        footnotecontainer,
        paragraph: _common__rspack_import_1.paragraph,
        heading1: _common__rspack_import_1.heading1,
        heading2: _common__rspack_import_1.heading2,
        heading3: _common__rspack_import_1.heading3,
        heading4: _common__rspack_import_1.heading4,
        heading5: _common__rspack_import_1.heading5,
        heading6: _common__rspack_import_1.heading6,
        blockquote: _common__rspack_import_1.blockquote,
        horizontal_rule: _common__rspack_import_1.horizontal_rule,
        figure: _common__rspack_import_1.figure,
        image: _common__rspack_import_1.image,
        figure_equation: _common__rspack_import_1.figure_equation,
        figure_caption: _common__rspack_import_1.figure_caption,
        text: prosemirror_schema_basic__rspack_import_3.nodes.text,
        hard_break: prosemirror_schema_basic__rspack_import_3.nodes.hard_break,
        citation: _common__rspack_import_1.citation,
        equation: _common__rspack_import_1.equation,
        cross_reference: _common__rspack_import_1.cross_reference,
        ordered_list: _common__rspack_import_1.ordered_list,
        bullet_list: _common__rspack_import_1.bullet_list,
        list_item: _common__rspack_import_1.list_item
    }),
    marks: orderedmap__rspack_import_2["default"].from({
        em: prosemirror_schema_basic__rspack_import_3.marks.em,
        strong: prosemirror_schema_basic__rspack_import_3.marks.strong,
        link: _common__rspack_import_1.link,
        underline: _common__rspack_import_1.underline,
        anchor: _common__rspack_import_1.anchor,
        comment: _common__rspack_import_1.comment,
        annotation_tag: _common__rspack_import_1.annotation_tag,
        deletion: _common__rspack_import_1.deletion,
        insertion: _common__rspack_import_1.insertion,
        format_change: _common__rspack_import_1.format_change
    })
}

spec.nodes = spec.nodes.append(
    (0,prosemirror_tables__rspack_import_0.tableNodes)({
        tableGroup: "table_block",
        cellContent: "block+"
    })
)

spec.nodes = spec.nodes.update(
    "table",
    Object.assign({}, spec.nodes.get("table"), {
        attrs: {
            track: {default: []}
        },
        parseDOM: [
            {
                tag: "table",
                getAttrs(dom) {
                    return {
                        track: dom.dataset.track
                            ? JSON.parse(dom.dataset.track)
                            : []
                    }
                }
            }
        ],
        toDOM(node) {
            const attrs = {}
            if (node.attrs.track.length) {
                attrs["data-track"] = JSON.stringify(node.attrs.track)
            }
            return ["table", attrs, ["tbody", 0]]
        }
    })
)

const fnSchema = new prosemirror_model__rspack_import_4.Schema(spec)


}),
"./js/modules/schema/footnotes_convert.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  fnNodeToHtml: function() { return fnNodeToHtml; },
  fnNodeToPmNode: function() { return fnNodeToPmNode; },
  htmlToFnNode: function() { return htmlToFnNode; }
});
/* import */ var prosemirror_model__rspack_import_1 = __webpack_require__("./node_modules/.pnpm/prosemirror-model@1.25.0/node_modules/prosemirror-model/dist/index.js");
/* import */ var _footnotes__rspack_import_0 = __webpack_require__("./js/modules/schema/footnotes.js");




// Convert the footnote HTML stored with the marker to a PM node representation of the footnote.
const htmlToFnNode = content => {
    const footnoteDOM = document.createElement("div")
    footnoteDOM.classList.add("footnote-container")
    footnoteDOM.innerHTML = content
    const node = prosemirror_model__rspack_import_1.DOMParser.fromSchema(_footnotes__rspack_import_0.fnSchema).parse(footnoteDOM, {
        preserveWhitespace: true,
        topNode: false
    })
    const json = node.firstChild.toJSON().content

    return json
}

const fnNodeToPmNode = fnContents => {
    const footnote = {
        type: "footnotecontainer",
        content: fnContents
    }
    return prosemirror_model__rspack_import_1.Node.fromJSON(_footnotes__rspack_import_0.fnSchema, footnote)
}

const fnNodeToHtml = jsonString => {
    const pmNode = fnNodeToPmNode(jsonString),
        serializer = prosemirror_model__rspack_import_1.DOMSerializer.fromSchema(_footnotes__rspack_import_0.fnSchema)
    return serializer.serializeNode(pmNode).innerHTML
}


}),
"./js/modules/schema/i18n.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  BIBLIOGRAPHY_HEADERS: function() { return BIBLIOGRAPHY_HEADERS; },
  CATS: function() { return CATS; }
});
const BIBLIOGRAPHY_HEADERS = {
    "af-ZA": "Bibliografie",
    "sq-AL": "Bibliografi",
    ar: "قائمة المراجع",
    ast: "Bibliografía",
    be: "Бібліяграфія",
    br: "Levrlennadur",
    bg: "библиография",
    ca: "Bibliografia",
    "ca-ES-Valencia": "Bibliografia",
    "zh-CN": "参考书目",
    da: "Bibliografi",
    nl: "Bibliografie",
    "en-AU": "Bibliography",
    "en-CA": "Bibliography",
    "en-NZ": "Bibliography",
    "en-ZA": "Bibliography",
    "en-GB": "Bibliography",
    "en-US": "Bibliography",
    eo: "Bibliografio",
    fr: "Bibliographie",
    gl: "Bibliografía",
    "de-DE": "Literaturverzeichnis",
    "de-AU": "Literaturverzeichnis",
    "de-CH": "Literaturverzeichnis",
    el: "Βιβλιογραφία",
    he: "ביבליוגרפיה",
    is: "Bókaskrá",
    it: "Bibliografia",
    ja: "書誌",
    km: "គន្ថនិទ្ទេស",
    lt: "Bibliografija",
    ml: "ഗ്രന്ഥസൂചിക",
    "nb-NO": "Bibliografi",
    "nn-NO": "Bibliografi",
    fa: "کتابشناسی",
    pl: "Bibliografia",
    "pt-BR": "Bibliografia",
    "pt-PT": "Bibliografia",
    ro: "Bibliografie",
    ru: "библиография",
    tr: "Kaynakça",
    "sr-SP-Cy": "Библиографија",
    "sr-SP-Lt": "Bibliografija",
    sk: "Bibliografia",
    sl: "Bibliografija",
    es: "Bibliografía",
    sv: "Bibliografi",
    ta: "நூற்பட்டியல்",
    tl: "Bibliograpiya",
    uk: "Бібліографія"
}

const FIGURE = {
    "af-ZA": "Figuur",
    "sq-AL": "figurë",
    ar: "الشكل",
    ast: "Figura",
    be: "Малюнак",
    br: "Sifr",
    bg: "Фигура",
    ca: "Figura",
    "ca-ES-Valencia": "Figura",
    "zh-CN": "数字",
    da: "Figur",
    nl: "Figuur",
    "en-AU": "Figure",
    "en-CA": "Figure",
    "en-NZ": "Figure",
    "en-ZA": "Figure",
    "en-GB": "Figure",
    "en-US": "Figure",
    eo: "Figuro",
    fr: "Figure",
    gl: "Figura",
    "de-DE": "Abbildung",
    "de-AU": "Abbildung",
    "de-CH": "Abbildung",
    el: "Φιγούρα",
    he: "דמות",
    is: "Mynd",
    it: "Figura",
    ja: "図",
    km: "រូបភាព",
    lt: "Pav",
    ml: "ചിത്രം",
    "nb-NO": "Figur",
    "nn-NO": "Figur",
    fa: "شکل",
    pl: "Postać",
    "pt-BR": "Figura",
    "pt-PT": "Figura",
    ro: "Figura",
    ru: "фигура",
    tr: "şekil",
    "sr-SP-Cy": "Фигура",
    "sr-SP-Lt": "Figura",
    sk: "Figúra",
    sl: "Slika",
    es: "Figura",
    sv: "Figur",
    ta: "படம்",
    tl: "Figure",
    uk: "Малюнок"
}

const EQUATION = {
    "af-ZA": "Vergelyking",
    "sq-AL": "Ekuacioni",
    ar: "معادلة",
    ast: "Ecuación",
    be: "Yраўненне",
    br: "Kevatalenn",
    bg: "Yравнение",
    ca: "Equació",
    "ca-ES-Valencia": "Equació",
    "zh-CN": "方程",
    da: "Ligning",
    nl: "Vergelijking",
    "en-AU": "Equation",
    "en-CA": "Equation",
    "en-NZ": "Equation",
    "en-ZA": "Equation",
    "en-GB": "Equation",
    "en-US": "Equation",
    eo: "Ekvacio",
    fr: "Équation",
    gl: "Ecuación",
    "de-DE": "Gleichung",
    "de-AU": "Gleichung",
    "de-CH": "Gleichung",
    el: "εξίσωση ",
    he: "משוואה",
    is: "Jöfnu",
    it: "Equazione",
    ja: "方程式",
    km: "សមីការ",
    lt: "Lygtis",
    ml: "സമവാക്യം",
    "nb-NO": "Ligning",
    "nn-NO": "Likning",
    fa: "معادله",
    pl: "Równanie",
    "pt-BR": "Equação",
    "pt-PT": "Equação",
    ro: "Ecuaţie",
    ru: "Yравнение",
    tr: "Denklem",
    "sr-SP-Cy": "Једначина",
    "sr-SP-Lt": "Jednačina",
    sk: "Rovnica",
    sl: "Enačba",
    es: "Ecuación",
    sv: "Ekvation",
    ta: "சமன்பாடு",
    tl: "Equation",
    uk: "Pівняння"
}

const TABLE = {
    "af-ZA": "Tabel",
    "sq-AL": "tabelë",
    ar: "الطاولة",
    ast: "Tabla",
    be: "Табліца",
    br: "Taolenn",
    bg: "маса",
    ca: "Taula",
    "ca-ES-Valencia": "Taula",
    "zh-CN": "表",
    da: "Tabel",
    nl: "Tafel",
    "en-AU": "Table",
    "en-CA": "Table",
    "en-NZ": "Table",
    "en-ZA": "Table",
    "en-GB": "Table",
    "en-US": "Table",
    eo: "Tabelo",
    fr: "Table",
    gl: "Táboa",
    "de-DE": "Tabelle",
    "de-AU": "Tabelle",
    "de-CH": "Tabelle",
    el: "Τραπέζι",
    es: "Tabla",
    he: "שולחן",
    is: "Tafla",
    it: "Tavolo",
    ja: "表",
    km: "តារាង",
    lt: "Lentelė",
    ml: "മേശ",
    "nb-NO": "Tabel",
    "nn-NO": "Tabel",
    fa: "جدول",
    pl: "Stół",
    "pt-BR": "Tabela",
    "pt-PT": "Tabela",
    ro: "Tabel",
    ru: "Таблица",
    tr: "Tablo",
    "sr-SP-Cy": "Табела",
    "sr-SP-Lt": "Tabela",
    sk: "Stôl",
    sl: "Tabela",
    sv: "Tabell",
    ta: "மேசை",
    tl: "Table",
    uk: "Таблиця"
}

const PHOTO = {
    "af-ZA": "Foto",
    "sq-AL": "Foto",
    ar: "صورة فوتوغرافية",
    ast: "Foto",
    be: "Фота",
    br: "Foto",
    bg: "снимка",
    ca: "Foto",
    "ca-ES-Valencia": "Foto",
    "zh-CN": "照片",
    da: "Foto",
    nl: "Foto",
    "en-AU": "Photo",
    "en-CA": "Photo",
    "en-NZ": "Photo",
    "en-ZA": "Photo",
    "en-GB": "Photo",
    "en-US": "Photo",
    eo: "Foto",
    fr: "Photo",
    gl: "Foto",
    "de-DE": "Foto",
    "de-AU": "Foto",
    "de-CH": "Foto",
    el: "φωτογραφία",
    he: "תמונה",
    is: "Mynd",
    it: "Foto",
    ja: "写真",
    km: "រូបថត",
    lt: "Nuotrauka",
    ml: "ഫോട്ടോ",
    "nb-NO": "Bilde",
    "nn-NO": "Bilde",
    fa: "عکس",
    pl: "Zdjęcie",
    "pt-BR": "Foto",
    "pt-PT": "Foto",
    ro: "Fotografie",
    ru: "Фото",
    tr: "Fotoğraf",
    "sr-SP-Cy": "Фото",
    "sr-SP-Lt": "Foto",
    sk: "Fotografie",
    sl: "Fotografija",
    es: "Foto",
    sv: "Foto",
    ta: "புகைப்பட",
    tl: "Larawan",
    uk: "Фото"
}

const LISTING = {
    "af-ZA": "Lys",
    "sq-AL": "Listë",
    ar: "قائمة",
    ast: "Llista",
    be: "Спіс",
    br: "Roll",
    bg: "Списък",
    ca: "Llista",
    "ca-ES-Valencia": "Llista",
    "zh-CN": "清单",
    da: "Liste",
    nl: "Lijst",
    "en-AU": "Listing",
    "en-CA": "Listing",
    "en-NZ": "Listing",
    "en-ZA": "Listing",
    "en-GB": "Listing",
    "en-US": "Listing",
    eo: "Listo",
    fr: "Liste",
    gl: "Listaxe",
    "de-DE": "Listing",
    "de-AU": "Listing",
    "de-CH": "Listing",
    el: "Λίστα",
    he: "רשימה",
    is: "Listi",
    it: "Elenco",
    ja: "リスト",
    km: "បញ្ជី",
    lt: "Sąrašas",
    ml: "പട്ടിക",
    "nb-NO": "Liste",
    "nn-NO": "Liste",
    fa: "فهرست",
    pl: "Lista",
    "pt-BR": "Listagem",
    "pt-PT": "Listagem",
    ro: "Listing",
    ru: "Листинг",
    tr: "Liste",
    "sr-SP-Cy": "Листа",
    "sr-SP-Lt": "Lista",
    sk: "Zoznam",
    sl: "Seznam",
    es: "Listado",
    sv: "Lista",
    ta: "பட்டியல்",
    tl: "Listahan",
    uk: "Лістинг"
}

const EXAMPLE = {
    "af-ZA": "Voorbeeld",
    "sq-AL": "Shembull",
    ar: "مثال",
    ast: "Exemplu",
    be: "Прыклад",
    br: "Skouer",
    bg: "Пример",
    ca: "Exemple",
    "ca-ES-Valencia": "Exemple",
    "zh-CN": "示例",
    da: "Eksempel",
    nl: "Voorbeeld",
    "en-AU": "Example",
    "en-CA": "Example",
    "en-NZ": "Example",
    "en-ZA": "Example",
    "en-GB": "Example",
    "en-US": "Example",
    eo: "Ekzemplo",
    fr: "Exemple",
    gl: "Exemplo",
    "de-DE": "Beispiel",
    "de-AU": "Beispiel",
    "de-CH": "Beispiel",
    el: "Παράδειγμα",
    he: "דוגמה",
    is: "Dæmi",
    it: "Esempio",
    ja: "例",
    km: "ឧទាហរណ៍",
    lt: "Pavyzdys",
    ml: "ഉദാഹരണം",
    "nb-NO": "Eksempel",
    "nn-NO": "Eksempel",
    fa: "مثال",
    pl: "Przykład",
    "pt-BR": "Exemplo",
    "pt-PT": "Exemplo",
    ro: "Exemplu",
    ru: "Пример",
    tr: "Örnek",
    "sr-SP-Cy": "Пример",
    "sr-SP-Lt": "Primer",
    sk: "Príklad",
    sl: "Primer",
    es: "Ejemplo",
    sv: "Exempel",
    ta: "உதாரணம்",
    tl: "Halimbawa",
    uk: "Приклад"
}

const SNIPPET = {
    "af-ZA": "Snipper",
    "sq-AL": "Fragment",
    ar: "مقتطف",
    ast: "Fragmentu",
    be: "Фрагмент",
    br: "Tamm",
    bg: "Фрагмент",
    ca: "Fragment",
    "ca-ES-Valencia": "Fragment",
    "zh-CN": "片段",
    da: "Uddrag",
    nl: "Fragment",
    "en-AU": "Snippet",
    "en-CA": "Snippet",
    "en-NZ": "Snippet",
    "en-ZA": "Snippet",
    "en-GB": "Snippet",
    "en-US": "Snippet",
    eo: "Fragmento",
    fr: "Extrait",
    gl: "Fragmento",
    "de-DE": "Snippet",
    "de-AU": "Snippet",
    "de-CH": "Snippet",
    el: "Απόσπασμα",
    he: "קטע",
    is: "Brot",
    it: "Frammento",
    ja: "スニペット",
    km: "កម្មវិធីតូច",
    lt: "Fragmentas",
    ml: "സ്നിപ്പറ്റ്",
    "nb-NO": "Utdrag",
    "nn-NO": "Utdrag",
    fa: "قطعه",
    pl: "Fragment",
    "pt-BR": "Trecho",
    "pt-PT": "Excerto",
    ro: "Fragment",
    ru: "Фрагмент",
    tr: "Parçacık",
    "sr-SP-Cy": "Фрагмент",
    "sr-SP-Lt": "Fragment",
    sk: "Fragment",
    sl: "Delček",
    es: "Fragmento",
    sv: "Utdrag",
    ta: "துணுக்கு",
    tl: "Snippet",
    uk: "Фрагмент"
}

const TUTORIAL = {
    "af-ZA": "Tutoriaal",
    "sq-AL": "Udhëzues",
    ar: "تعليمي",
    ast: "Tutorial",
    be: "Падручнік",
    br: "Kelenn",
    bg: "Урок",
    ca: "Tutorial",
    "ca-ES-Valencia": "Tutorial",
    "zh-CN": "教程",
    da: "Vejledning",
    nl: "Tutorial",
    "en-AU": "Tutorial",
    "en-CA": "Tutorial",
    "en-NZ": "Tutorial",
    "en-ZA": "Tutorial",
    "en-GB": "Tutorial",
    "en-US": "Tutorial",
    eo: "Instruilo",
    fr: "Tutoriel",
    gl: "Tutorial",
    "de-DE": "Anleitung",
    "de-AU": "Anleitung",
    "de-CH": "Anleitung",
    el: "Οδηγός",
    he: "מדריך",
    is: "Kennsla",
    it: "Tutorial",
    ja: "チュートリアル",
    km: "ការបង្រៀន",
    lt: "Vadovėlis",
    ml: "പരിശീലനം",
    "nb-NO": "Veiledning",
    "nn-NO": "Rettleiing",
    fa: "آموزش",
    pl: "Samouczek",
    "pt-BR": "Tutorial",
    "pt-PT": "Tutorial",
    ro: "Tutorial",
    ru: "Учебник",
    tr: "Öğretici",
    "sr-SP-Cy": "Водич",
    "sr-SP-Lt": "Vodič",
    sk: "Návod",
    sl: "Vadnica",
    es: "Tutorial",
    sv: "Handledning",
    ta: "பயிற்சி",
    tl: "Tutorial",
    uk: "Посібник"
}

const EXERCISE = {
    "af-ZA": "Oefening",
    "sq-AL": "Ushtrim",
    ar: "تمرين",
    ast: "Exerciciu",
    be: "Практыкаванне",
    br: "Goulennadenn",
    bg: "Упражнение",
    ca: "Exercici",
    "ca-ES-Valencia": "Exercici",
    "zh-CN": "练习",
    da: "Øvelse",
    nl: "Oefening",
    "en-AU": "Exercise",
    "en-CA": "Exercise",
    "en-NZ": "Exercise",
    "en-ZA": "Exercise",
    "en-GB": "Exercise",
    "en-US": "Exercise",
    eo: "Ekzerco",
    fr: "Exercice",
    gl: "Exercicio",
    "de-DE": "Übung",
    "de-AU": "Übung",
    "de-CH": "Übung",
    el: "Άσκηση",
    he: "תרגיל",
    is: "Æfing",
    it: "Esercizio",
    ja: "演習",
    km: "លំហាត់",
    lt: "Pratimas",
    ml: "അഭ്യാസം",
    "nb-NO": "Øvelse",
    "nn-NO": "Øving",
    fa: "تمرین",
    pl: "Ćwiczenie",
    "pt-BR": "Exercício",
    "pt-PT": "Exercício",
    ro: "Exercițiu",
    ru: "Упражнение",
    tr: "Alıştırma",
    "sr-SP-Cy": "Вежба",
    "sr-SP-Lt": "Vežba",
    sk: "Cvičenie",
    sl: "Vaja",
    es: "Ejercicio",
    sv: "Övning",
    ta: "பயிற்சி",
    tl: "Ehersisyo",
    uk: "Вправа"
}

const EXERCISE_SOLUTION = {
    "af-ZA": "Oplossing",
    "sq-AL": "Zgjidhje",
    ar: "حل",
    ast: "Solución",
    be: "Рашэнне",
    br: "Diskoulm",
    bg: "Решение",
    ca: "Solució",
    "ca-ES-Valencia": "Solució",
    "zh-CN": "解答",
    da: "Løsning",
    nl: "Oplossing",
    "en-AU": "Solution",
    "en-CA": "Solution",
    "en-NZ": "Solution",
    "en-ZA": "Solution",
    "en-GB": "Solution",
    "en-US": "Solution",
    eo: "Solvo",
    fr: "Solution",
    gl: "Solución",
    "de-DE": "Lösung",
    "de-AU": "Lösung",
    "de-CH": "Lösung",
    el: "Λύση",
    he: "פתרון",
    is: "Lausn",
    it: "Soluzione",
    ja: "解答",
    km: "ដំណោះស្រាយ",
    lt: "Sprendimas",
    ml: "പരിഹാരം",
    "nb-NO": "Løsning",
    "nn-NO": "Løysing",
    fa: "راه حل",
    pl: "Rozwiązanie",
    "pt-BR": "Solução",
    "pt-PT": "Solução",
    ro: "Soluție",
    ru: "Решение",
    tr: "Çözüm",
    "sr-SP-Cy": "Решење",
    "sr-SP-Lt": "Rešenje",
    sk: "Riešenie",
    sl: "Rešitev",
    es: "Solución",
    sv: "Lösning",
    ta: "தீர்வு",
    tl: "Solusyon",
    uk: "Розв'язок"
}

const CATS = {
    figure: FIGURE,
    equation: EQUATION,
    table: TABLE,
    photo: PHOTO,
    listing: LISTING,
    example: EXAMPLE,
    snippet: SNIPPET,
    tutorial: TUTORIAL,
    exercise: EXERCISE,
    exercise_solution: EXERCISE_SOLUTION
}


}),
"./js/modules/schema/index.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  FW_DOCUMENT_VERSION: function() { return FW_DOCUMENT_VERSION; }
});
/** The current Fidus Writer document version.
 * The importer will not import from a higher version and the exporter
 * will include this number in all exports.
 */
const FW_DOCUMENT_VERSION = "3.5"


}),
"./js/modules/schema/mini_json.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  toFullJSON: function() { return toFullJSON; },
  toMiniJSON: function() { return toMiniJSON; }
});
/* import */ var fast_deep_equal__rspack_import_0 = __webpack_require__("./node_modules/.pnpm/fast-deep-equal@3.1.3/node_modules/fast-deep-equal/index.js");
/* import */ var fast_deep_equal__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(fast_deep_equal__rspack_import_0);


function toMiniJSON(node) {
    // Similar to the ProseMirror internal toJSON function,
    // but leaving out attributes that have default values and dealing with
    // attributes that are objects.
    // Adapted from https://github.com/ProseMirror/prosemirror-model/blob/6d970507cd0da48653d3b72f2731a71a144a364b/src/node.js#L340-L351
    const obj = {type: node.type.name}
    for (const attr in node.attrs) {
        if (!fast_deep_equal__rspack_import_0_default()(node.type.attrs[attr].default, node.attrs[attr])) {
            if (!obj.attrs) {
                obj.attrs = {}
            }
            obj.attrs[attr] = ["string", "number", "boolean"].includes(
                typeof node.attrs[attr]
            )
                ? node.attrs[attr]
                : JSON.parse(JSON.stringify(node.attrs[attr]))
        }
    }
    if (node.content.size) {
        obj.content = node.content.content.map(childNode =>
            toMiniJSON(childNode)
        )
    }

    if (node.marks.length) {
        obj.marks = node.marks.map(mark => toMiniMarkJSON(mark))
    }

    if (node.text) {
        obj.text = node.text
    }

    return obj
}

function toMiniMarkJSON(mark) {
    // Adapted from https://github.com/ProseMirror/prosemirror-model/blob/6d970507cd0da48653d3b72f2731a71a144a364b/src/mark.js#L76-L83
    const obj = {type: mark.type.name}
    for (const attr in mark.attrs) {
        if (!fast_deep_equal__rspack_import_0_default()(mark.type.attrs[attr].default, mark.attrs[attr])) {
            if (!obj.attrs) {
                obj.attrs = {}
            }
            obj.attrs[attr] = ["string", "number", "boolean"].includes(
                typeof mark.attrs[attr]
            )
                ? mark.attrs[attr]
                : JSON.parse(JSON.stringify(mark.attrs[attr]))
        }
    }
    return obj
}

function toFullJSON(miniJson, schema) {
    if (!miniJson.type) {
        miniJson.type = "doc"
    }
    return schema.nodeFromJSON(miniJson).toJSON()
}


}),
"./js/modules/schema/text.js": (function (__unused_rspack_module, __webpack_exports__, __webpack_require__) {
"use strict";
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  getNonDeletedTextContent: function() { return getNonDeletedTextContent; }
});
function getNonDeletedTextContent(topNode) {
    let text = ""
    topNode.descendants(node => {
        if (
            node.marks.find(mark => mark.type.name === "deletion") ||
            (node.attrs &&
                (node.attrs.track?.find(track => track.type === "deletion") ||
                    node.attrs.hidden))
        ) {
            return
        } else if (node.isBlock) {
            text += "\n"
        } else if (node.isText) {
            text += node.text
        }
    })
    return text
        .replace(/(^\s*)|(\s*$)/gi, "")
        .replace(/[ ]{2,}/gi, " ")
        .replace(/\n /, "\n")
        .replace(/\n{2,}/gi, "\n")
}


}),
"./js/plugins/citation_dialog/index.js": (function () {


}),
"./js/plugins/editor/index.js": (function () {


}),
"./js/plugins/schema_export/index.js": (function () {


}),

}]);